# Project Task Tracker

> 归档副本：`project-task.archived.2026-04-18.md`（本轮快照） · `project-task.archived.2026-04-11.md`（本轮快照） · `project-task.archived.2026-04-01.md`（更早）

## Current Task List

以下表格仅保留进行中任务；已完成任务统一沉淀到下方 `Recent Completed` 或归档文件。

| ID | Task | Status | Started On | Notes |
| --- | --- | --- | --- | --- |
| 2026-06-29-windows-sdl3-fse-xinput-direct-poller | 修复 Windows FSE 冷启动 SDL3 枚举为空时手柄无输入 | Done | 2026-06-29 | 简单任务。基于 [`reports/2026-06-29-windows-sdl3-fse-gamepad-input-audit.md`](reports/2026-06-29-windows-sdl3-fse-gamepad-input-audit.md) 落地修复：SDL3 source 增加 Windows-only XInput direct poller，跟随 8ms snapshot tick 扫描 XInput user 0..3；`XInputGetState` 成功时合成稳定设备 `xinput:user:N` 并发送 `Connected + Snapshot`，trace 标记 `sampleSource=xinputDirect`；SDL opened-device 带同一 `player_index=N` 后 direct poller 断开 `xinput:user:N`，保留 SDL3 主线权威；opened-device fallback 继续标记 `sampleSource=xinputFallback`，fallback miss 继续记录 `player-index-missing/player-index-out-of-range/xinput-get-state-failed`。已更新 FSE 验证指南。验证：`cargo fmt -p ohmygamepad-sdl3 --check`、`cargo check -p ohmygamepad-sdl3`、`cargo test -p ohmygamepad-sdl3 --lib -- --nocapture`。待 Windows FSE fresh trace 验收：SDL 枚举为空时出现 `deviceId=xinput:user:0`、`sampleSource=xinputDirect`、`lastBackendSampleActivityAtMs > 0`、`lastSampleProgressAtMs` 随输入推进。 |
| 2026-06-29-windows-sdl3-fse-gamepad-input-audit | 排查 SDL3 手柄在 Windows FSE 模式下的输入问题 | Done | 2026-06-29 | 简单任务。Report：[`reports/2026-06-29-windows-sdl3-fse-gamepad-input-audit.md`](reports/2026-06-29-windows-sdl3-fse-gamepad-input-audit.md)。已完成官方资料、历史提交、当前实现与 trace 缺口排查；结论已由 `2026-06-29-windows-sdl3-fse-xinput-direct-poller` 落地修复。 |
| 2026-06-26-runtime-frame-rtp-rewind-audit | 修复 runtime/input/native pending 的 frameSeq 回绕漏判 | In Progress | 2026-06-26 | 简单任务。复盘最新 trace `runtime-trace-1782453170367-1.jsonl`：receive gate PASS，`cleanAnchorCommitted=3`，但组合 gate FAIL；midsegment `submit_age_ms P95=4393ms`、`present_age_ms P95=4392ms`，`localBackpressureBestEffortOverflow` 连续 613，frame delta 为 `accessUnits/decodeGateEmit=223` 但 `pacerSubmit/rendererSubmit/hostEnqueue/hostPresent=29`，说明网络与恢复链已能产出帧，停滞仍在 post-decode/display 供给。继续自查发现 runtime health、runtime sync、input metadata 仍只按 `frameSeq` 单调推进，native pending 覆盖也先比较 frameSeq 再比较 RTP；恢复后新 RTP 时间线低 frameSeq 会被吞掉或拒绝覆盖旧 pending；`renderer actor` 接受帧后丢弃 `present_frame()` 返回的 `frame_stats`，导致 runtime/input 仍可能读旧 `latest_video_frame`；native presenter 在进入 `ScheduledFrameSlot` 前还有一层 `submittedFrameStale` 预筛，使用通用 `frame_age_budget_ms()`，会绕过 host mailbox 的 recovery/starved 扩展预算；placeholder backend 与 runtime test fixture 的 `update_host_video_present_metrics()` 漏同步 submit RTP、last displayed seq/RTP/at、view generation、no-pending 事实，导致 runtime 回归测试的 host 事实不完整；native presenter 把提交尝试提前计入 `present_enqueue_count_total`，拒收和 stale 虽回滚 `latest_submit_time_ms`，仍会污染 `recent_submit_times_ms`，让 runtime 把失败尝试误读成 host enqueue 与 submit cadence；runtime host visibility stall 又把历史 `host_mailbox_enqueue_count_total > host_frame_present_epoch` 当成当前 pending 证据，latest-only overwrite 合并后会留下永久计数差，并在后续 present age 变大时误判 host stalled。修复：`XbxEngineRuntimeHealth` 增加上一帧 RTP，`record_video_frame` / sync 推进改为 frameSeq 前进或当前帧 RTP 32 位环绕前进；`XbxEngineVideoFrameStats` 增加 `rtp_timestamp`，由 render frame 写入当前帧 RTP，runtime/metadata 不再从全局 host/decode stats 推断当前帧 RTP；renderer 接受帧后写回 `stats.latest_video_frame`，确保 runtime sync 与 input metadata 读取当前帧 seq/RTP；metadata frame 去重同步记录当前帧 RTP，允许 frameSeq 回绕但 RTP 前进时继续发送；native pending 比较改为 RTP 优先、frameSeq 兜底；native presenter 的前置 stale gate 改为复用 `host_mailbox_submit_stale_budget_ms(frame)`，避免 Windows D3D11、WGPU 与 macOS layer 路径提前丢掉 recovery/host-stall 窗口里的可用帧；placeholder backend 与 runtime test fixture 补齐 host present metrics 字段同步；`ScheduledFrameSlot::submit_frame` 只在 `Accepted` 分支推进 `record_enqueue()`，Windows D3D11/WGPU 与 macOS WGPU/layer presenter 只计算候选 `submitGapMs`，stale/rejected/updateFailed 均不推进 `host_mailbox_enqueue_count_total` 和 accepted-submit cadence；runtime host visibility stall 去掉历史 enqueue/present 总数差作为单独触发条件，改由当前 submit RTP 未显示、submit-present gap 或 view pending 证明卡住；继续收紧 packet-only 误推进，`latest_video_packet_arrival_rtp_timestamp` 只代表网络最新包，不能证明 `latest_video_frame` 已换帧；接受 frameSeq 前进但无 RTP 的帧时清空 RTP 基线，避免旧 RTP 跨帧污染后续低 frameSeq 判断。验证：`cargo fmt`、`cargo test -p xbxengine placeholder_backend_copies_complete_host_present_metrics --lib -- --nocapture`、`cargo test -p xbxengine media::video::render::actor --lib -- --nocapture`、`cargo test -p xbxengine runtime_sync_ --lib -- --nocapture`、`cargo test -p xbxengine metadata_frame_ --lib -- --nocapture`、`cargo test -p xbxengine session::recovery --lib -- --nocapture`、`cargo test -p xbxengine compare_latest_only --lib -- --nocapture`、`cargo test -p xbxengine reconnect_settled --lib -- --nocapture`、`cargo test -p xbxengine runtime_display_supply_critical --lib -- --nocapture`、`cargo test -p xbxengine runtime_local_host_stall_reset --lib -- --nocapture`、`cargo test -p xbxrc --lib native_video::scheduling -- --nocapture`、`cargo test -p xbxrc --lib native_video -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。本轮新增验证：`cargo fmt`、`cargo test -p xbxrc --lib native_video -- --nocapture`、`cargo test -p xbxengine host_visibility_stall --lib -- --nocapture`、`cargo check -p xbxrc`、`cargo check -p xbxengine`、`git diff --check`。待 fresh trace 复验：clean anchor 后 runtime rendered 事件、metadata、host pending/accepted/present 均按新 RTP 时间线持续推进；同一 `latest_video_frame` 不被 packet RTP 重复记为新帧；`hostMailboxRejected outcome=already_presented` 与 `submittedFrameStale` 不形成长段；`host_mailbox_enqueue_count_total` 只随 `hostMailboxAccepted` 推进，runtime host stall 只由当前 pending/view 事实触发；`localBackpressureBestEffortOverflow` 不形成长段；midsegment latency gate 收敛。 |
| 2026-06-26-native-host-seq-rewind-stall | 修复 native host frameSeq 回绕被误判已呈现 | Done | 2026-06-26 | 简单任务。最新 trace `runtime-trace-1782453170367-1.jsonl` 显示 receive gate PASS、网络 TWCC 稳定且 decode 已产出新 clean anchor，但 host 仍停在旧 displayed `frameSeq=350/rtp=1746264948`，随后 `frameSeq=7..20`、`rtp=2856576214+` 的新帧连续被 `hostMailboxRejected outcome=already_presented` 拒绝，直到下一个 keyframe `frameSeq=22` 才清旧 displayed 并恢复，造成出图停滞与后续 ingress overflow。修复：`ScheduledFrameSlot` 在旧 displayed 已 stale/no-pending 且 incoming RTP 按 32 位环绕语义前进、frameSeq 回绕、incoming 未过期时，将其识别为新 source timeline 并先 `begin_media_epoch()`，避免旧 displayed seq 拦截 post-recovery continuation；pending RTP 新旧比较同步改为 wrap-aware；共享 `compare_latest_only_frame_meta()` 的 frame RTP 与 recovery owner RTP 比较也改为 wrap-aware，覆盖 decode mailbox 与 pacer render queue；native host accepted/rejected trace 补 `framePresentationValueRole`，便于下次确认角色传播。验证：`cargo fmt`、`cargo test -p xbxengine compare_latest_only --lib -- --nocapture`、`cargo test -p xbxengine media::video::decode --lib -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor --lib -- --nocapture`、`cargo test -p xbxrc --lib native_video::scheduling -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：卡死窗口不再出现新 RTP 时间线下连续 `hostMailboxRejected outcome=already_presented`；decode/pacer latest-only 不在 RTP wrap 附近保留旧帧；`hostMailboxAccepted/hostFramePresent` 在 clean anchor 后持续推进；`localBackpressureBestEffortOverflow` 不形成长段。 |
| 2026-06-26-post-decode-reconnect-logic-audit | 自查 post-decode 与 reconnect-settled 同类断供逻辑 | Done | 2026-06-26 | 简单任务。基于 `runtime-trace-1782453170367-1.jsonl` 继续审查最新修复的相邻链路，发现四处同类低级问题：1）decode 空邮箱放行 late continuation 后，pacer 仍可能在 host 已 `starved/noPending` 且 render 队列空时按 deadline 丢掉所有 late continuation，把断点从 decode 后移到 pacer；2）`request_keyframe_after_reconnect_settled` 只在 reconnect 成功瞬间尝试一次，transport/feedback target 尚未 ready 时只写 `keyframeDeferred:*`，后续 tick 没有重试入口；3）`DecodeIngressAdapter` 在 host stall throttle 前先 `drain_expired_for_decode`，并且纯 delta 队列会被 `discard_non_keyframe_prefix_for_host_stall` 清空，导致 decode/pacer 的保供给策略拿不到 continuation；4）`RtcVideoSourceSink` 在 ingress loop 消费首包后才更新 `FrameBoundaryTracker`，队列背压下 IDR/FU-A 首包尚未被消费时，同 RTP timestamp 的后续分片会被当成 best-effort 并被本地背压丢弃，导致关键帧请求看似成功但组不出可用 IDR。修复：pacer 在 host starved/no-pending 且 render 队列空时释放一帧 late non-keyframe continuation，steady host 仍按 deadline 丢；runtime 对 `reconnectSettled:keyframeDeferred:*` 增加退避后的 tick 重试，transport Connected 且 feedback target 恢复后重新请求 PLI，并写回 `last_recovery_action=requestPli`；ingress host-stall throttle 有 keyframe 时继续 reveal keyframe，无 keyframe 时保留最新 delta continuation，每次 drain 只交一个 delta；source sink 将 `try_send` 结果拆成 `Sent/Backpressure/Dropped`，只在包实际进入 sender channel 或本地 backlog 后更新 `FrameBoundaryTracker`，让已接收的关键帧后续分片在下游消费前继承 priority，同时避免被本地丢弃的 IDR 首包污染后续 continuation 分类。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::rtx_sink --lib -- --nocapture`、`cargo test -p xbxengine media::video::decode --lib -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor --lib -- --nocapture`、`cargo test -p xbxengine media::video::ingress::scheduler --lib -- --nocapture`、`cargo test -p xbxengine reconnect_settled --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-26-decode-output-stale-empty-mailbox | 修复 decode 后 stale 判定导致出图供给停滞 | Done | 2026-06-26 | 简单任务。最新 trace `runtime-trace-1782453170367-1.jsonl` 显示 receive gate PASS 但 combined gate FAIL；+48.77s 后 `decodeOutputPathObserved` 停在 obs=351，`latest_decode_candidate_decision=drop/staleAfterDecode`，`pacerSubmit/renderSubmit/hostEnqueue` 停在 294/294/293，packet 继续增长到 18139；+121s 重连恢复后 +124.37s 同样在 obs=86 后进入 `staleAfterDecode`，最终 `hostPresentStalled/noPending`。根因：decode output 空邮箱时仍按 20-30ms playout deadline 后的小 stale 窗口直接丢刚解出的 continuation，gap repair 或 host starved 下 late delta 无法进入 pacer。修复：空 decoded output mailbox 保留刚解出的非 keyframe continuation，让 pacer 统一做最终 deadline / latest-only 处理；stale keyframe/clean anchor 与邮箱已有候选时继续丢弃，避免旧锚点污染 recovery ledger。验证：`cargo fmt`、`cargo test -p xbxengine media::video::decode --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-26-remote-terminal-reconnect-fallback | 修复 receive remote-no-response 后出图停滞未升级重连 | Done | 2026-06-26 | 简单任务。最新 trace `runtime-trace-1782450537650-1.jsonl` 显示 +145s 后 RTP/packet 仍增长，但 `decodeGateEmit=196`、`pacerSubmit=164`、`hostMailboxEnqueue=162` 停住；`receivePictureRecoveryTerminal remote-no-response` 持续出现，`receive_keyframe_required=true`、`response_state=no-packet`、`unresolved` 从 19 涨到 271，runtime/session 未发新 reconnect。根因：runtime remote terminal fallback 只认 runtime-local `keyframe_requested_for_current_stall`，该标记会被持续入站包增长清掉；session ledger 又停在旧 `delegatedToReceive`。修复：`should_reconnect_for_remote_no_usable_idr_terminal` 增加 receive-side pressure 分支，remote terminal active 且 unresolved keyframe 达阈值时，在 decode/present 陈旧后直接触发 `MediaStalled` reconnect；新增 rust-owned 回归覆盖“包继续到达但 receive 已 remote-no-response”的卡死形态。验证：`cargo fmt`、`cargo test -p xbxengine reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxengine remote_terminal --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-26-scheduler-goal-regression-audit | 深度回归调度目标是否保持 WebRTC 参考与低延迟共存 | Done | 2026-06-26 | 简单任务。Report：[`reports/2026-06-26-scheduler-goal-regression-audit.md`](reports/2026-06-26-scheduler-goal-regression-audit.md)。结论：当前调度主线仍对齐“receive/WebRTC 事实主权 + 低延迟 latest-only 显示执行”。代码审计覆盖 reconnect、ingress 背压、decode mailbox、pacer、native scheduling：session/runtime reconnect 由 domain gate 与 connectedHealthyNoProgress gate 兜底；displaySupply/hostPresentStalled 只触发本地 presenter 自愈且受 no-pending、presentLoop、fresh post-decode evidence guard 约束；video ingress 固定 64 包主通道，best-effort 48 包软水位，priority/repair/best-effort backlog 保新丢旧；decode 输出 mailbox 为 current+latest，显示压力短突发四槽且不反向阻塞 ingress；pacer 保持 current+latest、render queue 1/2，Ready 提交、Drop 丢弃、Hold 等待；native scheduling 保持 pending latest 和 media epoch 清旧 displayed。验证：`cargo test -p xbxengine reconnect_lifecycle --lib -- --nocapture`、`cargo test -p xbxengine reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::rtx_sink --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive --lib -- --nocapture`、`cargo test -p xbxengine media::video::decode --lib -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor --lib -- --nocapture`、`cargo test -p xbxrc --lib native_video::scheduling -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。fresh trace gate 当前只命中旧样本 `runtime-trace-1782364737067-1.jsonl`，`traceFreshness=FAIL ageSeconds=82481`，旧样本仍有 `midsegment.globalLatencyGate=FAIL` 与 `localBackpressureBestEffortOverflow streak=466`；需要当前构建新 trace 证明实机验收。 |
| 2026-06-26-continuous-reference-insert-gate-regression | 回归并修正连续参考链下 InsertGate 过度 hold | Done | 2026-06-26 | 简单任务。代码回归和旧 trace `runtime-trace-1782364737067-1.jsonl` 显示总体方向仍保持 receive/WebRTC 事实主权与 post-decode latest-only，但 `continuous + decoder_reference_synced + active_gap` 会被 `normalize_action_stage_for_reference()` 归一到 `NackMissed`，并触发 `activeRepairHold`；旧 trace 中该形态 `holdRepair/activeRepairHold/nack_missed/continuous=150`、`nack_pending/continuous=13`，同时 midsegment `submit_age_ms P95=3104ms`、recovery pulses=86、`localBackpressureBestEffortOverflow` 连续 466。修复：`Continuous` 且 decoder reference synced 时将 `NackPending/NackMissed/WaitKeyframe/RequestIdr` 归一为 `Steady`，InsertGate 的 `activeRepairHold` 只绑定 `ReferenceChainState::Repairing`，保留真实 repair 窗口保护；PS/config strict 移除 `receiveDisplayStable` 前置，回到 packet/reference/decoder evidence；新增/更新回归测试覆盖 continuous emit、repairing hold 与 post-PS strict display 解耦。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::insert_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo check -p xbxengine`、`git diff --check -- crates/xbxengine/core/src/transport/rtc/recovery/contract/insert_control.rs crates/xbxengine/core/src/transport/rtc/receive/insert_gate.rs`。待 fresh trace 复验：`activeRepairHold` 在 `referenceState=continuous` 下归零或显著下降，`insertGateDecision emit/decodableToFeed` 增加，keyframe coalesced by insert-gate-supply-break 下降，host enqueue/present 连续性恢复，ingress overflow streak 与 midsegment latency gate 收敛。 |
| 2026-06-25-host-stall-no-pending-reset-guard | 阻断 hostPresentStalled 无 pending 时自激 reset | Done | 2026-06-25 | 简单任务。新 trace `runtime-trace-1782364737067-1.jsonl`：receive gate PASS，fresh IDR/clean anchor/display stable 链路已改善，但 midsegment 仍 FAIL；卡死前 4 次 `presenterResetForHostStall` 均发生在 `hostMailboxRetainedDisplayedStale` 且 `hasPendingFrame=false / pendingQueueDepth=0` 的窗口，最后一次 noPendingStreak=1395，reset 清空 mailbox 后进入长期 priming/noPending，随后 ingress overflow 连续 466 次；同类审查发现 `displaySupplyCritical/Degraded` 也有更重的 presenter/effect pipeline reset。修复：`host_no_pending_streak > 0` 时跳过 host stall 与 display recovery reset，分别记录 `hostPresentStalled:noPendingHostMailbox` / `displayRecoverySkipped:noPendingHostMailbox`，避免把上游暂无 pending 的显示断供扩大成 mailbox 清空或 presenter 重建；新增回归覆盖 no-pending retained displayed stale 与 fresh post-decode evidence 同时存在的 display critical。验证：`cargo fmt`、`cargo test -p xbxengine runtime_local_host_stall_reset_skips_when_mailbox_has_no_pending_frame --lib -- --nocapture`、`cargo test -p xbxengine runtime_local_host_stall_reset_still_runs_when_renderer_submit_is_fresh --lib -- --nocapture`、`cargo test -p xbxengine runtime_local_host_stall_reset --lib -- --nocapture`、`cargo test -p xbxengine runtime_display_supply_critical_skips_local_reset_when_mailbox_has_no_pending_frame --lib -- --nocapture`、`cargo test -p xbxengine runtime_display_supply_critical --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`。待 fresh trace 复验：卡死窗口不再出现 no-pending retained displayed stale 触发的 `presenterResetForHostStall` / `presenterResetForDisplayRecovery`，`hostMailboxEnqueue/hostFramePresent` 能在下一帧到达后继续推进，`localBackpressureBestEffortOverflow` 不形成长段。 |
| 2026-06-25-fresh-idr-owner-fallback-audit | 审查并修复 fresh IDR 在 episode 缺失时的 owner 丢失 | In Progress | 2026-06-25 | 简单任务。新 trace 显示 receive 侧在 `need-keyframe/non-idr-only` 后收到可用 IDR 并通过 `insertGateDecision freshIdr emit`，但 episode 投影缺失时 `recovery_owner_rtp_timestamp` 仍会丢失，后续 decode/pacer/render 只能把该帧当成普通 fresh frame，卡死窗口继续重绘旧 displayed frame。修复：`ingress_state/decode.rs` 在 `insert_ctx.keyframe_required` 且 current frame 具备 sustaining exit 时，允许回退到 receive ledger 的 `last_usable_keyframe_rtp` 作为 recovery owner；新增两条回归测试覆盖 recovery required / steady 两种 fresh IDR 形态。验证：`cargo fmt`、`cargo test -p xbxengine recovery_required_fresh_idr_without_episode_becomes_recovery_owner --lib -- --nocapture`、`cargo test -p xbxengine steady_fresh_idr_without_recovery_required_does_not_become_recovery_owner --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive --lib -- --nocapture`、`cargo test -p xbxengine media::video::decode --lib -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor --lib -- --nocapture`、`cargo test -p xbxengine media::video::render::renderer --lib -- --nocapture`、`cargo test -p xbxrc --lib native_video::scheduling -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-25-native-present-hotpath-lock-audit | 自查并收紧 native present 热路径同步锁 | Done | 2026-06-25 | 简单任务。基于 `runtime-trace-1782355055120-1.jsonl` 卡死根因继续审计同类风险，发现 renderer loop 在 host push 前仍会阻塞等待 `host_render_frame_push` 容器锁，且 native presenter `present()` 内部还会同步等待 `renderer_state/telemetry/frame_slot/layer_state`；这些等待点都位于 renderer 消费链路，存在同类停摆风险。修复：renderer actor 改为 `try_lock` 获取 push handle，并 clone `Arc<dyn XbxHostRenderFramePush>` 后释放容器锁再执行宿主 push；push handle 忙时记录 `hostPresentPushHandleBusy` 并丢弃当前 host push。macOS/Windows WGPU、macOS layer、Windows D3D11 presenter 的 host submit 热路径改为非阻塞 `try_lock`，render loop ensure 失败或 mailbox 内部锁忙时直接返回 rejected 并记录 `hostMailboxUpdateFailed`，避免等待显示线程或主线程相关状态。验证：`cargo fmt`、`cargo test -p xbxengine media::video::render::actor --lib -- --nocapture`、`cargo test -p xbxrc --lib native_video -- --nocapture`、`cargo test -p xbxrc runtime_state --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：`rendererSubmit/pacerSubmit` 不因 host push 或 presenter 内部锁停住，短时锁竞争体现为 `rendererPushDropped:pushHandleBusy` 或 `hostMailboxUpdateFailed`，ingress overflow 不形成长段。 |
| 2026-06-25-native-host-push-nonblocking | 修复 native host push 锁等待导致播放卡死 | Done | 2026-06-25 | 简单任务。新 trace `runtime-trace-1782355055120-1.jsonl`：网络与 receive feedback 健康，`receiveFeedbackGate=PASS`，卡死点在本地显示供给链路；进入 steady 后 `rendererSubmit` 停在 132，`pacerSubmit` 停在 135，`hostMailboxEnqueue` 后续归零，随后 host 持续 `noPendingFrame`，ingress sender 队列满到 64 并出现 `localBackpressureBestEffortOverflow=781`。根因收敛为 renderer 线程同步 host push 时等待 `host_present_route/native_video` 锁，导致 renderer 停止消费并反向压满 ingress。修复：`NativeVideoHostRenderFramePush` 改为 `try_lock` 快照 route，复制 `viewport_id/surface_id` 后立即释放；`native_video` registry 忙时直接返回 `RegistryUnavailable` 并记录 `nativeHostPushDropped reason=registryBusy`，route 忙时记录 `reason=routeBusy`，保证 renderer 消费链路继续前进。验证：`cargo fmt`、`cargo test -p xbxrc runtime_state --lib -- --nocapture`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：卡死窗口中 `rendererSubmit/pacerSubmit` 持续增长，`nativeHostPushDropped` 可解释短时丢 host push，`hostMailboxEnqueue` 不再永久归零，`localBackpressureBestEffortOverflow` 不形成长段。 |
| 2026-06-24-long-playback-ingress-depth-clamp | 钳制长播放 ingress sender 深队列并补分项 trace | Done | 2026-06-24 | 简单任务。最新 trace `runtime-trace-1782290280000-1.jsonl`：receive gate PASS、lifecycle reconnect gate PASS，健康窗口后 `runtimeLocalReconnectConsumed=0`、`rebuildPeerConnectionClosureCount=0`，midsegment 仍 FAIL；+113.478s 后 TWCC `delivery=1.0/loss=0.0`，但 `submit_age P95≈2265ms`、`decode_fps≈9.7`、`present_fps≈7.3`、`localBackpressureBestEffortOverflow=795`、`queueDepth=315/316`，根因继续收敛为本地 media ingress 背压与 host 供给断续。修复：`RtcVideoSourceSink` 即使收到大于 64 的 sender channel，也用 `MAX_EFFECTIVE_SENDER_QUEUE_DEPTH=64` 强制进入本地背压，避免主 sender 形成 256+ 级深队列；`frameDropped` / pacer-render 决策 trace 新增 `ingressQueueDepthBreakdown`，包含 `senderQueueDepth/senderMaxCapacity/senderQueueLimit/senderRemainingCapacity` 与 priority/repair/best-effort 队列长度和上限。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::rtx_sink --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`pnpm lint:fix`、`PYTHONPYCACHEPREFIX=/private/tmp/codex_pycache python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`、`python3 .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py runtime-logs/runtime-trace-1782290280000-1.jsonl --require-lifecycle-reconnect-gate --require-ingress-queue-gate`（旧 trace 预期 FAIL，证明 `missing ingressQueueDepthBreakdown` 与 `localBackpressureBestEffortOverflow streak=753` 会被 gate 捕获）、`git diff --check`。补第 5 点：`trace_webrtc_acceptance_gate.py` 新增 `--require-ingress-queue-gate`，检查 breakdown 存在性、`senderQueueLimit <= 64`、`senderQueueDepth <= 64`、`queueDepth <= 96` 与 best-effort overflow 连续段。待 fresh trace 复验：`queueDepth` 不再接近 8192/160/315，`ingressQueueDepthBreakdown.senderQueueDepth <= 64` 且 `senderQueueLimit=64`，`localBackpressureBestEffortOverflow` 不形成长段，`present_fps` 不掉到个位数，`submitAgeMs/presentAgeMs P95 < 200ms`。 |
| 2026-06-24-runtime-trace-profile-budget | 统一控制台日志、runtime trace profile/预算/维度与分析 skill | Done | 2026-06-24 | 复杂任务。RFC：[`rfcs/2026-06-24-runtime-trace-profile-budget.md`](rfcs/2026-06-24-runtime-trace-profile-budget.md)；Report：[`reports/2026-06-24-runtime-trace-profile-budget.md`](reports/2026-06-24-runtime-trace-profile-budget.md)。已实现 Rust trace schema v3 envelope：保留 `traceMode`，新增 `traceProfile/dimension/importance`；`runtime_trace_mode` 兼容旧值并收敛为 `off/production/dev`，debug 默认 `dev`，release 默认 `production`，release 中 `dev` 降级为 `production`；生产固定关键维度和 16MB x 5 文件预算，dev 64MB x 10，支持 `XBX_TRACE_DIMENSIONS` / hidden `runtime_trace_dimensions`；`RuntimeTraceRecorder` 已接入 metadata 入口、生产过滤、writer 队列降载、文件滚动、traceBudgetNotice；配置、启动、live apply、TS RPC 类型和设置文案已同步；`analyze-runtime-logs` skill/schema/playbook/summarize 脚本已同步 v3 profile、dimension、importance、budget notice 读法，并补 schema v3 黑盒测试；前端既有 lint 三处阻塞已收口。验证：`cargo fmt`、`cargo test -p xbxrc runtime_trace --lib`、`cargo test -p xbxrc config --lib`、`cargo test -p xbxengine diagnostics --lib`、`cargo check -p xbxrc`、`PYTHONPYCACHEPREFIX=/private/tmp/codex_pycache python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`、`python3 /Users/guo.xu/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/analyze-runtime-logs`、`pnpm lint:fix`、`./node_modules/.bin/vitest run src/player/infra/render/Renderers.test.ts src/shared/gamepad/wait-pad-neutral.test.ts src/streaming/diagnostics.test.ts`、`git diff --check`、`git diff --cached --check`。 |
| 2026-06-24-keepalive-400-after-stream-stop | 修复串流终止/重连窗口 keepalive 400 误报 | Done | 2026-06-24 | 简单任务。最新 trace `runtime-trace-1782265040881-1.jsonl` 显示 seq=1754 发起 keepalive，seq=1761 报 `HTTP 400`，响应体为 `{"code":"SessionInUnexpectedState","statusCode":400,"message":"KeepAlive : Failed"}`；现有 keepalive 忽略策略只覆盖 `SessionUnexpectedState`，漏掉服务端在终止/重连窗口返回的 `SessionInUnexpectedState`。修复：`should_ignore_keepalive_error` 将 `SessionInUnexpectedState + KeepAlive` 纳入已知状态冲突吸收，未知 400 继续上报；复查发现 Tauri `KeepAliveRemoteSession` adapter 丢弃 `send_keepalive` 的 `accepted=false` 并固定投成成功，已改为按真实 accepted 写 trace，false 时返回 `keepAliveRemoteSession:streaming:SessionNotActive`，让 xbxengine 现有 terminal session inactive 分支停止后续重协商。验证：`cargo fmt`、`cargo test -p xbox-streaming keepalive_error_policy_matches_legacy_behavior --lib -- --nocapture`、`cargo test -p xbxengine keepalive_not_accepted_error_is_terminal_session_inactive --lib -- --nocapture`、`cargo test -p xbxrc stop_runtime_control_trace_events_are_named --lib -- --nocapture`、`cargo check -p xbxrc`、`cargo check -p xbox-streaming`。 |
| 2026-06-24-runtime-pending-reconnect-healthy-gate | 阻断健康播放后 pending reconnect 误消费 | In Progress | 2026-06-24 | 简单任务。最新 trace `runtime-trace-1782278607293-1.jsonl` 显示中段 `GLOBAL_LATENCY_GATE=PASS / MEDIA_SUPPLY_GATE=PASS / STEADY_SUPPLY_GATE=PASS`，`decode_fps avg=58.2 / present_fps avg=54.6`；失败集中在健康窗口后 `receiverWaitingKeyframe` / `livenessNoProgressTimeout` 被 runtime pending action 消费成 `connectivity-transport` reconnect，触发 5 次 `rx closed cause=rebuildPeerConnection`。根因：session policy 已把多数 `waitKeyframe` 降级为 `delegatedToReceive`，runtime `take_pending_runtime_recovery_action()` 出口只按 domain 放行，缺少连接健康 + 媒体可服务兜底。坏帧冻结补充分析：native `hostFramePresented` 在 `presentationKind=refresh` 时持续重绘同一 displayed frame，`frameAgeMs` 出现 2s-12s 长尾；`record_present_refresh()` 推进 `presentAgeMs`，导致 `hostMailboxState.displayedFrameStale=false / retainedOldFrameRisk=false`。局部坏帧补充分析：画面仍更新但局部坏块持续，trace 显示 `nackSent=59 / recovered=27 / effective_rate=0.4576`，`mediaSupplyPhaseDiagnostic=repairing` 下大量 `insertGateDecision reason=decodableToFeed`，同时 `referenceState=continuous / receiveDisplayState=display-stable`，说明 active repair 窗口仍把 non-IDR continuation 喂给 decoder，参考污染可以继续传播。快放补充分析：冻结后恢复窗口出现旧 native epoch 混入新 source epoch，例：86.361s `lastDisplayedFrameSeq=1794 / hostFramePresentEpoch=1707 / submitAgeMs=8922ms`，86.681s 新帧 `frameSeq=2` 进入时 `displayedFrameSeq` 仍是 1794，随后 86.875s-87.903s `lastDisplayedFrameSeq 8->78`，暴露出违背低延迟优先的顺序追赶路径。修复：runtime 消费 pending reconnect 前增加健康兜底，`Connected + fresh TWCC/ICE + fresh decode/present/display-stable/reference-chain` 时把 `receiverWaitingKeyframe/livenessNoProgressTimeout` 记为 `reconnectBlocked:lifecycleGate:connectedHealthyNoProgress` 并清掉 pending；新增 `runtimeReconnectBlocked` trace 投影与回归；native video 增加 `RetainedDisplayedFrameStale`，displayed source frame 超过 300ms 后停止 refresh present freshness，进入 starved/no-pending 路径，并输出 `hostMailboxRetainedDisplayedStale`；`ScheduledFrameSlot::begin_media_epoch()` 清旧 displayed/pending/last_presented，同时保留 render loop，避免新 source epoch 接管时混用旧 displayed frame；InsertGate 在 `NackPending/NackMissed` active repair 窗口对非 IDR/非 bootstrap AU 返回 `activeRepairHold`，暂停 continuation，等待修复包或新 IDR，steady 后恢复 decoder-synced continuation；Pacer 去掉连续追赶释放语义，过期帧直接 drop，未到 deadline 的帧 hold 到节拍，恢复后只保留 latest-only 候选。验证：`cargo fmt`、`cargo test -p xbxengine runtime_blocks_receiver_waiting_reconnect_candidate_when_connected_media_is_serviceable --lib -- --nocapture`、`cargo test -p xbxengine runtime_consumes_pending_transport_reconnect_candidate_even_when_transport_is_connected --lib -- --nocapture`、`cargo test -p xbxengine reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxengine reconnect_lifecycle --lib -- --nocapture`、`cargo test -p xbxrc --lib native_video -- --nocapture`、`cargo test -p xbxrc trace_projection --lib -- --nocapture`、`cargo test -p xbxrc runtime_reconnect_blocked_projects_runtime_trace_event --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::insert_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::ingress_loop --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive --lib -- --nocapture`、`cargo test -p xbxengine media::video::render::pacer --lib -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor --lib -- --nocapture`、`cargo test -p xbxengine media::video::present_cadence --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：健康播放后 `runtimeLocalReconnectConsumed=0`、`rebuildPeerConnectionClosureCount=0`，出现 `runtimeReconnectBlocked`，秒级/十秒级 `hostFramePresented presentationKind=refresh frameAgeMs` 消失或转为 `hostMailboxRetainedDisplayedStale`，新 epoch 首帧 accepted 时 `displayedFrameSeq` 为空或同 epoch，repairing/NACK 窗口出现 `insertGateDecision reason=activeRepairHold`，`needKeyframeNonIdrFeedViolations=0`，恢复后无顺序快放追赶，局部坏块明显减少或消失。 |
| 2026-06-24-long-playback-active-gap-hold | 修复长播放 active gap 下 continuation 继续入解码导致局部坏块与尾段卡顿 | In Progress | 2026-06-24 | 简单任务。fresh trace `runtime-trace-1782282736016-1.jsonl` 时长 332s：midsegment 低延迟门禁 PASS，`runtimeLocalReconnectConsumed=0`、`rebuildPeerConnectionClosureCount=0`，且 raw trace 已出现 `runtimeReconnectBlocked blockReason=lifecycleGate:connectedHealthyNoProgress`；剩余问题集中在 receive repair 窗口，`NACK sent=115 / recovered=61 / effective_rate=0.5304`，`insertGateDecision` 里 `repairing + steady + emit + decodableToFeed=208`，`activeRepairHold=0`，说明 `referenceState=continuous` 与 active gap 同时存在时 stage 仍停在 `steady`，non-IDR continuation 继续进入 decoder。尾段 +300s~+330s 复查：网络 TWCC `lossRatio=0 / deliveryRatio=1.0`，+310s 起连续 `decode:drop:supersededAfterDecode`，+312s `present_fps≈5.9`，+328s `present_fps≈2.9`、`submitAgeMs=205~665ms`，+322s 后 `localBackpressureBestEffortOverflow` 连续出现，`queueDepth=8199`，根因为本地视频 RTP ingress 通道容量 8192 允许积压，背压时主流 continuation 与 repair 包排队过深，host 进入 retained displayed stale。新 trace `runtime-trace-1782288626404-1.jsonl` 显示 reconnect storm 已消失，健康窗口后 `runtimeLocalReconnectConsumed=0`、`rebuildPeerConnectionClosureCount=0`，剩余抖动集中在 1440p 本地 ingress 背压：`localBackpressureBestEffortOverflow=391`、`queueDepth≈160`、`NACK sent=195 / recovered=15 / effective_rate=0.0769`，说明 128 包主通道加本地 backlog 仍能形成队头延迟并压低 repair 效率。修复：`normalize_action_stage_for_reference()` 在 decoder reference synced 且 `has_active_gap=true` 时把 `steady/request_idr/wait_keyframe` 归一成 `NackMissed`，保留已有 `NackPending`，让 InsertGate 的 `activeRepairHold` 在 active repair 窗口生效；新增 `synced_active_gap_normalizes_steady_stage_to_active_repair_hold` 回归；`build_rtc_video_frame_source()` 新增 `video_ingress_channel_capacity()` 并把生产视频 RTP ingress 通道固定到 64 包；`RtcVideoSourceSink` 将 pending priority/repair/best-effort 预算收紧到 16/8/2 级别，按低延迟优先更早丢旧 continuation，保留新帧与修复价值包；`trace_lifecycle_reconnect_gate.py` 修正健康窗口后零 reconnect 候选时仍要求 block 事件的误报。验证：`cargo fmt`、`cargo test -p xbxengine video_ingress_channel_capacity_is_capped_for_low_latency --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::rtx_sink --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::insert_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`PYTHONPYCACHEPREFIX=/private/tmp/codex_pycache python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`、`python3 .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py runtime-logs/runtime-trace-1782288626404-1.jsonl --require-lifecycle-reconnect-gate`、`git diff --check`。待 fresh trace 复验：active gap/NACK 窗口出现 `insertGateDecision reason=activeRepairHold`，`repairing + decodableToFeed` 显著下降，尾段 `queueDepth` 不再接近 8192 且不再稳定到 160，`localBackpressureBestEffortOverflow` 不形成长段，+300s 后 `present_fps` 不掉到个位数，`submitAgeMs/presentAgeMs P95 < 200ms`，局部坏块减少。 |
| 2026-06-24-audio-low-latency-buffer-cap | 收紧长播放音频缓冲尾延迟 | Done | 2026-06-24 | 简单任务。fresh trace `runtime-trace-1782282736016-1.jsonl` 显示 312s 后 `audio_playout_latency_ms` 到 `984ms`，`audio_video_playout_delta_ms` 到 `908ms`；代码中 `MAX_BUFFERED_AUDIO_FRAMES=48000`，等价 1s Opus 缓冲，指标顶到上限。修复：将音频播放缓冲上限收紧为 `MAX_BUFFERED_AUDIO_LATENCY_MS=160`，溢出时继续丢最旧 PCM，保持低延迟优先。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::stream::audio --lib -- --nocapture`、`cargo test -p xbxengine playback_buffer --lib -- --nocapture`。待 fresh trace 复验：`audio_playout_latency_ms` 上限贴近 160ms，AV delta 显著回落。 |
| 2026-06-23-late-fps-decode-mailbox-drain | 修复长时间串流后 present 掉到个位数 | In Progress | 2026-06-23 | 简单任务。基线 trace `runtime-trace-1782199730727-1.jsonl`：+150s~+225s `present_fps avg=10.9`、`decode_fps avg=28.6`、`MEDIA_SUPPLY_GATE=PASS`、无 rebuild storm；后期 `video_pacer_submit_count_total` 每 5s 从约 110 递减到 23/16/13/10/7/5，`decode:drop:supersededAfterDecode` 每 5s 增至 100+，根因收敛为 decode output mailbox 在 present stressed 采样波动下过早压薄 pacer 供给。trace `runtime-trace-1782202167608-1.jsonl`：+150s~+225s 已改善到 `present_fps avg=23.6`、`decode_fps avg=29.6`、`submit_to_present P95=47ms`；尾段仍有约 `10fps` 样本，残留根因为单次 decode 调用内短突发打满输出 mailbox，且 `decode_fps≈26 / present_fps≈4~10` 时 stressed 判定短暂掉线。trace `runtime-trace-1782203808627-1.jsonl` freshness PASS：`receiveFeedbackGate=PASS`，默认 steady 窗口 `present_fps avg=20.8`、`decode_fps avg=29.5`、`submit_to_present P95=61.8ms`，+150s~+225s `present_fps avg=24.2`、+225s~+285s `present_fps avg=23.3`，后期个位数已退出；总 gate 仍 FAIL，剩余为 `STEADY_SUPPLY_GATE` 和局部 `submit_to_present P95=154/184ms`，raw counters 显示 `decodeEmit≈6929 / pacerSubmit≈4920 / decodeOutputDrop≈1963`，四槽 mailbox 生效但 30fps 源在 `present≈23-24fps` 时仍会掉出 stressed。修复：late submit/display 证据成立时允许 `present_fps=0` 维持 present-pipeline stressed；decode 成功后立即 flush pending decoded output 到 pacer；stressed decode output mailbox 扩为四槽短突发容忍；新增 severe-low-present 判定；本轮把 `PRESENT_PIPELINE_STRESSED_MAX_PRESENT_FPS` 从 `22` 抬到 `25`，让 30fps 源下 `present≈23-24fps / gap>=6fps` 保持 supply catch-up。验证：`cargo fmt`、`cargo test -p xbxengine present_pipeline --lib -- --nocapture`、`cargo test -p xbxengine decode_mailbox --lib -- --nocapture`、`cargo test -p xbxengine media::video::decode::actor --lib -- --nocapture`、`cargo check -p xbxengine`、`git diff --check`。待 fresh trace 复验：后期 `present_fps` 稳定退出个位数，`video_pacer_submit_count_total` 接近 decode 供给，`supersededAfterDecode` 长段下降，后段窗口 `STEADY_SUPPLY_GATE` 收敛。 |
| 2026-06-23-runtime-stop-drain-generation | 修复串流失败后 StopRuntime 卡住阻塞下一次启动 | Done | 2026-06-23 | 简单任务。最新 trace `runtime-trace-1782194645569-1.jsonl` 显示直连与 fallback 后仍无入站媒体，随后 `StopRuntime` 等待 runtime lock，第二次启动 `AttachViewport` 进入 `runtimeControlWaitingForRuntimeLock pendingControlCount=2`。修复：StopRuntime 拿到 runtime mutex 后先 detach viewport，切到新的 runtime generation 并释放主 mutex；旧 runtime 在后台 drain，旧 generation 的晚到 runtime event 不再推给前端；新增 `runtimeStopApplyEntered/runtimeStopRuntimeLockAcquired/runtimeStopApplyCompleted` 与 `runtimeStopDrainStarted/runtimeStopDrainCompleted` 诊断。验证：`cargo fmt`、`cargo test -p xbxrc stop_runtime_control_trace_events_are_named --lib`、`cargo test -p xbxrc runtime_state --lib`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-22-lifecycle-liveness-reconnect-gate | 修正健康网络下 livenessNoProgressTimeout 误升级 PeerConnection rebuild | In Progress | 2026-06-22 | 复杂任务。RFC：[`rfcs/2026-06-22-lifecycle-liveness-reconnect-gate.md`](rfcs/2026-06-22-lifecycle-liveness-reconnect-gate.md)。最新失败 trace `runtime-trace-1782114876554-1.jsonl` 显示 TWCC/ICE/remote track 健康且曾进入 60fps / DisplayStable，后续 `receiverWaitingKeyframe` 与 `livenessNoProgressTimeout` 被消费成 connectivity reconnect，触发多次 `rx closed cause=rebuildPeerConnection`，最终进入 connecting/priming 循环。已实现：`LifecycleRecovering/livenessNoProgressTimeout` 接入 expensive recovery gate，健康连接 + 服务化输出下输出 `reconnectBlocked:lifecycleGate:connectedHealthyNoProgress`，连续 rebuild 无 success edge 时输出 `reconnectBlocked:lifecycleGate:awaitSuccessEdge`；fresh direct ICE no-response、Disconnected/Recovering 硬连接证据与 remote terminal 出口保留；liveness reconnect attempt 只在 gate 放行后计数；新增 `trace_lifecycle_reconnect_gate.py` 并接入 `trace_webrtc_acceptance_gate.py --require-lifecycle-reconnect-gate`，用于 fresh trace 验收健康网络 rebuild regression；脚本现同时识别 `recoveryDecisionLedger.gateResult` 与结构化 `runtimeReconnectBlocked payload.blockReason=lifecycleGate:connectedHealthyNoProgress`。验证：`cargo fmt`、`cargo test -p xbxengine lifecycle_liveness --lib -- --nocapture`、`cargo test -p xbxengine reconnect_lifecycle --lib -- --nocapture`、`cargo test -p xbxengine connected_healthy_twcc --lib -- --nocapture`、`cargo test -p xbxengine reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::policy --lib`、`cargo check -p xbxengine`、`git diff --check`、`git diff --cached --check`、`PYTHONPYCACHEPREFIX=/private/tmp/codex_pycache python3 -m py_compile .agents/skills/analyze-runtime-logs/scripts/trace_lifecycle_reconnect_gate.py .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py`、`PYTHONPYCACHEPREFIX=/private/tmp/codex_pycache python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`（17 tests OK）；负样本单独 gate 与总验收均正确返回 `FAIL`，命中 `localRecoveryReconnectConsumedAfterHealthy` / `rebuildPeerConnectionAfterHealthy` / `missingLifecycleConnectedHealthyBlock`；旧日志 `runtime-trace-1782282736016-1.jsonl` 单独 lifecycle gate 识别 2 条 `runtimeReconnectBlocked`，`connectedHealthyNoProgressBlockCount=2`，combined gate 在无 freshness 限制下 PASS。本轮基于 `runtime-trace-1782710072770-1.jsonl` 继续修 direct `MediaStalled` 绕过 pending candidate gate，以及 rebuild 后 stats reset 导致 `transportRebuildInFlightNoProgress` 失效的问题：direct runtime recovery action 先走 lifecycle block，pending candidate 在 `Connecting + videoIngressTermination cause=rebuildPeerConnection` 且无 fresh direct ICE zero-response 时继续 block；验证 `cargo fmt`、`cargo test -p xbxengine runtime_blocks_reconnect_candidate_during_rebuild_even_after_media_stats_reset --lib -- --nocapture`、`cargo test -p xbxengine runtime_blocks_direct_media_stalled_reconnect_when_transport_remains_healthy --lib -- --nocapture`、`cargo test -p xbxengine api::runtime::tests::reconnect_recovery_matrix --lib -- --nocapture`。待真实串流 trace 验收：执行 `python3 .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py --latest --max-age-seconds 900 --require-lifecycle-reconnect-gate`，健康网络下无 `rx closed cause=rebuildPeerConnection` storm，并出现 lifecycle gate block 诊断。 |
| 2026-06-16-connected-healthy-twcc-reconnect-gate | 收紧健康网络下 receiverWaitingKeyframe 的 reconnect gate | Done | 2026-06-16 | 基于 `runtime-trace-1781587374660-1.jsonl` 复盘：TWCC stable / loss=0 / ICE 正常时，`staleAfterDecode`、`localBackpressureBestEffortOverflow`、host submit 停滞属于本地 media pipeline 断供。修复：`connectedHealthyTransportAwait` 现在同时接受新鲜稳定 local-feedback TWCC 样本作为健康连接证据，避免 ConnectionProjection 缺 path/rtt 时漏拦 `receiverWaitingKeyframe` reconnect；expensive recovery gate 改到最终动作成型后兜底，避免后置 fallback 绕过健康网络保护，并把 `reconnectBlocked:mediaGate:connectedHealthyTransportAwait` 投影到 ledger gate_result；RFC ceiling 已经把 reconnect candidate 降级为 cooldown 时补写具体 reconnectBlocked 诊断，避免其他 cooldown 原因被误标；remote terminal 仍保留 reconnect 出口，fresh stable TWCC 也不会吞掉远端终止恢复；runtime transport disconnected、remote-observed / stale / invalid / delayed / high-loss TWCC 与 fresh ICE direct no-response 不会被当作健康连接证据，stale ICE direct no-response 不会污染 fresh stable TWCC；trace-like repairing ReferenceChain 场景不要求 display-stable 也能拦住误 reconnect。验证：`cargo fmt`、`cargo test -p xbxengine connected_healthy_twcc --lib -- --nocapture`、`cargo test -p xbxengine trace_like_repairing_reference_chain_with_healthy_twcc_blocks_reconnect --lib -- --nocapture`、`cargo test -p xbxengine disconnected_runtime_state_ignores_fresh_twcc_health_for_reconnect_block --lib -- --nocapture`、`cargo test -p xbxengine latched_remote_terminal_home_recovery_stages_reconnect_candidate --lib -- --nocapture`、`cargo test -p xbxengine remote_terminal_transport_await_can_reconnect_even_on_healthy_transport --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::policy --lib`、`cargo test -p xbxengine reconnect_recovery_matrix --lib`、`cargo check -p xbxengine`、`git diff --check`。 |
| 2026-06-11-cloud-turn-fallback-hard-fail | 修复 cloud fallback TURN 无回包时继续停留连接中 | Done | 2026-06-11 | trace `runtime-trace-1781141076636-1.jsonl` 显示 direct 首轮仍在 `Connecting` 时 12s 定时器提前切入 fallback TURN；fallback 轮次 `turn:47.100.84.139:3478` allocation 10s 超时，`sentPackets=7/readPackets=0`，但 runtime host 仍把 fallback 记成 success。trace `runtime-trace-1781148446586-1.jsonl` 显示 fallback 已停止，direct 首轮长期 `Connecting`，远端返回 IPv6/IPv4 两条 host UDP candidate，但 family mismatch gate 在 `local=ipv4` 时过滤 IPv6，`out=1`。修复：移除连接中定时 TURN 兜底，只有直连 ICE 进入 `failed/closed` 且 launch spec 明确带 fallback TURN 时才重试 TURN；配置了 TURN server 时 relay allocation 失败直接让 `StartRuntime` 返回 `xbxEngineRtcTurnRelayAllocationFailed`，保留 `rtcTurnRelayAllocationFailed` 诊断；family mismatch gate 改为观测信号，不再删除远端 host candidate，Rust owned 与 browser runtime 候选策略都保留完整 direct ICE 候选集合，digest 增加 `familyMismatchObserved`。后续 trace `runtime-trace-1781155623392-1.jsonl` 证明直连候选已完整保留，但 Rust-owned direct 仍长期 `Connecting`、无 selected pair、无入站视频包；补 `rust-owned direct-first` 耗尽探测，首轮 direct 启动返回后延迟检查 stats，只有 `Connecting + 无 candidate pair + inboundVideoPacket/bytes=0 + 配置 fallback TURN` 时才触发一次 fallback。验证：`cargo fmt`、`./node_modules/.bin/vitest run src/streaming/runtime/ice-candidate-policy.test.ts src/streaming/runtime/runtime-host-policy.test.ts`、`cargo test -p xbxengine family_mismatch --lib`、`cargo test -p xbxengine remote_ice_policy_keeps_cross_family_host_candidates --lib`、`pnpm exec eslint src/streaming/runtime/ice-candidate-policy.ts src/streaming/runtime/ice-candidate-policy.test.ts src/streaming/runtime/browser-runtime.ts src/streaming/runtime/runtime-host.ts src/streaming/runtime/runtime-host-policy.ts src/streaming/runtime/runtime-host-policy.test.ts --fix`、`cargo check -p xbxrc`、`./node_modules/.bin/vitest run src/streaming/runtime/runtime-host-policy.test.ts src/streaming/runtime/ice-candidate-policy.test.ts`、`pnpm exec eslint src/streaming/runtime/runtime-host.ts src/streaming/runtime/runtime-host-policy.ts src/streaming/runtime/runtime-host-policy.test.ts --fix`、`git diff --check`；`pnpm lint:fix` 仍被既有三处无关 lint 阻塞。 |
| 2026-06-11-official-xbox-webrtc-capture | 采集官方 Xbox WebRTC 串流连接链路用于对照分析 | Done | 2026-06-11 | Report：[`reports/2026-06-11-official-xbox-webrtc-startup-comparison.md`](reports/2026-06-11-official-xbox-webrtc-startup-comparison.md)。产物一：`runtime-logs/official-xbox-capture-2026-06-11T06-20-13-579Z/`，官方控制面、session 创建、SDP/ICE exchange 均完成；PeerConnection 在 `checking/connecting` 约 15.4s 后 `failed`，candidate pair stats 显示 STUN checks `requestsSent≈34-35`、`responsesReceived=0`、无 selected/nominated pair。产物二：`runtime-logs/official-xbox-capture-2026-06-11T06-46-55-910Z/`，更换网络后成功进入 1080p60 画面，selected pair `succeeded/nominated=true`，local `prflx UDP IPv4` 到 remote `srflx UDP IPv4`，RTT 约 `20-35ms`，约 341s 稳定段视频码率 `15.67Mbps`、丢包率 `0.044%`、NACK 增量 `241`、PLI 增量 `1`。新增采集脚本 `scripts/official-xbox-capture.mjs`，验证：`node --check scripts/official-xbox-capture.mjs`。 |
| 2026-06-11-rust-ice-official-alignment | 按官方 Xbox ICE 采样优化 Rust-owned 建连诊断与收口 | Done | 2026-06-11 | 基于官方失败样本 `requestsSent≈34-35 / responsesReceived=0 / 无 selected-nominated pair` 与成功样本 `local prflx -> remote srflx / requests=responses=133`，Rust 侧新增 `iceConnectivityProbe` 观测与 `IceConnectivityProbeSampled` fact，candidate-pair 统计覆盖请求、响应、selected/nominated、local/remote candidate type 与 address family；`transport_path` 增加 `Direct (prflx->srflx)`；session policy 在首帧前 `Connecting/Recovering + STUN checks >=8 + responses=0 + 无 selected/nominated + probe 新鲜 + 无进展持续 12s` 时触发 reconnect candidate，避免 direct ICE 0 响应长期空转。验证：`cargo fmt`、`cargo test -p xbxengine transport_metrics --lib`、`cargo test -p xbxengine reconnect_lifecycle --lib`、`cargo test -p xbxengine family_mismatch --lib`、`cargo test -p xbxengine remote_ice_policy_keeps_cross_family_host_candidates --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-11-fresh-cloud-home-stream-followup | 修复 fresh cloud/home 串流 trace 的剩余 Rust-owned 策略缺口 | In Progress | 2026-06-11 | trace `runtime-trace-1781169820859-1.jsonl`：cloud 直连健康但 `decode_fps≈30.7 / present_fps≈19.8`，home 前段健康后长期 `displaySupplyStarved`，receive terminal 连续 `remote-no-response` 且未落成 reconnect。当前修复：remote terminal 下沉为 recovery contract shared fact，`terminal_total>0 + unresolved>=5 + no-packet/non-idr-only/idr-unusable + need-keyframe + display!=display-stable` 会在 reason 清空时 latch；coordinator/expensive gate 将该事实视为 transport-await hard evidence，并覆盖旧 displayed-IDR/clean-anchor 与 recent packet/fresh output 对 reconnect 的误阻断；remote terminal owner signal 跳过普通 IDR pressure/stale replay/ramp-up 吸收，并在策略层直接提升为 `RequestReconnectCandidate`，补上日志里 `nextOwnerHint=connectivity-reconnect-candidate` 但 runtime reconnect 未 staged 的缺口；只在 remote terminal + `receive_keyframe_required` 的 reconnect candidate 上提升 `ConnectivityTransport`，`display-stable` 继续保护。cloud 侧统一 present-stressed 阈值，并在 stressed 形态下把 decode output mailbox 扩为 `current + next_candidate + latest_candidate` 三槽，普通形态继续保持 `current + latest` 两槽容量，减少 `queueDepth=2` 时的 `supersededAfterDecode`。stats/DTO/trace/TS 已新增 `video_decode_output_mailbox_depth` 与 `video_decode_output_present_pipeline_stressed`；runtime reconnect candidate staged/consumed 分别输出 `rtcReconnectCandidateStaged` / `runtimeReconnectConsumed` trace 事件，decode output mailbox depth / present-stressed 变化输出 `decodeOutputMailboxState` trace 事件。最新 home trace `runtime-trace-1781228088951-1.jsonl` 暴露首帧前 `receiverWaitingKeyframe` 被消费成 `reasonDomain=connectivity-transport`，触发 `seq=976/1467/1940/2328` reconnect storm；已把 remote-terminal 强制 reconnect fallback 收窄为尊重 first-frame acquisition priority，首帧前保持 receive recovery，新增 `pre_first_frame_remote_terminal_waits_for_receive_recovery` 回归。本轮 trace `runtime-trace-1781235230138-1.jsonl` 显示网络健康但 18 次 rebuild 与后段 `submit_to_present` 秒级长尾；`refresh_successful_media_edge()` 现在在 decode/present/clean-anchor 成功边推进时清空 liveness no-progress 与 connected render-stall 计时，避免 frame_count 未推进但画面/解码已恢复时沿旧计时继续 rebuild；新增 `successful_media_edge_resets_liveness_no_progress_timer` 回归。新 trace `runtime-trace-1781514075152-1.jsonl` 显示 +57.998s~+60.019s 已健康上屏，随后网络仍可协商/keepalive accepted，但 `receiverWaitingKeyframe` 在健康链路下触发多次 PC rebuild，并最终因 `SessionInUnexpectedState: ServerIceExchangeCommandSent` 断连；本轮把 `receiverWaitingKeyframe` reconnect gate 加上 `connectedHealthyTransportAwait` 保护：连接健康、RTT/丢包健康且已有 fresh output / clean anchor / display-stable / recent success edge 时抑制 PC rebuild，remote terminal 仍允许 reconnect。验证：`cargo fmt`、`cargo test -p xbxengine remote_terminal --lib`、`cargo test -p xbxengine recovery_observation --lib`、`cargo test -p xbxengine reconnect_lifecycle --lib`、`cargo test -p xbxengine reconnect_recovery_matrix --lib`、`cargo test -p xbxengine decode_mailbox --lib`、`cargo test -p xbxengine media::video::decode --lib`、`cargo test -p xbxengine media::video::decode::actor --lib`、`cargo test -p xbxengine present_pipeline --lib`、`cargo test -p xbxengine diagnostics::stats --lib`、`cargo test -p xbxrc trace_projection --lib`、`cargo test -p xbxengine transport::rtc::session::policy --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`；`pnpm lint:fix` 仍被既有三处无关 lint 阻塞：`src/player/infra/render/Renderers.test.ts:1040`、`src/shared/gamepad/wait-pad-neutral.ts:58`、`src/streaming/diagnostics.ts:246`。待 fresh trace 验收：home 启动期不再出现 `transportReconnectCandidate:receiverWaitingKeyframe` storm，健康网络下 trace 应出现 `reconnectBlocked:mediaGate:connectedHealthyTransportAwait` 而非 rebuild；liveness/ICE 硬失败与 remote terminal 仍可 runtime reconnect；cloud `decodeOutputMailboxState` 显示 `mailboxDepth=3` / `presentPipelineStressed=true` 且 present/decode gap 收敛；本轮 trace 目标是后段 rebuild 次数下降且 +500s~+540s `submit_to_present P95 < 80ms`。 |
| 2026-06-10-windows-rust-owned-zero-copy-present | 修复 Windows Rust-owned 串流 0 CPU 拷贝直通上屏 | In Progress | 2026-06-10 | RFC：[`rfcs/2026-06-10-windows-rust-owned-zero-copy-present.md`](rfcs/2026-06-10-windows-rust-owned-zero-copy-present.md)。已落地：Windows `D3D11 texture` policy 改走 `NativeDirect`，新增 `WindowsD3d11Presenter` 用 D3D11 swapchain + NV12 plane SRV 直出 shared handle，CPU surface 在 native presenter 显式拒收，诊断写 `descriptorUploadMode=d3d11-native`。验证：`cargo fmt`、`cargo test -p xbxrc --lib native_video`、`cargo check -p xbxrc`、`cargo check -p xbxengine`、`git diff --check`；Windows 交叉检查受本机 `cargo xwin` 的 SDL3/Opus CMake/Ninja 环境阻塞，实机画面验证待跑。 |
| 2026-06-10-native-video-present-path-normalization-for-effects | 规范化 win/mac native video 上屏路径，为 WGPU effect pipeline 铺底 | In Progress | 2026-06-10 | RFC：[`rfcs/2026-06-10-native-video-present-path-normalization-for-effects.md`](rfcs/2026-06-10-native-video-present-path-normalization-for-effects.md)。目标先把 macOS 直出已有的调度节拍、retained redraw、host mailbox 诊断迁移到 macOS/Windows WGPU 路径，再规范化 WGPU renderer 与 effect pipeline 架构。已落地：1) `ScheduledFrameTakeOutcome::RetainedDisplayedFrame` 直接携带 displayed frame，macOS layer / macOS WGPU / Windows WGPU 都从 `ScheduledFrameSlot` 事实源取 retained frame；2) retained/idle/stale host mailbox 诊断抽成共享函数，layer / macOS WGPU / Windows WGPU 同口径输出；3) `hostFramePresented` 抽成共享函数，WGPU render 成功后与 layer 一样输出 `present/refresh` 上屏事实；4) `VideoEffectPipelineKind::required_presenter_mode()` 固定非 `Noop` effect 必须匹配 `GpuDirect`；5) `VideoEffectPipeline::process_frame()` 输出结构化 effect outcome，并记录 effect kind、active、fallback reason、render cost、input/output size；6) `WgpuFrameRenderResources` 与 `build_surface_config()` 抽出 macOS/Windows WGPU renderer 共享初始化逻辑；7) `render_wgpu_frame_texture_surface()`、`draw_frame_texture_render_source()`、`update_wgpu_surface_size()` 统一 surface acquire/reconfigure、render pass、aspect-fit viewport、submit/present；8) `create_rgba_texture_bundle()`、`write_rgba_texture()`、`create_nv12_cpu_texture_parts()`、`create_nv12_bind_group()`、`write_nv12_texture_planes()` 统一 RGBA/BGRA 与 CPU NV12 上传、NV12 bind group 构建，native import 差异继续留在平台段；9) `process_wgpu_render_take_outcome()` 统一 macOS WGPU / Windows WGPU ready、retained、idle、stale outcome 执行与 hostTiming/present telemetry 输出；10) `take_wgpu_scheduled_frame()` 统一 macOS WGPU / Windows WGPU display tick 记录、view epoch 切换、`take_ready_frame()` 与 diagnostics snapshot 采集，并补 ready diagnostics / view epoch replay 回归测试。验证：`cargo fmt -p xbxrc`、`cargo test -p xbxrc --lib native_video`（48 passed）、`cargo check -p xbxrc`、`git diff --check`；Windows target 交叉检查受本机工具链阻塞，MSVC 缺 Windows SDK / C 标准头，GNU 缺 `x86_64-w64-mingw32-gcc`，均停在依赖 C 构建阶段。 |
| 2026-06-10-runtime-control-lock-yield | 修复串流启动卡在连接中时 runtime control 被周期 tick 长时间占锁 | Done | 2026-06-10 | 最新 trace `runtime-trace-1781083187515-1.jsonl` 显示后续 cloud/home 启动停在 `runtimeAttachViewportApplyEntered`，缺少 `runtimeAttachViewportRuntimeLockAcquired`，退出也只有 `StopRuntime controlCommand`，说明 `AttachViewport/StopRuntime/StartRuntime` 正在等待 runtime mutex。修复：control 命令进入时记录 pending control，tick 看到 pending control 或 runtime lock busy 会让路；control 锁等待写 `runtimeControlWaitingForRuntimeLock`，tick 让路写 `runtimeTickSkippedRuntimeLockBusy`，`runtime*RuntimeLockAcquired.durationMs` 改为锁等待耗时。验证：`cargo fmt`、`cargo check -p xbxrc`。 |
| 2026-06-10-rust-owned-runtime-viewport-resolution | 修复 Rust-owned 运行期握手 viewport 固定值导致恢复 IDR 可回落低分辨率 | Done | 2026-06-10 | 最新 trace `runtime-trace-1781062857748-1.jsonl` 中 session plan 为 Tizen 1080p、`x-ms-device-info=1920x1080`、REMB 32Mbps 且 TWCC loss=0，但播放中收到 `decoded-keyframe-config-change` 的 `1280x720` SPS/PPS IDR；修复：post-handshake `dimensionschanged` 使用 runtime target resolution；播放期若 1080p+ target 收到低于 target 的 IDR 配置变化，立即重发目标 `dimensionschanged`、刷新 REMB、请求新关键帧，并按 RTP 幂等记录 `rtcRuntimeResolutionReasserted`。验证：`cargo fmt`、`cargo test -p xbxengine low_resolution_recovery_idr --lib`、`cargo test -p xbxengine post_handshake_dimensions_follow_runtime_target_resolution --lib`、`cargo test -p xbxengine transport::rtc::connection --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-10-nack-escalation-hard-gap-gate | 修复普通 transport/disposable NACK 耗尽触发播放期 NeedKeyframe 脉冲 | Done | 2026-06-10 | 最新 trace `runtime-trace-1781062857748-1.jsonl` 显示 receive gate 已 PASS，但 midsegment 仍因 `receiver-local-nack-blocking-admission` 对 `budget=disposable/evidence=unknown` 的 transport gap 触发 `nack-exhausted -> need-keyframe`，造成 `submitAgeMs` 秒级尖峰；修复：keyframe escalation 绑定 unresolved hard/reference gap，普通 transport gap 只停止低价值 NACK，不打开 hard keyframe 窗。验证：`cargo fmt`、`cargo test -p xbxengine suppressing_repairable_gap_escalation_prevents_immediate_rearm --lib`、`cargo test -p xbxengine disposable_transport_gap_nack_exhaustion_is_not_keyframe_escalation_worthy --lib`、`cargo test -p xbxengine hard_reference_gap_nack_exhaustion_is_keyframe_escalation_worthy --lib`、`cargo test -p xbxengine transport::rtc::receive::nack_maintenance --lib`、`cargo test -p xbxengine transport::rtc::receive::trace_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-10-reconnect-epoch-isolation-and-exchange-diagnostics | 修复重连 epoch 旧 ReferenceChain/timeline 投影污染，并补齐 exchangeOffer timeout 诊断 | Done | 2026-06-10 | 最新 trace `runtime-trace-1781059327765-1.jsonl` 显示播放前半段可用，后段 TWCC loss/RTT 恶化后进入 `supply-break`，reconnect 的 `exchangeOffer` 20s timeout 后 keepalive 返回 `SessionInUnexpectedState`。修复：新 transport recovery epoch 会清理旧 receive timeline / ReferenceChain / sparse mismatch / stats fallback 投影，避免旧 broken gap 污染新 epoch；host `exchangeOffer` trace 补 `requestId`、`timeoutMs`、`failureKind`、`phase`、`answerReceived`、`channel/restart`。验证：`cargo fmt`、`cargo test -p xbxengine advancing_transport_recovery_episode_clears_stale_anchor --lib`、`cargo test -p xbxrc exchange_offer_failure_kind_marks_timeout --lib`、`cargo test -p xbxrc exchange_offer_with_timeout_returns_error_when_future_stalls --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-06-09-nack-exhausted-bad-frame-hold | 修复画面大变化时 NACK 耗尽后继续喂 non-IDR 导致坏帧 | Done | 2026-06-09 | 最新 trace `runtime-trace-1780997509820-1.jsonl` 显示中段延迟与旧帧风险健康，但 TWCC 短窗丢包最高 `18.75%`、`nackSent=32/recovered=0`，同时 `mediaSupply=repairing` 下 insert gate 仍大量 `emit decodableToFeed` non-IDR。修复：receiver-local NACK escalation due 直接进入 receive ledger hard keyframe 窗，不再要求 trace gap 已先被判成 hard gap；`note_nack_exhausted()` 清理旧 clean anchor / decoder sync / display stable，reference chain 投影独立承认 `NackExhausted`，后续 non-IDR continuation 被 `mustIdrHold` 拦住直到新 usable IDR。验证：`cargo fmt`、`cargo test -p xbxengine nack_exhausted_reopens_window_and_holds_continuation_after_clean_anchor`、`cargo test -p xbxengine disposable_nack_exhausted_after_clean_anchor_reopens_keyframe_window`、`cargo test -p xbxengine transport::rtc::receive::trace_ledger`、`cargo test -p xbxengine transport::rtc::receive::insert_gate`、`cargo test -p xbxengine transport::rtc::receive::nack_maintenance`、`cargo test -p xbxengine transport::rtc::receive`、`cargo test -p xbxengine transport::rtc::recovery::contract`、`cargo check -p xbxengine`。 |
| 2026-06-09-video-continuation-no-output-recovery | 修复分辨率切换后 continuation no-output 继续喂入与旧帧风险漏报 | Done | 2026-06-09 | 最新 trace `runtime-trace-1780994575077-1.jsonl` 显示 1080p->720p 后 VideoToolbox 对 non-IDR continuation 出现 `backend-no-output`，并在 host 侧出现 `submitAgeMs/frameAgeMs` 超过 1s 的旧帧呈现。修复：decode no-output hint 在 receive 侧消费时进入硬 keyframe 窗口，清理旧 decoder sync / clean-anchor 投影并重开 recovery ledger，后续 non-IDR continuation 会被 `mustIdrHold` 挡住直到新 IDR；host retained-old-frame 风险判定纳入 `submit_age_ms`，steady cadence 下也能暴露旧源帧风险。验证：`cargo fmt`、`cargo test -p xbxengine nominal_continuation_no_output_exports_receive_keyframe_hint`、`cargo test -p xbxengine decoder_waiting_keyframe_reopens_window_and_holds_continuation`、`cargo test -p xbxengine transport::rtc::receive::insert_gate`、`cargo test -p xbxengine media::video::decode::video_decode`、`cargo test -p xbxrc host_mailbox_state`、`cargo check -p xbxengine`、`cargo check -p xbxrc`。 |
| 2026-06-09-main-window-black-background | 主窗口默认黑色背景，避免透明底色闪现 | Done | 2026-06-09 | 仅设置 Tauri main window `backgroundColor=#000000`，HTML/CSS 层保持现有 UI 与串流样式。验证：`cargo check -p xbxrc`、`git diff --check`；`pnpm lint:fix` 仍被既有 `Renderers.test.ts` / `wait-pad-neutral.ts` / `streaming/diagnostics.ts` 三处 lint 阻塞。 |
| 2026-06-09-agent-streaming-webrtc-behavior-standard | 将网络、恢复、显示、上屏的低延迟优先判断写入 agent 行为标准 | Done | 2026-06-09 | 更新 `AGENTS.md`，新增 Streaming / WebRTC Behavior Standard：网络层贴近标准 WebRTC，恢复层和 decode 后显示链以低延迟优先与 trace gate 验证收口；后续分析需覆盖网络健康、恢复链健康、media supply、steady supply、submit/present 长尾与帧供给 delta。 |
| 2026-06-09-keyboard-fallback-listener-self-heal | 修复无实体手柄时键盘 fallback 模拟输入无效 | Done | 2026-06-09 | trace `runtime-trace-1780989727891-1.jsonl` 显示仅 `virtual:keyboard` 连接、slot 全零、self-heal 因 keyboard-only 跳过物理 SDL prime；后续 `runtime-trace-1780990989976-1.jsonl` 显示 runtime 采样健康推进到 `sampleSeq=23801`，但 3325 个 UI slot 仍全零，根因为空 `gamepad_keyboard_mapping.bindings` 覆盖协议默认键位；最新 `runtime-trace-1780993887891-1.jsonl` 在默认映射修复后仍 2450 个 slot 全零，收敛为 macOS 底层键盘读取链没有读到按下态。修复：保留实体手柄 self-heal 的非 keyboard 条件，给 keyboard fallback 增加独立监听线程健康检查与重启链；配置默认值、配置归一化、启动持久化映射应用与运行时替换映射都会把空 keyboard mapping 恢复为协议默认键位；新增前端 DOM keyboard fallback bridge，窗口聚焦时用 `keydown/keyup` 通过 `submitKeyboardState` RPC 提交同一套 `virtual:keyboard` 状态，blur/隐藏时提交 neutral。验证：`cargo fmt`、`cargo test -p ohmygamepad-sdl3 keyboard_fallback_self_heal_restarts_missing_listener`、`cargo test -p ohmygamepad-sdl3 empty_keyboard_mapping_resets_to_default_mapping`、`cargo test -p ohmygamepad-sdl3`、`cargo test -p xbxrc normalize_config_replaces_empty_keyboard_mapping_with_default`、`cargo test -p xbxrc default_keyboard_mapping_is_non_empty`、`cargo check -p ohmygamepad-sdl3`、`cargo check -p ohmygamepad-host`、`cargo check -p xbxrc`、定向 eslint；`pnpm lint:fix` / `vue-tsc --noEmit` 仍受既有无关错误阻塞。 |
| 2026-06-09-xbxengine-exchange-offer-timeout | 修复 peer connection rebuild 后 `ExchangeOffer` 挂住导致视频保留最后一帧 | Done | 2026-06-09 | 最新 trace `runtime-trace-1780988415959-1.jsonl` 显示 `RtcVideoFrameSource rx closed cause=rebuildPeerConnection` 后停在 `RuntimePhaseChanged=ExchangingOffer`，native 层持续 `retainedDisplayed` 同一 `displayedFrameSeq=2445`。修复：Tauri xbxengine host bridge 的 media `exchangeOffer` 加 20s bounded wait，超时写 `exchangeOfferFailed` trace 并返回 `exchangeOffer:...timed out` 错误，避免 engine worker 无限阻塞。验证：`cargo fmt`、`cargo test -p xbxrc exchange_offer_with_timeout_returns_error_when_future_stalls`、`cargo check -p xbxrc`。 |
| 2026-06-08-cloud-stream-start-ice-fallback | Cloud 串流启动卡在 ICE Connecting 时自动 fallback TURN，并补 TURN relay allocation trace | Done | 2026-06-08 | 最近两份 trace `runtime-trace-1780892426606-1.jsonl` / `runtime-trace-1780889019408-1.jsonl` 显示 session/provisioning/offer/submit ICE 都成功，后续长期停在 `transportState=Connecting`，`inbound_video_packet_count_total=0`，且本地候选未见 relay。修复：Rust 手动收集的 host/srflx/relay candidate 同步写入 connection state，新增 `rtcTurnRelayAllocationSucceeded/Failed` 专用 stats + trace；前端把 cloud fallback TURN 纳入 direct-first，`connecting` 超过 12s 且未 connected 时触发一次 fallback TURN retry。新 trace `runtime-trace-1780967916315-1.jsonl` 确认 fallback TURN 已触发，阻塞点为 `turn:47.100.84.139:3478` allocation timeout；本轮将 TURN allocation 外层等待从 3s 调整到 10s，并把 `serverAddr/baseAddr/sentPackets/readPackets/timeoutEvents/lastSendTarget/lastReadPeer` 写入失败摘要。外部探测：UDP connect 成功，但标准 STUN Binding 与 TURN Allocate 均 3s 无回包，TCP 3478 超时。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::connection::turn_runtime --lib`、`cargo test -p xbxengine transport::rtc::connection::io_runtime --lib`、`cargo test -p xbxengine transport::rtc::connection --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`cargo test -p xbxrc record_runtime_trace_observations -- --nocapture`、`./node_modules/.bin/vitest run src/streaming/runtime/runtime-host-policy.test.ts`、`pnpm exec eslint src/streaming/runtime/runtime-host.ts src/streaming/runtime/runtime-host-policy.ts src/streaming/runtime/runtime-host-policy.test.ts --fix`。全量 `pnpm lint:fix` 被既有 `Renderers.test.ts` / `wait-pad-neutral.ts` / `streaming/diagnostics.ts` 三处 lint 挡住。 |
| 2026-06-05-owner-repairing-output-serviceable | owner 在 receive repairing + fresh output 时退出 supply-starved，降低恢复脉冲 | Done | 2026-06-05 | Fresh trace `runtime-trace-1780625764009-1.jsonl`：`receiveFeedbackGate=PASS`，keyframe outcome 从上一轮高频 sent 收敛到 `sent=46/coalesced=201/throttled=1`，`arbiterMismatchTotal=0`；剩余 `acceptanceGate=FAIL` 来自 midsegment `globalLatencyGate=FAIL`，`steady ratio=46.7%`、recovering signals=60、`submit_age_ms P95=364ms`、`present_fps avg=17.3`。raw seq 4888 显示 `decode_age_ms=23`、`present_age_ms=44`、`submit_age_ms=20`、track attached 且有视频字节、receive `keyframeRequired=false`、chain repairing/NACK pending，但 owner 仍输出 `supply-starved/displaySupplyStarved`。修复：`VideoSchedulingOwner` 新增 repairing chain serviceable weak-exit，connected + 无 host present stall + receive 未要求 keyframe + repairing/receiving + gap/insert 修复中 + decode/present/submit 新鲜时保持 `DegradedServing`，并补 `supply_starved_repairing_chain_with_fresh_output_exits_to_degraded_serving`。后续 fresh trace `runtime-trace-1780636701231-1.jsonl` 证明长恢复已消退：steady ratio 升至 `90.9%`、max continuous recovering streak `0.0s`、keyframe `sent=23/coalesced=134`、receive gate 继续 PASS；剩余失败集中在 submit/present 长尾（`submit_age_ms P95=357ms`、`submit_to_present_ms P95=339ms`、host submit gap P95 `611ms`）。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner`、`cargo test -p xbxengine transport::rtc::receive::feedback_arbiter`、`cargo check -p xbxengine`、`cargo check -p xbxrc`。 |
| 2026-06-04-hard-gap-recovery-ledger-window | hard gap keyframe 请求开启新 receive recovery 窗，避免旧 DisplayStable/UsableIdr 掩盖后续 supply-break | Done | 2026-06-04 | 现场 trace：`runtime-trace-1780537855068-1.jsonl`，stale latest `runtime-trace-1780543819343-1.jsonl` 仍显示旧窗口污染且 freshness gate 失败；fresh trace `runtime-trace-1780558882322-1.jsonl` 显示 `freshnessGate=PASS`、`localControlVideoKeyframeRequested=12`，但仍有 `displayStableWithoutLedgerClosure=106` 与 midsegment latency fail；后续 fresh trace `runtime-trace-1780559787522-1.jsonl` 中 control keyframe 仍有效（`localControlVideoKeyframeRequested=7`），剩余失败收窄到 `displayStableWithoutLedgerClosure=9`、`needKeyframeNonIdrFeedViolations=2`。修复：active hard gap 下 keyframe sent 清理旧 response/display/clean-anchor 窗口，仅保留历史 usable anchor，continuation-only 可重新升格并触发 terminal 判定；Rust keyframe request 与浏览器 direct 对齐，RTCP PLI/FIR 保留，同时 control channel ready 时发送 `{"message":"videoKeyframeRequested","ifrRequested":true}`，RTCP target warming 时可由 control path 形成 sent outcome，RTCP target ready 时也保留 local control catalog；本地 data-channel send 现在写 `channelMessageCatalog`，`trace_receive_feedback_report.py` / `trace_webrtc_acceptance_gate.py` 输出 `controlKeyframeRequests.localControlVideoKeyframeRequested`；receive ledger 在新的 keyframe_required / unresolved keyframe request 下会清理并阻止旧 display-stable 回灌，Tauri trace projection 只在 `keyframeRequired=false` 时投 `stableServingSettled`，并为 `displayed-idr` DisplayStable transition 补 receive control facts；insert gate 在 `NeedKeyframe` 阶段全程只允许 usable IDR，堵住 priming/first-frame 窗口的 `decodableToFeed` non-IDR 漏口。验证：`cargo fmt`、focused `cargo test -p xbxengine hard_gap_keyframe_request`、`cargo test -p xbxengine keyframe_request_after_clean_anchor_keeps_usable_idr_response_fact`、`cargo test -p xbxengine transport::rtc::receive::recovery_ledger`、`cargo test -p xbxengine control_video_keyframe_payload_matches_browser_direct_request`、`cargo test -p xbxengine request_video_pli_reports_feedback_target_unavailable_without_twcc_feedback_target`、`cargo test -p xbxengine video_recovery_uses_control_keyframe_when_rtcp_feedback_is_not_supported`、`cargo test -p xbxengine transport::rtc::connection`、`cargo test -p xbxengine transport::rtc::receive`、`cargo test -p xbxengine transport::rtc::recovery::contract`、`cargo test -p xbxengine keyframe_required_after_display_stable_reopens_display_debt`、`cargo test -p xbxengine unresolved_keyframe_request_blocks_stale_display_fact_replay`、`cargo test -p xbxengine need_keyframe_holds_decodable_non_idr_before_first_frame`、`cargo test -p xbxengine transport::rtc::receive::insert_gate`、`cargo test -p xbxrc record_runtime_trace_observations_projects_channel_message_catalog_once`、`cargo test -p xbxrc display_stable_projection_waits_until_keyframe_required_clears`、`cargo test -p xbxrc display_stable_transition_projection_includes_receive_control_facts`、`python3 .agents/skills/analyze-runtime-logs/tests/test_trace_receive_feedback_report.py`、`cargo check -p xbxengine`、`cargo check -p xbxrc`。 |
| 2026-06-02-webrtc-recovery-boundary-redesign | WebRTC 式恢复边界设计级修正：receive/decode 闭合 media recovery，display/host 降级为 post-media gate，移除 no-idr 自锁的跨层调度闭环 | Done | 2026-06-02 | RFC：[`rfcs/2026-06-02-webrtc-recovery-boundary-redesign.md`](rfcs/2026-06-02-webrtc-recovery-boundary-redesign.md)。Report：[`reports/2026-06-02-webrtc-recovery-boundary-redesign.md`](reports/2026-06-02-webrtc-recovery-boundary-redesign.md)。已完成代码侧 RFC 改造：H264 commit 下沉到 receive AU 准入点；clean anchor / decoder reference sync 优先清 first-delta、non-idr-only、stale decoder waiting/no-output、旧 gap/NACK/anchor debt；first-frame acquisition 真实发 PLI/FIR 且不写 `keyframe_required`；clean-anchor ack 清 receive-local blocking wait 并发布 `clean-anchor-committed`。最新根因修正：current clean anchor 统一承认 `decoded-usable-idr / clean-anchor-committed / displayed-idr`；submit starvation 不再反灌 `ReferenceChainState::NeedKeyframe`，也不再投成 `MustIdr/AwaitIdr`；`PresentationSupplyPhase` 已退出 `MustIdr`，`RecoverySurfacePhase::AwaitIdr` 由 receive-local keyframe fact / decoder waiting / packet action stage 直出；decode actor 与 session owner 会把 clean anchor / decoder sync 后的 stale receiver `waiting-keyframe` 归一为 `receiving/repairing`；clean anchor 后旧 `insert-gate-need-keyframe` timeline 不再阻止 soft SPS/PPS request；session/startup/policy 不再引用 displayed-IDR relaxed-control helper；ReferenceChain 主路径由 receive ledger 投影，stats 只补诊断字段；InsertContext 生产路径退出 stats-derived fallback；sparse-IDR rhythm、NACK/keyframe action stage、session fresh-output bypass 与 retry interval 统一读 receive ledger；`has_current_clean_anchor_from_stats()` 与 `current_clean_anchor_observed_at_ms_from_stats()` source 已统一，decode continuation bypass 改读 current clean anchor，decoded clean anchor 后 committed-SPS/PPS non-IDR continuation 可进入 decoder。最终 fresh 验收：`runtime-trace-1780483285846-1.jsonl` 组合 gate `acceptanceGate=PASS`，`receiveFeedbackGate=PASS`，`sent=5 / responseObserved=2 / decoded=2 / cleanAnchorCommitted=2 / DisplayStable=1`，midsegment `globalLatencyGate=PASS / mediaSupplyGate=PASS / steadySupplyGate=SKIPPED`；应用约 420 秒运行未见 panic，进程由 timeout 结束。 |
| 2026-06-01-native-video-retained-redraw | retained displayed frame 继续重绘，Rust present 链在断供时不再停住最后一帧 | In Progress | 2026-06-01 | 让 macOS / Windows retained displayed frame 走真实 repaint，先把“最后一帧停住”这个可见问题拉平。 |
| 2026-05-26-steady-supply-present-convergence | 稳态供给下 present 贴近 decode：SteadySupply 合同、host tick rerun 补链、pacer stressed release、trace 双门禁 | In Progress | 2026-05-26 | 挂 RFC [`2026-05-25-low-latency-display-scheduling-optimization.md`](rfcs/2026-05-25-low-latency-display-scheduling-optimization.md)。已落地：`finish_host_present_tick_guard_and_maybe_rerun`、`present_cadence`/`pacer` stressed、[`scripts/trace_midsegment_report.py`](scripts/trace_midsegment_report.py)。基线 `1779781188198` mid：`GLOBAL_LATENCY_GATE` PASS，`STEADY_SUPPLY` FAIL（改前）。最新 fresh trace `runtime-trace-1780567535265-1.jsonl` 已确认 receive/keyframe/ledger PASS 且 decode freshness 健康，剩余失败集中在 layer host submit/present：`submit_age_ms P95=394ms`、`submit_to_present_ms P95=295ms`、`ready ratio=6.2%`。本轮补齐 layer immediate tick 诊断：`run_on_main_thread_delay`、`present_tick_dispatch_coalesced`、`present_tick_rerun`，并修复 immediate tick enqueue 失败时释放 pending。2026-06-05 追加 fresh trace `runtime-trace-1780636701231-1.jsonl`：native 主线程健康，`run_on_main_thread_delay` 仅 1 次 34ms，`present_tick_dispatch_coalesced=24` 与 `present_tick_rerun=24` 配平；host mailbox enq/present 在稳态窗口配平且 drop/overwrite=0，残留为 pacer release 对 `decode≈23 / present≈19 / submit_age≈357ms / present_age≈181ms` 的边缘窗口触发偏保守。本轮修复：`present_pipeline_stressed_from_stats()` 增加 host steady + submit/present age 长尾 + decode 领先 present 的 latency-stressed 分支，pacer 在该形态下按 host refresh 间隔释放。后续 fresh trace `runtime-trace-1780652183550-1.jsonl` 显示节拍已改善：`submit_age_ms P95=125ms`、`present_age_ms P95=88ms`、`submit_to_present_ms P95=48ms`、`decode_fps avg=34.8 / present_fps avg=33.8`；剩余 FAIL 来自 `receive-ledger-hard-gap-nack-exhausted` recovery pulse。根因：16-bit RTP sequence 复用/跨帧 NACK 批次把旧 `disposable/unknown` gap `64271` 继承后续 anchor evidence，升级成 hard gap 并触发 `NeedKeyframe`。本轮修复：`ReceiverTraceLedger::update_gap()` 只在同一 frame RTP 或未绑定 frame RTP 时合并 importance/evidence，避免跨帧同 sequence 把旧 disposable gap 升级为 anchor hard gap。验证：`cargo fmt`、`cargo test -p xbxrc --lib native_video`、`cargo check -p xbxrc`；本轮追加验证：`cargo test -p xbxengine media::video::present_cadence`、`cargo test -p xbxengine media::video::pacer::actor`、`cargo test -p xbxengine transport::rtc::receive::trace_ledger`、`cargo test -p xbxengine transport::rtc::receive::nack_maintenance`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。 |
| 2026-05-25-low-latency-display-scheduling-optimization | 低延迟显示调度优化（不硬对齐 WebRTC）：steady displayed-IDR continuation 不打断、host present 节拍、四段延迟合同 | Done | 2026-05-26 | RFC/Report 闭环。Report：[`reports/2026-05-26-low-latency-display-scheduling-optimization.md`](reports/2026-05-26-low-latency-display-scheduling-optimization.md)。Steady Supply 跟进见 `2026-05-26-steady-supply-present-convergence`。 |
| 2026-05-20-receive-side-loop-rearchitecture | 基于 `rtc` 的低延迟优先 receive-side loop 重构：按标准 WebRTC receiver ownership 彻底重做接收侧闭环，删除旧 recovery/timeline 兼容层 | Done | 2026-05-20 | Phase B 完成：`receive/ReceiveEngine` 接线、receiver-local NACK/PLI、timeline/NACK 瘦身、全库 reason purge。Report 见 [`reports/2026-05-20-receive-side-loop-rearchitecture.md`](reports/2026-05-20-receive-side-loop-rearchitecture.md)。Cloud/Home trace 待人工回归。 |
| 2026-05-19-tauri-updater-github-release-integration | Tauri 应用内更新与 GitHub Release 集成：用 `tauri updater` 打通 `Release -> latest.json -> 应用内检查更新 -> 下载安装重启` 单一路径 | In Progress | 2026-05-19 | 复杂任务。RFC：[`rfcs/2026-05-19-tauri-updater-github-release-integration.md`](rfcs/2026-05-19-tauri-updater-github-release-integration.md)。Report：[`reports/2026-05-19-tauri-updater-github-release-integration.md`](reports/2026-05-19-tauri-updater-github-release-integration.md)。Phase 1 已落地：updater 模块、设置页通用区、release-beta/stable workflow；待推送 `release/test` 与 `v*` tag 做 E2E。 |
| 2026-05-15-browser-render-policy-compiler-unification | 浏览器侧 render policy compiler 收口：借鉴 Rust `Config -> Context -> Plan -> Projection` 模型，把浏览器 renderer 选择、SR 状态、display degrade 与 diagnostics 投影统一为二段策略编译合同 | In Progress | 2026-05-15 | 复杂任务。RFC：[`rfcs/2026-05-15-browser-render-policy-compiler-unification.md`](rfcs/2026-05-15-browser-render-policy-compiler-unification.md)。当前目标：1）新增 `BrowserRendererPlan` 与浏览器本地 render policy compiler；2）把 `browser-runtime.ts` 中散落的 `RendererRuntimeConfig` patch 收口为无 Auto 的计划；3）收拢 SR runtime state；4）让 `PlaybackService` 消费窄 `RendererAttachSpec`；5）按需扩展 Rust `RenderPlanProjection`，保持启动期公共策略与浏览器运行期事实分层。已推进：`PlaybackService/MediaService/PlayerClient` 新增批量 `updateRendererState(config, attach)`，浏览器 runtime 改为一次性下发 renderer config + attach；`requestVideoFrameCallback/timeupdate` 帧呈现元数据改由 player 侧统一发出 `media.videoFramePresented`，runtime 不再重复绑定浏览器跟踪器；display viewport 新增输出 DPR / 像素上限约束，browser render policy 在 display degrade 路径下显式约束 present target；WebGL uniform location 改为初始化缓存，避免每帧重复查找；相关 runtime / render / viewport 测试已补齐。 |
| 2026-05-14-dynamic-rtt-aware-recovery-timing | 动态 RTT 感知恢复时序：将 `NACK/PLI/FIR` 时序从静态阈值收成统一动态解析，并把 `NACK` 主策略改为“首发等待窗放宽优先、重试窄补充” | Planned | 2026-05-14 | 复杂任务。RFC：[`rfcs/2026-05-14-dynamic-rtt-aware-recovery-timing.md`](rfcs/2026-05-14-dynamic-rtt-aware-recovery-timing.md)。当前目标：1）引入 `RecoveryTimingResolver` 统一解析 `nack/pli/fir/commit-hold` 动态时序；2）把高价值缺口从“短窗首发 + 一次补发”收成“首发 survival window 放宽优先”；3）拆开 `nack_missed` 与 `wait_keyframe/request_idr` 语义；4）补齐 `firstAttemptSurvivalWindowMs / retrySuppressedReason / effectiveRttMs` 观测字段。 |
| 2026-05-14-gamepad-tauri-active-gate-and-always-on-sampling | Gamepad 输入线路收口：极简 backend gate + FSE 专线 + 精简前端 routing | In Progress | 2026-05-14 | 复杂任务。RFC：[`rfcs/2026-05-14-gamepad-tauri-active-gate-and-always-on-sampling.md`](rfcs/2026-05-14-gamepad-tauri-active-gate-and-always-on-sampling.md)。Report：[`reports/2026-05-19-gamepad-gate-fse-and-routing-line.md`](reports/2026-05-19-gamepad-gate-fse-and-routing-line.md)。已落地：gate=`lifecycleActive&&foregroundCandidate`、`fse_windows`、移除 500/2000/4000 主链、`business-input-arbiter`+`stream-input-route-controller`、neutral wait 可取消、SDL3 source 低频重枚举、FSE 触屏前台宽限与 interactive hint、物理设备/keyboard fallback 诊断与 self-heal 条件收紧。本轮继续收口：Tauri `lib.rs` 只分发 page-load / focus / resize / resumed 事件；采样恢复、gate、FSE 诊断与 nudge 调度进入 `shell/gamepad_sampling.rs`；Stream UI capture 队列收成 Promise tail + `beginUiCapture()`，会话 active 切换统一走 `streamInputRouteController.setStreamActive()`，执行层不再直接 patch arbiter；AppShell 触屏 interactive hint RPC、节流和 trace 更新收进 `useGamepadShellInteractiveHint()`；对照 chiaki-ng 后补齐 SDL3 source 输入运行时条件：`SDL_SetMainReady()`、background / PS4 rumble / PS5 rumble / SteamDeck HIDAPI hints、joystick+gamepad events 双开启、初始化枚举前主动 update、poll loop 增加 `SDL_UpdateJoysticks`、启动前 2 秒 100ms 快速重枚举与初始化 trace。验证：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`、`cargo test -p xbxrc startup_rearm_ignores_keyboard_fallback_only_snapshot`、`cargo test -p xbxrc foreground_refresh_still_attempts_self_heal_when_active_gate_is_open_but_sampling_stalled`、`./node_modules/.bin/vitest run src/components/layout/useGamepadShellInteractiveHint.test.ts src/pages/stream/stream-input-route-controller.test.ts src/shared/gamepad/business-input-arbiter.test.ts`、`pnpm exec eslint src/components/layout/AppShellLayout.vue src/components/layout/useGamepadShellInteractiveHint.ts src/components/layout/useGamepadShellInteractiveHint.test.ts src/pages/stream/stream-input-route-controller.ts src/pages/stream/stream-input-route-controller.test.ts src/streaming/useStreamExecution.ts src/streaming/xstream-page-ui.ts --fix`、`cargo check -p ohmygamepad-sdl3`、`cargo test -p ohmygamepad-sdl3 sampling_self_heal_requires_non_keyboard_connected_device`、`cargo test -p ohmygamepad-sdl3`、`cargo check -p xbxrc`、`git diff --check`；`pnpm lint:fix` 与 `pnpm exec vue-tsc --noEmit` 仍受无关既有错误阻塞。待办：Windows FSE 实机 Validation 勾选；`cargo xwin check -p xbxrc --target x86_64-pc-windows-msvc` 仍受本机 CMake/Ninja/Opus 交叉构建环境阻塞。 |
| 2026-05-13-transport-await-anchor-simplification | `transportAwaitRecoveryAnchor` 收边界与简判定：把显示供给压力、等待 usable IDR、episode 健康度、connectivity 升级收成少量主状态与轻量观测标签 | In Progress | 2026-05-13 | 复杂任务。RFC：[`rfcs/2026-05-13-transport-await-anchor-simplification.md`](rfcs/2026-05-13-transport-await-anchor-simplification.md)。当前目标：1）把入口从“显示压力直达 await-anchor”收成 `Stable -> Suspect -> AwaitAnchor`；2）把 `keyframe in-flight` 从布尔占位收成 `waiting-response / continuation-only / stalled` 三档健康度；3）让 `coalesced:keyframeInFlight` 只覆盖真正等待响应阶段；4）保持 reconnect 只由 connectivity 硬证据驱动；5）补齐 trace 字段，直接回答进入原因、卡住原因、未 reconnect 原因。新增约束：先收 owner 解释权，不直接补动作分支；`ContinuationOnly` 必须升格为主 blocker，`localRepairPending` 必须降回局部执行态，`displaySupplyStarved` 只保留入口职责。 |
| 2026-05-12-stream-panel-information-architecture-split | 串流面板信息架构重构：用户层收敛为共享体验指标面板，内部诊断拆为 browser/rust 两条独立面板与独立 view model | In Progress | 2026-05-12 | 复杂任务。RFC：[`rfcs/2026-05-12-stream-panel-information-architecture-split.md`](rfcs/2026-05-12-stream-panel-information-architecture-split.md)。当前目标：1）把诊断面板与性能面板中的用户可理解体验指标收敛为单一共享面板；2）把恢复链、renderer、control channel、decoder、RFC fault 等内部字段迁移到 browser/rust 分离的诊断面板；3）停止在统一大组件中按 `runtimeMode` 透传分支；4）收口新的共享体验 view model 与 browser/rust 独立诊断 view model。 |
| 2026-05-12-browser-fsr1-super-resolution-experimental | 浏览器侧实验性超分线路：新增独立 `SuperResolutionWebGL2Renderer`，固定 `FSR1 EASU + low RCAS`，输出 tier 以 configured target 为上限，启用后固定使用 SR，仅在技术性失败时回退普通 `webgl2 + CAS/USM` | In Progress | 2026-05-12 | 复杂任务。RFC：[`rfcs/2026-05-12-browser-fsr1-super-resolution-experimental.md`](rfcs/2026-05-12-browser-fsr1-super-resolution-experimental.md)。当前目标：1）把分辨率重建与标准锐化语义拆开；2）固定 `720 -> 1080`、`1080 -> 1080(同档 RCAS-only)`、`1080 -> 1440(仅当 configured target >= 1440)`、`1440 -> 1440(同档 RCAS-only)` 映射；3）取消运行期动态 SR 开关与动态 scale；4）补齐 renderer 选择、diagnostics 与失败回退合同。 |
| 2026-05-12-transport-repair-and-recovery-semantic-unification | 补包到恢复语义统一：固定低/中/高三档缺包价值与 `drop / nack_pending / nack_missed / wait_keyframe / request_idr` 单一阶段机，打通 budget、transport、timeline、owner、recovery 的一致合同 | In Progress | 2026-05-12 | 复杂任务。RFC：[`rfcs/2026-05-12-transport-repair-and-recovery-semantic-unification.md`](rfcs/2026-05-12-transport-repair-and-recovery-semantic-unification.md)。当前目标：1）把 small gap 从“任何活跃 gap 都会升成恢复压力”收回到“先区分低价值、本地可修补、明确坏链”三层；2）为低价值直接丢弃、中等价值先本地 NACK、高价值才等待关键帧/请求 IDR 建立统一合同；3）拆掉 `gap-repair-in-flight -> AwaitingRecoveryKeyframe` 与 `timeline.gap.is_some() -> ReferenceGap` 这两条过早升级路径；4）让 `NackDeadlineExpired` 携带原始预算上下文，避免 recovery 层只读被升格后的全局 timeline；5）在 trace 与 tests 中统一输出 `value_tier / risk_tier / action_stage / evidence_scope` 四维语义。 |
| 2026-05-11-settings-schema-driven-reorganization | 设置页按展示 schema 重组：保留配置存储与字段定义主线，在前端新增设置展示模型，按用户任务重组导航、section、工具入口与危险区 | In Progress | 2026-05-11 | 复杂任务。RFC：[`rfcs/2026-05-11-settings-schema-driven-reorganization.md`](rfcs/2026-05-11-settings-schema-driven-reorganization.md)。当前目标：1）保持 `rpc.config.getGroups/set`、配置 key、字段定义和存储协议稳定；2）新增独立于 config group 的设置展示 schema；3）按“通用 / 串流体验 / 连接与主机 / 输入设备 / 高级与诊断”重组页面；4）收口 `input tools`、`expert reset`、危险操作、重启生效提示等特殊项；5）移除空 `xcloud` 顶层页并消除展示层重复字段。 |
| 2026-04-30-post-decode-value-aware-latest-mail-alignment | 解码后 `latest-mail` 价值对齐：在现有 latest-only 主线下补齐 `presentation_role / recovery_epoch / owner_rtp` 单一合同，并把 pacer 收口为 decode 后唯一价值决策层 | In Progress | 2026-04-30 | 复杂任务。RFC：[`rfcs/2026-04-30-post-decode-value-aware-latest-mail-alignment.md`](rfcs/2026-04-30-post-decode-value-aware-latest-mail-alignment.md)。当前目标：1）把问题显式拆成 `decode` 前恢复链缺主 与 `decode` 后价值模型失配 两段；2）保留 single-track latest-only，不引入双分叉；3）定义 `fresh_anchor / recovery_continuation / steady_continuation / disposable` 统一 `presentation_role`；4）让 pacer 成为 decode 后唯一价值收口层；5）让 `supersededAfterDecode / mailboxOverwrite` 输出 kept/dropped 双边价值合同。 |
| 2026-04-29-playback-recovery-single-line-convergence | 播放期恢复单线收敛：将 render/host 的有价值呈现提升为 recovery 主线正式阶段，并与 keyframe family gate 两条已知修复一起完成 | In Progress | 2026-04-29 | 复杂任务。RFC：[`rfcs/2026-04-29-playback-recovery-single-line-convergence.md`](rfcs/2026-04-29-playback-recovery-single-line-convergence.md)。当前目标：1）将恢复主线从“fresh usable IDR 唯一完成线”改为单线分层：`WaitingResponse -> ContinuationSeen -> PlaybackRecovered -> FreshAnchorRecovered -> DisplayStable`；2）保留 `bootstrap` 与 `cleanAnchorCommitted` 的硬语义，不把 continuation 伪装成 fresh anchor；3）把 render/host 已承认的 serviceable continuation 证据提升到 `session/facts` 主线，避免播放恢复与恢复状态分叉；4）同步修复 `decoded_keyframe_without_clean_anchor_does_not_hold_family_gate_after_hold_window` 与 `non_idr_vcl_keyframe_response_does_not_hold_family_gate` 两条 keyframe family gate 缺口；5）拆开 playback recovered 与 fresh-anchor recovered 的 stats/trace 口径。最新补丁：reconnect 候选改为执行期才进入 state-machine in-flight 并消费预算，proposal/suppressed 阶段不再提前记账；`RecoveryBudget.consume_reconnect()` 改为 saturating 计数，阻断 `reconnect_used` 溢出击穿 decode actor；`runtime_stats_sink` 现已把 `transportAwaitRecoveryAnchor + transportExpiredDeadline` 收成统一 transport recovery keyframe family，continuation/blocker/clean-anchor submission 都会跟随当前 decoded owner 前移，避免旧 episode 长期持有 media gate。 |
| 2026-04-24-post-decode-latest-only-mailbox-convergence | 解码后 latest-only mailbox 收敛：将 `decode -> pacer -> render -> host present` 从排队系统改为最新候选链，顺序性只保留在 decode 前，并把 `rx closed / feedbackTargetPending / twcc mapping missing / pump exit / feedback target availability` 收成统一控制面故障链 | In Progress | 2026-04-24 | 复杂任务。主 RFC：[`rfcs/2026-04-24-post-decode-latest-only-mailbox-convergence.md`](rfcs/2026-04-24-post-decode-latest-only-mailbox-convergence.md)。子 RFC：[`rfcs/2026-04-28-pacer-single-decision-and-host-execution-chain.md`](rfcs/2026-04-28-pacer-single-decision-and-host-execution-chain.md)。当前目标：1）把 decode 输出收成 `current + latest_candidate`；2）把 pacer 收成 latest-only 准出控制器，并进一步收权为 decode 后唯一主决策层；3）把 render/runtime/host handoff 收成单拍 latest 交付，其中 `render/present` 执行化、`host` 只负责上屏与 visible fact；4）把本地 drop 语义从 `outputQueueOverflow/staleAfterDecode` 收成 `superseded* / missedDisplayDeadline / localHardCapProtection`；5）保留 `cleanAnchorCommitted -> DisplayStable` 两层 gate 与 `post-IDR climbing / clean-anchor` 画像策略，并将 `CleanAnchorCommitted` 钉到 host visible 合同；6）给 `upstreamSenderDropped` 增加结构化上游 cause；7）把 `videoRtcpFeedbackTargetPending`、TWCC mapping 缺失、pump exit 建立统一 episode 关联；8）把 `feedback target availability` 升级为 PLI/NACK/TWCC 共用的单一状态机。当前已推进：`renderer` latest-slot 与宿主 `pending/displayed` 链路已收成 mailbox 保护规则，删除 displayed 对 pending 的二次价值否决；owner 锚点待上屏期间继续受保护，普通 pending 继续 latest 覆盖；`CleanAnchorCommitted` 已收紧为 host visible 合同，仅当当前 recovery epoch 存在 clean-anchor submission 且 displayed RTP 与 submission RTP 精确匹配时才记账；`runtime_port -> runtime stats -> diagnostics/protocol/frontend snapshot/trace_projection` 的 host-present 指标已统一切到 `hostMailbox* / hostFramePresentEpoch` 新语义；`display_supply / session facts / render pacing` 的局部 `present_* / queue pressure` 命名也已同步切到 mailbox 口径。此前已补齐：TWCC RTCP 发送成功/失败并入 availability 状态机、receiver 重新绑定时即时回写 `ready`、availability 字段贯通 `protocol/diagnostics/trace_projection`，trace 不再依赖 `latest_observation` 单槽投影该状态。最新补丁：reconnect-settled keyframe 现在要求 transport 已连通才会发起，`videoRtcpFeedbackTransportNotReady` 保留为 deferred 语义，避免在 `ICE pair/SRTP` 未就绪窗口放大恢复失败；clean-anchor submission 已显式绑定 `episode_id`，host-present commit 与 decode 提交都按同一 episode/RTP 校验，旧恢复帧不会再跨等待周期串单；macOS wgpu presenter 现已把 immediate tick 收成 `pending + rerun_requested` 单一 dispatch 语义，pending 窗口内的新 mailbox accept 会补发下一拍，host view 重建后不会再吞掉首个 accepted frame。新增确认：`runtime-trace-1778662012335-1.jsonl` 已定位当前主瓶颈在 `runtime.tick -> take_latest_render_frame()` 拉取桥，`renderer submit` 长期远高于 `hostMailboxAccepted` 且 reject/overwrite 接近零；后续实现按“去 runtime pull、改 pacer 直推 host mailbox”收口。 |
| 2026-04-23-skills-sync-upgrade | 技能同步机制升级：统一 `skills:sync/skills:lock/skills:verify` 命令，增加本地哈希校验与锁文件回写 | In Progress | 2026-04-23 | 小任务。已新增 [`scripts/sync-skills.mjs`](/Users/guo.xu/Documents/code/games/xbxrc/scripts/sync-skills.mjs) 并接入 `package.json`；`skills:verify` 基线结果：`asir` hash 漂移、`use-spatial-navigation` 本地缺失。后续执行 `skills:sync -- --from-repo <repo-path>` 后再 `skills:verify` 闭环。 |
| 2026-04-22-sdl3-gamepad-single-track-backend-unification | SDL3 Gamepad 单轨后端统一：以 `SDL3::gamepad` 收敛桌面物理手柄接入层，并移除 `gilrs/XInput/WinRT` 长期双轨主线 | In Progress | 2026-04-22 | 复杂任务。RFC：[`rfcs/2026-04-22-sdl3-gamepad-single-track-backend-unification.md`](rfcs/2026-04-22-sdl3-gamepad-single-track-backend-unification.md)。当前状态：真实 SDL3 `event/source/backend/runtime/service` 主链已落地，运行时/协议/前端契约已切到 `sdl3 + slot + input_policy` 语义，`ohmygamepad-core` 相关 runtime tests 与 `cargo check -p xbxrc` 已通过。剩余工作：1）Windows/macOS 实机输入与 rumble 验收；2）补最终 Report；3）完成归档出当前页。 |
| 2026-05-11-gamepad-runtime-warm-sampling-and-stalled-self-heal | Gamepad runtime 常驻采样与 stalled 自愈：将壳层手柄恢复从事件补偿升级为 runtime 三态、BackgroundWarm 无副作用保温和后端自愈主线 | In Progress | 2026-05-11 | 复杂任务。RFC：[`rfcs/2026-05-11-gamepad-runtime-warm-sampling-and-stalled-self-heal.md`](rfcs/2026-05-11-gamepad-runtime-warm-sampling-and-stalled-self-heal.md)。**已实现**：core 三态与 `samplingHealth`/stalled 启发式；SDL service 节流 `try_stalled_sampling_self_heal`；Tauri 仅 `Active` 广播 `slotSnapshot` + Warm→Active `inputBaselineAbsorbed`；窗口 lifecycle hint；AppShell 单次恢复。**待办**：Xbox 大屏 / Game Bar 实机按 RFC Validation 勾选验收。 |
| 2026-05-12-gamepad-lifecycle-and-input-routing-simplification | Gamepad lifecycle 与输入路由收口：固定“SDL3 runtime 常驻采样 + shell 唯一协调 `BackgroundWarm/Active` + 前端只消费采样”，并把 `inputPolicy` 从 runtime 合同降级为待删除字段 | In Progress | 2026-05-12 | 复杂任务。RFC：[`rfcs/2026-05-12-gamepad-lifecycle-and-input-routing-simplification.md`](rfcs/2026-05-12-gamepad-lifecycle-and-input-routing-simplification.md)。当前目标：1）明确公开 lifecycle 只保留 `BackgroundWarm/Active` 两态，不再把停机语义混入主合同；2）把“输入是否可用”的唯一硬信号固定到真实 logical progress，而不是 lifecycle / baseline；3）让 shell 成为唯一 lifecycle/self-heal 协调层，删除前端 recovery 状态机；4）把 `shared/ui-only/stream-only` 从 runtime 主合同降级为前端消费层路由能力，并规划兼容迁移路径。 |
| 2026-04-22-recovery-action-single-track-cutover | 图片级恢复动作单轨切换：删除 `RequestKeyframe`，收口为 `PLI` 主路径与 `FIR` 重保底，并让 display 退出图片级恢复动作链 | In Progress | 2026-04-22 | 复杂任务。RFC：[`rfcs/2026-04-22-recovery-action-single-track-cutover.md`](rfcs/2026-04-22-recovery-action-single-track-cutover.md)。当前目标：1）删除 `RecoveryAction::RequestKeyframe`；2）固定控制动作链为 `NACK -> PLI`，`FIR` 只做重保底；3）固定恢复进度链为 `WaitingResponse -> ContinuationSeen -> AnchorSeen -> Decoded -> CleanAnchorCommitted -> DisplayStable`；4）删除常规 `control keyframe` 主链；5）删除 `display -> RequestKeyframe` 旧合同；6）将远端画像、`post-IDR climbing`、clean-anchor 宽容统一收成 phase policy 参数层；7）让 `session/owner/coordinator/trace` 全部改成显式 `RequestPli / RequestFir / RequestDecoderReset / RequestReconnectCandidate` 单轨动作语义。当前已把 recovery close 收口为两层 gate：`cleanAnchorCommitted -> DisplayStable`，`ramp_guard` 不再保留独立 settle 语义，`stableServingSettled` 只作为 `DisplayStable` 的落账事件名；`has_fresh_media_output` 与 `host_presentation_serviceable` 口径统一收成 `300ms`；decode/pacer/render 已改成恢复期阶段化预算，`staleAfterDecode` 阈值已放宽，session watchdog 已把 `outputQueueOverflow/staleAfterDecode/hostPresentStalled` 先归入本地 media self-healing，reconnect 仅在 transport 硬证据成立时升级。验证已通过：`cargo check -p xbxengine`、`cargo test -p xbxengine session::recovery -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor -- --nocapture`、`cargo test -p xbxengine recovery_signals_hold_keyframe_on_local_pipeline_stall_without_transport_evidence -- --nocapture`、`cargo test -p xbxengine enqueue_decoded_frame_recovery_window_uses_deeper_queue_budget -- --nocapture`、`cargo test -p xbxengine recovery_window_frames_allow_deeper_local_buffer_before_release -- --nocapture`。 |
| 2026-04-24-recovery-trace-collection-convergence | 恢复链 trace 采集收口：补 `pictureRecoveryTransition / pictureRecoveryBlockerObserved / videoIngressTermination / firstFrameLatencyObserved` 结构化事件，并统一 `NonIdrVcl` 与 `rx closed` 因果标签 | In Progress | 2026-04-24 | 小任务。实现路径：在 `runtime_stats_sink` 收敛 observation、在 `diagnostics/stats` 暴露 DTO、在 `trace_projection` 投出独立 event；要求保留两层 gate 语义，避免继续依赖字符串拼接和多处投影反推。当前新增收口：1）`hostPresentStalled` 仅在 present loop 已运行、且 `300ms` 内没有 fresh renderer submit / host render slot / decode / clean anchor 时触发 presenter reset，避免 reset 后窗口继续自击穿；2）`firstFrameLatencyObserved` 在已 decode 时优先报告 `noCleanAnchorCommit/noDisplayStable`，不再被 `missingPliSent` 覆盖；3）新增 `hostFramePresentResumed` 派生事件，分离 `renderer accepted` 与 host present 恢复。 |
| 2026-04-19-phase-aware-recovery-and-dynamic-repair-policy | 阶段化恢复进度与动态修复价值策略：收敛 `NonIdrVcl`、NACK、clean anchor、display completion 的长期语义边界 | In Progress | 2026-04-19 | 复杂任务。RFC：[`rfcs/2026-04-19-phase-aware-recovery-and-dynamic-repair-policy.md`](rfcs/2026-04-19-phase-aware-recovery-and-dynamic-repair-policy.md)。当前目标：1）把 recovery 主链收敛为六级恢复进度；2）把修复价值收敛为四档并允许按阶段重映射；3）明确 media recovery complete 与 display recovery complete 的分层边界。 |
| 2026-04-18-post-decode-display-scheduling-and-media-recovery-decoupling | 重新设计播放期调度边界：解码后显示链与媒体恢复解耦，采用 Moonlight 式 `release-clock + local drop` | In Progress | 2026-04-18 | 复杂任务。RFC：[`rfcs/2026-04-18-post-decode-display-scheduling-and-media-recovery-decoupling.md`](rfcs/2026-04-18-post-decode-display-scheduling-and-media-recovery-decoupling.md)。当前已把 `renderer` 收成 shadow + staging，并把 `display_supply` / runtime local reset / `MediaReady` / owner / session / `recovery/runtime_state` / `remote_profile_runtime` / `startup` / runtime `lifecycle` / `diagnostics+protocol+frontend snapshot` 全部收进 host-first telemetry 判据。 |
| 2026-04-21-degrade-host-timing-driven-decoder-reset-and-release-gate | 退化 host timing 驱动的 decoder reset 与 host release gate，优先稳定恢复后的连续供给 | In Progress | 2026-04-21 | 复杂任务。RFC：[`rfcs/2026-04-21-degrade-host-timing-driven-decoder-reset-and-release-gate.md`](rfcs/2026-04-21-degrade-host-timing-driven-decoder-reset-and-release-gate.md)。当前按方案 2 收缩控制环，继续验证 decode/pacer 本地缓冲、recovery 本地窗口与 host telemetry 的稳定性。 |
| 2026-04-21-decode-pacer-deadline-budget-decoupling | 解码与 Pacer 的 deadline/budget 解耦：从队列状态双向驱动收敛为 decode 产出、pacer 唯一调度 | In Progress | 2026-04-21 | 复杂任务。RFC：[`rfcs/2026-04-21-decode-pacer-deadline-budget-decoupling.md`](rfcs/2026-04-21-decode-pacer-deadline-budget-decoupling.md)。目标：1）decode 退出以“队列是否非空”为主的节奏控制；2）pacer 收敛为唯一 release governor；3）以 frame age / playout budget 取代 queue depth 作为主判据。 |
| 2026-04-21-build-dependency-hermeticization-openssl-ffmpeg | 构建依赖封装化：移除 OpenSSL 外部构建依赖，并把 FFmpeg 收敛为仓库内固定 SDK 与随包分发 | In Progress | 2026-04-21 | 复杂任务。RFC：[`rfcs/2026-04-21-build-dependency-hermeticization-openssl-ffmpeg.md`](rfcs/2026-04-21-build-dependency-hermeticization-openssl-ffmpeg.md)。已完成 OpenSSL->`p256` 替换与依赖清理，剩余为真实 SDK 资产下的双平台打包验收。 |

## Current Task Notes

- 2026-06-29: 复盘最新 fresh trace `runtime-trace-1782719191984-1.jsonl`：组合 gate 仍 FAIL，但 receive gate PASS、ingress queue PASS，中段 `submit_age_ms P95=52ms`、`present_age_ms P95=54ms`、`decode/present≈29.7/29.5fps`，尾段已消除上一轮的秒级 host present 停摆；`present_tick_dispatch_rescued=8` 后 `hostFramePresentEpoch` 继续推进，说明 stale pending rescue 生效。用户感知的最终卡住收敛为 rescue 后的低频呈现：尾段 `hostMailboxAccepted/overwrite` 持续增长，`present_tick_dispatch_rescued` 约每 250ms 出现一次，`presentFps≈4`，根因是 macOS layer 的 `immediateSubmit` 抢占全局 pending 后把 tick 投到主线程，主线程回调晚到时 displayLink 只能 coalesce，最终由 250ms watchdog 打穿。修复：displayLink 已启动时 `immediateSubmit` 只记录采样级 `present_tick_immediate_deferred` 并交给 displayLink 正常节拍消费，避免占用 pending；`present_tick_immediate_deferred` 与 `present_tick_dispatch_rescued` 诊断补齐 `presentAgeMs`、`submitAgeMs`、`latestPresentTimeMs`、`latestSubmitTimeMs`、`displayIntervalMs`、pending/rerun、epoch 与 mailbox counters，下一份 trace 可直接判断是否还退化为 watchdog 轮询。验证：`cargo fmt`、`cargo test -p xbxrc --lib native_video -- --nocapture`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：displayLink 可用时 `present_tick_immediate_deferred` 出现，`present_tick_dispatch_coalesced/rescued` 明显下降，尾段 `presentFps` 回到约 30fps，`presentAgeMs` 脱离 250ms sawtooth。
- 2026-06-29: 复盘最新 fresh trace `runtime-trace-1782718064605-1.jsonl`：组合验收 PASS，中段稳态 `decode/present≈30fps`、`submit/present P95≈53ms`，但尾段仍出现最终 host present 卡死；最后 1.5-2s `hostMailboxEnqueue 3150→3172`、`submitAgeMs≈7-33ms` 持续推进，`hostFramePresentEpoch=3116` 与 `hostDisplayTickEpoch=19208` 停住，`presentAgeMs` 拉到 2044ms；最后一个 `present_tick_dispatch_coalesced source=displayLink` 后没有 `present_tick_rerun`，说明 `render_loop_pending` 卡住导致 displayLink/fallback loop 后续只 coalesced。修复：macOS layer displayLink 与 fallback loop 在 coalesced 且上次 host present 超过 250ms 时清理 stale pending 并重派发 tick，新增 `present_tick_dispatch_rescued` 诊断；保持新鲜 present 下的 coalesced 语义不变。验证：`cargo fmt`、`cargo test -p xbxrc --lib stale_host_present_tick_dispatch_rescue -- --nocapture`、`cargo test -p xbxrc --lib native_video -- --nocapture`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：尾段出现 `present_tick_dispatch_rescued` 后 `hostDisplayTickEpoch/hostFramePresentEpoch` 继续推进，`presentAgeMs` 不再秒级增长。
- 2026-06-29: 继续按旧最新 trace `runtime-trace-1782453170367-1.jsonl` 自查出图停滞，当前日志仍非 fresh，组合 gate 仍 FAIL，核心形态是 receive 已能闭合但 post-decode/display 供给卡死。修复同类历史状态误用：`playbackRecovered` 仅在 fresh host present 或当前 episode 时间线内参与 steady/first-frame/progress/continuation bridge；`recovery_exit_path` 的 DecodeOutput 需要 fresh playbackRecovered + fresh host present；`runtime_state` 的 display continuity 和 adapterIdleTimeout 吸收必须有当前 host present 且 decode/present 不拉开；`VideoSchedulingOwner` 不再用累计 `host_frame_present_epoch > 0` 证明当前 host steady，receive-ledger 完成遇到明确 stale present 会继续保持 recovery，不让 clean anchor / media continuity 覆盖显示停滞。新增回归覆盖 stale playbackRecovered 不保持 steady、不退出 recovery、不算 first frame acquired、不推进 session facts、不绑定新 episode continuation，以及 stale present epoch 不释放 owner。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::runtime_state --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::decode_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::facts --lib -- --nocapture`、`cargo test -p xbxengine runtime_stats_sink --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`。待 fresh trace 复验：`submit_age_ms/present_age_ms` 长尾、`hostMailboxAccepted/hostFramePresent` 连续性、`localBackpressureBestEffortOverflow` streak、owner state 是否仍被旧 clean-anchor/playbackRecovered/present-epoch 压成 serving。
- 2026-06-29: 继续按最新 trace `runtime-trace-1782453170367-1.jsonl` 自查出图停滞：receive gate PASS，但 combined gate FAIL，`submit_age_ms P95=4393ms`、`present_age_ms P95=4392ms`、`localBackpressureBestEffortOverflow` 连续 613，失败仍集中在 post-decode/display 与状态误判。修复同类 displayed-idr 历史事实误用：`has_current_clean_anchor_from_stats()` 不再把 `recovery_displayed_idr_at_ms` 当作 current clean anchor；H264 `receiverLocalContinuation` blocker、remote terminal reconnect、coordinator progress、owner post-clean grace、receive recovery completion 都改为 current clean anchor / fresh anchor / observed-at 明确闭环；displayed-idr 继续只作为 serving/projection 事实。新增回归覆盖 stale displayed-idr 不算 current clean anchor、不压 blocker、不屏蔽 remote terminal reconnect、缺 clean-anchor observed-at 不闭合 recovery。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive --lib -- --nocapture`、`cargo test -p xbxengine runtime_reconnects_when_only_displayed_idr_fact_refreshes_after_remote_terminal --lib -- --nocapture`、`cargo test -p xbxengine runtime_reconnects_after_remote_continuation_only_even_with_stale_clean_anchor --lib -- --nocapture`、`cargo test -p xbxengine rust_owned_runtime_reconnects_when_receive_remote_terminal_persists_while_packets_arrive --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。待 fresh trace 复验：current clean anchor / DisplayStable 闭环不再由旧 displayed-idr 投影伪造，remote terminal 与 receiverLocalContinuation blocker 能继续暴露，post-decode submit/present 长尾和 ingress overflow 收敛。
- 2026-06-29: 继续自查首帧/恢复阶段的累计计数残留：`display_supply::host_is_priming_without_present()`、`VideoSchedulingOwner::first_present_feedback_gap_active()` 仍要求 `host_mailbox_enqueue_count_total == 0`，`diagnostics::resolve_presentation_health()` 也把 enqueue 累计值当作 present history；新 view / recovery epoch 中仅有历史 enqueue 残留时，会把“尚未 present 的首帧 feedback gap”误读成已有显示历史或供给故障。修复：首帧 gap 只看 display tick 已推进且 present epoch 仍为 0，enqueue count 仅作遥测；receive ledger 已闭合但首帧 present feedback 尚未回来时只释放到 `DegradedServing`；presentation health 只把真实 host present time 当作 present history。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::policy::display_supply --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::stats --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-29: 继续自查 `runtime_port` host visibility stall 的 RTP 对比：旧逻辑直接比较 `Option<submit_rtp> != Option<displayed_rtp>`，当 submit RTP 缺失但 displayed RTP 已知时会把 `None != Some(..)` 当成“当前 submit 未显示”，从而伪造 host stalled。修复：仅在 `latest_host_submit_rtp_timestamp=Some` 且与 last displayed RTP 不一致时认定 pending display；缺失 submit RTP 只作为缺失证据，不构成当前 pending。验证：`cargo fmt`、`cargo test -p xbxengine host_visibility_stall --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-29: 继续自查 `present_cadence` 的 host serviceable 判据：旧逻辑要求 `host_mailbox_enqueue_count_total >= host_frame_present_epoch`，但 host view replay 会重放已 displayed 帧并推进 present epoch，可能让 present 总数领先 enqueue；这属于宿主可服务状态，不应阻断 present pipeline stressed 识别。修复：`host_mailbox_serviceable()` 改为使用当前事实 `host_frame_present_epoch > 0 + cadence=steady + no_pending_streak=0`，去掉累计计数顺序依赖；新增 view replay present epoch ahead 回归。验证：`cargo fmt`、`cargo test -p xbxengine media::video::present_cadence --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-29: 继续自查 `2026-06-26-runtime-frame-rtp-rewind-audit` 的 post-decode/display 供给误判：`display_supply` 之前把 high drop/overwrite ratio 单独升级为 HardSupplyDrop，即使 `present_age_ms/decode_age_ms` 都很新鲜也会把 latest-only 丢旧帧当成供给硬故障。修复：drop/overwrite ratio 必须结合 present/decode age 压力才升级 degraded/critical；fresh latest-only overwrite 保持 Healthy，stale age + heavy overwrite 仍为 HardSupplyDrop。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::policy::display_supply --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-29: 继续按旧最新 trace `runtime-trace-1782453170367-1.jsonl` 自查出图停滞，日志仍非 fresh，组合 gate 仍 FAIL：receive gate PASS，`submit_age_ms/present_age_ms P95≈4.39s`，`localBackpressureBestEffortOverflow` 连续 613。修复同类过宽首帧/显示事实放行：`VideoSchedulingOwner` 将“检测到首帧 present feedback gap”和“可短暂 bridge 的首帧 gap”拆开，只有 `priming + host enqueue 已发生 + no_pending_streak<=2 + pressure 非 high/critical` 才能让 receive ledger 完成后释放到 serving；检测到 present=0 且 feedback gap 不可 bridge 时保持 `RebuildingSupply`，避免 no-pending critical 或无 enqueue 的窗口被 clean anchor 压成 Stable/Degraded。`displayed_idr_serving_from_stats()` 的 pending displayed-IDR 分支也必须有新鲜 host present age，累计 `host_frame_present_epoch` 不再单独证明当前 pending IDR 已可显示。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::facts --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::runtime_state --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib -- --nocapture`。待 fresh trace 复验：首帧/恢复 present gap 不再在 no-pending critical 下进入 playing，pending displayed-IDR 不再由旧 present epoch 伪装 serving，post-decode submit/present 长尾和 ingress overflow 收敛。
- 2026-06-29: 继续自查 current clean-anchor 边界，发现 `decoder_recovery_debt_cleared_by_anchor_from_stats()` 和 `has_current_clean_anchor_from_stats()` 仍直接接受裸 `recovery_fresh_anchor_recovered_at_ms`，依赖 stats sink 在新 episode 时清理；一旦旧 fresh anchor 残留，会把当前 `waiting-keyframe` / backend no-output 债错误清掉，并让 reference/insert 控制面回到 steady。修复：新增 `current_fresh_anchor_recovered_at_ms_from_stats()`，fresh anchor 只有已绑定当前 clean-anchor epoch，或发生在当前 recovery episode opened_at 之后才算 current；decoder waiting/no-output 清债与 current clean-anchor 判据统一复用该 helper。新增回归覆盖 stale fresh anchor 不能算 current clean anchor，不能清除当前 episode 的 decoder debt。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::facts --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::decode_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::insert_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::runtime_state --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib -- --nocapture`。待 fresh trace 复验：新 episode 中旧 fresh anchor 不再屏蔽 `NeedKeyframe/RequestIdr`，`referenceChainState` 与 `insertGateDecision` 不再被历史 clean-anchor/fresh-anchor 压成 continuous/emit。
- 2026-06-18: 补齐 reconnect 诊断字段，下一份 fresh trace 可按字段追踪 `policy -> stage -> runtime consume`：recovery ledger 输出 proposal reason/domain、runtime domain resolution 前后值、remote terminal promotion 与 reconnect gate detail；`rtcReconnectCandidateStaged/Rejected/StageFailed` 输出 observation/domain、pending observation/domain 与 stage outcome；`runtimeReconnectConsumed` 输出 reason/domain、transportRecovering、runtimeState 与 lastRecoveryActionAtMs。验证：`cargo fmt`、`cargo test -p xbxengine runtime_reconnect_reason_domain_keeps_transport_await_local --lib -- --nocapture`、`cargo test -p xbxengine connected_healthy_twcc_receiver_waiting_keyframe_does_not_emit_reconnect_command --lib -- --nocapture`、`cargo test -p xbxengine remote_terminal_transport_await_reconnect_promotes_connectivity_domain --lib -- --nocapture`、`cargo test -p xbxengine reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxrc trace_projection --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`、`git diff --cached --check`。
- 2026-06-09: 继续修复 fresh trace `runtime-trace-1780989727891-1.jsonl` 的稳态显示长尾：receive/network 已健康（`receiveFeedbackGate=PASS`、recovering pulse=0、`decodeGateEmit +2591`），剩余失败集中在 `submit_to_present_ms P95=128ms` 与局部 `hostMailboxSubmitGap=1280ms`。修复：`present_pipeline_stressed_from_stats()` 增加高 fps 延迟尖峰分支（`decode/present >=45fps` 且 submit/display age 长尾），pacer 在该形态下切到 host refresh 快速释放；`trace_midsegment_report.py` 新增 `frame_supply_deltas`，steady ready ratio 优先使用 `hostPresent/hostEnqueue` counter delta，健康起播窗口不再因为无 keyframe outcome 误 FAIL。验证：`cargo fmt`、`cargo test -p xbxengine media::video::present_cadence --lib`、`cargo test -p xbxengine media::video::pacer::actor --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、无缓存 Python 语法检查、`git diff --check`。Fresh trace `runtime-trace-1780990989976-1.jsonl` 组合验收 `acceptanceGate=PASS`，`GLOBAL_LATENCY_GATE=PASS`、`MEDIA_SUPPLY_GATE=PASS`、`STEADY_SUPPLY_GATE=PASS`，`submit_to_present_ms P95=12ms`。
- 2026-06-09: 修复最新 fresh trace `runtime-trace-1780916322424-1.jsonl` 暴露的稳态帧供给断流：receive gate 已 PASS 且 recovering pulse 为 0，但 midsegment `rtp_marker +114 / access_unit +91 / decode_gate_continue +91 / decode_gate_emit +0`，卡在 InsertGate 对 `bootstrapMissingIdr` continuation 的 `holdRepair/holdDefault`。修复：在 `referenceState=Continuous/Repairing`、`keyframeRequired=false`、已获取首帧、非 hard gap、非 post-PS strict、`supply_break_continuation=true` 时，允许软 missing-IDR continuation 进入 decoder；`NeedKeyframe` 和首帧前仍保持 hold。验证：`cargo fmt`、`cargo test -p xbxengine serviceable_anchor_supply_break_emits_soft_missing_idr_continuation --lib`、`cargo test -p xbxengine need_keyframe_keeps_holding_soft_missing_idr_continuation --lib`、`cargo test -p xbxengine transport::rtc::receive::insert_gate --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。仍待下一份 fresh trace 验收 `decode_gate_emit` 持续推进、`ready ratio` 恢复、`STEADY_SUPPLY_GATE=PASS`。
- 2026-06-08: 继续处理 `runtime-trace-1780652183550-1.jsonl` 暴露的持续修复态：网络 Direct / RTT 约 17-29ms / REMB 32Mbps 健康，瓶颈集中在 receive repairing/need-keyframe 反复覆盖已闭合 clean anchor。修复：active hard gap 的刷新型 keyframe 请求保留当前 picture window；`ReceiverTraceLedger` 在 clean anchor / decoder reference synced 且未 `keyframe_required` 时，把短暂 unresolved hard gap 保留为诊断 active gap，但 ReferenceChain 控制事实继续投 `Continuous`；NACK exhausted 和 unrecoverable frame 仍保持 `NeedKeyframe`。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::trace_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive::recovery_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。仍待 fresh trace 跑 `trace_webrtc_acceptance_gate.py --latest --max-age-seconds <n>` 验证 recovery pulses 与 continuous recovering streak 下降。
- 2026-06-08: 继续审计 active refresh 副作用：`note_keyframe_refresh_sent_for_active_gap()` 在当前 clean anchor / decoder reference synced 可吸收时只记录 ledger generation，不再增加 `unresolved_keyframe_request_count`，避免 refresh debt 阻断当前 anchor 的 `DisplayStable` stats replay 或触发 remote-no-usable-IDR 终端诊断；hard gap 请求仍通过 `note_keyframe_request_sent_for_hard_gap()` 打开新 picture recovery window。新增 `active_refresh_keeps_display_fact_replay_available_for_current_anchor` 覆盖 display fact 被清空后由当前 clean anchor 回灌。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::recovery_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive::trace_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive::ingress_state --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-08: 继续收紧 refresh-only 与 hard request 的窗口边界：当前 anchor 可吸收的 active refresh 不再写 `last_keyframe_request_sent_at_ms`，彻底避免 refresh-only 被 `terminal_reason_for_remote_no_usable_idr()` 当成 hard keyframe request 计时窗口；新增 `active_refresh_without_clean_anchor_does_not_open_remote_no_response_window` 覆盖仅有 decoder reference synced、尚无 clean anchor fact 的可服务场景。复查 InsertGate：主 hold 入口依赖 ReferenceChain，serviceable anchor 下 unresolved active gap 已投 `Continuous`，不会再进入 `need-keyframe -> mustIdrHold`。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::recovery_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive::trace_ledger --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo test -p xbxrc trace_projection --lib`、`cargo check -p xbxrc`、`./node_modules/.bin/eslint src/shared/rpc/xbxengine.ts src/streaming/runtime/xbxengine-runtime.ts src/streaming/types.ts src/player/domain/media.ts --fix`。
- 2026-06-08: 补齐调用侧 stats 窗口分离：`write_keyframe_request_outcome_stats()` 在 active hard gap sent 时先判断当前 anchor 是否可吸收 repair refresh；refresh-only 仍记录 `keyframeRequestOutcome=sent` 和 timeline，但不再写 `receive_keyframe_last_sent_at_ms`，避免 stats fallback 重新污染 sparse-IDR / terminal hard request 窗口。新增 `active_refresh_outcome_sent_does_not_reopen_hard_request_stats_window` 覆盖 executor outcome sent 与 hard request timestamp 分离。验证：`cargo fmt`、`cargo test -p xbxengine active_refresh_outcome_sent_does_not_reopen_hard_request_stats_window --lib`、`cargo test -p xbxengine transport::rtc::receive::ingress_state::feedback --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、定向 eslint。
- 2026-06-08: 补齐帧供给 counter 行为测试：`bootstrap_emit_path_records_frame_supply_counters` 覆盖 `rtp marker -> access unit -> decode gate emit`，`pre_first_frame_non_idr_continue_path_records_frame_supply_counters` 覆盖首帧前 non-IDR 被 decode gate hold 后的 continue 计数；旧 trace `runtime-trace-1780652183550-1.jsonl` 复跑组合 gate 仍为 `acceptanceGate=FAIL`，receive 为 `receiveFeedbackGate=PASS`，midsegment 失败项为 recovery pulses 5 与最长 recovering streak 10s，继续作为失败基线等待 fresh trace 验收。验证：`cargo fmt`、`cargo test -p xbxengine bootstrap_emit_path_records_frame_supply_counters --lib`、`cargo test -p xbxengine pre_first_frame_non_idr_continue_path_records_frame_supply_counters --lib`、`cargo test -p xbxengine transport::rtc::receive --lib`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-04: 新 fresh trace `runtime-trace-1780562354702-1.jsonl` 复盘：上一轮修正生效，`needKeyframeNonIdrFeedViolations=0`、`displayStableWithoutLedgerClosure=0`、`insertSurfacePhaseActionStage=0`、`insertControlProjectionMismatch=0`；receive gate 剩余 `arbiterMismatchTotal=1`，最早在 `seq=3855` 表现为 `action=none / coalescing=same-interval / outcome=sent / reason=forced-keyframe`，属于 SameInterval/RateLimited 下 executor outcome 与 arbiter action 的一致性口径问题。修复：`should_count_feedback_action_mismatch()` 只在 arbiter 明确计划 PLI/FIR 且实际 keyframe action 不一致时累计 mismatch，coalesced/same-interval 的 executor sent 继续作为 outcome 观测，不计为仲裁违约。验证：`cargo fmt`、`cargo test -p xbxengine coalesced_keyframe_executor_outcome_does_not_count_as_arbiter_mismatch`、`cargo test -p xbxengine transport::rtc::receive::ingress_state::feedback`、`cargo test -p xbxengine transport::rtc::receive::insert_gate`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。下一份 fresh trace 验收 `receiveFeedbackGate=PASS`，再继续看 midsegment `submit_age_ms` 与 recovering pulses。
- 2026-06-04: 新 fresh trace `runtime-trace-1780561491977-1.jsonl` 复盘：`freshnessGate=PASS`、`localControlVideoKeyframeRequested=21`、`mediaSupplyGate=PASS`、`remote-no-usable-idr=0`、`chainBuildSuccessRate=1.0`，无硬饿死/硬卡死；组合 gate 仍为 FAIL，剩余为 `needKeyframeNonIdrFeedViolations=1`、`displayStableWithoutLedgerClosure=7` 与 midsegment `GLOBAL_LATENCY_GATE=FAIL`（`submit_age_ms P95=304ms`、recovering pulses=61、最长 recovering streak=10.0s）。根因收窄到 trace 投影跨拍污染：旧 `insertGateDecision=emit/decodableToFeed` 被新 `need-keyframe` ledger facts 套用，DisplayStable 也在 `responseState=no-packet` / `receiveDisplayState=none` 时投出。修复：`stableServingSettled` 与 DisplayStable transition 只在 `keyframeRequired=false + responseState=usable-idr + receiveDisplayState=display-stable` 时投，need-keyframe 当前态下过滤陈旧 emit insert projection。验证：`cargo fmt`、`cargo test -p xbxrc trace_projection`、`cargo test -p xbxengine transport::rtc::receive::insert_gate`、`cargo check -p xbxrc`、`git diff --check`。下一份 fresh trace 验收 `needKeyframeNonIdrFeedViolations=0`、`displayStableWithoutLedgerClosure=0`。
- 2026-06-03: `2026-06-02-webrtc-recovery-boundary-redesign` 完成 fresh 组合验收并收口为 Done：`runtime-trace-1780483285846-1.jsonl` 通过 `trace_webrtc_acceptance_gate.py --latest --runtime-log-dir runtime-logs --max-age-seconds 900`，输出 `acceptanceGate=PASS`、`traceFreshness.freshnessGate=PASS`、`receiveFeedbackGate=PASS`、`receiveFeedbackGateFailures=[]`；keyframe chain 为 `sent=5 / responseObserved=2 / decoded=2 / cleanAnchorCommitted=2 / DisplayStable=1`，midsegment `globalLatencyGate=PASS / mediaSupplyGate=PASS / steadySupplyGate=SKIPPED`。应用约 420 秒运行未见 panic，进程由 timeout 结束。最终验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::recovery_ledger --lib`（30 passed）、`cargo test -p xbxrc trace_projection --lib`（75 passed）、`cargo test -p xbxengine transport::rtc::receive --lib`（204 passed, 16 ignored）、`python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`（12 OK）、strict receive gate、combined WebRTC acceptance gate、`git diff --check`、`git diff --cached --check`。
- 2026-06-03: 本轮最终清理并复验 WebRTC receive 边界补丁：移除未使用的 test helper / 空 re-export，补上 `recovery_exit_timed_fallback_when_submit_stalled_without_anchor` 回归测试标记，保持 packet action/reference mismatch 修正后的代码侧绿线。验证：`cargo test -p xbxengine transport::rtc::receive --lib`（197 passed, 16 ignored）、`cargo test -p xbxengine transport::rtc::recovery::contract --lib`（50 passed）、`cargo test -p xbxengine transport::rtc::session --lib`（77 passed）、`cargo test -p xbxengine --lib`（1069 passed, 24 ignored）、`cargo fmt`、`git diff --check`。仍待新 runtime trace 验收 `receiveFeedbackGate=PASS`、`mediaRecovered > 0`、`DisplayStable > 0`。
- 2026-06-03: receive trace gate 已补成机器可失败验收：`trace_receive_feedback_report.py --fail-on-gate --require-media-recovered --require-display-stable <trace>` 会在 `receiveFeedbackGate=FAIL`、无 media recovery 或无 `DisplayStable` 时返回 2；旧 `runtime-trace-1780467603921-1.jsonl` 严格 gate 返回 2，默认 JSON 报告兼容。验证：`python3 -B .agents/skills/analyze-runtime-logs/scripts/trace_receive_feedback_report.py --help`、默认报告输出、严格 gate 对旧 trace 返回 `gate_code=2`。下一条 fresh trace 直接用该命令作为 receive/display 完成证据。
- 2026-06-03: 为 receive trace 严格 gate 增加 Python 黑盒测试：最小 PASS trace 覆盖 `receiveFeedbackGate=PASS + cleanAnchorCommitted + DisplayStable` 返回 0，最小 FAIL trace 覆盖 `arbiterMismatchTotal` 返回 2。验证：`python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`（6 tests OK）、严格 gate 对旧 trace 返回 `gate_code=2`、`git diff --check`、`git diff --cached --check`。
- 2026-06-03: 新增 WebRTC 组合验收 gate：`trace_webrtc_acceptance_gate.py <trace>` 会同时运行 strict receive recovery gate 与 midsegment low-latency gate，输出 `acceptanceGate` JSON，返回 0/2。最小全绿 synthetic trace 已覆盖 `receiveFeedbackGate=PASS + media recovery + DisplayStable + steady midsegment`；旧 `runtime-trace-1780467603921-1.jsonl` 组合 gate 返回 2。验证：`python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`（7 tests OK）、`python3 -B .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py --help`、旧 trace `gate_code=2`。
- 2026-06-03: WebRTC 组合验收 gate 增加 fresh trace 选择与新鲜度门槛：`trace_webrtc_acceptance_gate.py --latest --max-age-seconds <seconds>` 会自动选 `runtime-logs` 最新 trace，并在 trace 过旧时输出 `traceFreshness.freshnessGate=FAIL`、返回 2。旧 `runtime-trace-1780467603921-1.jsonl` 在 `--latest --max-age-seconds 3600` 下同时暴露 freshness FAIL、receive FAIL 与 midsegment FAIL，避免把旧构建样本误当收口证据。验证：`python3 -B -m unittest discover .agents/skills/analyze-runtime-logs/tests`（9 tests OK）、`python3 -B .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py --latest --max-age-seconds 3600`（gate_code=2）。
- 2026-06-03: `runtime-trace-1780467603921-1.jsonl` 复盘显示 stale `nack-exhausted + continuous` 已改善，host mailbox 推进到 1058，`CleanAnchorCommitted=3`，但后段仍由 packet action/reference mismatch 卡死：`reference=continuous` 曾与 `packetRecoveryActionStage=request_idr` 并存，disposable/unknown transport gap 后续被 NACK exhausted 升级成 `need-keyframe`，InsertGate 进入 `request_idr + mustIdrHold`。本轮已把 NACK keyframe escalation、`ReceiverDecodeContext.nack_exhausted`、packet action stage 全部绑定 unresolved hard/reference gap；continuous/repairing + decoder synced 下的 `RequestIdr/WaitKeyframe` 会归一为 repair-first，runtime timeline diagnostic gap 退出生产 packet action stage。验证：`cargo test -p xbxengine transport::rtc::receive --lib`（197 passed, 16 ignored）、`transport::rtc::recovery::contract --lib`（49 passed）、`cargo test -p xbxengine --lib`（1068 passed, 24 ignored）。仍待新 runtime trace 验收 `insertSurfacePhaseActionStage=0`、`sparseMustIdrMismatchTotal=0`、`receiveFeedbackGate=PASS` 与 `DisplayStable`。
- 2026-06-03: `runtime-trace-1780456567200-1.jsonl` 复盘显示新构建已能进入 steady/healthy 且 NACK effective rate=1.0，但仍在 stale `nack-exhausted` 清债失败处卡住：`receiveFeedbackGate=FAIL`、`DisplayStable=0`、`host-retained-old-frame=346`、midsegment `submit_age_ms P95=4601ms`。本轮已把 decoder reference sync 对齐 WebRTC `FrameDecoded()` 语义，直接清 keyframe debt/NACK exhausted；NACK recovered、gap resolved、complete candidate 在无 unresolved hard gap 时清 stale NACK debt；reference projection 只在 hard gap 存在时把 `nack_exhausted` 作为控制事实。验证：`cargo test -p xbxengine transport::rtc::receive --lib`（194 passed, 16 ignored）、`transport::rtc::recovery::contract --lib`（49 passed）、`cargo test -p xbxengine --lib`（1065 passed, 24 ignored）、`git diff --check`。仍待新 runtime trace 验收 `keyframeRequired=true/cause=nack-exhausted` 与 `ReferenceChainState=continuous` 不再共存。
- 2026-06-03: `2026-06-02-webrtc-recovery-boundary-redesign` 继续收口 session picture recovery 权限：`ActionCoordinator` / `StateRecoveryCoordinator` 不再用 `DelegatedToReceive` 创建 `FrameRecovery`、`idr_requested` 或 session keyframe in-flight；`RecoveryCoordinator::sync_connectivity_escalation_state()` 对误入的 `RequestPli/Fir` 只记录 ownership violation 并委托 receive；session RFC cost、active recovery narrative、ledger command pending 已同步改成 receive delegation 口径。验证：`cargo test -p xbxengine transport::rtc::recovery --lib`（161 passed, 2 ignored）、`transport::rtc::session --lib`（77 passed）、`transport::rtc::policy --lib`（99 passed）、`diagnostics --lib`（107 passed）。仍待新 runtime trace 验收 continuation emit/decode、host mailbox 推进与 `DisplayStable`。
- 2026-06-03: 继续去除旧 keyframe-in-flight 叙事残留：first-frame acquisition 的 session 抑制路径现在直接 `DelegatedToReceive` 并记录 receive hint；H264 continuation blocker 与 deferred command unlock reason 改读 current clean anchor，decoded/committed anchor 可在 host displayed-IDR 前吸收陈旧 continuation/bootstrap 诊断。补充验证：`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib`（40 passed）、`transport::rtc::stack::transport_session --lib`（16 passed）、`transport::rtc::session --lib`（77 passed）、`transport::rtc::recovery --lib`（161 passed, 2 ignored）。

## Recent Completed

- 2026-06-23: 修复云游戏页默认区域下列表为空：重新登录并开启 TUN 后，当前 canonical store 已恢复 `auth.tokens.stream.xCloudToken`，`xCloudToken.data.market=US`、默认 region=`WESTUS`、cloud region count=19，说明登录和云 token 均可用；真正导致页面仍空的是 v2 目录缓存按 `uhs` 分片，而同一 gamertag/xid 重新登录后的 `uhs` 与历史 `data.xcloudCatalog.v2.*` 全部对不上，当前账号分片 miss 后远端刷新失败会让前端只显示空目录。修复：`resolve_web_token_claims()` 解析稳定 `xid`，`XcloudService::resolve_cache_scope()` 优先用 `xid` 作为目录缓存账号标识；`DataCacheRepository` 在当前分片缺少可渲染 overlay 时，回退读取同 region/language/market 的最近可渲染 v2 分片，保证刷新失败时仍显示最近可用目录；保留 `xgpuweb/xgpuwebf2p` 失败诊断。验证：`jq` 摘要检查 `settings.xbx.json`、`cargo fmt -p xbxrc -p xbox-auth-flow`、`cargo test -p xbxrc cache_scope_uses_stable_xid_when_available`、`cargo test -p xbxrc mods::data::cache_repository`、`cargo check -p xbxrc`；在线 `/v2/titles` 探测因审批器拒绝网络命令未执行。
- 2026-06-05: 复盘 fresh trace `runtime-trace-1780623190702-1.jsonl`：receive gate 仍 PASS，ledger/insert/decoder 违规为 0；midsegment 失败转为 receiver repair 与低源帧节奏，`decode_fps avg=20.9`、`present_fps avg=19.3`、`submit_to_present_ms P95=193ms`，新增 layer 诊断显示主线程 queue delay 仅 2 次（34/58ms）、`present_tick_dispatch_coalesced=39` 与 `present_tick_rerun=39` 配平，host tick 已排除为主因。关键帧风暴来源收窄为 receive-local：`insert-gate-hold-repair` 与 `receiver-local-nack-blocking-admission` 高频 sent。修复：soft keyframe 请求不再因 active recovery 强制绕过 interval，arbiter 对 soft keyframe 输出 interval-aware `sent/coalesced/throttled`，NACK blocking admission 只继续 NACK，只有 hard escalation / gap-too-large 才强制 PLI/FIR。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::feedback_arbiter`、`cargo test -p xbxengine transport::rtc::receive::nack_maintenance`、`cargo test -p xbxengine transport::rtc::receive::ingress_loop`、`cargo check -p xbxengine`、`cargo check -p xbxrc`。
- 2026-06-05: 复盘 fresh trace `runtime-trace-1780652183550-1.jsonl` 的 60fps 缺口：RTT 18-29ms、Direct、target REMB 32Mbps，显示侧已跟上 decode（midsegment `decode_fps avg=33.6 / present_fps avg=33.0 / gap=0.6`），5 秒窗口内 RTP clock 正常推进约 450k，但 pacer 只增加 126-181 帧，平均 RTP step 约 2500-3000，缺口收窄到远端帧边界 / SampleBuilder AU / DecodeGate emit 之前。本轮补齐前置统计：`inbound_video_frame_count_total`、`inbound_video_rtp_marker_count_total`、`inbound_video_access_unit_count_total`、`inbound_video_decode_gate_emit_count_total`、`inbound_video_decode_gate_continue_count_total`，并把原 frame arrival 从 `inbound_video_packet_count_total` 分离，避免 packet/frame 混合口径。下一份 trace 可直接比较 marker -> AU -> gate emit -> decode/pacer。验证：`cargo fmt`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`cargo test -p xbxengine transport::rtc::receive::ingress_loop`、`cargo test -p xbxengine transport::rtc::receive::rtp_frame_assembler`、`cargo test -p xbxrc trace_projection`、定向 eslint、`git diff --check`；全量 `pnpm lint:fix` 仍被 3 个既有 lint 问题阻塞。
- 2026-06-04: 复盘 fresh trace `runtime-trace-1780565776354-1.jsonl`：receive recovery 已收口（`receiveFeedbackGate=PASS`、`arbiterMismatchTotal=0`、`needKeyframeNonIdrFeedViolations=0`、`displayStableWithoutLedgerClosure=0`），剩余 `acceptanceGate=FAIL` 来自 midsegment latency/recovery pulse；根因是普通 continuation decoded-frame 已持续产出，但全局 `latest_video_decode_ok_time_ms` 卡在旧 IDR，导致 owner 看到 `decode_age_ms=77s/107s`，在 `present_age_ms=71/75ms`、host 无 drop/pressure 时仍误判 `displaySupplyStarved`。修复：`sync_decode_runtime_stats` 现在用最新 `DecodedFrame` observation 单调刷新 `latest_video_decode_ok_time_ms/rtp_timestamp`，避免 supply owner 被 stale decode freshness 拉回恢复态。验证：`cargo fmt`、`cargo test -p xbxengine decoded_frame_observation_refreshes_global_decode_freshness`、`cargo test -p xbxengine media::video::decode::actor`、`cargo check -p xbxengine`、`cargo check -p xbxrc`。
- 2026-06-01: 收紧 displayedIdr 作为当前锚点的资格：`transport_await`、`gap`、`supply`、`suspect_anchor_gate` 现在只在当前可见输出仍 fresh 时把 displayedIdr 当成控制证据，`displaySupplyStarved` 下的 retained old frame 不再继续压住恢复；验证：`cargo test -p xbxengine --lib displayed_idr -- --nocapture`。
- 2026-06-01: 继续收紧 owner 的 recovery 收口：`displayed_idr_serving_release_active` 现在认可“displayed-idr + host/decode/present 仍 fresh”的 playback 事实，即使 `receive_display_state` 尚未闭合，也能让 `RebuildingSupply` 退回 `DegradedServing`；验证：`cargo test -p xbxengine displayed_idr_playback_release_without_control_plane_exits_rebuilding_supply --lib`、`cargo test -p xbxengine runtime_reconnects_after_remote_continuation_only_even_with_stale_clean_anchor --lib`。
- 2026-06-01: 收紧 receive 侧 display-serving 投影：`receiver_state_from_runtime`、`publish_receiver_observation`、`keyframe_request_force_required`、`maybe_run_receiver_local_nack_maintenance`、`ReceiveRecoveryLedger::apply_decoder_facts_from_stats` 现在都会把 displayed IDR serving 当成收口事实，避免 waiting-keyframe / nack-exhausted 继续把 soft keyframe 拉回恢复态；验证：`cargo test -p xbxengine --lib displayed_idr_serving -- --nocapture`。
- 2026-06-01: 收紧 remote-continuation-only 的 reconnect 门：`video_anchor_clean_observed_at_ms / recovery_displayed_idr_at_ms` 只有在本轮 keyframe stall 窗内新鲜出现时才阻断 reconnect，旧 clean anchor 不再把 runtime 卡在恢复态；验证：`cargo test -p xbxengine runtime_reconnects_after_remote_no_usable_idr_terminal_and_keyframe_stall --lib`、`cargo test -p xbxengine runtime_reconnects_after_remote_continuation_only_even_with_stale_clean_anchor --lib`。
- 2026-06-01: no-idr 终止 reconnect 兜底：`remote-no-response / remote-continuation-only / remote-idr-unusable` 在 keyframe stall 窗后会由 runtime 直接转 reconnect，receive 侧继续保留终止诊断与 transport-await 语义；验证：`cargo test -p xbxengine runtime_reconnects_after_remote_no_usable_idr_terminal_and_keyframe_stall --lib`、`cargo test -p xbxengine reconnect_lifecycle --lib`。
- 2026-06-01: 收口 receive trace 语义：`episodeProjectionState` 不再被 `usable-idr` 提前闭合，`displaySupplyStarved` 的 serviceable 判定要求 fresh clean anchor，补齐 trace / diagnostics 回归测试。
- 2026-05-29: Receive Feedback Arbiter trace 验收（双主线）：本地 replay/continuation/healthy 跑 `trace_receive_feedback_report.py`；continuation `terminal=38`、无 `silentStuck`；脚本计入 `FreshAnchorRecovered`/`PlaybackRecovered`；投影 `episodeProjectionState`/`displaySupplyStarvedBlocker`。Report：[`reports/2026-05-29-receive-feedback-arbiter-trace-validation.md`](reports/2026-05-29-receive-feedback-arbiter-trace-validation.md)。待当前构建新采 healthy 冲全 gate PASS。
- 2026-05-29: 调度所有权回归点修复：禁止 `reference_chain_observation` 将 Unknown 提升为 Continuous；`has_established_usable_anchor` 无 decode sync 时投影 `NeedKeyframe`；`apply_host_display_facts_from_stats` 闭合 display→ledger；coordinator 禁止用 `receive_keyframe_last_sent_at_ms` 冒充 response observed。验证：`cargo test -p xbxengine --lib`（1021）。
- 2026-05-29: 调度所有权五差修复：`ReferenceChain`/`PacketRecoveryActionStage` ledger 单源；`observe_sample_loss`/`first-frame-acquisition` 经 arbiter；Insert `keyframe_required` 优先释放 usable IDR；coordinator/owner 改读 ledger（`receive_display_state`/`usable-idr`）；`displayed_idr` 降级为 projection。验证：`cargo test -p xbxengine --lib`（1017）、`cargo test -p xbxrc --lib trace_projection`（68）。trace 脚本待本地 `runtime-trace-1779961935840-1.jsonl` 复验。
- 2026-05-29: 收紧 receive 终端归因与 host latest-slot 快照：`ReceiveRecoveryLedger::terminal_reason_for_remote_no_usable_idr` 下沉 no-idr 终端判定，`RtcStackRuntimePort::take_latest_render_frame` 改为 latest-slot snapshot 读取，`feedback` 只保留轻量调用和统计投影。验证：`cargo test -p xbxengine recovery_ledger -- --nocapture`、`cargo test -p xbxengine runtime_port -- --nocapture`。
- 2026-05-29: Receive Feedback Arbiter 落地修复：`ReceiveRecoveryLedger` + `keyframe_required` 驱动 arbiter/reference/terminal；ledger-first `ReferenceChainState`；分层 terminal；trace/script gate 拆分。Checklist：[`rfcs/2026-05-29-receive-feedback-arbiter-landing-fix-checklist.md`](rfcs/2026-05-29-receive-feedback-arbiter-landing-fix-checklist.md)。Report：[`reports/2026-05-29-receive-feedback-arbiter-landing-fix.md`](reports/2026-05-29-receive-feedback-arbiter-landing-fix.md)。验证：`cargo test -p xbxengine --lib`（1006）、`cargo test -p xbxrc --lib trace_projection`（68）。
- 2026-05-28: Receive Feedback Arbiter Phase 0–5 闭环：ledger 驱动 `ReferenceChainState`、arbiter 统一 keyframe/NACK、`receiveFeedbackDecision`/`referenceChainStateChanged`/`receivePictureRecoveryTerminal` trace、验收脚本 rates/gate。RFC：[`rfcs/2026-05-28-receive-feedback-arbiter-webrtc-alignment.md`](rfcs/2026-05-28-receive-feedback-arbiter-webrtc-alignment.md)。Report：[`reports/2026-05-28-receive-feedback-arbiter-webrtc-alignment.md`](reports/2026-05-28-receive-feedback-arbiter-webrtc-alignment.md)。验证：`cargo test -p xbxengine --lib`（1000）、`cargo test -p xbxrc --lib trace_projection`（68）。
- 2026-05-27: 恢复链四标准结构性收口：escalation 永不产出 session PLI、coordinator sync 前委托 receive、Insert 首帧单轨、Owner 仅 `contract_snapshot`、`SparseIdrRhythm`+`receive_keyframe_last_sent_at_ms`、`insert_hold_decode_bypass_mismatch_total`。Report：[`reports/2026-05-27-recovery-four-standards-structural.md`](reports/2026-05-27-recovery-four-standards-structural.md)。验证：`cargo test -p xbxengine --lib`（983）、`cargo test -p xbxrc --lib trace_projection`（66）。
- 2026-05-27: 恢复链四问题收口：合同快照 decoder sync、Insert 单出口、`insertGateDecision` 观测、Session PLI 委托/收窄 coalesce、稀疏 IDR PLI 加速（不宽进 delta）。Report：[`reports/2026-05-27-recovery-four-issues-convergence.md`](reports/2026-05-27-recovery-four-issues-convergence.md)。验证：`cargo test -p xbxengine --lib`（981）、`cargo test -p xbxrc --lib trace_projection`（66）。
- 2026-05-26: Receive InsertGate 与恢复链收敛：pre-decode 单点 `InsertGate`、Receive-only PLI、`RecoverySurfacePhase`/`DerivedDecoderHealth`、TimedFallback decoder unstick 与 supply-break ledger；WebRTC 对齐：`decodable_to_feed` / `decoder_reference_synced` / `DelegatedToReceive` / trace `keyframeRequestOutcome`。RFC：[`rfcs/2026-05-26-receive-insert-gate-and-recovery-convergence.md`](rfcs/2026-05-26-receive-insert-gate-and-recovery-convergence.md)。Report：[`reports/2026-05-26-receive-insert-gate-recovery-convergence.md`](reports/2026-05-26-receive-insert-gate-recovery-convergence.md)。验证：`cargo test -p xbxengine --lib`（980 passed）、`cargo test -p xbxrc trace_projection`。
- 2026-05-25: WebRTC 调度/恢复硬收口：控制面改为 `ReceiverState` + host `DisplayedIdrFact`（`displayed-idr` / `FreshAnchorRecovered` / `PlaybackRecovered`），删除 `CleanAnchorSubmitted`、bridge、unsolicited submission 与 stall 作废 anchor；owner/facts/trace/前端诊断同步。Report：[`reports/2026-05-25-webrtc-scheduling-recovery-hard-convergence.md`](reports/2026-05-25-webrtc-scheduling-recovery-hard-convergence.md)。验证：`runtime_port` 13、`video_scheduling_owner` 65、`receive::` 126+、`diagnostics.test.ts` 10。
- 2026-05-19: 完成浏览器 SR 渲染四项改造：`SuperResolutionProcessor` 改为 `requestAnimationFrame` 稳刷新呈现、`requestVideoFrameCallback` 仅标记新帧；SR WebGL2 上下文对齐标准路径（`alpha:false`、`desynchronized:true`）；`resolveRenderCause` 纳入 callback/presentedFrames 采样 mismatch，并修正 telemetry 汇总后再分类的时序；SR tier 输出现受 `presentTarget` display budget 约束（policy clamp + renderer `min(renderOutput*)` 兜底）。验证：`vitest run browser-render-policy.test.ts browser-runtime.test.ts Renderers.test.ts`、`tsc --noEmit`。
- 2026-05-18: 继续拆分浏览器直连 dropped-like 绘制证据：`browser-runtime` 现在在保留 `renderDroppedFrames` 总计数的同时，额外输出 `callbackGapCountSinceLastSample / presentedFramesJumpCountSinceLastSample`，`renderFrameDropped` 单条事件新增 `callbackGap / presentedFramesJump`，runtime snapshot / diagnostics 同步暴露 `renderCallbackGapCount(LastSample) / renderPresentedFramesJumpCount(LastSample)`，后续可直接区分“callback 稀疏”与“`presentedFrames` 合批推进”，避免把两类信号都读成真实掉帧。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-runtime.test.ts src/streaming/diagnostics.test.ts`、`git diff --check -- src/streaming/runtime/browser-runtime.ts src/streaming/runtime/browser-runtime.test.ts src/streaming/diagnostics.ts src/streaming/diagnostics.test.ts src/streaming/types.ts src/player/domain/media.ts .agents/skills/analyze-runtime-logs/SKILL.md .agents/skills/analyze-runtime-logs/references/log-schema.md .agents/skills/analyze-runtime-logs/references/analysis-playbook.md docs/project-task.md`。
- 2026-05-18: 继续补齐浏览器直连绘制节拍归因 trace：`bindBrowserVideoFrameTracking()` 现在额外上报 `mediaTimeDeltaSec / expectedDisplayLeadMs`，`browser-runtime` 的 `renderTelemetryObserved` 新增 `callbackCountSinceLastSample / presentedFramesAdvancedSinceLastSample`，runtime snapshot 同步暴露 `renderCallbackCountLastSample / renderPresentedFramesAdvancedLastSample / renderFrameMediaTimeDeltaSec / renderFrameExpectedDisplayLeadMs`，后续可直接区分“50Hz 回调 + 60Hz 呈现推进”和“真实显示不足 60Hz”。`analyze-runtime-logs` skill 已同步补充这些字段的读取语义。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-video-display.test.ts`、`./node_modules/.bin/vitest run src/streaming/runtime/browser-runtime.test.ts src/streaming/runtime/browser-render-policy.test.ts src/streaming/diagnostics.test.ts`、`git diff --check -- src/streaming/runtime/browser-video-display.ts src/streaming/runtime/browser-video-display.test.ts src/streaming/runtime/browser-runtime.ts src/streaming/runtime/browser-runtime.test.ts src/streaming/types.ts .agents/skills/analyze-runtime-logs/SKILL.md .agents/skills/analyze-runtime-logs/references/log-schema.md .agents/skills/analyze-runtime-logs/references/analysis-playbook.md docs/project-task.md`。
- 2026-05-18: 修复浏览器直连绘制 telemetry 的帧事件双重记账：`browser-runtime` 不再重复消费 Player 侧 `stats.videoFrameProcessed`，runtime 内只保留 DOM `requestVideoFrameCallback/timeupdate` 这条 render frame tracking 主链，`renderTelemetryObserved.frameEventsSinceLastSample` 与 `callbackIntervalMs/presentedFramesDelta` 现在使用同一观测源，便于直接判断 Mac 前台全屏段的真实绘制节拍。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-runtime.test.ts`、`./node_modules/.bin/vitest run src/streaming/runtime/browser-video-display.test.ts src/streaming/runtime/browser-render-policy.test.ts`、`git diff --check -- src/streaming/runtime/browser-runtime.ts src/streaming/runtime/browser-runtime.test.ts src/streaming/runtime/browser-video-display.ts`。
- 2026-05-18: 同步更新 `analyze-runtime-logs` skill 的浏览器直连绘制口径：补充 `renderTelemetryObserved / renderFrameDropped / renderBackpressureChanged / renderCauseClassified / renderPolicyApplied` 的读取顺序、字段语义与分析模板，并明确 `trackingSource / sourceFpsEstimate / presentedFramesDelta / droppedLikeStreak` 的解释规则，后续分析 Mac/Windows WebGL2 直连日志可直接按统一技能执行。验证：`git diff --check -- .agents/skills/analyze-runtime-logs/SKILL.md .agents/skills/analyze-runtime-logs/references/log-schema.md .agents/skills/analyze-runtime-logs/references/analysis-playbook.md docs/project-task.md`。
- 2026-05-18: 为浏览器直连模式补齐绘制 telemetry trace：`bindBrowserVideoFrameTracking()` 现显式区分 `timeupdate / requestVideoFrameCallback` 跟踪来源，`browser-runtime` 新增 `renderTelemetryObserved` 周期汇总事件，统一输出 `trackingSource / presentedFramesDelta / sourceFpsEstimate / sourceFrameIntervalMs / droppedFramesSinceLastSample / droppedLikeStreak / maxCallbackIntervalMsSinceLastSample` 等绘制证据；runtime snapshot 同步暴露这些字段，便于直接判断浏览器侧绘制节奏、帧源节拍与局部 backpressure 状态。验证：`./node_modules/.bin/vitest run src/streaming/diagnostics.test.ts src/streaming/runtime/browser-render-policy.test.ts src/streaming/runtime/browser-video-display.test.ts src/streaming/runtime/browser-runtime.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-18: 继续细化浏览器显示几何刷新原因：`browser-runtime` 现在将 display-geometry refresh reason 细分为 `windowResize`、`fullscreenChange`、`containerResize`、`sourceDimensionsChanged`，并把 `displayGeometryTrigger` 一并写入 `renderAdaptiveProfileResolved` / `renderPolicyApplied` 等 runtime trace；同时修复 `media.videoReady` 仅更新 DOM 呈现、不刷新 renderer attach 的缺口，源分辨率变化现在会重新编译 present target。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-runtime.test.ts`、`./node_modules/.bin/vitest run src/streaming/runtime/browser-runtime.test.ts src/streaming/runtime/browser-render-policy.test.ts src/streaming/runtime/browser-video-display.test.ts src/streaming/diagnostics.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-18: 继续整理显示感知渲染实现边界：`resolveDisplayViewport()` 已从 [`browser-video-display.ts`](browser-video-display.ts) 抽到独立纯几何模块 [`display-viewport.ts`](display-viewport.ts)，`browser-render-policy` 与 DOM 呈现层共用同一 viewport 合同但不再互相耦合；`SuperResolutionWebGL2Renderer` 现对未变化的 output size / display viewport 做 no-op guard，避免重复 `canvas resize`、`gl.viewport` 与样式回写。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-render-policy.test.ts src/streaming/runtime/browser-video-display.test.ts src/player/infra/render/Renderers.test.ts src/streaming/runtime/browser-runtime.test.ts src/streaming/diagnostics.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-18: 继续优化浏览器显示感知渲染主线：`browser-runtime` 新增 `resize/fullscreenchange/ResizeObserver` 驱动的 display geometry 刷新，窗口缩放、全屏切换与容器尺寸变化都会重新编译 present target；标准 `webgl2` renderer 对未变化的 present target/backing size 加入 no-op guard，避免重复 `canvas resize + gl.viewport`；runtime snapshot / diagnostics 现补齐 `renderDisplayWidth/renderDisplayHeight`，方便区分容器尺寸与实际 viewport/present target。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-render-policy.test.ts src/streaming/runtime/browser-video-display.test.ts src/player/infra/render/Renderers.test.ts src/streaming/runtime/browser-runtime.test.ts src/streaming/diagnostics.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-18: 继续收紧浏览器全屏输出合同并补齐 refresh rate 回退：fullscreen `presentTarget` 现在会再受 configured/source 分辨率上限约束，`1080p` 模式不再因本机 DPR 被抬到 `2880x1620`；`browser-runtime` 新增 `requestAnimationFrame` 驱动的显示刷新率估算回退，`window.screen.frameRate` 缺失时仍可把 `120/144Hz` 等差异投影进 render policy；SR tier 规则改为“只把低于目标档位的源补到目标档位”，`1080 configured + 1080 source` 固定落在 `1080p`，`1440 source` 固定落在 `1440p`，同档统一走 `RCAS-only` 路径。验证：`./node_modules/.bin/tsc --noEmit --pretty false`、`./node_modules/.bin/vue-tsc --noEmit`、`./node_modules/.bin/eslint src/streaming/runtime/super-resolution-ladder.ts src/streaming/runtime/super-resolution-ladder.test.ts src/streaming/runtime/browser-render-policy.test.ts src/streaming/runtime/browser-runtime.test.ts --fix`。补充说明：当前环境的 `vitest` 因本地 `@rollup/rollup-darwin-arm64` optional dependency 签名错误未能启动。
- 2026-05-18: 完成浏览器显示感知全屏渲染收口：浏览器 render policy 现编译 `presentTarget`，将显示尺寸 / DPR / 刷新率 / 全屏状态与源分辨率投影到 renderer attach、runtime snapshot 与 diagnostics；标准 `webgl2` renderer 已拆开 source texture size 与 output present target size；`Contain` 全屏路径与显式宽高比路径共用 `resolveDisplayViewport()`，锁住 `1920x1200 -> 1920x1080 viewport` 合同。RFC：[`rfcs/2026-05-18-browser-display-aware-fullscreen-rendering.md`](rfcs/2026-05-18-browser-display-aware-fullscreen-rendering.md)。Report：[`reports/2026-05-18-browser-display-aware-fullscreen-rendering.md`](reports/2026-05-18-browser-display-aware-fullscreen-rendering.md)。验证：`./node_modules/.bin/vitest run src/streaming/runtime/browser-render-policy.test.ts src/streaming/runtime/browser-video-display.test.ts src/player/infra/render/Renderers.test.ts src/streaming/runtime/browser-runtime.test.ts`、`./node_modules/.bin/vitest run src/streaming/diagnostics.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-14: 前端 Gamepad 业务输入仲裁：`business-input-arbiter` 单一 owner 派生、移除 `input-routing` 历史壳层、`Stream.vue` 归一 `streamUiSurface`、Rust `streamPad` 路由与 `rustEngineStreamPadRoutedToSession` 竞态门、`stream-input-consumer-adapters`、菜单 `closeSheet` 显式 `resume-stream` outcome、`GamepadDriver` 按 `getOwner()==='stream'` 消费，并将 `gamepadBusinessInputRouteChanged` 与 UI/driver/shell trace 统一补齐 `businessInput*` 观测字段。RFC：[`rfcs/2026-05-14-gamepad-business-input-ownership-ui-stream-routing.md`](rfcs/2026-05-14-gamepad-business-input-ownership-ui-stream-routing.md)。验证：`pnpm test`、`pnpm lint:fix`、`vue-tsc --noEmit`。
- 2026-05-14: 收口 Windows 冷启动手柄恢复链：移除 provider 层 `activate/resume/self-heal` 的统一抢前台，`WinAPI + Tauri` 聚焦只保留在 `schedule_gamepad_cold_start_sdl_binding_nudge()` 首轮一次；Windows 启动期 `reopen` 只保留首轮 cold-start nudge 一次，后续 page-load/focus/resumed/awaiting-baseline 监控不再重复触发 startup reopen。验证：`cargo fmt`、`cargo check -p xbxrc`。
- 2026-05-13: 修正 `BackgroundWarm` 对外快照合同：`gamepad.runtimeSnapshot` 与 `rpc.gamepad.getRuntimeSnapshot()` 在 `BackgroundWarm` 下不再暴露实时 `slots`，仅保留 lifecycle/health/devices/progress 元数据，避免前端通过 runtime snapshot 回放后台 slot 而继续触发导航；内部采样推进与前台恢复链保持不变。验证：`cargo fmt`、`cargo test -p xbxrc background_warm_snapshot_hides_slots_for_external_consumers`、`cargo test -p xbxrc active_snapshot_keeps_slots_for_external_consumers`、`cargo check -p xbxrc`。
- 2026-05-13: 收紧 gamepad shell 的 `BackgroundWarm -> Active` 自动提升门，仅在主窗口 `visible && !minimized && focused` 时才允许后台进度触发 promote；保留启动期 `startup-active-without-progress` 自愈链路不变，避免 Windows/Xbox 大屏首开恢复与后台切断语义互相误伤。验证：`cargo fmt`、`cargo test -p xbxrc background_warm_auto_promote_requires_window_focus`、`cargo test -p xbxrc startup_rearm_stays_independent_from_focus_state`、`cargo check -p xbxrc`。
- 2026-05-12: 继续收紧 Xbox 大屏模式串流页首开无输入的恢复主链：[`Stream.vue`](D:/Code/xbxrc/src/pages/Stream.vue) 的 overlay 路由切换现改为统一调用 `resumeShellSampling(ui-only/stream-only)`，不再只做 `setInputPolicy()`；[`useGamepadRouteForStreamOverlay.ts`](D:/Code/xbxrc/src/pages/stream/useGamepadRouteForStreamOverlay.ts) 新增 `window focus / document visible` 时的目标态重收敛；[`browser-runtime.ts`](D:/Code/xbxrc/src/streaming/runtime/browser-runtime.ts) 的 `attachGamepadSession()` 也改为直接 `resumeShellSampling(stream-only)`，补齐 stream plain-layout 首开时绕过 `AppShell` 恢复链的空洞。验证：`pnpm lint:fix`、`cargo check -p xbxrc`。
- 2026-05-12: 修复 Xbox 大屏模式下手柄首开偶发“直到前后台切换后才恢复输入”的前端路由竞态与首轮快照空窗：[`useGamepadRouteForStreamOverlay.ts`](D:/Code/xbxrc/src/pages/stream/useGamepadRouteForStreamOverlay.ts) 现改为按 `sessionId + overlay` 连续收敛目标输入路由，不再依赖某一次 `overlay open -> close` 边沿才能切到 `stream-session`；[`gamepad-listener.ts`](D:/Code/xbxrc/src/navigation/core/gamepad-listener.ts) 与 [`GamepadDriver.ts`](D:/Code/xbxrc/src/player/infra/input/GamepadDriver.ts) 新增 `window focus / document visible` 主动 runtime snapshot 重放，确保即使错过首轮广播或恢复窗口，也会在前台恢复时立即重新消费当前基线。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-12: 修复 renderer 晚于 gamepad runtime 挂载时会错过初始 `slotSnapshot` baseline 的问题：[`gamepad-listener.ts`](D:/Code/xbxrc/src/navigation/core/gamepad-listener.ts) 现在会在启动时的 `rpc.gamepad.getRuntimeSnapshot()` 结果以及后续 `gamepad.runtimeSnapshot` 事件中，主动回放其中的 `slots` 到导航输入层，不再只依赖后续增量 `gamepad.slotSnapshot`；[`GamepadDriver.ts`](D:/Code/xbxrc/src/player/infra/input/GamepadDriver.ts) 同步改为在 runtime snapshot 桥接时立刻推送首帧 native pad state，避免播放器输入层“知道有手柄，但直到第一次真实变化才收到帧”。这对应 trace 中 `samplingHealth=healthy`、`gamepadRecoverySettled= sampling-baseline-ready` 已成立，但前端消费层仍空等首次 slot 变化的缺口。验证：`cargo check -p xbxrc`；`npx vue-tsc --noEmit` 与 `npm test` 在当前环境因 Volta 目录权限错误未能执行。
- 2026-05-12: 修复 SDL3 首开时“source 已稳定读到基线快照，但 runtime 长期仍显示 `lastBackendSampleActivityAtMs = 0`”的问题：[`backend.rs`](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/backend.rs) 的 `TrackedDevice::sync_snapshot()` 现在改为 `self.dirty_sample |= changed`，避免同一轮 backend poll 中首条建立基线的 `Snapshot` 被后续内容相同的稳定轮询快照重新冲成非脏态，导致 runtime 错过首条 raw sample。新增回归测试 `stable_snapshots_do_not_clear_initial_dirty_sample`，覆盖“Connected + 首条 Snapshot + 重复稳定 Snapshot”场景。验证：`cargo fmt`、`cargo test -p ohmygamepad-sdl3 stable_snapshots_do_not_clear_initial_dirty_sample`、`cargo check -p xbxrc`。
- 2026-05-12: 为 SDL3 首开采样残留问题补 source 级结构化 trace：[`trace.rs`](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/trace.rs) 新增可选 runtime trace sink；[`source.rs`](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/source.rs) 现会把 `connected-baseline / prime-sampling / reopen-prime / polled snapshot changed / polled snapshot stable` 等摘要事件写入同一份 runtime trace，字段包括 `pressedButtonCount / nonZeroAxisCount / maxAbsAxisMilli / leftTriggerMilli / rightTriggerMilli / allZero`，用于直接区分“首开阶段 SDL3 一直只读到全零态”与“读到非零但未进入 logical progress”两类根因；[`shell/mod.rs`](D:/Code/xbxrc/src-tauri/src/shell/mod.rs) 在服务初始化时接入 `gamepad-source` trace domain。验证：`cargo fmt`、`cargo check -p xbxrc`。
- 2026-05-12: 将 Xbox 大屏模式首开自愈从 `Stalled` 前移到 `AwaitingBaseline`：[`service.rs`](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/service.rs) 新增 `try_startup_sampling_self_heal()`，对“设备已连接、仍无 logical progress、仍停留首开基线等待态”的场景也允许复用同一条 `reopen + prime + refresh` 自愈链，并与 stalled 自愈共用冷却与计数；[`lib.rs`](D:/Code/xbxrc/src-tauri/src/lib.rs) 的 interactive/app-resume hint 现会同时尝试 startup self-heal；[`shell/mod.rs`](D:/Code/xbxrc/src-tauri/src/shell/mod.rs) 新增 `awaitingBaselineSelfHealTriggered` trace；[`mods/gamepad/service.rs`](D:/Code/xbxrc/src-tauri/src/mods/gamepad/service.rs) 新增 `tryStartupSamplingSelfHealCompleted` provider trace，便于直接核对“是否已提前出手，而不是等退化到 stalled 才补救”。验证：`cargo fmt`、`cargo check -p xbxrc`。
- 2026-05-12: 继续收口 Xbox 大屏模式首开“直到切一次多任务才开始有输入”的 SDL3 恢复链：[`service.rs`](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/service.rs) 的 `prime_and_refresh_runtime_sampling()` 现在优先执行“重开已跟踪 gamepad 句柄并立即 prime baseline snapshot”，若失败再退回旧的 `prime_sampling()`；[`source.rs`](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/source.rs) 新增 `ReopenDevicesAndPrimeSampling` 命令与 `sdl3_gamepad_reopen_prime_completed` 诊断日志，避免恢复链持续复用 Xbox 大屏首开阶段可能处于钝态的旧 SDL3 `Gamepad` 句柄。验证：`cargo fmt`、`cargo check -p xbxrc`。
- 2026-05-12: 为 Xbox 大屏模式首开“直到切一次多任务才开始有输入”的残余问题补 SDL3 backend 轮询快照兜底：[source.rs](D:/Code/xbxrc/crates/ohmygamepad/backends/sdl3/src/source.rs) 的 SDL3 source 线程现每 8ms 主动读取一次已打开 gamepad 的当前按钮/摇杆基线并发出 `Snapshot` 事件，不再只依赖 SDL 事件流驱动 raw sample 更新。这样即使首开时 SDL 事件在前台未及时送达，用户后续按下手柄也能通过状态轮询进入 runtime，避免必须先切一次多任务/焦点后 backend 才首次出样。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-12: 收紧 Xbox 大屏模式 gamepad 恢复判定并补后台采样自动提升保护：前端 [AppShellLayout.vue](D:/Code/xbxrc/src/components/layout/AppShellLayout.vue) 不再把 `none -> shared:1:1:0` 这类“拿到静态 snapshot 但没有真实 logical progress”的变化误判为恢复完成，只有 `sampleSeq / sampledAt / lastSampleProgressAtMs` 真正推进后才 settle；同时在页面可见但 runtime 仍停留 `BackgroundWarm` 时会主动补一次 `resumeShellSampling(shared)`。Rust [shell/mod.rs](D:/Code/xbxrc/src-tauri/src/shell/mod.rs) 还新增 `backgroundWarmProgressAutoPromoteRequested` 兜底：当窗口仍可见且未最小化、runtime 已在 `BackgroundWarm` 中产生真实逻辑采样进度时，自动请求提升回 `Active`，防止 Tauri 漏掉 `Focused(true)` 后一直卡在 warm 态导致无输入。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-11: 为 Xbox 大屏模式首开手柄排障补齐 gamepad 时序日志：前端 [AppShellLayout](D:/Code/xbxrc/src/components/layout/AppShellLayout.vue) 现记录 mounted / visibilitychange / 恢复触发 / shell-policy 纠偏 / runtime snapshot 关键字段变化；Rust [lib.rs](D:/Code/xbxrc/src-tauri/src/lib.rs) 现记录 page-load、窗口 focus/minimize/restore、RunEvent::Resumed；[shell/mod.rs](D:/Code/xbxrc/src-tauri/src/shell/mod.rs) 现记录 init activateSampling、runtime snapshot lifecycle/health/progress 变化、baseline absorbed 与 stalled self-heal 触发；[mods/gamepad/service.rs](D:/Code/xbxrc/src-tauri/src/mods/gamepad/service.rs) 现记录 activate/resume/lifecycle/self-heal 调用级 trace。用于本机 Xbox 大屏模式回放“首开事件触发、采样切换、壳层时序、前后端恢复决策”全链路。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-12: 放宽播放恢复链对本地显示抖动的自伤耦合，并把 render/host mailbox 从单槽提升为小深度队列：`renderer` 与 `native_video::scheduling` 现统一保留最多 4 个待呈现候选，允许 recovery anchor / owner 帧前插但不再因为单拍 miss 直接丢失后续供给；`runtime_port` 不再在 host visibility stall 时反向失效当前 clean-anchor；`runtime_state/startup` 将“显示连续可服务”阈值从 50fps+ 收回到约 35fps 并接受 `playbackRecovered` 作为连续供给证据；`firstFrameLatencyObserved` 在已恢复连续播放但尚未 clean-anchor 时改报 `playbackRecoveredAnchorPending`。验证：`cargo test -p xbxengine media::video::render::renderer`、`cargo test -p xbxengine transport::rtc::recovery::runtime_state`、`cargo test -p xbxrc --lib native_video::scheduling`。
- 2026-05-11: 修复 APP 首开时“手柄能震动但没有输入采样”的恢复回归：`ohmygamepad-sdl3` 的 `resume_shell_sampling / activate_sampling / set_suspended(false)` 共用恢复路径现在会先显式把 runtime lifecycle 提升回 `Active`，避免停留在 `BackgroundWarm` 时只有设备事实与 haptics、却不再向上发布 `slotSnapshot`/输入边沿；并新增 `resume_shell_sampling_promotes_background_warm_to_active` 回归测试锁住该语义。验证：`cargo fmt`、`cargo test -p ohmygamepad-sdl3 resume_shell_sampling_promotes_background_warm_to_active`、`cargo check -p xbxrc`。
- 2026-05-11: 将 `display_options` 的设置入口改成更易用的“画面风格”预设：设置页主入口现显示 `标准 / 清晰 / 柔和` 三档预设，参数滑杆下沉为 `手动微调`；列表摘要会在命中预设时直接显示预设名，手动值显示为 `手动微调`；同时将默认 `display_options.sharpness` 从 `2` 调整为 `0`，让 `标准` 真正对应“不额外锐化”。验证：`node` 解析 `src/i18n/locales/zh.json` 与 `src/i18n/locales/en.json` 通过；`cargo check -p xbxrc` 通过；`pnpm exec vue-tsc --noEmit` 仍受仓库既有错误 `SettingGamepadSection.vue`、`PlayerClient.ts`、`browser-runtime.ts` 阻塞。
- 2026-05-11: 校准设置页字段文案：统一修正中英文 `setting.fields.*` 的标题、描述与关键选项说明，补齐此前缺失的 `xhome_auto_connect_server_id`、`debug`、`runtime_trace_mode` 文案项，并将串流、连接、输入、诊断相关设置的描述收口为“实际控制什么、改动后会怎样”的表达。验证：`node` 解析 `src/i18n/locales/zh.json` 与 `src/i18n/locales/en.json` 通过。
- 2026-05-11: 继续清理设置有效性与暴露面：移除设置系统中的 `xhome_auto_connect_server_id` 展示定义与分组返回；复核当前状态确认 `performance_style`、`video_format`、`enable_audio_control` 仍在真实链路生效，`power_on` 仍进入 session policy，但当前 home 启动链路已自带 ready/wake 重试，用户侧影响显著收窄。验证待本轮 `json parse` 与 `cargo check -p xbxrc`。
- 2026-05-11: 继续收紧设置页暴露面：将 `power_on` 从设置定义与中英文文案中移除，保留底层 `session policy` 与 home 启动链路的自动唤醒/ready 重试能力，确保用户可见配置与真实行为一致。验证待本轮 `json parse` 与 `cargo check -p xbxrc`。
- 2026-05-11: 调整串流体验页 section 结构：将原单一“画质与传输”拆成 `通用 / 主机串流 / 云游戏` 三段，主机与云配置分别归位，保留字段 key、RPC 分组与底层配置语义不变。验证待本轮 `json parse` 与 `vue-tsc`。
- 2026-05-11: 调整“连接与主机”页 section 结构：拆成 `通用连接 / 主机串流` 两段，将 `xhome_turn_fallback` 归到主机串流，其余连接项保留在通用连接，字段 key 与底层行为不变。验证待本轮 `json parse`。
- 2026-05-11: 继续收紧串流设置分层：将 `codec` 从“串流体验 -> 通用”移到“高级与诊断 -> 高级”，让串流体验页优先保留直接体感相关项，字段 key 与配置语义不变。验证待本轮 `vue-tsc`。
- 2026-05-11: 统一设置行的无描述对齐规则：`SettingSectionList` 与 `SettingInputToolsSection` 都改为固定两行文案轨道，描述缺失时保留隐藏占位，确保右侧值列在不同 section 内保持一致的垂直锚点。验证待本轮 `vue-tsc`。
- 2026-05-11: 收紧“画面风格”弹窗默认复杂度：预设仍直接展示，手动参数区改为折叠在 `更多调整` 次级入口后面，避免弹窗打开就暴露“手动微调”大块参数面板，保留底层参数能力不变。验证待本轮 `json parse` 与 `vue-tsc`。
- 2026-05-11: 修正“画面风格”弹窗折叠后的默认焦点：首焦点从隐藏的参数输入框改到 `标准` 预设按钮，避免弹窗打开后焦点落到不可见节点。验证待本轮 `vue-tsc`。
- 2026-05-11: 将“画面风格”收成纯三选一弹窗：移除弹窗内手动参数区与相关文案，仅保留 `标准 / 清晰 / 柔和` 预设选择；设置列表摘要不再显示“手动微调”，遗留非预设值按 `标准` 展示，保证交互与文案一致。验证待本轮 `json parse` 与 `vue-tsc`。
- 2026-05-11: 将设置页里的 `画面风格` 改为复用通用单选弹窗：`display_options` 在设置页改走 `singleSelect + SettingSingleSelectPopupSheet`，选择值在前端映射回既有 display preset 参数对象，移除设置页专用 `SettingDisplayOptionsSheet` 链路；串流页内实时画面调整能力保留。验证待本轮 `vue-tsc`。
- 2026-05-11: 修复 `画面风格` 复用单选弹窗后的国际化回退：补齐 `setting.fields.display_options.options.{standard,clear,soft}` 中英文键，确保通用单选弹窗显示本地化预设名。验证待本轮 `json parse`。
- 2026-05-12: 调整串流体验分组：将 `codec` 从“高级与诊断”移回“串流体验 -> 通用”，恢复为用户直接可见的串流体验项，字段与配置语义不变。验证待本轮 `vue-tsc`。
- 2026-05-11: `webrtc-direct` 前端画像驱动调度落地：新增 [`src/streaming/runtime/browser-runtime-profile.ts`](src/streaming/runtime/browser-runtime-profile.ts)（画像分类、expected fps 上沿、`ProfilePolicyPreset`、相对帧率带宽判定）；[`browser-runtime.ts`](src/streaming/runtime/browser-runtime.ts) 接入 warmup/带宽/质量与显示 dwell 与自适应渲染阈值；snapshot / diagnostics 增加 `frontEnd*` 观测字段；单测 `browser-runtime-profile.test.ts` + diagnostics 用例。Report：[`reports/2026-05-11-webrtc-direct-frontend-profile-driven-scheduling.md`](reports/2026-05-11-webrtc-direct-frontend-profile-driven-scheduling.md)。验证：`pnpm vitest run src/streaming/runtime/browser-runtime-profile.test.ts src/streaming/diagnostics.test.ts`、`pnpm lint:fix`。
- 2026-05-04: 修复右摇杆 `Y` 方向问题：1）SDL3 源里 `Axis::RightY` 的 `AxisChanged` 与 `capture_gamepad_baseline_state` 快照路径符号已对齐，避免 prime/恢复后间歇跳变。2）串流出包按 Xbox 流端竖轴约定对 **左右摇杆 `Y` 均在边界取反**（`InputPacketEncoder` 与 `xbxengine` `data_channel_state` 一致），逻辑坐标仍保持「上为正」的 `LogicalPadState`，仅线包符号对齐远端。验证：`cargo fmt`、`cargo check -p xbxengine`、`cargo check -p ohmygamepad-sdl3`、`pnpm vitest run src/player/protocol/input/InputPacketEncoder.test.ts`。
- 2026-05-02: 手柄卡片补齐 SDL 设备判定信息展示：`GamepadProfileCard` 现在直接展示分类标签、置信度、VID/PID、SDL 设备类型、mapping、能力摘要、判定 reasons 与 device path，便于在无实机条件下直接分辨掌机内建手柄、虚拟 XInput 与 Steam Virtual 恢复路径。验证：`pnpm lint:fix`。
- 2026-05-02: 将 `0x0B05:0x1B4C` 加入 SDL3 已知掌机内建手柄白名单，覆盖 `ROG Xbox Ally X` 的内建 `XInput` 视图；现有分类会稳定打出 `掌机内建 + 虚拟手柄`，便于把“掌机内建兼容视图”与普通外接 `XInput Controller` 区分开。验证：`cargo fmt`、`cargo check -p xbxrc`。
- 2026-05-02: 将 Windows 桌面 haptics 主线切到 `win-xbox-haptics`，并对 `0x0B05:0x1B4C + XInput` 视图补 host 侧能力兼容：Windows 下默认 rumble provider 现在优先走 `Windows.Gaming.Input` 四路震动，`ROG Xbox Ally X` 内建 `XInput` 视图在前端快照中会显式呈现 `扳机震动` 能力，并统一显示为 `Xbox Controller`。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-02: 修复 Xbox 大屏模式直开与 Game Bar 返回后的手柄采样恢复缺口：Rust 壳层新增 `RunEvent::Resumed -> activate_sampling()` 恢复边沿，前端 `AppShellLayout` 新增 `document.visibilitychange -> visible` 的采样恢复桥，避免窗口重新活跃但未稳定派发 `Focused(true)` 时 runtime 长留在 suspended/未重建基线状态。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-02: 进一步放宽手柄采样恢复链：补 `Resized -> is_minimized() == false` 的对称恢复边沿；前端恢复从单次 `activateSampling()` 升级为“读取 `sampleSeq/sampledAtMs` 进度 token 后做结果确认”；恢复窗口内增加 `0ms / 150ms / 500ms` 三拍自动重试，覆盖 Xbox 大屏模式与 Game Bar 壳层切换里的延迟交接时序。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-04-28: 为播放期停播根因分析补齐 `StopRuntime/closeRuntime/closeExecution/closeSession/app terminate` 诊断埋点：前端 `runtime-host`、`stream execution` 与 Rust `xbxengine/shell` 现在统一记录 stop reason 与关闭阶段事件，`app` 退出链额外写入 `terminateRequested/terminateCompleted`，`xbxengine.shutdown()` 也会显式带上 `appTerminate` reason，便于区分页面卸载、用户退出、重试、关机与宿主收口。验证：`cargo check -p xbxrc`；`pnpm exec vue-tsc --noEmit` 当前仅剩设置页既有错误 `SettingGamepadSection.vue(176/535/540)`。
- 2026-04-27: 修复 `native_video` 的 host-stall presenter reset 误清空 viewport 呈现态：`reset_presenter_for_host_stall_recovery()` 现在只拆 presenter，保留 `latest_video_host_present_time_ms / host_frame_present_epoch / displayedFrame* / noPending* / cadencePhase` 等 host 指标，避免旧帧仍在屏上保活时 stats 被打回“从未 present”，进而把恢复策略误推向更激进的 reconnect。验证：`cargo test host_stall_presenter_reset_preserves_viewport_present_metrics`、`cargo test reset_viewport_present_runtime_state_clears_display_runtime_metrics`。
- 2026-04-25: 修复 `video_source::timeline` 的 `Repairing` ledger 退役缺口，链路跨过 stable recovery gate 并回到 `Healthy` 时会同步清理 lingering `Repairing` frame ledger，避免 owner 已回稳后后续帧仍继续带 `repairing` 标签；新增 `healthy_recovery_retires_lingering_repairing_frame_ledgers` 回归测试，并复验 `frame_without_recovery_ledger_defaults_to_steady_disposition`。
- 2026-04-25: 把 recovery owner frame 显式下沉到 `decode -> pacer -> render -> host` 全链路，新增 `recovery_owner_rtp_timestamp` 贯通 `Assembled/Encoded/Decoded/Render/Host` frame；同一 `recovery_epoch` 内统一按“owner frame 命中优先，其次再看 clean-anchor / rtp / 时间戳”做 latest-only 价值选择，避免各层只拿 `epoch` 近似后产生分叉。验证：`cargo test -p xbxengine media::video::decode::video_decode::tests`、`cargo test -p xbxengine media::video::pacer::actor::tests`、`cargo test -p xbxengine media::video::render::renderer::tests`、`cargo test -p xbxrc --lib native_video::scheduling`、`cargo test -p xbxrc --lib native_video::tests`。
- 2026-04-25: 推进关键帧恢复第二阶段到 host wgpu 末端，抽出 `compare_host_present_frame_value()` 统一 `presenters.rs` 的 macOS/Windows latest-frame 覆盖规则；host latest slot 现在保留更高 `recovery_epoch_tag` 的恢复帧，同 epoch 继续按 `rtp_timestamp/rendered_at_ms` 追最新，避免最后一公里把高 epoch 恢复帧重新冲掉。验证：`cargo test -p xbxrc --lib native_video::scheduling`、`cargo test -p xbxrc --lib native_video::tests`。
- 2026-04-25: 按“单 unresolved recovery session + owner frame 前移”收口关键帧恢复链：`runtime_stats_sink` 现在在同一 recovery 内复用 `transportAwaitRecoveryAnchor` session，但会在更晚 keyframe 响应到达时前移 owner frame；旧 owner 的 `decoded / clean-anchor submission` 不再闭合当前恢复。同步把 decode/pacer 的候选比较升级为“更高 recovery epoch 优先”，避免 decode 后和 pacer 末端继续保旧 owner。验证：`cargo test -p xbxengine runtime_stats_sink::tests`、`cargo test -p xbxengine media::video::decode::video_decode::tests`、`cargo test -p xbxengine media::video::pacer::actor::tests`、`cargo test -p xbxrc --lib native_video::scheduling`。
- 2026-04-25: 收紧 recovery coordinator 的 `PLI` 完成口径：`check_idr_completed()` 现在只接受当前 `transportAwaitRecoveryAnchor` episode 的新 keyframe 响应进展或当前 epoch clean-anchor，不再被旧 episode 的 `decoded` 残留状态提前清掉当前 `idr_in_flight`；已补 coordinator 回归测试覆盖“旧 decoded 不清当前 in-flight”和“当前轮次 keyframe packet 可清当前 in-flight”。验证：`cargo test -p xbxengine transport::rtc::recovery::coordinator`。
- 2026-04-25: 收紧 keyframe recovery 的 epoch ownership 与窗口语义：`record_transport_clean_anchor_submission()` 仅允许 `submission_epoch == transport_recovery_epoch` 提升当前 clean-anchor，旧 epoch 结果只保留为历史提交事实；`FrameRecovery` 从单个 `900ms` 长窗改成“短 `PLI refresh` 窗 + 长 `response timeout` 窗”，continuation-only 场景可按短窗重发 `PLI`，避免近 1 秒才再次触发恢复动作。验证：`cargo test -p xbxengine runtime_stats_sink::tests`、`cargo test -p xbxengine transport::rtc::recovery::state_machine::tests`、`cargo test -p xbxengine transport::rtc::recovery::action_coordinator::tests`。
- 2026-04-25: 把 trace 驱动的 continuation-heavy 黑盒合同落到 `session` 入口：当 `transportAwaitRecoveryAnchor` 已请求过 `PLI`、后续仅看到 recent continuation delta、decode/present 仍停在旧输出、最近仍持续收包时，`session::policy` 会在短 refresh 窗后重开一次 `PLI`，不再把这类未恢复网络事实吸成 `coalesced/cooldown`。同步补齐三段入口合同：首拍先发 `PLI`、连续 unresolved 时继续 refresh `PLI`、只有 clean-anchor 真到位后才停止动作。验证：`cargo test -p xbxengine recovery_integration_trace_contract_continuation_heavy_first_hit_requests_pli_not_reconnect --lib`、`cargo test -p xbxengine recovery_integration_trace_contract_continuation_heavy_refreshes_pli_without_reconnect --lib`、`cargo test -p xbxengine recovery_integration_trace_contract_continuation_heavy_second_refresh_still_requests_pli --lib`、`cargo test -p xbxengine recovery_integration_trace_contract_continuation_heavy_stops_only_after_clean_anchor --lib`。
- 2026-04-25: 把 `rxClosed/upstreamSenderDropped` 与首帧时延拆段也收成 stats 黑盒合同：`runtime_stats_sink` 现在稳定保留 ingress close intent 的 `upstream_cause`，同时 `latest_first_frame_latency_observation` 与 episode `transport_detail` 统一输出 `WaitingResponse -> AnchorSeen -> Decoded -> CleanAnchorCommitted -> DisplayStable` 的完整链路；sent-only 场景也会显式落成 `terminalPhase=WaitingResponse incompleteReason=noIdrPacket`。验证：`cargo test -p xbxengine rx_closed_keeps_close_intent_upstream_cause_after_other_observations --lib`、`cargo test -p xbxengine first_frame_latency_observation_records_complete_stage_breakdown --lib`、`cargo test -p xbxengine first_frame_latency_observation_marks_no_idr_packet_when_only_pli_was_sent --lib`。
- 2026-04-25: 修复 `native_video` host mailbox 的 media epoch 开闸条件，`ScheduledFrameSlot` 现在会为“更高 `recovery_epoch_tag` 的恢复桥接帧”切新 media epoch，避免 recovery continuation 因 `frame_seq` 回卷被持续记成 `already_presented` 而卡死在旧显示帧；已补 `scheduling` 回归测试覆盖“新 recovery epoch continuation 开闸”和“同 epoch 回卷 continuation 继续拒绝”两类场景。验证：`cargo test -p xbxrc --lib native_video::scheduling`、`cargo test -p xbxengine scheduling --lib`。
- 2026-04-25: 修复 host present 卡死时恢复归因与本地自愈的错位：`VideoSchedulingOwner` 在 `RebuildingSupply` 遇到 clean-anchor 已恢复且 `hostPresentStalled` 持续时，现在会切到 `SupplyStarved(hostPresentStalled)`；runtime `maybe_reset_native_presenter_for_host_stall()` 去掉 `freshRendererSubmit/freshHostRenderSlot/freshDecode/freshCleanAnchor` 延期门槛，允许 presenter reset 真正执行。验证：`cargo test -p xbxengine rebuilding_supply_with_clean_anchor_and_host_present_stall_switches_to_host_stall_supply_reason --lib`、`cargo test -p xbxengine runtime_local_host_stall_reset_still_runs_when_renderer_submit_is_fresh --lib`、`cargo test -p xbxengine playback_phase_int06_clean_anchor_progress_must_exit_toward_serving_not_starved_only --lib`、`cargo test -p xbxengine runtime_local_host_stall_reset_retries_after_cooldown_when_display_tick_advances --lib`、`cargo test -p xbxengine transport_await_request_pli_keeps_rebuilding_supply_in_active_recovery --lib`。
- 2026-04-25: 修复 `native_video` host mailbox 在 recovery keyframe 已显示后继续长期保旧帧的问题：`ScheduledFrameSlot` 不再让 displayed keyframe 永久压过同 epoch 的新 plain delta，fresh continuation 可以在 anchor 已落地后接管显示；新增 `fresh_plain_delta_replaces_displayed_recovery_keyframe_after_anchor_commit` 回归测试锁定 `frame_submit accepted -> frame_slot_take_retained old frame` 的 trace 形态。验证：`cargo test -p xbxrc --lib native_video::scheduling::tests::fresh_plain_delta_replaces_displayed_recovery_keyframe_after_anchor_commit`。
- 2026-04-27: 统一 `decode -> pacer -> render -> host` 的 latest-only comparator：共享 `XbxEngineLatestOnlyFrameMeta + compare_latest_only_frame_meta`，收口 recovery epoch / owner / value rank / RTP / rendered time 的判定顺序；`pacer` 与 `host scheduling` 只保留各自阶段必需的补充语义，plain delta 现在可以稳定接管“无 recovery signal 的旧 keyframe”，owner 偏好只在 recovery-valued 帧之间生效。验证：`cargo test -p xbxengine media::video::render::renderer -- --nocapture`、`cargo test -p xbxengine media::video::pacer::actor -- --nocapture`、`cargo test -p xbxrc --lib native_video::scheduling -- --nocapture`、`cargo test -p xbxrc --lib native_video::tests::host_present_frame_value -- --nocapture`。
- 2026-04-27: 修复 RTC lifecycle 重建时 `video ingress close intent` 被提前清空，避免旧 `RtcVideoFrameSource` 在 `rx closed` 时把 `rebuildPeerConnection/stackStop` 误记成 `upstreamSenderDropped`；`reset_runtime_state()` 现在保留 ingress close intent，仅清空新会话真正需要重置的运行态字段。验证：`cargo test -p xbxengine reset_runtime_stats_preserves_video_ingress_close_intent -- --nocapture`、`cargo test -p xbxengine rx_closed_records_close_cause_label -- --nocapture`。
- 2026-04-27: 修复首帧前 liveness no-progress 误判：`RtcSessionPolicy` 现在把 `inbound_primary_video_bytes_total` 与 `latest_video_track_status.video_packet_count_total/video_bytes_total` 纳入首帧前 progress token，RTP/track 仍在推进时会重置 no-progress 计时，避免 `videoRtcpFeedbackTargetPending` 窗口被过早升级成 `transportReconnectCandidate:livenessNoProgressTimeout`。验证：`cargo test -p xbxengine pre_first_frame_transport_progress_uses_relaxed_liveness_timeout -- --nocapture`、`cargo test -p xbxengine pre_first_frame_ingress_progress_resets_liveness_timer -- --nocapture`、`cargo test -p xbxengine recovering_pre_first_frame_without_transport_progress_uses_relaxed_liveness_timeout -- --nocapture`。
- 2026-04-27: 修复 `videoRtcpFeedback` availability 的首包绑定观测缺口：当 track 已 open、首个 inbound RTP 刚把 `receiver + media_ssrc` 绑定起来时，`observe_inbound_rtp()` 现在立即回写 `feedbackTargetBound/ready`，避免 trace 里长期残留旧的 `unbound` 状态误导恢复判断。验证：`cargo test -p xbxengine observe_inbound_rtp_marks_video_rtcp_feedback_ready_when_track_is_already_open -- --nocapture`。
- 2026-04-27: 修复 rebuild 后 `videoRtcpFeedbackTargetPending` 的 bootstrap 缺口：`apply_remote_description()` 现在会从 remote answer 预装视频 `track_id + media_ssrc + rtcp feedback + twcc ext`，`register_track_open()` 到达时可在首个 video RTP 之前直接恢复 video feedback target，避免重建轮次里“PLI 等首包、首包又等 PLI”的闭环卡死。验证：`cargo test -p xbxengine remote_answer_bootstrap_binding_restores_video_feedback_target_before_first_video_packet -- --nocapture`、`cargo test -p xbxengine observe_inbound_rtp_marks_video_rtcp_feedback_ready_when_track_is_already_open -- --nocapture`、`cargo test -p xbxengine request_video_pli_reports_feedback_target_unavailable_without_twcc_feedback_target -- --nocapture`。
- 2026-04-27: 收紧 `video_source` 的 continuation admission：`first_frame_acquired` 不再直接作为新 recovery 窗口里的 continuation 通行证，只有“当前不在等 recovery anchor”或“已经进入 clean-anchor 建链窗口”时才放行旧输出 continuation；同步保留 clean-anchor 建链窗口里的 stale-wait continuation 提交，避免一边大量 `continuationAcceptedWhileAwaitingIdr`、一边长期卡在 `noCleanAnchorCommit`。验证：`cargo test -p xbxengine transport::rtc::stream::video_source::source::tests -- --nocapture`。
- 2026-04-27: 深审并收紧 `session/facts` 的恢复窗口口径：`RecoveryObservationSnapshot` 现在只把近期 `ingress/decode/render` 计入当前媒体阶段信号，避免旧 decode/present 残留触发新的 transport-await reconnect fallback；`facts` 的 continuation 降级只接受绑定到同一 recovery episode 的 H264 inspection，避免无关 continuation 把当前 `Decoded` 回退成 `ContinuationSeen`。同步复验“等待关键帧期间旧帧继续保活显示”的相关合同未受影响。验证：`cargo test -p xbxengine transport::rtc::session::policy::tests::recovery_observation -- --nocapture`、`cargo test -p xbxengine recovery_progress_does_not_downgrade_from_unbound_continuation_observation -- --nocapture`、`cargo test -p xbxengine stale_transport_await_does_not_replay_during_steady_progress -- --nocapture`、`cargo test -p xbxengine host_present_stalled_with_fresh_transport_still_stays_out_of_reconnect -- --nocapture`、`cargo test -p xbxengine active_adapter_idle_timeout_is_suppressed_when_render_output_is_still_fresh -- --nocapture`。
- 2026-04-28: 收紧 clean-anchor recovery 收口语义：`VideoSchedulingOwner` 现在只把 committed clean anchor 视为正式 clean-anchor fact，`SubmittedCleanAnchor` 保留为 pending；同时新增 `hostVisibleAnchorPending` bridge，并允许 host present 在“同 recovery epoch、同 episode、serviceable continuation 已上屏”时兜底提交 `CleanAnchorCommitted`，避免 exact RTP 命中失败时 owner/latency 口径分叉。验证：`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner -- --nocapture`、`cargo test -p xbxengine transport::rtc::stack::runtime_port -- --nocapture`、`cargo test -p xbxengine runtime_stats_sink::tests -- --nocapture`、`cargo test -p xbxengine recovery_integration -- --nocapture`。
- 2026-04-27: 修复首帧前协商进展被误判成 `livenessNoProgressTimeout`：`RtcSessionPolicy` 现在把 `latest_remote_answer_observation`、`latest_rtc_builder_observation` 与 `videoRtcpFeedback/videoTwccFeedback` availability 变化纳入 pre-first-frame progress token，远端 answer 刚接受或 feedback target 刚推进时会重置 no-progress 计时，给当前 rebuild 协商窗口继续完成的时间，避免 trace 中 `remoteAnswerAccepted` 后立刻再次 `transportReconnectCandidate:livenessNoProgressTimeout`。验证：`cargo test -p xbxengine pre_first_frame_connecting_remote_answer_progress_resets_liveness_timeout -- --nocapture`、`cargo test -p xbxengine reconnect_command_is_throttled_and_re_emitted_during_continuous_recovering -- --nocapture`、`cargo test -p xbxengine cloud_lifecycle_reconnect_interval_is_more_relaxed_than_non_cloud -- --nocapture`。
- 2026-04-24: 修复串流输入包中手柄左右摇杆 `Y` 轴符号方向，`InputPacketEncoder` 改为按流端坐标系对 `leftStick.y/rightStick.y` 取反编码，并新增 `InputPacketEncoder.test.ts` 断言避免上下颠倒回归。
- 2026-05-11: 统一摇杆坐标系合同：SDL3 采样层明确保留官方 `up/left<0、down/right>0` 原义，键盘模拟改为同号语义，前端 `InputPacketEncoder` 与 Rust `data_channel_state` 发包统一收成 `stream_protocol_stick_y()` 单次转换，并按 `better-xcloud` 的 `ThumbYAxis=-y` 口径补齐回归测试，避免读取层与发送层各自做隐式取反。验证：`cargo fmt`、`cargo test -p ohmygamepad-sdl3 vertical_controls_follow_sdl_y_axis_sign`、`cargo test -p xbxengine gamepad_packet_converts_sdl_style_y_only_at_stream_boundary`、`pnpm exec vitest run src/player/protocol/input/InputPacketEncoder.test.ts`、`pnpm lint:fix`。
- 2026-05-11: 为 SDL3 手柄源补齐右摇杆 `Y` 轴回归测试：`source.rs` 新增 `translate_right_y_axis_keeps_normalized_sign`，锁定 `Axis::RightY` 与其它摇杆轴保持同号归一化，避免右摇杆上下方向再次被单侧反转。验证：`cargo fmt`、`cargo test -p ohmygamepad-sdl3 translate_right_y_axis_keeps_normalized_sign`。
- 2026-04-24: 完成文档根目录标准化与 `docs1` cutover，统一为单一 `docs/` 根目录，迁入 archive、ISU、RFC、Report、references、test-assets，并新增 [`README.md`](README.md)。RFC: [`rfcs/2026-04-24-docs-standardization-and-docs1-cutover.md`](rfcs/2026-04-24-docs-standardization-and-docs1-cutover.md)；Report: [`reports/2026-04-24-docs-standardization-and-docs1-cutover.md`](reports/2026-04-24-docs-standardization-and-docs1-cutover.md)
- 2026-04-24: 修复 dual-phase recovery chain 的阶段错位：clean-anchor 改为由携带 commit epoch 的 recovery IDR 在 decode 完成后提交，stable settle 收口改为强依赖 fresh host present，避免 decode-only 提前关单；已补 `source/decode/ramp_guard` 回归测试。
- 2026-05-02: 补齐 SDL3 手柄采样恢复链路：启动与窗口重新激活统一走 `activate_sampling`，Windows 窗口失焦不再让 runtime suspend，SDL 后端打开 `SDL_JOYSTICK_ALLOW_BACKGROUND_EVENTS`，`prime sampling` 改为发送全量 `Snapshot` 基线并由 runtime `RefreshSnapshot` 立即消费，保证 Xbox Full Screen Experience 壳层切换后手柄当前状态能重新建样。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-02: 为 SDL3 手柄源补齐兼容面治理：后端默认加载项目内 `src-tauri/resources/gamecontrollerdb.txt` 并支持额外 mapping overlay，新增 `OHMYGAMEPAD_SDL3_IGNORE_GUIDS / IGNORE_VID_PIDS / IGNORE_NAME_CONTAINS` 过滤规则，source 对热插拔与 remap 建立基于 `path/serial/vidpid/name` 的重复设备防御，减少壳层虚拟手柄和重复 attach 污染采样链。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-02: 补齐 SDL3 手柄能力与诊断链：`sdl3_capabilities` 新增 `supports_accel / supports_gyro`，descriptor 按 SDL sensor 实际能力上报；source 增加 `sdl3_unmapped_joystick / sdl3_ignored_gamepad / sdl3_gamepad_diagnostics` 结构化日志，便于现场采集 GUID、VID/PID、mapping 与 capability；前端 `gamepadUIListener` 新增显式 reset 事件，在 UI/stream 路由切换前统一清空 pressed/repeat/combo 边沿状态。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-05-03: 收紧手柄采样恢复策略与壳层恢复语义：Tauri 窗口最小化路径不再触发 `set_suspended(true)`，新增 `resumeShellSampling` 轻恢复 RPC，壳层前端恢复链改为 `set_input_policy(shared) + prime_sampling + refresh_snapshot` 主路径，保持 SDL 后台事件常开并把焦点/可见性恢复收成基线重建。验证：`cargo fmt`、`cargo check -p xbxrc`、`pnpm lint:fix`。
- 2026-04-23: 修复 `cargo check -p xbxrc` 构建脚本资源校验失败：移除失效的 `../target/release/SDL3.dll` Tauri 资源映射。验证：`cargo check -p xbxrc` 通过。
- 2026-04-23: 修复前端 `gamepad-listener` 的重复触发与策略切换状态机，按键 repeat 改为独立定时器驱动，并在 `stream-only` 策略切换及 listener 停止时统一清理 pressed/combo/timer 状态，避免手柄输入卡键与重复触发残留。
- 2026-04-23: 完成双阶段恢复链路改造（single media commit + display settle），clean anchor 提交改为首个可服务 IDR 完成即提交，ramp guard 拆分 acknowledge/close，并在 picture recovery episode 增加 `firstDecodeToCleanAnchorCommittedMs`、`cleanAnchorCommittedToDisplayStableMs` 两段尾延迟 trace。RFC: [`rfcs/2026-04-23-dual-phase-recovery-chain.md`](rfcs/2026-04-23-dual-phase-recovery-chain.md)
- 2026-04-23: 去除 gamepad 配置类 RPC 的重复事件转发，改为由 shell 后台订阅桥统一广播 `runtimeSnapshot/slotSnapshot/devicesChanged`，避免一次配置变更触发双份手柄事件。
- 2026-04-23: 修复手柄采样节流语义错位，收敛 `sampleSeq` 为真正的流输入变化令牌，并在 `ohmygamepad` runtime 落地 `uiPushRateHz` 与 `streamPushMode/streamPushRateHz`，避免空闲状态持续推送与 UI 事件风暴。
- 2026-04-23: 调整 Windows Tauri 窗口手柄暂停策略，不再因 `Focused(false)` 直接 suspend gamepad，改为在窗口最小化时再暂停，以避免 Xbox Full Screen Experience 下手柄输入被系统焦点切换误杀。
- 2026-04-22: 修正 Windows 播放器策略对 `D3D11 texture` 的错误路由，避免 `GpuDirect presenter` 误绑定仅支持 CPU surface 的 `Wgpu effect pipeline`，导致 `present_frame` 在 `can_process(native_handle)=false` 处提前返回、宿主 render loop 空转且始终黑屏。
- 2026-04-22: 修正 Windows `ffmpeg-d3d11va` 硬解输入路径，停止将 Annex-B access unit 重打为 AVCC，改为直接喂完整 Annex-B，并移除该后端上的 `AV_CODEC_FLAG2_CHUNKS`，避免首个 IDR 被硬解静默吞掉后长期无输出。
- 2026-04-22: 新增 RFC [`rfcs/2026-04-22-rtc-recovery-boundary-convergence.md`](rfcs/2026-04-22-rtc-recovery-boundary-convergence.md)，规划 RTC 恢复系统的收边界、减解释与高置信快速决策路径收敛方案。
- 2026-05-15: 修复 webrtc-direct WebGL pacing 后续问题：禁止 display degrade 自动从 WebGL2/CAS 切到原生 video，并降低 CAS quality shader 采样成本，优先保留 WebGL 路径解决帧节奏抖动。验证：`pnpm vitest run src/streaming/runtime/browser-runtime.test.ts src/player/infra/render/Renderers.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-15: 审查并收紧 webrtc-direct 前端策略：quality ladder 不再因本地 render starvation 降档，所有发送端策略保持 `maxFramerate=60`；display degrade 改为单次压力进 `displayL1`、连续压力才进 `displayL2`，避免旧策略想当然切重档。验证：`pnpm vitest run src/streaming/runtime/browser-runtime.test.ts src/player/infra/render/Renderers.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-15: 进一步拆分 `recoveryCause` 与 `senderPolicyCause`，明确只有网络拥塞、decode backpressure、控制通道异常可进入发送端策略解释；本地 `renderStarvation/renderBackpressure` 只保留为本地 render/display 调整原因，避免本地 present/WebGL 抖动误降远端码率。验证：`pnpm vitest run src/streaming/runtime/browser-runtime.test.ts src/player/infra/render/Renderers.test.ts`、`pnpm lint:fix`、`pnpm build`。
- 2026-05-15: 修复 webrtc-direct 高 RTT 与 60fps 识别策略：前端内容帧率改回显式 `fps=60` 优先，避免低观测上沿把目标误判成 `content30`；网络拥塞判定去掉 RTT 单项触发，要求 loss/jitter/feedback/packet age/低码率等实质压力证据，避免健康 cloud highRtt 链路反复 `L0 -> L2`。验证：`pnpm vitest run src/streaming/runtime/browser-runtime-profile.test.ts src/streaming/runtime/browser-runtime.test.ts src/player/infra/render/Renderers.test.ts`。
- 2026-05-15: 为 30/60fps 源统一补齐浏览器视频流原始帧率识别：`requestVideoFrameCallback` 现在从 `mediaTime + presentedFrames` 推出 `sourceFpsEstimate/sourceFrameIntervalMs`，runtime 用独立视频帧源窗口优先决定 `content30/content60`，并把源帧率估算写入 render trace，后续 pacing 以源帧率分档。验证：`pnpm vitest run src/streaming/runtime/browser-video-display.test.ts src/streaming/runtime/browser-runtime-profile.test.ts src/streaming/runtime/browser-runtime.test.ts src/player/infra/render/Renderers.test.ts`。
- 2026-06-29: 继续收紧播放停滞恢复链的 current-episode 判据：`displayed_idr_serving_from_stats()`、owner `has_established_displayed_idr_fact()`、reference chain prior-output、session steady hold、recovery exit 与 completion 时钟统一改读 current displayed/playback/fresh anchor helper，旧 episode 的 `displayedIdr/playbackRecovered/freshAnchor` 不能再释放 owner、压低 reference chain、维持 steady 或清 decoder debt。新增 stale displayed/playback/prior-output 回归测试；验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::decode_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::receive::insert_gate --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::facts --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::runtime_state --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib -- --nocapture`；fresh trace 待验收。
- 2026-06-29: 继续审查播放卡死恢复控制面，修复 `display-stable/reference-chain/fps` 裸投影阻断恢复的问题：runtime reconnect gate 与 `ExpensiveRecoveryGate` 不再把旧 `display-stable`、`reference_chain=continuous/repairing` 或平滑 FPS 当作健康播放证据，必须有真实新鲜 decode/present、current clean anchor 或成功边沿；`remote_picture_recovery_terminal_active_from_stats()` 只有在 display-stable 绑定 current clean anchor 时才清 terminal；owner `displayed_idr_control_plane_active()` 要求 `clean_anchor_observed_at_ms`。新增 stale display-stable、repairing reference-chain、owner observed-at 回归测试；验证：`cargo test -p xbxengine reconnect_lifecycle --lib -- --nocapture`、`cargo test -p xbxengine runtime_does_not_block_reconnect_from_stale_display_stable_without_fresh_output --lib -- --nocapture`、`cargo test -p xbxengine runtime_blocks_receiver_waiting_reconnect_candidate_when_connected_media_is_serviceable --lib -- --nocapture`、`cargo test -p xbxengine trace_like_repairing_reference_chain_without_fresh_output_does_not_claim_healthy_playback --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::contract --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`；`cargo test -p xbxengine reconnect_recovery_matrix --lib -- --nocapture` 当前仍有既有 reconnect-settled-keyframe defer 断言失败，和本轮 stale display-stable gate 无关。
- 2026-06-29: 继续针对旧 trace 的 `hostMailboxRetainedDisplayedStale + presentAgeMs 325ms+ + noPendingPressure=normal + stable-serving` 漏判自查：`display_supply` 新增 `host_retained_displayed_stale()`，让 `cadence=starved` 且 retained displayed 超过 degraded/critical present age 时分别进入 Degraded/SoftNoPendingAge；`VideoSchedulingOwner::chronic_low_present_throughput()` 只在 host cadence 仍为 `steady` 时吸收低 present 吞吐，避免 `starved` retained-stale 被误当吞吐瓶颈而继续 playing。新增 trace-like owner 回归测试；验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::policy::display_supply --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo test -p xbxrc native_video::scheduling --lib -- --nocapture`；fresh trace 仍待验收。
- 2026-06-29: 继续自查同类 stale host present 吸收口：`runtime_state` 的 `displaySupplyDegraded/healthy` 诊断吸收现在要求 `host_presentation_serviceable()`，fresh decode 不能再把 300ms+ stale host present 伪装成恢复完成或 display-constrained；`present_cadence` 的 decode/present gap 与 host serviceable 判据加入 `display_age_ms < 300ms` guard；`display_supply::present_pipeline_stressed()` 排除 retained-stale host frame，避免同一故障在 pacer/owner 侧继续被解释为普通吞吐瓶颈。新增 runtime_state、present_cadence、display_supply 回归测试；验证：`cargo test -p xbxengine transport::rtc::recovery::runtime_state --lib -- --nocapture`、`cargo test -p xbxengine media::video::present_cadence --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::display_supply --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。
- 2026-06-29: 复盘 fresh trace `runtime-trace-1782710072770-1.jsonl`，卡死仍集中在旧 displayed frame 被保留：host 连续 `hostMailboxRetainedDisplayedStale`，`PlaybackRecovered` 却由旧 `frameSeq=407` 写入，且 `ownerState=supply-starved`；receive 侧 hard keyframe request 只写 `keyframeRequestOutcome`，没有同步 picture recovery episode，导致 `episodeProjectionState=absent`、恢复链难以绑定同一轮。修复：`record_playback_recovered_fact()` 只接受当前 recovery episode 内、`display_age_ms <= 300ms` 且 owner 非 supply-starved/host-present-stalled 的新鲜 host present；receive-local hard PLI/FIR sent 同步创建并发送 picture recovery episode，active refresh 继续不打开 hard episode。新增 stale retained present 与 receive hard request episode 回归测试；验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::receive::ingress_loop --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib -- --nocapture`、`cargo test -p xbxrc trace_projection --lib -- --nocapture`、`cargo check -p xbxengine`、`cargo check -p xbxrc`、`git diff --check`。脚本层还发现 `trace_receive_feedback_report.py` 会用 ambient ledger 归因无显式 `ledgerGeneration` 的事件，补丁被自动审批层拦截，后续需单独处理验收脚本误报。
- 2026-06-29: 继续复盘 `runtime-trace-1782710072770-1.jsonl` 的最终出图停滞：healthy TWCC/输出窗口之后仍出现 `runtimeReconnectConsumed` 3 次、`rx closed cause=rebuildPeerConnection` 10 次，消费点处于 `transport_state=Connecting` 或 host `retainedDisplayedFrameStale`，`hasPendingFrame=false`、`noPendingStreak` 达 1200~1700+。修复：`ExpensiveRecoveryGate` 在“已有成功媒体边沿 + 已发 reconnect grant + Connecting 且无 direct ICE 零响应硬证据”时阻断 liveness/transport-await 重复 reconnect；runtime pending candidate 消费前也阻断 `Connecting` 状态下已有历史媒体边沿的本地 no-progress reconnect candidate，保留 direct ICE no-response 例外。新增 session/runtime 回归测试；同时整理旧 `reconnectSettledKeyframeDeferred` 断言，让 matrix 继续证明 reconnect request 已发出且 candidate reason 被保留。验证：`cargo fmt`、`cargo test -p xbxengine transport::rtc::session::policy::tests::reconnect_lifecycle --lib -- --nocapture`、`cargo test -p xbxengine runtime_blocks_no_progress_reconnect_candidate_while_transport_rebuild_is_in_flight --lib -- --nocapture`、`cargo test -p xbxengine api::runtime::tests::reconnect_recovery_matrix --lib -- --nocapture`、`cargo check -p xbxengine`、`git diff --check`。
- 2026-06-29: 继续自查同类 reconnect 绕过口：`session/recovery` 的自动 `RequestReconnect(MediaStalled)` 会直接调用 runtime `request_reconnect(..., Other)`，绕过 pending candidate block，解释了 trace 中 `RuntimePhaseChanged Reconnecting` 早于 `runtimeReconnectConsumed` 的首次 rebuild。修复：runtime 自动恢复动作消费前新增 direct reconnect gate，`MediaStalled` 在连接仍健康或 transport rebuild 已在飞且无 direct ICE 零响应硬证据时记录 `runtimeReconnectBlocked` 并阻断整路重建；远端 terminal 和显式控制面重连继续允许。验证：`cargo fmt`、`cargo test -p xbxengine runtime_blocks_direct_media_stalled_reconnect_when_transport_remains_healthy --lib -- --nocapture`、`cargo test -p xbxengine api::runtime::tests::reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::policy::tests::reconnect_lifecycle --lib -- --nocapture`、`cargo check -p xbxengine`、`git diff --check`。
- 2026-06-29: 继续审查 stale host present 的间接放行口：`unresolved_transport_await_episode_keeps_serviceable_continuation_bridge()` 之前只看 `recovery_playback_recovered_at_ms` 是否落在 keyframe bridge 时间窗内，旧 episode 的 `PlaybackRecovered` 仍可能在新 recovery episode 中绑定 continuation。修复：bridge 现在要求 playbackRecovered 属于当前 recovery episode，且沿用 host present 新鲜度、owner 非 `supply-starved`、reason 非 `displaySupplyCritical/hostPresentStalled` 的约束；新增跨 episode playbackRecovered 不绑定 continuation 回归。验证：`cargo fmt`、`cargo test -p xbxengine playback_recovered --lib -- --nocapture`、`cargo test -p xbxengine diagnostics::sink::runtime_stats_sink --lib -- --nocapture`、`cargo test -p xbxengine api::runtime::tests::reconnect_recovery_matrix --lib -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::policy::tests::reconnect_lifecycle --lib -- --nocapture`、`cargo check -p xbxengine`、`git diff --check`。
- 2026-06-29: 复盘 fresh trace `runtime-trace-1782715565941-1.jsonl`，receive gate、midsegment latency、ingress queue 均 PASS，lifecycle reconnect gate 仍 FAIL；健康播放窗口后 host 持续 `hostMailboxRetainedDisplayedStale`，`hasPendingFrame=false`，`noPendingStreak` 从几十增长到 1600+，但 `receiverWaitingKeyframe` 仍被 `connectivity-transport` 消费成 PeerConnection rebuild，首轮还可经 direct `MediaStalled` 路径绕过 pending gate。修复：runtime lifecycle gate 新增 `lifecycleGate:displaySupplyNoPendingLocal`，在 Connected、无 remote terminal、无 direct ICE 零响应硬证据、已有 stale displayed frame 且 host mailbox 长期 no-pending 时，同时阻断 pending `receiverWaitingKeyframe/livenessNoProgressTimeout` candidate 与 direct `MediaStalled` reconnect；保留 remote terminal 和真实 ICE failure 重建路径。新增 pending candidate 与 direct reconnect 两条 trace-like 回归。验证：`cargo fmt`、`cargo test -p xbxengine runtime_blocks_receiver_waiting_candidate_when_display_is_no_pending_local --lib -- --nocapture`、`cargo test -p xbxengine runtime_blocks_direct_media_stalled_reconnect_when_display_is_no_pending_local --lib -- --nocapture`、`cargo test -p xbxengine api::runtime::tests::reconnect_recovery_matrix --lib -- --nocapture`、`cargo check -p xbxengine`。
- 2026-06-29: 复盘 fresh trace `runtime-trace-1782716659031-1.jsonl`：pending `receiverWaitingKeyframe` 已归零，但 lifecycle gate 仍 FAIL；首次 rebuild 前 16ms 仍有 `StatsVideoFrameRendered` 和 video track packet 进展，随后 `TransportConnectionStateChanged=Disconnected` 立即触发 `rtcConnectionDisconnected` candidate 消费，进入 Reconnecting 并关闭 rx。修复：runtime candidate gate 将 `rtcConnectionDisconnected` 纳入本地恢复误重建阻断面，若 3s 内存在 packet/decode/host present/host mailbox/last displayed 进展且无 remote terminal、无 direct ICE 零响应硬证据，则记录 `lifecycleGate:transientDisconnectedWithFreshMedia` 并阻断 PeerConnection rebuild；`peer-closed`、remote terminal 与真实 hard disconnect 仍保留重建路径。新增 trace-like `runtime_blocks_transient_disconnected_candidate_with_fresh_media_progress` 回归。验证：`cargo fmt`、`cargo test -p xbxengine runtime_blocks_transient_disconnected_candidate_with_fresh_media_progress --lib -- --nocapture`、`cargo test -p xbxengine api::runtime::tests::reconnect_recovery_matrix --lib -- --nocapture`、`cargo check -p xbxengine`、`git diff --check`。已批准并完成 lifecycle trace gate 脚本更新：`rtcConnectionDisconnected` 误消费和 `transientDisconnectedWithFreshMedia` blockReason 已纳入验收统计；补充验证：`python3 -m py_compile .agents/skills/analyze-runtime-logs/scripts/trace_lifecycle_reconnect_gate.py .agents/skills/analyze-runtime-logs/scripts/trace_webrtc_acceptance_gate.py`、`python3 -m unittest discover -s .agents/skills/analyze-runtime-logs/tests -p 'test_trace_receive_feedback_report.py'`、`git diff --check -- .agents/skills/analyze-runtime-logs/scripts/trace_lifecycle_reconnect_gate.py .agents/skills/analyze-runtime-logs/tests/test_trace_receive_feedback_report.py`。
