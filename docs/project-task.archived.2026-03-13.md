# Project Task Tracker

> 归档副本文件：docs/project-task.archived.2026-03-10.md

## Current Task List

| ID | Task | Status | Started On | Completed On | Notes |
| --- | --- | --- | --- | --- | --- |
| P-471 | 结合 `better-xcloud` 评审当前到 session 建立阶段的增强能力完备性 | Done | 2026-03-13 | 2026-03-13 | 对照 `/Users/guo.xu/Documents/code/games/better-xcloud` 的网络与会话增强能力，完成 `xbxrc` 到 session 建立阶段的代码评审：1) 已确认能力闭环项（`x-ms-device-info`、`settings.locale/osName`、分辨率画像、bitrate/codec、会话调度、ReadyToConnect->connect token、IPv6 候选重排）；2) 识别未闭环项（`console_addrs` 注入仅停留在 negotiation plan，未进入 ICE 归一化执行；`offer_profile` 仅编译未消费；`preferred_region_name/client_ip_override` 有模型但未进入配置映射）；3) 结论为“增强能力未完全完善，需补执行链路而非仅建模”。验证命令：`cargo test -p xbox-streaming`（39/39 通过）。 |
| P-470 | 进行 P2 评估：Internal 字段（`offer_profile`/`audio_channels`）是否进入用户暴露层 | Done | 2026-03-13 | 2026-03-13 | 在 [rfc/streaming-config-blueprint.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/streaming-config-blueprint.md) 新增“7. P2 评估结论”：1) 给出两字段在“持久化/Tauri raw/adapter/compiler/execution”五段链路的完备性矩阵；2) 结论为 `audio_channels` 可进入 L2（后半链完整，仅缺 schema/raw 映射），`offer_profile` 暂不开放（执行层主链未消费，缺闭环）；3) 补充 P2 最小落地清单与前置条件（先落 `audio_channels`，`offer_profile` 先补执行链+校验再评估 L3）。此次仅更新 RFC 与任务追踪，未修改业务代码、未运行测试。 |
| P-469 | 实现 P1：streaming 用户暴露治理（L1/L2/L3 + 预设 + 专家安全） | Done | 2026-03-13 | 2026-03-13 | 1) [src/pages/Setting.vue](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/Setting.vue) 为 `streaming` 分组新增 `默认/画质优先/延迟优先` 预设写入、专家层默认隐藏（二次确认进入）、专家字段一键恢复默认与风险提示；2) [src/i18n/locales/zh.json](/Users/guo.xu/Documents/code/games/xbxrc/src/i18n/locales/zh.json)、[src/i18n/locales/en.json](/Users/guo.xu/Documents/code/games/xbxrc/src/i18n/locales/en.json) 同步补齐新 section key（`default/advanced/expert`）及预设/专家交互词条，满足 i18n 规范；3) 保持预设仅写 Policy 字段，不覆盖 `performance_style` 等 View 字段。验证通过：`pnpm exec vue-tsc --noEmit --pretty false`、`node -e \"JSON.parse(...)\"`、`cargo check -p xbxrc`。 |
| P-468 | 规划 streaming 配置整体蓝图，并聚焦先打通 Config->策略编译层 | Done | 2026-03-13 | 2026-03-13 | 按“需要完整蓝图，不只是已有字段梳理”的要求新增独立 RFC：[rfc/streaming-config-blueprint.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/streaming-config-blueprint.md)。内容包含：1) 全链路对象蓝图（持久化配置、Tauri raw config、crate `Config/Context/Plan/Projection`）；2) 全字段分层（Policy/View/Internal）；3) 第一阶段 `Config -> compiler` 打通清单与 DoD（schema 对齐、映射收口、字段级测试、可观测性）；4) 后续 P1/P2/P3 路线（用户暴露治理、internal 字段策略化、input 域解冻）。并在 [rfc/session-scheduler-convergence-plan.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md) 追加蓝图引用。此次仅更新 RFC 与任务追踪，未修改业务代码、未运行测试。 |
| P-467 | 完成 policy/Config 全量梳理并补齐 RFC 全链路盘点 | Done | 2026-03-13 | 2026-03-13 | 按“不是已有字段梳理，而是 policy/Config 完整梳理”新增全量盘点章节：1) [rfc/session-scheduler-convergence-plan.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md) 新增 `3.12 Policy / Config 全量梳理`，覆盖持久化 schema（`APP_CONFIG_KEYS` 28 项）、Tauri `StreamingConfigSnapshot`、crate `Config/Context/Plan/Projection` 全链路对象；2) 对 `Session/Negotiation/Input/Runtime/Render` 五个 `Config` 子域做逐字段盘点，明确“已映射/固定默认/未暴露”状态；3) 明确当前漂移与缺口：`debug`、`xhome_auto_connect_server_id` 为“UI 可见但不在持久化 schema”，`polling_rate` 为“已持久化并进入 compiler 但 UI 未暴露”；4) 给出 P1/P2/P3 治理优先级，便于后续按风险推进。此次仅更新 RFC 与任务追踪，未修改业务代码、未运行测试。 |
| P-466 | 规划 Config 对用户的暴露方式与程度，并写入 RFC | Done | 2026-03-13 | 2026-03-13 | 基于 Phase A-D 已落地结果，补充“用户可见配置面”的下一阶段规划：1) 在 [rfc/session-scheduler-convergence-plan.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md) 新增 `3.11 Config 对用户的暴露方式与程度规划`，定义 `L1 默认层 / L2 高级层 / L3 专家层` 三层暴露模型；2) 明确字段分层：L1 聚焦高收益低风险，L2 放策略细调，L3 限定高风险 TURN 文本项；3) 补齐交互规范（单位/范围、风险提示、覆盖优先级、即时生效提示）与 E1~E3 实施节奏及 DoD。此次仅更新 RFC 与任务追踪，未修改业务代码、未运行测试。 |
| P-465 | 修正配置分组 i18n：补齐 Phase C/D 新增 section 词条 | Done | 2026-03-13 | 2026-03-13 | 按“新增配置项需符合 i18n 标准”补齐多语言词条：1) [src/i18n/locales/zh.json](/Users/guo.xu/Documents/code/games/xbxrc/src/i18n/locales/zh.json) 新增 `setting.sections.streaming.policySession/policyNegotiation/policyRuntime/policyRender/view` 中文文案，并补 `setting.sections.xcloud.connection`；2) [src/i18n/locales/en.json](/Users/guo.xu/Documents/code/games/xbxrc/src/i18n/locales/en.json) 同步新增对应英文文案；3) 避免设置页继续依赖 `domain-definition` fallback label，保证新分组走统一 i18n 词条。验证：`node -e \"JSON.parse(...)\"` 校验双语 JSON 可解析，`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-464 | 修复 P2：移除 `xcloud` 分组与 `streaming` 分组的 bitrate 重复返回 | Done | 2026-03-13 | 2026-03-13 | 按回归结论修复 `config getGroups` 的低风险重复归属：1) [src-tauri/src/mods/config/grouping.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/config/grouping.rs) 将 `xcloud` 组从返回 `xcloud_bitrate_mode/xcloud_bitrate` 改为返回空对象（`pick_group_values(config, &[])`），保持这两个字段仅在 `streaming` policy 分组出现；2) 增加注释说明 `xcloud` 组当前不承载策略字段，避免后续再次重复归类。验证：`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-463 | 回归 RFC 落地情况（Phase A-D）并输出当前结论 | Done | 2026-03-13 | 2026-03-13 | 按 `rfc/session-scheduler-convergence-plan.md` 对当前代码做只读回归：1) Phase A 已落地，`resolve_streaming_plan()` 通过 `build_domain_context()` 显式注入 runtime/title/turn/remote_play 事实（见 [src-tauri/src/mods/streaming/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs)）；2) Phase B 已落地，`SessionSchedulePlan` 关键字段由 `policy/session/compiler.rs` 显式编译，且 `runtime.mode` 已透传到 session 编译入口（见 [crates/xbox-streaming/src/policy/session/compiler.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/session/compiler.rs)、[crates/xbox-streaming/src/policy/compiler.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/compiler.rs)）；3) Phase C/D 已落地，`streaming` 设置按 `Policy/View` 分层，`performance_style` 仅在 view 侧，且 RFC 已补齐字段级映射表（见 [src/shared/config/domain-definition.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/shared/config/domain-definition.ts)、[src/streaming/session.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/session.ts)、[rfc/session-scheduler-convergence-plan.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md)）；4) 记录一项低风险残留：`config/grouping.rs` 的 `xcloud` 组仍返回 `xcloud_bitrate_*`（与 `streaming` 组存在重复返回），当前 UI 不展示该组字段但建议后续收敛。验证：`cargo test -p xbox-streaming` 通过（39/39）。 |
| P-462 | 进入 Phase D：补齐 policy 字段级映射表并收口 UI 暴露面一致性 | Done | 2026-03-13 | 2026-03-13 | 按 Phase D DoD 完成“可配置字段与真实 compiler 消费链”对齐：1) [rfc/session-scheduler-convergence-plan.md](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md) 新增 `3.10 Phase D 字段级映射表`，逐项给出 `config key -> crate config path -> compiler consumer -> execution consumer`；2) [src/streaming/session.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/session.ts) 将 `STREAM_POLICY_CONFIG_KEYS` 去掉 `polling_rate/vibration`，确保 streaming 页“policy 读取键”严格对齐本轮 `session/negotiation/runtime/render` 范围；3) 保持 `performance_style` 仅在 `STREAM_VIEW_CONFIG_KEYS` 与设置页 `View` section 中出现，确认不进入 compiler/projection。验证：`cargo check -p xbxrc` 通过（仅仓库既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false` 仍被仓库既有/无关问题阻断（缺少 `@spatial-navigation/runtime` 类型声明、`Login.vue` 既有类型错误、若干 TS6133）。 |
| P-461 | 进入 Phase C：建立 streaming `Policy/View` 配置分层并对齐分组输出 | Done | 2026-03-13 | 2026-03-13 | 按 RFC Phase C 落地配置双层模型：1) [src/shared/config/domain-definition.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/shared/config/domain-definition.ts) 将 `streaming` 分组重组为 `Policy · Session/Negotiation/Runtime/Render + View` 五段，`performance_style` 仅保留在 `View`；2) [src-tauri/src/mods/config/grouping.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/config/grouping.rs) 将 policy+view 相关字段统一收口到 `streaming` 组返回，避免设置页分层与后端 group 数据错位；3) [src/streaming/session.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/session.ts) 将读取键拆分为 `STREAM_POLICY_CONFIG_KEYS` 与 `STREAM_VIEW_CONFIG_KEYS`，并通过 `STREAM_CONFIG_KEYS` 合并，明确 view 键仅用于页面态；4) 在 [`rfc/session-scheduler-convergence-plan.md`](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md) 新增 3.9 节，补齐 Phase C 当前落地规则。验证：`cargo fmt -p xbxrc`、`cargo check -p xbxrc` 通过（仅仓库既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false` 失败于仓库既有/无关错误（`src/pages/Login.vue` 与 `src/navigation/core/*` 的 TS6133/TS2345）。 |
| P-460 | 进入 Phase B：显式编译 session schedule 并固定字段规则文档 | Done | 2026-03-13 | 2026-03-13 | 按 RFC Phase B 落地“显式编译，不改默认语义”：1) [crates/xbox-streaming/src/policy/session/compiler.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/session/compiler.rs) 新增 schedule 命名常量并将 `compile_session_schedule` 改为显式编译全部关键字段（`monitor/keepalive/offer/ice/startup/ready/retry/wake/require`），输入维度覆盖 `target + power_on + runtime_mode`；2) [crates/xbox-streaming/src/policy/compiler.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/compiler.rs) 将 `runtime.mode` 传入 `compile_session(...)`，打通 Phase B 编译链；3) 为 session compiler 新增 2 条单测，覆盖“home+power_on 显式 schedule 生成”和“cloud 不唤醒 console”；4) 在 [`rfc/session-scheduler-convergence-plan.md`](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md) 新增 3.8 节，明确每个 schedule 字段当前规则及 `runtime_mode` 维度状态。验证：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`（39/39）、`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-459 | 修正 P1：收紧 remote console 地址提取范围，避免污染 `remote_play.console_addrs` | Done | 2026-03-13 | 2026-03-13 | 按回归结论修正 `extract_console_addrs()` 过宽提取问题：1) [src-tauri/src/mods/data/services/streaming_query_service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/data/services/streaming_query_service.rs) 中地址提取从“全量递归扫描任意 `ip/address + port`”改为“仅白名单路径提取（`serverDetails/streaming/remotePlay/network/configuration/connection/ice` 容器 + `streamingEndpoints/endpointCandidates/...` 候选键）”；2) `try_parse_console_addr()` 增加严格校验：`ip` 必须可解析为 `IpAddr`，`port` 必须落在 `1..=65535`，继续保留去重；3) 新增 3 条单测覆盖“已知路径可提取 / 无关地址不提取 / 重复地址去重”。验证：`cargo test -p xbxrc --lib streaming_query_service`（3/3）通过，`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-458 | 回归审查当前 `Context` 改造链路（runtime/title/turn/remote_play）并确认风险 | Done | 2026-03-13 | 2026-03-13 | 按“native_mkb、touch_surface 暂忽略，回归当前 context 改造”执行只读审查 + 定向验证：1) 审查 `src-tauri/src/mods/streaming/service.rs`、`src-tauri/src/mods/data/services/streaming_query_service.rs`、`src-tauri/src/mods/data/types.rs`、`src-tauri/src/mods/data/services/host_service.rs`、`src-tauri/src/mods/xbxengine/{mod,service}.rs`、`src-tauri/src/shell/mod.rs` 的新增逻辑；2) 发现主要风险为 `extract_console_addrs()` 采用全量递归弱约束提取，可能把非媒体地址（任意 `ip/address + port`）混入 `remote_play.console_addrs`，从而触发 negotiation 的 `inject_console_addrs`，有概率污染 home 侧 ICE 注入候选；3) 发现中风险为 title capability 依赖 `create_home_session_api()`（xHome token）去拉取 input configs，在 cloud 仅有 xCloud token 场景会静默回落最小能力兜底，导致“已接真实数据”在部分登录态下实际不生效；4) 定向验证：`cargo test -p xbxrc --lib streaming` 通过（3/3），`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-457 | 继续推进 1：将 runtime capability 改为基于 xbxengine provider 的真实可用性判定 | Done | 2026-03-13 | 2026-03-13 | 按“继续 1”将 `resolve_runtime_capabilities()` 从硬编码改为宿主状态驱动：1) [src-tauri/src/mods/xbxengine/mod.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/mod.rs) 在 `XbxEngineProvider` trait 新增 `is_runtime_available()` 能力接口；2) [src-tauri/src/mods/xbxengine/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/service.rs) 的 `PlaceholderXbxEngineService` 实现该接口并返回 `false`，明确当前宿主不具备真实 xbxengine runtime；3) [src-tauri/src/mods/streaming/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs) 让 `StreamingService` 持有 `xbxengine_provider`，并在构建 `DomainContext` 时调用 `resolve_runtime_capabilities(self.xbxengine_provider.is_runtime_available())`，使 `runtime.rust_owned` 与真实 provider 状态一致；4) [src-tauri/src/shell/mod.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/shell/mod.rs) 调整装配顺序，先构造 `xbxengine_service`，再注入 `StreamingService::new(...)`。验证：`cargo fmt -p xbxrc`、`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-456 | 继续推进 Phase A：接入 remote console 地址解析并注入 `remote_play.console_addrs` | Done | 2026-03-13 | 2026-03-13 | 在上一轮 title capability 注入基础上，继续把 `console_addrs` 从数据层接到 compiler `Context`：1) [src-tauri/src/mods/data/types.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/data/types.rs) 新增 `DataHostAddr { ip, port }`，并为 `DataHostSummary` 增加 `console_addrs` 可选字段；2) [src-tauri/src/mods/data/services/streaming_query_service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/data/services/streaming_query_service.rs) 在 `get_remote_consoles()` 中对原始 console payload 增加地址提取：新增 `extract_console_addrs/collect_console_addrs/try_parse_console_addr`，递归扫描常见 `ip/ipAddress/address + port/portNumber/streamingPort` 组合并做去重，填入 `DataHostSummary.console_addrs`；3) [src-tauri/src/mods/data/services/host_service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/data/services/host_service.rs) 同步补齐新字段默认值 `console_addrs: None`；4) [src-tauri/src/mods/streaming/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs) 新增 `resolve_remote_play_console_addrs()`，home 目标下按 `server_id/id/device_id` 匹配目标主机并将 `DataHostAddr` 映射为 crate `HostAddr`，最终注入 `DomainContext.remote_play.console_addrs`。验证：`cargo fmt -p xbxrc`、`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-455 | 继续推进 Phase A：将 title capability 改为真实数据注入，收口最小解析逻辑 | Done | 2026-03-13 | 2026-03-13 | 在已落地 `Context` 显式注入的基础上，继续把 title 能力从“纯占位”改为“优先真实数据”：1) [src-tauri/src/mods/streaming/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs) 为 `StreamingService` 持有 `data_provider`，新增 `resolve_title_capabilities()`，在 cloud 目标下调用 `data_provider.get_streaming_title_input_config(titleId)` 拉取标题输入配置；2) 新增 `parse_title_capabilities_from_input_config()` / `collect_string_tokens()` / `merge_title_capabilities()`，从 input config 中提取 `gamepad/mouse/keyboard/mkb/touch/native touch` 相关标记并合并进最小能力基线，避免继续完全依赖硬编码占位；3) `build_domain_context()` 改为异步注入该能力结果，同时保留 `remote_play.console_addrs=[]` 的显式保守值并补充中文注释说明“当前 data provider 尚未提供 ip:port”；4) 修复构造函数里 `data_provider` move 问题（`flow_provider` 与 `StreamingService` 各持一份 `Arc`）。验证：`cargo check -p xbxrc` 通过（仅仓库既有 warning）。 |
| P-454 | 推进 Phase A：补齐 streaming compiler `Context` 注入闭环（runtime/title/turn/remote_play） | Done | 2026-03-13 | 2026-03-13 | 按“继续推进”落地了 RFC Phase A 在主链上的最小实现：1) [src-tauri/src/mods/streaming/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs) 中 `resolve_streaming_plan()` 不再对 `DomainContext` 使用 `..Default::default()`，改为调用 `build_domain_context()` 显式注入运行事实；2) 在 `StreamingServiceInner` 新增 `fallback_turn_provider: Mutex<FallbackTurnProvider>`，通过 `resolve_fallback_turn_server()` 在编译前注入 `turn.fallback`（失败时仅告警并回退 `None`）；3) 新增 `resolve_runtime_capabilities()`，显式固定 `browser_webrtc/rust_owned/native_mkb/touch_surface/prefer_browser`，避免 runtime mode 决策继续隐式依赖默认值；4) 新增 `resolve_min_title_capabilities()`，按 target 注入最小 title 能力占位（cloud 默认 gamepad+touch，并在合法 title id 时补 native touch），为后续 data provider 注入细粒度能力预留边界；5) `remote_play.console_addrs` 当前显式注入空列表并附中文注释，明确这是“能力来源暂缺”的保守占位，而非默认值泄漏。验证：`cargo fmt -p xbxrc`、`cargo check -p xbxrc` 通过（仅仓库既有 warning）。附带变更：`cargo fmt` 对 [src-tauri/src/mods/data/services/streaming_query_service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/data/services/streaming_query_service.rs) 产生 1 处纯格式化改动。 |
| P-453 | 修复 streaming 重构回归：恢复默认超时语义并修正 session store cancel token 回写逻辑 | Done | 2026-03-12 | 2026-03-12 | 按回归审查结论修复了两个已确认的行为漂移点：1) [store.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/store.rs) 的 `upsert()` 改为在同一 session 状态回写时复用原有 `SessionCancelToken`，不再取消共享 token，避免 monitor/keepalive loop 在第一次 upsert 后自停；2) [mod.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/session/mod.rs) 将 `SessionSchedulePlan::default()` 的 `startup_timeout_ms/ready_timeout_ms` 恢复为与旧实现一致的 `45_000`；3) 为 store 补充 `upsert_preserves_cancel_token_for_same_session` 单测，防止同类回归再次发生。验证：`cargo test -p xbox-streaming` 通过（37 tests passed）。 |
| P-452 | 参考原实现审查 streaming 重构后的行为一致性并输出回归结论 | Done | 2026-03-12 | 2026-03-12 | 以重构前 `src-tauri/src/mods/streaming/service.rs`、`api_provider.rs`、`session_api.rs` 为行为基线，对当前 `crates/xbox-streaming/src/session/*`、`src-tauri/src/mods/streaming/service.rs`、`src-tauri/src/mods/data/services/streaming_query_service.rs` 做了只读回归审查：1) 发现 [store.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/store.rs) 的 `upsert()` 会取消同一 session 的旧 cancel token，而 cloned record 复用了同一个 token，导致 monitor/keepalive loop 在第一次状态回写后就会一起被标记取消，这与旧实现“持续监控直到 close/404”的行为不一致；2) 发现 [mod.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/session/mod.rs) 将 `startup_timeout_ms/ready_timeout_ms` 默认值设为 `90_000`，而旧实现监控卡住超时常量为 45 秒，导致 `Provisioning/ReadyToConnect` 卡住判定和启动等待窗口被拉长一倍；3) 其余已核对项包括 `Target::as_str()` 替换硬编码、`StreamingToken` 封装、`wait_until` 抽取、`SessionMonitorMetadata` 解耦、`SessionScheduler` 抽离、keepalive 忽略常量提取，未再发现更高优先级的行为漂移。验证：`cargo test -p xbox-streaming` 通过（36 tests passed）。 |
| P-451 | 规划 streaming 用户配置与编译完备性收敛方案，并写入 RFC | Done | 2026-03-12 | 2026-03-12 | 按“先解决用户配置和编译的完备性，再谈 UI 暴露面；`input config` 暂时忽略”的要求，扩展 [`rfc/session-scheduler-convergence-plan.md`](/Users/guo.xu/Documents/code/games/xbxrc/rfc/session-scheduler-convergence-plan.md)：1) 新增“用户配置与编译完备性收敛计划”章节，明确本轮目标、当前缺口、范围划分与设计原则；2) 将进入本轮的策略配置明确限定为 `session/negotiation/runtime/render` 四组稳定字段，并把 `performance_style` 定义为 view config、`input` 整组明确列为本轮非目标；3) 给出 Phase A~D 四阶段计划：先补 `Context` 运行事实注入闭环，再把 `SessionSchedulePlan` 从默认常量显式编译化，然后建立 `Streaming Policy Config / Streaming View Config` 双层模型，最后再把 UI 暴露面与真实 compiler 完备性对齐；4) 为每阶段补充 DoD、实施顺序和风险控制。此次仅更新 RFC 与任务追踪，未修改业务代码、未运行测试。 |
| P-450 | 审查当前策略层配置的生效范围，核对哪些设置真正进入 policy/compiler 与 projection | Done | 2026-03-12 | 2026-03-12 | 按“配置入口 -> Tauri 映射 -> `xbox-streaming` policy/compiler -> runtime/render/session projection”做了只读审查：1) 确认当前真正进入策略层的持久化设置仅包括 `preferred_game_language`、`force_region_ip`、`resolution`、`ipv6`、三组 bitrate、`codec`、`polling_rate`、`vibration`、`power_on`、`stream_runtime_mode`、自定义 TURN、`xhome_turn_fallback`、`enable_audio_control`、`video_format`、`display_options`；2) 发现 `performance_style` 仍只在前端页面 UI 使用，未进入 `ConfigService.get_streaming_config()` / `apply_streaming_preferences()` / policy compiler，因此不属于策略层生效范围；3) 发现 `resolve_streaming_plan()` 构造 `Context` 时仍使用大面积 `Default::default()`，导致 runtime capability、title input capability、fallback TURN context、console addrs 等运行事实没有注入，进而使策略层里对应分支当前在真实链路上基本处于未生效状态；4) 发现 `SessionSchedulePlan` 虽已在 crate 内建模并通过 projection 下发，但当前没有任何用户配置映射进去，实际只有 `power_on` 会影响 `wake_console/require_console_ready`，其余节奏常量仍是 crate 默认值。此次为审查与追踪更新，未修改业务代码、未运行测试。 |
| P-449 | 清理最近为串流联调临时加入的调试日志，恢复正常输出面 | Done | 2026-03-12 | 2026-03-12 | 按“只删临时诊断，不改行为”的原则回收了最近这轮为定位串流/输入问题增加的日志：1) 前端侧移除 [events.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/services/events.ts)、[useStreamExecution.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/useStreamExecution.ts)、[runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/runtime-host.ts)、[browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts)、[GamepadDriver.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/infra/input/GamepadDriver.ts)、[InputService.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/app/input/InputService.ts)、[InputChannel.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/protocol/channels/InputChannel.ts) 中新增的 `console.info/error/warn` 调试输出，并删除只为日志存在的调试计数字段；2) Rust 侧移除 [service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs)、[flow.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs)、[service_keyboard.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/ohmygamepad/backends/gilrs/src/service_keyboard.rs) 中最近加入的 start-session / monitor / keyboard-state 联调日志；3) 顺手清理了删日志后留下的未使用变量和参数。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/services/events.ts|src/streaming/useStreamExecution.ts|src/streaming/runtime/runtime-host.ts|src/streaming/runtime/browser-runtime.ts|src/player/infra/input/GamepadDriver.ts|src/player/app/input/InputService.ts|src/player/protocol/channels/InputChannel.ts|src-tauri/src/mods/streaming/service.rs|crates/xbox-streaming/src/session/flow.rs|crates/ohmygamepad/backends/gilrs/src/service_keyboard.rs"` 无命中。 |
| P-448 | 将 `xstream-page-ui` 从页面层迁移到 streaming 目录，收紧 stream 页面边界 | Done | 2026-03-12 | 2026-03-12 | 按“页面层不再挂一块 stream 专属协调逻辑”的方向继续收边界：1) 将 [xstream-page-ui.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/xstream-page-ui.ts) 从 `src/pages` 迁移到 `src/streaming`，让它和 `useStreamExecution`、runtime/session 侧对象落在同一 streaming 目录下；2) [XStream.vue](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/XStream.vue) 同步改为从 `../streaming/xstream-page-ui` 引用，不再依赖页面目录内的 stream helper；3) 迁移过程中只调整了类型导入相对路径，不改变 UI 行为。验证：`rg -n "xstream-page-ui" src -g'*.ts' -g'*.vue'` 仅剩 `XStream.vue` 对新路径的引用；`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming/xstream-page-ui.ts|src/pages/XStream.vue"` 无命中。 |
| P-447 | 修复 stream 页 UI 模式切换条件过窄，恢复 chrome 可见时的 spatial navigation | Done | 2026-03-12 | 2026-03-12 | 在“串流输入已经恢复，但 stream 页面 UI 导航仍然无效”的联调现象下，确认问题不在 spatial navigation 本身，而在 stream 页派生的 UI 输入模式条件：1) [src/pages/xstream-page-ui.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/xstream-page-ui.ts) 原先只在“未连接”或“overlay/sheet 打开”时才把 `stream-ui-input-mode` 切回 UI 模式；2) 这会导致 topbar/chrome 已经显示、按钮也可见，但 `ConsoleUIProvider` 的 keyboard/gamepad input sources 仍然关闭，从而看起来像“UI 导航坏了”；3) 现已将 `shouldEnableSpatialInput` 收紧为“只要 stream 页 chrome 处于可交互态（`shouldShowChrome` 为真），就启用 UI 输入模式”，避免出现“chrome 可见但 spatial nav 仍被关掉”的错位。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/pages/xstream-page-ui.ts|src/pages/XStream.vue|src/App.vue"` 无命中。 |
| P-446 | 修复 gamepad Rust->UI 事件 channel 契约错配，恢复运行时 snapshot 推送 | Done | 2026-03-12 | 2026-03-12 | 在继续排查“键盘监听已读到状态、`virtual:keyboard` 已挂到 `stream-session`，但前端始终没有新的 `gamepad.runtimeSnapshot` 日志”时，确认根因不是输入状态没变，而是 Rust/TS 事件 channel 名字根本不一致：1) [src/shared/events/gamepad.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/shared/events/gamepad.ts) 中 renderer 一直监听 `xbxrc:gamepad:*`；2) 但 [src-tauri/src/mods/gamepad/events.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/gamepad/events.rs) 仍在发旧名字 `gamepad.runtimeSnapshot / gamepad.devicesChanged / gamepad.padSnapshot / gamepad.routeChanged`，导致前端此前看到的 `native snapshot` 实际只来自 `rpc.gamepad.getRuntimeSnapshot()` 的 hydration，而不是后续事件推送；3) 现已将 Rust 侧四个 channel 常量统一为 `xbxrc:gamepad:*`，与 shared contract 对齐，并补了注释说明避免再次漂移。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/player/infra/input/GamepadDriver.ts|src/services/events.ts|src/shared/events|src-tauri/src/mods/gamepad/events.rs|src/streaming/runtime/browser-runtime.ts"` 无命中。 |
| P-445 | 为 ohmygamepad 键盘监听线程补充状态变化日志，确认按键是否真正被读取 | Done | 2026-03-12 | 2026-03-12 | 在确认 `virtual:keyboard` 已接入但按键不会触发新 snapshot 后，继续把诊断前移到 `device_query -> keyboard listener` 这一层：1) [crates/ohmygamepad/backends/gilrs/src/service_keyboard.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/ohmygamepad/backends/gilrs/src/service_keyboard.rs) 现在会在 `listener.poll_state()` 状态变化时输出 `ohmygamepad keyboard state changed`，直接打印 `pressed_keys`、左右摇杆、face buttons 和 dpad 状态；2) 若 `poll_state()` panic 或状态分发失败，也会明确记录 `warn`，不再静默停掉；3) 这样下一次联调时可以直接判断是“listener 根本没读到键盘状态”，还是“读到了但后续 runtime snapshot 没推进”。验证：`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-444 | 将 gamepad 输入诊断日志细化到状态变化级别，直接观察按键是否进入 snapshot | Done | 2026-03-12 | 2026-03-12 | 在上一轮“设备已路由到 `virtual:keyboard`”的基础上，继续把观测从“设备/route 是否接上”推进到“按键状态有没有变化”：1) [src/player/infra/input/GamepadDriver.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/infra/input/GamepadDriver.ts) 的 `native snapshot` 去重 key 现在包含 pad 状态摘要，而不再只看 `routeTarget + padDeviceIds`；2) 日志输出也收成 `activeButtons + leftStick/rightStick + triggers`，这样按下 `WASD` 或方向键时，控制台会直接打印状态变化，而不必展开整个 `buttons` 对象；3) 保持运行逻辑不变，仅把“键盘监听器已启动，但 pad state 是否真的变化”的诊断链补全。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；streaming/player 输入相关定向 `vue-tsc` 无新增命中。 |
| P-443 | 为 stream/gamepad/player 输入链补充诊断日志，定位键盘 fallback 卡点 | Done | 2026-03-12 | 2026-03-12 | 基于“桌面键盘监听器已启动，但串流内仍无输入效果”的新现象，继续补充输入链路观测而不改变行为：1) [src/streaming/runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 的 `attachGamepadSession()/detachGamepadSession()` 现在记录 `setRouteTarget()` 返回 snapshot 的 `routeTarget` 与 `padDeviceIds`，明确浏览器 runtime 切 route 后 gamepad 域是否真的挂到了当前 `stream-session`，以及 snapshot 里是否出现了 `virtual:keyboard`；2) [src/player/infra/input/GamepadDriver.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/infra/input/GamepadDriver.ts) 新增 `native snapshot` 日志，按 `routeTarget + padDeviceIds` 去重输出当前 gamepad runtime snapshot，并在前 5 个转发帧上输出 `forward native frame`，明确它是否真的看到 pad 状态并向 player 发出 frame；3) 保持现有行为不变，仅把“route 没切上 / keyboard device 没进 snapshot / driver 看到了但没转发”这三类断点拆开。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；streaming/player 输入相关定向 `vue-tsc` 无新增命中。 |
| P-442 | 回正 streaming 输入边界：撤回 browser runtime 键盘桥，恢复 gamepad 域负责 fallback，并单独截断 stream 页面方向键 UI 串扰 | Done | 2026-03-12 | 2026-03-12 | 根据“不要绕一趟，应该相信 gamepad 域，权限问题也应该由 gamepad 域解决”的要求，收回了上一轮 renderer->gamepad 模拟输入桥，重新按边界治理：1) 删除 [src/shared/rpc/contract.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/shared/rpc/contract.ts) 与 [src-tauri/src/mods/gamepad/{mod.rs,rpc.rs,service.rs}](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/gamepad/service.rs) 中临时加入的 `gamepad.submitSimulatedState` RPC，避免浏览器 runtime 再绕过 `gamepad` 域直接向其注入键盘态；2) [src/streaming/runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 回到只负责切换 `gamepad` 的 `routeTarget`，不再尝试自己桥接键盘 fallback 或切 `enableKeyboardFallback`；3) [src/App.vue](/Users/guo.xu/Documents/code/games/xbxrc/src/App.vue) 新增 stream gameplay 态的键盘 capture guard，在 `stream-ui-input-mode=false` 时对 `Arrow*/Tab/Enter/Space` 做 `preventDefault + stopPropagation`，单独切断 spatial navigation 对方向键/确认键的 UI 消费，避免“方向键还在导航 UI”；4) [crates/ohmygamepad/backends/gilrs/src/service_keyboard.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/ohmygamepad/backends/gilrs/src/service_keyboard.rs) 增加 gamepad 域自己的日志：桌面键盘监听器启动成功时记录 `info`，因权限/初始化失败降级时记录 `warn`，把“权限问题导致 fallback 不可用”的诊断责任收回到 `gamepad` 域；5) 为此在 [crates/ohmygamepad/backends/gilrs/Cargo.toml](/Users/guo.xu/Documents/code/games/xbxrc/crates/ohmygamepad/backends/gilrs/Cargo.toml) 新增 `log` 依赖。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；streaming/UI 相关定向 `vue-tsc` 无新增命中。 |
| P-441 | 统一 stream 键盘 fallback 到 gamepad 域：浏览器键盘桥接 `virtual:keyboard`，并截断方向键 UI 导航串扰 | Done | 2026-03-12 | 2026-03-12 | 针对“方向键仍在做 UI 导航、WASD 无效，说明没有统一走 gamepad 驱动”的实际联调现象，继续做了一轮输入链收口：1) 确认 `ohmygamepad` 默认映射中 `WASD` 是左摇杆、方向键是 D-pad，但当前 Rust 桌面键盘监听依赖系统权限且运行时无法动态补建，不能作为唯一入口；2) 在 [src/shared/rpc/contract.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/shared/rpc/contract.ts) 与 [src-tauri/src/mods/gamepad/{mod.rs,rpc.rs,service.rs}](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/gamepad/service.rs) 新增 `gamepad.submitSimulatedState` RPC，把 renderer 提交的 `LogicalPadStateDto` 写入 `ohmygamepad-host` 的 `submit_simulated_state()`；3) 在 [src/streaming/runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 新增浏览器键盘桥：stream active 且 `stream-ui-input-mode` 为 gameplay 时，按 `ohmygamepad` 默认映射把 `WASD/TGFH/JKUI/1234/ZX/Tab/Enter/789/方向键` 组装成统一 `LogicalPadStateDto`，通过 `gamepad.submitSimulatedState({ deviceId: 'virtual:keyboard', state })` 提交给 gamepad 域，再由现有 `gamepad.runtimeSnapshot -> GamepadDriver -> PlayerClient` 链路消费；4) 同时对这些键使用 capture + `preventDefault()`，避免方向键继续落入 spatial navigation UI 导航。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；streaming / gamepad / player 输入相关定向 `vue-tsc` 无新增命中。 |
| P-440 | 修复 player 输入层的 rumble 接口漂移：`InputService` 与 `RumbleService` 重新对齐 | Done | 2026-03-12 | 2026-03-12 | 针对运行时错误 `this.rumbleService.handlePacket is not a function` 做了输入层收口：1) [src/player/app/input/InputService.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/app/input/InputService.ts) 的 `handleRumble()` 不再调用已不存在的 `handlePacket(event, runtime)`，改为按当前 `RumbleService` 接口把 `MessageEvent<ArrayBuffer>` 解成 `DataView` 后调用 `onMessage()`；同时去掉了未使用的 `currentControlTransport` 成员；2) [src/player/app/input/RumbleService.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/player/app/input/RumbleService.ts) 按现有 `@shared/gamepad/contract` 的 DTO 重写为最小稳定实现：补回 `destroy()` 生命周期方法、修正 `GamepadRumbleTargetDto` union key 访问、为 `GamepadRumbleEffectDto` 补齐 `repeat` 字段、清理无效导入和残留死代码。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/player/app/input/InputService.ts|src/player/app/input/RumbleService.ts|src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts"` 无命中。 |
| P-439 | 修复 browser runtime 未正确消费 gamepad 主链：绑定 stream session route，并移除本地键盘虚拟手柄漂移 | Done | 2026-03-12 | 2026-03-12 | 针对“画面已出但键盘 fallback 无效，而 fallback 本应由 gamepad 域负责”的问题，确认了当前浏览器 runtime 的真实断点：1) [src/streaming/runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 之前并没有调用 `rpc.gamepad.setRouteTarget()`，也没有下发 `setSamplingStrategy(enableKeyboardFallback)`，导致 `ohmygamepad` 主链仍停留在 `shell-ui`，当前 stream session 并未真正接管 gamepad route；2) 同文件还额外维护了一套本地键盘虚拟手柄逻辑，这与 `GamepadDriver` 已经消费 `gamepad.runtimeSnapshot` 的主链职责发生漂移。此次修复将浏览器 runtime 改为在 `launch()` 时显式调用 `rpc.gamepad.setSamplingStrategy({ enableKeyboardFallback: true }) + setRouteTarget({ kind: 'stream-session', sessionId })`，在 `stop()` 时恢复到 `shell-ui` 并关闭该 fallback；同时删除 runtime 内部那套本地键盘虚拟手柄注入逻辑，避免与 gamepad 域重复造输入。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/contract.ts|src/shared/gamepad/contract.ts"` 无命中。 |
| P-438 | 修复“画面已出但仍停在启动播放器”与 `decideRecovery` 参数报错 | Done | 2026-03-12 | 2026-03-12 | 根据联调现象继续修了两处实际问题：1) [src-tauri/src/mods/streaming/types.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/types.rs) 为 `StreamingRuntimeFact` 的字段补上显式 `serde(rename = "...")`，把 Rust 反序列化形状对齐到前端当前发送的 camelCase（`connectionState/connectedElapsedMs/inactivityElapsedMs`），修复浏览器 runtime 调用 `streaming.decideRecovery` 时的 `missing field connection_state` 报错；2) [src/streaming/useStreamExecution.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/useStreamExecution.ts) 调整 `applySessionProgress()` 的文案覆盖逻辑，媒体连通后 session progress 继续更新健康信息，但不再把状态文案刷回“启动播放器”，避免“画面已经出来但状态还卡在启动播放器”；3) [src/streaming/runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 为卡流检查定时器补了 `catch -> emit(error)`，避免 recovery RPC 异常形成未处理 Promise。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`cargo test -p xbox-streaming` 通过（37 tests）；streaming 定向 `vue-tsc` 无新增命中。 |
| P-437 | 为 `SessionReady -> browser runtime` 启动链补充前端诊断日志 | Done | 2026-03-12 | 2026-03-12 | 针对“`SessionReady` 后启动播放器一直没反应”的现象，在前端链路补齐了启动诊断日志：1) [useStreamExecution.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/useStreamExecution.ts) 重新启用并增强 `RuntimeLaunch` 日志，明确记录 `runtimeLaunchSpec` 被消费以及 `startRuntime()` 失败；2) [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/runtime-host.ts) 为 `startRuntime/closeRuntime/phaseChanged/connectionStateChanged` 增加日志，能够确认 handoff 是否发生、runtime port 是否创建、launch Promise 是否返回，以及 transport 有没有真正变化；3) [browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 增加 `launch/bind/offer.create/exchange/ice.local/exchange/connect/reconnect/chat renegotiate/transport.state` 日志，方便判断浏览器 runtime 是没启动、卡在 offer、卡在 ICE，还是 transport 没连上。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；streaming 定向 `vue-tsc` 无新增命中。 |
| P-436 | 修复 session monitor 只跑首个 tick 就停掉的问题：`upsert` 不再误取消同一 session 的后台循环 | Done | 2026-03-12 | 2026-03-12 | 根据新增日志定位到真正根因：`streaming.monitor.tick` 只出现一次，timeout 时 `repeatedStateCount/monitorAttemptCount` 仍为 `1`，说明 monitor loop 在第一次状态回写后就停了。原因在 [crates/xbox-streaming/src/session/store.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/store.rs) 的 `SessionRuntimeStore::upsert()`：它对同一 `session_id` 的普通状态更新也会取消旧 record 的 `cancel token`，而 monitor/keepalive loop 正持有这个 token，于是第一次 `upsert` 后后台循环在下次检查时把自己停掉。修复为：同一 `session_id` 的 `upsert` 复用旧 `cancel token`，不再取消；并新增单测 `upsert_preserves_cancel_token_for_same_session` 锁住语义。验证：`cargo test -p xbox-streaming` 通过（新增后共 37 tests）；`cargo check -p xbxrc` 通过（仅既有 warning）；streaming 定向 `vue-tsc` 无新增命中。 |
| P-435 | 为 `streaming.startSession` 主链补充充分诊断日志，便于定位远端状态卡点 | Done | 2026-03-12 | 2026-03-12 | 针对实际仍出现的 `streamingStartTimeout`，在 Rust session orchestration 主链补齐了可追踪日志：1) [crates/xbox-streaming/src/session/flow.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs) 为 `start_session_execution`、`prepare_remote_console`、`wait_until_console_ready`、`wait_until_session_started_or_failed`、`monitor_session_tick` 增加 `info/warn` 日志，覆盖 plan/schedule 参数、session 创建结果、远端状态变化、重复状态心跳、connect token 发送结果、以及 timeout 瞬间的最后快照（`phase/streamState/playerState/repeatedStateCount/monitorAttemptCount/error`）；2) monitor 心跳做了节流，状态变化立即记录，长时间停留同一状态时每 15 次 tick 打一次，避免正常路径刷爆日志；3) [src-tauri/src/mods/streaming/service.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs) 的 `start_session` 入口补了 begin/plan/ready/failed 日志，方便从 RPC 边界串起一次调用；4) [crates/xbox-streaming/Cargo.toml](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/Cargo.toml) 新增 `log` 依赖。验证：`cargo test -p xbox-streaming` 通过（36 tests）；`cargo check -p xbxrc` 通过（仅既有 warning）；streaming 定向 `vue-tsc` 无新增命中。 |
| P-434 | 修复 `streaming.startSession` 的 45 秒误超时：让 monitor 消费 policy timeout，并放宽默认启动窗口 | Done | 2026-03-12 | 2026-03-12 | 针对实际发起串流时出现的 `streamingStartTimeout: sessionId=..., elapsedMs=45081` 做了 session 层修复：1) [crates/xbox-streaming/src/session/monitor.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/monitor.rs) 的卡住判定不再写死 45 秒，`SessionMonitorInput` 新增 `state_timeout_ms`，由 flow 把 `plan.session.schedule.startup_timeout_ms` 传入，使 `Provisioning/ReadyToConnect` 的失败窗口真正受 policy 驱动；2) [crates/xbox-streaming/src/session/flow.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs) 的 `wait_until_session_started_or_failed()` 增加一个 monitor tick 的宽限，避免 monitor 还没来得及把卡住状态收敛成 `failed`，这里先抛通用 `streamingStartTimeout`；3) [crates/xbox-streaming/src/policy/session/mod.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/session/mod.rs) 把默认 `startup_timeout_ms/ready_timeout_ms` 从 `45_000` 放宽到 `90_000`，更贴近旧实现“允许慢启动”的行为。验证：`cargo test -p xbox-streaming` 通过（36 tests）；`cargo check -p xbxrc` 通过（仅既有 warning）；streaming 定向 `vue-tsc` 无新增命中。 |
| P-433 | 修复 streaming 回归审查中发现的三处损坏：runtime contract import、browser runtime 卡流恢复去抖、performance polling 时序 | Done | 2026-03-12 | 2026-03-12 | 按回归审查结论补了三处明确问题：1) 修复 [runtime-contract.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/runtime-contract.ts) 在目录调整后遗留的错误相对 import，恢复 `StreamStats` 与 `streaming/types` 的正确引用；2) 在 [browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 恢复本地卡流恢复去抖，新增 `nextAllowedStallRecoveryAt` 和本地 backoff，避免一次恢复失败后每个 5s 检查周期都继续触发 `decideRecovery -> requestReconnect`；3) 在 [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/runtime-host.ts) 将 performance polling 的启动时机收回到 transport `connected` 事件，不再在 `launch()` 返回后立刻开始轮询，从而对齐旧实现的 stats 时序。验证：`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src-tauri/src/mods/streaming"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）；`cargo test -p xbox-streaming` 通过（36 tests）。 |
| P-432 | 对当前 streaming 做一次回归审查：核对 session 与浏览器 runtime 是否相对旧实现发生损坏 | Done | 2026-03-12 | 2026-03-12 | 按“接口调用、启动/恢复时序、配置生效性”对当前实现与旧版 `useStreamSession/useStreamRuntime/WebRtcDirectRuntime` 做了对照审查：1) session 主链确认未回退，旧前端里的 wake/preflight/fallback TURN/keepalive 已迁入 `xbox-streaming::session` 与 policy/projection，`startSession/getSessionProgress/decideRecovery` 的调用链仍闭合；2) browser runtime 侧确认主要配置仍能下沉到 player：TURN、codec、视频/音频码率、mono、polling rate、vibration、display options、video format 都仍有消费路径；3) 发现一个明确损坏：`src/streaming/runtime/runtime-contract.ts` 在目录调整后相对 import 未更新，`vue-tsc` 直接报 `Cannot find module '../player'` 与 `Cannot find module './types'`；4) 发现一个行为回归风险：`src/streaming/runtime/browser-runtime.ts` 的卡流恢复已丢失旧实现里的本地去抖窗口，`checkMediaStalled()` 会在恢复失败后按 5s 周期持续重试，而旧版 `WebRtcDirectRuntime` 会通过 `nextAllowedStallRecoveryAt` 抑制同一轮卡流重连风暴；5) 发现一个时序漂移：`src/streaming/runtime/runtime-host.ts` 现在在 `launch()` 完成后立刻开启性能轮询，而旧实现只在 transport `connected` 后才开始，存在前置空轮询/快照抖动风险。验证：`cargo test -p xbox-streaming` 通过（36 tests）；`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src-tauri/src/mods/streaming"` 命中 `src/streaming/runtime/runtime-contract.ts` 的 import 路径错误。 |
| P-431 | 回收 `runtime-host` 的过度拆分：保留私有 helper，删除额外文件层 | Done | 2026-03-12 | 2026-03-12 | 针对“`runtime-host` 有点过度拆分了”的反馈，做了一次回收：1) 将上一轮新增的 [runtime-port-factory.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-port-factory.ts) 和 [runtime-monitor.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-monitor.ts) 合并回 [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-host.ts)，保留 `createRuntimePort()` 和 `bindRuntimeEvents()` 这两个私有 helper 作为文件内职责分段，但不再额外占两个文件层级；2) host 当前仍维持清晰边界：模式选择、事件解释、recovery 调用和 performance polling 都还在，但实现上回到单文件收口，降低跳转成本；3) 删除上述两个过度拆分出来的文件。验证：`rg -n "runtime-port-factory|runtime-monitor|createRuntimePort\\(|bindRuntimeMonitor\\(" src/streaming src/pages` 仅剩 `runtime-host.ts` 内部私有 helper 命中；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出（命令以 `rg` 无命中返回码结束）；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-430 | 继续削薄 `runtime-host`：拆出 mode 选择和上行事件监视 | Done | 2026-03-12 | 2026-03-12 | 基于上一轮“单接口 + 单事件流”的协议，继续把 [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-host.ts) 从新的“大文件”趋势里拆开：1) 新增 [runtime-port-factory.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-port-factory.ts)，把 `mode -> RuntimePort` 的实现分派从 host 中拿走，host 不再直接 import browser/xbxengine 两种实现；2) 新增 [runtime-monitor.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-monitor.ts)，把上行事件解释、transport failure 回退分发、microphone/frame/error 回调收口成单一 monitor；3) [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-host.ts) 因此只剩 runtime 状态、start/stop 编排、performance polling 和对 UI 暴露的命令桥接，模式选择和事件解释都被移出；4) 保持前一轮协议不变，未再扩大共享接口。验证：`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出（命令以 `rg` 无命中返回码结束）；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-429 | 以 browser runtime 为样板收口 UI/runtime 协议：单接口、单事件流、runtime 自己吃 launch spec | Done | 2026-03-12 | 2026-03-12 | 按“runtime 共享契约收缩为最小接口层，browser runtime 直接实现，Rust runtime 通过 RPC 适配”的方向继续执行：1) [runtime-contract.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-contract.ts) 重写为真正的 UI/runtime 交互协议，明确下行只保留 `launch / stop / requestReconnect / applyDisplayState / setAudioVolume / setMicrophoneEnabled / pressHome / snapshotStats`，上行统一收成 `subscribe(listener)` 的单事件流（`phaseChanged / connectionStateChanged / microphoneStateChanged / framePresented / error`）；2) [runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 直接作为样板实现：它现在自己消费 `RuntimeLaunchSpec`，在 runtime 内部完成 player 创建、TURN 绑定、offer/ice 协商、display state 持有、frame tracking 和 microphone/chat renegotiation，不再要求 host 用 `viewport()/audio()/stats()` 这些内部对象去补生命周期空白；3) [runtime/browser-video-display.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-video-display.ts) 也同步简化为 runtime 内部 helper，不再承担共享事件总线角色；4) [xbxengine-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/xbxengine-runtime.ts) 按同一协议用 RPC 做适配；5) [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-host.ts) 改成只面向这组最小命令和单事件流，不再理解 runtime 内部 controller/audio/stats/viewport 对象树。验证：`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出（命令以 `rg` 无命中返回码结束）；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-428 | 收缩 runtime 共享契约：用单接口明确 UI 的上行事件与下行命令 | Done | 2026-03-12 | 2026-03-12 | 按“runtime 共享契约应该收缩，明确 runtime 对 UI 需要暴露哪些上行和下行通道，最小化收敛一个接口层”的要求继续收口：1) [runtime-contract.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-contract.ts) 从原来的 `viewport()/controllerInput()/audio()/stats()` 对象树，收成单一 `RuntimeInstance` 接口，明确下行命令为 `start/stop/requestReconnect/detachViewport/applyDisplayState/setAudioVolume/startMicrophone/stopMicrophone/pressHome/snapshotStats/bindFrameTracking`，上行通道统一为 `events()`；2) [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-host.ts) 同步改成只消费这组最小命令和事件，不再理解实现内部的 controller/audio/stats/view 结构；3) [runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 和 [xbxengine-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/xbxengine-runtime.ts) 都按新的单接口对外适配，浏览器 runtime 继续直接实现，Rust runtime 继续通过 RPC 适配。验证：`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-427 | 将 `streaming/runtime` 进一步收成纯浏览器实现，移出共享契约与泛化命名 | Done | 2026-03-12 | 2026-03-12 | 按“`streaming/runtime` 只负责浏览器 runtime，所以代码应该更简洁直接”的要求继续收口：1) 新增顶层 [runtime-contract.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-contract.ts)，把 `RuntimeInstance`、`RuntimeBootstrap`、`StreamRuntimePhase` 等 host/xbxEngine/browser runtime 共用的最小契约移出 `runtime/` 目录，避免 `runtime/` 再承担共享层职责；2) 新增 [runtime/browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 与 [runtime/browser-video-display.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-video-display.ts)，把原 `webrtc-direct` 命名改成更直接的浏览器 runtime 命名；3) [runtime-host.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime-host.ts)、[xbxengine-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/xbxengine-runtime.ts) 与 [useStreamExecution.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/useStreamExecution.ts) 改为从顶层 runtime contract / browser runtime 消费；4) 删除 `src/streaming/runtime/types.ts` 与整个 `src/streaming/runtime/webrtc-direct` 目录。结果是 `src/streaming/runtime` 现在只剩 [browser-runtime.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-runtime.ts) 和 [browser-video-display.ts](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/runtime/browser-video-display.ts) 两个浏览器实现文件。验证：`find src/streaming/runtime -maxdepth 2 -type f | sort` 仅输出这两个文件；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-426 | 将 xbxEngine runtime 桥接移出 `streaming/runtime`，确保该目录只保留浏览器 runtime | Done | 2026-03-12 | 2026-03-12 | 按“`streaming/runtime` 不应该出现 `rust-owned` 相关代码”的要求继续收口：1) 新增 `src/streaming/xbxengine-runtime.ts`，把原先位于 `src/streaming/runtime/rust-owned/runtime.ts` 中的 xbxEngine 宿主桥接整体提升到 `streaming` 顶层，明确它属于 streaming execution 对 Rust 宿主的桥，而不是浏览器 runtime 子域；2) 新增 `src/streaming/runtime-host.ts`，把 runtime mode 选择也从 `src/streaming/runtime/useStreamRuntimeHost.ts` 提升到顶层，避免 `runtime/` 目录内继续出现 `mode === 'rust-owned'` 这种实现分派；3) `src/streaming/useStreamExecution.ts` 改为使用新的顶层 `useStreamRuntimeHost()`；4) 删除 `src/streaming/runtime/useStreamRuntimeHost.ts` 和 `src/streaming/runtime/rust-owned/runtime.ts`，并清空 `src/streaming/runtime/rust-owned` 目录。结果是 `src/streaming/runtime` 现在只剩 `types.ts`、`webrtc-direct/runtime.ts`、`webrtc-direct/browser-video-display.ts` 三个浏览器 runtime 相关文件。验证：`find src/streaming/runtime -maxdepth 2 -type f | sort` 仅输出上述 3 个文件；`rg -n "rust-owned|xbxEngineRuntime|createXbxEngineRuntime|mode === 'rust-owned'" src/streaming/runtime src/streaming` 只命中顶层 `src/streaming/runtime-host.ts`、`src/streaming/xbxengine-runtime.ts` 与配置类型定义，无 `runtime/` 目录命中；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-425 | 大幅瘦身 `streaming/runtime`：收成 `host + implementations + minimal types` | Done | 2026-03-12 | 2026-03-12 | 按“`streaming/runtime` 不该这么复杂，只是 `RuntimeLaunchSpec <-> RuntimeFact` 的直接交互，不必为了 `rust-owned` 再套抽象层”的方向继续大并层：1) 新增 `src/streaming/runtime/types.ts`，只保留 runtime host 和两套实现都真正需要的最小共享类型（`StreamRuntimePhase`、`StreamRuntimeReconnectReason`、`RuntimeBootstrap`、`RuntimeEventMap`、`RuntimeInstance`），删除旧的 `contracts.ts` / `index.ts` 那套出口；2) `src/streaming/runtime/useStreamRuntimeHost.ts` 不再通过 `createStreamRuntime()` 间接创建实现，而是直接按 `launchSpec.runtime.mode` 分发到 `createWebRtcDirectRuntime()` / `createRustOwnedRuntime()`，彻底拿掉 `runtime` 域内“为了统一而统一”的选择层；3) `webrtc-direct` 收成 `src/streaming/runtime/webrtc-direct/runtime.ts + browser-video-display.ts` 两个文件，删除原 `WebRtcDirectRuntime.ts` / `createWebRtcDirectRuntime.ts` 的拆层；4) `rust-owned` 收成单一的 `src/streaming/runtime/rust-owned/runtime.ts`，把原 `RustOwnedRuntime.ts`、`createRustOwnedRuntime.ts`、`createRustOwnedXbxEngineClient.ts`、`xbxengine-client.ts` 四层直接并掉，让 renderer 到 `xbxEngine` 的桥接逻辑与 runtime 壳在一处完成；5) `src/streaming/useStreamExecution.ts` 改为直接从 `runtime/types.ts` 引入 `StreamRuntimePhase`。结果是 `src/streaming/runtime` 目录现在只剩 5 个文件，结构明确为：`types.ts`、`useStreamRuntimeHost.ts`、`webrtc-direct/runtime.ts`、`webrtc-direct/browser-video-display.ts`、`rust-owned/runtime.ts`。验证：`find src/streaming/runtime -maxdepth 2 -type f | sort` 仅输出上述 5 个文件；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-424 | 给 `streaming/runtime` 瘦身：去掉 driver/factory/capabilities 泛化，改成直接 runtime 构造 | Done | 2026-03-12 | 2026-03-12 | 按“runtime 应该更直接，`rust-owned` 不再绕一遍 UI 层”的方向继续收口：1) `src/streaming/runtime/contracts.ts` 删掉 `SessionRuntimeDriver`、`StreamRuntimeFactory`、`StreamRuntimeCapabilities`、`StreamRuntimeCreateInput`、`StreamRuntimeLaunchSpec` 等只用于层间转手的泛化类型，改为最小的 `StreamRuntimeBootstrap` + `StreamRuntime` 契约；2) `src/streaming/runtime/createStreamRuntime.ts` 从工厂数组改成按 `launchSpec.runtime.mode` 直接分发到 `createWebRtcDirectRuntime()` / `createRustOwnedRuntime()`，不再维护一层 `supports/createRuntime` 工厂对象；3) `src/streaming/runtime/useStreamRuntimeHost.ts` 去掉 `main-process-session-runtime-driver` 依赖，transport recovery 直接调 `rpc.streaming.decideRecovery()`，runtime 启动也改成“创建时拿完整 spec，随后直接 `start()`”，不再先建对象再喂第二遍 session/runtime/render 数据；4) `src/streaming/runtime/webrtc-direct/WebRtcDirectRuntime.ts` 改成直接对接 `rpc.streaming.exchangeOffer/exchangeIce/decideRecovery`，删除 driver 依赖；5) `src/streaming/runtime/rust-owned/RustOwnedRuntime.ts` 与 `createRustOwnedXbxEngineClient.ts` 改成直接消费 `StreamRuntimeBootstrap`，明确它只是 xbxEngine 的薄壳，不再维护额外 renderer session 抽象；6) 删除已无意义的 `src/streaming/runtime/main-process-session-runtime-driver.ts`。验证：`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/shared/rpc/xbxengine.ts|src/shared/rpc/streaming.ts"` 无输出；`rg -n "SessionRuntimeDriver|StreamRuntimeFactory|StreamRuntimeCapabilities|createMainProcessSessionRuntimeDriver|capabilities\\b" src/streaming` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-423 | 清空 `src/streaming/application`：将剩余 session helper 收成真实 `session` 模块 | Done | 2026-03-12 | 2026-03-12 | 继续沿“直接删层并层，不再保留假边界”的方向收口 streaming：1) 新增 `src/streaming/session.ts`，把原先散落在 `src/streaming/application/stream-session-model.ts`、`stream-session-remote.ts`、`stream-session-state.ts`、`stream-session-subscription.ts` 里的 route 派生、错误解析、session RPC、progress phase 映射、健康快照与 progress 轮询统一收成一个真实的 session 模块，`useStreamExecution.ts` 只从这一处消费 session 相关 helper；2) `src/streaming/useStreamExecution.ts` 的 imports 全部改为从 `./session` 引入，execution 入口继续只承担 orchestration / runtime host 协调 / execution view model 输出，不再挂着一层名为 `application` 的错位目录；3) 删除上述四个旧 helper 文件，并移除空的 `src/streaming/application` 目录，确保 `src/streaming` 顶层只剩 `session`、`runtime`、`useStreamExecution` 等真实边界。验证：`find src/streaming/application -maxdepth 1 -type f` 无输出；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-422 | 直接删层并层：以 `useStreamExecution` / `useStreamRuntimeHost` 替换旧的 application/runtime 假边界 | Done | 2026-03-12 | 2026-03-12 | 按“`streaming/application` 和 `runtime` 假边界过多，直接删层并层”的方向执行重组，而不是继续修补旧结构：1) 新增 `src/streaming/useStreamExecution.ts`，直接收口原先 `useStreamSession + useStreamController + stream-controller-effects` 三层职责，在一个入口里完成 session orchestration、runtime host 协调、warning 副作用与 execution view model 输出；2) 新增 `src/streaming/runtime/useStreamRuntimeHost.ts`，把原 `useStreamRuntime.ts` 明确降格为 runtime host，只负责具体 runtime 实现托管、本地观测、会话切换幂等和恢复桥接，不再冒充 application 层；3) `src/pages/XStream.vue` 改为直接消费 `useStreamExecution`，旧的 `src/streaming/application/useStreamController.ts`、`src/streaming/application/useStreamSession.ts`、`src/streaming/application/stream-controller-effects.ts`、`src/streaming/runtime/useStreamRuntime.ts` 全部删除，避免仓库继续同时存在新旧两套入口；4) 页面层现有的 `route + execution + pageUi + actions` 结构保持不变，但 execution 现在直接来自新的合并入口，而不是先穿过 controller/session 两层中转；5) 本轮故意不再新增新的 application helper 文件，目标就是减少假边界数量。验证：`rg -n "useStreamController|useStreamSession|stream-controller-effects|useStreamRuntime\\b" src` 无输出；定向 `pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/pages/XStream.vue|src/pages/xstream-page-ui.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-421 | 执行 UI 收口第二阶段：将页面本地 UI 返回面收成 `pageUi.state + pageUi.actions` | Done | 2026-03-12 | 2026-03-12 | 继续推进 `XStream` 页面层收口，但仍然不新增新的 streaming helper 文件：1) `src/pages/xstream-page-ui.ts` 的返回值从扁平一组 overlay/chrome/sheet 字段与方法，收成显式的 `state + actions` 两段，明确其职责就是页面本地 UI 状态与页面本地 UI 动作；2) `src/pages/XStream.vue` 同步改为消费 `pageUi.state` 和 `pageUi.actions`，不再从 `pageUi` 根对象上散拿 `overlayState/showFailedSheet/shouldShowChrome/openSheet/closeSheet/revealChrome` 等字段；3) 页面内与 sheet/chrome 相关的交互处理函数全部改为基于 `pageUi.actions` 组合调用，`handleStreamMenuAction` 也改成局部 handler map，删除原先整段串行 `if` 分发，减少页面脚本继续堆协议外派生判断；4) 结果是 `XStream.vue` 现在已经更明确地消费 `route + execution + pageUi + actions` 四段，其中 `execution` 来自 streaming controller，`pageUi` 明确属于页面层。验证：`rg -n "pageUi\\.(overlayState|showFailedSheet|showWarningSheet|shouldShowChrome|isMenuSheetOpen|isDisplaySheetOpen|isAudioSheetOpen|isTextSheetOpen|revealChrome|openSheet|closeSheet)" src/pages/XStream.vue src/pages/xstream-page-ui.ts` 无输出；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/pages/XStream.vue|src/pages/xstream-page-ui.ts|src/streaming/application/useStreamController.ts|src/streaming|src/pages"` 仅命中工作区既有 `src/pages/Login.vue` 错误，无本轮相关报错；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-420 | 执行 UI 收口第一阶段：将 streaming 页面消费面收成 `execution + pageUi + actions` | Done | 2026-03-12 | 2026-03-12 | 基于前面已稳定的 phase / handoff / spec/fact 边界，开始执行 UI 收口第一阶段，但不引入新的 streaming helper 文件：1) `src/streaming/application/useStreamController.ts` 将页面消费的运行态从泛化 `state` 收成显式 `execution` 视图模型，并在其中集中 `isLoading/isConnected/statusText/errorText/errorKind/hasError/warningVisible/displayOptions/resolutionMode/performance/audio/mic` 等 execution 视图字段，避免页面继续直接拼 session/runtime 两侧输出；2) `src/pages/XStream.vue` 同步改为消费 `route + execution + pageUi + actions`，删除页面脚本里把 controller 输出当成散装 `state` 的用法，并直接复用 `execution.hasError` 作为 overlay / spatial focus / topbar 判定输入；3) 这一步不改页面本地 UI 状态归属，`xstream-page-ui.ts` 仍保持页面层角色，只是其输入源切到更稳定的 execution view model；4) 结果是 streaming UI 已开始按此前蓝图进入“execution 展示 + page-local UI + user actions”三段组织，但还没有宣称后端已直接提供最终统一 execution state。验证：`rg -n "controller\\.state" src/pages/XStream.vue src/streaming/application/useStreamController.ts` 无输出；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/pages/XStream.vue|src/streaming/application/useStreamController.ts|src/streaming/application|src/streaming|src/pages/xstream-page-ui.ts"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-419 | 按当前代码基线重写 streaming session/runtime RFC 边界文档 | Done | 2026-03-12 | 2026-03-12 | 将 `rfc/session-scheduler-convergence-plan.md` 从“执行前原则清单”更新为“当前稳定基线 + 后续执行清单”，但仍保持只包含原则准则和执行清单两部分：1) 在原则部分新增“当前代码基线”，把已经落地的关键边界固定成文档事实，包括 `sessionReady` 语义拆分、`RuntimeLaunchSpec -> RuntimeFact` 贯通、`webrtc-direct` protocol session 骨架显式化、`rust-owned` renderer/shared 边界对齐、`xbxengine-protocol/core` 已开始承接 execution spec、以及 handoff 已由稳定 session state 派生；2) 在执行清单部分改为区分“已收敛基线”和“后续收敛”，明确当前哪些边界已经稳定，后面真正还要继续推进的是 `xbox-streaming::session` / `xbxengine/core::session` 协作契约、`rust-owned` 内部 protocol scheduling 对齐、execution spec 剩余字段消费、以及宿主桥 wire 形状单独收敛；3) 文档表述全面改成和当前代码一致的术语，避免 RFC 再次落回“还没做”的语气。验证：纯文档更新，未运行测试。 |
| P-418 | 执行 streaming 收敛第七步：让 `xbxengine` data-plane 真正承接 execution spec | Done | 2026-03-12 | 2026-03-12 | 继续把 `rust-owned` 从“边界语言对齐”推进到“data-plane 实际消费 execution spec”：1) `crates/xbxengine/protocol/src/control.rs` 为 `StartRuntime` 扩展 `runtime/render` 两段 DTO（`XbxEngineRuntimeProjectionDto`、`XbxEngineRenderProjectionDto` 及 codec 子对象），正式把启动控制对象定义成完整 execution spec，而不是只有 `session + viewport + audio_volume + mode`；2) `crates/xbxengine/core/src/api/runtime.rs` 的 `start/apply_control` 同步承接这两段 spec，并新增 `apply_execution_spec()`：启动前把可明确映射的 runtime 参数写入本地 negotiation config（当前先消费 `max_video_bitrate_kbps`、`max_audio_bitrate_kbps`、codec profile -> `offer_profile`），把 render 的 `display_options` 直接下发到 media backend 并写入 snapshot；3) 同时修正 `start()` 失败回滚边界，启动失败时恢复 `self.config`，避免 execution spec 或 mode 只回滚 state/snapshot 却遗留配置污染；4) 新增 `start_runtime_control_consumes_execution_spec` 单测，锁定 `StartRuntime` 控制命令会真正把 execution spec 吃进 `xbxengine`；5) 当前这一步仍然只收敛 protocol/core 边界，不宣称 wire JSON 形状已经和宿主桥全面统一。验证：`cargo test -p xbxengine` 通过（29 tests passed）；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-417 | 执行 streaming 收敛第六步：稳定 `sessionReady -> runtime` handoff 触发语义 | Done | 2026-03-12 | 2026-03-12 | 继续把 session/runtime 结合从“一次性 UI 消费槽”收敛成稳定执行条件：1) `src/streaming/application/useStreamSession.ts` 将 `runtimeLaunchSpec` 从可变 `ref` 改成基于 `sessionExecution + sessionHealth.phase === sessionReady` 的派生 `computed`，删除 `applySessionProgress` 中对 launch payload 的写入逻辑以及断开/重试时对该 payload 的手动清空，避免 session 再维护一个会被 UI 消费并置空的临时 handoff 槽；2) `src/streaming/application/useStreamController.ts` 改为监听 `runtimeLaunchSpec.sessionId` 这一稳定 key，并在回调里读取当前 spec 启动 runtime，删除 `consumeRuntimeLaunchSpec()` 这种一次性消费桥接；3) `src/streaming/runtime/useStreamRuntime.ts` 补上 runtime 宿主级幂等与切换保护：同一 `sessionId` 重复启动直接返回，不同 `sessionId` 启动前先关闭旧 runtime，避免 UI 再额外承担 launch 去重职责；4) 结果是 `sessionReady -> runtime` 的 handoff 时机现在由稳定 session state 派生并由 runtime host 自己兜底幂等，UI 不再通过“读到一次 spec 然后手动清空”来维持启动语义。验证：`rg -n "consumeRuntimeLaunchSpec|runtimeLaunchSpec\\.value = null" src/streaming/application src/streaming/runtime` 无输出；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src-tauri/src/mods/streaming|src/streaming/runtime|src/streaming/application"` 无输出；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-416 | 执行 streaming 收敛第五步：统一 crate / Tauri control-plane 的 `RuntimeFact` 语言 | Done | 2026-03-12 | 2026-03-12 | 继续把 runtime -> session 的协作语言从前端一路收平到 control-plane 核心：1) `crates/xbox-streaming/src/session/recovery.rs` 将旧的 `RuntimeLocalEvent` 重命名为 `RuntimeFact`，并同步把 `decide_runtime_recovery` 的入参语义改成“runtime 事实”而不是“本地事件”，保证 crate session 的恢复入口和前端/RPC 已收敛的 `RuntimeFact` 语言一致；2) `src-tauri/src/mods/streaming/service.rs` 同步改为把 `StreamingRuntimeFact` 映射到 crate `RuntimeFact`，移除 adapter 层对旧术语的依赖；3) 对应 recovery 单测一并更新，确保只是术语与边界统一，不改恢复判定行为；4) 结果是现在从 renderer runtime、shared RPC、Tauri adapter 到 `xbox-streaming::session` recovery，runtime -> session 的协作对象都统一叫 `RuntimeFact`，control-plane 不再保留“一半是 fact、一半是 local event”的双轨语义。验证：`rg -n "RuntimeLocalEvent" crates/xbox-streaming src-tauri/src/mods/streaming` 无输出；`cargo test -p xbox-streaming` 通过（36 tests passed）；`cargo check -p xbxrc` 通过（仅既有 warning）。 |
| P-415 | 执行 streaming 收敛第四步：对齐 `rust-owned` 的 protocol session 边界语言 | Done | 2026-03-12 | 2026-03-12 | 在不提前改 `xbxengine` 内核实现的前提下，先把 `rust-owned` 在 renderer/shared 边界上的语义收紧到和 `webrtc-direct` 同一层抽象：1) `src/streaming/runtime/rust-owned/xbxengine-client.ts` 将外部 client 契约改为 `startProtocolSession / requestProtocolReconnect / stopProtocolSession`，明确它承载的是 sidecar protocol session 桥，而不是散装 runtime 控制方法；2) `src/streaming/runtime/rust-owned/RustOwnedRuntime.ts` 同步引入 `startProtocolSession/stopProtocolSession` 私有入口，让 `StreamRuntime.start/stop/requestReconnect` 在内部也映射到统一 protocol session 语义，和 `webrtc-direct` 一侧的骨架处于同一抽象层次；3) `src/shared/rpc/xbxengine.ts` 将 `XbxEngineStartRuntimeParams` 从旧的扁平字段改为 `session + viewport + mode + runtime + render + audioVolume` 结构，直接对齐 `xbxengine-protocol` 已存在的控制 DTO 形状，避免 `rust-owned` 边界继续使用过时的平铺参数；4) `src/streaming/runtime/rust-owned/createRustOwnedXbxEngineClient.ts` 按新 DTO 组装启动参数，并把 `mode` 从原先硬编码的 `localHost` 收敛为基于 `targetType` 的显式映射（`home -> localHost`，`cloud -> cloudGaming`），避免 sidecar protocol mode 继续由前端拍脑袋写死；5) 结果是 `rust-owned` 虽然还没有和 `webrtc-direct` 统一内部实现，但在 renderer/shared 边界上已经对齐到同一套 protocol session 语言。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src-tauri/src/mods/streaming|src/streaming/runtime/rust-owned"` 无输出，说明本轮未引入新的 streaming / rust-owned 侧 TS 报错；全量 `vue-tsc` 仍存在既有非 streaming 阻断错误。 |
| P-414 | 执行 streaming 收敛第三步：显式化 `webrtc-direct` protocol session 骨架 | Done | 2026-03-12 | 2026-03-12 | 继续按执行清单收敛 `webrtc-direct` 的内部协议编排，但不提前动 `rust-owned` 实现：1) `src/streaming/runtime/webrtc-direct/WebRtcDirectRuntime.ts` 将 runtime 内部协商流程显式整理为 `startProtocolSession / stopProtocolSession / bindProtocolSession / connectMediaProtocol / renegotiateChatProtocol / requestReconnect` 一组稳定入口，并新增 `ProtocolSessionBinding / NegotiationAttempt / NegotiatedOffer` 辅助类型，避免主链路仍靠零散方法名串起来；2) 麦克风开关后的 chat 重协商统一走 `renegotiateChatProtocol`，重连路径统一走 `connectMediaProtocol({ restart: true })`，清除旧的 `renegotiateChatChannel/connectMedia` 遗留入口；3) `createChannelOffer` 的“attempt 已失活”分支改为显式 `null` 返回，调用侧显式短路，且通过 `NegotiatedOffer` 类型把“offer.sdp 一定存在”的保证表达在 helper 边界上，避免再次回退成 `offer.sdp ?? ''` 这类隐式兜底；4) 结果是 `webrtc-direct` 现在已经按执行清单把协议调度骨架明确成 bind/offer/ice/connect/chat renegotiation/reconnect/stop 七段，后续 `rust-owned` 对齐时可以直接对照这层语义，而不是对照浏览器实现细节。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src-tauri/src/mods/streaming"` 无输出，说明本轮未引入新的 streaming 侧 TS 报错；全量 `vue-tsc` 仍存在既有非 streaming 阻断错误。 |
| P-413 | 执行 streaming 收敛第二步：显式化 runtime launch spec 与 runtime fact 语言 | Done | 2026-03-12 | 2026-03-12 | 继续按执行清单收敛 session/runtime 交互语言且尽量不改行为：1) renderer 侧将 session -> runtime 的 handoff 从 `StreamRuntimeStartRequest` 重命名并收敛为 `RuntimeLaunchSpec`，`useStreamSession/useStreamController/useStreamRuntime` 同步改为 `runtimeLaunchSpec` 语义，避免继续使用“临时 start request”命名；2) runtime -> session 的恢复上报从 `StreamRuntimeLocalEvent/reportLocalEvent` 重命名为 `StreamRuntimeFact/reportRuntimeFact`，`src/streaming/runtime/contracts.ts`、`main-process-session-runtime-driver.ts`、`useStreamRuntime.ts`、`webrtc-direct/WebRtcDirectRuntime.ts` 与 shared RPC/Tauri DTO 同步改为 `fact` 字段，明确这是运行事实回传而非 UI 本地事件；3) `StreamRuntimeStartContext` 收敛为 `StreamRuntimeLaunchSpec`，`RustOwnedRuntime`、`createRustOwnedXbxEngineClient`、`RustOwnedXbxEngineClient` 契约同步改名；4) `src/shared/rpc/xbxengine.ts` 与 `createRustOwnedXbxEngineClient.ts` 扩展 `xbxEngine.startRuntime` 外部入口，开始透传完整 `runtime/render` projection，使 `rust-owned` 至少在入口语言上不再偏离统一 spec，即便后端当前尚未完全消费这些字段；5) 结果是 session 与 runtime 之间的交互方向已经更明确地固定为“launch spec 下发 + runtime fact 回传”。验证：`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false 2>&1 | rg "src/streaming|src/shared/rpc/streaming.ts|src/shared/rpc/xbxengine.ts|src-tauri/src/mods/streaming"` 无输出，说明本轮未引入新的 streaming 侧 TS 报错；全量 `vue-tsc` 仍被工作区既有非 streaming 错误阻断。 |
| P-412 | 执行 streaming 收敛第一步：拆分顶层 session ready 与本地媒体 connected 语义 | Done | 2026-03-12 | 2026-03-12 | 按执行清单先落地顶层 phase 语言收敛：1) `crates/xbox-streaming/src/session/flow.rs` 将顶层 progress 的 `Connected` 重命名为 `SessionReady`，并同步调整 `wait_until_session_started_or_failed`、keepalive 触发条件、`resolve_session_phase/default_status_text_key`，明确 `player_state=started` 只表示“远端 session 已就绪，可启动本地 runtime”，不再表示“媒体已连通”；2) `src-tauri/src/mods/streaming/types.rs` 与 `src/shared/rpc/streaming.ts` 同步将对外 DTO phase 改为 `sessionReady`，并更新 fallback progress 映射；3) `src/streaming/application/useStreamSession.ts` 改为在 `progress.phase === 'sessionReady'` 时下发 `runtimeStartRequest`，真正的 connected 仍只由 runtime 事件驱动；4) `src/streaming/application/stream-session-state.ts` 将 `sessionReady` 继续映射到 UI 的 waiting 态，避免页面误把远端就绪显示为媒体已连通；5) 新增 `xbox-streaming` 单测锁定 `started -> SessionReady` 与 progress 文案语义。验证：`cargo test -p xbox-streaming` 通过（36 tests passed）；`cargo check -p xbxrc` 通过（仅既有 warning）；`pnpm exec vue-tsc --noEmit --pretty false` 仍被工作区既有非 streaming 错误阻断：`src/components/layout/AppShellLayout.vue:8`、`src/pages/Login.vue:6/154`、`src/player/app/input/*`。 |
| P-411 | 将 streaming RFC 收缩为“原则准则 + 执行清单” | Done | 2026-03-12 | 2026-03-12 | 按“重构文档只需要原则准则和执行清单”的要求，重写 `rfc/session-scheduler-convergence-plan.md`：1) 删除现状审查和迁移叙述，只保留重构原则；2) 原则部分固定五类约束：分层原则、责任边界原则、交互原则、统一语言原则、当前阶段原则；3) 执行清单部分按顶层 session、runtime/session 契约、`webrtc-direct`、`rust-owned`、UI、Tauri adapter、实施顺序、非目标八段组织；4) 明确当前主线仍是“先用 `webrtc-direct` 收敛语言和边界，再回头对齐 `rust-owned`”。 |
| P-410 | 以当前代码为基线重写 streaming session/runtime RFC 审查文档 | Done | 2026-03-12 | 2026-03-12 | 重写 `rfc/session-scheduler-convergence-plan.md`，将文档从“过时迁移计划”改为“基于当前代码的现状审查 + 当前阶段收敛清单”：1) 按现有抽象层次重述五层结构（renderer 页面/应用层、renderer runtime host、tauri streaming adapter、`xbox-streaming` control-plane crate、`xbxengine/core` runtime 内核）；2) 明确哪些边界已经符合蓝图，不再把已完成的收口误写成待办；3) 将真正未收敛的问题收敛为 phase 语义混用、runtime 与 session handoff/fact 语言不够显式、`webrtc-direct` 与 `rust-owned` 统一了壳但未统一规格、两类 session 应是“分层协同”而非“结构合并”；4) 重新给出当前阶段收敛清单、非目标和实施顺序，固定“先用 `webrtc-direct` 收敛语言，再回头对齐 `rust-owned`”的表述。 |
| P-409 | 将 streaming session/runtime 当前阶段收敛清单固化到 RFC | Done | 2026-03-12 | 2026-03-12 | 更新 `rfc/session-scheduler-convergence-plan.md`，把本轮达成一致的约束写成文档基线：1) 明确区分两类 session：`xbox-streaming::session` 负责 Xbox 顶层会话编排（control-plane），`xbxengine/core::session` 负责 runtime 内部协议/媒体调度（data-plane）；2) 固定当前阶段策略为“以 `webrtc-direct` 打通主链路，不要求 `rust-owned` 同步实现，但所有新增契约必须可复用”；3) 新增“当前阶段收敛清单”章节，明确 phase 语义必须拆开 `SessionReady/MediaConnected`、runtime 与 session 采用 `RuntimeLaunchSpec -> RuntimeFact` 结合、runtime 内部协议调度骨架统一为 `bind/exchangeOffer/exchangeIce/connect/chat renegotiation/requestReconnect/stop`；4) 新增“当前阶段建议顺序”章节，固定先收敛 phase，再收 runtime/session handoff，再收 `webrtc-direct` protocol session，最后回头对齐 `rust-owned`。 |
| P-408 | 纠正 streaming UI 精简方向，回收过度拆分的 application 辅助文件 | Done | 2026-03-12 | 2026-03-12 | 根据“`streaming/` 目录文件越来越多、代码膨胀”的反馈收缩方案：1) 删除 `src/streaming/application/stream-controller-view.ts` 与 `src/streaming/application/useStreamControllerLifecycle.ts`，把展示派生与生命周期桥接收回 `src/streaming/application/useStreamController.ts`，避免把稳定边界内的逻辑再拆成碎片 helper；2) 删除 `src/streaming/application/useStreamPageUi.ts`，将页面临时 UI 状态迁到 `src/pages/xstream-page-ui.ts`，明确其属于页面层而非 streaming 域；3) 保留 `useStreamSession/useStreamController/useStreamRuntime` 作为主要边界，减少 `streaming/application` 文件数并避免继续扩散；4) 当前文件体量为 `useStreamController.ts` 266 行、`XStream.vue` 789 行，较前一轮停止了 streaming 域文件继续增长。验证：`pnpm exec vue-tsc --noEmit --pretty false` 仍仅剩既有非 streaming 错误：`src/pages/Login.vue:158` 与 `src/player/app/input/*`。 |
| P-407 | 收敛 streaming controller 为 coordinator，抽离展示派生与桥接副作用 | Done | 2026-03-12 | 2026-03-12 | 完成第二步 controller 精简：1) 新增 `src/streaming/application/stream-controller-view.ts`，集中 `eyebrow`、能力开关与展示派生状态，避免 `useStreamController` 内继续堆积 computed；2) 新增 `src/streaming/application/useStreamControllerLifecycle.ts`，收口 runtime->session 桥接回调、`runtimeStartRequest` 观察、连接/错误 watch 与挂载卸载生命周期；3) `src/streaming/application/useStreamController.ts` 改为只负责组装 `session/runtime/effects/view` 与页面命令，返回面收敛为 `route/ability/state/actions` 四段；4) `src/pages/XStream.vue` 同步改为消费分段 controller，并在页面内仅把模板需要的 ref 摊平，避免对象内 ref 在模板中的解包噪音；5) 保留旧行为：断开时重置 performance 面板、Esc 退出、runtimeStartRequest 单次消费、warning/error effect 语义不变。验证：`pnpm exec vue-tsc --noEmit --pretty false` 仍仅剩既有非 streaming 错误：`src/pages/Login.vue:158` 与 `src/player/app/input/*`。 |
| P-406 | 抽离 streaming 页面级 UI 状态，收敛 XStream 的 sheet/chrome/overlay 编排 | Done | 2026-03-12 | 2026-03-12 | 完成第一步页面层精简：1) 新增 `src/streaming/application/useStreamPageUi.ts`，集中管理 `activeSheet/overlayState/showFailedSheet/showWarningSheet/shouldShowChrome` 与 chrome 自动隐藏、spatial input 同步、用户交互唤醒逻辑；2) `src/pages/XStream.vue` 改为消费 `useStreamPageUi`，删除页面内零散的 `actionSheetOpen/displaySheetOpen/audioSheetOpen/textSheetOpen/chromeVisible/chromeTimer` 与对应 watch/onMounted/onBeforeUnmount 逻辑；3) sheet 开关统一收敛到单一 `activeSheet` 模型，overlay 展示统一收敛到 `overlayState` 判定，页面脚本复杂度下降且不再自己维护多组布尔状态；4) 保持 `useStreamController/useStreamSession/useStreamRuntime` 契约不变，降低本轮回归面。验证：`pnpm exec vue-tsc --noEmit --pretty false` 通过 streaming 本次改动文件检查，剩余错误均为既有非 streaming 问题：`src/pages/Login.vue:158` 与 `src/player/app/input/*`。 |
| P-405 | 规划 UI 侧 streaming 精简方案（收敛 controller/page 状态与浮层编排） | Done | 2026-03-12 | 2026-03-12 | 基于现状代码审查形成精简方案：1) 识别主复杂度集中在 `src/pages/XStream.vue`（874 行）与 `src/streaming/application/useStreamController.ts`（262 行），前者承担 chrome/sheet/overlay/spatial input 四类页面状态，后者同时承担 orchestration、page view model 与 runtime 副作用；2) 明确收敛目标为“三层”：`useStreamSession` 只管远端 session，`useStreamRuntime` 只管 player/runtime，新增 `useStreamPageUi` 只管本页临时 UI（chrome、sheet、focus、warning 展示）；3) 建议把 controller 缩成“stream coordinator”，仅保留 session<->runtime 桥接、页面级命令组合（retry/disconnect/powerOff）与一个稳定的 `viewModel` 输出；4) 建议把 `XStream.vue` 拆为 `StreamTopBar/StreamOverlayHost/StreamSheetHost` 三个展示组件，页面只组装，不再直接维护多组 open/close 与 overlay 判定；5) 建议用单一 `overlayState`/`activeSheet` 替代当前 `isLoading/error/warning + 4 个 sheet boolean` 的分散派生；6) 实施顺序按“先抽 page UI，再压 controller，再拆页面组件”推进，期间保持 session/runtime 契约不动，降低回归面。 |
| P-404 | 完成 UI 侧 streaming session 规整化重构（只保留状态/动作/订阅与 Player 交接） | Done | 2026-03-11 | 2026-03-11 | 按“仅改 UI streaming，不动其它层”完成 session 收敛：1) `src/streaming/application/useStreamSession.ts` 重构为 `route/ability/state/flow/actions/runtimeBridge` 六段接口，`flow` 只承载 session 生命周期（start/retry/disconnect），`actions` 仅保留界面按钮动作；统一 UI state（`sessionUiPhase/sessionHealth/sessionReportingEnabled`），启动链路固定为“加载配置 -> startSession -> 启用 progress 订阅 -> 按 progress 驱动 UI”；2) 新增 `src/streaming/application/stream-session-state.ts`，集中 phase->UI phase 映射与健康快照裁剪；3) 新增 `src/streaming/application/stream-session-subscription.ts`，封装 progress 轮询订阅（含防重入与会话切换防旧数据回写）；4) `src/streaming/application/stream-session-remote.ts` 新增 `getRemoteSessionProgress`，session 远端命令统一经 remote adapter；5) `src/streaming/application/useStreamController.ts` 改为仅消费 `useStreamSession` 公开面并处理 runtime 连接/UI 事件，去除对 session 内部字段和调度细节的直接依赖；6) 终态收敛：session `failed/closed` 自动停上报，connected 后只下发一次 `runtimeStartRequest` 交接给 Player。验证：`pnpm exec vue-tsc --noEmit`（失败，阻断为既有非 streaming 问题：`src/pages/Login.vue:158` 与 `src/player/app/input/*` 类型错误）；`pnpm exec vue-tsc --noEmit | rg "src/streaming|src/shared/rpc/streaming.ts"` 无输出，说明本次 streaming 侧未新增 TS 报错。 |
| P-403 | 规划 streaming session UI 状态/动作规整与 Player 交接切割方案 | Done | 2026-03-11 | 2026-03-11 | 按“UI 只做状态与动作、session 到达 player 边界后交接”的目标输出可执行计划：1) 在 UI 建立统一 `SessionViewState` + `SessionAction` 模型，收敛 `useStreamSession/useStreamController/useStreamRuntime` 交叉状态；2) 定义启动时序为 `subscribeSessionEvents -> startSession -> enableLocalReporting`，并明确每步输入/输出与失败语义；3) 引入 `SessionHandoffPayload` 作为 session->player 唯一交接对象（携带 sessionId/runtime/render/targetType），以 phase=`connected` 作为交接闸门；4) 切断旧链路中的散装字段与直接 session 控制入口，只保留 `start/stop/retry/report`；5) 将 `getSessionProgress` 升级为可持续订阅语义（短轮询或事件推送），UI 只消费规范化 phase；6) 补充迁移阶段 DoD、回归清单与回滚策略，确保行为可验证。 |
| P-402 | 落地 M4（阶段三）：下线 `createSession/getSession` 旧兼容命令，收敛到启动主链路 | Done | 2026-03-11 | 2026-03-11 | 完成会话入口收敛：1) `src/shared/rpc/streaming.ts` 删除 `StreamingCreateSessionParams/StreamingGetSessionParams`；2) `src/shared/rpc/contract.ts` 删除 `streaming.createSession/getSession` 声明与相关类型导入；3) `src-tauri/src/mods/streaming/types.rs` 删除 `StreamingCreateSessionParams/StreamingGetSessionParams` DTO；4) `src-tauri/src/mods/streaming/{mod.rs,rpc.rs}` 删除 `create_session/get_session` trait 能力、命令枚举与分发分支；5) `src-tauri/src/mods/streaming/service.rs` 删除 `create_session/get_session` 实现，并将 `resolve_streaming_plan` 改为直接接收 `target_type/target_id`，由 `start_session` 单一路径驱动；6) 保留 runtime 必需命令面：`startSession/getSessionProgress/closeSession/exchangeOffer/exchangeIce/decideRecovery/listActiveSessions`。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。补充：`pnpm exec vue-tsc --noEmit` 仍被工作区既有错误阻断（`src/pages/Login.vue:158` 与 `src/player/app/input/*` 一组类型错误），与本次兼容面收敛无直接关系。 |
| P-401 | 落地 M4（阶段二）：下线外部 `streaming.sendKeepAlive` 命令，仅保留 session 内部保活 | Done | 2026-03-11 | 2026-03-11 | 完成 keepalive 命令面收口：1) `src/shared/rpc/streaming.ts` 删除 `StreamingKeepAliveParams/StreamingKeepAliveResult`；2) `src/shared/rpc/contract.ts` 删除 `streaming.sendKeepAlive` 方法声明与相关类型导入；3) `src-tauri/src/mods/streaming/types.rs` 删除 `StreamingKeepAliveParams/StreamingKeepAliveResult` DTO；4) `src-tauri/src/mods/streaming/{mod.rs,rpc.rs,service.rs}` 删除 `send_keepalive` trait/命令枚举/分发分支与实现，避免 UI 再次直接驱动保活；5) crate `session::flow::keepalive_session_loop` 继续作为唯一保活执行入口（策略层 schedule 驱动）。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。补充：`pnpm exec vue-tsc --noEmit` 仍被工作区既有错误阻断（`src/pages/Login.vue:158` 及 `src/player/app/input/*` 一组类型错误），与本次 keepalive 命令面收口无直接关系。 |
| P-400 | 落地 M4（阶段一）：收敛 runtime 调用面到 `streaming.*`，移除 `streamHost.*` UI 路径 | Done | 2026-03-11 | 2026-03-11 | 完成“UI 仅调用统一 `streaming` 命令面”的收敛：1) `src/streaming/runtime/main-process-session-runtime-driver.ts` 将 `exchangeOffer/exchangeIce` 从 `rpc.streamHost.*` 切换到 `rpc.streaming.*`；2) `src/streaming/runtime/contracts.ts` 删除 `SessionRuntimeDriver.keepAliveRemoteSession/closeRemoteSession`，`src/streaming/runtime/webrtc-direct/WebRtcDirectRuntime.ts` 删除 reconnect 前的前端 keepalive 调用，保持 keepalive 完全由 crate session 调度；3) `src/shared/rpc/contract.ts` 删除 `streamHost` namespace，移除遗留 `src/shared/rpc/stream-host.ts`；4) `src-tauri/src/shell/rpc.rs` 删除 `streamHost` 路由分发，`src-tauri/src/mods/streaming/rpc.rs` 删除 `handle_stream_host_rpc` 与相关 DTO，`src-tauri/src/mods/streaming/{mod.rs,service.rs}` 清理仅用于 `streamHost` 兼容的 facade 方法。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。补充：`pnpm exec vue-tsc --noEmit` 当前被工作区既有错误阻断（含 `src/pages/Login.vue:158` 与 `src/player/app/input/*` 一组类型错误），与本次调用面收敛改动无直接关系。 |
| P-399 | 落地 M3：将 keepalive 调度下沉 crate session，移除 UI keepalive 定时器 | Done | 2026-03-11 | 2026-03-11 | 完成 keepalive 下沉并收紧 UI 边界：1) `crates/xbox-streaming/src/session/flow.rs` 在 `create_session` 中按 `SessionSchedulePlan` 启动 `monitor_session_loop` 与 `keepalive_session_loop`，保活间隔改由 `schedule.keepalive_interval_ms` 驱动；2) `keepalive_session_loop` 仅在 `connected/recovering` 阶段发送 keepalive，session 缺失或取消时自动退出，失败按 best-effort 忽略后重试；3) `exchange_offer/exchange_ice` 轮询 sleep 改用 `schedule.offer_poll_interval_ms/ice_poll_interval_ms`，统一由策略层指导 session 内部调度；4) UI 移除 keepalive 决策链路：`src/streaming/application/stream-controller-effects.ts` 删除 keepalive interval 逻辑，`useStreamController.ts` 取消 keepalive 注入，`useStreamSession.ts` 删除 `keepRemoteSessionAlive`，`stream-session-remote.ts` 删除 `sendRemoteStreamKeepAlive` 封装。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。补充：`pnpm exec vue-tsc --noEmit` 仍被既有错误阻断（`src/pages/Login.vue:158` provider 类型不匹配），与本次 keepalive 下沉无直接关系。 |
| P-398 | 落地 M2：将 wake/preflight/start-wait 编排迁入 crate session，并切换 UI 到 `startSession` | Done | 2026-03-11 | 2026-03-11 | 完成 M2 主链路迁移（保持兼容，不触及 `streamHost.*` 下线）：1) `crates/xbox-streaming/src/policy/config.rs` 新增 `SessionConfig.power_on`，`policy/compiler/session.rs` 改为由 policy 产出 wake/preflight 意图（`SessionSchedulePlan.wake_console/require_console_ready`）；2) `crates/xbox-streaming/src/session/flow.rs` 扩展 `SessionFlowProvider`（`power_on_console/get_remote_consoles`）与 `RemoteConsoleSnapshot`，新增 `start_session_execution()`，在 crate 内统一编排 `prepare_remote_console -> create_session -> wait_until_session_started_or_failed`；3) `src-tauri/src/mods/streaming/service.rs` 的 `start_session` 改为直接调用 crate `start_session_execution`，并通过 `DataProviderRef` 实现 wake/preflight 依赖注入；4) `src-tauri/src/shell/mod.rs` 注入 `data_service` 到 `StreamingService::new`；5) UI `src/streaming/application/useStreamSession.ts` 删除 wake/preflight/poll 定时器逻辑，改为单次调用 `rpc.streaming.startSession` 并按返回 progress 下发 `runtimeStartRequest`；6) `src/streaming/application/stream-session-remote.ts` 删除 `create/get session + waitForRemoteConsoleReady + powerOnRemoteConsole` 的旧编排入口，新增 `startRemoteStreamSession`；7) `src-tauri/src/mods/config/service.rs` 与 `src-tauri/src/mods/streaming/{types,service}.rs` 同步接入 `power_on` 到 policy 输入映射。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。补充：`pnpm exec vue-tsc --noEmit` 仍被既有错误阻断（`src/pages/Login.vue:158` provider 类型不匹配），与本次 streaming 迁移无直接关系。 |
| P-397 | 落地 M1：补齐 session 调度/进度契约与 `startSession/getSessionProgress` RPC（不改行为） | Done | 2026-03-11 | 2026-03-11 | 执行 M1“契约先行”实现：1) `crates/xbox-streaming/src/policy/plan.rs` 新增 `SessionSchedulePlan` 并并入 `SessionPlan.schedule`，`policy/compiler/session.rs` 编译会话时同步产出 schedule（当前仅承载默认调度与 home 唤醒/预检意图，不接入执行）；2) `crates/xbox-streaming/src/policy/projection.rs` 新增 `SessionSchedulePlanProjection` 与 `project_session_schedule_plan(plan)`；3) `crates/xbox-streaming/src/session/flow.rs` 新增 `SessionPhase/SessionProgressSnapshot` 与 `get_session_progress(session_id)`，由现有 runtime snapshot 推导稳定进度对象；4) `src-tauri/src/mods/streaming/{types,mod,service,rpc}.rs` 新增 `StreamingSessionProgressSnapshot`、`StreamingStartSessionParams/Result`、`StreamingGetSessionProgressParams`，并接入 `streaming.startSession/getSessionProgress` 命令，内部复用既有 create/get 流程；5) `src/shared/rpc/{streaming,contract}.ts` 同步扩展新 DTO 与 RPC schema。验证通过：`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`、`cargo fmt -p xbox-streaming -p xbxrc`。补充：`pnpm exec vue-tsc --noEmit` 当前仍被既有错误阻断（`src/pages/Login.vue:158` provider 类型不匹配），与本次 streaming 改动无直接关系。 |
| P-396 | 输出 session 调度下沉的接口草案与里程碑 RFC | Done | 2026-03-11 | 2026-03-11 | 按“UI 仅调用/下发/上报、session 调度入 crate、决策由策略层驱动”产出可执行 RFC：1) 新建 `rfc/session-scheduler-convergence-plan.md`，定义目标架构、边界约束与稳定态调用链；2) 给出 `SessionSchedulePlan`（policy 调度投影）与 `SessionProgressSnapshot`（session 进度投影）的 Rust 草案；3) 给出 `streaming.startSession/getSessionProgress` 等 RPC DTO 草案并明确 `streamHost.*` 分阶段下线策略；4) 明确 UI、tauri、crate 各层文件级改造清单；5) 给出 M1~M4 迁移里程碑、DoD、风险控制与回滚策略，供后续直接按阶段实施。 |
| P-395 | 制定“UI 仅调用/下发/上报，session 调度入 crate”改造计划 | Done | 2026-03-11 | 2026-03-11 | 基于当前实现差距给出分阶段迁移计划：1) 先冻结边界并收敛契约，明确 UI 不再承载 wake/preflight/poll/keepalive/recovery 决策；2) 在 `xbox-streaming::policy` 扩展 `SessionSchedulePlan`（轮询、保活、重试退避、超时/终态规则）并由 `session` 内部消费；3) 在 `xbox-streaming::session` 新增 orchestration scheduler，统一编排 create→monitor→signaling→keepalive→recovery→close；4) `src-tauri/mods/streaming` 仅保留 adapter 与 DTO 映射，逐步收敛 `streamHost.*` 到单一 `streaming.*` 调用面；5) UI 侧 `useStreamSession/useStreamController` 收缩为命令触发与状态展示，仅保留调用、下发、上报三类交互；6) 通过双栈灰度、兼容桥接和回归用例保障迁移期间行为一致。 |
| P-394 | 收敛 UI 启动信号泄漏：将 session 启动散装字段封装为单一 runtimeStartRequest | Done | 2026-03-11 | 2026-03-11 | 继续执行“session 内部可流转信息不泄漏到 UI”的边界收敛：1) `src/streaming/types.ts` 新增 `StreamRuntimeStartRequest`；2) `src/streaming/application/useStreamSession.ts` 以 `runtimeStartRequest` 取代 `playerStartRequested + sessionId + runtimeProjection` 的散装输出，`pollSession` 在 started 时一次性产出 `{ sessionId, targetType, runtime, render }` 启动请求，并提供 `consumeRuntimeStartRequest` 消费接口；3) `src/streaming/application/useStreamController.ts` 改为仅 watch `runtimeStartRequest` 并直接 `runtime.startRuntime(request)`，不再拼装/读取 session 内部流转字段；4) 会话内部 `sessionId` 继续仅用于 session 本地轮询/keepalive/close，不再对 controller 暴露。验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-393 | 收紧 UI 泄漏边界：回收 session `closing` 内部状态，不向 controller/runtime 暴露 | Done | 2026-03-11 | 2026-03-11 | 按“能在 session 内部流转就不泄漏到 UI”的要求做边界收紧：1) `src/streaming/application/useStreamSession.ts` 移除 `isClosing` 对外导出，`closing` 仅在 session composable 内部用于断连/错误保护；2) `src/streaming/application/useStreamController.ts` 不再读取 `session.isClosing`，failed/closed 分支统一调用 `session.handlePlayerDisconnected/handlePlayerError` 由 session 自身决定是否忽略；3) `src/streaming/runtime/useStreamRuntime.ts` 移除 `UseStreamRuntimeOptions.isClosing` 依赖，transport 恢复事件上报不再由 UI 注入 session 内部状态；4) 结果是 UI 层不再感知 session 的 `closing` 生命周期细节，仅消费公开状态与动作。验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-392 | 先行完成 streaming 职责拆解并映射三层架构（作为后续改造前置基线） | Done | 2026-03-11 | 2026-03-11 | 按“先拆职责、再谈实现”的要求完成架构基线文档：1) 新增 `rfc/streaming-responsibility-decomposition-baseline.md`，先将 streaming 拆成 12 个职责原子（策略输入、编译、投影、会话编排、信令、监控、恢复判定、恢复下发、运行事实采样、执行动作、页面副作用、文案收敛）；2) 明确三层归属矩阵（`policy` 负责 1-3、`session` 负责 4-8、`runtime` 负责 9-10，UI 只负责 11-12）；3) 基于现有代码标注“已正确落点”和“仍混层点”（如 `useStreamSession` 持有 wake/preflight/poll，UI effect 持有 keepalive 节奏，`streamHost.*` 与 `streaming.*` 双调用面并存）；4) 给出不含功能扩展的 Stage A-D 顺序与 DoD，作为下一步所有改造的唯一约束入口。 |
| P-391 | UI streaming 结构化降复杂：拆分 controller 副作用并收敛 session 轮询分支 | Done | 2026-03-11 | 2026-03-11 | 在 P-390 基础上继续做“可读性优先”的结构简化：1) 新增 `src/streaming/application/stream-controller-effects.ts`，把 warning/keepalive 计时器与连接副作用从 `useStreamController` 中抽离为 `createStreamControllerEffects`，controller 只调用 `onConnected/onDisconnected/onRuntimeReconnecting/onErrorChanged`；2) `src/streaming/application/useStreamController.ts` 删除定时器实现细节与重复逻辑，主流程仅保留 session+runtime 编排和 UI action；3) `src/streaming/application/stream-session-model.ts` 新增 `resolveSessionPollProgress`（`queued/pending/failed/started/noop`），把 `playerState` 分支判定抽到 model 层；4) `src/streaming/application/useStreamSession.ts` 的 `pollSession` 改为消费 `resolveSessionPollProgress` 结果，减少分支噪音。验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。当前范围说明：本次为结构重排与职责下沉，不改 RPC/会话协议行为。 |
| P-390 | UI 侧 streaming 控制层清理与精简（去冗余、减接口） | Done | 2026-03-11 | 2026-03-11 | 在不改功能语义前提下对 UI host/application 做轻量精简：1) `src/streaming/application/useStreamController.ts` 将 `handleRuntimeConnectionState` 改为同步处理，删除连接态重复副作用（connected 分支不再重复启动 keepalive/无帧告警），并新增 `resetConnectionUiState()` 统一清理 `keepAlive/warning` 逻辑，减少重复分支；2) `src/streaming/runtime/useStreamRuntime.ts` 删除外部未消费的 `requestReconnect` 暴露能力，保持恢复触发仅在 runtime host 内部闭合；3) `src/streaming/application/useStreamSession.ts` 删除未被消费的导出 `runtimeMode/runtimeTurnServer` 及对应类型依赖，收窄 session composable 对外面向；4) 结合前序收敛，`useStreamController` 继续只承担页面状态编排，不再持有恢复策略触发。验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。当前范围说明：本次以删冗余和减接口为主，未改动 session/runtime 远端协议行为。 |
| P-389 | 将 transport `failed/closed` 恢复触发从 controller 收敛到 runtime host | Done | 2026-03-11 | 2026-03-11 | 继续以“职责收敛而非重写”为原则推进统一：1) `src/streaming/runtime/useStreamRuntime.ts` 增加 `isClosing` 上下文回调与 `tryRecoverByTransportState`，在 runtime host 内统一通过 `sessionDriver.reportLocalEvent({ type: 'transportConnectionState' })` 获取恢复判定并执行 `runtime.requestReconnect`；2) `transport.connectionState` 事件处理改为：`failed/closed` 先尝试恢复，仅在未恢复时再上抛给 application，避免 controller 直接持有恢复策略触发；3) `src/streaming/application/useStreamController.ts` 移除对 `runtime-recovery` 的直接依赖，保留页面状态/错误收敛职责；4) 删除 `src/streaming/application/runtime-recovery.ts` 旧入口，恢复触发链路统一为 runtime host -> session/crate。验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。当前范围说明：controller 仍监听连接态用于 UI 状态与错误展示，但不再负责恢复判定触发。 |
| P-388 | 将 media stall 阈值策略从 web runtime 下沉到 crate session 恢复判定 | Done | 2026-03-11 | 2026-03-11 | 按“不过度改造、优先提取强逻辑”的目标继续收敛：1) `crates/xbox-streaming/src/session/recovery.rs` 扩展 `RuntimeLocalEvent::MediaHealth { connection_state, connected_elapsed_ms, inactivity_elapsed_ms }`，新增 `map_media_health_recovery_reason`，把首帧宽限（10s）与无帧阈值（15s）策略收口在 crate；2) `src/shared/rpc/streaming.ts` 与 `src-tauri/src/mods/streaming/types.rs` 的 `StreamingRuntimeLocalEvent` 增加 `mediaHealth` 事件形态，`src-tauri/src/mods/streaming/service.rs` 仅做 DTO 到 crate event 映射并调用 `decide_runtime_recovery`；3) `src/streaming/runtime/webrtc-direct/WebRtcDirectRuntime.ts` 移除本地 `FIRST_FRAME_GRACE_MS/MEDIA_STALL_THRESHOLD_MS` 判定逻辑，改为定时上报 `mediaHealth`（`connectedElapsedMs/inactivityElapsedMs`），由 session 判定后再触发 `requestReconnect`；4) runtime 仍保留本地观测职责（帧活动采样、定时检测），但不再解释恢复阈值策略，便于后续 rust/web runtime 复用同一套恢复规则。验证通过：`rustfmt --edition 2021 crates/xbox-streaming/src/session/recovery.rs src-tauri/src/mods/streaming/types.rs src-tauri/src/mods/streaming/service.rs`、`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。当前范围说明：transport `failed/closed` 的触发入口仍在 controller，尚未合并到 runtime host 的统一本地事件上报流程。 |
| P-387 | 收敛 runtime local event 恢复判定并接入 `media-stalled` 上报链路 | Done | 2026-03-11 | 2026-03-11 | 继续推进 recovery 归属下沉：1) `crates/xbox-streaming/src/session/recovery.rs` 新增 `RuntimeLocalEvent` 与 `decide_runtime_recovery(event, is_closing)`，统一承接 `transportConnectionState` 与 `mediaStalled` 两类 runtime 事实，同时保留 `decide_transport_recovery` 兼容入口；2) `src/shared/rpc/streaming.ts` 与 `src-tauri/src/mods/streaming/types.rs` 将 `StreamingDecideRecoveryParams` 改为携带 `event` 联合类型（`transportConnectionState`/`mediaStalled`），`src-tauri/src/mods/streaming/service.rs` 改为仅做 DTO -> crate event 映射；3) `src/streaming/runtime/contracts.ts` 扩展 `SessionRuntimeDriver.reportLocalEvent`，`src/streaming/runtime/main-process-session-runtime-driver.ts` 统一走 `rpc.streaming.decideRecovery`；4) `src/streaming/runtime/webrtc-direct/WebRtcDirectRuntime.ts` 的卡流检测由“直接 `requestReconnect('media-stalled')`”改为“先上报 local event，再按 session 判定结果触发重连”；5) `src/streaming/application/runtime-recovery.ts` 同步改用 transport local event 形态调用恢复判定。验证通过：`rustfmt --edition 2021 crates/xbox-streaming/src/session/recovery.rs src-tauri/src/mods/streaming/types.rs src-tauri/src/mods/streaming/service.rs`、`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。当前范围说明：`failed/closed` 事件仍由 controller 触发恢复请求，尚未完全收口到 runtime host 内部。 |
| P-386 | 将 transport 恢复判定下沉到 crate session 并通过 streaming RPC 暴露 | Done | 2026-03-11 | 2026-03-11 | 在已引入 `SessionRuntimeDriver` 的基础上继续下沉恢复策略：1) 新增 `crates/xbox-streaming/src/session/recovery.rs`，定义 `RuntimeReconnectReason` 与 `decide_transport_recovery(connection_state, is_closing)`，把 `failed/closed` 到 reconnect reason 的判定收口在 crate `session`；2) `src-tauri/src/mods/streaming/{types,mod,rpc,service}.rs` 新增 `decideRecovery` 命令与参数/返回 DTO（`StreamingDecideRecoveryParams/Result`），由 `StreamingService::decide_recovery` 直接调用 crate 决策函数；3) `src/shared/rpc/{streaming,contract}.ts` 增加 `streaming.decideRecovery` 契约；4) `src/streaming/application/runtime-recovery.ts` 改为通过 `rpc.streaming.decideRecovery` 获取是否重连及 reason，再执行 `runtime.requestReconnect`，前端不再持有本地状态映射规则。验证通过：`rustfmt --edition 2021 crates/xbox-streaming/src/session/recovery.rs crates/xbox-streaming/src/session/mod.rs src-tauri/src/mods/streaming/types.rs src-tauri/src/mods/streaming/mod.rs src-tauri/src/mods/streaming/rpc.rs src-tauri/src/mods/streaming/service.rs`、`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。当前范围说明：`media-stalled` 仍由 runtime 本地检测后直接发起重连，尚未接入 session 上报链路。 |
| P-385 | 执行第 2 步：引入 SessionRuntimeDriver 并收敛恢复协调入口 | Done | 2026-03-11 | 2026-03-11 | 按“`performance_style` 保持外部设置，先推进 session/runtime 组合边界”的决策完成第 2 步实现：1) 在 `src/streaming/runtime/contracts.ts` 新增统一 `SessionRuntimeDriver` 契约，并把 `StreamRuntimeCreateInput/StartContext` 改为显式注入 `sessionDriver`，不再允许 runtime 内部直接耦合散装 RPC；2) 新增 `src/streaming/runtime/main-process-session-runtime-driver.ts` 作为 main-process driver 实现，统一封装 `exchangeOffer/exchangeIce/keepAliveRemoteSession/closeRemoteSession`；3) `src/streaming/runtime/useStreamRuntime.ts` 改为集中创建并注入 driver，`webrtc-direct` 工厂与 `WebRtcDirectRuntime` 不再依赖旧 `main-remote-session-bridge`，后者已删除；4) 新增 `src/streaming/application/runtime-recovery.ts`，将 `failed/closed -> reconnect reason` 映射与触发入口收口为 `tryRecoverRuntimeByTransportState`，`useStreamController.ts` 仅负责连接态事件编排与错误收敛，不再散落重复恢复分支；5) 维持现有行为语义：runtime 本地重连优先，失败后回落页面错误。验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbxrc`。 |
| P-384 | 增加 render plan 并收敛 render 配置消费链路 | Done | 2026-03-11 | 2026-03-11 | 按“render 也属于全流程 plan”的方向继续收敛：1) 在 `crates/xbox-streaming/src/policy/{config,plan}.rs` 新增 `RenderConfig/RenderPlan` 与 display options 结构，并在 `policy/compiler/render.rs` 增加 render 编译逻辑，把 `enable_audio_control`、`video_format`、初始 `display_options` 纳入 crate plan；2) 在 `crates/xbox-streaming/src/policy/projection.rs` 新增 `RenderPlanProjection`，并将 `SessionExecutionSnapshot` 扩展为同时携带 `runtime + render`；3) `src-tauri/src/mods/streaming/{types,service}.rs` 与 `src-tauri/src/mods/config/service.rs` 同步扩展 `StreamingRenderProjection`、`StreamingDisplayOptionsValue` 和 raw config 提取逻辑，把 render 原始偏好映射到 crate `render` 配置，并通过 `createSession/getSession` 返回给前端；4) `src/shared/rpc/streaming.ts`、`src/streaming/types.ts`、`src/streaming/application/{useStreamSession,useStreamController}.ts`、`src/streaming/runtime/{contracts,useStreamRuntime}.ts`、`src/streaming/runtime/webrtc-direct/{createWebRtcDirectRuntime,browser-video-display,WebRtcDirectRuntime}.ts` 改为直接消费 `render projection`，去掉 runtime 对 `StreamConfigSnapshot.video_format/enable_audio_control/display_options` 的启动期依赖；5) 保留 display options 的运行期覆写能力，viewport `applyDisplayState` 改为“当前 display options + 固定 render plan”组合。验证通过：`rustfmt --edition 2021 crates/xbox-streaming/src/policy/config.rs crates/xbox-streaming/src/policy/plan.rs crates/xbox-streaming/src/policy/compiler/mod.rs crates/xbox-streaming/src/policy/compiler/render.rs crates/xbox-streaming/src/policy/projection.rs crates/xbox-streaming/src/session/flow.rs src-tauri/src/mods/config/service.rs src-tauri/src/mods/streaming/types.rs src-tauri/src/mods/streaming/service.rs`、`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。剩余边界：`performance_style` 仍是页面 UI 呈现设置，尚未纳入 crate plan。 |
| P-383 | 补齐 streaming raw config 到 crate policy 输入的映射闭环 | Done | 2026-03-11 | 2026-03-11 | 继续推进“policy 在 crate 内闭合”的输入面治理：1) 扩展 `src-tauri/src/mods/streaming/types.rs` 中的 `StreamingConfigSnapshot`，补齐 `xhome/xcloud/audio bitrate`、`codec`、`polling_rate`、`vibration`、`stream_runtime_mode`、`server_*`、`xhome_turn_fallback` 等原始偏好字段；2) `src-tauri/src/mods/config/service.rs` 的 `get_streaming_config()` 同步从规范化配置中提取这些字段，保证 Tauri facade 能拿到完整 raw config；3) 在 `src-tauri/src/mods/streaming/service.rs` 新增 `apply_streaming_preferences` 及解析辅助函数，把 raw config 明确映射到 `xbox_streaming::Config` 的 `negotiation/input/runtime` 子配置，包括 codec 选择、Mb/s -> Kbps 码率转换、polling/vibration、runtime mode、自定义 TURN 与 xHome fallback TURN；4) 为该映射补充单测，锁定“raw config 进 crate 偏好”的行为，避免后续回退成只传 locale/region/ipv6/resolution 四个字段。验证通过：`rustfmt --edition 2021 src-tauri/src/mods/streaming/service.rs src-tauri/src/mods/config/service.rs src-tauri/src/mods/streaming/types.rs`、`pnpm exec vue-tsc --noEmit`、`cargo check -p xbxrc`、`cargo test -p xbxrc --lib`。剩余缺口：渲染展示类设置（如 `video_format`、`enable_audio_control`、`display_options`）仍然保留在页面/runtime 宿主侧，尚未定义是否需要进入 crate policy。 |
| P-382 | 收敛 runtime transport/input 配置消费到 projection | Done | 2026-03-11 | 2026-03-11 | 基于第一阶段的 `SessionExecutionSnapshot` 继续推进 runtime 消费面收缩：1) 在 `crates/xbox-streaming/src/policy/projection.rs` 扩展 `RuntimePlanProjection`，把 codec、视频/音频 bitrate、mono audio、polling rate、vibration 一并从 `Plan` 投影出来；2) `src-tauri/src/mods/streaming/types.rs` 与 `src/shared/rpc/streaming.ts` 同步扩展 `StreamingRuntimeProjection`，让 execution DTO 能跨 crate/Tauri/frontend 传递完整 runtime 执行参数；3) `src/streaming/runtime/{contracts,createStreamRuntime,useStreamRuntime}.ts` 改为以 `runtime projection + renderConfig` 启动 runtime，不再把 `mode/config` 散装传入工厂；4) `src/streaming/runtime/webrtc-direct/createWebRtcDirectRuntime.ts` 改为直接消费 projection 中的 transport/input 参数，仅保留 `enable_audio_control`、`video_format` 这类渲染展示配置继续由页面 config 提供；5) `src/streaming/application/{useStreamSession,useStreamController}.ts` 改为把 session execution 中的 runtime projection 直接下发给 runtime。验证通过：`rustfmt crates/xbox-streaming/src/policy/projection.rs src-tauri/src/mods/streaming/types.rs`、`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。剩余缺口：`src-tauri/src/mods/streaming/service.rs::resolve_streaming_plan` 目前仍只把少量 raw config 映射进 crate config，bitrate/codec/polling/vibration 等字段尚未完全在 crate 内闭合。 |
| P-381 | 落地 streaming runtime projection 与 session execution DTO 第一阶段 | Done | 2026-03-11 | 2026-03-11 | 按“先补 crate/Tauri 契约，再收缩 UI 消费面”的顺序完成第一阶段实现：1) 在 `crates/xbox-streaming/src/policy/projection.rs` 新增 `RuntimePlanProjection` 与 `project_runtime_plan(plan)`，把 runtime mode/owner/resolved turn 从 `Plan` 投影为执行层可消费规格；2) 在 `crates/xbox-streaming/src/session/flow.rs` 新增 `SessionExecutionSnapshot<S, R>` 与 `create_session_execution/get_session_execution`，让 session 输出“远端会话快照 + runtime projection”；3) `src-tauri/src/mods/streaming/{types,mod,service}.rs` 新增 `StreamingRuntimeProjection`、`StreamingSessionExecutionSnapshot` 并将 `createSession/getSession` RPC 返回值切到 execution DTO；4) `src/shared/rpc/{streaming,contract}.ts` 与 `src/streaming/application/{useStreamSession,useStreamController}.ts` 同步改为消费 execution DTO，删除 UI 侧 `resolveStreamRuntimeMode.ts`，runtime 启动 mode/turnServer 改从 session 返回值读取；5) 验证通过：`pnpm exec vue-tsc --noEmit`、`cargo check -p xbox-streaming`、`cargo check -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。补充说明：`cargo fmt --all` 仍被工作区既有文件中的 trailing whitespace 阻断，本次未修改那些无关文件，仅对本次改动文件执行了定向 `rustfmt`。 |
| P-380 | 基于当前代码制定 runtime 与 policy/session 收敛实施计划 | Done | 2026-03-11 | 2026-03-11 | 将“runtime 应为执行层、policy 应在 crate 内闭合”的宏观建议落实为实际迁移计划，并补充到 `rfc/runtime-session-composition-stabilization.md`：1) 明确 Phase A~E 的落地顺序；2) 将改动点绑定到当前代码文件（如 `crates/xbox-streaming/src/policy/*`、`crates/xbox-streaming/src/session/flow.rs`、`src-tauri/src/mods/streaming/{service,types,rpc}.rs`、`src/streaming/application/{useStreamSession,useStreamController}.ts`、`src/streaming/runtime/{contracts,useStreamRuntime}.ts`）；3) 明确优先级应先补 crate projection/execution dto，再收缩 UI/runtime 消费面，最后再处理 recovery 与 runtime driver 收敛，避免只讲原则不落到现有实现。 |
| P-379 | 收敛 runtime 执行层定位与 policy crate 内闭合原则 | Done | 2026-03-11 | 2026-03-11 | 在 `rfc/runtime-session-composition-stabilization.md` 追加更宏观的架构约束：1) 明确 `policy` 的定义、编译、消费都应优先闭合在 `xbox-streaming` crate 内，而不是仅把 compiler 放进 crate；2) 明确 runtime 不是策略解释层，而是执行层，只承担 WebRTC 协议编排、网络调度、输入/媒体接线与渲染；3) 将落地顺序调整为“先补 crate 内 projection/consumption 链路，再扩 `SessionExecutionSnapshot`，再统一 runtime driver，最后才考虑 web/rust runtime 内部实现统一”，避免先统一实现、后治理边界导致问题搬家。 |
| P-378 | 制定 runtime 与 policy/session 组合稳定化方案 | Done | 2026-03-11 | 2026-03-11 | 基于当前 `xbox-streaming::policy`、`xbox-streaming::session` 与 `src/streaming/runtime` 的边界现状，新增 RFC `rfc/runtime-session-composition-stabilization.md`，明确先不处理 Rust/web runtime 实现统一，而是先固定组合接口：1) policy 仅产出 `StreamingPlan`；2) session 消费 `Plan` 并产出 `SessionExecutionSnapshot`；3) runtime 只消费 `RuntimePlanProjection`，不再读取 raw config 做二次推导；4) 引入统一 `SessionRuntimeDriver` 与 recovery 归属原则（runtime 只上报本地事实，session 负责套用 `RecoveryPlan` 并下发 reconnect 命令）；5) 给出 4 步落地顺序，优先切断 UI/runtime 对 raw config 的重复解释。 |
| P-377 | 评审 streaming 侧代码架构与实现合理性并输出问题清单 | Done | 2026-03-11 | 2026-03-11 | 以 code review 视角审查 `src/streaming/*`、`src-tauri/src/mods/streaming/*`、`crates/xbox-streaming/*` 及 `xbxengine` 事件桥接链路，并执行验证命令：`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`、`cargo check -p xbxrc`（均通过，存在既有 warning）。结论：分层方向总体合理（策略下沉 crate、Tauri 门面化、renderer application/runtime 分离），但当前存在阻断级实现缺陷：1) `SessionRuntimeStore::upsert` 会取消同一 `SessionCancelToken`，导致 monitor loop 在首个 tick 后自杀；2) streaming xbxengine 事件 channel 在 rust 与 shared contract 不一致，`rust-owned` runtime 无法收到运行时事件；3) `useStreamSession` 缺少启动链路取消控制，页面卸载时可能遗留远端 session/轮询并发生状态回写竞争。 |
| P-376 | 基于 better-xcloud 参考制定 session 链路三层收敛架构方案 | Done | 2026-03-11 | 2026-03-11 | 结合 `better-xcloud/summary.md` 与当前代码现状，形成“策略层 / 会话编排层 / Tauri 胶水层”端到端收敛方案并落地 RFC：`rfc/session-chain-convergence-architecture.md`。方案内容包括：1) 能力映射（header/body/region/config override/SDP/ICE/input/stats）；2) UI->Rust 链路与分层职责边界；3) RPC 收敛策略（`streaming.session.*` 统一入口、`streamHost.*` 兼容转发后移除）；4) 模块划分（避免过度拆分）；5) 4 个里程碑迁移路线（M1~M4）与 DoD 验收标准；6) 建议优先开工项（RPC 收敛、RecoveryPlan 下沉、PlanProjection）。 |
| P-375 | 对 streaming 第三轮调整执行回归审查 | Done | 2026-03-11 | 2026-03-11 | 对用户第二次修正后的代码执行第三轮回归 review：1) 复核 `crates/xbox-streaming`、`src-tauri/src/mods/streaming`、`src-tauri/src/mods/data`、`crates/xbox-webapi` 关键变更；2) 运行验证命令并确认结果：`cargo test -p xbox-streaming` 通过（27 tests）、`cargo test -p xbxrc --lib` 通过、`cargo check -p xbxrc` 通过、`cargo test -p xbox-webapi` 通过；3) 确认上轮阻断问题已修复（monitor 测试签名同步、`StreamingSessionSnapshot` 恢复 target 字段来源、`gsToken` 缺失不再静默降级、`SessionFlowServiceInner` 封装收敛）；4) 记录当前无阻断回归，仅保留后续优化项（例如 `Plan::default` 仅用于测试场景的约束）。 |
| P-374 | 对 streaming 二次改动执行回归审查并更新 working 文档 | Done | 2026-03-11 | 2026-03-11 | 基于用户“按文档修改后”的最新代码再次执行 review：1) 重新审查 `crates/xbox-streaming`、`src-tauri/src/mods/streaming`、`src-tauri/src/mods/data` 与 `crates/xbox-webapi` 的变更；2) 运行验证命令并记录结果（`cargo test -p xbox-streaming` 失败、`cargo test -p xbxrc --lib` 通过、`cargo check -p xbxrc` 通过、`cargo test -p xbox-webapi` 通过）；3) 在 `rfc/working.md` 追加“2026-03-11 第二轮回归审查”章节，输出按优先级排序的问题与修复方案：测试签名回归、`StreamingSessionSnapshot.target_id` 清空与 `target_type` 猜测、`gsToken` 静默降级为空、`SessionFlowServiceInner` 封装边界放宽。该任务仅更新文档与任务追踪，不改动运行逻辑。 |
| P-373 | 对 streaming 最近一轮 plan/session 重构做回归审查并输出问题清单文档 | Done | 2026-03-11 | 2026-03-11 | 以 review 视角检查 `crates/xbox-streaming` 与 `src-tauri/src/mods/streaming` 的最新修改，整理阻断问题与修正方向到 `rfc/working.md`：1) 记录当前编译阻断（`RuntimeConfig: Default` 缺失）；2) 记录 `Plan` 持有 `gs_token` 且 `StreamingSessionSnapshot` 暴露 `plan` 导致的凭证泄漏；3) 记录 create 路径丢失 `resolution`、session 固化旧 token、plan 与执行层脱节、payload/header 设备画像不一致、本地伪造 `sessionPath`、facade 穿透 session internals、DTO 契约污染等问题；4) 给出按优先级排序的修复顺序。该任务仅新增文档与任务追踪，不改动运行逻辑。 |
| P-372 | 收敛 `mods/streaming` / `xbox-streaming` / `xbox-webapi` 整体架构并输出可执行 RFC | Done | 2026-03-11 | 2026-03-11 | 基于当前实现与阶段性迁移结果，重写 `rfc/streaming-strategy-layer-and-runtime-convergence.md`：1) 明确 `mods/streaming` 的外部交互面（RPC、AppState 生命周期、config、auth、streamHost bridge）；2) 固定 `mods/streaming` 仅为应用门面、`xbox-streaming/protocol` 为唯一策略编译层、`xbox-streaming/session` 为唯一 session flow 编排层、`xbox-webapi` 为低层 API 层；3) 明确扁平依赖原则与 `mods/data -> mods/streaming/api_provider` 的拆解方向；4) 定义 `AppStreamingConfig / CreateSessionRequest / StreamingPlan / SessionSnapshot` 四类核心对象与“plan 驱动 session”的迁移阶段。该任务仅更新 RFC 与任务追踪，不涉及代码行为变更。 |
| P-371 | 执行 streaming 强逻辑迁移 Phase 2（第八步）：session flow 下沉并将 tauri service 门面化 | Done | 2026-03-10 | 2026-03-10 | 按“tauri 只做 UI 门面、session flow 细节下沉”的目标重构：1) 在 `crates/xbox-streaming/src/session/flow.rs` 新增 `SessionFlowService` + `SessionFlowProvider`，统一收口 create/get/close/list、offer/ice 轮询、keepalive 语义、monitor loop 与 store 回写；2) 通过 `SessionFlowSnapshot` 把快照构建与 runtime 绑定职责定义在 crate 契约层，`src-tauri/src/mods/streaming/types.rs` 实现该契约；3) `src-tauri/src/mods/streaming/service.rs` 重写为薄门面，仅保留 RPC DTO 映射、fallback TURN 获取与 `SessionFlowProvider` adapter（token + webapi gateway 装配），删除 tauri 侧 session 编排细节；4) `xbox-streaming` 新增 `tokio/async-trait` 依赖并从 `session/mod.rs` 导出 flow 模块。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-370 | 执行 streaming 强逻辑迁移 Phase 2（第七步）：monitor 映射与回写收敛到 crate 绑定层 | Done | 2026-03-10 | 2026-03-10 | 继续把 tauri `service` 中 monitor 记录映射下沉且保持“不过度抽象”：1) 在 `src-tauri/src/mods/streaming/types.rs` 为 `StreamingSessionSnapshot` 实现 `xbox_streaming::SessionRuntimeBinding`，并集中实现 `StreamingErrorDetails/StreamingQueueDetails/StreamingQueueSnapshot` 与 monitor 域 DTO 的双向转换；2) `src-tauri/src/mods/streaming/service.rs` 的 `monitor_session_tick` 改为直接调用 `apply_monitor_tick_to_record`，删除 `to_monitor_state/apply_monitor_state` 及 error/queue 映射辅助函数，tauri 层仅保留远端状态拉取、输入组装、副作用（send_connect_token）与存储 upsert；3) 等待队列降级路径改为复用类型层转换方法，减少 service 字段搬运噪音。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-369 | 执行 streaming 强逻辑迁移 Phase 2（第六步）：WebAPI Session/Signaling 网关下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 继续收缩 tauri 侧实现层：1) 新增 `crates/xbox-streaming/src/webapi_session.rs` 与 `webapi_signaling.rs`，把 session play payload 组装、stream state/queue DTO 适配、SDP/ICE 交换与 ICE 归一化封装迁入 crate；2) `src-tauri/src/mods/streaming/api_provider.rs` 与 `service.rs` 改为直接消费 `WebApiSessionGateway/WebApiSignalingGateway`，并仅保留本地 RPC DTO 转换；3) `src-tauri/src/mods/data/services/streaming_query_service.rs` 切换为复用 crate session gateway；4) 删除 tauri 侧重复模块 `session_api.rs`、`signaling_api.rs`、`ice_normalizer.rs` 并从 `mods/streaming/mod.rs` 取消导出。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-368 | 执行 streaming 强逻辑迁移 Phase 2（第五步）：会话生命周期语义下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 将 `service.rs` 中生命周期纯判定继续迁入 crate：1) 新增 `crates/xbox-streaming/src/session_lifecycle.rs`，收口 `session_path -> session_id` 解析、远端关闭 404 判定、active sessions targetType 默认规则；2) `src-tauri/src/mods/streaming/service.rs` 改为调用 `parse_session_id_from_path` / `is_remote_session_not_found` / `resolve_active_target_type`，tauri 仅保留网络调用与错误转译；3) 补充路径兼容（有无前导 `/`）与语义单测。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-367 | 执行 streaming 强逻辑迁移 Phase 2（第四步）：fallback TURN provider 下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 继续把 tauri 中残留的 Xbox 业务策略迁入 crate：1) 新增 `crates/xbox-streaming/src/turn_fallback.rs`（`FallbackTurnProvider`），收口 home fallback TURN 拉取、解析与缓存；2) `src-tauri/src/mods/streaming/service.rs` 改为依赖 crate provider，`get_fallback_turn_server` 仅做 `TurnServer -> StreamingTurnServerConfig` DTO 适配；3) crate 增加 `reqwest` 依赖并补充解析单测，行为与原实现一致；4) 删除 tauri 侧重复实现 `src-tauri/src/mods/streaming/fallback_turn_server_provider.rs` 并从 `mod.rs` 取消导出，避免双份逻辑。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-366 | 执行 streaming 强逻辑迁移 Phase 2（第三步）：会话内存存储与取消令牌下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 把 `service.rs` 中会话记录容器职责继续下沉且保持最小抽象：1) 新增 `crates/xbox-streaming/src/session_store.rs`（`SessionRuntimeStore`、`SessionRuntimeRecord`、`SessionCancelToken`），统一 insert/upsert/remove/list 与取消标记联动；2) `src-tauri/src/mods/streaming/service.rs` 改为使用 `SessionRuntimeStore<StreamingSessionSnapshot>` 持有会话，`monitor_session_loop` 改读 `SessionCancelToken`，`create_session/get_session/list_active/shutdown/clear` 统一走 store API；3) 不改变 RPC 结构与对外行为，仅缩减 tauri 层本地状态管理逻辑。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-365 | 执行 streaming 强逻辑迁移 Phase 2（第二步）：signaling 轮询与 keepalive 语义下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 在不引入过度抽象前提下继续抽离 `service.rs` 业务判定：1) 新增 `crates/xbox-streaming/src/session_signaling.rs`，收口 offer/ice 轮询决策（`PollDecision`、`decide_offer_poll`、`decide_ice_poll`）、可用 ICE 判定与 keepalive 忽略策略（按 status/body 解析）；2) `src-tauri/src/mods/streaming/service.rs` 的 `exchange_offer`/`exchange_ice` 改为调用 crate 决策函数并使用统一轮询间隔常量，`send_keepalive` 改为调用 crate 忽略策略；3) tauri 继续只做 API 调用、会话存在性检查与 DTO 适配。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-364 | 执行 streaming 强逻辑迁移 Phase 2（第一步）：会话监控状态机下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 继续按“不过度拆分”推进会话编排下沉：1) 在 `crates/xbox-streaming/src/session_monitor.rs` 新增纯状态机模块（`SessionMonitorState/Input/Result`、`apply_monitor_tick`、超时规则常量），把 `Provisioning/ReadyToConnect/WaitingForResources/Failed` 状态迁移与超时失败语义从 tauri 抽离；2) `src-tauri/src/mods/streaming/service.rs` 的 `monitor_session_tick` 改为“拉取远端状态 + 组装输入 + 调用 crate 状态机 + 回写记录 + 可选发送 connect token”；3) 保留 tauri 侧网络调用、auth token 获取与存储更新，不改变 RPC 对外契约。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo test -p xbxrc --lib`。 |
| P-363 | 执行 streaming 强逻辑迁移 Phase 1：接入解析与 ICE policy 下沉 `xbox-streaming` | Done | 2026-03-10 | 2026-03-10 | 完成最小可运行迁移实现且不改 RPC 契约：1) `crates/xbox-streaming/src/session_access.rs` 新增 token 解析与接入选择 API（`parse_session_access_context`、`resolve_session_access_from_token`），把 token->`gsToken/regions` 的强逻辑从 tauri 下沉到 crate；2) `src-tauri/src/mods/streaming/api_provider.rs` 改为调用 crate 入口，tauri 仅保留 config/target 适配与错误映射；3) `src-tauri/src/mods/streaming/ice_normalizer.rs` 的候选归一化算法下沉到 `crates/xbox-streaming/src/ice.rs`（`IcePolicy`），tauri 仅保留 `StreamingIceCandidate <-> IceCandidate` 适配；4) `src-tauri/Cargo.toml` 引入 `xbox-streaming` 依赖并完成联编。验证通过：`cargo fmt -p xbox-streaming -p xbxrc`、`cargo test -p xbox-streaming`、`cargo check -p xbxrc`。 |
| P-362 | 规划将 `mods::streaming` Xbox 强逻辑迁移至 `xbox-streaming`（参考 auth 域，避免过度拆分） | Done | 2026-03-10 | 2026-03-10 | 完成迁移设计评审与阶段规划：1) 明确强逻辑边界（会话接入解析、会话状态机、SDP/ICE 交换轮询、keepalive 语义、ICE 归一化、输入/协商/runtime plan 编译）应下沉 `crates/xbox-streaming`；2) 明确 `src-tauri/src/mods/streaming` 保留 RPC/参数校验/AppState 装配/Auth+Config provider 适配，退化为 adapter；3) 参考 `auth` 域采用“service + runtime_state + persistence/repository（按需）”风格，但按 streaming 实际复杂度控制为最小模块集，避免 DDD 化过度抽象；4) 形成三阶段迁移顺序（先 SessionAccess/Plan 编译复用，再会话编排下沉，最后 tauri 薄化与 runtime 对接收敛）及每阶段 DoD/回滚点。 |
| P-361 | 补齐 `xbox-streaming` compiler 失败路径与会话接入输出 API | Done | 2026-03-10 | 2026-03-10 | 在 `crates/xbox-streaming/src/compiler` 中继续把编译实现补全到“失败可感知、输出可复用”：1) `runtime::compile_runtime` 从静默 `unwrap_or(WebRtcDirect)` 改为返回 `Result<RuntimePlan, CompileError>`，并在 `compile` 入口透传 `RuntimeUnavailable`，避免 runtime 不可用时被悄悄降级；2) 新增公开函数 `resolve_session_access_output(config, context) -> Result<SessionAccessOutput, CompileError>`，让 `SessionAccessOutput` 不再是占位类型，便于 tauri 侧先单独复用会话接入解析；3) 单测增加 `runtime_unavailable_returns_error` 与 `resolve_session_access_output_wraps_access`，当前总计 8 个测试覆盖主干规则与失败路径。验证通过：`cargo fmt -p xbox-streaming`、`cargo check -p xbox-streaming`、`cargo test -p xbox-streaming`。 |
| P-360 | 将 `xbox-streaming` compiler 拆分为按职责的 Rust 模块 | Done | 2026-03-10 | 2026-03-10 | 按“更 Rust 化、减少单文件复杂度”的目标，把 `crates/xbox-streaming/src/compiler.rs` 拆分为 `src/compiler/mod.rs + {session,runtime,negotiation,input}.rs`：1) `mod.rs` 保留公开契约（`CompilerInput/Output`、`CompileError`、`compile`、`resolve_session_access`）与单测；2) `session.rs` 收口会话接入解析、region 选择、base URL 归一化、设备画像与 `x-ms-device-info` 头生成；3) `runtime.rs` 收口 runtime mode 选择与 TURN 解析；4) `negotiation.rs` 收口 codec/bitrate/offer profile/console addrs 协商编译；5) `input.rs` 收口输入模式判定与 touch/mic 编译。此改动为纯结构重排，行为保持不变。验证通过：`cargo fmt -p xbox-streaming`、`cargo check -p xbox-streaming`、`cargo test -p xbox-streaming`。 |
