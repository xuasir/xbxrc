# Project Task Tracker

> 归档副本文件：docs/project-task.archived.2026-03-18.md

## Current Task List

| ID | Task | Status | Started On | Completed On | Notes |
| --- | --- | --- | --- | --- | --- |
| P-916 | 重构 Transport 模块并全面迁移至新 RTC 架构，废弃旧有 WebRTC 实现 | Done | 2026-03-21 | 2026-03-21 | 彻底重构了 transport 模块，全面应用新 RTC 框架能力，并废弃了基于 `webrtc-rs` 的旧栈。将控制流、调度策略、以及 BWE 与 recovery 机制解耦迁移到了更现代、层级分明的新架构中。视频流采用了新的 frame source 适配器，而音频流改造为通过 MPSC 队列投递 `RtcAudioRtpPacket`。修复所有构建错误，测试已全量通过，完成整体模块替换。 |
| P-915 | 用新加的 RTC bootstrap 诊断日志继续确认 `HandshakeAck` 后为什么看不到后续 payload：是门槛未过、发送失败，还是被状态覆盖 | Done | 2026-03-21 | 2026-03-21 | 在前一份 trace 里，新 `rtc` 的 `OnOpen`、message handshake 和 `HandshakeAck` 都已经能明确看到，但 `rtcControlAuthorizationSent` / `rtcMessagePostHandshakeSent` / `rtcInputMetadataBootstrapSent` 一直没出现在日志中，无法只靠旧 trace 判断是“没走到”还是“走到了但被覆盖”。因此我在 `service.rs` 里给 `send_post_handshake_messages()`、`try_bootstrap_control_channel()`、`try_bootstrap_input_channel()` 增加了更细的 `WARN` 诊断：包括 bootstrap gate 是否被跳过、发送前的 channel 状态、以及每个 post-handshake / control / input bootstrap payload 的显式发送日志。随后两条 RTC 单测都再次通过，并在输出里直接看到 `sending post-handshake payload`、`control bootstrap starting/completed`、`input bootstrap starting/sending`，说明新栈的 bootstrap 链路本身是可运行的。下一步只需要用同样日志再抓一份 runtime trace，就能精确区分“trace 覆盖”与“bootstrap 未发”。 |
| P-914 | 回看 `runtime-trace-1774060938326`，只验证新 `rtc` 的 channel open / handshake / bootstrap 是否真的落到日志 | Done | 2026-03-21 | 2026-03-21 | 这份新 trace 已经把新 `rtc` 的生命周期证据补齐：`seq 216-223` 连续出现新加的 `data channel onopen observed` 与 `rtcControlChannelOpened` / `rtcMessageChannelOpened` / `rtcInputChannelOpened` / `rtcChatChannelOpened`，说明四个 DataChannel 的 `OnOpen` 真的发生了；紧接着 `seq 225` 仍然有 `rtcMessageHandshakeSent`，随后 `seq 228-315` 连续出现 `inbound message payload observed` 与 `inbound message handshake ack observed`，说明 message handshake 与 ack 消费链也正常工作。trace 中没有出现新 `rtc` 的 `rtcControlAuthorizationSent` / `rtcMessagePostHandshakeSent` / `rtcInputMetadataBootstrapSent` 这类后续 bootstrap 发送日志，因此当前新栈下一个可继续排查的点，已经从“channel 有没有 open / handshake 有没有到”收敛到“ack 之后为什么没有看到后续 bootstrap payload 出去”。末尾的 `[xbxengine][webrtc-rs] queue pending keyframe request until recovery protocol is ready (...)` 仍然属于旧栈调度日志，不参与新 `rtc` 的结论。 |
| P-913 | 给新 `rtc` 的 DataChannel `OnOpen` 增加不可覆盖的显式日志，并用单测确认 channel lifecycle 真的有被触发 | Done | 2026-03-21 | 2026-03-21 | 为了继续围绕新 `rtc` 本身验证，而不是被旧 `webrtc-rs` 的调度日志干扰，我在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 的 `apply_data_channel_event()` 里给 `OnOpen` 增加了四类显式 `WARN`：`data channel onopen observed`、`rtcMessageChannelOpened observed`、`rtcControlChannelOpened observed`、`rtcInputChannelOpened observed`、`rtcChatChannelOpened observed`。然后用两条已有的 RTC 单测验证输出：`service_bootstraps_message_and_control_payloads_after_handshake_ack` 和 `service_connects_to_raw_rtc_answer_peer_and_opens_control_channel` 都能打印出新 `rtc` 的 OnOpen 日志，证明 channel open 生命周期确实发生了，而不是之前仅仅被 `latest_observation_label` 或旧栈 recovery 日志覆盖掉。 |
| P-912 | 将新 `rtc` 的验证范围与旧 `webrtc-rs` 调度彻底隔离，只围绕新栈的 channel lifecycle / bootstrap 继续排查 | Done | 2026-03-21 | 2026-03-21 | 按用户要求收紧验证边界：旧 `webrtc-rs` 里的 recovery 调度、pending keyframe、delay prime 这些日志不再用于推断新 `rtc` 的状态，因为它们会把新旧实现的生命周期信号糅杂在一起。新 `rtc` 这边当前只保留三类有效证据：`rtcMessageHandshakeSent`、新加的 `inbound message payload observed` / `inbound message handshake ack observed`、以及 `rtcReadIngressObserved`。在这份 trace 中，没有出现任何新 `rtc` 的 `rtcMessageChannelOpened` / `rtcControlChannelOpened` / `rtcInputChannelOpened` / `rtcChatChannelOpened` 观测，因此后续只应继续验证新栈自己的 `OnOpen`/bootstrap 观测是否缺失，而不要再把旧栈的 `webrtc-rs` pending keyframe 当成新栈控制面状态。 |
| P-911 | 进一步收窄 `runtime-trace-1774060075122` 的判读边界：确认新 `rtc` 只证明了 message ingress/ack，`webrtc-rs` 的 pending keyframe 不能混读成新栈状态 | Done | 2026-03-21 | 2026-03-21 | 这轮只做“栈分离”核对，不再把旧实现状态误判到新实现上。trace 里新 `rtc` 的明确证据只有两类：一是 `seq 175` 的 `rtcMessageHandshakeSent`，二是 `seq 176-202` 的 `inbound message payload observed` / `inbound message handshake ack observed`，再加上 `seq 314` 的 `rtcReadIngressObserved`，说明 message 入站与 ack 消费链已经跑通；但 trace 里没有任何 `rtcMessageChannelOpened` / `rtcControlChannelOpened` / `rtcInputChannelOpened` / `rtcChatChannelOpened` 证据，因此不能把“channel open 已确认”当成已知事实。反过来，`seq 575` 的 `queue pending keyframe request until recovery protocol is ready (...)` 前缀明确是 `[xbxengine][webrtc-rs]`，属于旧栈的 recovery/control 状态，不应混进新 `rtc` 的判读里。当前最稳妥的结论是：新 `rtc` 至少已完成 message handshake 与入站消费，但 channel open 生命周期在这份 trace 中没有留下可直接确认的观测；旧 `webrtc-rs` 的 pending keyframe 则是另一个并行路径的日志，不能拿来证明新栈 control/bootstrap 未 ready。 |
| P-910 | 继续回看 `runtime-trace-1774060075122` 的双栈时序，确认新 `rtc` 路径已消费 `HandshakeAck`，而真正卡住的是旧 `webrtc-rs` recovery/control 仍未 ready | Done | 2026-03-21 | 2026-03-21 | 这份 trace 里新 `rtc` 路径已经有明确闭环证据：`seq 176-202` 连续打印了新加的 `inbound message payload observed` / `inbound message handshake ack observed`，说明 message 入站与 `HandshakeAck` 识别都已经跑通；`seq 314` 的 `rtcReadIngressObserved` 也说明 ingress/poll_read 链路在健康消费。真正仍在挂起的是旧 `webrtc-rs` recovery：`seq 575` 明确刷出 `[xbxengine][webrtc-rs] queue pending keyframe request until recovery protocol is ready (control_ready=false,handshake_acked=false,control_started=false,control_bootstrapped=false,pending_keyframe=true,pending_decoder_reset=false,control_open=false)`，而 trace 里没有出现对应的 `bootstrap control after message handshake ack`、`bootstrap input metadata after message handshake ack` 或 `flushing pending keyframe request after control channel recovery`。因此当前卡点已经从“ack 没收到”收敛为“双栈并存时，旧 webrtc-rs 栈的 control/bootstrap 仍没进入 ready 状态”，下一步应直接查旧栈的 control open / bootstrap 生命周期，而不是继续怀疑新 rtc 的 handshake 消费。 |
| P-909 | 基于 `runtime-trace-1774060075122` 复核 RTC message 入站与 `HandshakeAck` 消费时序，确认 ack 已到但后续 bootstrap 仍需继续追 | Done | 2026-03-21 | 2026-03-21 | 这份新 trace 与上一轮不同：`drain_peer_reads()` 新增的两条 ingress 日志已经直接打出 `HandshakeAck`，例如 `seq 176-202` 连续出现 `inbound message payload observed` / `inbound message handshake ack observed`，说明 message 通道入站、payload 识别与 ack 判断链路都已跑通；同一份 trace 里还能看到 `rtcReadIngressObserved`（`seq 314`），证明 `poll_read()` / ingress 消费链并未停掉。与此同时，恢复状态在关键窗口仍是健康的（`seq 314` 的 `videoHealth=healthy`、`primaryIssueChain=steady:healthy`），后来才在 `seq 571-577` 进入 `waitingKeyframe`，说明这次不再是“ack 根本没回来”或“入口早期背压打满”的问题。当前剩余疑点是：虽然 ack 已被观察到，但 trace 中仍未看到 `rtcMessagePostHandshakeSent`、`rtcControlAuthorizationSent`、`rtcInputMetadataBootstrapSent` 或明确的 `control_bootstrapped=true/handshake_acked=true` 状态落点，因此下一步应继续追 `ack_handshake()` 之后的 bootstrap 推进条件，而不是再怀疑 ack 本身。 |
| P-908 | 为 RTC message 入站消费链补 payload 预览日志，直接确认 `HandshakeAck` 实际收到的内容 | Done | 2026-03-21 | 2026-03-21 | 按前一轮结论继续往下补证据：在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 的 `drain_peer_reads()` 中，为 `MESSAGE_CHANNEL_LABEL` 的字符串入站增加两条 `WARN` 级日志，一条输出 `inbound message payload observed` 及 payload 预览，另一条在命中 `{"type":"HandshakeAck"}` 时输出 `inbound message handshake ack observed`。这样下一份 trace 不再只靠 `channelMessageCatalog`/状态摘要推断，而能直接看到实际入站字符串。验证：`cargo test -p xbxengine service_pump_observes_data_channel_message_from_poll_read -- --nocapture`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`，两条都通过，并且测试输出已确认 `HandshakeAck` 日志可正常打印。 |
| P-907 | 继续排查 RTC message 入站消费链是否真正吃到了 `HandshakeAck`，以 `pump()` 时序和后续 bootstrap 观测为准 | Done | 2026-03-21 | 2026-03-21 | 按用户要求聚焦第 2 条继续追：`RtcConnectionService::pump()` 的顺序是 `io_runtime.pump -> drain_peer_events -> drain_peer_reads -> try_send_message_handshake -> run_delayed_control_actions`，所以入站 `HandshakeAck` 如果存在，本轮是有机会被 `drain_peer_reads()` 读到并立即推进 `send_post_handshake_messages()` / `try_bootstrap_control_channel()` / `try_bootstrap_input_channel()` 的。trace 里虽然能看到 `channelMessageCatalog` 的 `message` 入站观测持续出现，说明 `poll_read()` 链路确实在跑，但完全没有后续的 `rtcMessagePostHandshakeSent` / `rtcControlAuthorizationSent` / `rtcInputMetadataBootstrapSent`，也没有 `HandshakeAck` 的直接痕迹。因此这次更像是“入站消息有进来，但并没有真正被识别成 HandshakeAck 并触发 bootstrap”，而不是 `poll_read()` 没有被执行。此次仅做时序核对与结论归档，没有改代码、没有跑测试。 |
| P-906 | 继续追踪 RTC `HandshakeAck` / `OnOpen` 的实际消费时序，判断 open 事件是否被 handshake sent 覆盖 | Done | 2026-03-21 | 2026-03-21 | 继续核对 `runtime-trace-1774055315405` 与 `service.rs` 的事件链后，结论更精确了：`RtcConnectionService::apply_data_channel_event()` 在收到 `OnOpen` 后会先 `publish_channel_lifecycle()`，再立即执行 `try_send_message_handshake()`；而 `try_send_message_handshake()` 会把最新观测直接写成 `rtcMessageHandshakeSent`。因此这份 trace 里只看到 handshake sent、看不到独立的 `rtcMessageChannelOpened` / `rtcControlChannelOpened`，并不能直接证明 open 没发生，反而更像是 open 与 handshake sent 发生在同一轮，open 标签被后续写入覆盖了。与此同时，这份 trace 仍然没有看到任何 `HandshakeAck`，也没有看到后续的 post-handshake/control bootstrap 闭环，所以真正的问题还是 ack/bootstrap 没闭合，而不是单纯缺少 open 标签。此次仅做时序核对与结论归档，没有改代码、没有跑测试。 |
| P-905 | 回看 `runtime-trace-1774055315405` 的 channel 打开与 `HandshakeAck` 时序，确认是否存在“已 open 但未记录” | Done | 2026-03-21 | 2026-03-21 | 延续 `P-904` 继续做时序核对：代码里 `RtcConnectionService::apply_data_channel_event()` 对 `RTCDataChannelEvent::OnOpen` 会立即发布 `rtcControlChannelOpened` / `rtcMessageChannelOpened`，并在 message open 后立刻调用 `try_send_message_handshake()`；`HandshakeAck` 解析也确实存在于 `service.rs` 的 `drain_peer_reads()` 链路中。回看这份 trace，只能找到 `rtcMessageHandshakeSent`，找不到任何 `HandshakeAck`，也找不到 `rtcMessageChannelOpened` / `rtcControlChannelOpened`。因此不是“已 open 但日志漏记”，而是这份 trace 里根本没有观察到对应 open / ack 事件，时序上只能说明 message handshake 被发出，但后续控制面没有进入可确认的 ack/bootstrap 闭环。此次仅做时序核对与结论归档，没有改代码、没有跑测试。 |
| P-904 | 核对 RTC message `HandshakeAck` 协议约定是否有效，并确认其对控制面 bootstrap 的作用 | Done | 2026-03-21 | 2026-03-21 | 继续追踪 `runtime-trace-1774055315405` 的恢复链后，先核对 `HandshakeAck` 是否是有效协议而不是无效占位。结论：当前旧 `webrtc` 连接层和新 `rtc` 连接层都把 message handshake ack 定义成同一个正式 payload：`{"type":"HandshakeAck"}`。在旧链路里，`message` 通道收到该 payload 后会置 `message_handshake_acked=true`，然后触发 post-handshake message、control bootstrap，以及 pending recovery request 的 flush；在新链路里也同样会 `ack_handshake()` 后推进 `send_post_handshake_messages()` / `try_bootstrap_control_channel()` / `observe_control_replay_if_ready()`。因此 `HandshakeAck` 本身是有效且必要的控制面门槛；本次 trace 里没有看到任何 `HandshakeAck` 痕迹，所以后续应优先继续查“为什么没有回 ack / 为什么 message channel 没推进到可识别的 ack”，再回看 `control` open 时序。此次仅做协议核对与结论归档，没有改代码、没有跑测试。 |
| P-903 | 固化 RTC 新连接层 control 时序：重动作仅允许 post-handshake 执行 | Done | 2026-03-21 | 2026-03-21 | 按“不要开关，默认时序合理固化”收紧连接层行为：在 [`control_channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/control_channel.rs) 将 `request_video_keyframe`、`request_decoder_reset`、`peek_replay_actions_if_ready` 从 `is_control_actionable()` 改为必须 `is_control_ready()`；保留 startup fallback 仅用于轻量 bootstrap/可达性，不再放行 keyframe/decoder-reset/replay 等重动作。在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 将 delayed keyframe prime 的发送门槛同步改为 `is_control_ready()`，未 ready 时只走 deferred/pending。并更新单测 `startup_fallback_is_not_treated_as_ready_for_recovery_actions`，锁住 pre-handshake 不可直接发送恢复动作。验证：`cargo test -p xbxengine transport::rtc::connection::control_channel::tests:: -- --nocapture`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`。 |
| P-902 | 对齐 RTC 新连接层 channel 启动语义（control/input 允许 pre-handshake 启动并补齐 lifecycle 观测） | Done | 2026-03-21 | 2026-03-21 | 针对“画面正常但输入无效、channel 未全对齐”的问题，按旧 `webrtc/data_channel/bootstrap` 语义在 [`control_channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/control_channel.rs) 把控制面状态从单一 `control_bootstrapped` 拆为 `control_started + control_bootstrapped_after_handshake`，并引入 `is_control_actionable()`（含 startup fallback：`!handshake_acked && control_started && control_open`）；`request_video_keyframe/request_decoder_reset` 与 replay 消费改为按 actionable 判定。在 [`runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs) 新增 `input_metadata_bootstrapped_after_handshake`；在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 取消 `input_stream_ready()` 对 `message_handshake_acked` 的硬门控，`try_bootstrap_input_channel()` 改为“pre-handshake 先发一次 + handshake 后再补发一次”，并补上 `rtcControlChannelOpened/rtcMessageChannelOpened` 结构化 lifecycle 观测；`run_delayed_control_actions()` 的 keyframe prime 发送条件改为 `is_control_actionable()`。同时修复连接层测试中的状态字段断言适配。验证：`cargo check -p xbxengine`、`cargo test -p xbxengine transport::rtc::connection::control_channel::tests:: -- --nocapture`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`。 |
| P-901 | 回归 `runtime-trace-1774055315405` 的 cloud 画面卡死原因，判断是否为缺包风暴 | Done | 2026-03-21 | 2026-03-21 | 基于 [`runtime-trace-1774055315405.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1774055315405.jsonl) 复盘，末尾并不是典型的缺包风暴：日志中没有看到持续的 `packet gap` / `nack` / `twcc` 丢失链，`latest_video_packet_gap` 也保持为空；真正刷屏的是 `legacy frame bridge ingress dropped err=no available capacity`，说明媒体入口被背压打满、上游包进来了但下游没有容量接住。继续追下去发现更关键的事实：只有一次 `queue pending keyframe request until recovery protocol is ready`，且直到结束都没有看到 `bootstrap control after message handshake ack` / `flushing pending keyframe request after control channel recovery`，也就是说 keyframe 请求卡在 control 面未就绪，`pending_keyframe=true` 没有被 flush，`recovery_keyframe_request_count` 也一直是 0。runtime 最终停在 `primaryIssueChain=recovery:ingressWaitKeyframe`、`stallKind=waitingKeyframe`、`videoHealth=waitingKeyframe`。结论：这次画面卡住更像是“控制面 bootstrap 未完成导致 keyframe 请求挂起，再叠加入口背压”，不是网络侧的缺包风暴。此次仅做日志分析与结论归档，没有改代码、没有跑测试。 |
| P-895 | 修复 RTC 连接层 `xbxEngineRtcSocketSendFailed: Invalid argument (os error 22)` 的 IPv4/IPv6 地址族错投 | Done | 2026-03-21 | 2026-03-21 | 基于 [`runtime-trace-1774050458094.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1774050458094.jsonl) 定位到新连接层在 `io_runtime` 仅使用单个 IPv4 UDP socket，遇到远端返回 IPv6 candidate（`2603:...`）时仍向 IPv4 socket `send_to`，触发 `EINVAL(22)`。在 [`crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 改为 v4/v6 双 socket 架构：`rebuild()` 同时绑定 `0.0.0.0:0` 与 best-effort `[::]:0`，`send_to_peer()` 按 `peer_addr` 地址族分流发送，`pump()` 分别读取 v4/v6 socket，`local_candidate()` 根据 advertised IP 家族选择对应端口；并补充 `resolve_local_addr_for_socket` 相关单测。验证：`cargo test -p xbxengine transport::rtc::connection::io_runtime -- --nocapture`、`cargo test -p xbxengine --lib transport::rtc::connection::service::tests::create_raw_offer_comes_from_real_rtc_peer_connection -- --nocapture`。 |
| P-894 | PATCH SCOPE A：rtc 连接层补齐 H264 family 注册与 offer 偏好约束基线 | Done | 2026-03-21 | 2026-03-21 | 在 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 增加 `register_owned_h264_codecs()` 与 `build_owned_h264_codec_preferences()`，在 `register_default_codecs()` 后补注册 `H264(640032/4d0032/42e01f/42001f)+RTX(apt=124)`，并保持 `audio(sendrecv)+video(recvonly)` 现有 transceiver 方向语义。由于 `rtc 0.9.0` 未公开 `transceiver.set_codec_preferences`，在 `configure_offer_primitives()` 明确注释采用“MediaEngine 预注册 + SDP policy 重排”的等效偏好策略。新增单测 `owned_h264_codec_preferences_include_main_profile_and_rtx_probe` 与 `register_owned_h264_codecs_is_compatible_with_default_registry`。验证：`cargo fmt`、`cargo test -p xbxengine owned_h264_codec_preferences_include_main_profile_and_rtx_probe`、`cargo test -p xbxengine register_owned_h264_codecs_is_compatible_with_default_registry`、`cargo test -p xbxengine create_raw_offer_comes_from_real_rtc_peer_connection`。 |
| P-893 | 提升启动阶段鲁棒性：Closed 不再立即失败，按 Recovering 窗口判定瞬态 | Done | 2026-03-21 | 2026-03-21 | 在 [`crates/xbox-streaming/src/session/flow.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs) 将 `wait_until_session_started_or_failed()` 从“`Closed` 立即 `streamingStartFailed`”改为“带恢复窗口判定”：新增 `startup_closed_recovery_grace_ms()` 和 `decide_startup_progress_action()`，当最近仍有 `Creating/WaitingSessionReady/RuntimeStarting/Recovering`（或错误文案含 reconnect/recover）信号时，把 `Closed` 视为瞬态并继续等待，仅 `Failed` 或超出恢复窗口/总超时才失败；并增加 4 个单测覆盖 `Closed` 瞬态/超窗失败/Recovering 刷新时间戳/reconnect 文案触发恢复窗口。同时在 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 的 `schedule_immediate_reconnect()` 增加恢复信号可观测字段（`reason/recoveryActionCreated/observationId`），用于区分瞬态 closed 与已进入恢复编排。验证：`cargo test -p xbox-streaming session::flow -- --nocapture`、`cargo test -p xbxengine --lib transport::rtc::connection::service -- --nocapture`。 |
| P-892 | 审计新 transport/connection/sdp/ice 实现并定位 Closed 直接失败链路 | Done | 2026-03-21 | 2026-03-21 | 完成对 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs)、[`crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs)、[`crates/xbxengine/core/src/transport/rtc/stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs)、[`crates/xbxengine/core/src/transport/rtc/sdp/*`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/mod.rs)、[`crates/xbox-streaming/src/session/flow.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs)、[`crates/xbox-streaming/src/session/signaling/{ice,logic}.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/signaling/ice.rs) 与 [`crates/xbox-streaming/src/session/recovery.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/recovery.rs) 的代码级审阅，输出连接层状态机、gather/poll 接入现状、SDP/ICE 适配现状，并标注 `Closed` 直接失败潜在命中点。此次为静态审计任务，无代码逻辑变更、无测试执行。 |
| P-891 | 对齐 webrtc-rs 本地边界处理，修正新 RTC advertised IP 选取逻辑 | Done | 2026-03-20 | 2026-03-20 | 按用户要求不再使用经验性网段猜测，改为对齐 webrtc-rs 本地 `local_interfaces` 边界语义（默认只排除 loopback）：在 [`io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 接入 `rtc::shared::ifaces::ifaces()` 枚举本机接口地址，先从接口集合中选非 loopback 地址；若默认路由探测地址也在该集合中则优先该地址；否则稳定排序后选首个地址。`advertised_ip_priority` 同步收敛为与 webrtc-rs 一致的“loopback/unspecified 过滤”口径，不再硬编码 `198.18/15`、`100.64/10` 等附加规则。新增/更新单测覆盖 loopback 拒绝与 IPv4/IPv6 非 loopback 接受。验证：`cargo fmt --all`、`cargo test -p xbxengine advertised_ip_priority -- --nocapture`、`cargo check -p xbxengine`。 |
| P-890 | 对齐新 RTC 连接层的 ICE 可达性与候选语义（修复 loopback/空 mid/默认 STUN） | Done | 2026-03-20 | 2026-03-20 | 针对“SDP/连接/ICE 都未对齐旧行为”的 P0 缺口，在 [`io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 将本地 UDP 绑定改为 `0.0.0.0`，并新增 advertised IP 探测（非测试环境）生成可达 host candidate，避免继续上送 `127.0.0.1`；同时将 `pump` 的 `TransportContext.local_addr` 对齐到 advertised IP+port，修复“candidate 地址与收发上下文地址不一致”导致的连通性问题；在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 补齐默认 STUN 列表（与旧 transport 对齐，测试环境关闭），并修复本地 candidate `sdp_mid` 为空串时强制回填 `"0"`；另外把 rtc-to-rtc 连接相关单测改为消费“全量 service candidates”，避免默认 STUN 引入后只取首 candidate 的时序脆弱。验证：`cargo test -p xbxengine create_raw_offer_comes_from_real_rtc_peer_connection -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`、`cargo test -p xbxengine service_pump_observes_data_channel_message_from_poll_read -- --nocapture`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`、`cargo check -p xbxengine`。 |
| P-889 | 对齐新 RTC 连接层 offer primitives 与旧行为（补齐 audio/video m-line + 校验日志） | Done | 2026-03-20 | 2026-03-20 | 修复“新连接层卡在 `ExchangingOffer` 且 offer 只有 `m=application`”的问题：1) 在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 的 `rebuild()` 增加 `configure_offer_primitives()`，在创建 data channel 前显式添加 `audio/video` transceiver，其中 `audio` 使用真实 `sendrecv`（注册默认 codecs + opus base encoding），`video` 使用 `recvonly`，确保语义与旧链路对齐且稳定生成 `m=audio/m=video`；2) 在 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 的 `create_offer()` 接入旧链路同款 `validate_local_offer_sdp()` 与 `summarize_sdp()` 日志，创建阶段即校验 `audio/video/application` 三段齐全，避免“发起了但结构不对”；3) 更新连接层单测断言，锁定 raw offer 必须包含 `m=audio/m=video/m=application`。验证：`cargo test -p xbxengine create_raw_offer_comes_from_real_rtc_peer_connection -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`、`cargo check -p xbxengine`。 |
| P-888 | 补齐 rtc/sdp 实质协商规则（不再仅 phase tag） | Done | 2026-03-20 | 2026-03-20 | 修复“SDP 内容未打通”：1) [`offer_policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/offer_policy.rs) 改为复用旧链路成熟的 `apply_offer_policy_contract`，把 `b=AS`、H264 payload 重排、`fmtp` 约束（`x-google-*`/`max-fs`/`max-fr`）真实投影到 rtc offer；2) [`candidate_adapter.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/candidate_adapter.rs) 增加候选归一化/过滤：剔除空候选、`end-of-candidates`、`UDP+tcptype` 非法项，统一去掉 `a=` 前缀；3) [`answer_adapter.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/answer_adapter.rs) 统一换行和空行清理，避免解析受平台换行影响；4) [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 改为 `filter_map(normalize_remote_candidate)`，避免把被过滤项继续送入连接层。新增 sdp 单测覆盖 answer/candidate 归一化。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::rtc::sdp -- --nocapture`、`cargo check -p xbxengine`。 |
| P-887 | 横向对比新 RTC 连接实现与旧 transport 策略耦合面 | Done | 2026-03-20 | 2026-03-20 | 完成对 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs)、[`crates/xbxengine/core/src/transport/rtc/stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs)、[`crates/xbxengine/core/src/transport/webrtc/transport/core.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/core.rs)、[`crates/xbxengine/core/src/transport/webrtc/stack/session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs)、[`crates/xbxengine/core/src/transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs)、[`crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 的并行审计：确认新栈已具备更细粒度连接生命周期、ICE 去重/回放、DataChannel 协议拉起与故障恢复动作暴露；同时明确旧栈中 transport observation→recovery/bwe policy 的闭环尚未在 `transport/rtc` 重建，当前差距主要在策略层挂点与媒体恢复调度耦合，而非纯连接可达性。 |
| P-886 | 并行完成连接层“最后40%”：gather/trickle 完整化 + 生命周期重连 + 故障注入回归 | Done | 2026-03-20 | 2026-03-20 | 并行收口连接层可运营能力：A 路在 [`crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs)、[`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs)、[`crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 完整化 ICE gather/trickle 时序与候选去重/分类观测，覆盖乱序、重复、late trickle、end-of-candidates 收敛；B 路在 [`crates/xbxengine/core/src/transport/rtc/events.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/events.rs)、[`crates/xbxengine/core/src/transport/rtc/connection/control_channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/control_channel.rs)、[`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs)、[`crates/xbxengine/core/src/transport/rtc/stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 补齐连接生命周期状态机、重连触发、pump 错误恢复、DataChannel close/reopen 恢复与控制面 replay 持久化。主线程已完成整合验收：`cargo check -p xbxengine`、`cargo test -p xbxengine transport::rtc::connection::service::tests -- --nocapture`（11/11 通过）、`cargo test -p xbxengine transport::rtc::connection::control_channel::tests -- --nocapture`（7/7 通过）、`cargo test -p xbxengine transport::rtc::media::service::tests -- --nocapture`（5/5 通过）。 |
| P-885 | 连接层鲁棒性回归：修复 P0/P1 错误吞噬、WouldBlock 丢包与 ICE candidate 幂等 | Done | 2026-03-20 | 2026-03-20 | 在 [`crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 引入 `pending_writes` 队列，修复 `poll_write()` 后 `send_to(WouldBlock)` 的静默丢包（改为入队重试）；在 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 将 handshake/post-handshake/control bootstrap/replay 路径的 `let _ =` 全部改为显式错误传播，并让 `send_text_on_channel_id`/`send_binary_on_channel_id` 对 `io_runtime.pump()` 失败返回错误；同时补入 remote ICE candidate 幂等键与去重注入流程，避免 pending+inline 重复 candidate 触发重复注入错误。状态字段在 [`crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs) 增加 `remote/pending/applied` candidate key 集。新增回归：`add_remote_ice_candidates_deduplicates_when_remote_description_missing`、`apply_remote_description_deduplicates_pending_and_inline_candidates`。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine transport::rtc::connection::control_channel::tests -- --nocapture`、`cargo test -p xbxengine add_remote_ice_candidates_deduplicates_when_remote_description_missing -- --nocapture`、`cargo test -p xbxengine apply_remote_description_deduplicates_pending_and_inline_candidates -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`、`cargo test -p xbxengine apply_remote_description_accepts_real_rtc_answer -- --nocapture`。 |
| P-884 | 并行子代理交付后的主线程验收与回归稳定性补强 | Done | 2026-03-20 | 2026-03-20 | 对第一阶段“媒体 contract + input/chat 协议”并行交付做主线程收口：1) 复跑 [`crates/xbxengine/core/src/transport/rtc/media/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/service.rs) 与 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 关键回归；2) 在 `service_bootstraps_message_and_control_payloads_after_handshake_ack` 断言里补充 message payload 观测上下文，便于后续定位时序抖动；3) 完成 `cargo check -p xbxengine` 与关键用例复验。说明：连续高频重复执行该 rtc-to-rtc 用例时，沙箱偶发 `xbxEngineRtcIoBindFailed: Operation not permitted`，属于本地执行环境端口绑定限制，不是协议逻辑回归。 |
| P-883 | 第一阶段补齐 rtc input/chat 业务协议的最小 open/close/message 观测与发送 | Done | 2026-03-20 | 2026-03-20 | 在 [`crates/xbxengine/core/src/transport/rtc/connection/data_channel_bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/data_channel_bootstrap.rs) 补入 input 最小 metadata/bootstrap packet 构造；在 [`crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs) 增加 input/chat channel 开关与 bootstrap 状态；在 [`crates/xbxengine/core/src/transport/rtc/connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 接上 input/chat 的 open/close/message 分发、input metadata 最小发送、chat 文本摘要 catalog 与 runtime stats 观测，并把 `service_connects_to_raw_rtc_answer_peer_and_opens_control_channel` / `service_bootstraps_message_and_control_payloads_after_handshake_ack` 扩成本地 rtc-to-rtc 回归，覆盖 input metadata 发送、chat 文本 catalog、chat/input close 观测以及既有 message/control 握手与 replay。验证：`cargo fmt --all`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`。 |
| P-882 | 第一阶段收口 rtc 媒体层稳定 contract：identity/route/snapshot/RTCP port | Done | 2026-03-20 | 2026-03-20 | 在 [`transport/rtc/media`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media) 补齐稳定媒体 ingress contract：1) [`packet_types.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/packet_types.rs) 新增 `RtcMediaStreamIdentity`，把 `track_id + ssrc` 归一化进入口对象，并保留 `mid/rid` 占位容器；2) [`packet_router.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/packet_router.rs) 将路由从 RTP/RTCP 二分升级为 `primaryVideo / repairVideo / audio / unknown`，并输出可解释 rule/reason；3) [`runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/runtime_state.rs) 与 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/service.rs) 新增 route/identity 统计与 snapshot 字段，`latest_observation_summary` 也同步带出 route/identity 摘要；4) [`sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/sink.rs) 增加 RTCP 发送端口抽象与 noop 默认实现，第一阶段不接业务；5) 补齐 service 单测覆盖 primary/repair/audio/unknown 路由与 snapshot 观测。验证：`cargo test -p xbxengine transport::rtc::media::service::tests -- --nocapture`。 |
| P-881 | 第一阶段把 rtc 控制面真正接到 DataChannel 文本协议并收口媒体 ingress 边界 | Done | 2026-03-20 | 2026-03-20 | 继续收口 [`transport/rtc`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/mod.rs) 第一阶段：1) 在 [`connection/control_channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/control_channel.rs) 把控制状态机从“open 即 ready”改成真实协议语义，新增 `message_channel_open/post_handshake_messages_sent/control_bootstrapped`，只有 `HandshakeAck + control bootstrap` 后才视为 control ready，并保留 pending keyframe/decoder-reset replay；2) 在 [`connection/data_channel_bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/data_channel_bootstrap.rs) 收敛 message/control 所需 payload builder，把旧链路里的 Handshake、post-handshake message、authorization、gamepadChanged、keyframe、decoderReset 协议文本迁入新目录；3) 在 [`connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 接上真实 DataChannel 下行：`OnOpen(message)` 发送 handshake，收到 `HandshakeAck` 后发送 post-handshake message 并 bootstrap control channel，再把 `request_video_keyframe()/request_decoder_reset()` 和 pending replay 真正落到 `rtc.data_channel(id).send_text()`；同文件继续保留 `poll_read()` 对 RTP/RTCP/DataChannel 的 read ingress 归一化，并把 raw RTP/RTCP 送入媒体 ingress 队列；4) [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 保持“三层组合根”不变，但请求 keyframe/reset 时改走新的连接层发送路径，媒体包仍只进入第一阶段 `media` 入口、不进 decode/render。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::rtc::connection::control_channel::tests -- --nocapture`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`、`cargo test -p xbxengine service_bootstraps_message_and_control_payloads_after_handshake_ack -- --nocapture`、`cargo test -p xbxengine service_pump_observes_data_channel_message_from_poll_read -- --nocapture`、`cargo test -p xbxengine transport::rtc::media::service::tests -- --nocapture`、`cargo check -p xbxengine`。 |
| P-880 | 第一阶段接入机会式 Sans-I/O 泵循环并锁定本地 rtc 对端互通 | Done | 2026-03-20 | 2026-03-20 | 在 [`connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 和 [`connection/io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 继续收敛第一阶段真实连接层：1) `RtcIoRuntime::pump()` 补上最小 Sans-I/O 驱动，按 `poll_timeout -> handle_timeout -> poll_write -> handle_read` 推进本地 UDP socket，与 `stack.rs` 的 `local_candidates_snapshot()/local_ice_gathering_complete()/snapshot_runtime_stats()` 接上，保证上层常规轮询就能持续驱动协议壳；2) `create_raw_offer()`、`apply_remote_description()`、`add_remote_ice_candidates()` 在关键信令步骤后主动执行一次 `pump()`，不再只停留在静态 SDP/候选注入；3) 修正 `RemoteDescriptionApplied` 事件的 transport stats 语义，不再在远端 answer 应用时提前写成 `Connected`，而是等待真实 `OnConnectionStateChangeEvent(Connected)` 驱动状态收敛；4) 新增本地互通单测 `service_connects_to_raw_rtc_answer_peer_and_opens_control_channel`，用一个原生 `rtc` answer peer + 本地 UDP 对端验证当前 `RtcConnectionService` 能完成真实 offer/answer、ICE、连接建立，并把 `message/control` channel 拉到 ready，最终允许 `request_video_keyframe()` 走通。验证：`cargo fmt --all`、`cargo test -p xbxengine service_connects_to_raw_rtc_answer_peer_and_opens_control_channel -- --nocapture`、`cargo test -p xbxengine create_raw_offer_comes_from_real_rtc_peer_connection -- --nocapture`、`cargo test -p xbxengine apply_remote_description_accepts_real_rtc_answer -- --nocapture`、`cargo check -p xbxengine`。 |
| P-879 | 第一阶段连接层接入真实 `rtc` PeerConnection/DataChannel/SDP | Done | 2026-03-20 | 2026-03-20 | 在 [`connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 将 phase-1 占位逻辑替换为真实 `rtc 0.9.0` 连接对象：1) `rebuild()` 改为创建真实 `RTCPeerConnection`，按 `message/control/input/chat` 建立 DataChannel，并从 [`io_runtime.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs) 绑定本地 UDP 端口后生成 host candidate 注入 `rtc`，再用 end-of-candidates 事件推进 `local_ice_gathering_complete`；2) `create_raw_offer()` 改为走真实 `create_offer() + set_local_description()`，不再手拼静态 SDP；3) `apply_remote_description()` / `add_remote_ice_candidates()` 改为走真实 `RTCSessionDescription::answer()` 与 `add_remote_candidate()`，并在 [`runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/runtime_state.rs) 增加 pending remote candidate 与 data-channel label 索引，保证 remote description 前后的 trickle 路径可收敛；4) [`data_channel_bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/data_channel_bootstrap.rs) 从“直接置 ready”改为“只创建真实 channel 拓扑”，把控制面 ready 留给后续事件循环；5) 保持 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 的三层边界不变，只把 `rebuild_peer_connection()` 接到新的 connection 实现。当前阶段限制明确保留：尚未接入真实 Sans-I/O UDP 收发与 timeout 驱动，所以 ICE gather 仍是 phase-1 的受控本地 host candidate 注入，不代表已经具备真实联网。依赖层面新增 workspace `rtc = 0.9.0`。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine create_raw_offer_comes_from_real_rtc_peer_connection -- --nocapture`、`cargo test -p xbxengine apply_remote_description_accepts_real_rtc_answer -- --nocapture`、`cargo test -p xbxengine raw_packet_observation_updates_summary -- --nocapture`、`cargo test -p xbxengine local_offer_contains_phase1_tags -- --nocapture`。 |
| P-878 | 第一阶段直切主线：新增 `transport/rtc` 三层骨架并切换 active stack | Done | 2026-03-20 | 2026-03-20 | 按“连接层 / SDP 适配层 / 媒体层”要求在 [`transport/rtc`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/mod.rs) 新增 phase-1 骨架：1) `connection` 层新增 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) + `runtime_state/control_channel/io_runtime/data_channel_bootstrap`，先用占位 candidate 和控制面就绪状态打通协商/控制请求路径，并以 runtime stats 记录连接事件；2) `sdp` 层新增 [`offer_policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/offer_policy.rs)、[`answer_adapter.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/answer_adapter.rs)、[`candidate_adapter.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/candidate_adapter.rs)，明确本地 offer 适配、远端 answer/candidate 归一化边界；3) `media` 层新增 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/service.rs) 等文件，仅做 raw packet 观测与计数，不接 decode/render；4) 新增 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 复刻上层 `XbxMediaStackPort` 形状并组合三层服务；5) 主线切换到 rtc stack：[`backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/backend.rs) 由 `transport::webrtc::stack` 改为 `transport::rtc::stack`，[`transport/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/mod.rs) 增加 `pub mod rtc`。阶段约束保持：不引入 `TrackRemote/interceptor` 到新栈、不接媒体播放链。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine rebuild_injects_placeholder_local_candidate -- --nocapture`、`cargo test -p xbxengine raw_packet_observation_updates_summary -- --nocapture`、`cargo test -p xbxengine local_offer_contains_phase1_tags -- --nocapture`。 |
| P-877 | 评估 Rust 侧 WebRTC 切换到 rtc Sans-I/O 的可行性 | Done | 2026-03-20 | 2026-03-20 | 结论：`webrtc-rs/rtc` 在协议与 I/O 解耦层面能满足更细的控制诉求，但它不是 `webrtc` 的无痛替换；若迁移，需要重写 `transport/webrtc` 里的 PeerConnection、Track/Receiver、DataChannel、RTCP/stats 和事件循环接线，同时保留 `adapter/recovery/media` 上层 contract。当前更适合先做视频接收侧 PoC，再决定是否全面切换。 |
| P-876 | 修复 xHome 启动阶段卡在 Provisioning：force_region_ip 不再作用于 xHome token | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773995721838.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773995721838.jsonl) 回归：启动流程已走过 `creatingSession`，但会话长时间停留在 `waitingSessionReady + streamState=Provisioning`，且这次配置包含 `force_region_ip=4.2.2.2`。为避免把 xCloud 的区域强制策略误带到 xHome，本轮在 [`flow.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-auth-flow/src/flow.rs) 调整 streaming token 构建：`xhome` 的 `get_streaming_token` 固定传空 `force_region_ip`，仅保留 xCloud 继续使用区域偏好。验证：`cargo check -p xbox-auth-flow`。 |
| P-873 | 放宽 startup + cloud 的首洞 deadline 与 packet-level NACK 策略 | Done | 2026-03-20 | 2026-03-20 | 按 cloud 云游戏基线 RTT≈214ms 的实际观测，继续收紧 `startup + cloud` 这条链的恢复口径：1) [`adapter/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 新增 `CLOUD_STARTUP_HEAD_HOLE_DEADLINE_FLOOR_MS=320ms`，并在 `maybe_run_nack_maintenance()` / `observe_forward_gap_and_nack()` 中对 cloud startup 的 head-hole deadline 先做 floor，再进入 packet-level NACK 调度；同时把 `sampleLoss / rtpGap / rtpWindow` 的 `max_age / retry_interval / max_tracked_sequences` 再向 startup cloud 倾斜，尤其只保留更前部的缺口，避免再拖出大段尾部序号；2) [`adapter/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/tests.rs) 新增 `cloud_startup_head_hole_deadline_floor_only_applies_in_startup`、`cloud_startup_sample_loss_policy_is_wider_than_cloud_steady_and_home`、`cloud_startup_rtp_gap_and_window_policies_widen_head_hole_budget`，分别锁定头洞 floor、startup/cloud/home 三档的 NACK 宽度梯度；3) [`compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) 保持 RustOwned+Cloud 的 cloud floor 方案不变，继续将顶层 cloud baseline 维持在宽档，不再把 cloud 回退到 home/低延迟档。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::adapter::tests -- --nocapture`、`cargo test -p xbox-streaming policy::runtime::compiler::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc -p xbox-streaming`。 |
| P-871 | 参考浏览器云游戏 `RTT≈214ms` 重调 Rust-owned cloud 策略 | Done | 2026-03-20 | 2026-03-20 | 依据“同机同游戏官方云游戏 `RTT=214ms`、`nackCount=8153` 仍可工作”的新基线，重新收敛 Rust-owned 的 cloud 恢复策略，不再让 adapter 的 packet-level policy 把顶层 cloud baseline 压回 low-latency 档。改动分三层：1) [`compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) 将 `RustOwned + Cloud` 的 `video_pipeline` 从此前“对齐浏览器档”进一步上调为 cloud floor：`nack_window_ms=700`、`nack_burst_count=16`、`nack_max_age_ms=420`、`nack_retry_interval_ms=90`、`nack_max_retry_count=6`、`jitter_buffer_max_delay_ms=48`、`idle_timeout_ms=260`、`late_frame_drop_threshold_ms=900`，保留 sidecar recovery 不变；2) [`adapter/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 新增 cloud-aware packet policy：通过 `session_target_type=Cloud` 或 `transport_policy_profile=cloudGaming` 识别 cloud，使用 `RTT floor = max(observed_rtt, 214ms)` 参与 `dynamic_repair_deadline()`，同时把 `sampleLoss/rtpGap/rtpWindow` 的 `max_age/retry_interval/max_tracked_sequences/burst_count` 放宽到 cloud 专属档，并让 sample/range 收集窗口更大；3) [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 将 cloud 的最近 NACK outcome 窗口从 `180ms` 放宽到 `520ms`，并取消 cloud 对 delta `expiredDeadline` 的过早 `TransportAwaitRecoveryKeyframe` 升级，改为先继续 `CooldownSuppressed`。补了单测：[`adapter/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/tests.rs) 新增 cloud-vs-local policy 对比；原有 [`compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) cloud test 也同步更新断言。验证：`cargo test -p xbox-streaming rust_owned_cloud_aligns_video_pipeline_but_keeps_sidecar_recovery_profile -- --nocapture`、`cargo test -p xbxengine transport::adapter::tests -- --nocapture`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc -p xbox-streaming`。 |
| P-870 | 回归 `pendingInconsistent` 分支后的新 trace，确认 reader/queue 当前真实状态 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773991497310.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773991497310.jsonl) 对 `P-869` 做只读回归。结论比前两轮更具体：1) `RTX-only` 协商仍正常，远端继续接受 `apt=124` 和 `ssrc-group:FID 1167950134 154943359`；2) `queued -> primaryReadPoll -> adapterRead` 这条链这次**至少成功走通了一次**，具体是 `seq 171` 出现 `queued primary packet ... seq=24888`，随后 `seq 178/179` 有 adapter 的 `reinjectReadPoll`，`seq 180` 有 `primaryReadPoll`，而 `seq 185/196/197/203/206` 的 `runtimeSummary/latestDecisionSummary` 已明确显示 `reinject:stage=adapterRead seq=24888 native=None pending=0 ...`，说明这次并非“永远出不来”，主 reader 确实成功把一张 reinject 包交给了 adapter；3) 同时整份 trace 中没有任何 `pendingInconsistent`，说明在当前实现下不存在“pending>0 但 pop 永远返回空”的索引错乱；4) 但会话后半段又退回到 `reinject:stage=queued seq=26049 ... pending=30` 且持续 `transportExpiredDeadline`，同时关键 NACK 仍固定在 `25931..25931`，而没有新的 `adapterRead/adapterResolved` 针对首洞出现。这说明当前问题已不再是 reader/queue 一定坏掉，而是 RTX reinject 只在早期偶发成功，进入恢复期后仍然主要拿到错位包，无法对关键首洞形成持续 coverage。此次仅完成 trace 归档，没有改代码，也没有跑测试。 |
| P-869 | 在主流 reader 中显式区分“pending 可 pop”与“pending 状态不一致” | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773991108358.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773991108358.jsonl) 的回归，已确认 adapter 仍在持续 `reinjectReadPoll`、`PrimaryStreamRtpReader` 也明确进入了 `primaryReadPoll`，但仍然既没有 `deliveredPrimary`、也没有 `innerReadPrimary`，说明此前 reader 分支仍留下了“pending>0 但到底是 queue 真空还是索引状态不一致”这一模糊空间。本轮直接在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs) 拆死分支：当 `pending_queue_len > 0` 时，`PrimaryStreamRtpReader.read()` 会先发布 `primaryReadPoll`，随后立即尝试 `pop_pending_packet()`；若成功则继续走 `deliveredPrimary`，若失败则直接发布新的 `pendingInconsistent` 观测并输出 `[xbxengine][rtx-reinject] pendingInconsistent ...` runtime log，然后才允许退回 `inner.read()`。这样下一份 trace 将不再存在“看到了 `primaryReadPoll`，但无法判断是队列真空还是状态错乱”的空白。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-868 | 回归 stale-pending 清理改造后的新 trace，确认主 reader 与队列消费现状 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773991108358.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773991108358.jsonl) 对 `P-867` 做只读回归。结论比前一轮更明确：1) `RTX-only` 协商仍正常，`offerSdpCapabilities` / `answerSdpCapabilities` 继续只包含 `video/rtx`，远端返回 `apt=124` 与 `ssrc-group:FID 472406270 2577570275`；2) adapter 侧明确还在持续 poll，`seq 174/175` 已出现 `[WebrtcVideoAdapter] reinjectReadPoll ... pending=1 ...`；3) `PrimaryStreamRtpReader` 也确实进入了，`seq 176` 已出现 `[xbxengine][rtx-reinject] primaryReadPoll primary_ssrc=472406270 pending=1 ...`；4) 但这次从头到尾都没有再出现 `innerReadPrimary`、`deliveredPrimary`、`adapterRead`、`sampleBuilderPush`、`adapterResolved` 或 `adapterResolveMiss`，同时关键首洞 `7182` 在 `seq 244` 被 `queued ... match=true gap=Some(7182) nack=Some(7182)..Some(7182)` 命中过一次，之后仍持续 `transportExpiredDeadline`。这说明当前 stale-pending 清理虽然修掉了“native 同 seq 覆盖后 pending 仍残留”的实现缺口，但新的事实是：主 reader 进入后仍然没有把 queued 的首洞命中包交付给上层；问题现已收敛到 `PrimaryStreamRtpReader.read()` 在 `pending>0` 时没有走出 `deliveredPrimary` 这一跳。此次仅做 trace 归档，没有改代码，也没有跑测试。 |
| P-867 | 修复 native 主流已覆盖同 seq 时 reinject 队列仍堆积的问题 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773989633401.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773989633401.jsonl) 的硬日志，已确认主流 reader 确实进入了 [`PrimaryStreamRtpReader`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs)，但在 `pending=1` 时仍发生 `innerReadPrimary native_seq=2659`，说明至少存在“原生主流已读到同 seq 包，而 reinject queue 仍未清理”的实现缺口。本轮直接在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs) 修复两点：1) `pop_pending_packet()` 现在会跳过已经被原生主流覆盖、其 key 已进入 `delivered_packet_keys` 的 stale pending reinject 包，避免这些包继续阻塞前部队列；2) `mark_primary_packet_seen()` 在 native 主流读到某个 `packet_key` 时，会同步从 `pending_packets` 中删除同 key 的 queued reinject 包，而不是只从 `pending_packet_keys` 里移除，避免队列元素与索引状态脱节导致“pending>0 但永远吐不出来”。新增 helper `remove_pending_packet_by_key()` 专门负责从 `VecDeque` 清理同 key 的 stale reinject。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-866 | 为 adapter poll 与 primary reader 入口补 runtimeLog 级硬证据，避免再依赖 summary 推断 | Done | 2026-03-20 | 2026-03-20 | 基于 `P-865` 的结果，确认仅靠 summary/host 轮询无法证明 `PrimaryStreamRtpReader` 是否真正被调用，因此这轮改成直接补 runtimeLog 级硬证据，而不是继续扩 summary 字段。改动在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs)、[`adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs)、[`adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs)：1) `PrimaryStreamState` 增加 `primary_read_poll_count` 与 `inner_read_count`；当 pending reinject queue 非空时，`PrimaryStreamRtpReader.read()` 会直接输出 `[xbxengine][rtx-reinject] primaryReadPoll ...` 和在 fallback 到主流 inner reader 时输出 `[xbxengine][rtx-reinject] innerReadPrimary ...`，并按首条/2 次幂节流；2) adapter 在 `latest_video_rtx_reinject_observation.stage == "queued"` 且 `pending_queue_len > 0` 时，也会直接输出 `[WebrtcVideoAdapter] reinjectReadPoll ... track_ssrc=... track_tid=... pending=... timeout_ms=...`，证明 queue 建立后 adapter 侧是否仍然持续调用 `track.read_rtp()`。这样下一份 trace 不再需要依赖 summary 推测，而能直接判断“adapter 有没有继续 poll”、“primary reader 有没有真正进入”、“进入后是否直接 fallback 到 inner read”。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-865 | 基于补全后的全链路 reinject 观测回归新 trace，确认是否进入主流 reader | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773988417468.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773988417468.jsonl) 对 `P-864` 的整链观测进行只读回归，结论是：`PrimaryStreamRtpReader` 在关键窗口内仍然没有把 reinject 包交给主链，而且更关键的是，新补的 `primaryReadPoll / deliveredPrimary / innerReadPrimary / adapterRead / sampleBuilderPush / adapterResolved / adapterResolveMiss` 七个 stage 在整份 trace 中**完全没有出现**。trace 只出现了旧有的 `queued primary packet` 和持续的 `transportExpiredDeadline`。协商仍是正确的 `RTX-only`：`offerSdpCapabilities` 与 `answerSdpCapabilities` 都只包含 `video/rtx`，远端继续返回 `apt=124` 与 `ssrc-group:FID 3030908368 418227426`。但在这次样本里，关键首洞是 `25922`，所有可见 reinject 序号都是 `26015/26017/26021/...`，没有命中首洞，因此没有 `headMatch=true` 样本；与此同时，从 `seq 163` 起 `transportExpiredDeadline` 持续刷到 `seq 245`。更重要的是，由于新增的 primary-reader 链路 stage 一条都没出现，这说明当前运行时里要么主流 reader 在 queue 建立后没有再被调用，要么实际媒体读取没有经过我们预期的 `PrimaryStreamRtpReader` 实例；至少可以确定，不存在“已经进入主流 reader 但在 deliveredPrimary 之后才丢失”的情况。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-864 | 一次性补全 RTX reinject 全链路观测，明确卡在哪一跳 | Done | 2026-03-20 | 2026-03-20 | 针对“不要再猜，直接把主流是否拿到 RTX 解出来的包一次性验证清楚”的要求，本轮把 reinject 观测扩成完整链路：`queued -> primaryReadPoll -> deliveredPrimary -> innerReadPrimary -> adapterRead -> sampleBuilderPush -> adapterResolved / adapterResolveMiss`。改动在 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs)、[`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs)、[`adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs)、[`observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs)、[`stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs)：1) `XbxEngineVideoRtxReinjectObservation` 新增 `pending_queue_len` 与 `native_sequence_number`，用于同时看 pending 队列深度和 inner 主流读链返回的原始 seq；2) `PrimaryStreamRtpReader.read()` 在 pending queue 非空时会先发布 `primaryReadPoll`，若成功从 pending queue 出队则发布 `deliveredPrimary`，若仍走了主流 inner reader 则发布 `innerReadPrimary`，从而一次性回答“主 reader 是否被调用、是否看到了 pending、有没有真实从 pending pop、是否退回主流读链”；3) adapter 侧 `adapterRead / sampleBuilderPush / adapterResolved / adapterResolveMiss` 继续透传这些附加字段，不再丢上下文；4) reinject 命中统计改成只对 `stage=queued` 累计，避免同一包在后续 stage 重复记命中率。验证：`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-863 | 基于“首洞命中时优先 drain reinject”改造回归新 trace 效果 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773987735969.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773987735969.jsonl) 对 `P-862` 做只读回归，结论是：优先 drain 还没有把关键首洞从 `queued` 推进到 `deliveredPrimary`。协商侧保持正确的 `RTX-only` contract：`offerSdpCapabilities` 与 `answerSdpCapabilities` 都只包含 `video/rtx`，不含 `RED/ULPFEC`，并且远端继续返回 `apt=124` 与 `ssrc-group:FID 4235053009 764907722`。关键首洞这次是 `23142`，`seq 201` 已经明确出现 `queued primary packet ... seq=23142 match=true gap=Some(23142) nack=Some(23142)..Some(23142)`，说明 sender 仍然给了关键首洞的 RTX coverage；但整份 trace 仍然只出现 `queued primary packet`，完全没有新的 `deliveredPrimary / adapterRead / sampleBuilderPush / adapterResolved`，同时 `transportExpiredDeadline` 从 `seq 199` 持续刷到 `seq 238`。这说明问题依旧卡在 `PrimaryStreamRtpReader` 出队之前，单纯把 adapter 侧 `track.read_rtp()` timeout 从 50ms 缩到 8ms，并不能迫使主 reader 消费 pending reinject queue。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-862 | 解决命中首洞的 reinject 包长期停留在 queued、无法进入主 reader 的出队问题 | Done | 2026-03-20 | 2026-03-20 | 结合 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs)、[`adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 与 `webrtc-rs` 的 [`TrackRemote::read_rtp()`](/Users/guo.xu/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/webrtc-0.17.1/src/track/track_remote/mod.rs#L272) 读链复核后，确认代码路径上 “PrimaryStreamRtpReader -> TrackRemote -> WebrtcVideoAdapter” 本身是连通的；当前更像是命中首洞的 queued reinject 包没有得到及时出队机会。为此在 [`adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 新增 `should_prioritize_reinject_drain()`：当 `latest_video_rtx_reinject_observation.stage == "queued"` 且 `matched_head_gap == true` 时，下一次 `track.read_rtp()` 不再走默认 50ms timeout，而是切到一个很短的优先 drain 窗口（8ms），强制主 reader 优先尝试把该 reinject 包吐出。这样做不改整体线程模型，只在“首洞已命中但仍卡在 queued”这一窄条件下加速出队，避免普通读路径一直错过最关键的 reinject 包。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-861 | 基于固定启用 RTX 协商与 reinject 的新 trace 回归实际效果 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773986578266.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773986578266.jsonl) 对“去掉环境变量、固定启用最小 RTX 路径”做只读回归。结论：1) 固定协商已生效，`exchangeOfferRequested` 的 `offerSdpCapabilities` 只带 `video/rtx`，不再带 `RED/ULPFEC`；`exchangeOfferResult` 的 `answerSdpCapabilities` 也只接受 `RTX`，并继续给出 `apt=124` 与 `ssrc-group:FID 2557715449 585063830`。2) repair stream 与 reinject 仍然正常工作，`seq 646` 明确出现 `queued primary packet ... seq=7095 match=true gap=Some(7095) nack=Some(7095)..Some(7095)`，说明首洞 `7095` 这次至少被 sender/repair 覆盖过一次。3) 但主链仍然持续刷 `transportExpiredDeadline`，从 `seq 644` 起一直延续到 `seq 680`；同时新的 stage 观测仍未出现 `deliveredPrimary / adapterRead / sampleBuilderPush / adapterResolved`，说明当前关键断点依旧在 reinject queue 之后，尚未证明命中的 `7095` 真正回流到 adapter 并闭洞。换句话说，收窄为 RTX-only 后，确实排除了 `RED/ULPFEC` 干扰，也证实 sender 至少给过一次关键首洞的 RTX；但最关键的问题没有变，仍是“命中的首洞没有进入主链并转化成 gap closure”。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-860 | 去掉 repair probe / RTX reinject 运行时开关，固定启用最小 RTX 路径 | Done | 2026-03-20 | 2026-03-20 | 按当前网络层收敛结论，不再保留 `XBXENGINE_EXPERIMENTAL_VIDEO_REPAIR_PROBE` 和 `XBXENGINE_EXPERIMENTAL_VIDEO_RTX_REINJECT` 运行时开关，而是把最小可用 RTX 路径直接固化进主线。改动在 [`transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs) 和 [`transport/core.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/core.rs)：1) offer video codec preference 固定追加 `video/rtx (PT 97, apt=124)`，不再依赖环境变量，也不再协商 `RED/ULPFEC`；2) interceptor registry 固定挂载 `RepairProbeInterceptorBuilder` 和 `RtxReinjectInterceptorBuilder`，保证 Rust-owned 主线始终带上 repair stream 观测与 RTX unwrap/reinject；3) 清理掉此前为 env toggle 准备的 mode / 解析逻辑，并将单测改成验证“始终带 RTX、始终不带 RED/ULPFEC”的固定 contract。验证：`cargo test -p xbxengine transport::webrtc::transport::setup::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-859 | 将 repair probe 默认收窄为仅协商 RTX，并保留 full 模式回归入口 | Done | 2026-03-20 | 2026-03-20 | 基于前面对多份 trace 的回归，当前更需要先隔离“RED/ULPFEC 是否干扰 repair coverage 判断”，而不是继续同时协商全部 repair codec。本轮仅修改 [`transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs)：将 `build_offer_video_codec_preferences()` 从布尔开关改成 `VideoRepairProbeMode`，新增 `Disabled / RtxOnly / Full` 三档；当 `XBXENGINE_EXPERIMENTAL_VIDEO_REPAIR_PROBE=1` 时，默认进入 `RtxOnly`，只在 offer 里追加 `video/rtx (PT 97, apt=124)`；若额外设置 `XBXENGINE_EXPERIMENTAL_VIDEO_REPAIR_PROBE_MODE=full`，才继续追加 `video/red (PT 122)` 和 `video/ulpfec (PT 116)`，以保留完整 repair codec 回归能力。这样当前实验默认只协商 RTX，能先排除 RED/ULPFEC 对 sender/repair coverage 与主链恢复判断的干扰，同时不影响已实现的 RTX probe / reinject 路径。测试同步更新并新增“默认 RTX-only / full 模式仍带齐全部 repair codec”两条用例。验证：`cargo test -p xbxengine transport::webrtc::transport::setup::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-858 | 基于新 trace 回归“首洞命中/范围命中分离 + 首洞优先 NACK”改造效果 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773985091026.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773985091026.jsonl) 做只读回归，确认 `P-857` 的两项改造已经产生预期方向但还没有解决根因。积极变化是：当前 pending NACK 范围已明显收窄，不再像之前那样拖出几千个序号，而是稳定在 `gap=Some(32153)`、`nack=Some(32153)..Some(32164)` 这类约 12 个序号的首洞优先窗口；说明 scheduler 的“只追前部缺口”已经生效。消极事实是：这份 trace 里所有 `queued primary packet` 仍然都是 `match=false`，如 `seq 151-157/161/167/169/172/176/183/192/212`，对应原始 seq 从 `32178` 一路到 `60179`，没有一个命中 `gap=32153`，因此 `headMatch` 实际仍为 0，`transportExpiredDeadline` 也没有明显收敛，仍从 `seq 148` 起持续刷屏。换句话说，这轮改造已经把诊断噪音从“宽范围误命中”压掉，但也更明确地证明：当前 sender/repair coverage 对关键首洞 `32153` 仍然完全没有覆盖，问题主因不在本地 NACK 范围过宽，而在 repair stream 给到的可见原始序号集合与关键首洞脱节。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-857 | 分离 reinject 的“首洞命中/范围命中”口径，并实现首洞优先、收窄 NACK 目标 | Done | 2026-03-20 | 2026-03-20 | 直接按当前 `webrtc-rs` 主线把恢复口径与调度一起收紧，而不是继续只加观测。1) 在 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs)、[`observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs)、[`stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 将 reinject 观测从单一 `match` 拆成 `matched_head_gap` 与 `matched_nack_range` 两个维度，并把统计总量拆成 `headHits / rangeHits / miss`；`runtimeSummary/latestDecisionSummary` 现在会明确显示 `headMatch` 与 `rangeMatch`，避免再把“命中 pending range 中某个后续 seq”误判成“命中首洞”。2) 在 [`transport/webrtc/nack_scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/nack_scheduler.rs) 为 `NackObservePolicy` 增加 `max_tracked_sequences`，`observe_missing_sequences_with_policy()` 只会把缺口前面的少量序号挂进 pending；同时在 [`transport/adapter/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 把 `collect_recent_missing_sequences()` 和 `collect_missing_sequences_for_sample()` 从“保留尾部缺口”改为“保留前部缺口”，并为 `sampleLoss / rtpWindow / rtpGap` 分别给出更小的首洞优先追踪窗口（例如 delta 仅追前 4 个，reference 追前 6-8 个，keyframe 追前 10-12 个）。这样恢复链路会优先围绕 `latest_video_packet_gap.expected_sequence` 这类首洞，而不是把整段宽 NACK range 都拖进 pending。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-856 | 基于新 trace 回归 `sampleBuilderPush / adapterResolveMiss / adapterResolved` 观测 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773983372606.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773983372606.jsonl) 做只读回归，结论进一步收敛：当前问题并没有推进到 `sampleBuilderPush` 或 `adapterResolveMiss` 这一步，而是又退回到了“命中后的后续 reinject 大多数仍错位”的形态。trace 中只看到大量 `queued primary packet`，包括 `seq 189` 明确记录一次 `queued primary packet ... seq=24129 match=true gap=Some(20968) nack=Some(20968)..Some(24167)`，但之后没有看到任何 `sampleBuilderPush`、`adapterResolveMiss` 或 `adapterResolved` 阶段摘要；同时从 `seq 139` 起持续刷 `transportExpiredDeadline`。这说明新的关键 pending 区间是 `20968..24167`，并且虽然有一次 reinject 被标为 `match=true`，但它命中的只是当前 pending NACK 范围，而不是 `gap=Some(20968)` 这一关键首洞本身；后续绝大多数 reinject 仍是更高序号的错位包，因此系统甚至没有进入“关键命中后还 resolve miss”的诊断阶段。换句话说，当前主要问题仍是 repair coverage 与 pending hole 的错位，而不是 sample builder 或 resolve_sequence 的后半段逻辑。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-855 | 继续细化 reinject 在 adapter 侧的后半段观测，区分 `sampleBuilderPush` 与 `adapterResolveMiss` | Done | 2026-03-20 | 2026-03-20 | 在 fallback 打通 `adapterRead` 后，继续沿现有 `webrtc-rs -> adapter` 主线把最后半步拆开，而不改恢复策略。本轮只改 [`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs)：当读取到最近一次 `deliveredPrimary` 对应的 reinject 包时，仍先记录 `stage=adapterRead`；随后在调用 `sample_builder.push()` 之前，若该包仍是当前 reinject 目标，则追加记录 `stage=sampleBuilderPush`；接着在 `nack_scheduler.resolve_sequence(seq, now_ms)` 后按结果分流，成功时记录 `stage=adapterResolved`，失败时显式记录 `stage=adapterResolveMiss`。这样下一轮 trace 就能直接判断关键包是“已经送进 sample builder 但 resolve miss”，还是“连 sample builder 都没走到”。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-854 | 基于 fallback 后新 trace 回归 RTX reinject 是否已进入 adapter 主链 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773983043663.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773983043663.jsonl) 做只读回归，结论是 fallback 路由已经生效：这次不再只有 `queued`，而是明确在 `seq 188` 起的 `directGamingState.runtimeSummary/latestDecisionSummary` 中出现 `reinject:stage=adapterRead seq=10295 ...`，说明 reinject 包已经穿过 `deliveredPrimary` 并真正进入 adapter 主链；同时此前的 “只有 queued、没有任何消费” 断点已经被打通。但新的关键 pending gap 变成了 `10625`，并且后续 `seq 1104` 明确出现了一次 `queued primary packet ... seq=10625 match=true gap=Some(10625) nack=Some(10625)..Some(10625)`，随后 summary 仍长期停在 `reinject:stage=queued seq=10886 match=false gap=None nack=Some(10625)..Some(10625) hits=1 miss=107 hitRate=0.009`，且持续刷 `transportExpiredDeadline`。这说明当前问题已从“进不进主链”收敛为“即便有一次命中关键洞 10625，后续也没有形成 `adapterResolved`，而且多数 reinject 包仍与当前 pending gap 错位”。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-853 | 为 RTX reinject PoC 增加单主视频流 primary SSRC fallback，验证是否是队列映射错位 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773980589089.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773980589089.jsonl) 的回归结果继续推进：由于 trace 里已经出现大量 `queued primary packet`，但没有任何 `deliveredPrimary/adapterRead/adapterResolved`，且同一时间主链仍在持续 decode，说明当前更像是 reinject 包被放进了无人消费的 primary queue，而不是简单的读链饿死。于是本轮在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs) 给 `enqueue_pending_packet()` 增加一个受控 fallback：若 answer/FID 推导出的 `primary_ssrc` 在当前 `primary_streams` 中不存在，但系统里只绑定了唯一一条主视频流，则把 reinject 包退化路由到这条唯一主流队列。这样可以验证“当前是否只是 `primary_ssrc` 映射错位，导致包一直堆在无人消费的队列里”。该 fallback 仍是 PoC 范围内的单视频流收口，不改变多流语义。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-852 | 基于新 trace 回归 RTX reinject 四阶段观测，确认最后一跳断点 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773980589089.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773980589089.jsonl) 做只读回归，核心结论比上一轮更收敛：当前 RTX reinject 的断点已经明确落在 `queued -> deliveredPrimary` 之间，而不是更后面的 adapter 侧。整份 trace 里只出现了大量 [`[xbxengine][rtx-reinject] queued primary packet ...`] 日志，没有任何 `deliveredPrimary`、`adapterRead` 或 `adapterResolved` 阶段观测；与此同时关键 pending gap 稳定卡在 `gap=Some(4339)`、`nack=Some(4339)..Some(4872)`，并持续伴随 `Transport escalation hint: transportExpiredDeadline`。典型样本如 `seq 211-217/221/226/228/231/234/240/251`，reinject 已经把 `4906/4908/4913/.../18207` 等原始序号排进主视频共享队列，但都 `match=false`，且后续没有任何“主 reader 实际吐出该 reinject 包”的证据；这说明当前问题不再是“adapter 读到了但没有 resolve”，而是 reinject 队列被持续填充却没有被主视频 reader 消费，或者被正常主流流量长期饿死。结合同一时段仍有大量 `[XbxDecodeActor] processing frame ...`，可以初步判断主链并未完全停摆，而是主流 reader 始终优先从原始主流连续读取，没机会回头消费 pending reinject queue。此次仅完成 trace 归档分析，没有改代码，也未跑测试。 |
| P-851 | 补齐 RTX reinject 最后一跳观测，区分 `queued -> deliveredPrimary -> adapterRead -> adapterResolved` | Done | 2026-03-20 | 2026-03-20 | 为了定位“关键缺包明明被 RTX 命中过一次，为什么主链仍未闭洞”，继续沿当前 `webrtc-rs` 主线补最后一跳观测，而不改恢复策略本身。此次在 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs) 为 `XbxEngineVideoRtxReinjectObservation` 增加 `stage` 字段；[`observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs) 与 [`stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 同步把摘要格式扩成 `reinject:stage=... seq=... match=... gap=... nack=...`。核心逻辑在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs)：1) reinject 入队时继续记录 `stage=queued`；2) 主视频 reader 从待回灌队列弹出包时记录 `stage=deliveredPrimary`，并在该时刻重新抓取最新 `gap/nack` 上下文；3) [`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 在 `track.read_rtp()` 返回且序号命中最近一次 `deliveredPrimary` 时记录 `stage=adapterRead`；4) 同一路径上若 `nack_scheduler.resolve_sequence(seq, now_ms)` 成功，则进一步记录 `stage=adapterResolved`。这样下一轮 trace 可以直接判断：某个 reinject 包是卡在主视频 reader 前、adapter 前，还是已经进了 adapter 但仍未 resolve。验证：`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`。 |
| P-850 | 基于新 trace 回归 `reinject hit/miss`，确认关键缺包是否被 RTX 回灌命中 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773977802679.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773977802679.jsonl) 做只读回归，核心结论已经比上一轮明确：当前失败链里的关键缺包不是之前的 `18894`，而是新的 `expectedSequence=705`，并且新补的 `reinject hit/miss` 观测明确证明 RTX 回灌没有命中这个洞。trace 的 `runtimeSummary/latestDecisionSummary` 在 `seq 490-516` 持续稳定显示 `reinject:seq=997 match=false gap=None nack=Some(705)..Some(705) hits=1 miss=230 hitRate=0.004`，同时主链连续刷 `packetGapDetected expectedSequence=705 receivedSequence=706`、`nackExpired action=expiredDeadline firstSequence=705 lastSequence=705`、`videoEscalation action=cooldownSuppressed reason=transportExpiredDeadline`。后续 reinject 日志也继续证明回灌的是别的包而不是关键洞：例如 `seq 519` 为 `seq=1248 match=false gap=Some(705) nack=Some(705)..Some(705)`，`seq 521` 为 `seq=1861 match=false gap=Some(705) nack=Some(705)..Some(705)`。这说明当前 PoC 不是“命中了 705 但来晚了”，而是更直接的“当前被 unwrap/reinject 的 RTX 包大多数根本不是 pending gap 705 对应的包”；因此现阶段 `transportExpiredDeadline` 的直接原因不是 RTT 太大，而是 sender/repair 流没有给出我们正缺的那个序号，或者我们看到的 repair 包与当前 pending gap 已经完全错位。此次仅完成 trace 归档分析，未改代码，也未跑测试。 |
| P-849 | 为 RTX reinject PoC 补充 `hit/miss` 观测，定位关键缺包是否被回灌命中 | Done | 2026-03-20 | 2026-03-20 | 在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs) 接入 [`RuntimeStatsSink`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs)，每次 `RTX unwrap -> reinject` 时记录原始 `sequence_number / rtp_timestamp`，并与当前 `latest_video_packet_gap.expected_sequence` 及 `latest_video_nack_observation.first_sequence..last_sequence` 做轻量匹配，产出 `matched_pending_gap / matched_gap_sequence / matched_nack_first/last_sequence`。同时在 [`backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs)、[`observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs)、[`stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 扩展 `VideoRtxReinject` 观测与 `reinject:seq=... match=... gap=... nack=... hits=... miss=... hitRate=...` 摘要，便于直接在 runtime trace / summary 判断 `18894` 这类关键洞是否被回灌命中。验证：`cargo fmt --all`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-847 | 基于新 trace 回归 RTX reinject PoC 是否已真正作用于主视频流 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773976013813.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773976013813.jsonl) 做只读回归，结论分两层：1) 受控 PoC 已经证明“回灌动作真实发生”，不是只停留在理论上。trace 中既有 repair codec 协商成功，也有 [`rtx-reinject`] 日志明确出现 `queued primary packet primary_ssrc=997806192 repair_ssrc=418003162 primary_pt=124 repair_pt=97 count=16/32/64`，说明 interceptor 已按 answer SDP 的 `apt=124 + ssrc-group:FID 997806192 418003162` 建立映射，并把 unwrap 后的 primary RTP 放回了主视频 reader 的共享队列；2) 但从主链结果看，这轮仍然没有证明 reinject 已经把关键缺包真正补回来。就在 `queued primary packet` 日志附近，trace 仍连续出现针对同一 `expectedSequence=18894` 的 `packetGapDetected -> nackExpired(expiredDeadline,keyframe) -> cooldownSuppressed`，`runtime_summary` 里的 repair-active 统计也仍是 `rec=22 exp=15000+ gaps=15000+ hit=0.002`，说明当前回灌虽然发生了，但至少对这批导致 `transportExpiredDeadline` 的关键包没有形成可见收益。综合判断：PoC 已经跨过“能不能把 RTX unwrap 后送回主流”的门槛，但还没跨过“送回的正是缺的包、而且时间上来得及被 sample builder 吃到”的门槛。下一步应优先给 reinject 增加更细的命中观测，直接记录 reinject 包的 sequence 是否命中当前 pending gap，以及 reinject 后是否出现对应 `NackRecovered` / gap closure，而不是继续只看汇总 hit rate。此次仅完成 trace 对照归档，未改代码，也未跑测试。 |
| P-846 | 实现受控的 RTX unwrap -> reinject PoC 以验证 repair stream 是否能真正作用于主视频流 | Done | 2026-03-20 | 2026-03-20 | 在 [`rtx_reinject_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/rtx_reinject_interceptor.rs) 新增一个受控 PoC：仅在环境变量 `XBXENGINE_EXPERIMENTAL_VIDEO_RTX_REINJECT=1` 开启时挂载 interceptor，通过 remote answer SDP 的 `apt` 与 `ssrc-group:FID` 解析出 `repair_ssrc -> primary_ssrc / primary_pt` 映射；repair stream 侧对 RTX 包做 unwrap，并把重建后的 primary RTP 入队，primary H264 stream reader 再优先消费这些 reinject 包并做最近窗口去重，尽量不改现有 `TrackRemote -> adapter -> sample builder` 主链。与此同时在 [`core.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/core.rs) 给 transport state 增加 remote answer SDP 共享存储，并在 `apply_remote_description()` 时更新给 interceptor 使用。当前 PoC 明确只支持 `RTX`，不尝试接入 `RED/ULPFEC`，也不把 `StreamInfo.mime/payload_type` 当作 source-of-truth，而是依赖 `associated_stream + answer SDP apt/FID` 做校准。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::webrtc::transport::rtx_reinject_interceptor::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-845 | 评估在当前 webrtc-rs 主线下实现 RTX unwrap -> reinject 的可行性 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773974251268.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773974251268.jsonl)、本地 [`webrtc-0.17.1/src/rtp_transceiver/rtp_receiver/mod.rs`](/Users/guo.xu/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/webrtc-0.17.1/src/rtp_transceiver/rtp_receiver/mod.rs) 与当前 [`repair_probe_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/repair_probe_interceptor.rs)、[`setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs)、[`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 做实现级核对。结论：对 `RTX` 而言，这条路线具备 PoC 可行性，因为 repair stream 已能稳定流经 interceptor 链，且 answer SDP 持续给出 `apt=124` 与 `ssrc-group:FID primary repair`，可以建立 `repair_ssrc -> primary_ssrc / primary_pt` 映射；同时 `webrtc-rs` 当前对 `repair_stream` 只做内部读取用于 TWCC，并未把恢复结果显式暴露给主链，因此我们可以在 interceptor 中对 repair stream 做 unwrap，并把重建后的 primary RTP 回灌到主视频 reader 的共享队列，再由现有 `TrackRemote -> adapter -> sample builder` 主链消费。主要风险不在“能否接到流”，而在三点：1) `StreamInfo.mime_type/payload_type` 对 repair stream 不可靠，必须以 `associated_stream + answer SDP apt/FID` 作为 source-of-truth；2) reinject 需要处理去重、乱序和主流已到包的竞争，避免把重复/过期包重新喂给 sample builder；3) `RED/ULPFEC` 不应和 `RTX` 一起做，这两者在当前主线下没有同等的接入面，成本高得多。综合判断：`RTX unwrap -> reinject` 可作为下一轮独立 spike 立项，适合先做“只支持 RTX / 只支持单 primary PT / 仅在 probe 开关下启用”的窄实现；不建议把它误判成低成本通用 repair 方案。 |
| P-844 | 校准 repair probe 的 source-of-truth 分类并贯穿 runtime summary | Done | 2026-03-20 | 2026-03-20 | 继续收紧 [`repair_probe_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/repair_probe_interceptor.rs) 的观测链，不再只记录 `mime/pt/assoc`，而是显式把 repair stream 的来源分类贯穿到 typed observation、observation bus 与 runtime summary。具体做法是新增 `RepairStreamClassification`，按 `repair-mime` / `associated-stream` / `repair-mime+associated-stream` 标记 probe 识别来源，并把 `class=` 打进 bind / packet / unbind 日志，以及 `repair:<class>:<mime>:<phase>` 的 summary note。这样下一轮 trace 可以直接看出当前看到的到底是“真正的 repair mime”还是“借助 associated stream 被识别出来的修复流”，避免再把 `video/h264 pt=0` 这类 interceptor 元数据误当成 repair source-of-truth。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::webrtc::transport::repair_probe_interceptor::tests -- --nocapture`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-843 | 回归新 trace 中 repair probe 是否改变主流恢复走势 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773971986502.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773971986502.jsonl) 做只读核对，结论是 repair stream 依旧能被 interceptor 观测到，但这次同样没有看到它把主流恢复显著拉正。trace 中 `repair-probe` 明确出现了 `bind remote repair stream id=... ssrc=1132333699 mime=video/h264 pt=0 clock=90000 assoc_ssrc=Some(787947182) assoc_pt=Some(0)`，随后按 `1/2/4/8/.../256` 递增的 `observed repair packet` 也持续出现，说明辅助流仍然流经可控链路；但主流侧的 repair-active 摘要从 `pkts=0 rec=0 exp=0 gaps=0 hit=-` 很快走到 `pkts=154 rec=2 exp=0 gaps=0 hit=-`，再到 `pkts=218 rec=2 exp=0 gaps=0 hit=0.000`、`pkts=226 rec=2 exp=24055 gaps=24057 hit=0.000`，同时 `nackExpired -> transportExpiredDeadline -> cooldownSuppressed` 持续刷屏，说明这次辅助流并没有让主流恢复命中率明显提升，最终仍被大量过期/冷却抑制压住。另一个重要事实是，trace 里本次 `exchangeOfferResult` 没有落出结构化 `offerSdpCapabilities/answerSdpCapabilities`，因此这轮只能确认 repair-probe 与主流恢复走势，不能再次反向验证协商层 repair codec 摘要。本次仅完成只读分析归档，未改代码，也未跑测试。 |
| P-842 | 继续校准 repair stream metadata/source-of-truth，并给 repair-active 窗口补恢复命中率 | Done | 2026-03-20 | 2026-03-20 | 在 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs)、[`repair_probe_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/repair_probe_interceptor.rs)、[`diagnostics/observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs) 与 [`diagnostics/stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 继续加固 repair probe 观测：把 repair stream 的 `id/ssrc/clock_rate/associated_stream` 一起纳入 typed `VideoRepairProbe` observation，并把 `repair-active` 窗口的恢复命中率写入 `runtime_summary/latest_decision_summary`。这样下一次 trace 不仅能看到 `[xbxengine][repair-probe] bind/observed repair packet`，还能直接看到 `repair:... id=... ssrc=... pt=... clock=... pkts=... rec=... late=... exp=... gaps=... hit=...`，方便核对 interceptor 看到的辅助流身份是否与 SDP / FID / apt 对得上，以及 repair stream 出现后主流恢复是否真的更偏向 `recovered` 而不是 `expiredDeadline`。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::webrtc::transport::repair_probe_interceptor::tests -- --nocapture`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-843 | 将当前 WebRTC 调度语义拆成独立矩阵并落盘为 md 归档 | Done | 2026-03-19 | 2026-03-19 | 已将当前调度语义整理到 [`docs/webrtc-scheduling-semantics-matrix.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/webrtc-scheduling-semantics-matrix.md)，按阶段、恢复诊断、恢复耦合、BWE 控制、恢复动作五层拆分职责，明确 `StartupLowQuality` 只应表示启动期低质探测与快速爬升，不应再承载通用拥塞回退语义。 |
| P-842 | 收紧 cloud Rust-owned 的 startupLowQuality 拥塞回退，避免 25k floor 把真实 TWCC 丢包吞掉 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773929840254.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773929840254.jsonl) 复核后发现，本次卡死不在 `RecoveringReferenceChain`，而是在 `StartupLowQuality` 仍被 25k floor 顶住：trace 里 `latest_video_bwe_observation` 的 `decision_reason` 仍是 `twcc-gcc-cloud-recovery-coupled-startup-hold`，同时 `loss_ratio` 已到 `0.95+`、`delivery_ratio` 掉到 `0.12 / 0.04`，但策略层仍只输出 hold，导致 `transportExpiredDeadline` 一直被重复抑制。为此在 [`crates/xbxengine/core/src/transport/webrtc/bwe_policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/bwe_policy.rs) 给 cloud 的 `RecoveryCouplingMode::StartupLowQuality` 增加了严重拥塞回退分支：不再读取被 25k floor 夹住的值，而是按真实 TWCC receive headroom 直接回退到更低码率，避免 startup low-quality 状态把 burst loss 放大成持续卡死。新增单测覆盖 cloud startupLowQuality 在严重 loss 下从 25k 回退到 5.5k 的行为，同时保留 reference-chain backoff 回归。验证：`cargo test -p xbxengine cloud_startup_low_quality_backs_off_under_congestion -- --nocapture`、`cargo test -p xbxengine cloud_recovery_reference_chain_backs_off_under_congestion -- --nocapture`、`cargo check -p xbxengine`。 |
| P-841 | 基于新 trace 回归 repair probe 与 repair-active 主流恢复观测 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773929840254.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773929840254.jsonl) 做只读核对，结论分两层：1) repair stream 的确流过了可控 interceptor 链，trace 中出现了 `bind remote repair stream` 与后续按 `1/2/4/8/.../1024` 计数增长的 `observed repair packet` 日志，说明协商成功后的辅助流并没有被完全吞掉，至少在 interceptor remote reader 上可以被观测到；2) 但 repair-active 窗口里的主流恢复结果没有出现“明显被内部 repair 改善”的证据。stats 摘要从早期 `repair:video/h264:packet pkts=63 ... rec=2 exp=0 gaps=2` 很快演化到 `repair:video/h264:packet pkts=304 ... rec=4 exp=16726 gaps=16731`，后续又继续涨到 `pkts=404 rec=4 exp=24994 gaps=24998`，同时 trace 持续刷 `nackExpired -> transportExpiredDeadline -> cooldownSuppressed`。这说明 repair stream 虽然在流，但当前主链层面看不到它把大量 `expiredDeadline` 压下去；更像是“辅助流存在，但 webrtc-rs 并未把它充分转化成我们主流可见的恢复收益”，至少对这次样本如此。另一个需要注意的事实是，当前 probe 把该辅助流标成了 `mime=video/h264 pt=0 assoc_pt=0`，而不是预期中的 `video/rtx`，说明 `webrtc-rs` 在 interceptor 里给到的 `StreamInfo` 元数据并不干净；这并不否定 repair stream 的存在，但意味着后续若真要依赖 interceptor 接 RTX，仍需先做一轮 metadata/source-of-truth 校准。此次仅完成 trace 对照分析，未改代码，也未跑测试。 |
| P-840 | 收紧 cloud Rust-owned 的 BWE 控制策略，在 reference chain recovery 期间遇到明显 TWCC 拥塞时主动 backoff | Done | 2026-03-19 | 2026-03-19 | 在 [`crates/xbxengine/core/src/transport/webrtc/bwe_policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/bwe_policy.rs) 的 `resolve_twcc_gcc_target()` 里给 `RecoveryCouplingMode::RecoveringReferenceChain` 增加了 cloud-only backoff 分支：当 TWCC 已经进入明显拥塞（`packet_loss_ratio >= congestion_loss_threshold` 或 `delivery_ratio <= congestion_delivery_threshold`）时，不再继续执行 `recovery-coupled-reference-hold`，而是按当前发送码率做保守回退，并以 cloud profile 的 `preferred_floor_kbps` 作为回退下限，避免把短时抖动持续放大成 burst loss。新增单测覆盖 cloud 25k floor 下的 reference-chain backoff 行为，验证 target 会从 25k 回退到 21.25k，而 direct recovery hold 行为不受影响。验证：`cargo test -p xbxengine cloud_recovery_reference_chain_backs_off_under_congestion -- --nocapture`、`cargo test -p xbxengine direct_recovery_coupling_holds_bwe_and_caps_peak -- --nocapture`、`cargo check -p xbxengine`。 |
| P-839 | 增加 repair-active 主流恢复观测，确认 webrtc-rs 是否已在内部利用 repair stream 改善恢复 | Done | 2026-03-19 | 2026-03-19 | 在 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs)、[`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs)、[`diagnostics/observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs)、[`diagnostics/stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 与 [`repair_probe_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/repair_probe_interceptor.rs) 补了“repair-active 观察窗口”。具体做法：observer-only repair interceptor 除了记录 repair stream bind/packet 外，还会通过 `RuntimeStatsSink` 发布 typed `VideoRepairProbe` observation；一旦某条 repair stream 首包出现，就开启一个 repair-active 窗口并清零局部计数，随后主流里的 `packetGap`、`nackRecovered`、`recoveredLate`、`expiredDeadline/expiredMaxAge` 会自动累计到该窗口，形成“repair stream 出现后主流恢复到底更偏向 recovered 还是 expired”的直接观测。为便于回归，不另外扩协议 DTO，而是先把摘要直接接进 `runtime_summary/latest_decision_summary`，格式类似 `repair:video/rtx:packet pkts=8 since=1000 rec=3 late=1 exp=0 gaps=2`。这轮仍然只做观测，不实现 RTX 解包或回灌，因此不会与 `webrtc-rs` 内部现有 repair 处理重复恢复。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::webrtc::transport::repair_probe_interceptor::tests -- --nocapture`、`cargo test -p xbxengine diagnostics::stats::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-838 | 增加 observer-only repair interceptor spike 以确认 repair stream 是否流经可控拦截链 | Done | 2026-03-19 | 2026-03-19 | 在 [`repair_probe_interceptor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/repair_probe_interceptor.rs) 新增了一个只观察不改写的 remote interceptor：当环境变量 `XBXENGINE_EXPERIMENTAL_VIDEO_REPAIR_PROBE` 开启时，它会在 `bind_remote_stream()` 里识别 `video/rtx`、`video/red`、`video/ulpfec`、`video/flexfec-03` 或带 `associated_stream` 的 remote stream，并通过日志记录 repair stream 的绑定信息与包流动情况；实现只做观测，不解包、不回灌、不参与恢复，目的是确认协商成功后的 repair stream 是否真的流经我们可控的 interceptor 链，而不会引入与 `webrtc-rs` 内部 RTX 处理重复恢复的风险。同时在 [`transport/core.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/core.rs) 将该 interceptor 仅在 probe 模式下挂入现有 registry，并补了分类单测锁住“识别 repair stream / 忽略主 H264 stream”两条语义。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::webrtc::transport::repair_probe_interceptor::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-837 | 拆分新 trace 中 `transportExpiredDeadline` 的原因并评估 RTX/FEC 价值 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773928503253.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773928503253.jsonl) 做只读核对，结论分两段：1) 前半段问题已经被前面改过的 cloud 恢复策略拉起来了，`exchangeOfferRequested` / `exchangeOfferResult` 明确显示当前会话已经协商了 `RTX / RED / ULPFEC / FID`，随后在 `transportAwaitRecoveryAnchor` 后成功发出 `videoKeyframeRequested`，统计快照里 `recovery_keyframe_request_count=1`、`latest_data_channel_message_catalog_observation` 记录了 outbound `videoKeyframeRequested`，说明 keyframe 恢复链是有效的；2) 后半段并不是“没有 RTX 导致救不回”，而是媒体链出现了更大规模的 burst loss / 退化：trace 在 `media loss detected before sample ts=3973856591 dropped_packets=3908 is_keyframe=false` 后，`nackExpired` 反复走到 `expiredDeadline`，`latest_video_twcc_observation.delivery_ratio` 从正常值一路掉到 `0.0392`、`packet_loss_ratio=0.9608`，最终把系统推回 `recovery:transportExpiredDeadline->cooldownSuppressed`。综合判断，当前失败更像是“极端丢包/长时间退化超过 NACK/RTX/FEC 恢复窗口”，而不是缺少 RTX 纠错能力本身。换言之，RTX/FEC 对早期零星丢包应该有帮助，但对这次尾段那种接近失联级别的丢包风暴，帮助有限，仍然需要更上游地降低丢包/重排/队列崩坏。此次仅完成只读分析归档，未改代码，也未跑测试。 |
| P-836 | 复核新 trace 中 keyframe 恢复是否真正生效以及后续回落原因 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773928503253.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773928503253.jsonl) 做只读核对，结论是前面刚刚修改的 cloud 恢复策略已经生效：日志在 `transportAwaitRecoveryAnchor` 后出现了 `queue pending keyframe request until recovery protocol is ready`，随后进入 `requestKeyframe`，并且统计快照里 `recovery_keyframe_request_count=1`、`latest_video_escalation_observation.action=requestKeyframe`、`video_health=waitingKeyframe`，说明这次确实把 keyframe 恢复链拉起来了；`transport_state` 也已在 `Connected`。但随后媒体链再次遭遇更大规模丢包，trace 中出现 `media loss detected before sample ts=3973856591 dropped_packets=3908 is_keyframe=false`，之后 `latest_video_nack_observation` 从 `sent` 逐步演化到 `expiredDeadline`，`latest_video_packet_gap` 也从 `missingCount=1` 演化到 `missingCount=2`，最终快照显示 `recovery_diagnosis=transportExpiredDeadline`、`recovery_keyframe_request_count=3`、`video_decoder_reset_count=1`、`latest_video_twcc_observation.delivery_ratio=0.0392`、`packet_loss_ratio=0.9608`、`video_health=healthy` 但 `stallKind=videoPaused`。这说明当前问题不再是“keyframe 根本没发出去”，而是后续链路在极端丢包下再次退化：NACK/keyframe/reset 都已经在工作，但恢复窗口内仍然收不回足够媒体包。此次仅完成只读分析归档，未改代码，也未跑测试。 |
| P-835 | 让 cloud 场景下 delta frame 的 `transportExpiredDeadline` 更快升级到 keyframe 恢复链 | Done | 2026-03-19 | 2026-03-19 | 针对 [`runtime-trace-1773926704599.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773926704599.jsonl) 中“delta frame `expiredDeadline` 被 coordinator 压成 `CooldownSuppressed`、看不到 keyframe dispatch”的问题，在 [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 收紧了 `resolve_recent_nack_outcome()` 的分流：当最近 NACK outcome 属于 delta frame 的 `expiredDeadline/expiredMaxAge`，且当前 recovery profile 为 `cloudGaming`、触发 reason 已是 `TransportExpiredDeadline` 时，不再继续停留在 `CooldownSuppressed`，而是直接提升为 `TransportAwaitRecoveryKeyframe`，让现有 escalation controller 立即进入 `RequestKeyframe` 分支；Home/Relay 仍保持原来的保守语义，只有 `stalled_with_fresh_packets` 等额外条件满足时才升级。为锁定行为，新增单测覆盖“cloud delta expired deadline 无需等待 renderer stall 也必须请求 keyframe”，并保留原有 home 抑制与 stalled 升级回归。验证：`cargo test -p xbxengine expired_delta_nack_in_cloud_requests_keyframe_without_waiting_for_stall_signal -- --nocapture`、`cargo test -p xbxengine expired_delta_nack_stays_suppressed_without_stall_signal -- --nocapture`、`cargo test -p xbxengine expired_delta_nack_requests_keyframe_when_pipeline_is_stalled -- --nocapture`、`cargo check -p xbxengine`。 |
| P-834 | 复核 `transportExpiredDeadline` 后是否实际发起 keyframe / decoder reset 恢复 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773926704599.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773926704599.jsonl) 与 recovery/data-channel 代码做只读核对，结论是：这次故障点后没有足够日志证据证明“已实际发出 keyframe request”，更大的可能是 recovery policy 在看到“最近一次 NACK outcome 属于 delta frame 的 `expiredDeadline/expiredMaxAge`”后，按设计先返回 `CooldownSuppressed`，根本没有进入 keyframe dispatch。原因链路如下：1) trace 能确认 control 通道至少已经 bootstrap，出现了 `bootstrap control before message handshake ack`，因此不能把问题直接归因到 control channel 未建立；2) 但 trace 尾部没有任何 `sending/sent keyframe request on control channel`、`queue pending keyframe request`、`decoder reset dispatch` 等日志，而且这份 trace 在首次 `media loss detected before sample ... dropped_packets=10 is_keyframe=false` 后很快结束，只剩重复的 `transportExpiredDeadline` 提示，缺少后续 stats snapshot，因此不能从运行态计数器进一步确认；3) 代码侧 [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 会在主 escalation 前先调用 `resolve_recent_nack_outcome()`，其中若最近 NACK outcome 是 delta frame 的 `expiredDeadline/expiredMaxAge`，默认直接返回 `RecoveryAction::CooldownSuppressed`；只有 `stalled_with_fresh_packets` 这类额外条件满足时，才会升级成 `TransportAwaitRecoveryKeyframe` 再走 keyframe 分支。与此同时 [`escalation.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/escalation.rs) 本身对 `TransportExpiredDeadline` 的默认动作确实是 `RequestKeyframe`，但前提是没有被 coordinator 前置抑制掉。综合判断：本次更像是“delta frame deadline 过期后被 coordinator 有意压成 cooldown，而不是 control channel 挂了导致 keyframe 发不出去”。这解释了为什么日志里持续刷 `transportExpiredDeadline`，却看不到 keyframe/reset dispatch 证据。本次仅完成分析归档，未改代码，也未跑测试。 |
| P-833 | 核对 webrtc-rs 在协商成功后是否把 repair stream 暴露给当前主链 | Done | 2026-03-19 | 2026-03-19 | 继续基于 [`runtime-trace-1773927614777.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773927614777.jsonl) 与本地 `webrtc-rs 0.17.1` 依赖源码做只读核对，确认“协商成功”是否等于当前 `TrackRemote -> adapter` 主链能拿到 repair stream。结论：当前主线并没有真正消费这些 repair stream。仓库代码侧，[`transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs) 的 `on_track` 只把 primary video/audio 挂到主链；若不是主视频也不是音频，就直接 `track.read_rtp()` drain 掉。更关键的是依赖源码显示，`webrtc-rs` 对 RTX 采用的是“receiver 内部 repair stream”模型：[`rtp_receiver/mod.rs`](/Users/guo.xu/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/webrtc-0.17.1/src/rtp_transceiver/rtp_receiver/mod.rs) 在主 track 旁维护 `repair_stream`，`receive_for_rtx()` 的注释明确写着“These packets aren't exposed to the user yet, but we need to process them for TWCC”；也就是说 RTX 至少在 0.17.1 中不会作为新的 `TrackRemote` 直接交给上层。对于 `RED/ULPFEC`，当前依赖源码中虽有 codec/SDP 层协商痕迹，但未发现与 RTX 对等的 receiver 暴露或恢复 API。结合 trace 中 repair codec 已协商成功、但运行时仍沿 `packetGap -> nackExpired -> transportExpiredDeadline` 路径升级，可确认现阶段“协商成功”并不等于“我们已用上 RTX/FEC”；当前主链依旧主要依赖 `NACK + keyframe/reset`。本次仅完成代码与 trace 对照分析，没有修改业务代码，也未运行测试。 |
| P-832 | 基于 repair probe trace 确认远端接受 RTX / RED / ULPFEC / FID | Done | 2026-03-19 | 2026-03-19 | 基于开启 `XBXENGINE_EXPERIMENTAL_VIDEO_REPAIR_PROBE` 后的成功会话 trace [`runtime-trace-1773927614777.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773927614777.jsonl) 做只读核查。结论：这次不是“我方单边 advertise”，而是远端 answer 明确接受了 `RTX / RED / ULPFEC`，并给出了 `FID` 语义。证据：[`exchangeOfferRequested`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773927614777.jsonl) 的 `offerSdpCapabilities` 显示 video offer 已带 `rtx/red/ulpfec`，其中 `rtx` 为 `PT 97` 且 `apt=124`；随后 [`exchangeOfferResult`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773927614777.jsonl) 的 `answerSdpCapabilities` 显示 `hasVideoRtx=true`、`hasVideoRed=true`、`hasVideoUlpfec=true`、`hasSsrcGroupFid=true`、`videoRepairCodecs=[\"red\",\"rtx\",\"ulpfec\"]`、`aptPairs=[{\"payloadType\":\"97\",\"aptPayloadType\":\"124\",\"codec\":\"rtx\"}]`，且 answer 的 `m=video` payload order 包含 `124 125 102 97 122 116`。trace 还在 `seq 346` 进入 `transportState=\"Connected\"` 且 `transportPath=\"Direct (host->host)\"`，说明这不是协商前早停样本。由此可确认：当前 cloud Rust-owned 远端并非“不支持 repair codec”，而是此前默认主线没有把这些能力放进 offer。本次仅完成 trace 事实确认，没有修改业务代码，也未运行测试。 |
| P-831 | 聚焦分析 cloud Rust-owned trace 中 `transportExpiredDeadline` 的直接触发原因 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773926704599.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773926704599.jsonl) 与 `xbxengine` transport adapter 代码做只读核对，结论是本次 `transportExpiredDeadline` 不是播放器切换失败，也不是 ICE 断开直接上报，而是 adapter 在检测到样本前存在丢包后开启 NACK 恢复，但缺失 RTP 包未能在计算出的帧级 deadline 前补齐，最终由 NACK 维护逻辑以 `deadline` 原因过期并映射成 `transportExpiredDeadline`。日志证据：会话已明确运行在 `rust-owned`，并成功走到 `Binding -> ExchangingOffer -> GatheringIce -> Connecting -> ExchangingIce -> ready`；随后 trace 首次出现 `[WebrtcVideoAdapter] media loss detected before sample ts=3315748157 dropped_packets=10 is_keyframe=false`，紧接着持续刷 `Transport escalation hint: transportExpiredDeadline`。代码链路对应为 [`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 在样本前丢包时调用 [`nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 的 `observe_sample_loss_and_nack()` 建立恢复窗口；若 `NackScheduler` 到期仍未收齐缺包，则上报 `TransportObservation::NackDeadlineExpired`，再由 [`session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs) 映射到 recovery diagnosis `transportExpiredDeadline`。同时本次 answer SDP 明确没有 `RTX / RED / ULPFEC / FID`，因此恢复路径只能依赖 `NACK + keyframe/reset`，没有 repair stream/FEC 兜底。当前这份 trace 能证明“缺包未在 deadline 内修复”，但还不能单独证明底层一定是真实网络丢包，也可能是重排/抖动超过本地恢复窗口。本次仅完成分析归档，未改代码，也未跑测试。 |
| P-830 | 为 Rust-owned offer 增加可开关的 RTX / RED / ULPFEC 能力探测 | Done | 2026-03-19 | 2026-03-19 | 为验证“远端是否会接受 repair codec”而不污染默认主线，在 [`transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs) 把视频 codec preference 改成显式经过 `build_offer_video_codec_preferences()` 组装；默认仍只带 H.264 family，不改变现有正式协商路径。新增环境开关 `XBXENGINE_EXPERIMENTAL_VIDEO_REPAIR_PROBE`：开启后会在 video offer 中追加 `video/rtx`（PT 97，`apt=124`）、`video/red`（PT 122）和 `video/ulpfec`（PT 116）三个探测 codec，并同步注册到 MediaEngine，便于直接观察远端 answer 是否接受这些 payload / fmtp / repair contract；关闭时保持原状。此次还补了 2 条单测，分别锁住“默认不带 repair probe codec”和“开启时会追加 `rtx/red/ulpfec`”两条语义。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::webrtc::transport::setup::tests -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-829 | 补 waitingSessionReady 诊断 trace 并复核 xHome force_region_ip/注册风险 | Done | 2026-03-19 | 2026-03-19 | 针对 xHome 会话停在 `waitingSessionReady` 的问题，在 [`crates/xbox-streaming/src/session/flow.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs)、[`crates/xbox-streaming/src/session/scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/scheduler.rs)、[`src-tauri/src/mods/streaming/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/streaming/service.rs) 增加只读诊断钩子与 runtime trace 事件：`sessionStatePolled`、`sessionStatePollFailed`、`sessionMonitorSnapshot`、`sessionConnectToken`，用于记录 `/state` 轮询返回值、monitor 投影结果以及 connect token 分发成败，不改变现有状态机语义。验证：`cargo fmt --all`、`cargo check -p xbox-streaming -p xbxrc`。随后继续只读核对 xHome 配置链路，结论是：1) `force_region_ip` 不只是本地 region 选择提示，在 [`crates/xbox-webapi/src/auth_api.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-webapi/src/auth_api.rs) 会直接以 `x-cosmos-ip` header 参与 streaming token 申请；而 policy 侧 [`crates/xbox-streaming/src/policy/session/compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/session/compiler.rs) 只有在 region `base_uri` 包含该 IP 时才会改选区域，因此像日志里的 `4.2.2.2` 这类公共 DNS 地址通常不会改变本地 `base_url`，却仍会影响 token 发行归属，存在“token 侧被强行绑到异常 IP、plan 侧仍落默认 WestUS2” 的错位风险。2) remote console 列表里的 `consoleStreamingEnabled=null` 并不会阻止当前启动流程；[`crates/xbox-streaming/src/session/flow.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs) 的 `is_remote_console_ready()` 仅把显式 `false` 视为失败，而 projection 侧 [`crates/xbox-streaming/src/policy/projection.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/projection.rs) 却把 `null` 投影成 `false`。这说明当前实现存在“能力状态未知也允许继续创建 session”的放行口，主机若尚未完整注册 Remote Play，便可能出现 `creatingSession` 成功但 `waitingSessionReady` 长时间不 ready` 的症状。本次完成代码改动与只读分析，未新增测试。 |
| P-828 | 基于成功协商 trace 确认远端 answer 是否宣告 RTX / ULPFEC / RED / FID | Done | 2026-03-19 | 2026-03-19 | 基于成功进入 `startingRuntime -> ready` 并完成 `exchangeOfferResult` 的 [`runtime-trace-1773926704599.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773926704599.jsonl) 做只读核查。结论：本次远端 media answer 明确只宣告了 H.264 视频能力，没有宣告 `RTX / RED / ULPFEC / FlexFEC`，也没有 `a=ssrc-group:FID`。证据直接来自 trace 中新增的结构化摘要：[`exchangeOfferRequested`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773926704599.jsonl) 的 `offerSdpCapabilities` 显示本地 offer 仅带 H.264 payload，未请求 repair codec；[`exchangeOfferResult`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773926704599.jsonl) 的 `answerSdpCapabilities` 显示 `hasVideoRtx=false`、`hasVideoRed=false`、`hasVideoUlpfec=false`、`hasVideoFlexfec=false`、`hasSsrcGroupFid=false`、`videoRepairCodecs=[]`、`aptPairs=[]`，视频 payload order 仅为 `124/125/102` 三个 H.264 payload。由此可确认，至少在这次 cloud Rust-owned 成功协商里，远端并未给我们任何显式 repair stream/FEC contract；当前恢复路径仍然只能建立在 `NACK + PLI/FIR + keyframe/reset` 之上，而不能假设存在 Moonlight 式 FEC ownership。本次仅完成 trace 事实确认，没有修改业务代码，也未运行测试。 |
| P-827 | 为 Rust runtime trace 增加 SDP 能力摘要，便于确认远端 RTX/ULPFEC/RED 宣告情况 | Done | 2026-03-19 | 2026-03-19 | 为继续完成“确认远端是否真的宣告 RTX/ULPFEC/RED/FID”这轮 spike，在 [`runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/runtime_state.rs) 新增 `summarize_sdp_capabilities()`，从 raw SDP 中提取 `hasVideo`、`videoPayloadOrder`、`videoCodecs`、`videoRepairCodecs`、`hasVideoRtx`、`hasVideoRed`、`hasVideoUlpfec`、`hasVideoFlexfec`、`hasSsrcGroupFid` 与 `aptPairs` 等结构化字段，并把它接入 host bridge 的 `exchangeOfferRequested` / `exchangeOfferResult` trace payload，分别记录 `offerSdpCapabilities` 与 `answerSdpCapabilities`。这样后续只要拿到一次成功协商的 runtime trace，就能直接从 trace 里确认远端 answer 是否真的宣告 repair codec / RTX apt / FID，而不必再手工回放 SDP。实现过程中顺手修正了 `m=video` payload 提取下标，避免把首个 payload type 漏掉；并在同文件新增 2 条单测，分别锁住“带 `rtx + ulpfec + FID + apt` 的视频 SDP 能被正确识别”和“纯 H264 offer 不应误报 repair codec”。验证：`cargo fmt --all`、`cargo test -p xbxrc summarize_sdp_capabilities -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-826 | 分析 xHome 运行日志为何停在 waitingSessionReady 无法连上 | Done | 2026-03-19 | 2026-03-19 | 基于 [`runtime-trace-1773921390336.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773921390336.jsonl) 做只读排查。结论：本次不是 ICE/TURN/WebRTC 已启动后“连不上”，而是更早的会话编排卡住了。日志显示 `resolvingContext -> wakingConsole -> waitingConsoleReady -> creatingSession` 均成功，随后在 `waitingSessionReady` 挂住；直到日志结束仍没有 `waitingSessionReady succeeded`、`startingRuntime`、`ready`，也没有任何 offer/answer/ICE 交换相关事件。同时 `xbxengine` 观测全程停在 `transportState=\"New\"`、`transportPath=null`、零入站字节，说明媒体运行时根本没有进入有效建链阶段。结合 [`crates/xbox-streaming/src/session/flow.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/flow.rs)、[`crates/xbox-streaming/src/session/scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/scheduler.rs)、[`crates/xbox-streaming/src/session/monitor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/session/monitor.rs) 可知，真正阻塞点位于 `start_stream()` 成功之后、远端 session state 尚未推进到 `ReadyToConnect/Provisioning/Provisioned` 的阶段；本日志在进入 `waitingSessionReady` 后约 47 秒结束，而策略超时是 90 秒，因此没有采到最终 timeout/error。附带异常事实：上下文快照里 `consoleAddrsCount=0`、`consoleStreamingEnabled=null`，配置里还带了非默认 `force_region_ip=4.2.2.2` 与自定义 fallback TURN；这些都值得继续核对，但仅凭本日志还不能证明它们是唯一根因。本次仅完成日志与代码对照分析，未修改业务代码，也未运行测试。 |
| P-825 | 评估 webrtc-rs FEC 能力面与自研 FEC 成本 | Done | 2026-03-19 | 2026-03-19 | 基于当前锁定依赖 `webrtc = 0.17.1` 与本地 crates 源码，对“现有 WebRTC 栈是否自带可用 FEC、能否支持类似 Moonlight 的自定义 FEC 策略、若不能则自研成本多大”做只读评估。结论：1) `webrtc-rs` 在 [`webrtc-0.17.1/src/api/media_engine/mod.rs`](/Users/guo.xu/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/webrtc-0.17.1/src/api/media_engine/mod.rs) 的 `register_default_codecs()` 中确实注册了 `video/ulpfec`，在 [`interceptor-0.17.1/src/stream_info.rs`](/Users/guo.xu/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/interceptor-0.17.1/src/stream_info.rs) 也保留了 auxiliary stream 到原流的抽象；`webrtc` 的 SDP 层还能识别 RTX repair flow，且 [`setting_engine/mod.rs`](/Users/guo.xu/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/webrtc-0.17.1/src/api/setting_engine/mod.rs) 暴露了 sender RTX 开关。2) 但这套能力并不足以让我们在当前 `webrtc-rs` 主线里实现 Moonlight 那种“自有 FEC block/queue/repair policy ownership”：上游 changelog 已明确说明 `video/rtx` 经由 `register_default_codecs()` 并非真正支持能力；本地未发现 `FlexFEC` / `RED` 的成体系接收与策略 API；而我们自己的接入层在 [`transport/core.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/core.rs) 虽调用 `register_default_codecs()`，但 [`transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs) 的视频 transceiver codec preference 只收敛到 H.264 family，没有把 `RED/ULPFEC/RTX` 纳入当前协商 contract。3) 因此若坚持“类似 Moonlight 的自定义 FEC 策略”，在现有 `webrtc-rs` 主线上成本会很高，实质上接近重做一段更低层的 RTP/FEC ownership：要么深入改造/扩展 `webrtc-rs` 取回 FEC 包和 repair timing ownership，要么绕开现有 TrackRemote/PeerConnection 主链另建自有接收面；两条路都远超简单参数调优。本次仅完成代码与依赖源码评估，没有修改业务代码，也未运行测试。 |
| P-824 | 将 transport recovery epoch 投影进 stats summary 并补单测 | Done | 2026-03-19 | 2026-03-19 | 继续把显式 `transport recovery epoch` 从内部恢复状态推进到可见观测摘要，而不新增 DTO 字段。此次在 [`diagnostics/stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 增加 `build_transport_recovery_note()` 与统一的 `append_runtime_notes()`，让 `runtime_summary` 与 `latest_decision_summary` 在保留既有 `obs:*` 观测摘要的同时，额外稳定投影 `repoch:{n}`；当 `transport_recovery_epoch > transport_recovery_epoch_at_last_escalation` 时会输出 `repoch:{n}:active`，用于直接辨认当前 recovery 轮次是否已经进入新 episode。并在同文件新增 2 条单测，锁住“runtime summary 必须带出 active epoch 注记”和“latest decision summary 在没有其他决策摘要时回退到 epoch 注记”两条语义。验证：`cargo fmt --all`、`cargo test -p xbxengine diagnostics::stats -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-823 | 显式化 transport recovery epoch 并补 recovery 集成测试 | Done | 2026-03-19 | 2026-03-19 | 在 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs) 为 `XbxEngineMediaRuntimeStats` 新增 `transport_recovery_epoch` 与 `transport_recovery_epoch_at_last_escalation`，把 transport 恢复轮次从“由新 packet gap / nack 事实近似推断”推进成显式状态；[`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 新增 `begin_transport_recovery_episode()`；[`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs) 在进入 `AwaitRecoveryKeyframe`、`RecoveryKeyframeRequested`、`NackDeadlineExpired`、`StreamIdleTimeout/ThinStall` 等关键 transport 恢复节点时显式开启新一轮 epoch；[`recovery/recovery_executor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_executor.rs) 在执行恢复动作时记录 `transport_recovery_epoch_at_last_escalation`；[`recovery/recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 的重复 suppress 现优先使用 epoch，而不是继续依赖“上次 escalation 之后是否出现新 gap/nack 事实”的近似条件。并新增 recovery 集成测试，验证新 epoch 会打破 `waitKeyframe` suppress。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine recovery_coordinator -- --nocapture`。 |
| P-822 | 网络层 6 类调度优化：补齐 recovery/transport 联动 | Done | 2026-03-19 | 2026-03-19 | 在 [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 与 [`video_track_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_stats.rs) 补齐网络层 6 类调度优化的剩余联动：1) `resolve_recent_repeat_suppression()` 不再只按固定时间窗 suppress，而是额外检查是否在上次 escalation 之后出现了新的 `latest_video_packet_gap` 或 `latest_video_nack_observation`，用“是否有新 transport 恢复事实”近似 recovery episode，避免旧一轮 `waitKeyframe/adapterIdleTimeout` 把新一轮 deadline 失败也一起压掉；2) `spawn_video_track_stats_loop()` 引入 pressure-aware 采样节奏，在 `video_pending_missing_packets > 0`、`recovery_coupling_mode=waitingKeyframe`、或最近存在 `expired/recoveredLate/sampleLoss` 传输恢复事件时临时缩短 feedback interval，让 BWE/coupling 更快看到 transport 恢复状态，恢复后退回基础采样周期。连同 [`transport/adapter/*`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter) 的 `P-821`，本轮已把“组包窗口、帧价值分级重传、修复触发前移、deadline 驱动 NACK、wait-keyframe 抑制、transport 观测服务调度”六类问题一起收敛到当前 `webrtc-rs` 主线。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`、`cargo test -p xbxengine transport::adapter::tests -- --nocapture`。 |
| P-821 | 网络层 6 类调度优化：adapter 侧实现 dynamic repairability / frame-value-aware deadline / wait-keyframe suppression / observation clarity | Done | 2026-03-19 | 2026-03-19 | 按 patch scope 仅改动 [`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs)、[`transport/adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs)、[`transport/adapter/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs)、[`transport/adapter/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/tests.rs)。实现内容：1) sample-loss NACK policy 引入动态 repairability 评分（结合帧价值、缺包规模、burst、waiting-keyframe、近期 NACK 恢复 EWMA）并动态调参 `max_age/retry_interval/burst/priority/deadline`；2) `rtpWindow/rtpGap` NACK 改为基于最近成功提交帧的 `FrameValue` 计算 deadline/importance，避免统一按低价值 delta；3) adapter observation 入队增加 wait-keyframe 去抖与优先级覆盖，减少重复 `AwaitRecoveryKeyframe` 噪声且避免低优先级事件覆盖高优先级信号；4) transport NACK observation 统一补全 `frame_importance/frame_is_keyframe/priority` 元信息。本次补充了 2 条 adapter 局部单测。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::adapter::tests -- --nocapture`。 |
| P-848 | 分析当前会话中关键缺包 `seq=18894` 的 NACK deadline 与 RTT 时序 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773976013813.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773976013813.jsonl) 与当前网络层实现核对 `seq=18894` 的关键缺包链。结论：1) 当前 cloud Rust-owned 会话的基础 `video_pipeline` 仍是 browser-style 宽窗口，配置来自 [`compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs#L128)，即 `nack_max_age_ms=200`、`nack_retry_interval_ms=60`、`nack_max_retry_count=5`、`late_frame_drop_threshold_ms=500`；但 adapter 会对 `rtpGap/rtpWindow/sampleLoss` 用更激进的 per-frame policy 覆盖，见 [`nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs#L413)。2) trace 中 `seq=18894` 首次在 `seq 294` 被观测到，`rtpGap`/`rtpWindow` 的首轮 deadline 为 `1773976040339.8713`，约比首观测晚 `~100ms`；随后在 `seq 323/327/333...` 被持续重试并最终升级为 keyframe 链上的 `expiredDeadline`。3) 同次会话实际 RTT 为 `24.0ms`，来源见 `bweUpdated.rttMs=24.0` 与 snapshot 的 `video_rtt_source=\"nack-recovery\"`（`seq 406/448/449`）；TWCC 反馈周期基本是 `~100ms`（如 `seq 675/706`），因此当前问题更像“关键缺包未在 deadline 内被有效修复”，而不是 RTT 本身过高。4) reinject PoC 已在 `seq 709/718/720` 证明会回灌 primary 包，但这些 reinject 日志均晚于 `seq=18894` 进入 `expiredDeadline` 的时间窗，尚不能证明它命中了该关键缺包。此次仅完成时序分析，没有改代码或跑新测试。 |
| P-820 | 第二阶段：拆 data-channel availability/dispatch 并扩展 transport observation bus | Done | 2026-03-19 | 2026-03-19 | 继续完成网络层第二阶段收口：1) 在 [`data_channel/channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs) 新增 `ChannelAvailabilitySnapshot` 与 `RecoveryActionDispatcher`，把 channel 可用性事实与 recovery action 执行入口分开；[`recovery/recovery_executor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_executor.rs) 现通过 dispatcher 获取 actionable control channel、pending 状态与请求发送，不再直接散落依赖 `XbxDataChannelState` 细节；[`data_channel/bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/bootstrap.rs) 在 control/message/bootstrap/open/close 等关键节点统一发布 data-channel availability。2) 在 [`diagnostics/observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs) 与 [`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 扩展 transport typed events，新增 transport metrics、data-channel availability、video track status、TWCC observation 发布能力；[`transport/video_track_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_stats.rs) 把 RTT/path/loss/bitrate 事实改走 observation bus，仅保留 session phase / recovery coupling / BWE 结果的策略投影；[`transport/twcc_owned_receiver.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/twcc_owned_receiver.rs) 现通过 sink 发布 TWCC observation。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未运行测试。 |
| P-819 | 第一阶段：adapter feedback/output contract 收口 | Done | 2026-03-19 | 2026-03-19 | 按阶段约束仅改动 [`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs)、[`transport/adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs)、[`transport/adapter/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs)、[`transport/adapter/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/tests.rs)、[`transport/webrtc/transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs)、[`transport/webrtc/stack/session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs)。核心变更：1) adapter 新增显式 `TransportFeedbackPort`，`WebrtcVideoAdapter` 不再直接持有 `RTCPeerConnection`/`write_rtcp`；2) `FrameSourceEvent` 恢复输出收窄为中性 `TransportObservation`（admission/loss/deadline/stall），不再由 adapter 直接产出 `TransportSevereDeadline`/`TransportSampleLossBurst`；3) 在 session 层集中把 observation 映射回既有 `VideoRecoverySignal`，deadline severe 判定迁移到 session 且阈值行为保持一致。验证：`cargo fmt --all`、`cargo check -p xbxengine`。本次未新增单测，仅同步调整既有 adapter 单测断言。 |
| P-818 | 拆分 transport stats loop 的 observation collector 与 BWE evaluator | Done | 2026-03-19 | 2026-03-19 | 按第一阶段要求在 [`video_track_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_stats.rs) 完成最小结构拆分：新增 [`video_track_observation_collector.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_observation_collector.rs) 负责纯 transport 采样（stats/loss/rtt/remb 观测，不触达策略字段）；新增 [`video_track_bwe_evaluator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_bwe_evaluator.rs) 负责 session phase、recovery coupling、transport profile 与 REMB 目标评估；`video_track_stats` 现仅做编排与 RTCP 注入，`transport_policy_profile` 等策略投影改由 evaluator 输出后统一写入 runtime stats，collector 不再直接写 strategy 字段。并在 [`transport/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/mod.rs) 增加模块接线。验证：`cargo fmt`、`cargo check -p xbxengine`。未新增测试。 |
| P-817 | 收敛 xbxengine-core 网络层后续改造清单 | Done | 2026-03-19 | 2026-03-19 | 基于 [`dev-docs/webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 与当前 `xbxengine-core` 代码事实，对网络接入层继续做边界核对，并结合 Moonlight 的 [`VideoStream.c`](/Users/guo.xu/Documents/code/games/moonlight-qt/moonlight-common-c/moonlight-common-c/src/VideoStream.c)、[`VideoDepacketizer.c`](/Users/guo.xu/Documents/code/games/moonlight-qt/moonlight-common-c/moonlight-common-c/src/VideoDepacketizer.c)、[`RtpVideoQueue.c`](/Users/guo.xu/Documents/code/games/moonlight-qt/moonlight-common-c/moonlight-common-c/src/RtpVideoQueue.c)、[`session.cpp`](/Users/guo.xu/Documents/code/games/moonlight-qt/app/streaming/session.cpp)、[`ffmpeg.cpp`](/Users/guo.xu/Documents/code/games/moonlight-qt/app/streaming/video/ffmpeg.cpp) 做对照，最终收敛出当前网络层后续优先改造项：1) 拆分 [`transport/video_track_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_stats.rs) 的 transport observation 与 BWE/recovery policy evaluator；2) 为 [`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs) / [`transport/adapter/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 引入显式 `TransportFeedbackPort`，避免 adapter 直接持有 `RTCPeerConnection` 并写 RTCP；3) 收窄 [`FrameSourceEvent`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs) 的输出 contract，避免 adapter 预先固化 `TransportSevereDeadline` / `TransportSampleLossBurst` 这类策略升级语义；4) 拆分 [`data_channel/channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs) 里的 channel availability 与 recovery action dispatch；5) 扩展 [`diagnostics/observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs) 的 transport typed events，并把 strategy projection 从 transport loop 里继续抽离。此次仅完成分析与清单收敛，未修改业务代码，也未运行测试。 |
| P-816 | 只读分析 Moonlight 网络/接收层并提炼 WebRTC 后续改造方向 | Done | 2026-03-19 | 2026-03-19 | 只读核对 Moonlight 的 [`VideoStream.c`](/Users/guo.xu/Documents/code/games/moonlight-qt/moonlight-common-c/moonlight-common-c/src/VideoStream.c)、[`VideoDepacketizer.c`](/Users/guo.xu/Documents/code/games/moonlight-qt/moonlight-common-c/moonlight-common-c/src/VideoDepacketizer.c)、[`RtpVideoQueue.c`](/Users/guo.xu/Documents/code/games/moonlight-qt/moonlight-common-c/moonlight-common-c/src/RtpVideoQueue.c)、[`session.cpp`](/Users/guo.xu/Documents/code/games/moonlight-qt/app/streaming/session.cpp)、[`ffmpeg.cpp`](/Users/guo.xu/Documents/code/games/moonlight-qt/app/streaming/video/ffmpeg.cpp)，并对照 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 提炼“Moonlight 网络层真实职责 / 当前 WebRTC 主线可借鉴语义 / 必须改写的结构 / 接下来 3-5 条最有价值的改造建议”。本次未修改业务代码，也未运行测试。 |
| P-815 | 修复 `.agents/skills/orchestrated` 技能注册元信息 | Done | 2026-03-19 | 2026-03-19 | 修复 [`orchestrated/SKILL.md`](/Users/guo.xu/Documents/code/games/xbxrc/.agents/skills/orchestrated/SKILL.md) 缺少技能发现所需 frontmatter 的问题，补充 `name` / `description` / `metadata.version`，并将标题收敛为与目录一致的 `# Orchestrated`，使其符合当前技能发现器的可识别格式。此次未运行额外测试；修复目标为技能注册兼容性。 |
| P-814 | 将 observation bus 推进到可见 summary 投影 | Done | 2026-03-19 | 2026-03-19 | 继续把 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 里的 observation bus 从“内部同步 dispatcher”推进到“可见观测投影”。这轮在 [`diagnostics/observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs) 为每次 `ObservationEvent` dispatch 增加统一的 `label + summary` 生成，并把结果写入 [`api/backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/backend.rs) 新增的 `XbxEngineMediaRuntimeStats.latest_observation_label/latest_observation_summary`；[`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 继续作为 facade，对外调用面不变，但 `record_*` 全部统一走 observation bus；[`diagnostics/stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/stats.rs) 现在会把这份观测摘要作为 `runtime_summary` / `latest_decision_summary` 的补充 suffix 投影出来，格式为 `obs:{label}:{summary}`，并避免 `Closed` 会话继续带出陈旧 observation note。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-813 | 将 recovery contract 再推进成 central session scheduler | Done | 2026-03-19 | 2026-03-19 | 继续收紧 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 里 `session -> recovery_driver` 的调度边界。在 [`stack/session_scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session_scheduler.rs) 新增显式 `MediaSessionScheduler`，把 `RecoverySchedulerInput` 的消费与 `RecoverySchedulerDispatch` 的执行统一包起来；[`stack/session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs) 现在只负责把 startup tick、transport signal、ingress signal 交给 scheduler，不再自己持有调度流程细节；[`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) 加入新模块接线。恢复语义未改，仍由 [`recovery_driver.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/recovery_driver.rs) 和 recovery 内核决定。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-811 | 落地 observation bus 第一阶段与 recovery scheduler contract | Done | 2026-03-19 | 2026-03-19 | 继续把 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 里剩余的两块深水区做成代码事实。1) observation bus 第一阶段：新增 [`diagnostics/observation_bus.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/diagnostics/observation_bus.rs)，定义同步 `ObservationBus` 与 `ObservationEvent`，并让 [`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 从“字段写工具”收紧成“observation bus facade”，现有 `record_*` 全部先转成事件再统一 dispatch 到 runtime stats；这样 runtime stats 明确成为 observation 的投影，而不是每个方法各自落字段。2) recovery scheduler contract：新增 [`stack/recovery_scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/recovery_scheduler.rs)，定义 `RecoverySchedulerInput`（`StartupRetryTick` / `TransportSignal` / `IngressSignal`）与 `RecoverySchedulerDispatch`（`PublishRuntimeState` / `ExecuteRecoveryAction`）；[`recovery_driver.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/recovery_driver.rs) 改成先 `schedule_input()` 产出 dispatch，再由 [`session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs) 显式消费 dispatch，不再让 session 隐式依赖 driver 内部副作用顺序。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-810 | Register `codex_orchestrator_fullpack` orchestration assets | Done | 2026-03-19 | 2026-03-19 | Documented the orchestration skill, workflows, prompts, and templates in `docs/orchestrator-pack.md` so the designer pack is wired into the repo. |
| P-809 | 收口 recovery 与测试里的剩余 runtime stats 直锁访问 | Done | 2026-03-19 | 2026-03-19 | 继续把 `xbxengine-core` 内最后一批散落的 `runtime_stats.lock()` 收回 [`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs)。这轮通过子代理分两组完成：1) recovery 路径在 [`recovery_executor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_executor.rs)、[`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs)、[`startup_recovery.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/startup_recovery.rs) 中改为使用 sink 新增的 `read_shared()` / `update_shared()` helper，不再在恢复判定与 escalation observation 写回里直接锁 `runtime_stats`；2) [`api/runtime/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/tests.rs) 新增测试 helper，用 `RuntimeStatsSink::update()` 统一覆盖测试 runtime stats，替掉剩余 5 处直接 `*runtime_stats.lock() = ...`。本地验收确认 `crates/xbxengine/core/src` 内剩余 `runtime_stats.lock()` 现在只存在于 sink 模块本身，运行时代码不再散落直接锁访问。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-808 | 继续用 RuntimeStatsSink 收口 actor 链与 transport 侧剩余 runtime stats 直写 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 把 core 内观测写入口从“局部模块各自锁写”推进到“统一 sink 门面”。这轮先本地补了 [`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 的通用 `update()` / `read()` 能力，再让子代理分别在互不冲突的写集中完成替换：1) [`decode/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs)、[`pacer/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/pacer/actor.rs)、[`render/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/render/actor.rs) 现在都通过 `RuntimeStatsSink` 写 decode/pacer/render 统计，不再直接 `runtime_stats.lock()`；2) [`transport/core.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/core.rs)、[`transport/setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs)、[`transport/video_track_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_stats.rs)、[`transport/twcc_owned_receiver.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/twcc_owned_receiver.rs)、[`data_channel/channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs)、[`media/audio_output.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/media/audio_output.rs) 也统一改成通过 sink 落写 transport/TWCC/audio/data-channel 观测；3) 本地验收确认这九个文件中已无残留 `runtime_stats.lock()` 直写，剩余未收口点主要集中在 recovery/read 路径。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-807 | 用 RuntimeStatsSink 收口 stack 与 adapter 主链的 runtime stats 写入口 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 推进 core 内观测汇总入口的收口，先覆盖 `transport/adapter + transport/webrtc/stack` 这条视频主链热路径。1) 在 [`runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 继续作为统一写入口承接帧到达、stream dimensions、frame drop、recovery runtime state、NACK/gap/loss 相关 observation 的写入；2) [`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs) 的 `WebrtcVideoAdapter` 改为直接持有 `RuntimeStatsSink`，并在 [`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 与 [`nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 中移除散落的 `runtime_stats.lock()`，统一改走 sink；3) [`stack/session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs) 与 [`stack/recovery_driver.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/recovery_driver.rs) 同步对齐到 sink contract，修平此前 `RuntimeStatsSink` 接入后留下的类型错位。结果是 `stack + adapter` 主链上已经不再直接写 `runtime_stats.lock()`，为后续统一 observation bus 继续收口打底。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-806 | 将 session task body 压成 MediaSessionLoop 统一承接 tick 与事件分发 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 收紧 per-source 会话控制边界。在 [`stack/session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs) 新增 `MediaSessionLoop`，把 decode-drain tick、startup-retry tick、`FrameSourceEvent` 分发、ingress submit、observation 记录和 recovery driver 调用统一收进 `run()`；`spawn_media_session()` 不再直接内联大段 `tokio::select!` 状态机，而是只负责装配 actor 和构造 `MediaSessionLoop`。这样 session task 本身不再散落局部可变状态，更接近“单一会话控制对象”而不是临时闭包。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-805 | 将 per-source 视频事件循环抽成独立 session helper 并显式管理旧 session 生命周期 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 把 `media_supervisor` 压成更纯的 source/session 编排壳。1) 新增 [`stack/session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/session.rs)，把每个视频 source 的 actor 装配、ingress 生命周期、decode drain、observation/recovery helper 驱动和事件循环整体收进 `spawn_media_session()`；2) `media_supervisor.rs` 现在只负责接收新的 `FrameSource`、停掉旧 session、启动新 session，不再自己持有 per-source loop 的局部状态；3) 新增 `ActiveMediaSession`，在 source 切换时会同时停止 decode/pacer/renderer actor 并 `abort` 旧 session task，修掉旧实现里“新 source 挂载后旧 session task 仍可能继续跑”的生命周期漏洞。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-804 | 将 media supervisor 的 recovery/startup driver 抽离到独立 helper | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 收紧 `transport/webrtc/stack` 的职责边界。1) 新增 [`stack/recovery_driver.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/recovery_driver.rs)，把 `RecoveryCoordinator` 的构造、startup retry 轮询、`VideoRecoverySignal` / `VideoIngressSignal` 的 dispatch，以及 `publish_recovery_runtime_state` / `apply_recovery_decision` 相关执行逻辑从 [`media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 中提到 helper；2) `media_supervisor.rs` 现在只在事件循环里消费 `RecoveryDriver::handle_startup_retry()`、`handle_ingress_signal()`、`handle_transport_signal()`，不再自己拼 recovery runtime state 或直接持有 startup/retry 的策略细节；3) `stack.rs` 通过 `mod recovery_driver;` 暴露新 helper，不触碰 observation/frame stats 的逻辑。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-803 | 将 media supervisor 的 observation/stats helper 抽离到独立模块 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 收紧 `transport/webrtc/stack` 的职责边界。1) 新增 [`stack/observation.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/observation.rs)，把 media supervisor 中的帧率统计、recent receive window、ingress drop observation、transport escalation hint 节流和 stream width/height 回填收进单独 helper；2) `media_supervisor.rs` 只保留事件路由与 recovery driver 调用，`frame_count`、`frame_drop_observation_id`、`recent_receive_frame_times_ms`、`last_transport_escalation_hint` 等 observation 状态从主循环中移出；3) `stack.rs` 通过 `mod observation;` 暴露新 helper，不改 recovery/startup driver 的行为和边界。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-802 | 将 transport deadline 与 recovery signal dispatch 收口回各自分层 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 消除 `adapter` 与 `recovery` 的剩余语义漂移。1) 新增 [`transport/adapter/frame_cadence.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/frame_cadence.rs)，把原先放在 `transport/webrtc` 的 `FrameDeadlineTracker` 收回 adapter，并改成明确的 `TransportFrameDeadlineTracker` / `record_frame_arrival()` / `next_transport_deadline_for_value_at_ms()` 语义；对应移除 [`transport/webrtc/frame_deadline.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/frame_deadline.rs)，避免再把 transport NACK deadline 误表述成 playout target 跟踪。2) 在 [`recovery_diagnosis.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_diagnosis.rs) 为 `VideoRecoverySignal` / `VideoIngressSignal` 增加 `diagnose()`，并在 [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 新增 `RecoveryDispatch`、`runtime_state_for_label()`、`on_transport_signal_with_runtime_stats()`、`on_ingress_signal_with_runtime_stats()`，把 signal -> diagnosis -> decision -> runtime state 的翻译链收口到实时调度策略层。3) [`stack/media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 不再直接依赖 diagnosis helper，也不再自己拼 runtime state，只消费 coordinator 返回的 dispatch 结果。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-801 | 将 ingress budget 与 decode submit 回退语义进一步收口到 decode 前边界 | Done | 2026-03-19 | 2026-03-19 | 继续按 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 把 decode 前裁决点收紧到 `media/video/ingress` 一侧。1) 新增 [`ingress/budget.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/budget.rs)，把 `AssembledVideoFrame -> EncodedFrame` 的 playout budget/materialize 逻辑从 [`stack/media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 内联 helper 收回 ingress 模块，让 budget 语义显式归属 decode 前准入层；2) 在 [`ingress/scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 增加 `requeue_front()`，并把 [`decode/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs) 的 `submit()` 返回值改成携带原帧的 `DecodeSubmitError`，使 [`stack/media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 在 decode mailbox 竞态满队列时可以把原帧无拷贝退回 ingress，而不是在 `ingress.pop()` 之后静默丢失。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。本次未跑测试。 |
| P-800 | 按 streaming layer model 收紧 adapter frame contract 与 decode handoff | Done | 2026-03-19 | 2026-03-19 | 基于 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 继续收紧视频主链的“组帧/准入 -> decode”边界。此次主要做了两件事：1) 在 [`types.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/types.rs) 新增 `AssembledVideoFrame`，并把 [`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs) 的 `FrameSourceEvent::Frame` contract 改成只输出“已完成组帧但尚未赋予 playout deadline 的编码帧结果”；[`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 仍负责 sample builder、H.264 inspection、sample-loss/recovery 闸门和 transport deadline 跟踪，但不再把 `target_playout_time` 作为 adapter 层产物写死，最终 playout budget 改由 [`stack/media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 在进入 ingress 前统一 materialize；2) 在 [`decode/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs) 为 decode mailbox 增加显式 `available_slots()` 容量信号，并在 [`stack/media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 把原先“while ingress.pop() { try_send }”式 drain 改成 capacity-aware handoff：supervisor 只在 decode mailbox 有剩余预算时才从 ingress 出队，同时增加一个轻量 `decode_drain_tick` 周期性尝试出队，避免把 backlog 全部推进 decode 再靠 `try_send` 报满丢帧。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。尝试运行 `cargo test -p xbxengine video_ingress_reconfigure -- --nocapture`，但环境仍缺少 `cmake`，导致 `audiopus_sys` build script 失败，测试未能完成。 |
| P-799 | 按 streaming layer model 对 transport supervisor 与 trace projection 做首轮结构收口 | Done | 2026-03-19 | 2026-03-19 | 基于 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 的分层约束，对当前实现做了首轮“结构收口但不改运行时语义”的重构。1) 在 `transport/webrtc/transport` 新增 [`video_track_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/video_track_stats.rs)，把原本挤在 [`setup.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs) 的视频 track stats/BWE/REMB loop 抽到专门 helper，使 on-track callback 更聚焦于网络接入、track 挂载和 adapter 创建；2) 在 `transport/webrtc/stack` 新增 [`stack/media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs)，把 frame-source loop、ingress drop 观测、startup retry 与 recovery 转发从 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) 中迁出，让 `XbxActiveMediaStack` 更接近组合根；3) 在 `src-tauri/src/mods/xbxengine` 新增 [`trace_projection.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/trace_projection.rs)，把 [`runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/runtime_state.rs) 中大段 trace event/state/snapshot 映射收拢为旁路 helper，宿主 tick 只保留节流与调用。验证：`cargo fmt --all`、`cargo check -p xbxengine -p xbxrc`。尝试运行 `cargo test -p xbxengine ...`，但环境缺少 `cmake`，导致 `audiopus_sys` build script 失败，测试未能完成。 |
| P-798 | 复核 xbxengine-core 视频主链与 streaming layer model 在边界与背压上的偏差 | Done | 2026-03-19 | 2026-03-19 | 对照 [`webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md) 复核 `transport/webrtc + transport/adapter + media/video` 当前实现，聚焦网络接入层、组帧/准入层、`stack.rs` 组合根职责与 push/backpressure 边界。结构性结论有五点：1) [`transport/adapter/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 现在同时承担 `track.read_rtp/sample_builder`、H.264 inspection、sample-loss 恢复判定、waiting-keyframe、以及 playout delay 计算和 `EncodedFrame` 生成，已经把“网络接入层只产出媒体样本与 transport 事实”和“组帧/准入层作为 decode 前闸门”的职责揉在一起；2) [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) 除了媒体栈组合根，还承担了 actor 装配、视频 supervisor loop、跨层 stats 写入、ingress->recovery diagnosis 映射和 recovery executor 调用，职责明显过重；3) [`decode/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs)、[`pacer/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/pacer/actor.rs)、[`render/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/render/actor.rs) 仍是 `sync_channel(2/2/1)` 级联 bounded push mailbox，且 drop 主要发生在 decode 之后，偏离了文档要求的“backlog 控制尽量前移到组帧/准入层、网络层不能等待 decode/render”；4) [`transport/adapter/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/mod.rs) 当前 `FrameSource` contract 直接输出 `EncodedFrame`，使 adapter 天然成为“网络接入层 + 组帧/准入层”的混合层，不利于未来把样本组装和 admission 解耦；5) [`media/video/ingress/scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 虽已具备 bootstrap/backlog/reconfigure 的 decode 前闸门语义，但由于 adapter 先做了一轮 inspection/recovery/playout deadline，ingress 还没有成为文档定义下唯一的 decode 前裁决点。建议优先级：先拆 adapter contract 和 `EncodedFrame` 生成边界，再把 `stack.rs` 中的视频 supervisor/recovery wiring 抽成更窄的 video pipeline coordinator，最后引入显式 decode budget/backpressure handoff，避免继续靠多级 `try_send` 丢帧来维持线程隔离。本次只完成结构评估归档，没有修改代码，也未跑测试。 |
| P-797 | 将 xbxengine-core 必须遵循视频分层开发指导写入 AGENTS 约束 | Done | 2026-03-19 | 2026-03-19 | 在 [`AGENTS.md`](/Users/guo.xu/Documents/code/games/xbxrc/AGENTS.md) 的 `Development Policy` 中新增硬约束：`crates/xbxengine/core` 的开发、重构与架构判断必须遵循 [`dev-docs/webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md)。约束明确要求，凡是涉及网络接入、组帧与准入、解码、渲染呈现、观测汇总、实时调度策略的改动，都必须先按该文档判断层归属、输入输出 contract、交互模型与 Moonlight 对照口径，并禁止偏离当前 `webrtc-rs` 主线去生搬硬套 Moonlight 的 RTSP/RTP/FEC 结构。本次只更新仓库协作规范与任务跟踪，没有修改运行时逻辑，也未跑测试。 |
| P-796 | 将 webrtc-rs 视频主链六层模型落盘为稳定开发指导 | Done | 2026-03-19 | 2026-03-19 | 按前一轮架构评估结论，将当前 `webrtc-rs` 视频主链的六层模型正式整理为稳定开发指导，新增 [`dev-docs/webrtc-streaming-layer-model.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/webrtc-streaming-layer-model.md)。文档明确了两条总原则：1) 以当前主线为准，围绕 [`backend.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/backend.rs) / [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) 以及 `adapter -> ingress -> decode -> pacer -> render -> diagnostics/runtime_state` 来定义分层；2) 借鉴 Moonlight 的线程隔离、`pull model`、bounded queue、controlled drop、quick recovery 等控制哲学，但不把其 RTSP/RTP/FEC/SDL 主循环结构照搬成当前事实。文档对六层分别给出了职责边界、核心目标、输入/输出 contract、执行/交互模型，以及“可直接借鉴 / 必须改写”的对照清单；同时单列“线程模型与层交互”章节，明确 Moonlight 的 `LiWaitForNextVideoFrame()` pull handoff、`CAPABILITY_PULL_RENDERER`、`PACER_MAX_OUTSTANDING_FRAMES` 与当前主线 actor + bounded push 的对应关系，要求实现时始终保持“网络不等解码、解码不等渲染、观测旁路消费”的隔离原则。本次仅新增开发指导文档并更新任务跟踪，没有修改运行时逻辑，也未跑测试。 |
| P-795 | 评估 xbxrc 当前 webrtc 主线与 Moonlight 在策略层、观测汇总、中央调度与分层调度上的架构对齐可行性 | Done | 2026-03-19 | 2026-03-19 | 本次把评估重点从“单个 runtime 参数”扩展为当前 webrtc-rs 主线与 Moonlight 架构原则的系统对齐可行性。对照 [`STREAMING_ARCHITECTURE_ANALYSIS.md`](/Users/guo.xu/Documents/code/games/moonlight-qt/STREAMING_ARCHITECTURE_ANALYSIS.md) 与当前实现核对后，结论是：1) 结构上不适合照搬 Moonlight 的 RTSP/RTP/FEC 栈，但在语义上高度可对齐，尤其是“分场景分档、受控丢帧、坏参考链快速恢复、观测驱动调度”这四条；2) 当前仓库已经存在两级中央控制雏形，[`lifecycle.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/lifecycle.rs) 的 `runtime.tick()` 负责会话级编排，[`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) + [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 负责媒体级 supervisor/recovery，因此正确方向是强化这两个中心的分工，而不是再造第三个全局调度器；3) 当前最接近 Moonlight 的层是 [`scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 与 [`pacer/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/pacer/actor.rs)，而偏差最大的层是 [`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/source.rs) 与 [`decode/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs) 的 push 型背压边界；4) 最值得优先做的改进不是 Tokio 线程数，而是把 backlog/budget 控制前移到 adapter+ingress+decode 边界、补“启动前事实 + 建链后 transport 事实”的两阶段策略切档，并把宿主侧 [`runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/runtime_state.rs) 的分层 trace 汇总继续收束成稳定的 media health snapshot。此次仅完成架构评估与结论归档，没有修改运行时逻辑，也未跑测试。 |
| P-794 | 基于 Moonlight 架构总结评估 xbxrc 场景下 Rust runtime 调优方向 | Done | 2026-03-19 | 2026-03-19 | 对照 [`STREAMING_ARCHITECTURE_ANALYSIS.md`](/Users/guo.xu/Documents/code/games/moonlight-qt/STREAMING_ARCHITECTURE_ANALYSIS.md) 的低延迟原则复核当前实现，结论是这次不应把重点放在 Tokio worker/runtime 线程数，而应继续围绕“按场景分档 + 允许受控丢帧/清队列 + 更快恢复”调优 Rust-owned 媒体栈。现状上，[`compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) 已经部分对齐 Moonlight：Cloud Rust-owned 复用更宽的 browser-style `video_pipeline`，但保留更激进的 sidecar recovery；[`policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/policy.rs) 也已按 `HomeLan/Cloud/Relay` 分档 BWE 与 recovery profile。真正仍偏离 Moonlight 的点主要有三类：1) decode/pacer/renderer 当前仍是 `sync_channel(2/2/1)` 的 push 链，虽然已经会丢帧，但 trace 里仍出现 `pacer queue full` / `renderer queue full`，说明 backlog 是在解码后才爆，尚未像 Moonlight 的 pull-model 那样把背压尽量隔离在 decode 之前；2) Tokio runtime 仍是默认 `new_multi_thread().enable_all()`，当前代码没有证据表明线程数是瓶颈，贸然调大只能增加调度噪声，收益远小于继续收紧视频队列与 startup/decode 恢复；3) Moonlight 会把网络类型、MTU、音频 backlog drop、decoder fallback/能力探测都前置成显式策略，而我们现在主要只在 video pipeline / BWE / recovery 上做分档，音频与解码器选择仍有补齐空间。建议优先级：先继续收敛 H.264 bootstrap/坏 AU 过滤、把 backlog 控制前移到 ingress/decode 边界、补 Home direct vs Home relay 的更细 video pipeline 档位；Tokio runtime 若要动，也只建议做小范围实验，例如固定较小 `worker_threads` 与命名线程便于观测，而不是把它作为主调优方向。本次为评估结论归档，没有修改运行时逻辑，也未跑测试。 |
| P-793 | 快速回收 Rust `target` 空间并抑制后续开发构建体积持续膨胀 | Done | 2026-03-19 | 2026-03-19 | 先检查 `target` 体积来源，确认主要占用集中在 `target/debug/deps`（约 25G）与 `target/debug/incremental`（约 18G）。为快速回收空间，删除 `target/debug/incremental`，将整体 `target` 从约 46G 降到约 34G，避免触碰 `deps` 导致下一次开发构建大规模重编。随后在 [`Cargo.toml`](/Users/guo.xu/Documents/code/games/xbxrc/Cargo.toml) 调整开发 profile：把 `debug` 从 `true` 改为 `line-tables-only`，仅保留基础行号信息以减小后续调试产物；新增 `incremental = false`，阻止 `target/debug/incremental` 继续累积为十几 GB 的历史缓存。验证：`du -sh target target/debug target/debug/deps target/debug/build`、`cargo check -p xbxrc`。 |
| P-792 | 修复 control data channel close/reopen 后 recovery 仍认为 control 不可用的问题 | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773827525075.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773827525075.jsonl) 继续追查 startup 恢复失败，发现 trace 出现了明显矛盾：已经打印 `bootstrap control before message handshake ack`，但紧接着 recovery pending 日志仍报告 `control_started=false/control_open=false`。对照代码确认根因在 data channel 生命周期：[`bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/bootstrap.rs) 只在 `install_data_channel_contracts()` 初始安装时向 `state.control_channel` 写入一次 control channel；而 `reset_state_on_channel_close()` 在 control close 时会把它清成 `None`。若 control 在 startup/recovery 期发生短暂 close/reopen，`on_open` 之前并不会重新回填该引用，导致 [`channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs) 的 `recovery_requests_ready()` 永远判断 `control_open/control_ready=false`，所有 keyframe/decoder-reset 请求都只能 pending，最终演化成 `adapterIdleTimeout + transportExpiredDeadline`。此次修复在 control channel 的 `on_open` 分支显式重新绑定 `state.control_channel = Some(control_channel.clone())`，并补一条 `rebound control channel reference after open` 日志，确保 close/reopen 后 recovery 再次可见真实 control channel。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine startup_fallback_allows_recovery_when_handshake_not_acked_and_control_open -- --nocapture`。 |
| P-791 | 补齐 recovery 执行与 control-channel 发送链路日志，区分“做了决策”与“消息真正发出” | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773826621378.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773826621378.jsonl) 的继续分析，发现 `requestKeyframe` 决策与 `channelMessageCatalog(videoKeyframeRequested)` 之间仍存在观测断层，因此补强 recovery/control 双侧日志，不改业务策略，只提升下一轮 trace 的可解释性。1) 在 [`recovery_executor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_executor.rs) 为每次 recovery action 增加统一入口日志，输出 `observation_id`、`action`、`reason`，以及 `control_ready/control_open/message_handshake_acked/control_bootstrapped/pending_*` 等状态；同时在 keyframe/decoder-reset 分发路径上分别记录“走 actionable control channel / 走 state-gated fallback / 因 pending 跳过 / 实际发送成功或失败”。2) 在 [`channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs) 为 `request_video_keyframe_on_control_channel` 和 `request_decoder_reset_on_control_channel` 增加发送前后日志，明确记录 control open 与 recovery protocol readiness；对 `request_*_from_state` 的 pending 排队日志也补充同一组状态快照，不再只有一句抽象的 “until recovery protocol is ready”。3) 在 [`bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/bootstrap.rs) 为 pending flush 与 delayed keyframe prime 增加发送结果日志，便于区分“启动补发”与“常规 recovery 执行”的来源。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine startup_fallback_allows_recovery_when_handshake_not_acked_and_control_open -- --nocapture`。 |
| P-790 | 补齐 startup/reconfigure 观测：输出 ingressReconfigure 具体原因并修正 recovery keyframe 请求计数 | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773824555865.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773824555865.jsonl) 的后续分析，补上两条关键观测链路。1) 在 [`scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 增加 `describe_reconfigure_reason()`，统一把 `parameterSetsChanged` / `dimensionsChanged` / `codecChanged` 折叠成稳定原因串；[`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) 在记录 `latest_video_frame_drop` 时，对 `IngressDecision::Reconfigure` 改写成 `reconfigure:<reason>`，让 runtime trace 直接暴露具体触发源，不再只有模糊的 `reconfigure`。2) 在 [`sync.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/sync.rs) 新增 `sync_recovery_snapshot()`，把 runtime snapshot 的 `recovery_keyframe_request_count` 改为同步 `XbxEngineMediaRuntimeStats.video_pli_request_count_total`，修复“日志里已经发过 `requestKeyframe`，但 snapshot 长期显示 0” 的统计断线。补齐单测：[`scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 验证 reconfigure 原因优先输出参数集/尺寸变化组合；[`tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/tests.rs) 验证 runtime snapshot 会随 media stats 的 PLI 计数更新。验证：`cargo fmt --all`、`cargo test -p xbxengine describe_reconfigure_reason_prefers_parameter_sets_dimensions_and_codec -- --nocapture`、`cargo test -p xbxengine runtime_syncs_keyframe_request_count_from_media_stats -- --nocapture`、`cargo check -p xbxengine`。 |
| P-789 | 完成 H.264 parser-backed 首帧准入改造，统一 adapter/ingress/decode 语义并引入 h264-reader 0.8.0 | Done | 2026-03-18 | 2026-03-18 | 按方案引入官方最新 `h264-reader = 0.8.0`，新增共享 [`crates/xbxengine/core/src/media/video/h264/inspection.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/h264/inspection.rs) 作为 Annex B access unit inspection 入口，统一输出 `bootstrap_ready` / `MissingSps|MissingPps|InvalidSliceHeader` 等 verdict；`WebrtcVideoAdapter`、`VideoIngress` 和 `VideoToolbox` 解码路径改为消费同一份 inspection 结果，首帧准入仅允许“干净 IDR + 完整 SPS/PPS + 无 sample loss + 语法有效”进入硬解，AVCC sample 由 inspection 过滤后的 NAL 生成，避免把 in-band SPS/PPS/AUD 直接喂进 VT。后续收尾补强：adapter 侧对 `slice_headers_valid=false` 的 AU 实施硬拦截，统一复用现有 `TransportAwaitRecoveryKeyframe` 语义，不再让语法无效 AU 流入 ingress/decode。补齐单测覆盖 `IDR+SPS+PPS` 可 bootstrap、缺 SPS/PPS 不可 bootstrap、sample loss 不可 bootstrap、参数集变化触发 reconfigure/等待 keyframe、以及 decode 侧不混入参数集 NAL。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine h264 -- --nocapture`、`cargo test -p xbxengine invalid_slice_header_au_requests_wait_keyframe_recovery -- --nocapture`、`cargo test -p xbxengine waiting_keyframe_requires_clean_bootstrap_frame -- --nocapture`、`cargo test -p xbxengine bad_data_failure_waits_for_next_keyframe_before_decoding_again -- --nocapture`。 |
| P-788 | 复核 cloud Rust-owned 新 trace，确认首帧失败根因仍在自有解码链而非恢复策略 | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773822299313.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773822299313.jsonl) 复核 cloud Rust-owned 最新运行，确认上一次把 `recovery` 回退到 sidecar 档已经生效，但首帧坏帧问题并未消失：1) `compiledPlan` / `sessionExecutionStarted` / `StartRuntime` 都显示当前生效参数是“browser-style `videoPipeline` + sidecar `recovery`”，即 `first_frame_grace_ms=1800`、`keyframe_request_stall_ms=300`、`reconnect_stall_ms=2400` 已恢复，同时 `jitter_buffer_max_delay_ms=30`、`nack_max_age_ms=200` 仍保持放宽；2) 在真正首帧出画前，decoder 已连续收到大量尺寸异常分散的 startup frame，并伴随 `pacer queue full` / `renderer queue full`，随后仍然直接命中 `xbxEngineVideoToolboxOutputCallbackFailed:status=-12909` 并进入 `wait-keyframe recovery`；3) 之后又出现 `media loss detected ... dropped_packets=2305`、`transportSampleLoss` 和持续 `transportExpiredDeadline`，说明“放宽 video pipeline”并没有消掉后续传输退化，但更关键的是它也没有解决更早发生的首帧坏 AU/坏参考链问题。结论：当前主因仍是 Rust-owned 自有 H.264 access unit 组装/首帧准入/VideoToolbox 喂帧链路过早接受了不可安全解码的 startup 数据，而不是 cloud recovery 策略仍然错误。本次只完成日志核对与结论归档，没有修改代码，也未跑测试。 |
| P-787 | 收紧 Tauri 控制台日志默认过滤，去掉 `webrtc_srtp::session` 刷屏输出 | Done | 2026-03-18 | 2026-03-18 | 用户已明确依赖 `runtime-logs/*.jsonl` 做回归，不需要终端继续承载第三方 info 噪声。此次在 [lib.rs](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/lib.rs) 调整 `env_logger` 初始化：1) 将默认控制台过滤从全局 `info` 收紧为 `warn`，同时保留 `xbxrc_lib=info` 以继续输出本应用关键 info；2) 显式把 `webrtc_srtp::session` 限制到 `warn`，去掉诸如 `srtp ssrc=... duplicated` 的高频刷屏日志；3) 仍保留 `RUST_LOG` 覆盖入口，后续若需临时放开终端调试无需改代码。验证：`cargo check -p xbxrc`。 |
| P-786 | 修正 cloud Rust-owned 策略放宽后“画面出不来”的首帧回归 | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773821820776.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773821820776.jsonl) 回归排查，确认这次异常不是建链失败，而是我把 cloud Rust-owned 的 `recovery` 也一起放宽到浏览器档，导致 sidecar 首帧前/坏参考链恢复节奏被拖慢：trace 里 `sessionExecutionStarted` / `StartRuntime` 已明确使用 `first_frame_grace_ms=8000`、`keyframe_request_stall_ms=1500`、`reconnect_stall_ms=4000`，随后很快就命中 `hardware decode failed ... -12909` 并进入 wait-keyframe，但没有及时按 sidecar 节奏拉 keyframe/reset，因此用户体感是“画面出不来”。此次在 [compiler.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) 回退 cloud Rust-owned 的 `recovery` 到原 sidecar 参数，只保留 `video_pipeline` 的 browser-style 放宽：1) `compile_recovery` 恢复为仅按 `RuntimeMode` 分支，Rust-owned 统一回到 `first_frame_grace_ms=1800`、`keyframe_request_stall_ms=300`、`decoder_reset_after_keyframe_wait_ms=120`、`reconnect_stall_ms=2400`；2) cloud Rust-owned 仍保持放宽后的 `video_pipeline`，继续验证更宽的 jitter/NACK 窗口是否能收敛 `transportExpiredDeadline`；3) 单测更新为“cloud 只放宽 video pipeline，但 recovery 必须保持 sidecar 档”。验证：`cargo fmt`、`cargo test -p xbox-streaming rust_owned_cloud_aligns_video_pipeline_but_keeps_sidecar_recovery_profile -- --nocapture`、`cargo test -p xbox-streaming rust_owned_home_keeps_low_latency_sidecar_profile -- --nocapture`、`cargo check -p xbox-streaming`。 |
| P-785 | 将 Rust-owned 的 Cloud runtime 策略先放宽到浏览器档位以验证稳定性 | Done | 2026-03-18 | 2026-03-18 | 按最新排查结论，先不继续“猜更细的恢复参数”，而是把 `Target::Cloud + RuntimeMode::RustOwned` 的 runtime compiler 策略临时对齐到浏览器档位，验证问题是否主要来自过窄的 sidecar 媒体恢复窗口。此次在 [compiler.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) 把 `compile_video_pipeline` / `compile_recovery` 改为接收 `Target`：1) `Cloud + RustOwned` 直接复用 `WebRtcDirect` 的视频 pipeline 与 recovery 门限，包含 `nack_max_age_ms=200`、`jitter_buffer_max_delay_ms=30`、`late_frame_drop_threshold_ms=500`、`first_frame_grace_ms=8000`、`reconnect_stall_ms=4000` 等；2) `Home + RustOwned` 继续保留原来的低时延 sidecar 档位，不跟着放宽；3) 新增两条单测分别锁住“cloud 必须对齐浏览器档”和“home 必须保持原档”。验证：`cargo fmt`、`cargo test -p xbox-streaming rust_owned_cloud_aligns_video_pipeline_and_recovery_with_browser_profile -- --nocapture`、`cargo test -p xbox-streaming rust_owned_home_keeps_low_latency_sidecar_profile -- --nocapture`、`cargo check -p xbox-streaming`。 |
| P-784 | 追查为何同网络/同游戏/同配置下浏览器 runtime 正常而 Rust-owned runtime 进入 `transportExpiredDeadline` | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773819626069.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773819626069.jsonl) 与 runtime policy / adapter 代码对照，结论不是“浏览器走了另一条网络”，而是两套媒体恢复策略容错差异明显：1) trace 里 Rust-owned 会话已经成功建链并稳定出画，路径是 `Direct (srflx->host)`，随后在同一连接上出现 `rtpWindow` 视角的 47 包缺口，并在极短窗口内被判成 `expiredMaxAge/expiredDeadline`，触发 `transportExpiredDeadline`；但同一时刻 TWCC 仍报告 `delivery_ratio=1.0`、`packet_loss_ratio=0.0`、`receive_bitrate_kbps≈7797`，说明更像 RTP 重排/突发抖动超出了本地 adapter 窗口，而不是网络瞬时完全断流。2) policy 明确把 `WebRtcDirect` 和 `RustOwned` 编译成两档完全不同的视频 pipeline：浏览器档 `nack_max_age_ms=200 / jitter_buffer_max_delay_ms=30 / nack_burst_count=12 / late_frame_drop_threshold_ms=500`，Rust-owned 档只有 `24 / 8 / 4 / 180`，recovery 侧的 stall / keyframe / reconnect 门限也更激进；浏览器 runtime 因为 transport/decode/render 都归 `Owner::Browser`，实际走的是浏览器原生 WebRTC jitter buffer/NACK/解码栈，因此在同样网络下能把这类 burst loss / reordering 吃住，而 Rust-owned 当前 cloud profile 会更早把参考链判坏并进入恢复。此次只完成日志与代码核对，没有修改运行时逻辑，也未跑测试。 |
| P-781 | 修复 webrtc 0.17 升级后 VideoToolbox `-12909` 连发时 decoder reset 仍继续吃预测帧的问题 | Done | 2026-03-18 | 2026-03-18 | 基于 [`runtime-trace-1773818190849.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773818190849.jsonl) 继续收敛“已经首帧出画、随后持续 `xbxEngineVideoToolboxOutputCallbackFailed:status=-12909`”问题。此次先确认 `-12909` 在本机 SDK 中对应 `kVTVideoDecoderBadDataErr`，说明根因仍是本地解码器看到坏 access unit / 坏参考链，而不是不支持新 webrtc 版本。针对当前恢复缺口，在 [video_decode.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/video_decode.rs) 把 bad-data / reference-missing 失败收敛成“本地立即 reset + 进入 wait-for-keyframe”：1) `XbxVideoDecodeState` 新增 `waiting_for_recovery_keyframe`，`request_decoder_reset()` 后不再允许预测帧直接重进新 decoder session；2) 遇到 `status=-12909/-17694` 时，本地直接 `request_decoder_reset()`，清空已解码队列并把后续非 keyframe 全部拦在 decoder 入口外，直到下一张 keyframe 才恢复送解；3) 新增单测锁住“bad-data 后必须等待下一张 keyframe 才再次调用 decoder”这一语义，避免后续回退成 `reset 后继续吃 delta 帧`。验证：`cargo test -p xbxengine bad_data_failure_waits_for_next_keyframe_before_decoding_again -- --nocapture`、`cargo test -p xbxengine request_decoder_reset_calls_hardware_decoder_reset -- --nocapture`、`cargo check -p xbxengine`。 |
| P-782 | 确认 webrtc 0.17 升级后 Rust-owned 运行时是否已经能拿到 RTT | Done | 2026-03-18 | 2026-03-18 | 结论：代码路径上“能拿”，但这份 [`runtime-trace-1773818190849.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773818190849.jsonl) 实测“没拿到”。当前 `webrtc = 0.17.1`，在 [setup.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs) 已按 `RemoteInboundRTP.round_trip_time -> selected CandidatePair.current_round_trip_time -> CandidatePair avg RTT -> any CandidatePair RTT -> nack-recovery` 的顺序做 RTT 采集并写入 `video_rtt_ms/video_rtt_source`，前端 DTO 也会把它格式化到 `rtt` 字段；但 trace 中 `rtt`、`video_rtt_source`、BWE 里的 `rttMs` 全程为 `null/""`，说明这次运行里 `webrtc-rs` stats 没有产出可用 RTT 样本。此次仅完成代码与日志核对，没有改动运行时逻辑，也未新增测试。 |
| P-783 | 让 Rust-owned WebRTC RTT 真正参与 BWE/REMB 决策 | Done | 2026-03-18 | 2026-03-18 | 基于前面对 `webrtc 0.17.1` 依赖源码的核对，确认 RTT 主来源应为 `RemoteInboundRTP.round_trip_time`（RTCP RR/XR 反推），而我们自己的 BWE 链路此前虽然把 `video_rtt_ms` 写进 runtime stats / observation，但 `resolve_target_remb_kbps` 完全没有消费该输入，导致 RTT 只能显示、不能决策。此次在 [setup.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/transport/setup.rs)、[bwe_policy.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/bwe_policy.rs)、[policy.rs](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/policy.rs) 把 `rtt_ms` 从采样环显式传入 BWE policy，并按 Home/Cloud/Relay 三类 profile 增加 `high_rtt_ms_threshold` / `severe_rtt_ms_threshold`：1) `twcc-gcc` 在 TWCC 干净但 RTT 明显偏高时会执行 `high-rtt-hold`，严重 RTT 时执行 `severe-rtt-backoff`；2) `hybrid` 在没有 TWCC 观测时，也能仅凭 RTT 进入 hold/backoff，不再只依赖 loss；3) 单测补齐“clean TWCC + high RTT 必须阻止 ramp-up”和“无 TWCC + severe RTT 必须回退”两条语义。验证：`cargo fmt`、`cargo test -p xbxengine direct_high_rtt_holds_ramp_up_even_when_twcc_is_clean -- --nocapture`、`cargo test -p xbxengine hybrid_bwe_caps_to_actual_headroom_when_loss_is_sustained -- --nocapture`、`cargo test -p xbxengine hybrid_bwe_backoffs_on_severe_rtt_without_twcc -- --nocapture`、`cargo check -p xbxengine`。 |
| P-872 | 回归 cloud 策略放宽后的 `runtime-trace-1773993181762` 卡死原因 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773993181762.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773993181762.jsonl) 回归最新 cloud baseline 放宽后的实际表现。结论：1) 放宽后的 cloud 策略确实让会话先进入稳定出画阶段，`seq 401-413` 期间 runtime 处于 `cloudGaming/startup/startupLow/startupLowQuality`，`twccFeedbackSent.deliveryRatio≈0.97`、`receiveBitrate≈2.7Mbps`，并且 reinject 机械链路至少成功过一次，summary 明确显示 `reinject:stage=adapterRead seq=11290 pending=0`；2) 但关键首洞 `11680` 的 packet-level deadline 依然很短，trace 中第一次 `nackSent` 的 `deadlineAtMs=1773993210111.8994` 对应观测时间 `1773993210010`，实际只有约 `102ms`，随后从 `seq 437` 开始反复进入 `nackExpired(action=expiredDeadline)` 和 `cooldownSuppressed`；3) 到 `seq 760-788`，runtime 已退化为 `cloudGaming/startup/paused/healthy`，`presentAgeMs` 超过 `1s`、`reinject:stage=queued pending=117..150`，而关键 NACK 仍固定在 `11680..11680`，说明本轮 stall 不是“完全没出画”，而是出画后在 startup 期被过早的 head-hole deadline 杀死；4) 因而当前 cloud 卡死的主因已从“窗口普遍太窄”收敛为“startup 阶段仍沿用过于激进的首洞 packet deadline / escalation 语义”，下一步应直接重构 cloud startup 下的 head-hole deadline 和 `transportExpiredDeadline` 门槛，而不是继续追 RTX reader 机械链路。 |
| P-873 | 收紧 cloud startup 的 transportExpiredDeadline 升级门槛为连续失败预算 | Done | 2026-03-20 | 2026-03-20 | 在 [`crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 中加入 session-local 的 cloud startup 失败预算：仅当 `session_target_type=Cloud` 且 `session_phase=Startup` 时，对 `latest_video_nack_observation.action=="expiredDeadline"` 的同一 head-hole 连续失败进行计数，在 `1_200ms` 观察窗内累积到 `3` 次才从 `CooldownSuppressed` 升级到 `RequestKeyframe`；前两次保持抑制，避免单个首洞短时 miss 直接把 cloud startup 调度层打死。补充单测锁住“前两次 suppress、第三次 requestKeyframe”以及非 startup / 非 cloud 不受影响。验证：`cargo test -p xbxengine recovery_coordinator -- --nocapture`、`cargo check -p xbxengine --quiet`。 |
| P-873 | 实现 cloud startup 的首洞宽窗与连续失败预算 | Done | 2026-03-20 | 2026-03-20 | 按最新 cloud trace 的结论，直接实现两块策略收敛：1) 在 [`nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/nack.rs) 引入 `is_cloud_startup_transport_profile()` 与 `CLOUD_STARTUP_HEAD_HOLE_DEADLINE_FLOOR_MS=320ms`，将 `rtpGap/rtpWindow/sampleLoss` 的 head-hole deadline、`max_age_ms`、`retry_interval_ms`、`max_tracked_sequences` 按 `cloud+startup` 单独放宽，且缺口收集改为保留前部首洞窗口；同时保留 `cloud steady` 和 `home` 的分离语义。2) 在 [`recovery_coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/recovery/recovery_coordinator.rs) 为 `cloud+startup+expiredDeadline` 增加连续失败预算：`1_200ms` 观察窗内前两次继续 `CooldownSuppressed`，第三次才允许按 `TransportExpiredDeadline` 升级，避免刚出画就被单个首洞 miss 打死。3) 同步补齐/更新 [`transport/adapter/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/adapter/tests.rs) 与 recovery coordinator 单测，锁住 `cloud startup` 比 `cloud steady/home` 更宽的首洞策略，以及“前两次 suppress、第三次升级”的恢复语义。验证：`cargo test -p xbxengine transport::adapter::tests -- --nocapture`、`cargo test -p xbxengine recovery_coordinator -- --nocapture`、`cargo test -p xbox-streaming rust_owned_cloud_aligns_video_pipeline_but_keeps_sidecar_recovery_profile -- --nocapture`、`cargo check -p xbxengine -p xbxrc -p xbox-streaming`。 |
| P-874 | 回归 `runtime-trace-1773994832171`：画面出不来已转为 control/bootstrap 就绪错位 | Done | 2026-03-20 | 2026-03-20 | 基于 [`runtime-trace-1773994832171.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1773994832171.jsonl) 回归最新 cloud 调整后的失败模式。结论：1) 这次不再是单纯的 cloud 首洞/NACK deadline 过紧；trace 中没有 `videoRtpStarted`、没有 `StatsVideoFrameProcessed`、也没有正常媒体已连接后的统计推进，但 [`XbxDecodeActor`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs) 已持续 `processing frame`，说明解码线程实际上已经在吃数据；2) repair/RTX 观测仍然工作，存在 `queued primary packet` 与 `primaryReadPoll`，但 recovery 控制协议始终未就绪，日志明确反复出现 `control_ready=false, handshake_acked=false, control_started=false, control_bootstrapped=false, control_open=false`，导致 `keyframe request` 与 `decoder reset` 只能排队，无法真正执行；3) 随后 adapter 侧持续出现 `media loss detected before sample`，并进入 `transportSampleLoss -> transportAwaitRecoveryKeyframe -> transportExpiredDeadline`，最终表现为“画面直接出不来”。因此当前排查重点应从 cloud packet-level 参数转向 [`data_channel/channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs)、[`data_channel/bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/bootstrap.rs) 以及首帧/媒体 ready 状态建立链路，查明为何 decode 已跑、但 control/bootstrap 与媒体就绪状态未能完成闭环。本次仅完成 trace 分析，没有改运行时逻辑，也未新增测试。 |
| P-875 | 修复 data-channel startup fallback 与 `request_*_from_state()` 判定不一致 | Done | 2026-03-20 | 2026-03-20 | 在 [`data_channel/channel.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs) 中将 `request_video_keyframe_from_state()` 与 `request_decoder_reset_from_state()` 的 control channel 选择逻辑改为复用统一的 `actionable_control_channel_locked()`，与 [`RecoveryActionDispatcher::actionable_control_channel()`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/data_channel/channel.rs) 保持一致。这样在 `message handshake` 尚未 ack、但 `control` 已 open 的 startup fallback 场景下，delayed prime 与恢复动作不再永远只排进 `pending_keyframe_request/pending_decoder_reset`，而能与 recovery executor 走同一条“可执行控制通道”判定。此次没有改动 `recovery_requests_ready()` 的正式语义，只修复了请求入口与 dispatcher 的判定分叉。验证：`cargo test -p xbxengine startup_fallback -- --nocapture`、`cargo check -p xbxengine -p xbxrc`。 |
| P-896 | 收敛 keepalive `HTTP 410 SessionNotActive` 的重连风暴并转为会话终态 | Done | 2026-03-21 | 2026-03-21 | 在 [`lifecycle.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/lifecycle.rs) 增加 `is_terminal_remote_session_inactive_error()` 分类：当 `KeepAliveRemoteSession` 返回 `HTTP 410 / SessionNotActive` 时，不再继续 `recoverTransportReconnectCandidateFailed` 循环，而是记录 `recoverTransportReconnectSessionNotActive` 错误、将 `last_recovery_action` 标记为 `reconnectSessionInactive`，并立即执行 `stop()` 收敛到 `Stopped/Closed` 终态，避免会话已失效时继续做候选重连风暴。同时在 [`runtime/tests.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/tests.rs) 扩展 `TestHostBridge` 的自定义 keepalive 失败消息能力并新增回归用例 `runtime_stops_reconnect_loop_when_keepalive_reports_session_not_active`，验证 410 场景会停机收敛且发出新错误码。验证：`cargo test -p xbxengine --lib runtime_stops_reconnect_loop_when_keepalive_reports_session_not_active -- --nocapture`、`cargo test -p xbxengine --lib runtime_consumes_pending_transport_reconnect_candidate_once -- --nocapture`、`cargo test -p xbxengine --lib reconnect_failure_restores_running_state -- --nocapture`。 |
| P-897 | 基于协商后的 answer SDP/PT 做 RTC 媒体包分类，修复 Opus(111) 被误归为视频 | Done | 2026-03-21 | 2026-03-21 | 在 [`packet_router.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/packet_router.rs) 新增 `RtcPayloadRouteMap` 与 `parse_payload_route_map_from_answer()`：按 answer SDP 的 `m=`/`rtpmap`/`fmtp apt=` 构建动态 PT 路由，视频侧区分 primary 与 `RTX/RED/ULPFEC/FlexFEC` repair codec，分类优先级改为“先走 SDP/PT 映射，再退回 track hint/旧 fallback”。在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/service.rs) 增加 `apply_remote_answer_sdp()` 并在 `observe_ingress_packet()` 使用映射分类；在 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 的 `apply_remote_description()` 接入该映射加载。新增/更新单测覆盖：answer 中 `111 opus` 必须归为 `Audio`、`97 apt=124` 归为 repair、`124 H264` 归为 primary。验证：`cargo fmt --all`、`cargo test -p xbxengine transport::rtc::media -- --nocapture`、`cargo test -p xbxengine transport::rtc::media::service::tests::ingress_route_prefers_negotiated_answer_payload_map -- --nocapture`。 |
| P-898 | 补齐浏览器 answer（H264 + transport-cc + NACK/PLI/FIR + RED/ULPFEC/FlexFEC）下的 SDP/PT 分类回归 | Done | 2026-03-21 | 2026-03-21 | 在 [`packet_router.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/packet_router.rs) 新增浏览器 answer 形态回归用例，覆盖 `a=rtcp-fb`（`transport-cc`、`nack`、`nack pli`、`ccm fir`）与 `RED/ULPFEC/FlexFEC` 共存时的 PT 分类，确保 `111->Audio`、`124(H264)->PrimaryVideo`、`97/125/116/122->RepairVideo`；在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/service.rs) 增加端到端用例，验证 ingest 阶段 `RED/FlexFEC` 会稳定计入 `repairVideo`。验证：`cargo test -p xbxengine transport::rtc::media -- --nocapture`。 |
| P-899 | 将 RTC 新连接层对接旧组帧媒体链路，并把连接/媒体/channel 有效性观测落入 runtime-log | Done | 2026-03-21 | 2026-03-21 | 在 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 为 RTC 栈引入独立媒体 runtime + 旧 `media_supervisor`，并在 `rebuild_peer_connection()` 挂载 legacy frame pipeline；挂载结果改为结构化写入 `latest_observation_label/summary`（`rtcLegacyFramePipelineMounted` 等），不依赖终端文本。新增 [`frame_source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/frame_source.rs)，基于旧 `SampleBuilder<H264Packet> + H264 inspection` 从 RTC ingress RTP 组装 `AssembledVideoFrame`，用于复用旧 ingress/decode/render 链验证媒体正确性。为支撑该桥接，在 [`packet_types.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/packet_types.rs) 与 [`connection/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 增加 RTP payload 透传；在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/media/service.rs) 增加可替换 sink。同时增强 channel 结构化观测：`publish_channel_lifecycle()` 与 ingress 读循环会更新 `latest_data_channel_message_catalog_observation`，便于人工回归 channel 有效性。为复用旧 supervisor，将 [`webrtc/stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack.rs) 的 `media_supervisor` 模块以及 [`media_supervisor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/webrtc/stack/media_supervisor.rs) 的 context/spawn 接口提升为 `pub(crate)`。验证：`cargo fmt --all`、`cargo check -p xbxengine`、`cargo test -p xbxengine transport::rtc::media -- --nocapture`。 |
| P-900 | 对齐 RTC data-channel 协议参数并打通输入上行链路（修复画面正常但输入无效） | Done | 2026-03-21 | 2026-03-21 | 针对 home 会话“连接/媒体正常但输入无效”，在 [`data_channel_bootstrap.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/data_channel_bootstrap.rs) 将 `message/control/input/chat` 的创建参数对齐旧实现：显式设置 `ordered=true` 与协议字符串（`messageV1/controlV1/chatV1/1.0`），避免因子协议不对齐导致远端通道行为异常；在 [`service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/service.rs) 增加 `input_stream_ready()` 与 `send_input_stream_packet()`，将输入包发送纳入连接层并写入结构化观测 `rtcInputStreamPacketSent`；在 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 将此前空实现的 `push_keyboard_pointer_input()` 接入旧输入编码链路（复用 `build_input_stream_packet`/`drain_pending_input_frames`），并在连接 pump 周期内持续发送输入流（含 gamepad keepalive 语义），同时把 `set_keyboard_pointer_enabled()` 与旧 data-channel 状态同步。验证：`cargo check -p xbxengine`、`cargo test -p xbxengine transport::rtc::connection::service::tests:: -- --nocapture`。 |
| P-916 | 回归 `runtime-trace-1774061632175`：新 rtc 的 open/handshake/bootstrap 已闭环，旧 webrtc-rs 仍是独立噪声 | Done | 2026-03-21 | 2026-03-21 | 基于 [`runtime-trace-1774061632175.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1774061632175.jsonl) 的最新回归确认：新 `rtc` 栈的四个 data channel 都完成 `OnOpen`，`message` 通道收到并识别 `HandshakeAck`，随后依次发送了 post-handshake payload、`rtcControlAuthorizationSent`、`rtcInputMetadataBootstrapSent`，且控制面/输入面 bootstrap 观测都已落地；trace 后段再次出现的 `[xbxengine][webrtc-rs] queue pending keyframe request ...` 仅代表旧 `webrtc-rs` 恢复链在并行运行，不应与新 `rtc` 的 bootstrap 状态混读。当前可以把新 `rtc` 判定为“连接、握手、bootstrap 均正常”，后续如果还要追卡顿，应只盯新 `rtc` 的媒体 ingress 和实际播放链路，不再回头看旧栈 pending keyframe。 |
| P-917 | 修正 transport-rtc-migration RFC 的两个阻断点：control 前置抽象与旧栈回退窗口 | Done | 2026-03-21 | 2026-03-21 | 已更新 [`docs/rfcs/transport-rtc-migration.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/transport-rtc-migration.md)：补入 `P0` control channel 前置抽象，明确 `recovery_executor` 之前必须先稳定 `RtcControlChannelService` contract；同时把 `webrtc/` 与 `adapter/` 的删除改为最后收口条件，新增 feature flag / 兼容窗口 / 失败回退约束，避免把高风险切换伪装成纯搬迁。 |
