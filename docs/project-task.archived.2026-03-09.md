# Project Task Tracker（已归档）

> 本文件已于 2026-03-06 归档一次。
> 归档副本文件：docs/project-task.archived.md

## Current Stage

`src/main`、`src/preload`、`src/renderer` 作为唯一活跃代码库继续承接后续开发。
其中 renderer 侧串流功能以 `src/renderer/src/streaming/application` 与 `src/renderer/src/streaming/runtime` 作为当前活跃边界，`src/renderer/src/player` 仅作为低层本地 runtime 实现存在。

## Current Task List

| ID | Task | Status | Started On | Completed On | Notes |
| --- | --- | --- | --- | --- | --- |
| P-325 | Auth 静默刷新回归修复：Sisu claims 出现 `null` 导致反序列化报错 `invalid type: null, expected a string` | Done | 2026-03-09 | 2026-03-09 | 修复 `src-tauri/src/mods/auth/types.rs` 的 Sisu claims 类型约束过严问题：将 `TitleClaims/UserClaims/AuthClaims` 从 `HashMap<String, String>` 结构放宽为 `serde_json::Value`，兼容 Xbox 返回中 `DisplayClaims` 的 `null`/动态字段；并在 `src-tauri/src/mods/auth/service.rs` 增加 `[Sisu刷新流程] 解析 Sisu token 失败` 明确日志，便于后续定位。回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
| P-324 | OAuth token 交换回归修复：`oauth20_token.srf` 表单体误发为 JSON 导致 `invalid_client` | Done | 2026-03-09 | 2026-03-09 | 基于线上日志 `HTTP 400 invalid_client / missing client_id` 定位并修复两处偏差：1) `crates/xbox-webapi/src/transport.rs` 对"`Value::String` + 非 JSON Content-Type"改为原始 body 发送（`request.body(raw)`），不再走 `request.json(...)`；2) `crates/xbox-webapi/src/auth_api.rs` 的 `exchange_code_for_token/refresh_user_token` 改为标准 URL 编码生成表单参数（避免 `code/redirect_uri` 特殊字符带来的解析偏差）。这样 `application/x-www-form-urlencoded` 请求体可被 `login.live.com/oauth20_token.srf` 正确解析，恢复与迁移前 Node/TS 版一致的提交语义。回归通过：`cargo fmt -p xbox-webapi -p xbxrc`、`cargo check -p xbxrc`。 |
| P-323 | Auth 登录态二次对齐（对照迁移前）：回调成功后立即置 authenticated 并修正 token 落盘/解析兼容 | Done | 2026-03-09 | 2026-03-09 | 按迁移前 `xal-auth-adapter` 语义做二次收口：1) `src-tauri/src/mods/auth/service.rs` 的 `complete_sisu_login` 改为"token 获取成功即置 `is_authenticated=true`"，`app_level` 以当轮拿到 `xCloudToken` 与否判定（1/2），不再以 `get_valid_session_snapshot` 作为唯一门禁，避免字段轻微漂移时卡在登录页；2) stream token 落盘结构改为与旧实现一致（无 xCloud 时不写 `xCloudToken: null`，只写存在的 key）；3) `src-tauri/src/mods/auth/token_repository.rs` 对齐旧实现边界：`get_cached_app_level` 忽略 `null` token；`is_web_token_valid` 的时间解析放宽为多格式兼容（贴近 JS `Date` 行为）；4) 回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
| P-322 | Auth 登录态回归修复：迁移后 `isAuthenticated` 误判 false 导致登录页不跳转 | Done | 2026-03-09 | 2026-03-09 | 修复 `src-tauri` 鉴权状态判定链路：1) `mods/auth/token_repository.rs` 的 stream token 有效期判定增加多形态兼容（`durationInSeconds` 数字/字符串、`not_after` 兼容）并在缺少 duration 时回退解析 `gsToken(jwt)` 的 `exp`，避免迁移后字段变化导致快照恒失效；2) `mods/auth/service.rs` 的 `get_state` 增加基于持久化快照的兜底同步（非认证进行中时自动回填 `is_authenticated/app_level`），修复回调链路偶发漏更新后的前端长期 false；3) `mods/auth/events.rs` 将 auth 会话事件 channel 对齐为 `xbxrc:auth:session-ready`，与 renderer 合同一致；4) 回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
| P-321 | xbxengine/Tauri 边界再修订：确认保持 `GamepadRuntimeHost::shared()` 现有单实例架构 | Done | 2026-03-09 | 2026-03-09 | 按最新指令修订 `rfc/xbxengine-core-tauri-migration.md`：1) 明确不调整 gamepad ownership 架构，`src-tauri` 与 `xbxengine-core` 都是 `GamepadRuntimeHost::shared()` 使用者；2) 删除"owner 回收到 core"表述，改为"共享单实例保持现状"；3) 代码改造清单改为边界校验（仅清查并禁止绕过 `shared()` 的第二实例路径）；4) 迁移阶段改为"先做单实例约束校验，再做 Electron 风格清理"。 |
| P-320 | xbxengine/Tauri 边界修订：gamepad owner 回收到 core，Tauri 仅 UI 暴露层 | Done | 2026-03-09 | 2026-03-09 | 该版本结论已被 `P-321` 覆盖，不再作为当前执行基线。 |
| P-319 | Auth/Session Flow 独立 crate 改造计划：输出两份可直接施工的 RFC（强依赖 xbox-webapi） | Done | 2026-03-09 | 2026-03-09 | 新增两份执行文档：`rfc/auth-flow-crate-refactor-plan.md` 与 `rfc/session-flow-crate-refactor-plan.md`。内容按"当前锚点 -> 目标 crate 结构 -> 旧新方法映射 -> 分阶段任务（含勾选项）-> DoD -> 测试计划 -> 非目标"组织，并明确采用你要求的方案：flow 直接依赖 `xbox-webapi`，不要求 `src-tauri` 提供 API 适配层，`src-tauri` 仅保留应用状态与编排。 |
| P-318 | xbxengine/Tauri 边界 RFC 聚焦重写：职能划分矩阵 + Electron 风格清理清单 | Done | 2026-03-09 | 2026-03-09 | 按评审反馈将 `rfc/xbxengine-core-tauri-migration.md` 从泛化计划重写为边界导向文档：1) 明确 `xbxengine-core` 与 `src-tauri` 职责矩阵（runtime/协商/输入/事件/窗口/宿主绑定）；2) 固化三类边界接口（Tauri->core 控制、core->Tauri 事件、Tauri 注入 ports）；3) 给出文件级改造清单（`src-tauri/Cargo.toml` 移除 `xbxengine-app`、`shell/mod.rs` 改直连 core、`core/Cargo.toml` 去 `ohmygamepad-host`）；4) 给出 Electron 风格清理清单（下线 `xbxengine-api`/`ohmygamepad-bridge-napi`、移除 `stdio` 协议与 sidecar 命名、清理脚本与文档）；5) 增加架构验收门禁与 grep 门禁，确保迁移可执行可验收。 |
| P-317 | xbxengine-core 架构迁移方案文档：Tauri 主线下收口 core 边界并下线 N-API/STDIO | Done | 2026-03-09 | 2026-03-09 | 新增 RFC 文档 `rfc/xbxengine-core-tauri-migration.md`，明确本轮架构决策与迁移基线：1) 固化三条准则（`core` 仅运行时内核、窗口归 `src-tauri`、清理 N-API/STDIO）；2) 给出目标依赖方向与 `core` 目录收口模型（`contracts/runtime/session/media/input/diagnostics`）；3) 定义 crate 级处置策略（`xbxengine-app` 退出主链路、`xbxengine-api` 与 `ohmygamepad-bridge-napi` 下线、`xbxengine-protocol` 移除 `stdio`）；4) 给出 Phase0~4 迁移步骤、DoD、风险控制与验收标准，作为后续实施依据。 |
| P-316 | Rust HTTP 收口方案文档：统一汇总到 `xbox-webapi` 并明确 auth/session 拆分粒度（Request-first + Flow-second） | Done | 2026-03-09 | 2026-03-09 | 新增 RFC 文档 `rfc/http-unification-xbox-webapi.md`，完成这轮目标的可执行拆分：1) 明确当前痛点是 Rust 模块内 HTTP 客户端分散与请求实现重复；2) 确立边界：Xbox 请求能力统一收口到 `crates/xbox-webapi`，`src-tauri` 仅保留应用态编排/状态；3) 对 auth/session 粒度给出结论：先提取请求能力（必须），再按"无状态"原则可选提取流程 helper（避免把 tauri 状态机与存储语义下沉到 crate）；4) 给出分阶段任务（Phase0~4）、DoD、回归与风险控制，作为后续实施基线。 |
| P-315 | Tauri 应用装配与生命周期落地：启动容错、退出幂等、构建失败非 panic | Done | 2026-03-08 | 2026-03-08 | 按 `docs/tauri-rust-architecture-guidelines.md` 落地装配与生命周期收口：1) `src-tauri/src/shell/mod.rs` 的 `load_startup_flags` 对配置读取改为显式 `match`，读取失败仅告警并回退默认启动参数；2) `shell::terminate` 改为幂等（`is_quitting.compare_exchange`），避免 `ExitRequested/Exit` 重复触发重复 shutdown；3) `terminate` 增加 `try_state::<AppState>` 防御路径，处理 setup 失败导致状态未注入场景；4) `src-tauri/src/lib.rs` 去掉 `.build(...).expect(...)` 的 panic 退出，改为错误日志后安全返回；5) 保持 run 生命周期统一走 `shell::terminate` 收敛；6) 回归通过：`cargo check -p xbxrc`。 |
| P-314 | Tauri 模块架构重构：按 application/domain/infrastructure 拆分 mods | Done | 2026-03-08 | 2026-03-08 | 按 `docs/tauri-module-architecture-blueprint.md` 完成 mods 目录结构重构：1) `gamepad` 模块创建 `application/domain/infrastructure` 三层，迁移 `service.rs` 和 `types.rs`；2) `xbxengine` 模块创建三层结构，迁移 `service.rs` 和 `types.rs`；3) `streaming` 模块创建三层，迁移 `service.rs` 和 `types.rs` 到对应目录；4) `auth` 模块创建三层，迁移 `service.rs` 和 `transfer_token_service.rs` 到 `application/`，迁移 `types.rs` 到 `domain/model.rs` 并创建 `ports.rs` 定义 `AuthProvider` trait；5) `config` 模块创建 `application/domain` 两层，迁移 `service.rs`、`defaults.rs`、`grouping.rs` 到对应目录；6) `app_state` 模块创建 `application/domain` 两层，迁移 `service.rs` 并创建 `ports.rs`；7) 所有模块的 `mod.rs` 更新导出路径；8) 回归通过：`cargo check -p xbxrc`、`cargo clippy --all-targets`（仅既有 warning）。 |
| P-313 | Rust 多包工程架构文档补充：保持 `src-tauri` 命名并定义 workspace 组织规则 | Done | 2026-03-08 | 2026-03-08 | 新增 `docs/rust-workspace-architecture.md`：1) 明确目标工程形态（根 workspace + `src-tauri` app 包 + `crates/*` 复用库）；2) 约束 `members/default-members`、`workspace.dependencies`、`profile` 的统一治理方式；3) 定义依赖方向（`src-tauri -> crates/*`，禁止反向依赖）与 crate 命名/新增准入规则；4) 给出根 `Cargo.toml` 收敛模板与低风险分步迁移顺序。 |
| P-312 | 设置保存报错修复：`config.set` 遇到 `Operation not permitted (os error 1)` 时自动回退可写存储 | Done | 2026-03-08 | 2026-03-08 | 修复 `src-tauri/src/mods/config/repository.rs`：1) 新增主存储 `settings.json` 与回退存储 `settings.v2.json` 机制；2) `set_by_patch` 写入前预探测主存储可写性，遇到权限错误（含 `PermissionDenied`/`Operation not permitted`）自动切换到回退文件；3) 首次切换时将主存储内存条目迁移到回退存储并持久化；4) 读取路径优先使用已存在的回退存储，避免后续反复触发权限错误；5) 错误信息补充具体存储文件名，便于定位。回归：`cargo check -p xbxrc` 通过（仅存在既有 warning）。 |
| P-311 | Tauri data 性能回归修复：拆分 DataService 全局锁，解除 xCloud 预热对 getUserProfile 的串行阻塞 | Done | 2026-03-08 | 2026-03-08 | 定位到 `src-tauri/src/mods/data/application/data_service.rs` 使用 `inner: Mutex<DataServiceInner>` 导致 `getXcloudTitles` 的长链路请求持锁期间阻塞 `getUserProfile/getHosts`。已重构为细粒度锁：`web_api_provider: Mutex<XboxWebApiProvider>`、`xcloud_service: Mutex<XcloudService>`，其余 `host/profile/streaming_query` 改为无全局锁直接调用；`get_user_profile` 不再依赖全局锁，`get_xcloud_titles` 仅串行化 xCloud 自身访问，解除跨接口排队。已执行 `cargo fmt -p xbxrc`。`cargo check -p xbxrc` 在当前环境被外部构建条件阻断（先后出现 `target/.fingerprint` 写入异常与 `apple-sys` 的 CoreFoundation module 构建失败），未发现与本次改动直接相关的 Rust 语义错误。 |
| P-310 | Tauri 架构蓝图补充 Rust-style 约束：减少机械分层、按需抽象 trait、强化强类型边界 | Done | 2026-03-08 | 2026-03-08 | 更新 `docs/tauri-module-architecture-blueprint.md`：1) 新增"Rust Style 约束（防过度抽象）"章节，明确小域可先最小结构（不强拆五层）；2) 明确 trait 仅在多实现（含测试替身）时引入，禁止为未来预抽象；3) 明确跨层强类型优先（`struct/enum`），禁止业务链路长期传 `serde_json::Value`；4) 补充错误模型约束（业务层统一 `AppError/AppResult`，边界再适配）；5) 验收标准新增"未到多实现不新增转发型 trait"。 |
| P-309 | 编译回归修复：清理 `AppError` 与 `String` 错误边界不一致导致的 `cargo check` 失败 | Done | 2026-03-08 | 2026-03-08 | 修复 `cargo check -p xbxrc` 的 9 处阻断错误：1) `mods/data/application/data_service.rs` 对 auth provider 调用补 `map_err(|e| e.to_string())`；2) `mods/data/infrastructure/bridges/auth_service_bridge.rs` 对齐 `Result<_, String>` 错误映射；3) `mods/xbxengine/rpc.rs` 修正 `SnapshotStats` 返回为 `await?`；4) `mods/app_state/service.rs` 修复 `clear_auth_cache` 错误类型转换；5) `mods/streaming/service.rs` 去除重复包装 `AppError::Streaming(e)` 的错误路径，改为 `?` 或 `map_err(AppError::Streaming)`。回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`（剩余为 warning）。 |
| P-308 | Tauri 模块架构规划：定义目录分层、职责模板与跨模块抽象边界（防止目录铺平） | Done | 2026-03-08 | 2026-03-08 | 新增 `docs/tauri-module-architecture-blueprint.md`，明确：1) Rust 侧目标目录分层（`app/contracts/mods/shell`）与 `mods/<domain>` 统一模板（`rpc/application/domain/infrastructure/events`）；2) 每层职责边界与依赖方向（`rpc -> application -> domain/infrastructure`）；3) 跨模块调用规则（通过 `ports/bridges`，禁止跨域直连 infrastructure）；4) Tauri 前端配套边界（`modules/streaming/shared`）；5) 可读性硬约束（目录文件数、单文件规模、`mod.rs` 职责）与渐进迁移顺序（`gamepad -> xbxengine -> config -> auth/data -> app/lifecycle`）。 |
| P-307 | event_bridge 拆分落地：事件定义下沉到各 mod，桥接层仅保留基础 emit 能力 | Done | 2026-03-08 | 2026-03-08 | 完成"事件定义拆到模块内"的实现收口：1) 新增 `tauri/src-tauri/src/mods/auth/events.rs`、`mods/gamepad/events.rs`、`mods/xbxengine/events.rs`，分别承接 channel 常量、payload 映射与域内 emit helper；2) `tauri/src-tauri/src/event_bridge.rs` 从业务常量/映射函数收缩为基础设施层（`emit_json/emit`）；3) 替换调用点：`mods/auth/rpc.rs`、`mods/gamepad/service.rs`、`shell/mod.rs`、`shell/bridge.rs` 全部改为依赖对应 domain events；4) `mods/{auth,gamepad,xbxengine}/mod.rs` 导出 `events` 子模块，形成"事件随业务域走、发射出口统一"结构；5) 格式化已执行 `cargo fmt -p xbxrc`。编译回归尝试 `cargo check -p xbxrc` 仍被分支内既有类型错误阻断（`mods/data/*`、`mods/app_state/service.rs`、`mods/streaming/service.rs`、`mods/xbxengine/rpc.rs`），非本次改动引入。 |
| P-306 | 主线代码库模块架构规划：定义目录蓝图、职责边界与分层依赖规则（防止目录铺平） | Done | 2026-03-08 | 2026-03-08 | 新增架构蓝图文档 `docs/module-architecture-blueprint.md`：1) 明确主线范围仅 `src/main`、`src/preload`、`src/renderer`；2) 给出目标目录结构（`main/mods` 分域分层、`preload/bridge+contract`、`renderer/modules` 业务域化与 `player-core` 低层能力定位）；3) 定义模块职责清单与"负责/不负责"边界；4) 定义依赖规则（`rpc -> application -> domain/infrastructure`、跨模块通过 port/interface、renderer 页面层禁止直连底层 transport）；5) 制定可读性约束（目录文件数、单文件规模、命名规范）与渐进迁移顺序（`stream-session -> xcloud -> settings -> home -> auth`），用于后续持续重构执行基线。 |
| P-305 | event_bridge 统一收口模块分发架构规划：从"事件函数集合"演进到"dispatcher + domain handler registry" | Done | 2026-03-08 | 2026-03-08 | 按"统一模块分发"目标输出实施计划（仅规划，未改代码）：1) 保留现有事件模型，不引入新 RPC 协议；2) `event_bridge` 拆为 `contract/dispatcher/handlers/emitter` 四层，统一入口 `dispatch(event_name, payload)`；3) 各 domain（auth/gamepad/xbxengine）仅注册 handler 与 payload mapper，禁止散落 emit；4) 增加统一中间层（校验、日志、指标、错误兜底），保证模块内聚与可观测；5) 迁移顺序 `auth -> gamepad -> xbxengine`，按"注册 -> 双写校验 -> 旧入口下线"逐域收口。 |
| P-304 | tauri/src-tauri 编译回归修复：清理模块混杂与 trait 接线错误，恢复 `cargo check` 通过 | Done | 2026-03-08 | 2026-03-08 | 针对 `tauri/src-tauri` 大量编译失败做一轮结构化收口：1) 补齐依赖 `async-trait`，修复 async provider trait 的动态分发可用性；2) 修复模块与导入错误（`data/mod.rs` 移除不存在 `repository`、`app_state/service.rs` 去自引用导入并补 `Ordering`、`streaming/rpc.rs` 补 `json` 宏导入、`setup/mod.rs` 修正 `ConfigRepository` 路径与 trait 引入）；3) 修复服务层缺陷（`gamepad/service.rs` 补 `GamepadProvider` 导入与 `new` 构造、`xbxengine/service.rs` 补 trait/宏导入）；4) 重构 `mods/auth/service.rs`，统一内部状态锁与 OAuth/silent flow 实现，移除混杂的旧字段访问与重复方法；5) 扩展 `AuthProvider` trait 并对齐调用方，修复 `auth/rpc.rs` 的 async 调用；6) 清理 `data_service.rs` 旧实现重复块；7) 修复 `AuthServiceBridge` 从旧 `read/write` 访问迁移到 `AuthProviderRef` 直接调用；8) 修复 macOS 窗口事件签名不匹配。回归通过：`cargo check`（工作目录 `tauri/`）。 |
| P-303 | data hosts 对齐修复（对照 Electron）：主机列表改为宽松字段映射并补链路判定日志 | Done | 2026-03-08 | 2026-03-08 | 按 Electron `host-service` 语义回归：1) `tauri/src-tauri/src/mods/data/client.rs` 将主机列表解析从严格 `serde` 反序列化改为宽松按字段提取（仅要求存在 console identity 字段之一），避免因单字段类型不匹配导致整条主机记录被丢弃；2) 解析候选键保留并增强（含 `results/result/consoles/devices/items/...`）；3) `data_service.get_hosts` 补 `no session / no web api client / result count` 日志，便于判定是认证、client 构建还是响应映射问题。回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
| P-302 | data 主机列表回归修复：smartglass 响应键遗漏导致 hosts 解析为空 | Done | 2026-03-08 | 2026-03-08 | 修复 `getHosts` 空列表问题：`tauri/src-tauri/src/mods/data/client.rs` 的 `extract_consoles` 递归候选键缺少 `results`（smartglass 常见返回结构），导致可用响应未被命中；现补齐候选键 `results/consoles` 并保留其余回退键，同时增加解析数量日志 `"[data][hosts] parsed consoles count=..."` 便于回归。回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
| P-301 | auth 回归修复：sisu token 解析模型与实际响应不兼容导致登录回落未认证 | Done | 2026-03-08 | 2026-03-08 | 基于日志定位并修复根因：`complete_sisu_login` 将 `do_sisu_authorization` 响应整体反序列化为 `SisuTokenData`，但响应缺失 `DeviceToken` 字段导致失败。现改为按字段组装 `SisuTokenData`（`TitleToken/UserToken/AuthorizationToken` 从响应提取，`device_token` 使用当前请求 device token 回填），与 Electron 语义一致并避免回调后落回未登录。回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
