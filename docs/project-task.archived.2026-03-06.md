# Project Task Tracker（已归档）

> 本文件已于 2026-03-06 归档，当前不再作为活跃任务清单使用。后续如需新增任务，请建立新的任务清单文件并在此处链接。
> 归档副本文件：docs/project-task.archived.md

## Current Stage

`src/main`、`src/preload`、`src/renderer` 作为唯一活跃代码库继续承接后续开发。
其中 renderer 侧串流功能以 `src/renderer/src/streaming/application` 与 `src/renderer/src/streaming/runtime` 作为当前活跃边界，`src/renderer/src/player` 仅作为低层本地 runtime 实现存在。

## Current Task List

| ID | Task | Status | Started On | Completed On | Notes |
| --- | --- | --- | --- | --- | --- |
| P-226 | 将 xbxengine core 架构摘要写入 docs | Done | 2026-03-06 | 2026-03-06 | 新增 docs/xbxengine-core-architecture.md，总结 runtime/媒体后端抽象/Active Stack/输入/恢复/统计关键点，并补代码引用链接。 |
| P-227 | 移除文档内的本地代码链接 | Done | 2026-03-06 | 2026-03-06 | 按“文档要拿到仓库之外”要求，清理 docs/xbxengine-core-architecture.md 内的 `file:///` 本地链接，改为仅保留模块名引用。 |
| P-228 | 为架构文档补充精简代码示例 | Done | 2026-03-06 | 2026-03-06 | 在 docs/xbxengine-core-architecture.md 增加“代码示例”段：媒体后端合同、Active Stack 接口、数据通道握手、视频管线 NACK/PLI、恢复决策、协商配置等示例，便于仓库外读者理解实现。 |
| P-225 | 设置页风格二次收敛：贴近 Xbox 主机设置的纯平全屏左导航 + 右列表视觉 | Done | 2026-03-05 | 2026-03-05 | 根据视觉参考图继续收敛 `src/renderer/src/pages/Setting.vue`：1) 左侧导航改为高对比暗面板（取消卡片化边框/阴影）+ 大标题 `setting.title`，导航项改为主机风格高密列表并保留 active pill；2) 右侧内容区改为无卡片纯平列表，移除 panel 装饰，扩大组标题与留白，列表项改为“文本主导 + 右侧值/箭头”的主机设置视觉；3) 行焦点改为弱发光内描边，不再使用过重边框块状反馈；4) 补齐窄屏降级（`narrow` 下单列 + 左导航横向滚动）；5) `src/renderer/src/components/settings/SettingToggleRow.vue` 同步视觉收敛并保持 `leftNeighborId` 导航回跳。并新增 i18n 键：`src/renderer/src/i18n/locales/{zh,en}.json` 的 `setting.title`。已确认 `pnpm exec eslint src/renderer/src/pages/Setting.vue src/renderer/src/components/settings/SettingToggleRow.vue` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-224 | 设置页重构为全屏左右分栏：左侧导航列 + 右侧内容列（贴近 play.xbox.com 风格） | Done | 2026-03-05 | 2026-03-05 | 将 `src/renderer/src/pages/Setting.vue` 从“顶部 tabs + 单列面板”重构为全屏双栏布局：1) 新增 `setting-page__layout` 网格，左侧 `setting-sidebar` 固定导航列、右侧 `setting-panel` 滚动内容列；2) 用 `Focusable` 重建左侧分组导航，沿用 `SPATIAL_NAV_NODE_IDS.settingTabs.*` 作为节点 ID，保留现有空间导航体系；3) 右侧新增 sticky 组标题头（滚动态阴影）并保留 section 分组内容；4) 行级样式统一切到新语义 token（`--color-* / --tile-* / --settings-* / --focus-*`）；5) `src/renderer/src/components/settings/SettingToggleRow.vue` 新增 `leftNeighborId`，支持从设置项左移回导航列，同时同步视觉 token。已确认 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 与 `pnpm exec eslint src/renderer/src/pages/Setting.vue src/renderer/src/components/settings/SettingToggleRow.vue` 通过。 |
| P-223 | 修复 TAB_NAV 联动：焦点切换后同步触发 tab 状态切换（覆盖手柄来源） | Done | 2026-03-05 | 2026-03-05 | 针对“TAB_NAV 切到 `focusedNode` 时应同步切 tab 状态”的问题，移除仅依赖 monkey patch `runtime.dispatch` 的脆弱路径，改为统一消费 runtime debug action 事件：1) `src/renderer/src/navigation/spatial-nav.constants.ts` 新增 `SPATIAL_NAV_RUNTIME_EVENTS.tabNavAction`；2) `src/renderer/src/App.vue` 通过 `ConsoleUIProvider.config.onDebugEvent` 捕获 `TAB_NAV:*` 并广播 runtime 事件，覆盖键盘与手柄两类输入来源；3) `src/renderer/src/composables/useSpatialNavRuntimeBehavior.ts` 改为监听该事件并订阅 runtime state，在 `TAB_NAV` 导致焦点变化时自动派发 `CONFIRM`，使 tab focus 与 active 状态同步生效，同时用微任务清理未消费动作防止串扰。已确认 `pnpm exec eslint src/renderer/src/App.vue src/renderer/src/composables/useSpatialNavRuntimeBehavior.ts src/renderer/src/navigation/spatial-nav.constants.ts` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-222 | 修正分辨率配置文案：`1081` 档位改为 `1080p HQ` | Done | 2026-03-05 | 2026-03-05 | 按“分辨率配置中 1440p 描述错误，实际为 1080p HQ”的反馈，统一修正文案：1) `src/shared/config/domain-definition.ts` 中 `resolution=1081` 标签改为 `Auto (1080p HQ)`；2) `src/renderer/src/i18n/locales/en.json` 同步改为 `Auto (1080p HQ)`；3) `src/renderer/src/i18n/locales/zh.json` 同步改为 `自动（1080p HQ）`。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-221 | Rust 侧 chat/mic 对齐补齐：重连恢复 mic track 与停麦 paused 语义一致化 | Done | 2026-03-05 | 2026-03-05 | 针对“基本对齐后剩余差异”补齐两处行为：1) `crates/xbxengine/core/src/runtime.rs` 的 `request_reconnect()` 在 `microphone_capturing=true` 时，重连协商完成后先调用 `set_microphone_capturing(true)` 恢复本地 mic track，再执行 chat 重协商，避免仅有 chat offer 但本地音轨未重新挂载；2) `StopMicrophone` 路径将 `microphone_paused` 与 `ChatStateChanged.paused` 从 `false` 调整为 `true`，与 `webrtc-direct` 停麦语义保持一致；3) 新增单测 `reconnect_restores_microphone_capture_when_microphone_is_on`，并更新原有停麦断言。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（25/25）通过。 |
| P-220 | Rust 侧完整对齐 webrtc-direct：落地 chat channel + Mic 实时采集发送链路 | Done | 2026-03-05 | 2026-03-05 | 按“完整对齐实现”完成 Rust 侧 chat/mic 全链路闭环：1) 继承既有 chat 重协商能力，Mic 开/关继续走 `ExchangeOffer(channel=chat)` 并保持失败回滚；2) 在 `crates/xbxengine/core/src/webrtc_rs_microphone.rs` 新增真实麦克风链路：`cpal` 采集、单声道 PCM 归一化、48k 最近邻重采样、20ms Opus 编码、`TrackLocalStaticSample` 推流与 RTCP 读循环；3) `webrtc_rs_transport`/`webrtc_rs_stack`/`webrtc_rs_backend` 打通 `set_microphone_capturing` 到 WebRTC 传输层，支持添加/移除本地音频轨；4) 修复 `cpal::Stream` 非 `Send` 回归：改为独立采集线程持有流对象，session 内仅保留 stop flag + thread handle，确保 `WebRtcRsMediaStackPort: Send` 约束成立。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（24/24）通过。 |
| P-219 | 配置重构：移除 `debug`/`xhome_auto_connect_server_id`，并按“应用/串流/主机/云游戏/输入”五组收口 | Done | 2026-03-05 | 2026-03-05 | 按最新配置整理要求完成端到端重构：1) 在 `src/main/mods/config/domain/{types,defaults,schema,grouping}.ts` 移除 `debug` 与 `xhome_auto_connect_server_id`，并将 group 统一为 `app/streaming/host/xcloud/input`；2) 在 `src/shared/rpc/contract.ts`、`src/shared/config/domain-definition.ts`、`src/renderer/src/pages/Setting.vue`、`src/renderer/src/navigation/spatial-nav.constants.ts` 与中英文 i18n 中同步更新设置页分组/分段与字段映射；3) `src/main/shell/domain/types.ts` 与 `src/main/shell/application/shell-bootstrap.ts` 去除 `xhome_auto_connect_server_id` 启动兜底，保留 CLI `--auto-connect` 单一路径。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false`、`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-218 | Rust 侧补齐 chat/mic 控制链：Mic 开关走 chat offer 重协商，并在重连后自动恢复 | Done | 2026-03-05 | 2026-03-05 | 按“chat channel、Mic 在 Rust 一并实现”的要求，完成 `xbxengine` runtime 控制面补齐：1) `crates/xbxengine/core/src/backend.rs` 为 `XbxEngineMediaBackend` 新增 `create_offer()`，支持在既有 PeerConnection 上发起增量协商；`webrtc_rs_backend` 与测试桩同步实现；2) `crates/xbxengine/core/src/runtime.rs` 新增 `renegotiate_chat_channel()`，在 `StartMicrophone/StopMicrophone` 时通过 `ExchangeOffer(channel=chat)` 完成 chat 重协商并回灌 answer（失败时回滚麦克风状态，避免本地/远端状态分叉）；3) `request_reconnect()` 补齐“麦克风已开启时重连后自动恢复 chat 协商”逻辑；4) 新增单测 `microphone_toggle_renegotiates_chat_offer` 与 `reconnect_restores_chat_negotiation_when_microphone_is_on` 覆盖关键路径。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（24/24）通过。 |
| P-217 | 梳理当前配置体系：盘点配置项、生效链路与可配置化候选 | Done | 2026-03-05 | 2026-03-05 | 完成 `src/main` / `src/preload` / `src/renderer` 全链路梳理：1) 以 `src/main/mods/config/domain/{types,defaults,schema,grouping}.ts` 与 `src/shared/config/domain-definition.ts` 为基线，确认当前共有 30 个应用级配置键并按 app/auth/streaming/input/xhome 五组组织；2) 对 `shell-bootstrap`、`auth`、`streaming`、`renderer streaming runtime` 与 `xbxengine-native-binding` 做键级消费核对，识别出“全局生效”“仅 `webrtc-direct` 生效”“仅设置页可见/暂未消费”三类；3) 同步梳理系统级配置入口（`resources/xbxengine.runtime.json` + `XBXENGINE_*` 环境变量、`electron-builder.yml`、`dev-app-update.yml`、`tsconfig*`）；4) 产出可配置化候选清单（如串流轮询与告警阈值、xCloud market/language、窗口基线尺寸、runtime config 下放到设置页等）供后续排期。 |
| P-216 | REMB 目标再上调 3Mbps（30M 设定点，33M 上限）并回归稳定性 | Done | 2026-03-05 | 2026-03-05 | 按“REMB 应再提高 3M”的要求，更新 `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs`：`REMB_TARGET_BPS` 从 `27_000_000` 提升到 `30_000_000`，`REMB_MAX_BPS` 从 `30_000_000` 提升到 `33_000_000`，保持 `REMB_MIN_BPS=20_000_000` 不变，以给调度层更多上探空间且不改变低位保护。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）通过。 |
| P-215 | 三管线回归修正：补齐 recovered 统计链路与主轨真实码率口径，消除 `lossLateRecovered=0` 误判和 32Mbps 异常峰值 | Done | 2026-03-05 | 2026-03-05 | 针对“`lossLateRecoveredPkts` 长期为 0 + 码率偶发 32Mbps 异常”完成统计口径修复：1) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 新增 `pending_inbound_primary_video_bytes_total`，只在主 H264 控制轨累计并写入 `stats.inbound_primary_video_bytes_total`，同时修复 `apply_video_pipeline_runtime_stats` 漏拷 `video_loss_recovered_count_total`；2) `crates/xbxengine/core/src/runtime.rs` 的 `DiagnosticsPulse` 计算新增 `inbound_primary_video_kbps` 与 `video_loss_recovered_packets_in_window`，窗口起点/复位状态同步新增主轨字节与 recovered 计数；3) `crates/xbxengine/protocol/src/runtime.rs` 扩展 `DiagnosticsPulse` DTO 字段；4) `src/main/mods/streaming/application/xbxengine-service.ts` 日志改为 `bitrateKbps=主轨码率`，保留 `totalVideoKbps` 观察总视频吞吐，并新增 `lossRecoveredPkts`，`lossNetPkts = finalized - recovered`（下限 0）。这样 `lossLateRecoveredPkts` 不再承担“总恢复”语义，且 32Mbps 峰值会体现在 `totalVideoKbps` 而不污染主显示码率。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-214 | 三管线自查补漏：接入 RTX 恢复回冲，避免主 H264 管线 loss 统计系统性偏高 | Done | 2026-03-05 | 2026-03-05 | 复查三管线后发现同类问题：当前只在主 H264 轨统计/恢复缺包，若恢复包走 `video/rtx` 轨则不会回冲 `missing_packets`，导致 `lossFinalizedPkts` 偏高并拖慢 REMB 回升。本次改造：1) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 新增 `on_recovered_sequence(sequence, now_ms)`，统一处理“缺包恢复/晚到恢复”回冲逻辑；2) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 新增当前主控制轨 pipeline 共享句柄，非主 `video/rtx` 轨按 RFC4588 解析 OSN（payload 前 2 字节）并回写主管线恢复；3) 主控制轨结束时清理共享句柄，避免悬挂引用。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-213 | NetLoop 主线路隔离：每包统计写入与诊断日志迁移到 StatsLoop 批量处理，消除热路径锁竞争 | Done | 2026-03-05 | 2026-03-05 | 按“NetLoop 不要被非主线路工作干扰”的要求重构 `crates/xbxengine/core/src/webrtc_rs_transport.rs`：1) 移除 `read_rtp` 分支内每包 `runtime_stats` Mutex 写入，改为本地累计 `pending_inbound_*`，在 `StatsLoop` 周期批量 flush；2) 将 track 吞吐诊断 `maybe_log_track_throughput_diagnostics` 从每包路径迁移到 `StatsLoop` 周期执行，避免热路径格式化开销；3) `StatsLoop` 改为所有 track 都运行（不再仅主控制轨），确保非主视频轨/音频轨字节统计也被定期上报；4) 新增 `snapshot_video_pipeline_runtime_stats/apply_video_pipeline_runtime_stats`，并在 `StatsLoop/ControlLoop` 中避免 `video_pipeline` 锁与 `runtime_stats` 锁交叉持有，减少主路径锁等待。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `cargo test -p xbxengine`（22/22）通过。 |
| P-212 | 丢包回归修正：NetLoop 去阻塞化，NACK/PLI 下发改异步避免 `read_rtp` 热路径卡顿 | Done | 2026-03-05 | 2026-03-05 | 针对“码率上不去且稳态 `loss1s/loss5s` 持续 2~3%”的现象，定位到 `crates/xbxengine/core/src/webrtc_rs_transport.rs` 中 `read_rtp` 分支存在阻塞点：NACK/PLI 发送在热路径内 `await`，可能导致 RTP drain 不及时、形成接收侧自造丢包。已改为异步下发（`tokio::spawn`），`read_rtp` 只负责收包和轻量状态更新；同时保留失败日志用于诊断 RTCP 写入异常。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `cargo test -p xbxengine`（22/22）通过。 |
| P-211 | 调度保守性回归修正：2~3% 丢包区间不再持续下压，恢复慢速探测回升 | Done | 2026-03-05 | 2026-03-05 | 针对“调度生效但码率无法回到 23Mbps 峰值”的现象，调整 `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 的 `decide_remb_target_bps`：1) 新增 `REMB_LOSS_PRESSURE_START_THRESHOLD=3%`，低于该阈值不进入连续 pressure 下压；2) 轻中度 pressure 回撤斜率由 `0.03+0.08*pressure` 下调为 `0.015+0.055*pressure`，降低在 20Mbps 附近被过快锁死的概率；3) 新增 `REMB_GENTLE_RECOVERY_STEP_BPS=1Mbps` 的稳态慢速回升通道（当 `loss1s<4%` 且 RTT 未恶化时），避免“既不 severe 降级也不满足严格 recovered 条件”时长期停在低位。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-210 | RTT 诊断一致性修复：`[rtt]` 日志在回退路径下同步显示 `nack_recovery` 选中值 | Done | 2026-03-05 | 2026-03-05 | 针对“control 日志已有 `rttSource=nack_recovery`，但 `[rtt]` 日志仍显示 `selected=null/source=none`”的可观测性偏差，更新 `crates/xbxengine/core/src/webrtc_rs_transport.rs`：在 control tick 中，当 stats RTT 为空但 pipeline 已使用 `nack_recovery` 回退时，构造 `effective_rtt_sample` 覆盖 `selected_rtt_ms/selected_source` 后再输出 `[xbxengine][webrtc-rs][rtt]`，确保诊断日志与控制实际一致。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `cargo test -p xbxengine`（22/22）通过。 |
| P-209 | RTT 可用性收口：stats RTT 缺失时启用 NACK 恢复 RTT 回退并标记 `rttSource=nack_recovery` | Done | 2026-03-05 | 2026-03-05 | 基于回归日志（`cpRaw*` 持续为 0 导致 `rttMs=null`）继续收口 RTT：1) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 的 `on_control_tick` 改为“stats RTT 优先，`video_nack_recovery_rtt_ms` 兜底”，并在控制动作中返回 `used_nack_recovery_rtt`；2) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 在 `selected_source` 为空且命中回退时，显式写入 `stats.video_rtt_source = nack_recovery`，确保 `[XbxEngine][Metrics]` 不再长期 `rttSource=null`。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-208 | 三层管线纠偏：ControlLoop 解耦为独立异步任务，避免 `get_stats`/RTCP 阻塞 `read_rtp` 热路径 | Done | 2026-03-05 | 2026-03-05 | 基于回归日志（`rtt` 源长期为空且吞吐异常）审视实现后发现结构问题：`NetLoop/StatsLoop/ControlLoop` 仍在同一 `tokio::select!` 任务内，`control` 分支里的 `sample_peer_connection_rtt(...).await` 与 REMB 发送会打断 `read_rtp`。本次在 `crates/xbxengine/core/src/webrtc_rs_transport.rs` 完成解耦：1) `WebRtcRsVideoPipelineState` 改为 `Arc<Mutex<...>>` 共享；2) `control` tick 改为 `tokio::spawn` 独立执行，主读包循环仅负责 NetLoop 热路径；3) 增加 `control_tick_inflight(AtomicBool)` 防止控制任务重叠；4) RTT/Control 诊断节流时间戳改为 `Arc<Mutex<Option<f64>>>`，保持原日志节流语义。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-207 | RTT 采样回归修正：放宽 video RTT 报告筛选并补齐“为何为 null”的计数诊断 | Done | 2026-03-05 | 2026-03-05 | 按“先回归、别猜、审视调度合理性”的要求，修正 `crates/xbxengine/core/src/webrtc_rs_transport.rs` 的 `sample_peer_connection_rtt`：1) CandidatePair 选择从“必须 succeeded+responses>0”改为“按 succeeded/nominated/是否有 RTT/responses 打分择优”，避免过严过滤导致 RTT 长期空值；2) RemoteOutbound/RemoteInbound 对 video 报告改为“按目标 SSRC 命中 + measurements/reports + current/avg RTT 可用性”打分择优，非目标 SSRC 作为降级回退，不再直接丢弃；3) `[xbxengine][webrtc-rs][rtt]` 诊断新增 `cpSucceeded/roVideoReports/roTargetSsrcReports/riVideoReports/riTargetSsrcReports`，用于明确 `rttMs=null` 是“源数据缺失”还是“筛选条件过严”。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-206 | renderer Token 改造第二阶段（续）：Home/XCloud/Rail 切换到语义 token | Done | 2026-03-05 | 2026-03-05 | 继续完成内容卡片域收尾迁移：1) `src/renderer/src/components/common/HorizontalListRail.vue` 将 rail 间距/文案色/滚动内边距切到 `--shelf-*` 与 `--color-text-*`；2) `src/renderer/src/pages/Home.vue` 的空态文案与刷新按钮切到 `--color-text-secondary` 与 `--btn-*`，focus ring 改为 `--color-focus-ring*`；3) `src/renderer/src/pages/XCloud.vue` 的标题/副标题/搜索框/搜索输入/结果文案/动作按钮切到新语义 token（`--color-* / --btn-*`），并统一焦点态；4) 补齐 Prettier 格式。已确认 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 与针对上述 3 个文件的 `pnpm exec eslint` 通过。 |
| P-205 | renderer Token 改造第二阶段（首批）：导航与核心卡片组件切换到新语义 token | Done | 2026-03-05 | 2026-03-05 | 在保持布局和 density 尺寸 token 不变的前提下，完成首批组件迁移：1) `src/renderer/src/components/navigation/TopNavBar.vue` 的 active/focus 态改为 `--color-state-selected / --color-focus-ring*`，头像占位改为 `--color-border-subtle / --color-surface-2`；2) `src/renderer/src/components/navigation/SpatialNavIconButton.vue` 切换按钮文字、focus/active、空图标占位到新 token（`--btn-* / --color-* / --pill-radius`）；3) `src/renderer/src/components/common/GameCard.vue` 切换边框/表面/focus/image 底色/标题文字到 `--color-*` 语义 token；4) `src/renderer/src/components/common/ConsoleStatusCard.vue` 增加 token 化边框与 focus ring，并统一主/次文字色来源；5) `src/renderer/src/styles/_theme-semantic.scss` 的通用 `.ui-page-panel/.ui-action-*` 已优先消费新 token。已确认 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 与针对上述 4 个组件的 `pnpm exec eslint` 通过。 |
| P-204 | renderer Token 改造第一阶段：接入 RFC token 真源与兼容桥接层 | Done | 2026-03-05 | 2026-03-05 | 基于 `rfc/token.md` 完成 renderer 侧第一阶段落地：1) 新增 `src/renderer/src/styles/tokens.css`，引入 RFC 语义 token（brand/color/typography/space/radius/motion/layout/z-index/component）；2) 新增 `src/renderer/src/styles/token-alias.scss`，建立“新 token -> 现有 `--ui-*`”兼容桥接，确保迁移初期视觉稳定；3) 新增 `src/renderer/src/styles/tokens.ts`，提供 TypeScript token 类型与常量导出，供后续组件逻辑/主题切换消费；4) `src/renderer/src/main.ts` 接入 `tokens.css`，`src/renderer/src/styles/theme.scss` 接入 `token-alias.scss`；5) `src/renderer/src/styles/_theme-semantic.scss` 的通用 `.ui-page-panel/.ui-action-*` 样式改为优先消费新 token（focus/border/surface/button）。已确认 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 与 `pnpm exec eslint src/renderer/src/main.ts src/renderer/src/styles/tokens.ts` 通过。 |
| P-203 | 调整调度策略：REMB 稳态设定点上浮到 27Mbps，并增加 NACK 恢复 RTT 回退 | Done | 2026-03-05 | 2026-03-05 | 按“REMB 不应贴着目标、RTT 仍需可用”的要求完成两处改造：1) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 将稳态 `REMB_TARGET_BPS` 从 25Mbps 提升到 27Mbps（上限仍 30Mbps，保持可回退空间）；2) 在视频管线新增 `video_nack_recovery_rtt_ms`，当包在 NACK 后恢复时按恢复时延做 EWMA（8~400ms 过滤）作为 RTT 回退样本，并在 control tick 无 stats RTT 时使用该样本；3) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 在 `rtt` 原始来源为空且已有 RTT 回退样本时，将 `video_rtt_source` 标记为 `nack_recovery`。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-202 | 码率异常四次回归：新增按远端 track 的窗口吞吐诊断，定位主轨与有效码流是否一致 | Done | 2026-03-05 | 2026-03-05 | 为了定位“控制面目标 25Mbps，但实收仅几十 kbps”是否来自轨道选择/远端发流异常，`crates/xbxengine/core/src/webrtc_rs_transport.rs` 新增 `[xbxengine][webrtc-rs][track]` 诊断日志（复用 `rttDiagnostics` 开关和周期）：按 track 输出 `kind/mime/payloadType/ssrc/primaryVideo/mid/trackId/packets/payloadBytes/bitrateKbps/avgPayloadBytes`，用于直接判断主 H264 轨是否在稳定收高码流、以及是否存在非主轨占流量。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `cargo test -p xbxengine`（22/22）通过。 |
| P-201 | 码率异常三次回归：修正 SDP `b=AS` 单位并限制 `x-google-*` 仅作用于 H264 payload | Done | 2026-03-05 | 2026-03-05 | 针对“REMB=25Mbps 但实收仅几十 kbps”的情况，进一步修复 SDP 策略层：1) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 的 `apply_offer_policy_contract` 将 `b=AS` 注入值改为 `kbps`（此前按 bps 注入异常大值）；2) `patch_video_fmtp_constraints` 不再按 `m=video` 全 payload 盲写 `x-google-min/start/max-bitrate`，改为仅对 `a=rtpmap` 识别出的 `H264` payload 生效，避免污染 RTX 等 payload 的 `fmtp`。该修复用于排除“错误 SDP 约束触发云端极保守档位”的可能。 |
| P-200 | 基于 `rfc/token.md` 规划 renderer 侧 Design Token 改造分阶段路线 | Done | 2026-03-05 | 2026-03-05 | 完成 renderer 现状盘点（`_theme-foundation.scss -> _theme-semantic.scss -> 组件 var(--ui-*)`），并输出可执行迁移计划文档 `docs/renderer-token-migration-plan.md`：包含 5 个执行阶段（基线映射、token 基础设施并行接入、公共基座迁移、业务域迁移、清理收口）、四类模块落地范围（ConsoleCard、GameTile/Shelf、Settings、Guide Drawer）、验证矩阵与风险缓解策略，作为后续 renderer 样式改造的执行基线。 |
| P-199 | 码率异常二次回归：关闭脚本默认强制 REMB，并补齐控制面/吞吐联动诊断字段 | Done | 2026-03-05 | 2026-03-05 | 针对“修复后仍稳定在 ~50kbps”的问题，补充两类改造：1) `scripts/dev-rtcp-test.sh` 去掉默认 `XBXENGINE_FORCE_REMB_KBPS=120000`，改为默认 `auto`（不强制 REMB），避免压测脚本隐式锁死反馈策略；2) 诊断增强：`crates/xbxengine/core/src/runtime.rs` 与 `crates/xbxengine/protocol/src/runtime.rs` 的 `DiagnosticsPulse` 新增 `inbound_video_packets_in_window`、`video_remb_bps` 字段，`src/main/mods/streaming/application/xbxengine-service.ts` 的 `[XbxEngine][Metrics]` 新增 `videoPackets/rembKbps` 输出；3) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 新增 `[xbxengine][webrtc-rs][control]` 周期日志，输出 `forcedRembKbps/adaptiveRemb/rembBps/loss/jitter/rtt`，用于判定是否控制面误压码率。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol -p xbxengine-api -p xbxengine-app`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-198 | 修复码率异常回归：视频吞吐统计覆盖所有 video 轨，main 配置白名单化避免残留 forced REMB 污染 | Done | 2026-03-05 | 2026-03-05 | 针对“码率掉到 50kbps”回归，完成两处修复：1) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 中 `inbound_video_bytes_total` 改为对全部 `is_video` 轨累计（含 RTX/FEC），但 loss/NACK/PLI/REMB 控制仍只在主 H264 轨执行，避免主轨识别影响吞吐指标；2) `src/main/mods/streaming/application/xbxengine-native-binding.ts` 的 runtime config 改为显式白名单，`forcedRembKbps` 仅接受 `XBXENGINE_FORCE_REMB_KBPS` 环境变量（不再从配置文件隐式继承历史字段），并新增启动时生效配置日志，便于排查误限速。 |
| P-197 | 修正三层管线主轨选择：非 H264 视频轨不再参与 loss/NACK/PLI/REMB 控制 | Done | 2026-03-05 | 2026-03-05 | 针对实测稳态 `loss1s/loss5s` 偏高问题，修复 `crates/xbxengine/core/src/webrtc_rs_transport.rs`：1) 新增 `is_primary_video_track = is_video && mime == video/h264` 判定，明确只有主 H264 视频轨进入三层管线；2) 将 `on_rtp_packet`、分辨率提取、video bytes 统计、NACK/PLI 发送、StatsLoop 与 ControlLoop 全部改为仅在主轨执行；3) 对 `video/rtx` 等非主视频轨只做 drain，不再污染缺包窗口和反馈节流决策；4) RTT 诊断日志保留并扩展 candidate pair 原始字段（cpReports/cpRaw*）用于下一步定位 `rttMs=null`。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-196 | 将丢包阈值与 RTT 诊断改为 main 配置文件下发：放宽 NACK finalize（400ms/5 次）并输出 RTT 来源 | Done | 2026-03-05 | 2026-03-05 | 按“继续做 1/2，并由 main 处理环境变量、Rust 通过 N-API 接收配置文件”的要求完成改造：1) `crates/xbxengine/core/src/runtime.rs` 扩展 `XbxEngineRuntimeConfig`，新增 `webrtc.video_pipeline(nackWindowMs/nackRetryIntervalMs/nackMaxRetryCount)` 与 `webrtc.rtt_diagnostics(enabled/logIntervalMs)`；2) `create_active_media_backend`、`webrtc_rs_backend`、`webrtc_rs_stack`、`webrtc_rs_transport` 全链路改为消费 runtime config，不再由 transport 直接读 `FORCE_REMB/ADAPTIVE_REMB` 环境变量；3) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 引入 `WebRtcRsVideoPipelineConfig`，将缺包 finalize 判定改为可配置，默认收敛到 `400ms + 最多 5 次重试`；4) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 增加 RTT 采样快照与来源选择（candidate pair / remote outbound / remote inbound），并把 `video_rtt_source` 写入 runtime stats；5) `crates/xbxengine/core/src/backend.rs`、`crates/xbxengine/core/src/runtime.rs`、`crates/xbxengine/protocol/src/runtime.rs`、`src/main/mods/streaming/application/xbxengine-service.ts` 透传并打印 `rttSource`；6) `crates/xbxengine/api/src/lib.rs` 为 `XbxEngineNativeBinding` 新增 `set_runtime_config_json`，下发新配置时会重建 worker；7) `src/main/mods/streaming/application/xbxengine-native-binding.ts` 新增 runtime 配置文件加载（默认 `resources/xbxengine.runtime.json`）+ env 覆盖（`XBXENGINE_NACK_*` 与 `XBXENGINE_RTT_DIAGNOSTICS_*`）并在 N-API 启动前调用 `setRuntimeConfigJson`。附：新增默认配置文件 `resources/xbxengine.runtime.json`。已确认 `cargo fmt -p xbxengine -p xbxengine-api -p xbxengine-app -p xbxengine-protocol`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-195 | 将 Rust 解码侧 jitter buffer 显式收敛到 20~30ms 窗口（为云游戏低延迟目标预置） | Done | 2026-03-05 | 2026-03-05 | 按“jitter buffer 设置成 20~30ms”的要求，更新 `crates/xbxengine/core/src/webrtc_rs_video_decode.rs`：1) `SampleBuilder` 从固定 `max_late=128` 改为可配置构造，默认 `max_late_packets=512` 并启用 `with_max_time_delay(Duration::from_millis(30))`，把重排等待上限钳到 30ms；2) 新增解码帧队列门限策略，队列内有后续帧时达到 20ms 即释放，否则最长不超过 30ms，形成“20~30ms”抖动缓冲窗口；3) 增加环境变量覆盖入口：`XBXENGINE_VIDEO_JITTER_BUFFER_MIN_MS`、`XBXENGINE_VIDEO_JITTER_BUFFER_MAX_MS`、`XBXENGINE_VIDEO_JITTER_BUFFER_MAX_LATE_PACKETS`。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app` 与 `cargo test -p xbxengine`（22/22）通过。 |
| P-194 | 完成三管线 loss 分解回归：区分最终丢包与晚到恢复并透传到 Metrics 日志 | Done | 2026-03-05 | 2026-03-05 | 针对“稳态 `loss1s/loss5s` 持续 2~3% 原因不清”的问题，补齐三层管线诊断口径：1) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 修复 `cleanup_missing_packets` 逻辑，新增 `video_loss_finalized_count_total` 与 `video_loss_late_recovered_count_total` 计数，并增加两条单测覆盖“缺包超时 finalize”和“finalize 后晚到恢复”；2) `crates/xbxengine/core/src/backend.rs` 扩展 runtime stats 结构体字段；3) `crates/xbxengine/core/src/runtime.rs` 增加诊断窗口基线，向 `DiagnosticsPulse` 输出 `video_loss_finalized_packets_in_window` 与 `video_loss_late_recovered_packets_in_window`；4) `crates/xbxengine/protocol/src/runtime.rs` 与 `src/main/mods/streaming/application/xbxengine-service.ts` 同步协议和日志，`[XbxEngine][Metrics]` 新增 `lossFinalizedPkts/lossLateRecoveredPkts/lossNetPkts`，用于判断 2~3% 是“真实最终丢包”还是“窗口统计与乱序恢复时差”导致。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（22/22）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-193 | 收紧指标真实性：分辨率禁用协商 fallback，REMB 增加启动宽容期并补强 RTT 采样回退 | Done | 2026-03-05 | 2026-03-05 | 按“分辨率只要真实值、不允许 fallback”与“继续优化启动阶段码率爬升”的要求完成两类改造：1) `crates/xbxengine/core/src/runtime.rs` 与 `crates/xbxengine/core/src/stats.rs` 删除了协商 `snapshot.video_size` 的分辨率回退路径，分辨率仅来自 `decoded frame` 或 `stream SPS`；`src/main/mods/streaming/application/xbxengine-service.ts` 的 `[XbxEngine][Metrics]` 也不再回退到 `lastStats.resolution`，无真实值时输出 `unknown`；2) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 在 ControlLoop 新增 `REMB_STARTUP_GRACE_TICKS=5` 的启动宽容期，放宽前 5 个 control tick 的 loss 降档/恢复阈值，减少启动阶段过早保守降码；3) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 的 RTT 采样由“只读 current RTT”扩展为“current RTT + total/measurements 平均 RTT 回退”（CandidatePair/RemoteOutbound/RemoteInbound），提升 `rttMs` 采样命中率。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（20/20）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-192 | 让分辨率指标脱离协商回退：从 H264 码流（SPS）实时提取并作为诊断优先来源 | Done | 2026-03-05 | 2026-03-05 | 针对“当前分辨率主要来自协商回退、指标无效”的问题，完成了无解码场景下的真实分辨率提取：1) 新增 `crates/xbxengine/core/src/webrtc_rs_h264_resolution.rs`，实现轻量 H264 分辨率追踪器，支持 `Single NAL / STAP-A / FU-A` 三种 RTP 负载形式下的 SPS 解析（含 RBSP 去转义与 Exp-Golomb 读取）；2) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 在视频 `NetLoop` 热路径接入 tracker，按包提取并写入 `runtime_stats.latest_video_stream_width/height`；3) `crates/xbxengine/core/src/backend.rs` 新增 `latest_video_stream_width/height` 字段；4) `crates/xbxengine/core/src/runtime.rs` 的 `DiagnosticsPulse` 分辨率来源改为 `decoded frame -> stream SPS -> snapshot fallback`；5) `crates/xbxengine/core/src/stats.rs` 的 `resolution` 拼接同样优先 stream SPS，减少“协商尺寸假稳定”误导。新增 3 条单测覆盖 single/stap-a/fu-a SPS 解析。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（20/20）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-191 | 修正三层管线观测偏差：`loss` 口径改为超时未恢复缺包，`jitter` 异常值保护，`rtt` 空值保持最近有效样本 | Done | 2026-03-05 | 2026-03-05 | 针对压测日志里 `loss` 偏高、`rttMs=null`、`jitterMs` 偶发极端异常值的问题，重构了 `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs`：1) `loss` 由“RTP gap 即时计损”改为“缺包超时（`NACK_WINDOW_MS`）且未恢复后再计损”，并在 `on_stats_tick` 也执行 finalize，降低乱序对丢包率的虚高影响；2) `jitter` 增加时间戳跳变保护（`MAX_JITTER_TIMESTAMP_DELTA_RTP_UNITS`）、样本钳制（`MAX_JITTER_SAMPLE_RTP_UNITS`）与最终上限（`MAX_REPORTED_JITTER_MS`），异常时重置基线避免输出离谱值；3) `on_control_tick` 对 RTT 改为“仅在收到有效样本时更新”，避免偶发采样空值把已有 RTT 覆盖成 `null`。同步补充 2 条单测（RTT 保持与 jitter 异常保护），并更新 loss 相关断言。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine`（17/17）与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-190 | 收敛 `XbxEngine` 指标日志为分辨率/码率/loss/rtt/jitter 并清理无效噪声输出 | Done | 2026-03-05 | 2026-03-05 | 针对“日志里看不到 `loss/rtt/jitter` 且存在无关输出”的问题做了收敛：1) `crates/xbxengine/protocol/src/runtime.rs` 与 `crates/xbxengine/core/src/runtime.rs` 扩展 `DiagnosticsPulse` 字段，新增 `inbound_video_loss_ratio_1s/5s`、`video_rtt_ms`、`inbound_video_jitter_ms`，并由 runtime 每个诊断窗口直接携带；2) `src/main/mods/streaming/application/xbxengine-service.ts` 的 `[XbxEngine][Metrics]` 统一输出 `resolution/bitrateKbps/loss1sPct/loss5sPct/rttMs/jitterMs`；3) `src/main/mods/streaming/application/xbxengine-native-binding.ts` 对 stdio 模式的原生日志做降噪，只保留包含 `panic/error/failed/fatal` 的关键错误行，避免普通状态日志刷屏；4) `crates/xbxengine/api/src/lib.rs` 移除高频 host 往返 `eprintln`（request sent / response received），避免运行时控制面日志干扰指标观察。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-189 | 完成 `rfc/shedule.md` 三层管线闭环：`transport` 三循环化并移除旧 REMB 独立任务 | Done | 2026-03-05 | 2026-03-05 | 以“三管线完整优先、继续切断 decode/render”为目标补齐闭环：1) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 的 `on_track` 改为 `tokio::select!` 三循环：`NetLoop`（`read_rtp` + NACK/PLI 动作）、`StatsLoop`（1s tick 调 `on_stats_tick` 并回写 runtime stats）、`ControlLoop`（1s tick 采样 RTT 调 `on_control_tick` 并发送 REMB）；2) 移除了旧的 `start_remb_feedback_loop` 与 `remb_feedback_task` 状态，避免双控制源冲突，`forced/adaptive` 开关继续生效但由 `WebRtcRsVideoPipelineState::new(forced, adaptive)` 统一决策；3) `crates/xbxengine/core/src/backend.rs` 补齐管线统计字段合同（`inbound_video_jitter_ms`、`video_nack_per_sec`、`video_pli_per_min`、`video_pending_missing_packets`），`crates/xbxengine/core/src/stats.rs` 同步填充 `jit` 展示。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-188 | 为三层管线接入自适应 REMB 控制环（EWMA 平滑）并补 RTT 采样 | Done | 2026-03-05 | 2026-03-05 | 在 `P-187` 的 NACK/丢包窗口基础上继续补齐码率控制闭环：1) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 的 REMB 发送任务升级为“强制 REMB / 自适应 REMB”双模式：当设置 `XBXENGINE_FORCE_REMB_KBPS` 时维持原固定值；未设置时默认启用自适应 REMB（可用 `XBXENGINE_ADAPTIVE_REMB_ENABLED=0/false` 关闭）；2) 自适应策略按 1s tick 读取 `loss_1s + RTT` 决策目标码率，并做 EWMA 平滑（`alpha=0.2`），参数为 `target=25Mbps / min=20Mbps / max=30Mbps`，降档条件 `loss>5% 或 RTT>180ms`（乘 `0.85`），恢复条件 `loss<1% 且 RTT<100ms`（每次 `+2Mbps`）；3) RTT 通过 `peer_connection.get_stats()` 采样（优先 `CandidatePair.current_round_trip_time`，回退 `RemoteOutbound/RemoteInbound`），并写回 runtime stats；4) `crates/xbxengine/core/src/backend.rs` 与 `crates/xbxengine/core/src/stats.rs` 扩展了 `video_rtt_ms`、`video_remb_bps` 及 `pl/rtt/br` 聚合输出。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-187 | 为三层管线补齐 NACK 批处理/节流与 1s/5s 丢包窗口统计 | Done | 2026-03-05 | 2026-03-05 | 在 `P-186` 的“先切断 decode/render”基础上继续完善反馈稳定器：1) `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs` 新增缺包跟踪队列（最多 256 条）、NACK 批处理策略（20ms batch 节流、200ms 生存窗口、60ms 重试间隔、最多 3 次重试、单批最多 16 个序列），并输出 1s/5s 丢包率窗口；2) `crates/xbxengine/core/src/webrtc_rs_transport.rs` 在 `on_track` 热路径接入 `TransportLayerNack` RTCP 发送，按管线动作发送 batch NACK，同时保留 PLI 节流；3) `crates/xbxengine/core/src/backend.rs` 扩展运行时指标字段，补充 `inbound_video_loss_ratio_1s/5s`、`video_nack_request_count_total`、`video_nack_batch_count_total` 等统计，作为后续 REMB/控制策略输入。附加补充：为管线新增测试覆盖 NACK 与窗口统计。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-186 | 按 `rfc/shedule.md` 落地 xbxengine 三层管线首版并先切断解码渲染链路 | Done | 2026-03-05 | 2026-03-05 | 以“先稳网络与反馈、暂不做 decode/render”为目标完成首版改造：1) 新增 `crates/xbxengine/core/src/webrtc_rs_video_pipeline.rs`，把视频接收拆为网络层（RTP 到达/序列跟踪）、统计层（包量与丢包缺口估算）、控制层（PLI 节流触发，最小间隔 200ms）；2) `webrtc_rs_transport` 的 `on_track` 热路径已移除 `process_remote_video_packet`，改为仅 drain RTP、累计吞吐/包统计并按管线动作发送 RTCP PLI；3) `XbxEngineMediaRuntimeStats` 增加视频包活动字段（latest packet time/seq、包总数、缺口估算、PLI 计数）；4) `recovery/runtime` 改为优先基于“视频包活动”判定 stall，避免切断渲染后误触发重连，并在 `DiagnosticsPulse` 分辨率读取上回退到协商尺寸。附加补充：`webrtc_rs_video_pipeline` 新增 3 条单测覆盖缺口估算与 PLI 节流。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app`、`cargo test -p xbxengine` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-185 | 新增 RTCP 压测启动脚本并暴露 npm 命令入口 | Done | 2026-03-05 | 2026-03-05 | 新增 `scripts/dev-rtcp-test.sh` 作为一键启动脚本：默认开启 `XBXENGINE_NEGOTIATION_BRANCH=rtcp-test`，并提供协商分辨率/码率、强制 REMB、N-API 路径的环境变量默认值（均可外部覆盖），启动前自动执行 `pnpm run cargo:build:xbxengine-api:release` 再拉起 `electron-vite dev`。`package.json` 已新增 `dev:rtcp:test` 命令指向该脚本。已确认 `bash -n scripts/dev-rtcp-test.sh` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-184 | 新增 RTCP 压测分支并支持环境变量覆盖协商码率/分辨率 | Done | 2026-03-05 | 2026-03-05 | 参考 `rfc/rtcp.md` 的吞吐探针思路，在 Rust 侧新增协商测试分支：`crates/xbxengine/core/src/webrtc_rs_negotiation_profile.rs` 统一解析环境变量并提供单例协商配置；分支开关 `XBXENGINE_NEGOTIATION_BRANCH=rtcp-test` 打开后，可用 `XBXENGINE_NEGOTIATION_RESOLUTION=WIDTHxHEIGHT` 覆盖协商分辨率、`XBXENGINE_NEGOTIATION_BITRATE_KBPS` 覆盖协商码率（支持 `N` 或 `MIN,TARGET,MAX` 两种格式），并可通过 `XBXENGINE_NEGOTIATION_MIN_BITRATE_KBPS` / `XBXENGINE_NEGOTIATION_MAX_BITRATE_KBPS` / `XBXENGINE_NEGOTIATION_MAX_FRAME_RATE` 细调。`network_profile` 的 `dimensionschanged`、`webrtc_rs_transport` 的 SDP `b=AS/x-google/max-fs/max-fr` 注入、`webrtc_rs_backend` 的协商期视频尺寸都已改为读取该配置，确保同一测试分支内协商参数一致。已确认 `cargo fmt -p xbxengine` 与 `cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app` 通过。 |
| P-183 | 纠正分辨率改造范围：仅将 Rust 测试链路协商切到 1081（1440p 档） | Done | 2026-03-05 | 2026-03-05 | 根据“只改 Rust 测试链路分辨率协商为 1081”要求，先撤回了 TS 侧误改：`src/main/mods/streaming/infrastructure/bridges/config-service-bridge.ts` 恢复 `resolution: config.resolution ?? 1080`，`src/main/mods/config/domain/defaults.ts` 恢复默认 `resolution: 720`。随后把 Rust 协商链路切到 1081 语义（Auto/1440p Max Quality）：`crates/xbxengine/core/src/network_profile.rs` 的 `STREAM_VIEWPORT_WIDTH/HEIGHT` 从 `1920x1080` 调整为 `2560x1440`（影响 `dimensionschanged` 协商与 SDP `max-fs` 约束基线），并同步 `crates/xbxengine/core/src/webrtc_rs_backend.rs` 协商期默认视频尺寸到 `2560x1440`。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-182 | 将串流分辨率调整为 1081（测试期强制） | Done | 2026-03-05 | 2026-03-05 | 按“把分辨率调整为 1081”的要求，先在主进程串流配置桥 `src/main/mods/streaming/infrastructure/bridges/config-service-bridge.ts` 强制返回 `resolution: 1081`（覆盖本地历史配置，保证压测链路一致）；同时将默认配置 `src/main/mods/config/domain/defaults.ts` 的 `resolution` 从 `720` 调整为 `1081`，确保新环境初始化一致。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-181 | 为 Rust WebRTC 栈补齐 RTCP/TWCC 反馈并增加强制 REMB 压测开关 | Done | 2026-03-05 | 2026-03-05 | 按“需要通过 REMB/反馈机制影响发送端带宽估计，测试码率上限”的目标，在 `crates/xbxengine/core/src/webrtc_rs_transport.rs` 做了两项改造：1) `APIBuilder` 初始化改为显式调用 `register_default_interceptors`（启用默认 NACK + RTCP SR/RR + TWCC receiver），不再是空拦截器链；2) 新增可选强制 REMB 回馈环，通过环境变量 `XBXENGINE_FORCE_REMB_KBPS` 打开，按 500ms 周期向 peer 写 `ReceiverEstimatedMaximumBitrate` RTCP 包（绑定当前视频 SSRC），用于上限压测时主动抬高接收侧估计上限。并在重建/停止 peer connection 时显式中止 REMB 任务避免泄漏。已确认 `cargo fmt -p xbxengine` 与 `cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app` 通过。 |
| P-180 | 修复 Rust offer 预补丁触发 `setLocalDescription` 失败回归 | Done | 2026-03-05 | 2026-03-05 | 回归表现为 `setLocalDescriptionFailed:new sdp does not match previous offer`。根因是 `webrtc-rs` 栈对 `create_offer()` 产物与 `set_local_description()` 入参做一致性校验，不能像浏览器实现那样直接在 `setLocalDescription` 前改写 SDP。已将 `crates/xbxengine/core/src/webrtc_rs_transport.rs` 的流程恢复为“原始 offer 先 set_local_description，随后仅对发往远端的 SDP 做策略补丁”，从而避免本地状态机校验失败，同时保留本轮 30/20/60 与 1080 约束验证。已确认 `cargo fmt -p xbxengine` 与 `cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app` 通过。 |
| P-179 | 在 Rust WebRTC offer 里硬编码 1080p 与 30/20/60 Mbps 码率策略用于验证 | Done | 2026-03-05 | 2026-03-05 | 按“先不接配置，先写死验证，SDP 标准参考 webrtc-direct”完成一版 Rust 侧硬编码：`crates/xbxengine/core/src/webrtc_rs_transport.rs` 在 `create_offer()` 后新增 `apply_offer_policy_contract()`，对本地 offer 执行与 `webrtc-direct` 一致的 `b=AS` 视频带宽注入（使用 `max * 1024` 口径），并补充 `x-google-min/start/max-bitrate=20000/30000/60000`；同时在 video `fmtp` 追加 `max-fs`（按 `1920x1080` 计算）与 `max-fr=60`，并保持 `useinbandfec=1; stereo=1` 音频处理。分辨率维持 `network_profile` 的固定 `1920x1080` dimensions 合同。已确认 `cargo fmt -p xbxengine` 与 `cargo check -p xbxengine -p xbxengine-api --features napi -p xbxengine-app` 通过。 |
| P-178 | 将 session HostBridge 协议收口到 shared，并在 main 统一桥实现 | Done | 2026-03-05 | 2026-03-05 | 按“webrtc-direct/rust-owned 都一跳，桥统一在 main，协议放 shared”的要求，新增 `src/shared/rpc/stream-host.ts` 与 `XBoxRpcSchema.streamHost`（`exchangeOffer/exchangeIce/keepAliveRemoteSession/closeRemoteSession`）；main 侧新增 `src/main/mods/streaming/application/stream-host-bridge-service.ts` 作为统一 HostBridge 实现，并新增 `src/main/rpc/handlers/stream-host.ts` 暴露 RPC。`webrtc-direct` 现改为调用 `rpc.streamHost.*`（`src/renderer/src/streaming/runtime/webrtc-direct/main-remote-session-bridge.ts`），`xbxengine-service` 也已改为依赖同一 `StreamHostBridgeService`（`src/main/mods/streaming/application/xbxengine-service.ts`），不再直接耦合 `StreamingService`。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-177 | 将 main 侧观测收敛为码率与分辨率，并移除冗余日志 | Done | 2026-03-05 | 2026-03-05 | 按“只要码率和分辨率两个指标，并去除不必要日志”的要求，`src/main/mods/streaming/application/xbxengine-service.ts` 已将周期日志收敛为 `[XbxEngine][Metrics]`，只输出 `resolution` 与 `bitrateKbps`（视频入站码率）；并移除了 host request、offer/ice 往返、phase/transport 等高频 `console.info` 冗余日志，仅保留必要错误日志。为保证分辨率在无渲染场景下也可读，`DiagnosticsPulse` 协议新增 `video_width/video_height`，并在 `crates/xbxengine/core/src/runtime.rs` 中按窗口填充，`crates/xbxengine/protocol/src/runtime.rs` 同步更新。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-176 | 扩展 `N-API` 诊断脉冲为网络吞吐观测，支持无渲染场景测码率上限 | Done | 2026-03-05 | 2026-03-05 | 按“先走 N-API、不管渲染，测试 WebRTC 发送端码率上限”的目标，补了不依赖解码帧的吞吐观测：`crates/xbxengine/core/src/backend.rs` 的 `XbxEngineMediaRuntimeStats` 新增入站总字节/视频字节/音频字节累计计数；`crates/xbxengine/core/src/webrtc_rs_transport.rs` 在 `on_track` RTP 读取循环里按包累计这些计数；`crates/xbxengine/core/src/runtime.rs` 的 `DiagnosticsPulse` 新增窗口吞吐字段（`inbound_kbps/inbound_video_kbps/inbound_audio_kbps`），即使 `frames=0` 也可判断网络侧是否在持续收包；协议层 `crates/xbxengine/protocol/src/runtime.rs` 与主进程日志桥 `src/main/mods/streaming/application/xbxengine-service.ts` 已同步字段并打印。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol -p xbxengine-api -p xbxengine-app`、`cargo check -p xbxengine -p xbxengine-protocol -p xbxengine-api --features napi -p xbxengine-app` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-175 | 移除 renderer application 对 HostBridge 的注入，收口到 runtime 内部 main bridge | Done | 2026-03-05 | 2026-03-05 | 根据“`streaming/runtime` 两个实现不应依赖 application 传入 HostBridge”的要求，已删除 `src/renderer/src/streaming/application/createStreamRuntimeHostBridge.ts`，并把 `StreamRuntimeStartContext` 中的 `hostBridge` 入参移除；`useStreamController.ts` / `useStreamRuntime.ts` 不再构造与传递 HostBridge。`webrtc-direct` 侧新增 `src/renderer/src/streaming/runtime/webrtc-direct/main-remote-session-bridge.ts`，在 runtime 内部直接通过 main RPC 处理 `exchangeOffer/exchangeIce/keepAlive/closeRemoteSession`，避免“application 组装 bridge 再下传 runtime”的链路；`rust-owned` 也同步脱离该入参，仅保留 `session/viewport/config/audio` 启动上下文。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-173 | 在 control bootstrap 阶段立即请求关键帧，缩短首帧挂起窗口 | Done | 2026-03-04 | 2026-03-04 | 针对 `dev:stdio:release` 下 `transport=Connected` 但 `frames=0` 的首帧长期挂起现象，在 `crates/xbxengine/core/src/webrtc_rs_data_channel.rs` 的 `should_start_control` 分支补充了“授权 + gamepadRemoved”后的立即 `videoKeyframeRequested`，不再完全依赖 5 秒周期 loop 的下一拍触发，降低首帧卡在空窗期的概率。已确认 `cargo fmt -p xbxengine` 与 `cargo check -p xbxengine -p xbxengine-app -p xbxengine-api --features napi` 通过。 |
| P-172 | 增加 `release + stdio` 开发脚本以拉起 Rust 原生窗口 | Done | 2026-03-04 | 2026-03-04 | 为解决“`dev:release-napi` 不会打开 Rust 原生窗口”的问题，在 `package.json` 新增 `cargo:build:xbxengine-app:release` 与 `dev:stdio:release`。新脚本会先 release 构建 `xbxengine-api` 和 `xbxengine-app`，再用 `XBXENGINE_BINDING=stdio` 与 `XBXENGINE_APP_PATH=target/release/xbxengine-app` 启动 Electron，从而在 release 模式下走 stdio 并拉起原生窗口。 |
| P-171 | 新增 release N-API 开发脚本，避免 debug 软解导致指标失真 | Done | 2026-03-04 | 2026-03-04 | 按“补一个脚本”的要求，在 `package.json` 新增 `cargo:build:xbxengine-api:release` 与 `dev:release-napi`：前者用 `--release` 构建 `xbxengine-api`，后者通过 `XBXENGINE_NAPI_PATH=target/release/libxbxengine_api.dylib` 强制 Electron 开发态加载 release addon，避免默认优先命中 `target/debug` 导致软解码指标偏低。 |
| P-170 | 将 `xbxengine` 渲染帧像素载荷改为共享引用，减少大块内存复制 | Done | 2026-03-04 | 2026-03-04 | 按“帧数据改成共享引用，避免大块 `Vec<u8>` 复制”的要求，已把渲染帧类型统一切到共享缓冲：`crates/xbxengine/core/src/backend.rs` 的 `XbxEngineRenderFrame.rgba_bytes` 由 `Vec<u8>` 改为 `Arc<[u8]>`；`crates/xbxengine/core/src/webrtc_rs_render.rs` 的中间帧 `WebRtcRsRenderFrame` 同步改为 `Arc<[u8]>`；`crates/xbxengine/core/src/webrtc_rs_video_decode.rs` 在 `openh264` 解码后将像素缓存收敛为 `Arc<[u8]>` 再流转；`crates/xbxengine/app/src/main.rs` 的 bootstrap 帧也改为共享缓冲；`crates/xbxengine/app/src/wgpu_renderer.rs` 上传路径改为 `frame.rgba_bytes.as_ref()`。这样 `core -> app` 链路里帧对象 clone 只复制引用计数，不再复制整块像素数据。已确认 `cargo fmt -p xbxengine -p xbxengine-app`、`cargo check -p xbxengine -p xbxengine-app -p xbxengine-api --features napi` 与 `cargo test -p xbxengine` 通过。 |
| P-169 | 优化 Rust data channel 背压与任务句柄生命周期 | Done | 2026-03-04 | 2026-03-04 | 针对四个 channel 的稳定性继续优化：`crates/xbxengine/core/src/network_profile.rs` 新增 `STREAM_INPUT_MAX_BUFFERED_AMOUNT_BYTES=256KB` 背压阈值，并收紧 `message` 握手 ack 校验（`type` 必须匹配，`id/version` 存在时需与约定常量一致）；`crates/xbxengine/core/src/webrtc_rs_data_channel.rs` 中 `input` 上行循环改为复用 `frames` 缓冲与固定签名数组，减少 8ms 热路径分配；发送流程改为“先判定 buffered amount，再发送成功后提交 sample signature”，避免拥塞期误判已发送导致状态丢失。同时将 delayed `gamepadAdded`、`control keyframe loop`、`input stream loop` 全部改为显式 `JoinHandle` 管理，channel close 时会中止对应后台任务并允许 reopen 干净重启，不再依赖裸 `spawn` 自行回收。已确认 `cargo fmt --all`、`cargo check -p xbxengine -p xbxengine-api -p xbxengine-app`、`cargo test -p xbxengine --lib network_profile`、`npm run typecheck:node` 通过。 |
| P-168 | 新增 `xbxengine` 诊断脉冲事件并在 Electron main 打印 | Done | 2026-03-04 | 2026-03-04 | 为了先做一条可观测链路定位卡顿，`crates/xbxengine/protocol/src/runtime.rs` 新增 `DiagnosticsPulse` runtime event（包含 window 时长、窗口内帧数、估算 fps、render idle、transport state）；`crates/xbxengine/core/src/runtime.rs` 在 `tick()` 内新增 1 秒窗口诊断发射逻辑，持续向宿主推送当前渲染活跃度，不依赖 renderer 侧统计；`src/main/mods/streaming/application/xbxengine-service.ts` 则在 `runtimeEvent` 入口优先识别 `DiagnosticsPulse` 并直接打印 `[XbxEngine][Diagnostics]` 日志（当前不桥接到 renderer，先收敛到 Electron main 观察）。已确认 `cargo fmt -p xbxengine -p xbxengine-protocol`、`cargo check -p xbxengine -p xbxengine-app -p xbxengine-api --features napi` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-167 | 收敛 `xbxengine-app` 渲染节奏，降低无新帧时空转重绘 | Done | 2026-03-04 | 2026-03-04 | 继续处理“有画面但卡顿”的渲染抖动：`crates/xbxengine/app/src/wgpu_renderer.rs` 的 present mode 从优先 `AutoNoVsync` 改为优先 `AutoVsync/Fifo`，先保证帧间隔稳定；`crates/xbxengine/app/src/main.rs` 的事件循环改为按需 `request_redraw`，仅在有新真实帧或 bootstrap 阶段才重绘，避免收到首帧后无新帧时仍持续空转；窗口 resize 事件会显式请求一次 redraw，保证尺寸变化后立即刷新。已确认 `cargo fmt -p xbxengine-app`、`cargo check -p xbxengine-app` 与 `cargo check -p xbxengine-api --features napi` 通过。 |
| P-166 | 补齐 Rust 四类 data channel 的关键时序并完成全量迁移回归 | Done | 2026-03-04 | 2026-03-04 | 按“全量迁移 channel 到 Rust”的要求，继续对齐 web 协议时序并补足可恢复性：`crates/xbxengine/core/src/network_profile.rs` 新增 `STREAM_CONTROL_GAMEPAD_ADDED_DELAY_MS=500`；`crates/xbxengine/core/src/webrtc_rs_data_channel.rs` 中 `control` 启动流程改为 `authorization -> gamepadRemoved -> 延迟 gamepadAdded`（与 web `ControlChannel` 对齐），并保留 5s 周期 keyframe 请求；同时修正 tokio `interval` 的首 tick 立即触发行为，`control/input` 循环都改成首个周期后再发送，避免和浏览器 `setInterval` 节奏漂移。新增 data channel `on_close` 状态复位逻辑，确保 `message/control/input/chat` 在抖动后的 reopen 能重新执行 handshake/bootstrap，不会卡在“已启动”状态。结合上一轮已落地内容，Rust 侧 `message/control/input/chat` 四个 channel 现在都具备对应协议实现（其中 chat 维持当前协议无消息体的轻实现边界）。已确认 `cargo fmt --all`、`cargo check -p xbxengine -p xbxengine-api -p xbxengine-app`、`cargo test -p xbxengine --lib network_profile`、`npm run typecheck:node` 通过。 |
| P-165 | 去掉 `xbxengine core` 内部重复渲染上传并收敛帧拷贝路径 | Done | 2026-03-04 | 2026-03-04 | 针对“画面能渲染但明显卡顿”的 Rust 内部链路，先完成一轮止血重构：`crates/xbxengine/core/src/webrtc_rs_render.rs` 删除了 headless `wgpu` staging 上传逻辑，`core` 渲染状态收敛为“仅缓存 latest frame + display state”，不再在 `core` 内做额外 GPU upload；`crates/xbxengine/core/src/webrtc_rs_video.rs` 同步去掉 `render_frame.clone()` 路径，改为移动所有权到 render state，并由 `present_frame()` 回传轻量 `frame_stats`；`crates/xbxengine/core/Cargo.toml` 同步移除已无使用的 `pollster` 与 `wgpu` 依赖，避免后续再把 `core` 拉回上传职责。同时修复了 `crates/xbxengine/core/src/webrtc_rs_data_channel.rs` 中 data channel 回调闭包的 move 变量复用问题，保证本次回归可编译。已确认 `cargo fmt -p xbxengine`、`cargo check -p xbxengine`、`cargo test -p xbxengine` 通过。 |
| P-164 | 纠正 `xbxengine` 输入架构偏差，恢复 `core/api` 直连 `GamepadRuntimeHost` | Done | 2026-03-04 | 2026-03-04 | 根据“`xbxengine-app` 不关心输入，`xbxengine-api / core` 应该直连 `GamepadRuntimeHost`，整个应用只保留一个 gamepad 单实例”的要求，撤回了上一轮把输入误收口到 Electron host bridge 的方向：`crates/xbxengine/core/src/input.rs` 恢复为 `OhMyGamepadXbxEngineInputBackend` 直接依赖 `GamepadRuntimeHost::shared()`，`crates/xbxengine/core/Cargo.toml` 重新显式依赖 `ohmygamepad-host/protocol`；`crates/xbxengine/api/src/lib.rs` 的 runtime 构造也恢复为使用 `OhMyGamepadXbxEngineInputBackend::new()`，因此 `xbxengine-api` 与 `XbxEngineGamepadNativeBinding` 在同一进程内会共享同一份 gamepad owner。与此同时，`crates/xbxengine/app/src/{lib,main,stdio_mode}.rs` 已退回纯窗口壳语义：runtime 默认注入 `NoopXbxEngineInputBackend`，不再经由 host bridge 代理输入 owner；`crates/xbxengine/protocol/src/host_bridge.rs`、`src/main/mods/streaming/application/xbxengine-service.ts`、`src/main/mods/gamepad/application/gamepad-runtime-controller.ts` 中上一轮新增的输入 host request/forwarding 也一并移除，避免 Electron 被误变成 gamepad 必经桥。已确认 `cargo fmt --all`、`cargo check -p xbxengine -p xbxengine-api -p xbxengine-app` 与 `npm run typecheck:node` 通过。 |
| P-163 | 将 `xbxengine` 输入 owner 收回宿主桥，去掉 `app/core` 直连 `OhMyGamepad` 单例 | Done | 2026-03-04 | 2026-03-04 | 按“`xbxengine-app` 不该自己拥有手柄输入 owner，channel/通讯/输入语义都应留在 core，后续还要支持窗口融合”的要求，已把 active 输入架构改成 host-driven：`crates/xbxengine/protocol/src/host_bridge.rs` 新增 `AttachInputSession / PressControllerButton / SnapshotInputStatus / StopInputSession` 及对应 status/sampling DTO；`crates/xbxengine/core/src/input.rs` 删除直接 `GamepadRuntimeHost::shared()` 的 `OhMyGamepadXbxEngineInputBackend`，改为 `HostBridgeXbxEngineInputBackend`，由 core 经由 host bridge 请求宿主侧唯一 gamepad owner；`crates/xbxengine/app/src/lib.rs`、`crates/xbxengine/app/src/{main,stdio_mode}.rs` 与 `crates/xbxengine/api/src/lib.rs` 也同步收口，不再把 `OhMyGamepad` backend 直接注入 runtime，而是共享同一 host bridge 给 runtime 与 input backend。主进程 `src/main/mods/gamepad/application/{gamepad-native-binding,gamepad-runtime-controller}.ts` 新增 host 所需的 stream-session attach/stop 与 `pressControllerButton` 能力，`src/main/mods/streaming/application/xbxengine-service.ts` 则将这些新 host request 全部转发到 `getGamepadRuntimeController()`，让 Electron main 继续作为唯一 gamepad 单例 owner。与此同时，`crates/xbxengine/core/Cargo.toml` 已移除对 `ohmygamepad-host/protocol` 的直接依赖，边界重新回到“core 只依赖宿主协议，不依赖具体 gamepad 实现”。已确认 `cargo fmt --all`、`cargo check -p xbxengine -p xbxengine-api -p xbxengine-app` 与 `npm run typecheck:node` 通过。 |
| P-162 | 将 `xbxengine-app --stdio` 的输入宿主从 `Noop` 切回真实 `OhMyGamepad` backend | Done | 2026-03-04 | 2026-03-04 | 在确认“键盘不该额外映射为手柄，串流手柄输入逻辑上只能来自 gamepad”后，继续收口当前原生窗口验证链的真实问题：`crates/xbxengine/app/src/stdio_mode.rs` 之前仍把 runtime input backend 注入为 `NoopXbxEngineInputBackend`，导致 `xbxengine-app --stdio` 虽然能出画面，但手柄输入链在 app 内部被直接短接。现已改为使用 `OhMyGamepadXbxEngineInputBackend::new()`；此前默认本地 bootstrap 模式也已切到同一 backend，保证原生窗口验证宿主与主线 `ohmygamepad -> xbxengine` 输入 owner 保持一致。已确认 `cargo check -p xbxengine -p xbxengine-app` 通过。 |
| P-161 | 修复 `xbxEngine` 在显式 `stdio` 模式下因 app 二进制缺失导致的启动回归 | Done | 2026-03-04 | 2026-03-04 | 根据主进程启动报错确认，当前环境里只有旧的 `target/debug/streamsidecar-app`，而 `src/main/mods/streaming/application/xbxengine-native-binding.ts` 在检测到 `XBXENGINE_BINDING=stdio` 后只查找新的 `xbxengine-app`，找不到就直接返回 `null`，导致后续 `xbxEngineNativeBindingUnavailable`。现已将该逻辑收敛为两层兜底：一是 `resolveXbxEngineAppCandidates()` 同时兼容 `xbxengine-app` 与旧的 `streamsidecar-app` 二进制名；二是即便显式请求了 `stdio`，如果 app 二进制不存在，也会记录告警后自动回退到 `xbxengine-api` N-API，而不再直接把启动打死。同时 `package.json` 新增 `cargo:build:xbxengine-app`，`dev:stdio` 会同时构建 `xbxengine-api` 与 `xbxengine-app`，避免显式 stdio 调试入口继续依赖旧产物。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过，并验证当前 worktree 下确实存在 `target/debug/streamsidecar-app`、不存在 `target/debug/xbxengine-app`。 |
| P-160 | 将 renderer `rust-owned` client 命名与当前文档表述继续收口到 `xbxEngine` | Done | 2026-03-04 | 2026-03-04 | 按“继续 1、2”的要求，继续清掉 `rust-owned` renderer client 层和当前文档里的 `Sidecar/streamsidecar` 具体命名：`src/renderer/src/streaming/runtime/rust-owned/createRustOwnedSidecarClient.ts` 与 `sidecar-client.ts` 已分别迁到 `createRustOwnedXbxEngineClient.ts` 和 `xbxengine-client.ts`，对应接口/工厂函数统一改为 `RustOwnedXbxEngineClient` 与 `createRustOwnedXbxEngineClient()`，`RustOwnedRuntime` 的注释与 import 也已同步；同时更新 `src/shared/config/domain-definition.ts`、`crates/README.md` 与 `docs/streaming-architecture.md` 中面向当前主线的旧 `sidecar/streamsidecar` 表述，统一到 `xbxEngine` 语义。当前 active 代码里仅保留 `contracts.ts` 中 `browser | sidecar` 这类抽象 owner 枚举作为语义边界，不再残留具体 `streamsidecar*` 文件名、类型名或宿主入口命名。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-159 | 将 active TS 宿主协议与事件命名从 `streamsidecar` 收口到 `xbxEngine` | Done | 2026-03-04 | 2026-03-04 | 在完成 Rust `xbxengine` 目录与 crate 重命名后，继续把当前活跃的 Electron/Vue/TypeScript 接入层命名同步到新主线：`src/shared/rpc/streamsidecar.ts` / `src/shared/events/streamsidecar.ts` 已分别迁到 `src/shared/rpc/xbxengine.ts` 与 `src/shared/events/xbxengine.ts`，共享 DTO 类型统一改为 `XbxEngine*`；`src/shared/rpc/contract.ts` 的 RPC namespace 也从 `streamingSidecar` 改为 `xbxEngine`，renderer 事件名从 `streaming.sidecarRuntimeEvent` 改为 `streaming.xbxEngineRuntimeEvent`。与此同时，主进程 `src/main/mods/streaming/application` 里的 native binding / service、`src/main/rpc/handlers` 的 handler 装配、`src/main/shell/application/feat` 的 runtime bridge 与 lifecycle 注册点都已切到 `XbxEngine*` 命名，renderer 侧 `createRustOwnedSidecarClient.ts` 也改为消费 `rpc.xbxEngine` 与新事件名。当前 active 代码里残留的 `STREAMSIDECAR_*` 仅作为 `XBXENGINE_*` 环境变量的兼容 fallback，不再是主命名。已确认 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-158 | 将 active `xbxengine` 的 `webrtc-rs` 视频协商收紧到 H264 | Done | 2026-03-04 | 2026-03-04 | 在确认当前 active Rust 媒体栈已迁移到 `crates/xbxengine/core`，且视频解码链仍然只有 `openh264 + H264Packet` 支持后，为避免远端协商到 `VP8/VP9` 这类当前根本不能解码的 codec，已在 `crates/xbxengine/core/src/webrtc_rs_transport.rs` 中对 video transceiver 显式设置 H264 codec preferences，只保留当前实现能消费的几组 H264 profile；这样当前 `webrtc-rs` offer 会与 active 解码能力保持一致。已确认 `cargo check -p xbxengine -p xbxengine-app` 通过。 |
| P-157 | 将 Rust `streamsidecar` 家族重命名为 `xbxEngine` 并收敛到统一目录 | Done | 2026-03-04 | 2026-03-04 | 按“关注 Rust 侧，把 `streamsidecar` 重命名为 `xbxEngine` 并且收敛到统一目录”的要求，已将原 `crates/streamsidecar-protocol`、`crates/streamsidecar`、`crates/streamsidecar-api`、`crates/streamsidecar-app` 统一迁移到 `crates/xbxengine/{protocol,core,api,app}`，并同步完成 crate 名、Rust import、N-API 导出类名、动态库文件名与开发构建脚本的重命名：当前主宿主统一加载 `xbxengine_api`，导出 `XbxEngineNativeBinding` / `XbxEngineGamepadNativeBinding`，原生窗口验证宿主改为 `xbxengine-app`。`src/main/mods/streaming/application/streaming-sidecar-native-binding.ts` 与 `src/main/mods/gamepad/application/gamepad-native-binding.ts` 现已优先使用 `XBXENGINE_*` 环境变量与新产物名，并保留旧 `STREAMSIDECAR_*` 变量作为兼容 fallback；`crates/README.md` 与 `docs/streaming-architecture.md` 也已同步当前命名。过程中顺手修复了 `crates/xbxengine/core/src/runtime.rs` 测试假体 `ScriptedMediaBackend` 仍使用 `Rc<RefCell<_>>` 导致不满足 `Send` 的旧问题，改为 `Arc<Mutex<_>>` 后已确认 `cargo fmt -p xbxengine -p xbxengine-api -p xbxengine-app -p xbxengine-protocol`、`cargo check --workspace`、`cargo check -p xbxengine-api --features napi`、`cargo test -p xbxengine -p xbxengine-protocol -p xbxengine-api` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-156 | 将 streaming 与 Electron 开发入口切到 `streamsidecar-api` N-API 主模式 | Done | 2026-03-04 | 2026-03-04 | 在完成 `P-155` 的 owner 收敛后，继续把当前活跃接入模式真正切到 `streamsidecar-api`：`package.json` 的 `dev` 现已改为默认构建并加载 `streamsidecar-api --features napi`，不再默认强制 `STREAMSIDECAR_BINDING=stdio`；同时新增 `dev:stdio` 作为显式原生窗口/stdio 验证入口。`src/main/mods/streaming/application/streaming-sidecar-native-binding.ts` 的说明也已同步为“N-API 为主、stdio 仅作显式调试路径”，`crates/README.md` 也补充了 Electron 开发期默认已切回 `streamsidecar-api` N-API 的说明。这样当前 streaming 与 Electron 主路径都已经以 `streamsidecar-api` 为统一接入层，`stdio` 只在显式选择时启用。 |
| P-155 | 将 OhMyGamepad owner 收敛到独立 host，并切到 `streamsidecar-api` 主宿主路径 | Done | 2026-03-04 | 2026-03-04 | 按“继续 1、2、3”的要求，新增 `crates/ohmygamepad/bridge/host` 作为 `ohmygamepad` 域内的单实例 owner 层，用 `OnceLock<Arc<OhMyGamepadService>>` 收口当前进程里的唯一 gamepad runtime；随后将 `crates/streamsidecar/src/input.rs` 改为依赖 `GamepadRuntimeHost`，不再直接 `spawn/use OhMyGamepadService`，并在 `stop()` 时仅把 route target 复位到 `ShellUi` 而不再关闭全局 owner。与此同时，为 `crates/streamsidecar-api` 新增 `StreamSidecarGamepadNativeBinding` 与 gamepad JSON contract，使宿主可以直接经由 `streamsidecar_api` 暴露 gamepad `snapshot/subscribe/route/sampling/rumble` 接口；`src/main/mods/gamepad/application/gamepad-native-binding.ts` 也已改为从 `streamsidecar_api` 加载 `StreamSidecarGamepadNativeBinding`，`package.json` 的开发构建脚本同步切到 `cargo build -p streamsidecar-api --features napi`。`ohmygamepad-bridge-napi` crate 仍保留在 workspace 中，但已从当前 Electron 主路径禁用。已确认 `cargo fmt -p ohmygamepad-host -p streamsidecar -p streamsidecar-api`、`cargo check --workspace`、`cargo check -p streamsidecar-api --features napi` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过；额外尝试 `cargo test -p streamsidecar -p ohmygamepad-host` 时，命中了 `crates/streamsidecar/src/runtime.rs` 里现有测试假体 `ScriptedMediaBackend` 未满足 `Send` 的旧问题，这不是本次 owner 收敛引入的新编译错误。 |
| P-154 | 为 rust-owned 原生窗口补齐远端视频 codec 静默失败日志 | Done | 2026-03-04 | 2026-03-04 | 在 ICE 已经成功喂进 sidecar、但窗口仍只显示占位画面后，继续收窄问题到视频 track / decode 层：`crates/streamsidecar/src/webrtc_rs_transport.rs` 的 `on_track` 现已显式记录远端视频 codec，如果收到的 video track 不是当前 active 解码链唯一支持的 `H264`，会直接输出 `unsupported remote video codec` 日志，而不是继续静默卡在占位画面；若是 `H264`，仍沿用现有“首个视频 RTP 包 / 首个解码帧”日志用于判断是 track 没来还是 decode 没出帧。已确认 `cargo check -p streamsidecar -p streamsidecar-app` 通过。 |
| P-153 | 修复 rust-owned 原生窗口会话链过早接受 `end-of-candidates` 的 ICE 回归 | Done | 2026-03-04 | 2026-03-04 | 根据主进程日志定位到当前 native-window 验证链卡在 `Connecting | New` 的根因：`src/main/mods/streaming/application/stream-signaling-service.ts` 里的 `waitForIceCandidates()` 只要拿到任意非 `null` 响应就结束轮询，而 `StreamingIceNormalizer` 在远端候选尚未真正可用时也会返回仅包含 `a=end-of-candidates` 的数组，导致 sidecar 过早拿到“只有结束标记、没有真实远端 candidate”的 ICE 响应。现已调整为“至少包含一个可用远端 candidate”才结束轮询；同时在 `src/main/mods/streaming/application/streaming-sidecar-service.ts` 的 `ExchangeIce` 响应日志中补充 `usableCandidateCount`，便于继续判断会话是否真正拿到了远端 ICE。已确认 `npm run typecheck:node` 通过。 |
| P-152 | 删除 xCloud 标题目录里的第三方 jsDelivr 源 | Done | 2026-03-04 | 2026-03-04 | 按要求从 `src/main/mods/data/application/services/xcloud-service.ts` 中彻底移除 jsDelivr 托管的第三方 `titles.json` 源：删除对应常量、响应类型、`extractOfficialProductIds()` 解析分支，以及 `fetchAndCacheTitles()` 中对该 URL 的并发请求。当前 xCloud 目录只再使用 xCloud live titles、MRU 和 Game Pass catalog 这几条现有来源，不再在启动预热或页面加载时访问该第三方地址。 |
| P-151 | 修复 dev 启动时 OhMyGamepad N-API 缺失的回归路径 | Done | 2026-03-04 | 2026-03-04 | 修复 `src/main/mods/gamepad/application/gamepad-native-binding.ts` 的 native addon 候选路径回归：恢复对旧 `ohmygamepad_api` 动态库/`.node` 文件名的兼容查找，避免已有旧构建产物时仍误报 “addon not found”；同时在缺失告警里补出明确的 `pnpm run cargo:build:ohmygamepad-bridge-napi` 提示。另将 `package.json` 的 `dev` 脚本前置为自动执行 `cargo build -p ohmygamepad-bridge-napi`，避免开发启动依赖手工先编译 Rust bridge。 |
| P-150 | 修正原生窗口将协商期默认视频尺寸误当成真实首帧信号的问题 | Done | 2026-03-04 | 2026-03-04 | 在继续排查“窗口只剩测试图案”时，确认 `crates/streamsidecar/src/runtime.rs` 会在协商完成后立刻发出带默认 `1920x1080` 的 `MediaVideoReady`，这不是实际解码帧；因此前一轮把 `window_state.video_size` 当成“远端媒体已就绪”信号会导致原生窗口过早停掉 bootstrap 动画。现已在 `crates/streamsidecar-app/src/main.rs` 去掉这条误判，只保留“真正收到 render frame”才能停 bootstrap；同时在 `src/main/mods/streaming/application/streaming-sidecar-service.ts` 增加 `runtime.phaseChanged / transport.connectionState / error` 的主进程日志，方便继续定位当前会话为何停在 `Connecting | New`。已确认 `npm run typecheck:node` 与 `cargo check -p streamsidecar-app` 通过。 |
| P-149 | 按最小粒度收敛 OhMyGamepad `gilrs` 的 service 相关模块 | Done | 2026-03-04 | 2026-03-04 | 按“按最小粒度拆分”的要求，将 `crates/ohmygamepad/backends/gilrs` 继续收口为更稳定的最小边界：新增 `src/service_source.rs` 承接 `OhMyGamepadServiceSource` 这层事件入口适配；将原先只服务 `gilrs::Gamepad -> capability/connection` 推断的 `src/capability.rs` 合回 `src/source.rs`；将原先只服务 rumble target 解析的 `src/service_device.rs` 合回 `src/service_rumble.rs`。这样当前 `gilrs` 服务相关实现已稳定收敛为 `oh_my_gamepad_service / service_source / service_keyboard / service_rumble / source` 这几个必要模块，不再保留额外中间层。已确认 `cargo fmt -p ohmygamepad-gilrs -p ohmygamepad-core -p ohmygamepad-bridge-napi`、`cargo test -p ohmygamepad-gilrs -p ohmygamepad-core -p ohmygamepad-bridge-napi` 与 `cargo check --workspace` 通过。 |
| P-148 | 修复原生窗口首帧阶段因锁竞争导致的 bootstrap 误回退 | Done | 2026-03-04 | 2026-03-04 | 在首轮“测试图案只在首帧前显示”的修复后，继续追到另一个会导致持续闪烁的根因：`crates/streamsidecar-app/src/main.rs` 主线程原先用 `try_lock()` 非阻塞读取 `StreamSidecarApp`，而 stdio worker 线程会在 `tick()/drain_events()` 中高频持锁，导致原生窗口在首帧阶段可能连续抢不到锁，持续误判成“尚未收到真实帧”并继续 present bootstrap pattern。现已改为短路径 `lock()` 保证主线程能稳定取到首帧，同时把 `window_state.video_size` 也作为“远端媒体已就绪”的辅助信号，一旦收到 `MediaVideoReady` 就彻底停用 bootstrap。已确认 `cargo fmt --all` 与 `cargo check -p streamsidecar-app` 通过。 |
| P-147 | 修复 Rust 原生窗口在真实视频与测试图案之间闪烁的问题 | Done | 2026-03-04 | 2026-03-04 | 在 `crates/streamsidecar-app/src/main.rs` 中修正原生窗口主循环的 frame 选择逻辑：测试图案现在只在“尚未收到任何远端真实帧”时作为首帧前占位，一旦 `take_latest_render_frame()` 首次返回真实远端帧，就保持上一张真实纹理继续 present，不再因为某一轮没有拿到新帧而回退到 bootstrap pattern，避免“游戏画面/测试图案”交替闪烁。已确认 `cargo fmt --all` 与 `cargo check -p streamsidecar-app` 通过。 |
| P-146 | 为 Electron 主进程补齐 `streamsidecar-app --stdio` 会话接线 | Done | 2026-03-04 | 2026-03-04 | 在不改现有 `StreamingSidecarService` 会话/信令语义的前提下，继续把 Rust 原生窗口验证链接回主进程：`src/main/mods/streaming/application/streaming-sidecar-native-binding.ts` 现已新增 `StdioStreamingSidecarNativeBinding`，负责拉起 `target/{debug,release}/streamsidecar-app --stdio`（可由 `STREAMSIDECAR_APP_PATH` 覆盖）、通过 line-delimited JSON 转发 control/host-request/runtime-event，并在主进程关闭时回收子进程；binding 选择也已收口为“默认优先 N-API，`STREAMSIDECAR_BINDING=stdio` 时强制走原生窗口验证宿主，N-API 缺失时也会自动回退到可执行的 `streamsidecar-app`”。已确认 `npm run typecheck:node` 通过，并用 Node 直接验证了 `ready -> StartRuntime -> hostRequest(ExchangeOffer)` 这条最小会话桥；后续完整远端画面仍需在本机真实图形环境下由用户启动 Electron 验证。 |
| P-145 | 将 OhMyGamepad `gilrs` 的键盘 fallback 状态机从 service 中剥离 | Done | 2026-03-04 | 2026-03-04 | 按“继续 1”的要求，继续缩小 `crates/ohmygamepad/backends/gilrs/src/oh_my_gamepad_service.rs` 的职责：将原先内嵌在 `OhMyGamepadServiceSource` 中的键盘 fallback 抑制/恢复状态机提炼为 `src/service_keyboard.rs` 内的 `ServiceKeyboardFallbackGate`，统一承接“初始键盘设备公告、首个物理设备接入后抑制键盘、最后一个物理设备断开后恢复键盘、抑制期间丢弃键盘输入”这组规则；同时为该 gate 新增独立单测，避免 fallback 语义只靠 service 集成测试间接覆盖。已确认 `cargo fmt -p ohmygamepad-gilrs -p ohmygamepad-core -p ohmygamepad-bridge-napi`、`cargo check --workspace` 与 `cargo test -p ohmygamepad-gilrs -p ohmygamepad-core -p ohmygamepad-bridge-napi` 通过。 |
| P-144 | 继续下沉 OhMyGamepad `gilrs` 的设备能力与 rumble 解析逻辑 | Done | 2026-03-04 | 2026-03-04 | 按“继续 1”的要求，继续收敛 `crates/ohmygamepad/backends/gilrs` 中 `OhMyGamepadService` 对 backend 细节的直接感知：新增 `src/capability.rs` 统一承接 `gilrs::Gamepad` 到 `connection/capabilities` 的推导，`src/service_device.rs` 统一承接 rumble target 到已连接设备列表的解析，`src/service_rumble.rs` 则扩成完整的 rumble dispatch 规划模块，负责 `TargetNotFound / Unsupported / NotImplemented / accepted` 这些结构化结果判定；`src/source.rs` 与 `src/oh_my_gamepad_service.rs` 因而退回到“采集事件/编排命令”的职责，不再内嵌设备能力推断与 rumble 支持决策。已确认 `cargo fmt -p ohmygamepad-gilrs -p ohmygamepad-core -p ohmygamepad-bridge-napi`、`cargo check --workspace` 与 `cargo test -p ohmygamepad-gilrs -p ohmygamepad-core -p ohmygamepad-bridge-napi` 通过。 |
| P-143 | 为 Rust 原生窗口宿主恢复可用的 stdio 外部 host bridge 验证入口 | Done | 2026-03-04 | 2026-03-04 | 在 `P-141` 的原生窗口 present 链基础上，继续把 `crates/streamsidecar-app` 从“只能本地 bootstrap 测试图案”推进到“可接真实外部宿主”的状态：`crates/streamsidecar-app/src/lib.rs` 与 `src/window.rs` 已改为跨线程安全的 `Arc<Mutex>`/`Send` 结构，`crates/streamsidecar/src/{backend,input,webrtc_rs_control,webrtc_rs_stack}.rs` 也同步补齐 `Send` 边界，使 runtime 可以安全放进 worker thread；新增 `crates/streamsidecar-app/src/stdio_mode.rs`，恢复 `streamsidecar-protocol` 现有的 stdio 消息协议，形成“stdin 读 control/host response、stdout 写 ready/control/runtime-event/host-request、runtime worker 线程驱动 `StreamSidecarApp`、主线程只负责 `winit + wgpu` 原生窗口渲染”的验证架构；`crates/streamsidecar-app/src/main.rs` 现支持 `--stdio` 模式，默认仍保留本地 bootstrap。已确认 `cargo fmt --all`、`cargo check --workspace` 通过，并实际运行 `cargo run -p streamsidecar-app -- --stdio` 验证进程能正常启动并输出 `{\"kind\":\"ready\"}`。当前是否出真实远端视频，取决于外部进程是否按协议回送合法的 `OfferExchanged/IceExchanged`。 |
| P-142 | 将 OhMyGamepad `dto` 重命名为 `protocol` 并继续收敛 core/gilrs 结构 | Done | 2026-03-04 | 2026-03-04 | 按“先把 dto 改名为 protocol，然后执行 1、2 项”的要求，已将 `crates/ohmygamepad/dto` 重命名为 `crates/ohmygamepad/protocol`，同步修正 root workspace、各 crate `Cargo.toml`、Rust crate 名、代码 import 与相关路径引用；同时继续收敛 `ohmygamepad-core` 目录结构，把原先平铺的 `backend/config/profile/sink/engine/runner/mapping/filter` 重排为 `api / model / mapping / filter / runtime` 五层，并把 `ohmygamepad-gilrs` 服务里原本内嵌的键盘 fallback 线程与 rumble 判定逻辑分别拆到 `service_keyboard.rs` 和 `service_rumble.rs`，减少 `OhMyGamepadService` 的单文件职责。已确认 `cargo fmt -p ohmygamepad-protocol -p ohmygamepad-core -p ohmygamepad-gilrs -p ohmygamepad-bridge-napi -p streamsidecar`、`cargo check --workspace` 与 `cargo test -p ohmygamepad-core -p ohmygamepad-gilrs -p ohmygamepad-bridge-napi` 通过。 |
| P-141 | 打通 Rust 独立原生窗口的 `wgpu + winit` 渲染验证宿主 | Done | 2026-03-04 | 2026-03-04 | 按“先不考虑 Electron 交付，直接在 Rust 侧打开原生窗口走通流程”的要求，重新把 `crates/streamsidecar-app` 纳入 workspace，改造成 `winit + wgpu` 的独立原生窗口宿主：`crates/streamsidecar/src/backend.rs` / `runtime.rs` / `webrtc_rs_{render,stack,backend}.rs` 现已补出 `take_latest_render_frame()` 出口，让 runtime 可以直接吐出最新 RGBA render frame；`crates/streamsidecar-app/src/wgpu_renderer.rs` 新增原生窗口 present 实现，`src/main.rs` 则在主线程创建 `winit` 窗口、持续拉取最新 render frame 并在无真实远端媒体时回退到本地测试图案，以验证 Rust 原生窗口 present 链本身。过程中还顺手修正了当前 worktree 已经切到 `crates/ohmygamepad/protocol` 后，`crates/streamsidecar/Cargo.toml` 仍残留旧 `dto` path 的 workspace 编译阻断。已确认 `cargo fmt --all`、`cargo check --workspace` 通过，并实际运行 `cargo run -p streamsidecar-app` 验证进程可稳定进入原生窗口事件循环；当前仍缺真实 host bridge answer，所以独立宿主里看到的是测试图案而非真实远端视频。 |
| P-140 | 调研 `win-xbox-haptics` 的实现路线与平台兼容边界 | Done | 2026-03-04 | 2026-03-04 | 已确认 `crates/ohmygamepad/backends/win-xbox-haptics/src/lib.rs` 当前仅为占位实现，统一返回 `HapticsProviderError::Unsupported`，运行时选择器 `crates/ohmygamepad/core/src/runtime/selector.rs` 也尚未把它纳入实际桌面选择，当前真实 rumble 仍只走 `gilrs` 的基础双马达能力。结合 Microsoft 文档，后续若要实现 Xbox 四马达/扳机震动，Windows 桌面侧更合适的真实路线是 `GameInput` 或 `Windows.Gaming.Input`；其中 `XInput` 仅稳定覆盖双马达，`GameInput` 则在 PC 上通过 `Microsoft.GameInput` NuGet + redistributable 提供更完整的 haptics/rumble 能力，兼容范围可回溯到 Windows 10 19H1。结论是：当前仓库里该 backend 在所有平台都只是“可编译占位”；如果要落地真实能力，应保留非 Windows stub，并在 Windows 下新增 `cfg(target_os = "windows")` 的专用实现与分发策略。 |
| P-139 | 将 active render 链切到最小 headless `wgpu` backend | Done | 2026-03-04 | 2026-03-04 | 继续按单一路线收口 Rust render 主线：`crates/streamsidecar/src/webrtc_rs_render.rs` 已从默认 no-op backend 切到 `WebRtcRsWgpuRenderBackend`，当前会在收到 `WebRtcRsRenderFrame` 后初始化最小 `wgpu` device/queue，并将 RGBA 帧上传到 GPU staging buffer；同时 `crates/streamsidecar/src/webrtc_rs_stack.rs` 新增 `apply_display_state()` 口，把显示状态稳定收口到 render owner，`crates/streamsidecar/src/webrtc_rs_backend.rs` 也同步改为将 `surface_id` 暴露为 `wgpu:{viewport_id}` 并把 display state 转发给 stack/render。当前仍未接入 `winit` 窗口 surface，也没有 shader/颜色调节等高级能力，但 render 主线已经不再是 no-op 占位。已确认 `cargo fmt --all` 与 `cargo check --workspace` 通过。 |
| P-138 | 删除当前阶段无保留价值的 OhMyGamepad mock/recorder/mapper-cli crates | Done | 2026-03-04 | 2026-03-04 | 按当前 active Rust 主线收口 `ohmygamepad` workspace：删除 `crates/ohmygamepad/backends/mock`、`crates/ohmygamepad/tools/recorder` 与 `crates/ohmygamepad/tools/mapper-cli`，同时把 `bridge/napi` 与 `streamsidecar` 退回为只直接依赖 `ohmygamepad-gilrs`，不再保留 mock 选择分支；`ohmygamepad-core` 的 `DesktopBackendPreference` / `DesktopDriverSelector` 也同步去掉 `Mock` 枚举与测试，避免“目录删了但选择逻辑仍可选 mock”的坏状态。并更新 `crates/README.md` 与任务台账，明确当前 active Rust 输入主线只保留 `gilrs + bridge + sidecar`。已确认 `cargo fmt -p ohmygamepad-core -p ohmygamepad-bridge-napi -p streamsidecar`、`cargo check --workspace` 与 `cargo test -p ohmygamepad-core -p ohmygamepad-bridge-napi` 通过。 |
| P-137 | 移除主线 runtime 中残留的 softbuffer RGB 帧接口 | Done | 2026-03-04 | 2026-03-04 | 继续按“主线只保留 Electron + N-API + webrtc-rs”收口 active 视频链：`crates/streamsidecar/src/backend.rs` 中已删除 `StreamSidecarDecodedVideoFrame` 与 `take_latest_video_frame()` 合同，`crates/streamsidecar/src/runtime.rs`、`crates/streamsidecar/src/webrtc_rs_backend.rs`、`crates/streamsidecar/src/webrtc_rs_stack.rs` 同步去掉这条仅服务 `softbuffer` demo 的宿主接口；随后又直接对 render 链做破坏性收口，在 `crates/streamsidecar/src/webrtc_rs_render.rs` 中建立显式 render backend 合同与默认 no-op backend，删除 `crates/streamsidecar/src/webrtc_rs_video_output.rs`，并将 `webrtc_rs_video_decode.rs` / `webrtc_rs_video.rs` 改为只产出和消费统一的 `WebRtcRsRenderFrame`。这样 active 视频链已经从“decode + GStreamer output/present”切到“decode -> render backend”，后续可以直接往 `wgpu/winit` 目标实现推进；`crates/streamsidecar/Cargo.toml` 里的 `gstreamer/gstreamer-app` 依赖也已随之移除。已确认 `cargo fmt --all` 与 `cargo check --workspace` 通过。 |
| P-136 | 显式锁定 Rust toolchain 并同步 workspace 版本声明 | Done | 2026-03-04 | 2026-03-04 | 当前本机已升级到 `rustc 1.93.1`，仓库原先没有显式的 toolchain/`rust-version` 声明，容易继续出现“系统版本变了但项目约束没跟上”的漂移。现已在根目录新增 `rust-toolchain.toml`，锁定 `1.93.1` 并声明 `rustfmt`/`clippy` 组件；同时在 root `Cargo.toml` 的 `workspace.package` 中补充 `rust-version = "1.93"`，并让所有活跃 workspace crate 通过 `rust-version.workspace = true` 继承该约束。已确认 `cargo fmt --all --check`、`cargo check --workspace` 与 `cargo test --workspace` 通过。 |
| P-135 | 收口 Rust 主线选型并移除非主线技术栈入口 | Done | 2026-03-04 | 2026-03-04 | 按 `rfc/render.md` 与当前 Electron 主产品边界收口主线选型：root workspace 已移除 `crates/ohmygamepad/bridge/tauri` 与 `crates/streamsidecar-app` member，`crates/streamsidecar/src/runtime_stack.rs` 改为只保留 active `webrtc-rs` 媒体栈入口，不再并行暴露 `DirectWindowGstreamerBackend` / reference stack；`crates/streamsidecar/src/gstreamer_backend.rs` 与 `scripts/run-streamsidecar-gst-demo.sh` 已删除，`crates/streamsidecar/Cargo.toml` 也同步移除 `gstreamer-webrtc/gstreamer-sdp/gstreamer-video` 这类只服务旧 `webrtcbin` 路线的依赖。与此同时，`docs/streaming-architecture.md` 与 `crates/README.md` 的当前表述已统一为 `Electron + N-API + webrtc-rs` 主线，GStreamer 仅作为 active 视频链内部的 decode/output 过渡实现，render 目标固定为 `wgpu + winit`。 |
| P-134 | 移除 bridge/sidecar 对 OhMyGamepad `api` crate 的依赖层 | Done | 2026-03-04 | 2026-03-04 | 按新架构收掉 `ohmygamepad-api` 这层过渡 facade：`crates/ohmygamepad/bridge/napi` 与 `crates/streamsidecar` 现已直接组合 `ohmygamepad-core + backends/{gilrs,mock}`，各自持有本地 runtime delegate 与 selector 选择逻辑，不再通过额外 API crate 启动服务；同时从 root workspace 删除 `crates/ohmygamepad/api` member，删除该 crate 文件，更新 `streamsidecar-api` 构造残留、`src/main/mods/gamepad/application/gamepad-native-binding.ts` 中旧 `ohmygamepad_api` 动态库 fallback、`package.json` 构建脚本命名以及 `crates/README.md` 当前结构说明。已确认 `cargo fmt -p streamsidecar -p streamsidecar-api -p ohmygamepad-bridge-napi -p ohmygamepad-bridge-tauri`、`cargo check --workspace`、`cargo test -p ohmygamepad-core -p ohmygamepad-mock -p ohmygamepad-recorder -p ohmygamepad-bridge-napi`、`pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-133 | 将 active `webrtc-rs` 的 keyframe 请求接回现有 control data channel | Done | 2026-03-04 | 2026-03-04 | 在保持当前有效协商/媒体路径不变的前提下，将 `crates/streamsidecar/src/webrtc_rs_control.rs` 中的 control port 从纯占位改为 `WebRtcRsDataChannelMediaControl`：通过一个很小的 `WebRtcRsMediaControlContext` 直接委托 `crates/streamsidecar/src/webrtc_rs_transport.rs`，再复用 `crates/streamsidecar/src/webrtc_rs_data_channel.rs` 里现有 `control` channel 的 `videoKeyframeRequested` payload 发送逻辑。这样 runtime/recovery 触发的 `request_video_keyframe()` 已经能走回当前事实链路，而不需要提前引入 PLI/flush 等高级动作；同时保留 `WebRtcRsNoopMediaControl` 作为后续扩展/测试备用实现。已确认 `cargo fmt --all` 与 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-132 | 按 RFC 目录层级重排 OhMyGamepad Rust workspace | Done | 2026-03-04 | 2026-03-04 | 继续收敛“结构必须像 RFC，不只是语义接近”的问题：已将 `crates/ohmygamepad` 下原本扁平的 `gilrs/mock/hid-dualsense/win-xbox-haptics/bridge-napi/bridge-tauri/recorder/mapper-cli` 目录重排为 `backends/{gilrs,mock,hid-dualsense,win-xbox-haptics}`、`bridge/{napi,tauri}`、`tools/{recorder,mapper-cli}`，并同步修正根 workspace members 与各 crate/sidecar 的 path 依赖。这样当前仓库结构已经真实收敛到 RFC 里的 `core / backends / bridge / tools` 层级，而不是只在命名上接近。已确认 `cargo check --workspace` 与 `cargo test -p ohmygamepad-core -p ohmygamepad-api -p ohmygamepad-mock -p ohmygamepad-recorder` 通过。 |
| P-131 | 为 active `webrtc-rs` stack 抽出独立 control/action port | Done | 2026-03-04 | 2026-03-04 | 继续按“先把架构收口到最佳状态，再回填原有链路能力”的原则，在 `crates/streamsidecar/src/webrtc_rs_control.rs` 中新增 `WebRtcRsMediaControlPort` 与默认 `WebRtcRsNoopMediaControl`，把 recovery/runtime 后续会触发的 `request_video_keyframe()` 动作从 `webrtc_rs_stack` 主体中剥离为独立控制面；`crates/streamsidecar/src/webrtc_rs_stack.rs` 现已改为通过 `with_control()` 注入 control port，stack 自身只保留 transport/data-channel/video/runtime stats 的组合职责。这样后续补真实 PLI/flush/render control 时，不需要再回写 backend 或 stack 主体结构。已确认 `cargo fmt --all` 与 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-130 | 将 active `webrtc-rs` backend 继续压薄为 stack port 外壳 | Done | 2026-03-04 | 2026-03-04 | 继续按“先把架构收口到最佳状态，再回填原有链路能力”的原则，在 `crates/streamsidecar/src/webrtc_rs_stack.rs` 中新增 `WebRtcRsMediaStackPort` 与 `WebRtcRsActiveMediaStack` 组合根，让 active `webrtc-rs` 栈自己持有 tokio runtime、transport/data-channel/video 状态与后续 no-op 动作位；`crates/streamsidecar/src/webrtc_rs_backend.rs` 现在只剩 placeholder 输入门面 + `Box<dyn WebRtcRsMediaStackPort>` 外壳，不再直接耦合 active stack 的内部状态布局。这样后续补 keyframe/recovery/render owner 时，可以优先扩 stack port，而不是再把 backend 壳重新做胖。已确认 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-129 | 为 RFC 缺位模块补占位工程并移除 renderer 末端 legacy `InputFrame` | Done | 2026-03-04 | 2026-03-04 | 按用户要求同时推进两件事：一是在 Rust workspace 中新增 `crates/ohmygamepad/{bridge-tauri,hid-dualsense,win-xbox-haptics,mapper-cli}` 四个占位 crate，分别提供可编译的 bridge/backend/CLI 壳与统一 `Unsupported` 行为；二是收掉 renderer 输入末端的 legacy `InputFrame`，将 `src/renderer/src/player/domain/input.ts`、`GamepadDriver`、`InputService`、`InputChannel`、`PlayerClient` 和 `streaming/runtime` 控制输入边界统一改为消费 `GamepadFrame + LogicalPadStateDto + LogicalButtonDto`，只在 `InputPacketEncoder` 最末端做一次 packet 映射编码。顺手修复了 `StatsService` 的空值统计类型问题与 `InputPacketEncoder` 的 pointer target 类型收窄，使 `vue-tsc` 恢复通过。已确认 `cargo fmt -p ohmygamepad-core -p ohmygamepad-api -p ohmygamepad-hid-dualsense -p ohmygamepad-win-xbox-haptics -p ohmygamepad-bridge-tauri -p ohmygamepad-mapper-cli`、`cargo check --workspace`、`cargo test -p ohmygamepad-core -p ohmygamepad-api -p ohmygamepad-mock -p ohmygamepad-recorder`、`pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与 `pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 通过。 |
| P-128 | 继续拆分 active `webrtc-rs` 视频链的 decode 与 output 边界 | Done | 2026-03-04 | 2026-03-04 | 在保持当前有效媒体路径不变的前提下，新增 `crates/streamsidecar/src/webrtc_rs_video_decode.rs` 与 `crates/streamsidecar/src/webrtc_rs_video_output.rs`，将 active `webrtc-rs` 栈里原先混在 `webrtc_rs_video.rs` 中的 H264 sample builder / openh264 软解与 GStreamer output/present 逻辑进一步拆开；`webrtc_rs_video.rs` 现在只保留薄组合层，把 decode 产物与 output 写入、runtime stats 更新串起来。这样 active 视频路径内部已经形成 decode/output 两段边界，后续接真实 render/present owner 或替换 output 路径时不需要再回到单文件里混改。已确认 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-127 | 继续拆分 active `webrtc-rs` transport 协商层 | Done | 2026-03-04 | 2026-03-04 | 在继续保持现有有效信令/媒体路径不变的前提下，新增 `crates/streamsidecar/src/webrtc_rs_transport.rs`，将 active `webrtc-rs` 栈里的 peer connection 创建、offer/answer 应用、ICE candidate 处理、transceiver 配置、data channel 创建与 transport 回调安装从 `webrtc_rs_backend.rs` 中抽离出来；`webrtc_rs_backend.rs` 现在只保留 active stack 外壳、transport/video/data-channel 状态组装与 `StreamSidecarMediaBackend` 实现。这样 active 路径内部已经形成 backend/data-channel/video/transport 四段边界，后续补真实 keyframe/recovery/render 时不需要再回到单大文件里改。已确认 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-126 | 收拢 OhMyGamepad core 的 provider 与 selector 边界 | Done | 2026-03-04 | 2026-03-04 | 继续按 RFC 收敛 `ohmygamepad-core`：新增 `src/api/{input_provider,haptics_provider}.rs` 与 `src/runtime/selector.rs`，显式补出 `InputProvider`、`HapticsProvider` 和 `DesktopDriverSelector` 三层边界；其中 selector 当前统一返回 `DesktopInputProviderKind + DesktopHapticsProviderKind`，默认选择 `gilrs + basic haptics`，mock 模式选择 `mock + none`。同时将 `crates/ohmygamepad/api/src/lib.rs` 的 facade 启动路径改为通过 core selector 选择 backend，而不再自己直接 `match backend_preference`。已确认 `cargo fmt -p ohmygamepad-core -p ohmygamepad-api`、`cargo test -p ohmygamepad-core -p ohmygamepad-api -p ohmygamepad-mock -p ohmygamepad-recorder`、`cargo check --workspace` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-125 | 为 OhMyGamepad 补齐 recorder 与 mock replay 最小闭环 | Done | 2026-03-04 | 2026-03-04 | 继续按 `rfc/new-gamepad.md` 收敛 Rust 侧回归测试能力：新增 `crates/ohmygamepad/recorder` crate，定义 JSONL 录制格式 `Connect/Disconnect/SubmitState`，提供 `read_recording_jsonl` / `write_recording_jsonl` 与 `ohmygamepad-recorder summary|validate` CLI；同时在 `crates/ohmygamepad/mock/src/replay.rs` 中新增 `replay_recording()`，可将录制脚本按顺序或实时节奏回放到现有 `OhMyGamepadMockService`。这样 `mock` 已不再只是手工注入状态，而是能直接承接录制脚本做回放测试。已确认 `cargo fmt --all`、`cargo test -p ohmygamepad-recorder -p ohmygamepad-mock -p ohmygamepad-core -p ohmygamepad-api`、`cargo check --workspace` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-124 | 将 OhMyGamepad runtime snapshot 事件源下沉到 core runner | Done | 2026-03-04 | 2026-03-04 | 继续把 gamepad bridge 的 emitter 语义往 Rust 内部下沉：在 `crates/ohmygamepad/core/src/runner.rs` 中新增 runtime snapshot broadcaster，由 runner 线程在启动、采样推进、设备变化、路由切换、采样配置变更、binding/profile 更新后统一发布 `OhMyGamepadRuntimeSnapshotDto`；`InputRuntimeHandle`、`OhMyGamepadService`、`OhMyGamepadMockService` 与 `OhMyGamepadFacade` 现已暴露 `subscribe_runtime_snapshot()` 订阅接口。随后将 `crates/ohmygamepad/bridge-napi/src/lib.rs` 改为直接消费 facade 订阅流，不再内部轮询 `facade.snapshot()`；并在 `core` 补了一条订阅测试覆盖初始快照和后续 route 变更推送。已确认 `cargo fmt --all`、`cargo test -p ohmygamepad-core -p ohmygamepad-bridge-napi -p ohmygamepad-api -p ohmygamepad-mock`、`cargo check --workspace` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-123 | 继续拆分 active `webrtc-rs` 媒体栈内部职责 | Done | 2026-03-04 | 2026-03-04 | 延续“先收口架构，不改有效路径”的策略，继续拆分 `streamsidecar` 当前实际生效的 active `webrtc-rs` 媒体栈：新增 `crates/streamsidecar/src/webrtc_rs_data_channel.rs` 与 `crates/streamsidecar/src/webrtc_rs_video.rs`，将 data channel 握手/后握手 contract、视频 RTP 处理/openh264 软解/GStreamer 输出从 `webrtc_rs_backend.rs` 主文件中拆出；`webrtc_rs_backend.rs` 现在主要保留 peer connection 协商、active stack 组装与 backend trait 实现。该重构不改变当前信令与媒体行为，仅降低 active stack 的单文件复杂度，并已确认 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-122 | 先按有效信令/媒体路径收口 Rust sidecar 架构 | Done | 2026-03-04 | 2026-03-04 | 按“当前信令、媒体都是有效路径，要先参考好”的原则，先只做 `streamsidecar` 内部架构收口而不改现有行为：新增 `crates/streamsidecar/src/{recovery,stats,runtime_stack}.rs`，把 runtime 里的 stall/reconnect 判定、统计聚合、active/reference 媒体栈组合入口拆成独立模块；同时为 `Box<dyn StreamSidecarMediaBackend>` 补齐 trait 转发，让 `streamsidecar-api` 与 `streamsidecar-app` 都改为统一通过 `create_active_media_backend()` 组装当前 active 的 `webrtc-rs` 媒体栈，不再各自硬编码具体 backend。该重构明确保留 `webrtc-rs` 为实际生效路径、`gstreamer/webrtcbin` 为参考路径，并已确认 `cargo check -p streamsidecar -p streamsidecar-api -p streamsidecar-app` 通过。 |
| P-121 | 将 OhMyGamepad bridge-napi 收敛为事件推送优先 | Done | 2026-03-04 | 2026-03-04 | 继续沿 `rfc/new-gamepad.md` 收敛 Rust 宿主桥：在 `crates/ohmygamepad/bridge-napi/src/lib.rs` 中新增 `subscribe_runtime_snapshot` / `unsubscribe_runtime_snapshot`，由 bridge 内部后台线程轮询 facade 快照、做 JSON 去重后通过 `napi` `ThreadsafeFunction` 主动推送给 Node；同时将 `src/main/mods/gamepad/application/gamepad-native-binding.ts` 改为优先注册 Rust 侧 runtime snapshot 回调，只对旧 addon 保留原有 60Hz polling fallback。这样 Electron main 不再对新 bridge 固定频率拉整份快照，宿主行为已更接近 RFC 中 `bridge/napi` 的 emitter 形态。已确认 `cargo fmt --all`、`cargo test -p ohmygamepad-bridge-napi -p ohmygamepad-api -p ohmygamepad-mock`、`cargo check --workspace` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-120 | 结合 `rfc/render.md` 评估当前 Rust 侧实现 | Done | 2026-03-04 | 2026-03-04 | 对照 `rfc/render.md` 审查当前 Rust sidecar 落地后，确认现状只达到“runtime 状态机 + WebRTC 协商/软解骨架 + N-API 宿主桥”的中间态，与 RFC 目标仍有四类关键差异：一是当前 Electron 实际启用的 `rust-owned` 路径仍以 `WebRtcRsNegotiationBackend` 为核心，活跃链路里没有 RFC 期望的 `render(wgpu + winit)` 模块，唯一原生 present 仍停留在 `streamsidecar-app` demo 的 `softbuffer` 软件渲染器；二是输入链路没有形成 RFC 要求的“共享状态覆盖写 + 固定 InputTickThread + DataChannel 背压降频”，当前 active backend 只完成 data channel 握手与 input metadata 首包，尚未在 sidecar 内看到稳定的控制器 packet tick/背压控制；三是恢复策略只部分落地，runtime 虽有 stall 检测与 keyframe/reconnect 阶梯，但 `WebRtcRsNegotiationBackend::request_video_keyframe()` 仍是空实现，缺少 RFC 指定的 PLI/flush/reconnect 完整链路；四是统计与模块分层也未达标，`snapshot_stats()` 仍主要返回空字符串/占位值，`streamsidecar` 也还没有按 RFC 收敛成独立的 `transport/video/render/recovery/stats` 子模块边界。 |
| P-119 | 回归 OhMyGamepad 当前实现与 `rfc/new-gamepad.md` 的剩余差异 | Done | 2026-03-04 | 2026-03-04 | 基于本轮 Rust 收敛后的代码再次对照 `rfc/new-gamepad.md`：已补齐 `crates/ohmygamepad/{dto,core,gilrs,mock,api,bridge-napi}` 的基础分层，`mock` 也已从占位入口升级为独立 backend/service，`api` 回到纯 Rust facade，`bridge-napi` 单独承接 Electron 宿主导出；但与 RFC 目标相比仍有四类主要差异未消除：一是 `backends/hid-dualsense`、`backends/win-xbox-haptics`、`bridge/tauri`、`tools/recorder`、`tools/mapper-cli`、`packages/js`、`examples/desktop-debug` 仍缺位；二是 `core` 还没有按 RFC 拆成 `model/mapping/filter/runtime/api` 目录，也尚未形成明确的 `HapticsProvider/InputProvider/selector` 组合；三是 Electron 集成仍由 main 侧 60Hz 轮询 snapshot 驱动，尚未达到 RFC 里 `bridge/napi` emitter 式事件桥的目标；四是 renderer 最终消费端仍保留 legacy `InputFrame` 映射，logical pad 模型尚未彻底贯穿到宿主末端。 |
| P-118 | 将 OhMyGamepad N-API 从 facade 拆到独立 bridge crate | Done | 2026-03-04 | 2026-03-04 | 继续收敛 Rust 分层：新增 `crates/ohmygamepad/bridge-napi`，将原先混在 `ohmygamepad-api` 中的 JSON contract、`OhMyGamepadNativeBinding` 和 `napi`/`serde_json` 宿主导出逻辑全部迁出；`crates/ohmygamepad/api` 现在只保留纯 Rust facade，不再带 `napi` feature、`cdylib` 产物或 build script。主进程 `src/main/mods/gamepad/application/gamepad-native-binding.ts` 已改为优先加载 `ohmygamepad_bridge_napi` 动态库/`.node`，同时保留旧文件名 fallback；`package.json` 的 Rust 构建脚本也已指向新 crate。已确认 `cargo test -p ohmygamepad-api -p ohmygamepad-mock -p ohmygamepad-bridge-napi`、`cargo check --workspace` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-117 | 将 mock 收敛为独立 Rust backend 与 facade service | Done | 2026-03-04 | 2026-03-04 | 继续沿 Rust 主线收敛 `ohmygamepad`：将 `crates/ohmygamepad/mock` 从单纯复用 `NoopGilrsSource` 的占位入口升级为独立实现，新增 `MockInputBackend`、`MockBackendHandle` 与 `OhMyGamepadMockService`，由 mock crate 自己维护设备生命周期、虚拟状态注入、采样策略绑定同步与无 rumble 的结构化拒绝结果；同时将 `crates/ohmygamepad/api/src/lib.rs` 改为 backend enum 委托，facade 不再直接持有 `OhMyGamepadService`，而是可在 `gilrs` / `mock` service 之间切换。已确认 `cargo test -p ohmygamepad-mock -p ohmygamepad-api` 与 `cargo check --workspace` 通过。 |
| P-116 | 收拢 OhMyGamepad Rust crates 并补 mock backend 选择入口 | Done | 2026-03-04 | 2026-03-04 | 聚焦 Rust 侧实现继续收敛工程结构：将 `ohmygamepad` 相关 workspace member 统一迁移到 `crates/ohmygamepad/{dto,core,gilrs,mock,api}` 目录下，更新根 workspace 与 `streamsidecar*` 对这些 crate 的 path 依赖；新增 `ohmygamepad-mock` crate，先复用 `NoopGilrsSource + OhMyGamepadService` 形成无硬件环境可启动的 mock service 入口；同时让 `crates/ohmygamepad/api/src/lib.rs` 根据 `InputCoreConfig.backend_preference` 选择 `gilrs` 或 `mock` backend，而不再在 facade 启动阶段只硬编码直连 `gilrs`。已确认 `cargo check --workspace` 与 `cargo test -p ohmygamepad-mock -p ohmygamepad-api` 通过。 |
| P-115 | 结合 `rfc/new-gamepad.md` 评估当前实现 | Done | 2026-03-04 | 2026-03-04 | 对照 `rfc/new-gamepad.md` 完成当前 gamepad 实现评估：现状已具备 `ohmygamepad-core + ohmygamepad-gilrs + ohmygamepad-api(napi)` 的最小闭环，Electron main/renderer 也已能消费运行时快照并驱动现有串流输入链；但与 RFC 目标结构相比仍有明显差距，主要体现在三类缺口：一是 bridge 仍是 `main` 侧 60Hz 轮询 `getRuntimeSnapshotJson()` 的快照桥，尚未形成 RFC 期望的独立 `bridge/napi` emitter 模块，也没有 `tauri` bridge；二是后端插件化尚未完成，workspace 里还没有 `hid-dualsense`、`win-xbox-haptics`、`mock`、`recorder`、`mapper-cli` 等 crate，`ohmygamepad-api` 仍直接绑定 `ohmygamepad-gilrs`；三是 renderer 输入边界仍停留在 legacy `InputFrame`，逻辑 pad 模型到 `GamepadDriver` 后又被重新映射回旧协议，说明统一输入模型尚未贯穿到最终消费端。 |
| P-114 | 修复 streamsidecar runtime 失败回滚与 webrtc 假首帧统计 | Done | 2026-03-04 | 2026-03-04 | 收敛 Rust runtime 宿主稳定性问题：在 `crates/streamsidecar/src/runtime.rs` 中为 `start()` 与 `request_reconnect()` 增加失败回滚，启动/重连若在 keepalive、offer 或 ICE 交换阶段报错，会恢复到上一个稳定状态而不再卡在 `Starting/Reconnecting`；同时将 `StreamSidecarMediaNegotiation` 的首帧时间字段改为 `Option<f64>`，在 `crates/streamsidecar/src/webrtc_rs_backend.rs` 中移除协商完成时伪造的 `latest_video_frame` 与首帧时间，改为只在真实视频帧到达后再由 runtime 填充首帧指标。`crates/streamsidecar/src/gstreamer_backend.rs` 与 `crates/streamsidecar/src/backend.rs` 已同步适配新合同，并补充 3 个单测覆盖启动失败回滚、重连失败回滚、等待真实首帧后才写入统计。已确认 `cargo test -p streamsidecar` 与 `cargo check -p streamsidecar -p streamsidecar-api --features napi` 通过。 |
| P-113 | 移除 Electron 到 Rust sidecar 的 stdio 集成链路 | Done | 2026-03-04 | 2026-03-04 | 按“所有宿主集成都必须采用 N-API”收敛当前实现：在 `src/main/mods/streaming/application/streaming-sidecar-native-binding.ts` 中删除 `streamsidecar-app --stdio` fallback、子进程读写与 `STREAMSIDECAR_STDIO_PATH` 解析，`createDefaultStreamingSidecarNativeBinding()` 现在只在成功加载 `streamsidecar-api` addon 时返回 binding；同步在 `crates/streamsidecar-app/src/main.rs` 中移除 `--stdio` 入口，在 `crates/streamsidecar-app/src/stdio_mode.rs` 中删除整套 stdio 宿主实现，并清理 `crates/streamsidecar-app/Cargo.toml` 的遗留 `serde_json` 依赖。已确认 `cargo check -p streamsidecar-app -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-112 | 审查 Rust 侧代码 | Done | 2026-03-04 | 2026-03-04 | 审查当前 Rust workspace（`ohmygamepad-*`、`streamsidecar-*`）的编译状态、宿主桥接与运行时恢复逻辑，确认 `cargo check` 通过；同时识别出三类需要后续修复的风险：`stdio` sidecar 在主线程内同步等待 host response，会与同线程的 response 分发形成自锁；`StreamSidecarRuntime::request_reconnect()` / `start()` 在失败路径下不会回滚状态，可能把 runtime 卡死在 `Reconnecting/Starting`；`webrtc-rs` backend 在真正收到远端视频前就提前写入 `latest_video_frame` 与 `MediaVideoReady` 相关状态，会污染首帧指标与卡顿恢复判断。 |
| P-111 | 收敛 rust-owned 恢复阶段的 keepAlive/closeSession 竞态 | Done | 2026-03-04 | 2026-03-04 | 在 `webrtc-rs` 协商、ICE、data channel 与远端媒体都已打通后，联调中暴露出恢复阶段的会话生命周期竞态：renderer 定时 keepAlive 与 sidecar 内部 `request_reconnect()` 会在 `ServerSdpExchangeCommandSent` 窗口同时命中 `POST /keepalive`，后端返回 `SessionUnexpectedState`，随后本地又继续对已被远端回收或已清理的 session 执行 `closeSession`，最终把原本已经连通的会话自己打断。现已在 `src/main/mods/auth/infrastructure/clients/http-client.ts` 中为认证 HTTP 客户端补齐结构化错误字段 `status/body/url`，并在 `src/main/mods/streaming/application/stream-session-service.ts` 中新增恢复期错误解析：对 `404` 和 `400 SessionUnexpectedState / ServerSdpExchangeCommandSent` 的 keepAlive 统一降级为可恢复状态并记录 warning，不再向上抛硬错误；同时将 `closeSession` 改成幂等关闭，本地 session 缺失或远端返回 `404` 时直接记录 `skip/ignore` 日志并安全返回，避免 sidecar 在延迟 host request 中再次命中 `Session not found`。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关文件 eslint 通过。 |
| P-110 | 将 webrtc-rs 收到的 H264 sample 直接交给 GStreamer 解码出窗 | Done | 2026-03-03 | 2026-03-03 | 在确认 `webrtc-rs` 主协商、ICE、data channel 与远端 H264 RTP 都已打通后，按照“先把流交给 GStreamer 出画”的目标调整 Rust 媒体主线：在 `crates/streamsidecar/src/webrtc_rs_backend.rs` 中新增 `gstreamer-app` 输出管线状态，将 `SampleBuilder` 组好的 H264 access unit 同时送入一条 `appsrc -> h264parse -> avdec_h264 -> videoconvert -> queue(leaky=downstream) -> osxvideosink/autovideosink` 管线；首次收到 sample 时自动初始化 GStreamer 输出窗口，并持续以 sample duration 驱动 PTS。这样 `rust-owned` 在保持 `webrtc-rs` 协商的同时，视频解码/显示改由 GStreamer 承担，优先目标从“自绘软渲染”切回“尽快稳定出画”。另外主进程 sidecar binding 已恢复为优先 N-API、stdio 仅作 fallback，避免再次优先命中空白 popup sidecar。已确认 `cargo check -p streamsidecar -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-app` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-109 | 让 rust-owned 默认走可弹窗的 stdio sidecar 宿主 | Done | 2026-03-03 | 2026-03-03 | 为了直接看到 Rust 侧原生画面，不再让 `rust-owned` 只停留在主进程内的 N-API runtime，而是恢复为“主进程托管、Rust 自己弹窗渲染”的 sidecar 路径：在 `crates/streamsidecar-app/src/stdio_mode.rs` 中将 `--stdio` 模式改造成带原生窗口的事件循环宿主，复用 `StreamSidecarApp + SoftwareFrameRenderer`，在处理 Electron 主进程发来的 control/host response 的同时，持续提取最新软解帧并刷到 Rust popup 窗口；在 `src/main/mods/streaming/application/streaming-sidecar-native-binding.ts` 中新增 stdio binding，优先拉起 `target/{debug,release}/streamsidecar-app --stdio`，找不到可执行文件时再回退到原有 N-API binding。这样 `rust-owned` 模式默认就会直接弹出 Rust 原生画面，而不需要额外切换调试宿主。已确认 `cargo check -p streamsidecar-app` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-108 | 为 Rust 媒体链补齐 H264 软解与软件窗口渲染最小链路 | Done | 2026-03-03 | 2026-03-03 | 在 `webrtc-rs` 主协商、ICE、data channel 与远端 RTP 都已打通后，开始把“有媒体但只有日志”推进到真正可渲染状态。现已在 `crates/streamsidecar/src/backend.rs` 中新增统一的已解码视频帧接口 `take_latest_video_frame()` 与 `StreamSidecarDecodedVideoFrame`；在 `crates/streamsidecar/src/webrtc_rs_backend.rs` 中接入 `rtp::codecs::h264::H264Packet + webrtc-media::SampleBuilder + openh264::decoder::Decoder`，将远端 H264 RTP 组帧、软解为 RGBA，再转成 `softbuffer` 可直接刷新的 `0x00RRGGBB` 像素缓冲，并把解码后的宽高/帧序号继续同步回 runtime stats；在 `crates/streamsidecar-app/src/software_renderer.rs` 与 `src/main.rs` 中新增 `softbuffer` 软件渲染器，用原生窗口按当前窗口大小做最小 CPU 缩放拷贝并展示最新软解帧。这样 sidecar 已具备“协商成功 -> 收到 H264 RTP -> Rust 软解 -> 软件窗口渲染”的主链路骨架。已确认 `cargo check -p streamsidecar-app -p streamsidecar -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `cargo build -p streamsidecar-app` 通过。 |
| P-107 | 为 webrtc-rs 后端补远端 track/RTP 观测与首帧统计 | Done | 2026-03-03 | 2026-03-03 | 在 `webrtc-rs` 主协商、answer、ICE 与 data channel 全部打通后，下一阶段需要明确区分“连接已建立但没有远端媒体”和“远端媒体已经到达但尚未渲染”。现已在 `crates/streamsidecar/src/webrtc_rs_backend.rs` 中为 peer connection 增加 `on_track` 处理：打印远端 track 的 `kind/mid/stream_id/track_id/payload_type/mime`，并对每条 track 启动 `read_rtp()` 循环；视频轨在 marker 包上累计 `frame_seq`、更新 `latest_video_frame`，并输出首个视频帧与后续周期性帧日志，音频轨则输出首个音频包日志。这样 runtime 的 `snapshot_runtime_stats()` 与 `StatsVideoFrameProcessed` 事件会开始基于真实远端 RTP 到达更新，不再只能依赖占位 negotiation 时间戳。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `cargo check -p streamsidecar-app -p streamsidecar` 通过。 |
| P-106 | 对齐 webrtc-rs 远端 ICE candidate 的浏览器兼容规则 | Done | 2026-03-03 | 2026-03-03 | 在 `webrtc-rs` 主协商路径下，`ExchangeOffer` 与 remote answer 已正常通过，但联调时在首个远端 candidate 应用阶段报出 `addRemoteIceCandidateFailed:ErrAttributeTooShortIceCandidate`。对照浏览器模式的 `src/renderer/src/player/infra/webrtc/WebRtcTransport.ts` 可确认原实现本来就会跳过 `a=end-of-candidates` 与一类携带无效 `tcptype` 的 UDP candidate，因此 Rust 侧需要遵循同样的容错规则，而不能把 signaling 服务返回的 candidate 原样交给 `webrtc-rs`。现已在 `crates/streamsidecar/src/webrtc_rs_backend.rs` 中新增 `normalize_remote_ice_candidate()`，在应用前统一执行：忽略空 candidate、忽略 `a=end-of-candidates/end-of-candidates`、忽略浏览器侧同样跳过的 `UDP + tcptype` 组合，并在必要时去掉 `a=` 前缀后再调用 `add_ice_candidate`；同时补充了 skipped/applied 诊断日志，便于继续收 track/datachannel 阶段的问题。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `cargo check -p streamsidecar-app -p streamsidecar` 通过。 |
| P-105 | 修复 N-API sidecar host response 回流自锁超时 | Done | 2026-03-03 | 2026-03-03 | 在 `webrtc-rs` 主协商路线下，联调日志已经确认 Rust 本地 offer 与 main 侧 `ExchangeOffer` 响应都已正常返回，但仍会在 main 终端看到 `streamSidecarHostRequestTimedOut:ExchangeOffer:*`。根因在 `crates/streamsidecar-api/src/lib.rs`：Rust runtime 线程在 `NativeHostBridge::request()` 中同步等待 host response，而 JS 回送的 `HostResponse/HostError` 又被设计成重新进入同一个 worker 主循环后才分发到 `pending_host_responses`，形成“等待中的线程阻塞了自己处理回包”的自锁。现已将 `HostResponse/HostError` 改为在 `send_incoming_message()` 入口直接旁路写入 `pending_host_responses`，不再经过 runtime worker 主循环；主循环中原来的 `HostResponse/HostError` 分支也已退为空分支。这样 main 侧一旦完成 `OfferExchanged/IceExchanged` 回送，Rust 同步请求就能立即收到响应，不再被 10 秒超时器误判。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi`、`cargo check -p streamsidecar-app -p streamsidecar` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-104 | 冻结 webrtcbin 主协商路线并切换到 webrtc-rs backend | Done | 2026-03-03 | 2026-03-03 | 根据浏览器模式对照日志已经确认基线协商形状稳定为“两条 transceiver + 四条 data channel + 同时具备 `audio/video/application` 三类 m-line”，而 `webrtcbin` 路线在多轮调整 transceiver、caps、时序后仍持续产出只有 `m=application` 的本地 offer，因此正式冻结其作为主协商栈的角色，不再继续围绕 `gstreamer_backend.rs` 的本地 offer 生成做主线开发。现已在 `crates/streamsidecar/src/webrtc_rs_backend.rs` 新增 `WebRtcRsNegotiationBackend`，改用 `webrtc-rs` 负责浏览器等价的 peer connection、audio/video transceiver、`input/control/chat/message` 四条 data channel、ICE 收集、local offer 校验与 remote answer/ICE 应用；同时将 `crates/streamsidecar-api` 与 `crates/streamsidecar-app` 的实际 backend 实例切换到该实现，并为浏览器对齐保留统一的 network profile / data channel 握手辅助逻辑。当前目标先把 Rust 主协商路径收敛到更接近浏览器语义的 `webrtc-rs`，渲染与更完整媒体链后续再继续接回 Rust 实时层。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo check -p streamsidecar-app -p streamsidecar`、`cargo build -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-103 | 将 rust-owned 的 transceiver/data channel 初始化时序后移到 pipeline 运行后 | Done | 2026-03-03 | 2026-03-03 | 在浏览器模式对照日志明确 `createOffer` 前已经稳定具备两条 transceiver 与四条 data channel 后，继续收窄 Rust 侧协商差异。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中将 `configure_webrtc_offer_primitives()` 从 pipeline 启动前后移到 `pipeline.set_state(Playing)` 成功之后，再继续创建本地 data channel；并新增 `on-new-transceiver` 诊断日志，输出 `kind/direction/current_direction/mline/mid`，用于确认 `webrtcbin` 是否在运行态下真正接纳了音视频 transceiver。这样可以排除“元素尚未进入运行态导致 transceiver 空协商”的变量。已确认 `cargo check -p streamsidecar-api --features napi` 与 `cargo build -p streamsidecar-api --features napi` 通过。 |
| P-102 | 为 webrtc-direct 补齐与 rust-owned 对照用的网络层日志 | Done | 2026-03-03 | 2026-03-03 | 为了停止继续靠人工反复试错，在 `src/renderer/src/player/infra/webrtc/WebRtcTransport.ts` 与 `src/renderer/src/player/app/session/SessionService.ts` 中补齐浏览器模式下与 `rust-owned` 一一对应的网络层日志：包括 `bind` 后的 transceiver 摘要、每条本地 data channel 的创建、local/remote ICE candidate 应用、`createOffer` 请求参数，以及 `SessionService` 中“原始 offer / SDP 改写后 offer / setLocalDescription 后描述”的统一 SDP 摘要（`audio/video/application` 三类 m-line、长度与预览）。这样下一轮可以直接对比 browser 与 Rust 的本地 offer 结构，而不是继续从远端响应倒推。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关文件 eslint 通过。 |
| P-101 | 为 rust-owned transceiver 显式补 RTP caps 并保留 codec-preferences | Done | 2026-03-03 | 2026-03-03 | 在继续对齐 `rust-owned` 与浏览器 `webrtc-direct` 的协商行为时，结合 `libgstwebrtc` 中 `no caps available for transceiver` / `Trying to add data channel ... doesn't have caps` 的内部提示，确认此前将 `add-transceiver` 第二个参数改成 `None::<gst::Caps>` 仍不足以让 `webrtcbin` 生成音视频 m-line。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中改为显式传入基础 RTP caps：`application/x-rtp, media=audio|video, payload=[0,127]`，同时继续通过 `codec-preferences` 分别收窄到 `OPUS` 与 `H264`，并补充 `current_direction/mline/mid` 诊断日志，确保本地 offer 生成前 `webrtcbin` 已拥有可协商的音视频 transceiver 形状。已确认 `cargo check -p streamsidecar-api --features napi` 与 `cargo build -p streamsidecar-api --features napi` 通过。 |
| P-100 | 将 rust-owned 的音视频 transceiver 改为更接近浏览器的无 caps 协商语义 | Done | 2026-03-03 | 2026-03-03 | 根据 `invalidLocalOfferSdp:audio=false:video=false:application=true` 的本地校验结果，确认当前 Rust offer 之所以只包含 `m=application`，根因在 `webrtcbin add-transceiver` 这一层仍未按浏览器 `offerToReceiveAudio/Video` 的语义工作。此前传入的半成品 `application/x-rtp` caps 很可能让 `webrtcbin` 直接忽略了音视频接收 m-line。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中将 transceiver 创建改为更贴近浏览器的方式：音频使用 `Sendrecv`、视频使用 `Recvonly`，且第二个参数改为 `None::<gst::Caps>`，同时记录返回 transceiver 的方向日志，便于确认 `webrtcbin` 是否真实接受了这两个接收轨道。已确认 `cargo check -p streamsidecar-api --features napi` 与 `cargo build -p streamsidecar-api --features napi` 通过。 |
| P-099 | 收敛浏览器与 Rust 侧的单一网络 profile，并补最小 data channel 握手 | Done | 2026-03-03 | 2026-03-03 | 为了严格对齐 `rust-owned` 与原浏览器 `webrtc-direct` 的网络层合同，这次把浏览器侧固定协议常量先抽到 `src/renderer/src/player/protocol/networkProfile.ts`，统一收口四条 data channel 定义、`message` 握手 ID、`control` accessKey、默认分辨率与 `input metadata` 的 maxTouchpoints；并让 `SessionService`、`ControlChannel`、`MessageChannel`、`InputService` 全部改为消费这套 profile，避免后续 TS 自身继续漂移。与此同时，在 `crates/streamsidecar/src/network_profile.rs` 中建立 Rust 镜像 profile，并在 `crates/streamsidecar/src/gstreamer_backend.rs` 中补齐最小 data channel 状态机：`message` open 后发送 Handshake，收到 `HandshakeAck` 后发送 system UI / client app install id / orientation / touch / dimensions 等同款配置消息，并串起 `control` 授权、gamepad remove/add、首次 keyframe request 与 `input` 的 `ClientMetadata` 首包。这样 Rust 侧的信令/datachannel 网络层不再只是“建出同名 channel”，而是按浏览器固定合同发出最小必需握手。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi`、`pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关 TS 文件 eslint 通过。 |
| P-098 | 为远端会话轮询补可观测日志并收口卡死超时 | Done | 2026-03-03 | 2026-03-03 | 根据实际联调中“只有 `CreateSession` 与首次 `Provisioning`，后续没有任何日志”的现象，确认当前问题可能在远端会话阶段就悬挂，导致本地 runtime 根本不会启动。现已在 `src/main/mods/streaming/application/stream-session-service.ts` 中为每次状态轮询补 `SessionPoll` 摘要日志，记录 attempt、重复状态次数、总耗时与当前状态耗时；同时为长期停留在 `Provisioning/ReadyToConnect` 的会话加入 45 秒本地超时，超时后会显式转为 `failed` 并打印 `SessionTimeout`，不再无限挂起。并在 `src/renderer/src/streaming/application/useStreamController.ts` 中补上 `RuntimeStart requested` 日志，明确区分“远端会话未就绪”和“已开始进入本地 runtime”两类问题。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关文件 eslint 通过。 |
| P-097 | 对照 `rfc/render.md` 复盘 Rust 渲染侧现状与调整方向 | Done | 2026-03-03 | 2026-03-03 | 基于 `rfc/render.md` 与当前 `crates/streamsidecar` / `src/renderer/src/streaming/runtime/rust-owned` 实现完成一次对照复盘：现状已经做到“输入 fixed-rate 下沉到 Rust、GStreamer `webrtcbin` 接收链跑通、双 queue 丢旧帧、tick 驱动冻结检测与关键帧/重连恢复”的第一阶段能力，但仍未达到 RFC 里“真正可控的 Rust 实时渲染引擎”目标。当前最主要缺口有三类：一是渲染仍依赖 `autovideosink/osxvideosink` 与 GStreamer 内部调度，`attachViewport` / `surface_id` 还只是逻辑占位，尚未形成可嵌入 Electron 视口的 native surface，也没有真正的 `LatestFrameSlot=1` 呈现线程；二是冻结恢复阈值与动作仍偏粗，当前 1.5s 请求关键帧、4s 重协商，且没有 decoder flush，不符合 RFC 期望的 150–250ms 检测与分层自愈；三是可观测性仍明显不足，`snapshotStats()` 只稳定输出 resolution/fps，`rtt/pl/fl/jit/br/decode` 仍为空，`first_frame_packet_arrival_time_ms` / `frame_decoded_time_ms` 也主要是占位值，尚未形成 input→present、decode、present jitter、freeze 原因统计闭环。后续建议优先把目标拆成两段：先把 GStreamer 路线做成“低延迟可控 sidecar 接收链”（补齐 appsink/native surface、真实指标、分层恢复），再决定是否继续往完全自持的 Rust 三线程渲染引擎推进。 |
| P-096 | 修正 rust-owned 创建 data channel 的时序到 webrtcbin 已运行后 | Done | 2026-03-03 | 2026-03-03 | 在继续对齐 `rust-owned` 与浏览器侧建链行为时，确认 `createWebRtcDataChannelFailed:input` 更像是 `webrtcbin` 时序差异而非单纯参数问题：之前在 `pipeline.set_state(Playing)` 之前就尝试创建 `input/control/chat/message` data channel，而 `webrtcbin` 在未进入运行态时可能直接返回空 channel。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中将初始 data channel 创建移动到 `pipeline.set_state(Playing)` 成功之后，并在失败信息中带上 pipeline `current/pending state`，让后续问题能够直接区分是 webrtcbin 还未 ready，还是 signal 参数仍不兼容。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-095 | 修正 rust-owned data channel 参数并为本地 offer 增加结构校验 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/gstreamer_backend.rs` 中进一步对齐浏览器侧的初始协商行为，修正 `webrtcbin create-data-channel` 的参数形状：第二个参数改为直接传 `GstStructure` 而不是错误的 `Option<Structure>`，并使用更贴近社区示例的 `config` dictionary 名称，避免 `createWebRtcDataChannelFailed:*` 的假失败；同时在 `create-offer` 后新增本地 SDP 结构校验，要求至少包含 `m=audio`、`m=video`、`m=application` 三类 m-line，否则直接在本地抛出 `invalidLocalOfferSdp:*` 并附短预览，不再继续把无效 offer 发到远端让用户反复试错。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-094 | 修复 rust-owned 创建 data channel 时的可空返回 panic | Done | 2026-03-03 | 2026-03-03 | 在为 `rust-owned` 补 `input/control/chat/message` 初始 data channel 后，`webrtcbin` 的 `create-data-channel` signal 实际返回可空 `WebRTCDataChannel`，而 Rust 侧错误地按非空对象直接 `emit_by_name::<WebRTCDataChannel>` 取值，导致运行时在 `glib::closure` 层 panic。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中改为 `emit_by_name::<Option<WebRTCDataChannel>>` 并对 `None` 显式抛出 `createWebRtcDataChannelFailed:*` 错误，避免 sidecar 线程被 signal 返回值解包直接打崩。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-093 | 对齐 rust-owned 初始 offer 形状并补 answer 缺失诊断 | Done | 2026-03-03 | 2026-03-03 | 根据 `ExchangeOffer` 日志确认 Rust sidecar 已把本地 SDP 发到 main，但远端返回的 `exchangeResponse` 中没有 `sdp`，说明当前 offer 形状不被 Xbox 远端接受。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中将 WebRTC 初始协商补齐为更接近浏览器实现的形状：音频 transceiver 改为 `sendrecv`、视频保持 `recvonly`，并在 `create-offer` 前通过 `webrtcbin` 预建 `input/control/chat/message` 四条 data channel，同时为 channel open/error/message 增加 stderr 日志；另外在 `src/main/mods/streaming/infrastructure/streaming-signaling-api.ts` 中补充 `answer payload missing sdp` 的原始 `exchangeResponse` 摘要日志，便于下一轮直接看远端拒绝原因。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi`、`pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关 main 文件 eslint 通过。 |
| P-092 | 为 rust-owned 信令往返补 host request 日志与超时 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar-api/src/lib.rs` 为 Rust native host bridge 增加 `ExchangeOffer/ExchangeIce/KeepAlive/CloseRemoteSession` 请求发送、响应接收、错误返回日志，并加入 10 秒 host request 超时，避免 sidecar 在等待 main 响应时无声挂住；同时在 `src/main/mods/streaming/application/streaming-sidecar-service.ts` 中补充 `HostRequest` 进入、完成、失败日志，以及 `ExchangeOffer/ExchangeIce` 的请求/响应长度与 candidate 数量日志。这样下一次 `rust-owned` 若卡在“本地 offer 已生成但没有 remote description/pad-added”，可以直接区分是 main 根本没收到请求、请求发出但远端没回，还是响应已回到 Rust 但后续媒体链未继续。已确认 `cargo build -p streamsidecar-api --features napi`、`pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关 main 文件 eslint 通过。 |
| P-091 | 为 rust-owned webrtc 补关键路径 stderr 日志 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/gstreamer_backend.rs` 中为真实 WebRTC 路径补齐关键 stderr 日志，覆盖 pipeline 构建、local offer 生成、local ICE 收集、remote answer/ICE 应用、`pad-added` 与实际 audio/video receive chain 挂接。这样在“窗口已打开但无画面、且没有显式报错”的阶段，可以直接从主进程终端看出是没有收到远端 answer、没有 ICE、还是收到 pad 但没有挂上正确的 depay/decode 链。已确认 `cargo build -p streamsidecar-api --features napi` 与 `cargo check -p streamsidecar-api --features napi` 通过。 |
| P-090 | 为 rust-owned 渲染侧补固定 tick 输入、最新帧优先与冻结自愈 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/input.rs` 将 sidecar 手柄采样切到固定 tick/固定流推送频率（500Hz backend poll / 250Hz logical sample / 120Hz fixed-rate stream push），避免 UI 抖动影响输入上行；在 `crates/streamsidecar/src/gstreamer_backend.rs` 为 WebRTC 视频接收链加入双 queue 限长与 `downstream` 丢旧帧策略，并通过 `identity` handoff 持续记录真实帧活动/FPS，同时支持向上游发送 `UpstreamForceKeyUnitEvent` 请求关键帧，确保显示尽量“永远最新”；在 `crates/streamsidecar/src/runtime.rs` 与 `crates/streamsidecar-api/src/lib.rs` 新增 sidecar worker 周期 tick，基于真实 transport/frame 活动做冻结检测，先请求关键帧，超阈值后自动触发 `mediaStalled` 快速重协商；`src/renderer/src/streaming/runtime/rust-owned/RustOwnedRuntime.ts` 同步把 `stats.videoFrameProcessed` 接到 frame tracking，恢复页面层最后一帧观测。已确认 `cargo fmt --all`、`cargo test -p streamsidecar --lib`、`cargo check -p streamsidecar -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-089 | 修复 TypedEventEmitter 对事件映射的过严泛型约束 | Done | 2026-03-03 | 2026-03-03 | 在 `src/renderer/src/player/api/events.ts` 中将 `TypedEventEmitter` 的泛型边界从 `Record<string, unknown>` 收窄为普通对象类型，避免 `PlayerEvents`、`StreamRuntimeEventMap`、`WebRtcTransportEvents` 这类显式事件表因为缺少字符串索引签名而被 `vue-tsc` 误判不满足约束。已确认 `pnpm exec eslint src/renderer/src/player/api/events.ts --ext .ts` 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过；`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 中这组 `TypedEventEmitter` 报错已全部消失，当前仅剩 `StatsService` 和 `InputPacketEncoder` 的 4 个既有类型问题。 |
| P-088 | 收紧 rust-owned webrtc 启动诊断并在 macOS 优先使用显式视频 sink | Done | 2026-03-03 | 2026-03-03 | 根据 `startWebRtcPipelineFailed:StateChangeError:gst-state-changed:/GstFakeSink:audio_sink` 日志确认上一版 bus 诊断仍会误抓普通状态变化，无法定位真实 GStreamer 根因。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中将 bus 诊断收紧为仅采集 `Error/Warning`，在无错误消息时回退输出 pipeline `current/pending state`；同时将 WebRTC 路径的视频 sink 创建改为 macOS 优先尝试 `osxvideosink`，失败再回退 `autovideosink`，减少 `autovideosink` 自动挑选不稳定 GL sink 的干扰。已确认 `cargo check -p streamsidecar-api --features napi` 与 `cargo build -p streamsidecar-api --features napi` 通过。 |
| P-087 | 清理失效手柄配置并将多手柄选择权完全收口到 Rust | Done | 2026-03-03 | 2026-03-03 | 继续收掉 main/shared/renderer 中已失效的手柄配置字段：删除 `enable_audio_rumble`、`audio_rumble_threshold`、`vibration_mode`、`gamepad_kernal`、`gamepad_mix`、`gamepad_index`、`dead_zone`、`edge_compensation`、`virtual_gamepad_opacity`、`gamepad_maping` 的 schema、类型、设置分组与中英文文案；同步将 `InputRuntimeConfig` / `AudioRuntimeConfig` 收窄为仅保留仍在使用的 `pollingRate`、`vibrationEnabled`、`volume`、`enableAudioControl`，并让 `GamepadDriver` 不再根据 renderer 配置做 pad merge / index 选择，统一直接消费 Rust runtime 给出的逻辑 pad 快照，多手柄采样策略完全交由 OhMyGamepad 控制。已确认相关文件 prettier、eslint 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过；`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 仍只剩仓库既有的 `TypedEventEmitter` 泛型与 `InputPacketEncoder` 历史类型错误。 |
| P-086 | 为 rust-owned webrtc 启动失败补总线诊断并先关闭音频输出兜底 | Done | 2026-03-03 | 2026-03-03 | 真实 `webrtcbin` 路径切通后，Rust sidecar 在 `set_state(Playing)` 阶段仍只返回裸 `StateChangeError`，无法判断具体是 `webrtcbin`、sink 还是系统设备问题。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中为 `startWebRtcPipelineFailed` 补充 GStreamer bus 错误/警告/状态消息摘要，同时把 WebRTC 路径默认音频 sink 从直接启用 `autoaudiosink` 调整为默认 `fakesink`，只在显式设置 `XBXRC_STREAMSIDECAR_ENABLE_AUDIO_OUTPUT=1` 时才打开本地音频设备，避免音频输出初始化失败把整条视频链拖死。已确认 `cargo check -p streamsidecar-api --features napi` 与 `cargo build -p streamsidecar-api --features napi` 通过。 |
| P-085 | 修复 rust-owned webrtc overlay 构造时的 GStreamer 枚举属性 panic | Done | 2026-03-03 | 2026-03-03 | `rust-owned` 切到真实 `webrtcbin` 后，`textoverlay` 在创建阶段因为 `valignment/halignment` 使用了 `.property(..., \"top\"/\"left\")` 而直接 panic；GStreamer 这里要求的是枚举属性，不接受 Rust 侧原始字符串。现已在 `crates/streamsidecar/src/gstreamer_backend.rs` 中改为 `.property_from_str(\"valignment\", \"top\")` 与 `.property_from_str(\"halignment\", \"left\")`，让 GObject 按枚举 nick 解析，避免运行时崩溃。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-084 | 让 rust-owned 默认进入真实 webrtcbin，demo pipeline 退为显式调试开关 | Done | 2026-03-03 | 2026-03-03 | 根据实际联调结果确认 `rust-owned` 已成功创建远端 xHome 会话并启动 N-API sidecar，但窗口仍显示 `Rust sidecar demo` 彩条。根因在 `crates/streamsidecar/src/gstreamer_backend.rs`：代码错误地把“是否存在 TURN 配置”当成“是否启用真实 WebRTC”的条件，导致 xHome 在无 fallback TURN 时静默退回 demo pipeline。现已改为默认始终走 `webrtcbin`，只有显式设置环境变量 `XBXRC_STREAMSIDECAR_FORCE_DEMO=1` 时才启用本地 demo 彩条链路，这样普通 `rust-owned` 会话要么进入真实远端媒体链路，要么直接暴露真实错误，不再被 demo fallback 掩盖。已确认 `cargo check -p streamsidecar-api --features napi`、`cargo build -p streamsidecar-api --features napi` 与 `pnpm exec tsc -p tsconfig.node.json --noEmit` 通过。 |
| P-083 | 补齐 main RPC 运行时校验中的 streamingSidecar namespace | Done | 2026-03-03 | 2026-03-03 | 定位到 `RPC namespace not found: streamingSidecar` 的真实根因不在 handler 或进程缓存，而在 `src/main/rpc/validation.ts` 的运行时 `rpcParamSchemas`：类型层与 handler 层已经包含 `streamingSidecar`，但 zod 校验表遗漏了该 namespace，导致请求在进入 main handler 前就被 `parseRpcInvokePayload()` 拒绝。现已补齐 `streamingSidecar` 全量方法的参数 schema，包括 `startRuntime`、`requestReconnect`、`attachViewport`、`applyDisplayState`、键鼠输入与音量/麦克风控制，并重建 `out/main` 产物。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit`、`pnpm exec eslint src/main/rpc/validation.ts --ext .ts --quiet` 与 `pnpm exec electron-vite build --outDir out` 通过。 |
| P-082 | 将 rust-owned 主进程宿主从 stdio sidecar 切到 N-API native binding | Done | 2026-03-03 | 2026-03-03 | 在 Cargo workspace 中新增 `crates/streamsidecar-api`，复用现有 `streamsidecar-protocol` 的 control/host/event 消息协议，通过 `napi` 导出 `StreamSidecarNativeBinding`，在主进程内启动 Rust worker、GStreamer backend 与 OhMyGamepad 输入后端，并以队列方式暴露 `ready/controlResponse/runtimeEvent/hostRequest`；同时新增 `src/main/mods/streaming/application/streaming-sidecar-native-binding.ts` 动态加载 `libstreamsidecar_api`/`.node`，将 `src/main/mods/streaming/application/streaming-sidecar-service.ts` 切换为优先使用 native binding 托管 `rust-owned`，不再默认依赖 `streamsidecar-app --stdio`。文档同步说明当前 `rust-owned` 走 N-API 宿主。已确认 `cargo check -p streamsidecar-api --features napi`、`pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关 main 文件 eslint 通过。 |
| P-081 | 放宽 OhMyGamepad 对 gilrs 设备的 rumble 能力门槛 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs/src/oh_my_gamepad_service.rs` 中调整 rumble 设备筛选策略：原先会严格依赖 `device.capabilities.basic_rumble/advanced_haptics`，导致部分桌面 `gilrs` 设备即使能震动，也会因为 `is_ff_supported()` 报告过于保守而被提前判成 `unsupported`。现在改为“能力标志优先，但只要当前存在 rumble backend，就允许真实 `gilrs` 设备继续尝试下发”，并新增单测覆盖“capability 标志为 false 的 gilrs 设备仍会派发 rumble”。已确认 `cargo fmt --all`、`cargo test -p ohmygamepad-gilrs` 与 `cargo check -p ohmygamepad-gilrs -p ohmygamepad-api` 通过。 |
| P-080 | 修正 Rust gamepad 快照映射到 player 时的摇杆 Y 轴方向 | Done | 2026-03-03 | 2026-03-03 | 在 `src/renderer/src/player/infra/input/GamepadDriver.ts` 中修正 `LogicalPadStateDto -> InputFrame` 的摇杆 Y 轴映射：由于 `InputPacketEncoder` 在写入协议包时还会再对 `LeftThumbYAxis/RightThumbYAxis` 取反一次，Rust 逻辑态里的“向上为正”若直接透传会导致前后颠倒；因此在进入 legacy `InputFrame` 时先将左右摇杆 `y` 翻转，恢复与串流协议一致的前进/后退方向。已确认相关文件 prettier、eslint 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过。 |
| P-079 | 收窄 xHome preflight，仅在实际 WakeUp 后等待主机注册完成 | Done | 2026-03-03 | 2026-03-03 | 根据 `RemoteConsoleReadyCheck` 日志确认当前回归来自 renderer 侧 preflight 过于激进：目标主机匹配成功但长时间停在 `ConnectedStandby`，而新逻辑把所有 xHome 都要求先进入 `On` 才允许建会话，导致把待机主机直接拦死。现已在 `src/renderer/src/streaming/application/useStreamSession.ts` 中改为仅在 `powerOnRemoteConsole()` 实际返回 `accepted=true` 后才执行 `waitForRemoteConsoleReady()`；若本次未触发 WakeUp，则恢复为直接走原会话创建路径，同时补充 `wakeAccepted` 调试日志。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关文件 eslint 通过。 |
| P-078 | 为 xHome preflight 补主机匹配与状态快照诊断 | Done | 2026-03-03 | 2026-03-03 | 在 `src/renderer/src/streaming/application/stream-session-remote.ts` 中把 `waitForRemoteConsoleReady()` 从简单布尔返回扩展为结构化结果，逐轮输出 `[Stream][RemoteConsoleReadyCheck]` 日志，并记录目标主机是否匹配、最后一次命中的 `serverId/id/deviceId/powerState/remoteManagementEnabled/consoleStreamingEnabled`；`useStreamSession.ts` 在 preflight 超时时会把这些诊断信息带入错误字符串，`stream-session-model.ts` 与中英文 i18n 文案同步支持展示诊断 JSON。这样后续可以直接判断是目标 ID 未匹配，还是主机已匹配但一直未进入 `On + consoleStreamingEnabled` 状态。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit` 与相关 TS 文件 eslint 通过。 |
| P-077 | 删除 streaming/runtime 中已失效的 keyboardPointerInput 合同 | Done | 2026-03-03 | 2026-03-03 | 从 `src/renderer/src/streaming/runtime/contracts.ts` 中移除 `keyboardPointerInputOwner`、`keyboardPointerInput()` 以及相关 pointer/keyboard 输入事件类型，统一收口为只暴露 controller/gamepad 输入能力；同步清理 `WebRtcDirectRuntime`、`RustOwnedRuntime`、`createRustOwnedSidecarClient()`、`sidecar-client.ts` 与 `useStreamRuntime.ts` 里残留的 `setKeyboardInputEnabled` / `setKeyboardPointerEnabled` / `pushKeyboardPointerInput` 调用链，并让 `useStreamController.ts` 的 `setTextInputActive()` 退回纯 UI 占位，不再向 runtime/player 透传浏览器键盘开关。已确认相关文件 eslint 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过；`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 仍然只剩仓库既有的 `TypedEventEmitter` 泛型和 `InputPacketEncoder` 历史类型错误。 |
| P-076 | 为 xHome 唤醒链路补主机注册等待与会话状态调试日志 | Done | 2026-03-03 | 2026-03-03 | 针对 `WaitingForServerToRegister` 回归，在 `src/renderer/src/streaming/application/stream-session-remote.ts` 中新增 `waitForRemoteConsoleReady()`，并在 `useStreamSession.ts` 的 xHome `powerOnConsole` 路径里于 `WakeUp` 成功后轮询 `rpc.data.getRemoteConsoles()`，等待目标主机进入 `powerState=On && consoleStreamingEnabled!==false` 再继续建会话，避免“唤醒已接受但主机串流服务尚未注册完成”时立刻触发 `startStream`。同时在 `src/main/mods/streaming/application/stream-session-service.ts` 中为 `createSession / connectSession / updateSessionState` 补充结构化日志，方便下一次直接定位是否卡在 `Provisioning / ReadyToConnect / Failed`。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit` 与针对改动文件的 eslint 通过。 |
| P-075 | 移除 player 本地键盘输入链并清理失效输入配置 | Done | 2026-03-03 | 2026-03-03 | 从 `src/renderer/src/player` 中删除本地 `KeyboardDriver` 路径，`InputService` 不再采集或合并浏览器键盘状态，`GamepadDriver` 也移除了 `window keydown/keyup` 注入，确保 player 内部的手柄/键盘输入都只来自主进程/Rust `gamepad.runtimeSnapshot`；同时删除本地 `force_trigger_rumble` 覆盖逻辑，并把 `force_trigger_rumble`、`enable_native_mouse_keyboard`、`mouse_sensitive`、`input_mousekeyboard_maping` 从 main/shared/renderer 的 config schema、类型、设置定义、stream config 合同与 i18n 文案中一并移除。已确认相关文件 eslint 与 `pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过；`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 仍只剩仓库既有的 `TypedEventEmitter` 泛型与 `InputPacketEncoder` 历史类型错误。 |
| P-074 | 将 stream_runtime_mode 接入设置页，允许直接切换 rust-owned | Done | 2026-03-03 | 2026-03-03 | 在 `src/shared/config/domain-definition.ts` 中把 `stream_runtime_mode` 加入 streaming/session 配置分组并定义为单选项，选项为 `webrtc-direct` / `rust-owned`；同时补充 `src/renderer/src/i18n/locales/zh.json` 与 `src/renderer/src/i18n/locales/en.json` 的文案，使设置页可以直接展示并切换 runtime 模式，不再只能依赖底层 schema 或手改配置。这样配合前一步的 `resolveStreamRuntimeMode()` 与 sidecar 宿主链路，用户已可通过现有设置界面切到 `rust-owned` 后再启动真实串流会话。 |
| P-073 | 接通 rust-owned 运行时的主进程 sidecar 宿主与 renderer 选择链路 | Done | 2026-03-03 | 2026-03-03 | 为 `rust-owned` 打通了 `renderer -> main -> Rust sidecar` 的实际控制链：在 `src/shared/rpc/streamsidecar.ts` 与 `src/shared/events/streamsidecar.ts` 中新增 sidecar RPC/事件契约，在 `src/main/mods/streaming/application/streaming-sidecar-service.ts` 中实现主进程 sidecar 宿主，负责拉起 `streamsidecar-app --stdio`、转发 Rust host request 到现有 `StreamingService`，并把 runtime 事件广播回 renderer；`src/renderer/src/streaming/runtime/rust-owned/createRustOwnedSidecarClient.ts` 改为通过 `rpc.streamingSidecar` 与 `events.on('streaming.sidecarRuntimeEvent')` 驱动真正的 runtime client；同时补充 `stream_runtime_mode` 配置键，让 `resolveStreamRuntimeMode()` 可以切到 `rust-owned`。Rust 侧则在 `streamsidecar-protocol` 中补了可序列化 stdio 协议，并为 `streamsidecar-app` 增加 `--stdio` 模式。已确认 `pnpm exec tsc -p tsconfig.node.json --noEmit`、针对新文件的 eslint 以及 `cargo check --offline -p streamsidecar-protocol -p streamsidecar -p streamsidecar-app` 通过；`cargo test --offline -p streamsidecar -p streamsidecar-app` 中 `streamsidecar` 单测通过，但 `streamsidecar-app` 现有窗口测试仍会因 macOS/GStreamer 主线程告警停住，这轮未继续清理。 |
| P-072 | 收口 player/runtime 中已失效的鼠标触摸配置合同 | Done | 2026-03-03 | 2026-03-03 | 从 `src/renderer/src/player/domain/input.ts` 的 `InputRuntimeConfig` 中移除 `mouseSensitivity`、`mouseKeyboard`、`touch`，同时删除 `PlayerClientOptions` 里的 `mouseKeyboardConfig`，让 `PlayerClient` 内部固定使用默认 `KeyboardDriver` 映射；`webrtc-direct/createWebRtcDirectRuntime.ts` 不再把 `enable_native_mouse_keyboard`、`input_mousekeyboard_maping`、`mouse_sensitive` 注入 player runtime，`SessionService` / `MessageChannel` 里的 `touchInputEnabled` 也固定为 `false`。这次只收口 player/runtime 合同与示例文档，不改主配置 schema，也不删除输入编码层中保留的 pointer/mouse/keyboard packet 结构。已确认相关文件 eslint 通过；`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 仍被既有的 `TypedEventEmitter` 泛型与 `InputPacketEncoder` 历史类型错误阻塞。 |
| P-071 | 删除 player 中现存鼠标/触摸输入层接线 | Done | 2026-03-03 | 2026-03-03 | 删除 `src/renderer/src/player/app/input/PointerInputController.ts`，并同步清理 `InputService` 与 `PlaybackService` 中的 pointer/mouse/wheel/pointer-lock keyboard 事件接线：输入包现在只继续发送 metadata 与 gamepad 数据，不再从 video 元素采集鼠标、触摸和指针锁键盘事件。已确认 `rg` 已搜不到 `PointerInputController` 与对应 `onPointer*` / `onKeyboardPointerLocked*` 调用，且针对改动文件的 eslint 通过。`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 仍然被既有的 `TypedEventEmitter` 泛型与 `InputPacketEncoder` 历史类型错误阻塞，这轮未额外清理。 |
| P-070 | 恢复 streaming 输入通道的服务器 rumble 二进制解析 | Done | 2026-03-03 | 2026-03-03 | 参考旧版 `RumbleService` 的协议逻辑，在 `src/renderer/src/player/app/input/RumbleService.ts` 中恢复 `reportType=128` 的二进制 rumble 包解析：按 `reserved + gamepadIndex + 4 路马达幅值 + duration/delay/repeat` 读取 `ArrayBuffer`，将 `gamepadIndex` 映射到 `logical-pad` 目标，并把服务器 rumble 与本地 `force_trigger_rumble` 拆成两条 effect 通道后再合成，避免两者互相覆盖；其中服务器 rumble 继续沿用旧逻辑的 `duration/10` 与 `delay + rawDuration` 重复调度节奏，通过 `rpc.gamepad.playRumble/stopRumble` 发往主进程 native binding。已确认 `pnpm exec prettier --write src/renderer/src/player/app/input/RumbleService.ts` 与 `pnpm exec eslint src/renderer/src/player/app/input/RumbleService.ts --ext .ts` 通过。 |
| P-069 | 为 streamsidecar 接入真实 webrtcbin 协商与远端接收链骨架 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/gstreamer_backend.rs` 中为 `DirectWindowGstreamerBackend` 增加真实 `webrtcbin` 分支：当 `session.turn_server` 存在时，backend 会创建 `webrtcbin`、注册 `on-ice-candidate` 与 `pad-added` 回调、添加 recvonly transceiver，并通过 `create-offer / set-local-description / set-remote-description / add-ice-candidate` 驱动真实 SDP/ICE 路径；远端媒体 pad 会按 `H264/VP8/VP9/OPUS` 等 caps 动态挂接到 depay/decode 与现有 video/audio sink 链。无 `turn_server` 时继续保留本地 GStreamer demo pipeline 作为开发 fallback。已确认 `env PKG_CONFIG_PATH=/opt/homebrew/Cellar/gstreamer/1.28.1/lib/pkgconfig cargo check -p streamsidecar -p streamsidecar-app` 与带 GStreamer runtime 环境的 `cargo test -p streamsidecar -p streamsidecar-app` 通过，同时 `bash scripts/run-streamsidecar-gst-demo.sh` 仍能启动原生 demo 窗口。当前这一步完成的是“真实 WebRTC 分支可编译并接入 runtime 合同”，还未使用真实远端会话完成端到端媒体验证。 |
| P-068 | 打通 OhMyGamepad 基础 rumble 下发与 renderer 原生 rumble 调用 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs/src/source.rs` 中为 `RealGilrsSource` 接入 `gilrs` force feedback：启用 `with_force_feedback(true)`，新增 `GilrsRumbleHandle` 命令通道，在输入线程内按设备创建/停止基础 `EffectBuilder` rumble effect，并用 2 条单测覆盖强弱马达幅值归一化与 repeat/delay 调度。`crates/ohmygamepad-gilrs/src/oh_my_gamepad_service.rs` 同步引入 service 内 rumble backend 抽象，让 `play_rumble` / `stop_rumble` 对支持 rumble 的真实设备返回 `accepted=true` 并派发到 backend，同时新增 2 条 service 测试覆盖真实派发路径。renderer 侧在 `src/renderer/src/player/app/input/RumbleService.ts` 中恢复主进程原生 rumble 调用：将强制 trigger rumble 映射为 `rpc.gamepad.playRumble/stopRumble`，带节流去重与销毁时停止逻辑，继续保留服务器二进制 rumble 包入口待后续协议补齐。已确认 `cargo test -p ohmygamepad-gilrs -p ohmygamepad-api -p ohmygamepad-protocol`、`cargo check --workspace --all-targets`、`cargo build -p ohmygamepad-api --features napi`、`pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与相关 rumble 文件 eslint 通过。 |
| P-067 | 为 OhMyGamepad 补 rumble API 并扩展主进程 native 命令面 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-protocol` / `src/shared/gamepad/contract.ts` 中新增 rumble target/effect/result DTO，以及多手柄采样策略 DTO；`crates/ohmygamepad-gilrs/src/oh_my_gamepad_service.rs` 补充 `play_rumble` / `stop_rumble`，先实现 target 解析、设备能力筛选与结构化拒绝结果（`target-not-found` / `unsupported` / `not-implemented`），并新增 2 条 service 测试覆盖未知目标和可震动设备的未实现占位行为。与此同时扩展 `ohmygamepad-api` 的 feature-gated `napi` 导出与主进程 `gamepad-native-binding`/`GamepadRuntimeController`/RPC 契约，新增 `setSamplingStrategy`、`setPrimarySamplingDevice`、`pauseSamplingDevice`、`resumeSamplingDevice`、`playRumble`、`stopRumble` 命令。已确认 `cargo check --workspace --all-targets`、`cargo test -p ohmygamepad-protocol -p ohmygamepad-core -p ohmygamepad-gilrs -p ohmygamepad-api`、`cargo build -p ohmygamepad-api --features napi`、`pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 与相关 main 文件 eslint 通过，并额外用 Node `dlopen` 验证了新增 `playRumbleJson` / `setSamplingStrategyJson` 等 addon 方法存在且可返回结果。 |
| P-066 | 以 GStreamer demo backend 打通 Rust sidecar 的本地画面与输入链路 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/gstreamer_backend.rs` 中将 `DirectWindowGstreamerBackend` 从 placeholder 替换为真实 GStreamer demo backend：使用 `videotestsrc -> x264enc -> avdec_h264 -> autovideosink` 跑通本地“编码后再解码”的最小画面链路，并把键盘、鼠标、手柄输入映射到 overlay 文本、测试图案切换与 `videobalance` 显示参数上；同时新增 `scripts/run-streamsidecar-gst-demo.sh` 开发脚本，统一收口 `RUSTFLAGS`、`DYLD_LIBRARY_PATH`、`GST_PLUGIN_SYSTEM_PATH` 与 GStreamer 开发启动方式。期间修复了 Homebrew `gstreamer` bottle 的路径异常（通过重装恢复 `.pc` 与 dylib 中的真实 `/opt/homebrew/...` 引用），最终确认 `env PKG_CONFIG_PATH=/opt/homebrew/Cellar/gstreamer/1.28.1/lib/pkgconfig cargo check -p streamsidecar -p streamsidecar-app` 与带 GStreamer runtime 环境的 `cargo test -p streamsidecar -p streamsidecar-app` 均通过，且 `bash scripts/run-streamsidecar-gst-demo.sh` 可启动原生 sidecar 主循环。 |
| P-065 | 删除 player 中旧 React Native Native bridge 残留 | Done | 2026-03-03 | 2026-03-03 | 删除 `src/renderer/src/player/infra/bridge/NativeBridge.ts` 及其在 `PlayerClient`、`InputService`、`RumbleService` 中的接线，不再让 player 通过 `ReactNativeWebView.postMessage` 发送 `nativeVibration/deviceVibration`；同时移除 `StatsService` 中针对 `ReactNativeWebView` 的 decode 时间补偿逻辑，彻底清掉 player 目录里旧 React Native 宿主假设。`RumbleService` 现阶段退化为纯占位壳，等待后续 Rust rumble 能力接入。已确认相关 player 文件 eslint 通过，且 `rg` 已搜不到 `ReactNativeWebView` / `NativeBridge` 残留。 |
| P-064 | 清理 renderer 侧旧 Web Gamepad 路径并切断浏览器 rumble | Done | 2026-03-03 | 2026-03-03 | 将 `src/renderer/src/player/infra/input/GamepadDriver.ts` 收口为只消费主进程/Rust `gamepad.runtimeSnapshot` 的 native 输入壳，移除 `navigator.getGamepads()` 轮询、浏览器映射与 `Web` 模式分支；同步将 `InputRuntimeConfig.gamepadKernel` 固定为 `Native`，并把 `webrtc-direct` 的 `vibrationMode` 调整为 `Native`。同时在 `RumbleService` 中彻底切断对浏览器 Gamepad API 的依赖，仅保留发往 native bridge 的占位输出；`AudioEffectsService` 删除音频联动震动逻辑，退回为纯音量控制。已确认针对改动文件的 eslint 通过；`pnpm exec vue-tsc --noEmit -p tsconfig.web.json --composite false` 仍存在一批既有 `TypedEventEmitter` / streaming 相关错误，本次未额外清理。 |
| P-063 | 让 ohmygamepad-api 直接承载 napi 并接入主进程 native binding | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-api` 中加入 feature-gated `napi` 导出，保留默认纯 Rust facade 给 `streamsidecar` 使用，同时在 crate 内新增与 TS `shared/gamepad/contract.ts` 对齐的 JSON contract 适配层与 4 条序列化/反序列化测试；主进程 `src/main/mods/gamepad/application/gamepad-native-binding.ts` 改为直接动态加载 `ohmygamepad-api` 生成的本地库，通过轮询 `runtimeSnapshot` 驱动现有 `GamepadRuntimeController`，并支持 `route target` / `sampling` / `logical pad binding` 命令回写。为避免 macOS 无辅助功能权限时 `device_query` 直接 panic，这次也把桌面键盘 listener 收敛成静默降级。已确认 `cargo check --workspace --all-targets`、`cargo test --workspace`、`cargo build -p ohmygamepad-api --features napi`、`pnpm exec tsc --noEmit -p tsconfig.node.json --composite false` 通过，并额外用 Node 直接 `dlopen` 验证了 addon 可返回 runtime snapshot。 |
| P-062 | 为 streamsidecar-app 落地原生窗口主循环与键鼠输入回灌 | Done | 2026-03-03 | 2026-03-03 | 为 `crates/streamsidecar-app` 引入 `tao` / `raw-window-handle`，将 sidecar 入口改为真实原生窗口事件循环：窗口标题会跟随 runtime phase、transport state、surface/video 状态更新，同时把原生键盘、鼠标移动、点击和滚轮事件映射成 `StreamSidecarControlCommandDto::PushKeyboardPointerInput` 回灌到 runtime，打通“sidecar 窗口 + 原生输入”最小链路。当前机器缺少 `gstreamer-1.0` / `gstreamer-webrtc-1.0` 系统库，因此媒体后端仍保留 placeholder/direct-window 结构，待系统依赖就绪后直接替换 `DirectWindowGstreamerBackend`。已确认 `cargo test --offline -p streamsidecar-app -p streamsidecar` 与 `cargo check --offline --workspace --all-targets` 通过。 |
| P-061 | 删除 OhMyGamepad host/bridge 过渡层并收口统一门面 | Done | 2026-03-03 | 2026-03-03 | 从 Rust workspace 中删除 `crates/ohmygamepad-host` 与 `crates/ohmygamepad-bridge`，新增 `crates/ohmygamepad-api` 作为统一门面，让 `streamsidecar` 直接依赖这层 facade；同时移除主进程里的 host source 协议文件，将 `GamepadRuntimeController` 收口为只面向未来 napi 的 `GamepadNativeBinding` 接口，不再让 main 直接感知额外 Rust host 进程协议。 |
| P-060 | 将 streamsidecar-app 收口为独立窗口宿主结构 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/gstreamer_backend.rs` 中新增 `DirectWindowGstreamerBackend` 目标类型，明确后续独立窗口直出要替换的 media backend 位置；同时在 `crates/streamsidecar-app/src/window.rs` 中新增 `StreamSidecarWindowHost` 抽象，让 app 宿主从“runtime + host bridge”扩展为“runtime + host bridge + window host”三段，并把 `MediaSurfaceReady/MediaVideoReady` 事件同步到窗口状态，保持后续接 `tao` 时只需要替换窗口宿主实现。已确认 `cargo test --offline -p streamsidecar-app -p streamsidecar` 与 `cargo check --offline -p streamsidecar-app -p streamsidecar` 通过。 |
| P-059 | 为主进程接入 dev 阶段的 Rust gamepad host 数据源 | Done | 2026-03-03 | 2026-03-03 | 在 Cargo workspace 中新增 `crates/ohmygamepad-host` 二进制，基于 stdio JSON 协议启动 `OhMyGamepadService` 并持续输出 runtime snapshot；同时在 `src/main/mods/gamepad` 中新增 runtime source/controller，把主进程 `GamepadService` 收口为快照缓存与事件分发层，RPC 命令优先代理到 Rust host，失败时再退回内存态缓存。当前这版主要服务开发期主进程/Rust 联调，正式打包集成后续再补。 |
| P-058 | 打通 streamsidecar-app 宿主循环并修复 Rust workspace 编译回归 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar-app/src/lib.rs` 中新增 `StreamSidecarApp` 宿主循环，负责把 control command、runtime、host bridge 与事件回收串成同一条 Rust 进程内链路；同步让 `src/main.rs` 走同一套 app 宿主入口，并补 1 条宿主层测试。为保证当前 Rust worktree 能继续离线校验，还补了 `crates/ohmygamepad-host` 的最小 bridge 依赖与若干 DTO/数值类型修正；最终确认 `cargo test --offline -p streamsidecar-app -p streamsidecar` 与 `cargo check --offline --workspace --all-targets` 通过。 |
| P-057 | 打通 streamsidecar 的最小输入链路与首帧媒体事件链 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/input.rs` 中新增 `StreamSidecarInputBackend` 与 `OhMyGamepadStreamSidecarInputBackend`，把 `streamsidecar` 的最小输入链路接到真实 `OhMyGamepadService`：启动时会把 route target 绑定到 `StreamSession`，按钮注入走虚拟手柄状态提交；同时在 `crates/streamsidecar/src/backend.rs` 与 `crates/streamsidecar/src/runtime.rs` 中补齐输入状态同步、surface/video ready 以及 `StatsVideoFrameProcessed` 首帧事件链，让 host bridge 信令、占位解码/渲染元数据和真实输入状态可以在同一条 runtime 流程里闭合，并新增 5 条单元测试覆盖这批行为。 |
| P-056 | 先将 streaming 的 gamepad 输入源切到 Rust/主进程快照 | Done | 2026-03-03 | 2026-03-03 | 在 `src/renderer/src/streaming/runtime/webrtc-direct/createWebRtcDirectRuntime.ts` 中将 `PlayerClient` 的 `gamepadKernel` 从 `Web` 调整为 `Native`，使当前串流链路优先消费主进程/Rust 提供的 gamepad 快照，而浏览器 `navigator.getGamepads()` 仍暂时保留在非 streaming 场景与后备实现中，避免一次性全局切换过于激进。 |
| P-055 | 让 renderer 的 Native gamepad 模式改用主进程快照 | Done | 2026-03-03 | 2026-03-03 | 将 `src/renderer/src/player/infra/input/GamepadDriver.ts` 的 `Native` 模式从读取 `globalThis.gpState` 的占位实现，改为通过 renderer 侧 RPC + 事件桥订阅主进程 `gamepad.runtimeSnapshot`，并把 `LogicalPadSnapshotDto` 映射回现有 `InputFrame`；同时按逻辑 pad 连接状态向 control channel 汇报单控制器增减，使 Electron 层开始真正以主进程/Rust gamepad 快照为输入源，而浏览器 `navigator.getGamepads()` 仅保留给 `Web` 模式。 |
| P-054 | 将 streamsidecar 重构为可替换 media backend 的 Rust runtime | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/backend.rs` 中新增 `StreamSidecarMediaBackend` 抽象与默认 `PlaceholderStreamSidecarMediaBackend`，并重写 `crates/streamsidecar/src/runtime.rs` 让运行时围绕可替换后端组织：协商时由 backend 生成本地 offer/ICE 与 surface/video 元信息，host bridge 只负责远端请求转发，runtime 统一维护 snapshot、surface/video 状态与控制命令；同时新增 4 条单元测试覆盖启动协商、重连顺序、控制命令与 backend 可替换性，为后续接 GStreamer `webrtcbin` 留出稳定替换点。 |
| P-053 | 将 streamsidecar 骨架推进为可测试的 Rust 状态机 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/streamsidecar/src/runtime.rs` 中把原先的空壳 runtime 推进为可测试的高内聚状态机：补齐本地 snapshot、运行阶段事件、host bridge 请求顺序、重连 keepalive/重协商占位链路，以及控制命令对音量、显示、麦克风、键鼠输入和按钮注入的本地状态更新；同时新增 3 条单元测试覆盖启动协商、重连顺序与控制命令落地，确认 `cargo test -p streamsidecar` 与 `cargo check --workspace --all-targets` 通过。 |
| P-052 | 补齐 OhMyGamepad 键盘 fallback 闭环与映射命令协议 | Done | 2026-03-03 | 2026-03-03 | 将键盘 keymap DTO 下沉到 `crates/ohmygamepad-protocol`，为 service/bridge 新增 `ReplaceKeyboardMapping` 命令；同时让 `OhMyGamepadService` 默认托管 `device_query` 桌面键盘监听线程，在启用 keyboard fallback 时自动采样并注入逻辑 pad 状态，且在真实手柄存在时跳过键盘同步等待，修复之前键盘路径未纳入 service 生命周期、映射无法走统一协议以及被抑制时白等的问题，并补齐 service/bridge 测试。 |
| P-051 | 搭建 rust-owned sidecar workspace 骨架并收口 renderer 壳 | Done | 2026-03-03 | 2026-03-03 | 在 Cargo workspace 中新增 `crates/streamsidecar-protocol`、`crates/streamsidecar`、`crates/streamsidecar-app` 三个 crate，先落 sidecar 控制 DTO、runtime 骨架与可执行进程入口；同时将 renderer 侧 `RustOwnedRuntime` 收口为真正的 sidecar client 壳，新增 `createRustOwnedSidecarClient.ts` 与 `sidecar-client.ts`，让后续主进程宿主接入只需要替换客户端实现；顺手补上 `OhMyGamepadServiceCommandDto::ReplaceKeyboardMapping` 的遗漏分支，恢复 `cargo check --workspace --all-targets`、`pnpm exec vue-tsc --noEmit` 与 rust-owned 目录 eslint 全绿。 |
| P-050 | 收敛 rust-owned 模式的社区库选型与实现规划 | Done | 2026-03-03 | 2026-03-03 | 结合当前 `StreamRuntime` / host bridge 架构与现有 Cargo workspace，收敛 `rust-owned` 的首选技术路线：输入继续复用 `ohmygamepad-*`，sidecar 控制面采用本地 socket IPC，实时媒体链路优先基于 GStreamer Rust bindings 与 `webrtcbin` 落地，sidecar 自持连接状态机；同时将 `webrtc-rs + ffmpeg + wgpu` 明确为后续纯 Rust 深化路线，而不是第一阶段实现。 |
| P-049 | 为 Rust 库补齐默认键盘映射 API | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs/src/oh_my_gamepad_keyboard.rs` 中新增 `OhMyGamepadKeyboardKey`、`OhMyGamepadKeyboardControl`、`OhMyGamepadKeyboardMapping`、`OhMyGamepadKeyboardMapper` 与默认 keymap，实现按下/抬起/切换/清空后的逻辑 pad 状态计算；同时让 `oh_my_gamepad_keyboard_debug` example 直接复用这套库内 mapper，并补齐默认映射、释放、方向抵消与清空行为测试。 |
| P-048 | 为 Rust 输入内核补充键盘调试 example | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs/examples/oh_my_gamepad_keyboard_debug.rs` 中新增一个零依赖终端调试用例：通过 `NoopGilrsSource + keyboard fallback` 驱动 `OhMyGamepadService`，把键盘按键切换为逻辑 pad 状态并实时打印当前按钮、摇杆、扳机与 runtime mirror；同时兼容 macOS/Linux 终端 raw mode，确认 `cargo check --workspace --all-targets` 与 `cargo test --workspace` 均通过。 |
| P-046 | 清理 streaming 目录遗留 lint 警告 | Done | 2026-03-03 | 2026-03-03 | 复跑 `src/renderer/src/streaming` 的 eslint 后，清理了 `stream-session-model.ts`、`stream-session-remote.ts`、`runtime/contracts.ts`、`createStreamRuntime.ts`、`webrtc-direct` 相关文件与 `streaming/types.ts` 中遗留的 Prettier 格式警告，使 `pnpm exec eslint src/renderer/src/streaming --ext .ts,.vue` 恢复为零告警。 |
| P-047 | 将 Rust 公开 DTO 与 bridge/runtime 类型统一为 OhMyGamepad 命名 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-protocol`、`crates/ohmygamepad-core`、`crates/ohmygamepad-bridge`、`crates/ohmygamepad-gilrs` 中把剩余公开 `Gamepad*` 类型统一改为 `OhMyGamepad*`，包括 device、routing、sampling、bridge command/event、runtime snapshot 以及 core/gilrs 对这些 DTO 的消费；同时保留现有多手柄策略与逻辑 pad 命名不变，最终再次确认 `cargo check --workspace --all-targets` 与 `cargo test --workspace` 全绿。 |
| P-045 | 合并 streaming 架构文档为单一主文档 | Done | 2026-03-03 | 2026-03-03 | 将双模式设计说明彻底并入 `docs/streaming-architecture.md`，把它收口为当前唯一主文档；同时清理任务台账中对旧双模式文档路径的陈旧引用，避免后续继续沿着失效文档名查找架构说明。 |
| P-044 | 将 Rust 对外服务门面类型统一为 OhMyGamepad 品牌命名 | Done | 2026-03-03 | 2026-03-03 | 将 `crates/ohmygamepad-gilrs` 对外门面从 `OhMyGamepadService*` 重命名为 `OhMyGamepadService*`，并同步把服务命令 DTO 收口为 `OhMyGamepadServiceCommandDto`；测试、README 与任务文档中的对外 API 名称也一并更新，继续保持 Rust workspace 编译与测试全绿。 |
| P-043 | 统一 Rust 包名与关键源码文件名为 OhMyGamepad 命名 | Done | 2026-03-03 | 2026-03-03 | 将 Rust workspace 的四个 crate 目录与 Cargo 包名统一重命名为 `ohmygamepad-protocol`、`ohmygamepad-core`、`ohmygamepad-bridge`、`ohmygamepad-gilrs`，同步修正所有 crate 依赖与 `use` 路径；同时把服务级源码文件收口为 `oh_my_gamepad_service.rs`，更新 workspace README、RFC 与任务记录中的旧包名引用，并确认 `cargo fmt --all`、`cargo check --workspace --all-targets`、`cargo test --workspace` 全部通过。 |
| P-042 | 补齐 webrtc-direct 卡流恢复的文档与关键注释 | Done | 2026-03-03 | 2026-03-03 | 在当前主文档 `docs/streaming-architecture.md` 中补充 `webrtc-direct` 的 `media-stalled` 判定与恢复顺序说明，并为 `WebRtcDirectRuntime`、`useStreamRuntime`、`useStreamController` 中的卡流检测、帧追踪清理与 warning 收口逻辑补充简短中文注释，方便后续继续演进恢复策略时快速定位边界。 |
| P-041 | 统一 Rust OhMyGamepadService 门面 DTO 并修复 service/bridge 编译回归 | Done | 2026-03-03 | 2026-03-03 | 将多手柄采样策略、模拟手柄描述与服务级命令 DTO 下沉到 `crates/ohmygamepad-protocol`，在 `crates/ohmygamepad-gilrs` 的 `OhMyGamepadService` 上新增 `apply_command(OhMyGamepadServiceCommandDto)` 统一命令门面，并补齐 `ohmygamepad-bridge` 对新增命令的翻译分支；同时为虚拟设备连接/断开和模拟输入提交增加短暂同步等待，修复查询接口与后台采样线程之间的时序抖动，最终恢复 `cargo check --workspace --all-targets` 与 `cargo test --workspace` 全绿。 |
| P-040 | 为 webrtc-direct 补齐 media-stalled 检测与自动恢复 | Done | 2026-03-03 | 2026-03-03 | 在 `WebRtcDirectRuntime` 内部新增浏览器直出模式的卡流检测：基于 transport connected 后的媒体活动时间做 5 秒轮询、15 秒超时判定，在长时间无新视频帧时自动触发 `requestReconnect('media-stalled')`，优先走 ICE restart，失败再回退到本地 runtime 重建；同时在 controller 层于 `reconnecting` 阶段收口 warning UI，避免恢复过程中沿用旧连接的“无帧告警”。 |
| P-039 | 清理 player 旧类型债并消除重构后的 lint error | Done | 2026-03-03 | 2026-03-03 | 以不改业务语义为前提，集中清理 `src/renderer/src/player` 中阻塞校验的旧 `any`/空方法问题：为 emitter、输入帧、浏览器扩展能力与 rumble/stats/message channel 等补上显式类型，修正 `GamepadDriver`、`KeyboardDriver`、`InputPacketEncoder` 的动态索引写法，并将这批文件统一过一遍 Prettier；最终使 `player` 与 `streaming` 目录在 eslint quiet 模式下无 error，同时保持 `vue-tsc` 通过。 |
| P-038 | 为 Rust OhMyGamepadService 补齐多手柄策略与虚拟输入能力 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs` 的 `OhMyGamepadService` 上新增多手柄采样策略 `MultiControllerSamplingStrategy`，支持默认 merge、primary 优先、单设备 pause/resume、设备查询/发现，以及键盘 fallback；同时加入外部模拟手柄注入接口，可连接/断开虚拟设备并提交逻辑态输入，补齐 5 条 service 级端到端测试覆盖 merge、primary、keyboard fallback、device query 与 pause/resume 行为。 |
| P-037 | 清理 streaming 重构回归并修正 webrtc-direct 运行时残留问题 | Done | 2026-03-03 | 2026-03-03 | 审查 `streaming/application` 与 `streaming/runtime` 重构后的运行时链路，修正 `webrtc-direct` 中未使用参数报错；同时收口 runtime 重连过程中的帧追踪/定时器残留，避免 `connected -> reconnect -> connected` 后重复绑定 frame tracking；并在 controller 层统一重置 warning 计时与可见性，防止重连期间仍沿用旧连接的“无帧告警”状态。 |
| P-036 | 为 Rust 输入服务补齐采样预设与 OhMyGamepadService 门面 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-protocol` 中新增 `OhMyGamepadSamplingPresetDto` 与内置频率配置，提供 `PowerSaver`、`Balanced`、`HighResponse`、`MaxPrecision` 四档采样预设；同时在 `crates/ohmygamepad-gilrs` 中新增 `OhMyGamepadService` 与 `OhMyGamepadServiceConfig`，统一收口 runtime 启动、快照读取、采样切换、路由/绑定/profile 更新等常用操作，并补齐 facade 与采样预设测试。 |
| P-035 | 完成 webrtc-direct runtime 内聚落地与浏览器侧重建兜底 | Done | 2026-03-03 | 2026-03-03 | 将 `webrtc-direct` 改造成真正内聚的 runtime 模块：`WebRtcDirectRuntime` 不再直接暴露固定 `PlayerClient` 实例，而是自持 `client factory + runtime emitter`，统一中继 transport/chat/media/error 事件；在浏览器侧先尝试 ICE restart，失败后再本地重建 `PlayerClient`、恢复 viewport/帧追踪与麦克风状态；同时让 application 在 `closed` 状态也走 runtime 重连，并同步修正文档中关于 application/runtime 职责和 xCloud 时序的旧表述。 |
| P-034 | 打通 Rust 内部 gilrs runtime 入口与端到端测试 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs` 中新增 `spawn_gilrs_input_runtime` 与可注入 source 的 runtime 入口，让真实 `gilrs` 后端可以直接启动 `ohmygamepad-core` 后台线程；同时补齐跨 crate 线程测试，覆盖 `gilrs event -> backend poll -> logical pad snapshot` 与 DPad 按钮态映射，先把 Rust 内部链路跑通，暂不接入主进程。 |
| P-033 | 接入真实 gilrs 桌面手柄事件源 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs` 中引入真实 `gilrs` 依赖与 `RealGilrsSource`，默认 backend 已通过 `GilrsBuilder` 初始化桌面手柄上下文，并把 `gilrs::Event` 翻译为内部标准化事件；同时补齐按钮/轴索引映射、DPad 轴到按钮扇出、启动时已连接设备发现、时间戳转换等测试，使 Rust 侧已经具备真实桌面手柄输入接入能力。 |
| P-032 | 落地 host bridge + runtime 内聚连接状态机改造 | Done | 2026-03-03 | 2026-03-03 | 将 renderer 侧串流代码从“application 直接编排 offer/ICE”切到“application 提供 host bridge、runtime 自己管理连接状态机”的实现：新增 `createStreamRuntimeHostBridge.ts`，删除 `stream-runtime-signaling.ts`，让 `useStreamRuntime` 与 `WebRtcDirectRuntime` 改为 `start/requestReconnect/stop` 高层接口；同时补上浏览器侧 ICE restart 支持、chat 重协商下沉、`reconnecting` 状态文案，并更新当前架构与 xCloud 时序文档。 |
| P-031 | 为 Rust gilrs 后端补齐状态聚合与生命周期测试 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-gilrs` 中将原 stub 重构为“标准化 source 事件 + 有状态 backend 聚合器”两层，支持设备 Added/Updated/Removed 生命周期、按钮/轴输入聚合、单轮 poll 合并 sample、断连前冲刷 dirty sample 以及 `max_events_per_poll` 限流；同时补齐 5 个单元测试，为后续接入真实 `gilrs` crate 保留了清晰的适配边界。 |
| P-030 | 为 Rust 输入内核补齐稳定设备 profile 匹配 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-core` 中将设备 profile 从仅依赖运行期 `device_id` 升级为 `matcher` 规则匹配，支持组合 `device_id`、`vendor_id`、`product_id`、backend 与名称特征，并在 `engine` 中按匹配特异性择优应用；同时补齐设备重连后 profile 仍然命中、精确 `device_id` profile 覆盖硬件通配 profile、runner 线程更新 profile 等测试，使输入内核更接近真实桌面手柄环境下的稳定行为。 |
| P-029 | 重构双模式串流架构为 host bridge + runtime 状态机方案 | Done | 2026-03-03 | 2026-03-03 | 明确 renderer 拆成业务控制面与实时连接面，要求 `application` 只保留业务会话编排，`runtime` 自己拥有本地连接状态机、信令时序、重连/ICE restart；同时引入 `StreamRuntimeHostBridge`、`StreamRuntimeSessionContext` 等目标抽象，作为后续把信令和恢复能力下放到 `webrtc-direct` / `rust-owned` 的设计基线。当前说明已并入 `docs/streaming-architecture.md`。 |
| P-028 | 为 Rust 输入内核补齐设备 profile 配置链路 | Done | 2026-03-03 | 2026-03-03 | 在 `crates/ohmygamepad-core` 中新增设备 profile 抽象，支持按设备精确覆盖按钮映射、轴映射与过滤参数；让 `engine` 与后台 runner 都能应用和更新 profile，并补充 profile 映射、deadzone 覆盖与线程更新 profile 的测试，使 Rust 输入内核从固定标准布局扩展为可配置实现。 |
| P-027 | 继续收窄非导航胶囊按钮圆角 | Done | 2026-03-03 | 2026-03-03 | 为非导航 action 按钮新增独立 `--ui-action-pill-radius`，将 `Home` 刷新、`XStream` 顶部/错误按钮、设置弹层 action、串流弹层 action 等从胶囊形收窄为更克制的圆角矩形，同时保留顶部导航圆形按钮不变。 |
| P-026 | 完成 Rust 输入阶段的采样调度与线程 runner | Done | 2026-03-03 | 2026-03-03 | 在 `native/ohmygamepad-core` 中拆分 backend poll 与 logical pad sampling，新增可测试的采样调度器 `SamplingSchedule` 与后台线程句柄 `InputRuntimeHandle`；补齐 route/sampling/rebind 命令处理与线程级测试，使本阶段 Rust 输入内核已经具备独立采样循环与命令驱动能力。 |
| P-025 | 收敛双模式 runtime 分层并下沉 webrtc-direct 实现 | Done | 2026-03-03 | 2026-03-03 | 将 `streaming/runtime` 改造成真正的 mode 工厂与宿主层：拆分 `controllerInput/keyboardPointerInput/viewport` 合同与 `GamepadSidecarClient/RustOwnedRuntimeSidecarClient` 抽象；新增 `webrtc-direct` 与 `rust-owned` 两类 runtime 实现入口；把浏览器显示与 `PlayerClient` 依赖下沉到 `runtime/webrtc-direct`，并让 application 统一经由 `StreamRuntime` 合同驱动。 |
| P-024 | 实现 Rust 输入内核基础流水线并补测试 | Done | 2026-03-03 | 2026-03-03 | 在 `native/ohmygamepad-core` 中落地原始 sample 入库、标准手柄映射、deadzone/trigger 过滤、逻辑 pad 合并、多绑定策略解析与快照 diff；补充 `filter`、`mapping` 与 `engine` 单元测试共 14 项，并新增 `cargo:test` 脚本统一执行 Rust workspace 测试。 |
| P-023 | 打通 gamepad 主进程桥接骨架与 gilrs 包边界 | Done | 2026-03-03 | 2026-03-03 | 在 shared RPC/events 中新增 `gamepad` 契约，落地主进程 `gamepad` 内存态 service、RPC handler 与 renderer 事件桥；同时在 Rust workspace 中补充 `native/ohmygamepad-gilrs` 包骨架，使逻辑 pad DTO、主进程 bridge 与桌面后端包边界可以并行演进。 |
| P-022 | 回调焦点样式为内描边并保留头部圆形导航 | Done | 2026-03-03 | 2026-03-03 | 将全局焦点阴影从外侧留白 ring 回调为内侧细描边，避免焦点外扩；同步增加 tabs 与 icon button 的内部留白，减轻描边贴内容的问题；顶部导航与头像保留为真正的圆形按钮。 |
| P-021 | 搭建 Rust 输入 workspace 与逻辑 pad DTO 骨架 | Done | 2026-03-03 | 2026-03-03 | 在仓库根目录新增 Cargo workspace，建立 `native/ohmygamepad-protocol`、`native/ohmygamepad-core`、`native/ohmygamepad-bridge` 三包结构；落地 Rust/TypeScript 双端的逻辑 pad、采样、路由、桥接 DTO 契约，补充输入内核与桥接骨架，并将 `rfc/gamepad.md` 同步为根级多包工程方案。 |
| P-020 | 收敛全局焦点态与圆角风格 | Done | 2026-03-03 | 2026-03-03 | 将全局焦点 token 调整为更克制的细边框与外侧留白 ring，减少大面积发光与贴边描边；同步下调 foundation 层圆角基准，并把卡片、标签、设置弹层、串流操作区等高频控件统一收敛到更小的圆角尺度。 |
| P-019 | 收紧 Rust 手柄输入 RFC 的边界与采样模型 | Done | 2026-03-03 | 2026-03-03 | 继续修订 `rfc/gamepad.md`，明确 Rust 层仅负责手柄发现/映射/过滤/路由/逻辑 pad/独立采样线程，不负责 `xCloud` 编码；补充多手柄绑定与灵活采样策略，并将平台优先级收敛为 macOS / Windows，高级震动与移动端仅保留架构扩展口。 |
| P-018 | 修正根节点留白与标签栏缩放细节 | Done | 2026-03-03 | 2026-03-03 | 显式将 `html/body` 的 `margin` 与 `padding` 归零，避免根节点默认留白参与布局；将 `SpatialNavTabs` 的选中横条尺寸改为语义 token 驱动，并在 `compact/narrow` 档位回调 tab 字号，修复小窗口下标签文字过小且横条未同步缩放的问题。 |
| P-017 | 修正双模式 runtime 文档中的输入与渲染假设 | Done | 2026-03-03 | 2026-03-03 | 修正双模式 runtime 设计与合同草案：明确双模式共性仅为“手柄输入 Rust 化”，`webrtc-direct` 保持 browser render，只有 `rust-owned` 使用 native sidecar 渲染；同时将 runtime capabilities 拆成 render/controller-input/keyboard-pointer-input 三类所有者。当前说明已并入 `docs/streaming-architecture.md`。 |
| P-016 | 收敛 Rust 手柄抽象层落地方案 | Done | 2026-03-03 | 2026-03-03 | 重写 `rfc/gamepad.md`，基于当前 `renderer` 输入链路、`InputFrame` 兼容需求与双 runtime sidecar 目标，收敛为单 `ohmygamepad-core` crate 起步、`gilrs` 优先、UI/串流双 sink、输入先行而高级震动后置的渐进式 Rust 手柄方案。 |
| P-015 | 收敛窗口适配中的黑边与视觉冗余 | Done | 2026-03-03 | 2026-03-03 | 修正 renderer 根容器与 `AppShellLayout` 的铺满策略，缓解窗口变化时界面四周露黑；让 `ConsoleStatusCard` 改为带上下限的响应式尺寸，缩窗时主机卡片会继续收紧；将 `XCloud` 页头降回简朴结构，统一收轻核心圆角，并把设置页改为最大宽度居中布局。 |
| P-014 | 设计双模式串流 runtime 目标架构与抽象 | Done | 2026-03-03 | 2026-03-03 | 明确 `webrtc-direct` 与 `rust-owned` 双模式下的 renderer/runtime/sidecar/main 分层、native sidecar 模块建议、输入/渲染/传输抽象与迁移顺序；同时新增 `src/renderer/src/streaming/runtime/contracts.ts` 作为 renderer 侧统一 runtime 合同草案。当前设计说明已并入 `docs/streaming-architecture.md`。 |
| P-013 | 精修 XCloud 与 XStream 的视觉层级 | Done | 2026-03-03 | 2026-03-03 | 重构 `XCloud` 页头为带状态提示的 hero/search/filter 分层结构，强化 `GameCard` 的封面聚焦与标题可读性；同步精修 `XStream` 顶部状态区、操作区与性能面板的玻璃质感和层级关系，使 UI 密度适配后的页面观感更稳定。 |
| P-012 | 扩展 UI 密度档位到剩余浮层与串流界面 | Done | 2026-03-03 | 2026-03-03 | 将 `UserProfileMenu`、`Login`、`Setting*Sheet`、`SettingToggleRow`、`XStream` 与各类 stream sheet/performance panel 接入密度 token；补齐 loading/tabs 等基础 token；清理 renderer 中遗留的 `@media (max-width: ...)` 适配写法，统一为 `data-ui-density` 驱动。 |
| P-011 | 建立 UI 密度档位与页面适配骨架 | Done | 2026-03-03 | 2026-03-03 | 新增 renderer 侧 `data-ui-density` 机制，以窗口宽度切分 `comfortable / standard / compact / narrow` 四档；在主题语义层补充 rail、卡片、设置页、搜索区等离散 token；将 AppShell、TopNav、Home、XCloud、Setting 及核心卡片组件改为消费档位 token，并以页面重排替代全局等比缩放。 |
| P-010 | 补齐串流重构分层与流程文档 | Done | 2026-03-03 | 2026-03-03 | 扩展 `docs/streaming-architecture.md`，补充 renderer/main 分层职责、职责清单、主链路时序图与后续移动端/Rust 化边界说明，作为当前串流重构后的设计留档。 |
| P-009 | 清理 renderer 侧现存类型错误 | Done | 2026-03-03 | 2026-03-03 | 统一将串流页与 streaming application/runtime 中的浏览器定时器句柄收敛为 `number`；将 runtime 实例引用改为 `shallowRef` 以保留 `PlayerClient` 类实例类型；修正鼠标键盘映射推导类型，并补回 `GamepadDriver` 的键盘监听注册，最终使 `typecheck:web` 恢复通过。 |
| P-008 | 建立 1080p 默认窗口基准 | Done | 2026-03-03 | 2026-03-03 | 将主窗口默认尺寸调整为 `1920x1080`，作为当前 UI 调整的统一设计基线；多分辨率适配策略后续放在 UI 层按具体页面与组件推进。 |
| P-007 | 完成串流 application/runtime 边界迁移 | Done | 2026-03-02 | 2026-03-02 | 新增 `src/renderer/src/streaming/application` 与 `src/renderer/src/streaming/runtime`，移除旧 `composables/stream` 路径；将 keepalive、fallback 重试、信令 RPC 交换收敛回 application 层；将 `src/renderer/src/player` 收窄为低层本地端点实现，去除回调式 signaling API，并补充 `docs/streaming-architecture.md` 说明新的职责边界。 |
| P-006 | 拆分串流会话编排与播放器编排职责 | Done | 2026-03-02 | 2026-03-02 | 将 `useStreamSession` 拆分为 route/model、remote RPC、session orchestration 三层；将 `useStreamingPlayer` 拆分为 player factory、signaling orchestration、player lifecycle 三层，减少单文件职责耦合并收敛 WebRTC/DOM/RPC 细节。 |
| P-005 | 修复架构审查发现的关键边界问题 | Done | 2026-03-02 | 2026-03-02 | 为主进程 RPC 增加统一运行时入参校验；修复串流页在 `targetId` 变化时的复用问题；将 fallback TURN 发现下沉回 main/streaming 域；收敛 streaming 对 auth token/transfer token 逻辑的重复实现。 |
| P-004 | 评估 streaming 域、player 与串流页的职责边界 | Done | 2026-03-02 | 2026-03-02 | 确认 `src/renderer/src/player` 已基本具备库边界；`composables/stream` 更接近应用编排层而非纯 domain，其中 `useStreamSession` 混入 route/router/i18n/RPC，`useStreamingPlayer` 混入 signaling/keepalive；`XStream.vue` 主要承担页面与交互编排，职责总体合理。 |
| P-003 | 审查当前代码架构 | Done | 2026-03-02 | 2026-03-02 | 审查 Electron 主进程、preload、renderer、RPC/事件桥接与串流子系统的模块边界，识别出串流页职责过载、跨域基础设施复用不当、远程依赖下沉到 renderer 以及缺少自有测试覆盖等风险。 |
| P-002 | 调整 README 为英文主文档并补充中文版本 | Done | 2026-03-02 | 2026-03-02 | 将根 README 改为英文说明，新增 README.zh-CN.md 作为中文版本，并在中英文文档之间增加互链，保持项目说明与致谢信息一致。 |
| P-001 | 初始化项目 | Done | 2026-03-02 | 2026-03-02 | 将项目初始化，包括项目结构、依赖管理、代码规范等。 |


## Archive Notes

- P-001：初始化项目

## Maintenance Rules

- `docs/project-task.md` 只记录当前活跃开发阶段的任务与规则。
- 新任务统一使用 `P-xxx` 编号，按完成顺序递增维护。
- 每次任务完成后，必须立即更新本文件。
