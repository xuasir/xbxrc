# Project Task Archived Snapshot

> 来源：`docs/project-task.md`
> 日期：2026-04-18
> 说明：本文件为从主任务表归档出的历史条目（主表仅保留最近 3 条）。

## Archived Task Rows

| ID | Task | Status | Started On | Completed On | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-04-18-unify-native-video-host-budget-with-submit-cadence | 统一 native_video 的 host stale budget 与视频流 submit 节拍，修复高刷 host 下播放期过早 stale/供给饥饿 | Completed | 2026-04-18 | 2026-04-18 | 小任务。改动文件：`src-tauri/src/mods/native_video/scheduling.rs`、`src-tauri/src/mods/native_video/scheduling.test.rs`。修复内容：1）`HostCadenceTelemetry` 新增 submit 时间窗口统计，补齐 `submit_interval_ms()` 与 `effective_frame_interval_ms()`；2）`frame_age_budget_ms()` 从仅基于 `display_interval_ms()` 改为基于 `max(display_interval_ms, submit_interval_ms)` 的统一有效提交间隔，避免 120Hz host 把 30fps 左右视频流长期误判成 24ms stale 窗口；3）`reset_frame_slot()` 同步清理 submit 时间窗口；4）新增定点回归测试 `low_video_fps_submit_interval_relaxes_host_stale_budget_under_high_refresh_ticks`，锁定“高刷新 display tick + 低视频 fps submit”场景下预算会按视频流节拍放宽。验证：`cargo test -p xbxrc low_video_fps_submit_interval_relaxes_host_stale_budget_under_high_refresh_ticks`、`cargo test -p xbxrc mods::native_video::scheduling`。 |
| 2026-04-18-keep-waiting-keyframe-recovery-from-falling-into-display-supply-starved | 修正 owner 在 fresh `NonIdrVcl` 但 decode/present 已不可服务时的归因，避免恢复从 waiting-keyframe 滑回 `displaySupplyStarved` | Completed | 2026-04-18 | 2026-04-18 | 小任务。改动文件：`crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs`、`crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.test.rs`。修复内容：1）新增 `invalid_bootstrap_waiting_keyframe_blocked()`，当 post-startup fresh `NonIdrVcl`/terminal-invalid bootstrap 仍存在且当前输出已不可服务时，owner 继续保持 `RebuildingSupply`，不再误落入 `SupplyStarved`；2）补充回归测试 `fresh_terminal_invalid_bootstrap_after_serving_stays_rebuilding_supply`，锁定“曾经稳定输出后再次等待关键帧”的 trace 场景。验证：`cargo test -p xbxengine fresh_terminal_invalid_bootstrap_after_serving_stays_rebuilding_supply`、`cargo test -p xbxengine transport::rtc::policy::video_scheduling_owner::tests`。 |
| 2026-04-18-fix-playback-freeze-host-reset-keyframe-stale-and-tick-skip | 修复播放期卡死三连问题：host timing shift 误触发 decoder reset、恢复关键帧过早 stale、runtime tick skip 放大供帧稀疏 | Completed | 2026-04-18 | 2026-04-18 | 小任务。改动文件：`crates/xbxengine/core/src/transport/rtc/stack/runtime_port.rs`、`src-tauri/src/mods/native_video/scheduling.rs`、`src-tauri/src/mods/native_video/scheduling.test.rs`、`src-tauri/src/mods/xbxengine/service.rs`。修复内容：1）`LocalDecoderResetPolicyState` 仅在前后都是稳定 host display interval 样本时才允许把 timing shift 升级为本地 decoder reset，避免 presenter 重建 / 首帧 priming 的 `Some(..)<->None` 抖动误触发 `external-decoder-reset-requested`；2）`HostCadenceTelemetry` 为恢复关键帧单独放宽 stale budget，避免 host stall 后首个 `repairing` keyframe 刚入队就因 24ms 稳态阈值被 `scheduledFrameStale` 打掉；3）`xbxengine service` 的 runtime tick missed behavior 从 `Skip` 改为 `Delay`，避免恢复期单拍超时后继续丢拍放大 latest-slot 稀疏供帧。新增验证：`presenter_epoch_rebuild_timing_shift_does_not_trigger_local_decoder_reset`、`recovery_keyframe_is_not_dropped_as_stale_immediately_after_submit`、`runtime_tick_does_not_skip_missed_intervals`。执行验证：`cargo test -p xbxengine runtime_port::tests`、`cargo test -p xbxrc mods::native_video::scheduling::tests`、`cargo test -p xbxrc mods::xbxengine::service::tests`。 |
| 2026-04-18-add-stream-freeze-diagnostic-log-profile | 补齐串流卡死诊断日志画像，覆盖 decode/pacer/renderer/native_video/keyframe-h264 关键链路 | Completed | 2026-04-18 | 2026-04-18 | 小任务。改动文件：`crates/xbxengine/core/src/media/video/decode/actor.rs`、`crates/xbxengine/core/src/media/video/pacer/actor.rs`、`crates/xbxengine/core/src/media/video/pacer/actor.test.rs`、`crates/xbxengine/core/src/media/video/render/actor.rs`、`crates/xbxengine/core/src/media/video/render/pacer.rs`、`crates/xbxengine/core/src/runtime_stats_sink.rs`、`src-tauri/src/mods/native_video/scheduling.rs`、`src-tauri/src/mods/native_video/scheduling.test.rs`、`src-tauri/src/mods/native_video/mod.rs`、`src-tauri/src/mods/native_video/presenters.rs`。内容：1）`ScheduledFrameSlot` 与 `HostCadenceTelemetry` 新增 diagnostics snapshot，native_video 在 `frame_submit / frame_slot_take_skipped / frame_slot_take_retained / frame_slot_take_dropped / prepare_sample_ready` 中统一记录 slot 深度、displayed/pending frame、tick/present epoch、cadence/no-pending 状态；2）decode/pacer 的 backpressure summary 改为携带队列深度、candidate detail、recovery state、host epoch/cadence 信息，`decode outputQueueOverflow` 额外记录被挤掉帧的 seq/rtp/recovery state；3）renderer 接收帧时补齐 `frameSeq/rtpTimestamp/isKeyframe/overwrittenPreviousLatest` 摘要；4）`keyframeRequestEpisodeResponseObserved` 补齐 sent->first packet 时序和 first packet keyframe 上下文，`h264InspectionObserved` 补齐 `isIdr/bootstrapReady/bootstrapRejectReason/boundEpisodeId/boundAsRecoveryResponse`，便于区分远端非 IDR 与本地绑定失真。验证：`cargo test -p xbxengine keyframe_request_episode_response_observed_tracks_non_keyframe_then_rejected_keyframe`、`cargo test -p xbxengine decode_backpressure_summary_includes_candidate_and_recovery_state`、`cargo test -p xbxengine render_backpressure_summary_includes_host_epochs_and_queue_depths`、`cargo test -p xbxrc slot_diagnostics_snapshot_reports_displayed_and_pending_frames`、`cargo check -p xbxengine --quiet`、`cargo check -p xbxrc --quiet`。 |
| 2026-04-18-fix-recovery-command-accounting-and-replay-truthfulness | 修复恢复命令记账、control replay 与 NACK 时间投影失真 | Completed | 2026-04-18 | 2026-04-18 | 小任务。改动文件：`crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs`、`crates/xbxengine/core/src/transport/rtc/connection/service.rs`、`crates/xbxengine/core/src/transport/rtc/connection/data_channel.rs`、`crates/xbxengine/core/src/transport/rtc/connection/control_channel.rs`、`crates/xbxengine/core/src/api/runtime/lifecycle.rs`、`crates/xbxengine/core/src/session/recovery.test.rs`、`crates/xbxengine/core/src/transport/rtc/connection/service.test.rs`。修复内容：1）keyframe 请求不再依赖全局 `latest_observation_label` 猜测结果，改为由 `VideoKeyframeRequestOutcome` 显式返回 `Suppressed/PLI/FIR/Control`，并据此落账 command status 与 escalation success；2）`resolve_recovery_command_family_decision()` 改为按当前 `decision_id` 从 `recent_recovery_decision_ledgers` 绑定语义，不再读取 `latest` ledger 串账；3）control replay 改成逐条成功逐条清理 pending，避免“部分成功重复重放”和“发送失败后永久丢单”两类半提交失真；4）`transport_await_local_recovery_active()` 只把近期 `action=sent` 的 NACK 视为活跃本地修复；5）runtime recovery 对 NACK 时间投影补齐 `expiredMaxAge` 与 `recoveredLate`。新增/更新定点测试：`keyframe_suppressed_outcome_is_recorded_as_deferred`、`replay_send_failure_retains_pending_actions`、`expired_max_age_is_treated_as_recent_nack_failure`、`recovered_late_still_counts_as_recent_nack_recovery`、`skipped_nack_does_not_count_as_active_local_recovery`。验证：`cargo check -p xbxengine --quiet`、`cargo test -p xbxengine keyframe_suppressed_outcome_is_recorded_as_deferred -- --nocapture`、`cargo test -p xbxengine replay_send_failure_retains_pending_actions -- --nocapture`、`cargo test -p xbxengine expired_max_age_is_treated_as_recent_nack_failure -- --nocapture`、`cargo test -p xbxengine recovered_late_still_counts_as_recent_nack_recovery -- --nocapture`、`cargo test -p xbxengine skipped_nack_does_not_count_as_active_local_recovery -- --nocapture`。 |
| 2026-04-18-fix-recovery-coordinator-reconnect-completion-and-retired-signal-layer | 修复 reconnect 完成条件过早与 connectivity 证据旁路，并清理退役 signal/diagnosis 层 | Completed | 2026-04-18 | 2026-04-18 | 小任务。改动文件：`crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs`、`crates/xbxengine/core/src/transport/rtc/recovery/mod.rs`、`crates/xbxengine/core/src/transport/rtc/pipeline/session_loop.rs`，删除 `crates/xbxengine/core/src/transport/rtc/recovery/diagnosis.rs` 与 `crates/xbxengine/core/src/transport/rtc/recovery/signal.rs`。修复内容：1）`check_reconnect_completed()` 不再把“Connected + 新包到达”视为恢复完成，改为要求当前 epoch clean anchor，或 reconnect episode 之后出现新的 decode/present 进展；2）`TransportExpiredDeadline` / `TransportSampleLoss` 在高 repairability 且仍属 packet-loss 级别时，先走 observation/state coordinator 保持在 local repair，不再一律直接压成 `RequestKeyframe`；3）删除已退役 signal/diagnosis 层及相关注释残留，避免继续制造旧分层仍在主线生效的错觉。新增定点测试：`reconnect_completion_requires_post_episode_decode_or_present_progress`、`connectivity_reason_with_high_repairability_stays_in_local_repair`。验证：`cargo test -p xbxengine reconnect_completion_requires_post_episode_decode_or_present_progress`、`cargo test -p xbxengine connectivity_reason_with_high_repairability_stays_in_local_repair`、`cargo test -p xbxengine owner_signal_local_evidence_reaches_observation_layer`、`cargo test -p xbxengine connected_reconnect_request_does_not_clear_inflight_without_edge`、`cargo test -p xbxengine recovery_integration_repeated_transport_expired_deadline_upgrades_to_connectivity_reconnect`、`cargo check -p xbxengine`。 |
| 2026-04-17-fix-recovery-execution-context-accessors | 修复 RecoveryExecutionContext 访问方法不完整问题 | Completed | 2026-04-17 | 2026-04-17 | 代码质量审查发现 `RecoveryExecutionContext` 的访问方法严重不完整。在 `crates/xbxengine/core/src/transport/rtc/session/policy.rs` 的 `RecoveryExecutionContext` impl 块中添加了 8 个缺失的访问方法：`recovery_action_label()`、`unlock_reason()`、`preempt_reason()`、`fault_domain_evidence()`、`reconnect_reason()`、`failed_terminal()`、`cost_ceiling()`、`budget_after()`。所有方法均使用正确的返回类型（引用或值），并添加了中文文档注释。验证：`cargo build -p xbxengine` 编译通过。 |
| 2026-04-18-fix-clean-anchor-sustaining-nonidr-suppression | 修复 clean anchor 后 `sustaining-recovery` 错误吞掉 fresh `NonIdrVcl`，导致关键帧请求被 suppress | Completed | 2026-04-18 | 2026-04-18 | 小任务。根因：`recovery/contract.rs` 的 `has_current_transport_await_issue_from_stats()` 只信任 timeline，把 `sustaining-recovery/recoverySustaining` 直接当作“无 unresolved transport-await”，没有把 clean anchor 之后的新鲜 invalid bootstrap（如 `NonIdrVcl`）纳入口径，导致 `connection/service.rs` 把恢复请求错误记为 `rtcVideoRecoverySuppressed`。修复：在 `crates/xbxengine/core/src/transport/rtc/recovery/contract.rs` 中补充“当前 epoch 已有 clean anchor + timeline 处于 `sustaining-recovery` + 最新 H264 inspection 仍是 fresh invalid bootstrap”时继续视为 unresolved transport-await 的统一合同；并新增 contract 定点测试。已有 `crates/xbxengine/core/src/transport/rtc/connection/service.test.rs` 红测随之转绿。验证：`cargo test -p xbxengine transport::rtc::connection::service::tests::request_video_keyframe_does_not_suppress_sustaining_recovery_when_fresh_non_idr_blocks_bootstrap -- --exact`、`cargo test -p xbxengine transport::rtc::connection::service::tests::request_video_keyframe -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::contract::derive_gap_observation_tests -- --nocapture`。 |
| 2026-04-17-fix-recovery-coordinator-command-chain-review-findings | 修复 recovery coordinator 到 command 链路的 4 个 review finding | Completed | 2026-04-17 | 2026-04-17 | 小任务。改动文件：`crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs`、`crates/xbxengine/core/src/transport/rtc/recovery/action_coordinator.rs`、`crates/xbxengine/core/src/transport/rtc/recovery/state_coordinator.rs`、`crates/xbxengine/core/src/transport/rtc/session/policy.rs`、`crates/xbxengine/core/src/transport/rtc/session/policy_tests/recovery_integration.rs`、`crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs`。修复内容：1）`last_connected` 改为 `Option<bool>` 并按真实连接边沿清除 reconnect in-flight，避免已连接态的 media reconnect 在下一拍被误释放；2）`convert_to_proposal()` 直接保留 `decision.action`，正确透传 `CoalescedKeyframeInFlight` / `CoalescedDecoderResetInFlight`，不再误降级为 `CooldownSuppressed`；3）proposal 的 `budget_before/budget_after` 改为从状态机真实快照读取，并在动作协调器内为 reconnect 请求真实落账，避免使用按当前动作临时拼装的伪 budget；4）`RecoveryOwnerSignal` 新增 `gap_severity` / `repairability` 并从 `session::policy` 把 local recovery evidence 注入 `RecoveryObservation`，恢复 packet-loss 路径对严重性与可修复性的升级判断；5）补齐 `transport_await_has_hard_recovery_evidence()` 的 hard evidence 语义，只将 transport-await 对齐的 terminal keyframe failure / fresh invalid bootstrap 视为硬证据，避免把“仅 reset 进行中”误放行为 reconnect，同时恢复 `awaitSuccessEdge` 门控顺序。新增定点测试：`connected_reconnect_request_does_not_clear_inflight_without_edge`、`coalesced_keyframe_action_is_preserved_in_proposal`、`reconnect_budget_snapshots_reflect_before_and_after_state`、`owner_signal_local_evidence_reaches_observation_layer`。验证：`cargo test -p xbxengine transport::rtc::recovery::coordinator::tests -- --nocapture`、`cargo test -p xbxengine media_reconnect_candidate_waits_for_success_edge_before_regrant -- --nocapture`、`cargo test -p xbxengine media_reconnect_candidate_is_blocked_while_transport_await_reset_progress_is_active -- --nocapture`、`cargo check -p xbxengine` 通过。 |
| 2026-04-17-fix-recovery-render-pressure-and-stale-freshness | 修复恢复层两个 P2：render 压力门控误读 latest-slot 决策、恢复期 stale FPS 仍可误判稳定输出 | Completed | 2026-04-17 | 2026-04-17 | 小任务。改动文件：`crates/xbxengine/core/src/transport/rtc/session/facts.rs`、`crates/xbxengine/core/src/transport/rtc/recovery/runtime_state.rs`。修复内容：`read_owner_runtime_facts()` 仅从 `latest_video_frame_drop` 中提取 pacer 写入的 render-stage backpressure 细节（`rendererQueueOverflow` / `rendererQueueRejectLowerValue`），不再误读 `latest_render_candidate_decision` 的 latest-slot 替换观测；恢复层 `stable_output` 改为必须同时满足真实 fresh output、恢复带宽阈值和 present FPS，避免 `Recovering` 相位在 decode/present 已停更时仍被滞后平滑 FPS 提前判成 `Healthy`。新增定点回归测试：`read_owner_runtime_facts_prefers_render_stage_frame_drop_for_renderer_pressure`、`recovering_phase_without_fresh_output_does_not_return_healthy_from_stale_fps`。验证：`cargo test -p xbxengine read_owner_runtime_facts_prefers_render_stage_frame_drop_for_renderer_pressure -- --nocapture`、`cargo test -p xbxengine recovering_phase_without_fresh_output_does_not_return_healthy_from_stale_fps -- --nocapture`。 |
| 2026-04-16-enhance-analyze-runtime-logs-skill | 增强 `analyze-runtime-logs` 技能，对关键帧请求与 NACK 增加抑制、成功、失败、有效性及“成功后是否真正建链恢复”分析能力 | Completed | 2026-04-16 | 2026-04-16 | 小任务。改动文件：`.agents/skills/analyze-runtime-logs/scripts/summarize_runtime_trace.py`、`.agents/skills/analyze-runtime-logs/SKILL.md`、`.agents/skills/analyze-runtime-logs/references/analysis-playbook.md`、`.agents/skills/analyze-runtime-logs/references/log-schema.md`。增强内容：`recovery_audit` 新增 `keyframeEffectiveness` 与 `nackEffectiveness`，关键帧按 `keyframeRequestEpisode + videoChainTransition + linkedH264*` 统计 sent/response-observed/packet-seen/decoded/invalid/有效恢复/decoded 后未回到 healthy chain，并统计 ledger 中 keyframe suppression/coalesced；NACK 按 `nackSent/nackRecovered/nackSkipped/nackExpired` 统计成功、迟到成功、失败、无效恢复及 disposition/unrecoverable reason；新增 `chainBuildSuccessRate`（建链成功率聚合）、`nackEffectiveness.effectiveRate`（NACK 有效性聚合）、`repairabilityPersistence`（repairability 评分持久化统计）与 `recoveryEffectiveness.score`（恢复有效性综合评分）并同步文档口径。验证：`python3 -m py_compile .agents/skills/analyze-runtime-logs/scripts/summarize_runtime_trace.py`、`python3 .agents/skills/analyze-runtime-logs/scripts/summarize_runtime_trace.py --help` 通过。 |
| 2026-04-16-fix-github-actions-ffmpeg-deps | 修复 GitHub Actions 打包环境缺少 FFmpeg 开发依赖导致 `ffmpeg-sys-next` 构建失败的问题 | Completed | 2026-04-16 | 2026-04-16 | 小任务。改动文件：`.github/workflows/build-tauri.yml`。修复内容：macOS runner 从仅安装 `opus` 调整为同时安装 `pkg-config` 与 `ffmpeg`；Windows runner 新增显式 `vcpkg` 安装 `ffmpeg:x64-windows`，并导出 `VCPKG_ROOT` / `VCPKG_DEFAULT_TRIPLET`，让 `ffmpeg-sys-next` 能通过 `pkg-config` / `vcpkg` 发现开发库。验证：基于 `ffmpeg-sys-next 8.1.0` build script 构建路径核对完成；本地 `pnpm build`、`cargo check -p xbxrc` 已通过，CI 依赖链已补齐。 |
| 2026-04-16-fix-github-actions-build-tauri | 修复 GitHub Actions `build-tauri.yml` 中 Rust toolchain action 引用错误，避免 workflow 在拉取 action 阶段直接失败 | Completed | 2026-04-16 | 2026-04-16 | 小任务。改动文件：`.github/workflows/build-tauri.yml`。修复内容：将 `dtolnay/rust-toolchain@1.93.1` 改为 GitHub Actions 支持的 `uses: dtolnay/rust-toolchain@stable` + `with.toolchain: 1.93.1`，避免把 Rust 版本号误写成 action ref，导致 CI 在解析 action 时失败。验证：本地 `pnpm build`、`cargo check -p xbxrc` 通过；工作流 YAML 已按 action 约定修正。 |
| 2026-04-15-remote-behavior-log-probes | 在 sink.rs / source.rs / pacer/actor.rs 添加远端行为采集日志，覆盖 Q1（Xbox RTP payload 帧级标识）、Q2（keyframe 请求响应延迟）、Q3（IDR 帧是否带丢包）、Q4（host_frame_age_budget_ms 与 RTT 对比） | Completed | 2026-04-15 | 2026-04-15 | 小任务。改动文件：`sink.rs`（新增 `probe_packet_count`/`last_primary_rtp_timestamp` 字段，每 500 个 Primary 包或每次 IDR 候选包采集 payload 前 8 字节 hex + ts_delta）；`source.rs`（`request_recovery_keyframe_from_source`/`request_recovery_keyframe_soft_from_source` 记录请求时间；`recv_frame_inner` IDR 帧到达时记录 `response_delay_ms`/`media_dropped_packets`/`retry_count`；`maybe_retry_waiting_recovery_keyframe` 重试耗尽时记录 `total_wait_ms`）；`pacer/actor.rs`（主循环每 2000 次采集 `host_frame_age_budget_ms` 与 `video_rtt_ms` 对比）。文档更新：`packet-to-frame-pipeline.md` 新增第 5 节"远端行为采集计划"（Q1/Q2/Q3）；`frame-to-display-pipeline.md` 新增第 3 节"远端行为采集计划"（Q4）。验证：`cargo check -p xbxengine` 通过。 |
| 2026-04-15-frame-to-display-pipeline-doc | 补全 `dev-docs/isu/frame-to-display-pipeline.md` 的 2.3~2.6 章节分析，并修复 2.1/2.2 章节代码审查发现的 6 个问题 | Completed | 2026-04-15 | 2026-04-15 | 小任务。代码修复：1) `budget.rs` `resolve_playout_delay` 拆分 `Exhausted`/`Unknown` 分支，`Exhausted` 加 ratio.min(1100) 收紧，bias 分开为 5/7；2) `scheduler.rs` `can_exit_waiting_keyframe_with_recovery_continuation` 补注释说明 IDR 分支删除意图和冷启动不可触发；3) `scheduler.rs` continuation commit 路径补注释；4) `signal.rs` 新增 `FrameQueued` 变体，`DropBacklogEvictQueued` 不再映射到 `WaitKeyframe`；5) `source.rs` `playout_base_at` fallback 时保持 `None`，log 判断改为 `is_none()`；6) `source.rs` `build_ingress_materialization_fallback_budget` 补注释说明 Unknown 退回旧逻辑的原因。文档补全：2.3 RecoveryCoordinator（信号→诊断→升级三层结构、单调梯子、场景化参数、transport-await 分阶段恢复、hard fallback 超时、burst rollback）；2.4 解码 actor（背压机制、stall 判定、本地 reset、输出队列溢出）；2.5 Pacer（FramePacingPolicy、宿主机节拍对齐、QueueHistoryController 多维度决策、render_queue 替换策略、cadence_lag_ratio 动态 sleep guard）；2.6 Renderer（present 流程、candidate decision 追踪、mailbox 背压缺口）。验证：`cargo check -p xbxengine` 通过；`cargo test -p xbxengine` 984 passed 0 failed。 |
| 2026-04-14-source-drop-recovery-and-sustaining-exit | 修复 2.5 章节两个缺口：DropAndRequestKeyframe 路径在 NACK 无法定位缺失序列号时缺少后续恢复动作；SustainingRecovery 退出条件不感知解码器状态 | Completed | 2026-04-14 | 2026-04-14 | 小任务。改动文件：`source.rs`（新增 `handle_drop_and_request_keyframe_action`，NACK 无缺失序列号时升级为 soft/hard keyframe 请求；新增 `decoder_feedback_allows_sustaining_exit`，`mark_frame_complete_candidate` 加 decoder stall/recent decode ok 守卫；`set_waiting_for_recovery_keyframe` 调用移入各 match 分支显式处理）、`source.test.rs`（新增三个测试：`drop_and_request_action_contract_keeps_resolver_stateless`、`drop_and_request_fallback_without_missing_sequences_requests_keyframe`、`sustaining_recovery_exit_gate_requires_recent_decoder_feedback`）。验证：`cargo build` 通过；`cargo test -- --skip rtp_to_decode_to_pacer_to_renderer_pipeline_reaches_latest_frame_and_overwrite_signal` 981 passed 0 failed（跳过的长跑测试在改动前同样卡住，与本次改动无关）。 |
| 2026-04-14-packet-to-frame-pipeline-nack-fixes | 修复 packet-to-frame 链路 NACK 层四个问题：pending 合并时 last_sent_at_ms 未更新、定时维护 tick 被高包速率持续推迟、keyframe 重试无次数上限、OOS 准入提前 return 跳过 prefers_chain_broken 检查 | Completed | 2026-04-14 | 2026-04-14 | 小任务。改动文件：`nack_scheduler.rs`（合并时 retry_interval 收紧后同步 last_sent_at_ms）、`nack.rs`（maybe_run_nack_maintenance 只在真正到 tick 间隔时更新时间戳；OOS 降级加 prefers_chain_broken 守卫）、`nack_policy.rs`（新增 RECOVERY_KEYFRAME_RETRY_MAX_COUNT=8）、`source.rs`（maybe_retry_waiting_recovery_keyframe 加上限检查；should_run_nack_maintenance_tick 改为 pub(super)）。新增测试：`waiting_recovery_keyframe_stops_retrying_after_max_count`、`oos_signal_with_low_value_delta_is_skipped_but_reference_is_not`；修复已有测试 flaky 断言。验证：`cargo test transport::rtc::stream` 183 passed。 |
| 2026-04-10-fact-driven-recovery-modeling-adjustment | 基于运行时日志重建恢复建模，按帧价值、缺包严重性、升级门槛、InFlight 解锁、coalescing 五个维度收口事实驱动恢复合同 | In Progress | 2026-04-10 |  | 复杂任务。RFC：[`docs/rfcs/2026-04-10-fact-driven-recovery-modeling-adjustment.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-10-fact-driven-recovery-modeling-adjustment.md)。现状修正：已完成统一枚举合同、`transport_session/repeat_suppression` 的 in-flight/family gate 调整，以及 `recoveryDecisionLedger` 新字段贯通到 stats/trace/RPC；2026-04-11 复查确认 `video_source/timeline + nack_policy`、`recovery/coordinator`、`video_scheduling_owner`、`session::policy` 尚未全量接入统一模型，本轮继续补齐运行态主路径：`coordinator` 现在会对 `transportDeferred + unsent + invalid bootstrap + clean anchor` 直接 reopen keyframe，不再继续 `waitForBurst/coalesced:keyframeInFlight`；`video_source/nack` 已把 transport gap 的 `Recovery` window source 贯穿到 cloud latency admission，不再在二次 budget 计算时退回 `Transport`，同时把“恢复压力下的低价值 skip”升级为 `chain broken -> request keyframe` 主路径，避免坏链场景继续傻重试；进一步地，`sampleLoss` 和 `displayStarvedLowValueAdmission` 也已并入同一条 low-value escalation gate：低 repairability 的 `sampleLoss` 不再只是 `SkippedLowValue`，而会直接升级到 `SkippedChainBroken`，恢复压力下的 `displayStarved` 软 skip 也会转成坏链证据。最新继续收口到显式调度 phase：`nack.rs` 新增 `Startup/Recovery/Steady` 三态，建链期的 `reference/supply` near-deadline 不再被过早判死，steady 期维持 `SkippedTooLate`，只有恢复压力下才升级 `SkippedChainBroken`；同时 sample loss 已取消硬编码 `Recovery` window，改为复用统一 `transport_nack_window_source()` 与 timeline-merged `frame_value`，repairability 也开始显式吃 `phase/window source`。本轮又把 `owner/session/coordinator` 对“当前 transport-await 是否仍有效”的入口收成同一条 contract helper：若当前 recovery epoch 已有更晚的 clean anchor，则旧 unresolved timeline 不再继续算 current issue；owner 的 post-startup invalid bootstrap 也加入 freshness 与 clean-anchor 截断，避免旧 `bootstrapMissingSps/Pps/InvalidSliceHeader/NonIdrVcl` 在新链成立后继续续命恢复；`SupplyStarved + clean anchor + 轻 probe` 现在也不会再回跳 `RebuildingSupply`。进一步补齐残留出口后，`session::policy` 的 `recovery_transport_await_unresolved / recovery_ingress_waiting / recovery_exit_gate` 与 `coordinator` 的 `transport_await_ingress_still_waiting()` 也都已切到 current helper，不再让 stale `frame-await-recovery-anchor` 在 clean anchor 之后继续把 session 或 recovery lane 维持在 blocked 状态；同时 `displaySupplyDegraded` overlap 也不再被旧 escalation/session recovering 标记污染。`coordinator` 的 `has_recent_transport_await_keyframe_attempt()` 现要求当前 unresolved issue 仍在，`bootstrap_in_flight` 的 attempt context 也要求与 clean-anchor submit 时间窗对齐，避免旧 episode/escalation 继续污染 sustaining/probe/family hold。`source` 侧则删除了“lossy keyframe 直接本地 `TriggerWaitKeyframe`”分支，改成只做 decoder safety 的 `DropAndRequestKeyframe`，把后续是继续补包还是升级关键帧交给统一 NACK/recovery admission；inspection/wait-keyframe 路径也已收口到统一 helper。`budget` 侧已确认 recovery window 下的 `reference/supply` retry budget 为 `2`；`video_scheduling_owner` 也已允许 `terminal invalid bootstrap` 在 `clean anchor + committed SPS/PPS + delta continuation ready + degraded supply` 下释放 `RebuildingSupply`。最新又继续收紧了播放期恢复的关键灰区：当 `transportAwaitRecoveryAnchor` 已进入“无本地进展 + decoder reset budget 耗尽”的坏窗时，`coordinator` 不再继续把动作压回 `RequestDecoderReset / waitForBurst`，而会转为 `RequestReconnectCandidate`（若 reconnect 预算也耗尽则落到 `CooldownSuppressed`），避免 local recovery 死循环；`transport_session` 的 decoder-reset family gate 现已绑定真实 reset 尝试，并在 reset 后立刻看到 `NonIdrVcl/invalid bootstrap + 无 clean anchor` 时释放旧 family，不再继续 `sameFamilyCoalesced:decoderResetInFlight`；同时 `coordinator` 的 `transport_await_decoder_reset_attempt_still_in_flight_from_stats()` 也已允许直接使用 reset 之后的 fresh invalid inspection 释放 stale decoder-reset coalesce，避免新 inspection 仍挂在老 episode 上时继续产出 `CoalescedDecoderResetInFlight`。这一轮继续把 fallback 主动作收口到真实 episode 语义：一旦出现“旧 episode 已 `decoded/missed`，reset 后又收到 fresh invalid bootstrap，且当前已无 keyframe retry 阻塞”，`coordinator` 现在优先重开 `RequestKeyframe`，而不是重复 `RequestDecoderReset`，从而把播放期卡死从 local reset 回路拉回真正的 keyframe recovery 闭环。最新又补齐了 `video_scheduling_owner` 的剩余出口：当 fresh invalid bootstrap 已证明当前恢复响应无效、metadata 已齐、输出仍 serviceable，但当前 recovery epoch 还没有 clean anchor 时，owner 现在允许从 `RebuildingSupply` 降到 `DegradedServing`；反过来，若 clean anchor 已存在但 bootstrap blocker 仍未真正解除（例如 recent `NonIdrVcl` 且缺少 committed SPS/PPS/delta），则不允许借 clean-anchor restore 分支误退恢复态。定向验证：`cargo test -p xbxengine recovery_window_reference_gets_two_retry_budget -- --nocapture`、`cargo test -p xbxengine refresh_boost_supply_gets_two_retry_budget -- --nocapture`、`cargo test -p xbxengine cloud_latency_admission_preserves_recovery_window_for_transport_gap -- --nocapture`、`cargo test -p xbxengine transport_gap_uses_recovery_window_when_timeline_shows_recovery_pressure -- --nocapture`、`cargo test -p xbxengine low_value_skip_under_recovery_pressure_reopens_chain_recovery -- --nocapture`、`cargo test -p xbxengine sample_loss_low_repairability_promotes_low_value_skip_to_chain_broken -- --nocapture`、`cargo test -p xbxengine display_starved_low_value_skip_under_recovery_pressure_promotes_chain_broken -- --nocapture`、`cargo test -p xbxengine startup_supply_near_deadline_keeps_nack_attempt -- --nocapture`、`cargo test -p xbxengine steady_supply_near_deadline_stays_too_late_without_recovery_escalation -- --nocapture`、`cargo test -p xbxengine recovery_supply_near_deadline_escalates_chain_broken -- --nocapture`、`cargo test -p xbxengine supply_starved_probe_with_clean_anchor_stays_out_of_rebuilding_supply -- --nocapture`、`cargo test -p xbxengine degraded_supply_still_releases_terminal_invalid_bootstrap_waiting -- --nocapture`、`cargo test -p xbxengine clean_anchor_with_terminal_invalid_bootstrap_releases_rebuilding_supply -- --nocapture`、`cargo test -p xbxengine terminal_invalid_bootstrap_without_clean_anchor_releases_rebuilding_supply_when_output_serviceable -- --nocapture`、`cargo test -p xbxengine terminal_invalid_bootstrap_without_clean_anchor_and_without_serviceable_output_stays_rebuilding_supply -- --nocapture`、`cargo test -p xbxengine recent_non_idr_codec_evidence_keeps_owner_in_rebuilding_supply -- --nocapture`、`cargo test -p xbxengine post_first_present_bootstrap_missing_sps_still_enters_rebuilding_supply -- --nocapture`、`cargo test -p xbxengine deferred_transport_await_episode_does_not_keep_keyframe_family_in_flight -- --nocapture`、`cargo test -p xbxengine stale_transport_await_replay_is_absorbed_after_terminal_deferred_invalid_response -- --nocapture`、`cargo test -p xbxengine stale_transport_await_does_not_replay_during_steady_progress -- --nocapture`、`cargo test -p xbxengine recovery_integration_steady_serving_ignores_stale_transport_await_diagnosis -- --nocapture`、`cargo test -p xbxengine clean_anchor_absorbs_stale_transport_await_ingress_waiting_stage -- --nocapture`、`cargo test -p xbxengine lossy_keyframe_defers_to_nack_recovery_admission -- --nocapture`、`cargo test -p xbxengine transport_await_hard_fallback_decoder_reset_budget_exhaustion_upgrades_to_reconnect -- --nocapture`、`cargo test -p xbxengine transport_await_hard_fallback_does_not_treat_ingress_without_output_as_local_progress -- --nocapture`、`cargo test -p xbxengine transport_await_hard_fallback_keeps_connected_ingress_local_when_decoder_reset_path_exhausted -- --nocapture`、`cargo test -p xbxengine transport_await_reconnecting_stage_stays_in_decoder_reset_after_hard_fallback_timeout -- --nocapture`、`cargo test -p xbxengine invalid_transport_await_response_releases_decoder_reset_family_gate -- --nocapture`、`cargo test -p xbxengine request_decoder_reset_command_enqueues_local_reset_without_connection_support -- --nocapture`、`cargo test -p xbxengine invalid_transport_await_keyframe_response_releases_decoder_reset_inflight -- --nocapture`、`cargo test -p xbxengine stale_transport_await_decoder_reset_without_progress_can_reopen_decoder_reset -- --nocapture`、`cargo test -p xbxengine transport_await_hard_fallback_does_not_treat_nonidr_packet_seen_as_local_decode_progress -- --nocapture`、`cargo test -p xbxengine transport_await_invalid_nonidr_inspection_after_reset_does_not_coalesce_stale_decoder_reset -- --nocapture`、`cargo test -p xbxengine transport_await_invalid_nonidr_response_releases_reset_and_decode_wait_lanes -- --nocapture`。 |
| 2026-04-10-recovery-sustaining-phase-refactor | 将媒体恢复从“等到 keyframe”重构为“恢复链先保活、稳定后再回稳态”，引入正式恢复保活阶段替代旧短窗补丁的主状态机职责 | In Progress | 2026-04-10 |  | 复杂任务。RFC：[`docs/rfcs/2026-04-10-recovery-sustaining-phase-refactor.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-10-recovery-sustaining-phase-refactor.md)。本轮进展：统一恢复硬门 contract（owner/coordinator/session），clean-break 旧标签 `bootstrapInFlight -> recoverySustaining`，新增 `recovery_phase/recovery_exit_gate/ingress_waiting/transport_await_unresolved` 观测字段；在 [`scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 落地“冷启动仍需 clean bootstrap、恢复期允许 committed SPS/PPS + stable continuation 退锁”的 ingress 真退锁逻辑；并继续收口 `sustaining-recovery` 语义：[`contract.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/contract.rs) 不再把 `sustaining-recovery / recoverySustaining` 当作 unresolved transport-await，[`video_scheduling_owner.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs) 新增“恢复保活证据 -> ServingReady”以尽快退出 `RebuildingSupply`，[`coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs) 则把 `recoverySustaining` 保持在 `BootstrapInFlight/WaitForBurst` 本地等待域；底层主闸门已改为正式建链语义：[`timeline.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/timeline.rs) 已将 `SustainingRecovery` 从 `waiting_for_recovery_keyframe` 语义中剥离，[`source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/source.rs) 新增 sustaining continuation admission，且已彻底删除 `SOFT_REENTRY_BUDGET` / `capacity=3` 主判定语义；[`video_scheduling_owner.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs) 现已兼容 `frame-inspection-rejected-trigger-recovery-anchor`，[`source.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/source.test.rs) 也补了“sustaining 失败 -> 退出建链并重新请求关键帧”的 source 级回归，确保建链阶段失败时能重新落回请求关键帧动作而不是卡死。验证：`cargo fmt --all` 已通过；目标 `cargo test -p xbxengine ...` 仍受环境缺少 `cmake` / `pkg-config` 阻塞，失败于 `audiopus_sys` / `ffmpeg-sys-next` 构建。 |
| 2026-04-10-post-anchor-transport-gap-supply-promotion | 在 `clean anchor` 后短窗内提升 transport gap 的价值判定，避免 post-anchor 缺包一律继承 `delta/disposable` 导致 `retry_budget=0` | In Progress | 2026-04-10 |  | 复杂任务。RFC：[`docs/rfcs/2026-04-10-post-anchor-transport-gap-supply-promotion.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-10-post-anchor-transport-gap-supply-promotion.md)。已实现：cloud + current recovery epoch clean-anchor + recent present/decode 的 320ms 短窗内，将 transport gap 从 plain predicted 提升为 `refresh_boost` predicted，复用现有 `Supply` 预算，并补 3 条单测。验证：`cargo fmt --all` 已通过；`cargo test -p xbxengine transport::rtc::stream::video_source::nack -- --nocapture` 受环境缺少 `pkg-config` / `cmake` 阻塞。 |
| 2026-04-09-frontend-huge-files-split-1 | 前端巨型文件止血拆分（串流/设置）：把串流 overlay→手柄路由切换从 `Stream.vue` 下沉到 composable；把 `useStreamExecution.ts` 的 view-state reducer 拆出；把 `Setting.vue` 的 gamepad 诊断区拆成独立组件 | In Progress | 2026-04-09 |  | 小任务。范围：[`src/pages/Stream.vue`](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/Stream.vue)、[`src/pages/Setting.vue`](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/Setting.vue)、新增 [`src/pages/stream/useGamepadRouteForStreamOverlay.ts`](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/stream/useGamepadRouteForStreamOverlay.ts)、新增 [`src/streaming/execution/view-state.ts`](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/execution/view-state.ts)、新增 [`src/pages/settings/SettingGamepadSection.vue`](/Users/guo.xu/Documents/code/games/xbxrc/src/pages/settings/SettingGamepadSection.vue)。验证：通过 eslint/类型检查诊断（本轮未跑端到端）。 |
| 2026-04-09-runtime-media-recovery-refactor-implementation | 开始落地 RFC [`docs/rfcs/runtime-media-recovery-refactor.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/runtime-media-recovery-refactor.md) 的阶段 1/2：收口本地 decoder 维护执行链，并将“早判坏/早放弃”策略前移到媒体链 | In Progress | 2026-04-09 |  | 复杂任务，沿用现有 RFC 跟踪。本轮已完成：1) [`scheduling.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/scheduling.rs) 不再把 `RequestDecoderReset` 走 transport 主路径，新增 `SessionCommand::LocalDecoderReset` 承载本地维护动作；2) [`session/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/actor.rs)、[`policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs)、[`transport_session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs) 已切到 transport / local maintenance 分轨执行；3) 组合动作 `RequestKeyframeAndDecoderReset`、`StartupLowQualityRetry` 已从 recovery action 主线移除；4) [`media/video/ingress/scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 增加 backlog 过期帧主动清理；5) [`media/video/decode/video_decode.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/video_decode.rs) 增加 decode 后 stale frame 直接丢弃，减少无时效价值帧继续进入渲染队列。测试同步补到 [`scheduling.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/scheduling.rs)、[`video_decode.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/video_decode.test.rs)、[`scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/ingress/scheduler.rs) 内。验证：`cargo fmt --all`；Rust 测试仍受环境缺少 `cmake` / `pkg-config` 阻塞，`audiopus_sys`、`ffmpeg-sys-next` 无法构建。 |
| 2026-04-09-streaming-dual-window-overlay-architecture-refresh | 重新规划串流双层窗口 canonical 方案：上层 `main` 负责 Web UI/HUD，下层 `native-video-*` 负责纯视频流展示；仅在播放稳定后主窗透明；视频窗支持保比例/铺满等显示模式；视频窗无交互；双窗外观高度融合 | In Progress | 2026-04-09 |  | 复杂任务。RFC：[`docs/rfcs/2026-04-09-streaming-dual-window-overlay-architecture-refresh.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-09-streaming-dual-window-overlay-architecture-refresh.md)。本轮先完成方案重规划与 owner/状态机收口：1) 明确 `native_video` 是双层窗口唯一编排 owner；2) 固定 `Idle -> PreparingWindow -> AwaitingFirstPresent -> OverlayActive -> Streaming -> Stopping/Failed` 状态机；3) 明确只以宿主 stable present 触发主窗透明，禁止按进入页面或普通 `frameReady` 提前透明；4) 固定视频窗非交互和首版 `contain / cover` 显示模式合同；5) 后续实现需同步覆盖位置/尺寸/全屏、黑底基底、圆角/阴影/背景等视觉融合与失败回退。 |
| 2026-04-06-win-mac-h264-ffmpeg-video-backend-unification | 引入 FFmpeg 统一替换 Win/mac H.264 软解与平台硬解入口，保留现有 recovery / stats / present 主权 | In Progress | 2026-04-06 |  | 已建立 RFC [`docs/rfcs/2026-04-06-win-mac-h264-ffmpeg-video-backend-unification.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-06-win-mac-h264-ffmpeg-video-backend-unification.md)。当前范围限定为 `Win/mac + H.264`，不覆盖 Linux/H.265/AV1/HDR；设计重点参考 Moonlight 的统一 FFmpeg 主控、候选链路与 probe 验活、失败后重选 decoder/backend、自愈 reset，以及“应用自带 FFmpeg runtime、不依赖宿主 FFmpeg 环境”的分发策略。需求基线现已上调为强约束：1) 默认必须硬解优先于软解；2) 默认必须落实 0 拷贝主线。现状上，第 1 点基本满足：Windows 候选链已是 `ffmpeg-d3d11va -> ffmpeg-software -> noop`，macOS 候选链已收敛为 `ffmpeg-videotoolbox -> ffmpeg-software -> noop`，旧手写 `videotoolbox-legacy` 已从编译与候选链中移除；同时旧 `XbxVideoDecoderBackend::reset()` trait 合同也已删除，decoder reset 语义现统一收敛为“reset 语义、probe factory 重建 backend 实现”。本轮已把 Windows 从“默认 copyback”推进到“decode descriptor 主线 + Vulkan external-memory presenter 主线”：`ffmpeg-d3d11va` backend 默认先尝试导出 shared handle descriptor，`native_video` 新增 `WindowsWgpuPresenter`，`wgpu_renderer` 新增 `texture_from_d3d11_shared_handle()` 导入路径并直接消费 `NV12` plane 视图。剩余主任务已收敛到 Windows 目标编译、驱动/feature 命中验证、实机串流验证，以及 FFmpeg runtime 打包/发布闭环。 |
| 2026-04-06-startup-recovery-unified-lifecycle | 统一 startup / recovery 顶层生命周期语义，收口 Rust 主权状态并让前端只消费单一语义快照 | In Progress | 2026-04-06 |  | 已建立 RFC [`docs/rfcs/2026-04-06-startup-recovery-unified-lifecycle.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-06-startup-recovery-unified-lifecycle.md)。当前已完成：1) Rust diagnostics 投影 `stream_lifecycle_phase`，旧 `session_phase` 合同保持不变；2) trace projection / runtime snapshot / diagnostics 改为统一生命周期优先；3) i18n 与前端类型合同已补齐；4) 目标 trace 已复核，确认 41.3s 启动长窗口本质属于 `startup`，媒体已 first-present 但 ledger 仍 `recovering` 的窗口本质属于收口滞后而非持续同级恢复；5) `crates/xbox-streaming/src/session/flow.rs` 已在 `RuntimeStarting(ReadyToConnect)` 直接放行 startup wait，不再继续等 `Provisioned/sessionReady`；6) `crates/xbxengine/core/src/api/runtime/lifecycle.rs` 已把 `record_media_ready()` 前移到 `apply_remote_description()` 成功后，首屏 surface/video ready 不再被完整 ICE 交换串行阻塞；7) 已补齐 `sessionExecutionStarted -> Binding` 空窗的跨层分段埋点，覆盖前端 `runtime-host/xbxengine-runtime`、Rust `shell rpc`、`xbxengine service`、`runtime_state apply_control/runtime lock`，下一份 trace 可直接区分 UI 延迟、RPC 入口延迟、dispatch 排队、runtime 锁争用；8) 基于 [`runtime-trace-1775452872224-1.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1775452872224-1.jsonl) 的“execution 即可启动 runtime”假设，在下一份回归 [`runtime-trace-1775453736234-1.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1775453736234-1.jsonl) 中已被证伪：前端提前约 0.5s 触发 `StartRuntime/exchangeOffer` 后，服务端直接返回 `SessionNotConnectable`，明确要求 session 先进入 provisioned/connectable 状态；9) 已据此完成顶层修正：新增后端显式 `runtimeLaunchState` 合同，由 `crates/xbox-streaming/src/session/flow.rs` 统一输出 `blocked/ready/closed/failed`，并经 `src-tauri` RPC 层透传到前端与 trace；10) [`src/streaming/useStreamExecution.ts`](/Users/guo.xu/Documents/code/games/xbxrc/src/streaming/useStreamExecution.ts) 现只消费后端 `runtimeLaunchState === 'ready'` 作为 runtime 启动门槛，前端不再从 `phase` 或 `execution` 推断启动时机；11) 问题 2 的旧语义滞留在前一份 trace 中仍已明显改善，当前后段 recovering 更偏向真实恢复故障而非状态显示滞后；12) 基于 [`runtime-trace-1775454893142-1.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1775454893142-1.jsonl) 已进一步完成 recovery 顶层修正：`video_scheduling_owner` 现显式消费 `latest_h264_inspection_observation`，把 `RebuildingSupply + recent NonIdrVcl` 视为真实 `await-recovery-keyframe` 阻塞，避免 repaired/healthy 伪候选误导收口；`session/policy` 现会把 `RebuildingSupply + Anchor` 的被动 recovery surface 继续送入 coordinator family hold，而不再因 owner 本地 `emit=false` 直接写成 `no-signal`，从而恢复 stage upgrade / hard fallback 的连续性；13) 基于 [`runtime-trace-1775455942676-1.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1775455942676-1.jsonl) 已完成第二层 recovery 顶层修正：`recovery/escalation` 与 `transport_session` 的 keyframe in-flight 语义统一切到“只有 `sent_at_ms` 已落账才算真正在飞”；`requested but unsent` 现在会在 coordinator 下一拍回滚 provisional keyframe budget、释放内部 `keyframe_epoch`，transport family gate 也不再把它当成 active episode，从而避免 `coalesced:keyframeInFlight` 长尾自锁；14) 已继续完成观测闭环收口：`session/policy` 现将 `latest_recovery_decision_ledger` 语义改为“pending actionable decision 优先”，新的 `no-signal/none` 仍会完整写入 `recent_recovery_decision_ledgers`，但不会在命令结果落账前覆盖 latest，避免 trace/UI 顶层把已发生的 `requestKeyframe/requestDecoderReset` 误显示成“根本没推进”。新增验证已通过 `cargo fmt --all`、`cargo test -p xbxengine recovery_decision_ledger_keeps_pending_action_latest_while_recent_history_records_no_signal -- --nocapture`、`cargo test -p xbxengine recovery_decision_ledger_allows_no_signal_to_be_latest_after_pending_command_is_resolved -- --nocapture`、`cargo test -p xbxengine command_result_updates_matching_recovery_decision_ledger -- --nocapture`、`cargo test -p xbxengine command_result_updates_historical_ledger_when_latest_has_rotated -- --nocapture`、`cargo check -p xbxengine`。剩余待补：用下一份真实 trace 同时验证 `coalesced:keyframeInFlight` 长尾是否已被打断，以及 latest ledger 顶层是否已稳定保留真实 pending recovery action。 |
| 2026-04-05-xbxengine-mainline-pollution-isolation | 系统性隔离 xbxengine 主流程污染，拆分视频 present 主链与 haptics / trace / 宿主副作用 / 恢复控制面的耦合 | In Progress | 2026-04-05 |  | 已建立 RFC [`docs/rfcs/2026-04-05-xbxengine-mainline-pollution-isolation.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-05-xbxengine-mainline-pollution-isolation.md)。当前已完成九批关键收口：第一批包括 [`src-tauri/src/mods/xbxengine/runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/runtime_state.rs) 锁外采样 `native_video` host feedback、[`src-tauri/src/mods/runtime_trace/service.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/runtime_trace/service.rs) 异步 writer、[`src-tauri/src/mods/native_video/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/native_video/mod.rs) 高频 `hostTiming` 采样；第二批已把 rumble 从 runtime 主链彻底剥离：[`crates/xbxengine/core/src/api/runtime/lifecycle.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/lifecycle.rs) 改成只提交 rumble 意图、[`src-tauri/src/mods/xbxengine/rumble_worker.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/rumble_worker.rs) 新增独立 worker、[`src-tauri/src/mods/xbxengine/trace_projection.rs`](/Users/guo.xu/Documents/code/games/xbxrc/src-tauri/src/mods/xbxengine/trace_projection.rs) 加入语义门控与采样；第三批已将 recovery 统一仲裁改造成按 family/in-flight/upgrade 调度；第四批直接修复恢复执行闭环失真：[`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 引入 `recent_recovery_decision_ledgers` ring buffer、[`crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs) 改为按 `decision_id` 回写历史 ledger 命令结果、[`crates/xbxengine/core/src/runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 为 keyframe episode 增加 recent 轨迹并把 `requested` 记账移到 family gate 之后；第五批针对启动阶段误升级恢复：[`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 新增 pre-first-frame startup hold，在 `Connected + priming/startup/handshaking + remoteTrackAttached + 尚无 decode/present 首帧反馈` 时吸收 `transportAwaitRecoveryKeyframe / ingressWaitKeyframe`；第六批继续收紧 `VideoSchedulingOwner` 的恢复收口，在 [`crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs) 将 `ServingReady` 从 `RebuildingSupply` 扩展到 `SupplyStarved`，允许恢复主链先退出到 `DegradedServing`；第七批则把 session 侧结案语义和首帧门控补齐：`DegradedServing` 不再直接记作恢复完成，并将 `displaySupplyDegraded` 与 intent 分支上的 `adapterIdleTimeout` 一并纳入首帧门控，避免“首帧未到就建 recovery epoch”和“弱退出被误结案”；第八批继续收口 `recovering -> ramp-up -> stable` 生命周期：[`crates/xbxengine/core/src/runtime_stats_sink.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/runtime_stats_sink.rs) 不再在 `cleanAnchor` 时提前关闭 recovery episode，而是改到 `stableServingSettled` 再结案；[`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 把 `Connected + stable-serving + current clean anchor + active episode` 显式记为 `ramp-up`，只在健康输出持续一段窗口后才转 `stable` 并关 episode；[`crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs) 与 [`crates/xbxengine/core/src/transport/rtc/recovery/escalation.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/escalation.rs) 则把“clean anchor 轻确认”和“stable recovery 最终释放”拆开，避免恢复刚回来就立刻重置动作抑制；[`crates/xbxengine/core/src/transport/rtc/recovery/runtime_state.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/runtime_state.rs) 同步把 `recovery_stage` 暴露为 `ramp-up`；第九批则针对同一 unresolved transport-await debt 长窗反复重立案的问题，在 [`crates/xbxengine/core/src/transport/rtc/recovery/repeat_suppression.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/repeat_suppression.rs) 改成基于“新鲜 unresolved timeline/anchor 证据 + 尚无客观恢复成功”的 family-level coalescing，不再依赖 episode 标志短窗抑制，从而让同一坏链在 `requestKeyframe/requestDecoderReset` 已 in-flight 时持续复用同一 family，只有 clean anchor + healthy chain + 新鲜 decode/present 输出或新 recovery epoch 时才重开。验证已通过：`cargo test -p xbxengine clean_anchor_keeps_transport_recovery_episode_open_until_stable_settle -- --nocapture`、`cargo test -p xbxengine clean_anchor_acknowledgement_resets_transport_await_streak -- --nocapture`、`cargo test -p xbxengine recovery_integration_ramp_up_absorbs_display_idle_and_short_transport_await_before_stable -- --nocapture`、`cargo test -p xbxengine recovery_integration_same_unresolved_gap_transport_await_reuses_in_flight_family -- --nocapture`、`cargo test -p xbxengine recovery_integration_transport_await_reopens_after_clean_anchor_and_new_recovery_epoch -- --nocapture`、`cargo test -p xbxengine recovery_integration_stale_transport_await_after_completion_evidence_stays_no_signal -- --nocapture`、`cargo check -p xbxengine`；`cargo test -p xbxengine transport::rtc::session::policy -- --nocapture` 已补跑，大部分用例已通过，结束前停在既有慢测上未拿到完整收尾输出。 |
| 2026-04-05-recovery-scheduling-integration-matrix | 重新设计恢复调度的集成测试矩阵，统一验证本地缺口吸收、恢复出口与真正网络故障升级链 | In Progress | 2026-04-05 |  | 已新增 RFC [`docs/rfcs/2026-04-05-recovery-scheduling-integration-matrix.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-05-recovery-scheduling-integration-matrix.md)。目前已完成现状盘点、矩阵设计，并在 [`session/policy.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.test.rs) 落第一版 `RecoveryIntegrationHarness`，先把 `owner -> recovery coordinator -> session policy` 串成统一场景入口；第一批已覆盖 `transportAwaitRecoveryAnchor` 恢复出口、本地 `displaySupplyCritical` 只停留在局部恢复、`stale adapterIdleTimeout` 被 steady 进展吸收但真实 transport 断链仍升级 reconnect，以及新增的 `recovery_integration_local_ingress_drop_stays_no_signal_under_healthy_baseline`：在 healthy timeline/track baseline 下，即便 runtime stats 已出现 `latest_video_frame_drop(reason=localBackpressureRepairOverflow)`，当前 `session policy` 仍必须保持 `no-signal`、不得产出 recovery/reconnect command。本轮继续补组合场景 `recovery_integration_local_ingress_and_stale_idle_stay_no_signal_under_healthy_baseline`，明确“healthy baseline + local ingress provenance + stale adapterIdleTimeout diagnosis 共存”时，policy 仍保持 `input_signal=none` / `no-signal`，并显式断言 `latest_recovery_decision_ledger.input_signal=none`，不得误升级 reconnect。当前又把 runtime gate 纳入同一矩阵骨架：[`api/runtime/mod.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/mod.test.rs) 新增 `PendingReconnectCandidateMatrixCase` 夹具，并补两组成对场景，明确 `localBackpressureDeltaGap/displaySupplyCritical` 在 `Local` 域必须被 runtime gate 拒绝，而 `peer-closed/livenessNoProgressTimeout` 在 `ConnectivityTransport` 域必须被放行；后续继续补齐 `transportAwaitRecoveryAnchor`、`transportSevereDeadline`、`rtcConnectionRecovering` 的跨拍污染矩阵，确认上一拍 `Local` candidate 被拒绝后，不会污染下一拍真正的 connectivity reconnect。与此同时在 [`stream/video_source/source.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/source.test.rs) 继续锁住 repair/RTX 的源头合同：命中 bootstrap 缺口的 repair 包完成本地组装后不得额外冒出 transport 观测，未命中缺口的单包和 burst 型 RTX reinject 都只保留 `adapterResolveMiss` provenance，不得直接抬成 `AwaitRecoveryKeyframe`；本轮进一步把真实 `sink -> source -> session` 驱动上提成 [`video_source/test_fixtures.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/test_fixtures.rs) 里的 `LocalIngressReplayFixture`，并继续收口为 `LocalIngressReplayProfile` 驱动。新增的生产修复已切掉三条恢复风暴放大链：[`recovery/coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs) 现在会在 `clean anchor`、`recovery epoch` 轮转、stall evidence 清空和非 `transportAwait` reason 下真正复位 `transportAwait` hard-fallback 内部计时，并禁止 `Connected + ingress progress` 的 `transportAwait` 直接升 reconnect；[`session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 则把 runtime reconnect domain 从静态 `reason` 映射改成按 proposal 语义判定，确保 `transportAwaitRecoveryAnchor` 留在 `Local`，而 `transportExpiredDeadline` 仍保留 `ConnectivityTransport`。本轮继续切掉一条新暴露的边界缺口：即使没有 clean anchor，只要 owner 当前拍已经回到 `stable-serving` 且 timeline/track/media output 健康，fresh `transportAwaitRecoveryAnchor` 也不得再通过 fallback diagnosis 抢回 recovery proposal；对应在 [`session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 收紧 `should_absorb_stale_transport_await_replay(...)`，新增“owner healthy 优先吸收”分支，同时保留旧的 `stale + clean anchor` 吸收路径不变。现在又补齐了 mixed-signal 剩余矩阵：[`session/policy.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.test.rs) 新增 `recovery_integration_repeated_transport_expired_deadline_upgrades_to_connectivity_reconnect`、`recovery_integration_transport_sample_loss_overrides_same_tick_local_display_recovery`、`recovery_integration_transport_sample_loss_overrides_same_tick_local_transport_await`、`recovery_integration_transport_recovered_late_overrides_same_tick_local_display_recovery`、`recovery_integration_transport_recovered_late_overrides_same_tick_local_transport_await`，确认 repeated `transportExpiredDeadline` 会升级成 `ConnectivityTransport` reconnect，且 `transportSampleLoss/transportRecoveredLate` 在同拍中也优先于本地 `displaySupplyCritical/transportAwaitRecoveryKeyframe`。runtime gate 侧新增 [`runtime_accepts_transport_severe_candidate_after_local_display_candidate_was_rejected`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/mod.test.rs)、[`runtime_accepts_recovering_candidate_after_local_transport_await_candidate_was_rejected`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/mod.test.rs)，锁住 Local rejected 不会污染下一拍真实 connectivity candidate；全链 replay 侧新增 [`local_repair_noise_does_not_block_following_repeated_transport_severe_deadline_reconnect`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/sink.test.rs)，确认真实 `sink -> source` 本地 repair overflow 噪声不会挡住后续 repeated severe deadline 升级 reconnect。此次又沿 budget/cooldown 边界补了 [`transport_severe_deadline_requires_fresh_second_hit_before_reconnect`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.test.rs)、[`transport_expired_deadline_window_resets_after_large_gap`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.test.rs)、[`reconnect_budget_is_released_after_new_epoch_for_transport_severe_deadline`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.test.rs)、[`transport_recovered_late_does_not_inherit_severe_deadline_reconnect_counter`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.test.rs)。其中 `transport_severe_deadline_requires_fresh_second_hit_before_reconnect` 首次打出真实缺口，并据此在 [`recovery/escalation.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/escalation.rs) 修复 `TransportSevereDeadline` 的陈旧计数继承问题：超过 `severe_deadline_reconnect_window` 的旧 severe 信号现在会先清空 `reconnect_candidate_signals`，避免很久以前的一次 severe 让后续孤立 severe 直接 reconnect；其余两条边界测试当前保持绿灯，说明 reconnect budget 的 epoch 释放与 `recoveredLate` 对 `severe` reconnect 计数的隔离目前没有新的显性结构问题。当前又把 replay fixture 进一步抽成“阶段化场景骨架”：[`video_source/test_fixtures.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/test_fixtures.rs) 新增 `build_connected_snapshot()`、`mark_transport_connectivity_degraded()`、`mark_transport_recovered()` 三个公共入口，配合 [`sink.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/sink.test.rs) 新增 4 条长序列变体 `multi_stage_replay_steady_local_noise_severe_then_recover_stays_stable`、`multi_stage_replay_steady_local_noise_expired_then_recover_stays_stable`、`multi_stage_replay_steady_local_noise_sample_loss_then_recover_stays_local`、`multi_stage_replay_steady_local_noise_recovered_late_then_recover_stays_local`，把 `steady -> local noise -> connectivity reason -> recover` 这一段从单一 severe 扩展到真实多 reason 节拍。其中一个关键结论已被固定进测试：`transportSevereDeadline` 是 fresh second-hit 升 reconnect，而 `transportExpiredDeadline` 在当前策略里是跨窗口 third-hit 才升 reconnect，不能再拿 severe 的两拍节奏去套 expired。本轮又进一步把“更真实的场景设计”写进 RFC：按 `Home/Cloud` 两类链路画像、`Steady/CloudStartup/CloudHighRtt/DisplayConstrained` 等动态子画像、`JitterBurst/ReorderBurst/LossBurst/FeedbackDelay/IntermittentBlackhole/HardDisconnect` 等网络扰动模型，整理出可直接映射 replay fixture 的扩展矩阵与 8 个高优先级落地场景，作为下一批真实回放用例的设计基线。现在第一批画像场景已经开始落地：[`session/policy.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.test.rs) 新增 `recovery_integration_home_clean_anchor_short_jitter_keeps_steady_serving`、`recovery_integration_home_render_deadline_jitter_stays_local_display_path`、`cloud_startup_transport_progress_does_not_reconnect_before_first_frame`、`recovery_integration_cloud_reconnect_then_clean_recovery_exit_does_not_reenter_storm`，分别锁住 `HOME-S1/HOME-S3/CLOUD-S1/CLOUD-S6` 四条合同。第二批又补齐 `HOME-S4/CLOUD-S2/CLOUD-S4/CLOUD-S5`：`recovery_integration_home_short_idle_blackhole_is_absorbed_until_progress_returns`、`recovery_integration_cloud_media_loss_prefers_transport_await_before_reconnect`、`cloud_high_rtt_repeated_transport_severe_deadline_second_hit_reconnects`、`cloud_high_rtt_repeated_transport_expired_deadline_third_hit_reconnects` 已全部通过；同时在 [`session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 修正 `Recovering + rtcConnectionRecovering` 的优先级/标签边界，确保真实连接恢复证据出现时，不会继续被残留 `transportAwaitRecoveryAnchor` 本地恢复面永久遮挡，而 `expiredDeadline` third-hit 用例也改成真实时间窗驱动。本轮又把 `CLOUD-S6` 先上提成一条更完整的闭环：[`api/runtime/mod.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/mod.test.rs) 新增 `runtime_cloud_recovery_replay_accepts_transport_reconnect_after_local_noise_rejection_and_exits_cleanly`，复用 [`video_source/test_fixtures.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/test_fixtures.rs) 的真实 `sink -> source` local noise 输入，再串到 `session -> transport_session -> runtime`，验证本地 Local candidate 会先被 gate 拒绝，随后真实 `rtcConnectionRecovering` transport reconnect 会被接受，恢复后不再重复 reconnect。为此在 [`stack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack.rs) 增加了最小测试可见桥接重导出，避免在 runtime test 里复制 transport_session 逻辑。随后又补上两条剩余 runtime 画像边界：`runtime_home_render_deadline_jitter_replay_stays_local_and_never_reaches_reconnect` 锁住 Home render/deadline 抖动只允许停留在本地 `displaySupplyCritical -> requestKeyframe` 路径，`runtime_cloud_startup_transport_progress_replay_does_not_reconnect_before_first_frame` 锁住 Cloud 首帧前即使 transport bytes/packets 持续前进也只能保持 `no-signal`、不得透传 reconnect，更不得残留 pending reconnect action。现在再补上两条“8 个主样板之外”的高收益补充：[`source.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/source.test.rs) 新增 `repair_reorder_gap_closure_stays_local_and_records_resolved_gap_match`，把 `HOME-S2` 的 repair/reorder 补齐留在 local 域合同锁回归；[`session/policy.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.test.rs) 新增 `cloud_high_rtt_sample_loss_then_recovered_late_stays_local_until_severe_deadline_reconnect`，把 `CLOUD-S3` 的连续恶化节拍锁成“`sampleLoss -> recoveredLate` 先 local recover，直到后续 `severeDeadline` second-hit 才升级 reconnect”。现在又把 `HOME-S5/CLOUD-S7` 这两条硬断链上界补齐：[`session/policy.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.test.rs) 新增 `recovery_integration_home_hard_disconnect_emits_connectivity_reconnect_without_local_absorption` 与 `cloud_hard_disconnect_reconnect_budget_exhaustion_enters_failed_terminal_without_spinning`，分别锁住 Home 真断链不得被本地吸收吞掉、Cloud 真断链在长窗口后允许 reconnect 且最终进入 `failed-terminal` 不再自旋；[`api/runtime/mod.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/api/runtime/mod.test.rs) 新增 `runtime_home_hard_disconnect_candidate_reaches_reconnect_restart` 与 `runtime_cloud_hard_disconnect_keepalive_session_not_active_stops_cleanly`，把这两条再串到 runtime 侧，分别确认 Home reconnect 真正落成 restart，以及 Cloud 在 keepalive 返回 `HTTP 410 SessionNotActive` 时必须干净停机。最新又沿这次 `connected-but-unrecoverable` 事故链补了 4 条更尖锐的组合行为测试：[`session/policy.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.test.rs) 新增 `recovery_integration_home_connected_ingress_without_output_progress_reenters_local_transport_recovery`、`recovery_integration_cloud_stale_transport_await_replay_reenters_local_recovery_when_output_stalls`、`recovery_integration_fresh_transport_await_absorption_expires_once_output_stalls`，锁住“短 idle/旧 replay/fresh absorb”都不能把真实 `transportAwaitRecoveryKeyframe + ingress-only + output stalled` 漂白成 `no-signal`，必须重新进入本地恢复链；[`recovery/coordinator.test.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.test.rs) 新增 `transport_await_hard_fallback_upgrades_to_reconnect_after_decode_progress_turns_stale`，确认同一 recovery episode 内先因 fresh decode 留在 `CooldownSuppressed` 后，一旦 decode 也陈旧，必须升级 `RequestReconnectCandidate`。本批验证：`cargo test -p xbxengine recovery_integration_home_connected_ingress_without_output_progress_reenters_local_transport_recovery -- --nocapture`、`cargo test -p xbxengine recovery_integration_cloud_stale_transport_await_replay_reenters_local_recovery_when_output_stalls -- --nocapture`、`cargo test -p xbxengine recovery_integration_fresh_transport_await_absorption_expires_once_output_stalls -- --nocapture`、`cargo test -p xbxengine transport_await_hard_fallback_upgrades_to_reconnect_after_decode_progress_turns_stale -- --nocapture`。下一步若继续补，就该转去 Home/Cloud 的边界组合变体，而不是主画像缺口。 |
| 2026-04-04-h264-preset-profile-semantics-closure | 收口 H264 codec preset 与实际 profile family 的歧义，统一 Rust-owned/browser 侧的 H264 family 排序与协商语义 | In Progress | 2026-04-04 |  | 已新增 RFC [`docs/rfcs/2026-04-04-h264-preset-profile-semantics-closure.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-04-h264-preset-profile-semantics-closure.md)，当前已定位到三处语义分叉：[`crates/xbox-streaming/src/policy/negotiation/compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/negotiation/compiler.rs) 将 `H264High` 编译成 `4d`，[`crates/xbxengine/core/src/transport/rtc/sdp/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/sdp/policy.rs) 的 H264 family 排序把 `4d` 放在 `64` 之前，而浏览器直出 [`src/player/infra/webrtc/SdpManipulator.ts`](/Users/guo.xu/Documents/code/games/xbxrc/src/player/infra/webrtc/SdpManipulator.ts) 则使用 `64 > 4d > 42e > 420`。本轮会把 preset 语义收口为单一 canonical 映射，并补齐 Rust 侧回归测试。 |
| 2026-04-04-host-tick-release-gating | 让 host tick / 最近 host present cadence 直接参与 pacer release eligibility，而不是只作为 host age budget 与诊断输入 | In Progress | 2026-04-04 |  | 已新增 RFC [`docs/rfcs/2026-04-04-host-tick-release-gating.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-04-host-tick-release-gating.md)，并完成第一轮落地：[`crates/xbxengine/core/src/media/video/render/pacer.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/render/pacer.rs) 已给 `FramePacingPolicy` 增加 host release gate；[`crates/xbxengine/core/src/media/video/pacer/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/pacer/actor.rs) 现用 `latest_video_host_present_time_ms + host_display_interval_ms` 计算下一次允许 release 的窗口，并在 host cadence 失活时自动退化，不锁死 pacer。验证：`cargo fmt --all`、`cargo test -p xbxengine media::video::render::pacer -- --nocapture`、`cargo test -p xbxengine media::video::pacer -- --nocapture`、`cargo check -p xbxengine`。 |
| 2026-04-04-decode-pull-driven-session-loop | 将 decode 推进从 tick/mailbox 改为 pull-driven 状态机，给 ingress -> decode handoff 建立显式 demand contract，并移除 session loop 固定 decode tick | In Progress | 2026-04-04 |  | 已新增 RFC [`docs/rfcs/2026-04-04-decode-pull-driven-session-loop.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-04-decode-pull-driven-session-loop.md)，并完成首轮落地：[`crates/xbxengine/core/src/media/video/decode/actor.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/actor.rs)、[`crates/xbxengine/core/src/media/video/decode/video_decode.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/media/video/decode/video_decode.rs)、[`crates/xbxengine/core/src/transport/rtc/pipeline/ingress.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/pipeline/ingress.rs)、[`crates/xbxengine/core/src/transport/rtc/pipeline/session_loop.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/pipeline/session_loop.rs) 已将 decode handoff 改为 demand 驱动，`session_loop` 不再保留 4ms `decode_drain_tick`。验证：`cargo fmt --all`、`cargo test -p xbxengine media::video::decode -- --nocapture`、`cargo test -p xbxengine transport::rtc::pipeline -- --nocapture`、`cargo check -p xbxengine`。 |
| 2026-04-03-frame-budget-trace-regression | 让当前帧预算模型在 trace 中可回归：把 frame budget 结构化投影到 recovery / nack / frame drop 观测和 observability snapshot | In Progress | 2026-04-03 |  | 继承 [`docs/rfcs/2026-04-03-moonlight-frame-value-model-convergence.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-04-03-moonlight-frame-value-model-convergence.md) 的帧模型，补 trace 可回放锚点。2026-04-03 已完成 transport/NACK 侧预算事实透传：`FrameBudgetContext` 已贯通到 NACK batch/resolved/skipped/expired 与 frame recovery ledger，并在 assembled frame 处优先消费 ledger budget（涉及 [`crates/xbxengine/core/src/transport/rtc/stream/nack_scheduler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/nack_scheduler.rs)、[`crates/xbxengine/core/src/transport/rtc/stream/video_source/nack.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/nack.rs)、[`crates/xbxengine/core/src/transport/rtc/stream/video_source/mod.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/mod.rs)、[`crates/xbxengine/core/src/transport/rtc/stream/video_source/source.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/source.rs)、[`crates/xbxengine/core/src/transport/rtc/stream/video_source/timeline.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stream/video_source/timeline.rs)）；验证：`cargo test -p xbxengine transport::rtc::stream::nack_scheduler -- --nocapture`、`cargo test -p xbxengine transport::rtc::stream::video_source::timeline -- --nocapture`、`cargo check -p xbxengine`。 |
| 2026-03-30-cloud-recovery-liveness-simplification | Cloud 恢复调度活性收口（单一状态机 + 单一升级链 + 决策账本） | In Progress | 2026-03-30 |  | 顶层目标：彻底消除 `Recovering -> Connecting` 后长期无动作静默区，改为“有预算的恢复 + 明确稳定收口 + 超时硬兜底”。按 RFC 分三阶段推进：Phase 1（决策账本与静默窗口上界观测）、Phase 2（调度裁决点收口为单一状态机）、Phase 3（升级链与预算语义统一并完成网络矩阵回放验收）。当前只按 RFC 契约推进，不再做补丁式门槛调参。2026-03-31：已将首帧前 liveness 统一为 15s 保守上界，并把 `failed-terminal` 解锁改为“仅真实媒体帧前进”以阻断无帧重连风暴；同日补齐 reconnect 执行收口（pending 可更新 + Reconnecting single-flight + 6s 最小重连间隔），降低恢复链自我打断；针对“出画后立刻关闭”新增 `KickForClosedGame` 终态优先处理（直接 stop，不再走 reconnect keepalive -> 410）；新增 `Connected + owner 非 stable` 场景下的呈现活性兜底（10s）：当 `present` 持续陈旧且 `noPending` 高压、同时主视频入流字节持续增长时，强制推进 lifecycle reconnect，避免 `frame_count` 仍增长却长期卡在 `rebuilding-supply/waitKeyframe` 的活锁。2026-04-01：完成恢复语义二次收口，显式分离“连接故障”与“媒体恢复”域（媒体域禁止触发 reconnect，仅允许 keyframe/decoder reset），并移除 recovery decision ledger 的 `proposal=None` 早退抑制，确保每个策略 tick 均有可观测账本记录。验证：`cargo test -p xbxengine transport::rtc::recovery::escalation::tests:: -- --nocapture`、`cargo test -p xbxengine transport::rtc::recovery::coordinator::tests:: -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::policy::tests:: -- --nocapture`、`cargo test -p xbxengine transport::rtc::stack::transport_session::tests:: -- --nocapture`。RFC：[`docs/rfcs/2026-03-30-cloud-recovery-liveness-simplification.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-30-cloud-recovery-liveness-simplification.md)。 |
| 2026-03-28-m1-m6-recovery-scheduling-hardening | M1-M6 改造推进：M1-M4 已完成，M5/M6 继续收口（REMB 重发语义、display supply 统一恢复入口、摘要口径纠偏、阈值 profile 化） | In Progress | 2026-03-28 |  | 当前阶段：M1-M4 ✅，M5/M6 ⏳。本轮继续完成两项结构性收口：1) lifecycle reconnect 已统一并入 `owner signal -> coordinator -> planner -> transport command` 主链，[`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 不再单独持有 lifecycle proposal 语义，[`crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/recovery/coordinator.rs) 的 `propose_lifecycle_reconnect()` 已退化为 `propose_from_owner_signal()` 兼容包装；2) owner 的 anchor issue 判定已从“读取 `recovery.latest_diagnosis_label`”改为“优先读取 timeline raw fact（`chain.state/reason + source_event`）”，[`crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs) 与 [`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 已明确切断 `owner <- recovery diagnosis` 的反向依赖；3) `SchedulingPolicyEngine` 已进一步把 `owner_state/owner_health` 用于 BWE hard gate，`RebuildingSupply/SupplyStarved` 期间不再透传 BWE 更新；4) 既有阻塞已修复：[`crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/policy/video_scheduling_owner.rs) 的测试构造已补齐 `SchedulingDemandSignal::default()`，[`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 的 `unstable_hold_requires_consecutive_confirmation_before_emit` 已补成允许 BWE 的 owner 语义前置；验证：`cargo test -p xbxengine transport::rtc::policy::scheduling -- --nocapture`、`cargo test -p xbxengine transport::rtc::session::policy -- --nocapture` 均通过；5) RFC 已新增“1-6 点收口映射”，明确当前对应关系为：顶层状态机 / 动作 owner / 价值判断 / 健康定义 / 观测摘要契约 / 阶段验收与防腐化。当前剩余风险收敛为两项：A. diagnostics 的 `display phase/stall kind` 仍保留辅助推导，虽不覆盖 canonical owner，但仍需继续退化；B. 仍需用最新 Cloud/Home trace 验证 `rebuilding-supply:transportAwaitRecoveryKeyframe` 时长、pending frame 累积与 keyframe/recovery epoch 频率是否实质下降。RFC：[`docs/rfcs/2026-03-27-timeline-value-runtime-upgrade.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-27-timeline-value-runtime-upgrade.md)。 |
| 2026-03-27-timeline-value-runtime-upgrade | 以 `docs/mode.md` 为目标形态，规划并推进 Xbox 远端视频运行时向“时间线价值优先”完整升级 | In Progress | 2026-03-27 |  | RFC：[`docs/rfcs/2026-03-27-timeline-value-runtime-upgrade.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-27-timeline-value-runtime-upgrade.md)。当前已完成 `video_source` 内统一 `timeline owner` 与 `VideoTimelineObservation` 观测链打通，decode/pacer/render 首轮结构化 `frameDrop` 闭环已落地（`stage/action/detail + frame metadata`），并进一步补齐 `latest-slot -> host present success` ACK 主链（runtime present 成功后回写 render owner，`render_signal_snapshot` 可持续反映最近已确认输出时刻）。近期继续把 timeout 收口到 owner：`timeline` 新增 `ChainState::Stalled` 与 `on_timeout_detected`，在帧恢复时补齐 `Stalled -> Repairing -> Healthy` 状态迁移，不再仅靠 trace 层推导；同时新增 decode/render candidate owner 显式状态机（`Nominal/Backpressure`、`Nominal/LatestOverwrite`）与 `latest_*_candidate_decision`，并通过 actor 在决策变化时写入 `latest_observation_label/summary`（`decodeCandidateState` / `renderCandidateState`），使 runtime stats 可直接观察状态迁移，trace 增加 `decodeCandidateStateTransition`/`renderCandidateStateTransition`。验证：`cargo check -p xbxengine`、`cargo test -p xbxengine acknowledge_keeps_last_present_time_for_snapshot -- --nocapture`、`cargo test -p xbxengine present_frame_reports_overwritten_latest_metadata -- --nocapture`、`cargo test -p xbxengine enqueue_decoded_frame_returns_dropped_oldest_frame -- --nocapture`、`cargo test -p xbxengine transport::rtc::stream::video_source::timeline -- --nocapture`、`cargo test -p xbxengine transport::rtc::stream::nack_scheduler -- --nocapture`、`cargo test -p xbxengine transport::rtc::stream::video_source:: -- --nocapture`、`cargo test -p xbxengine media::video:: -- --nocapture`、`cargo test -p xbxrc trace_projection -- --nocapture --test-threads=1`、`pnpm -s exec tsc --noEmit`。下一阶段重点转向 runtime trace 验收与阈值收敛。 |
| 2026-03-27-cloud-high-rtt-latency-first-recovery | 在 Cloud 高 RTT + NACK 场景落地低延迟优先恢复：NACK budget admission、unrecoverable 帧前置放弃与 wait-keyframe 可观测性 | In Progress | 2026-03-27 |  | 已在 `crates/xbxengine/core/src/transport/rtc/stream/nack_scheduler.rs` 恢复并扩展 `PacketRecoveryDisposition`、`estimated_recovery_arrival_ms`、`frame_playout_deadline_at_ms`、`frame_unrecoverable_reason`，实现 Cloud 高 RTT 下 `SkippedTooLate` admission；`crates/xbxengine/core/src/transport/rtc/stream/video_source/nack.rs` 已把 skipped/expired/recovered 统一投影到结构化 NACK observation，并把 Cloud 高 RTT 下的 `skipped/expired/recoveredLate` 映射到真实帧级恢复账本；`crates/xbxengine/core/src/transport/rtc/stream/video_source/mod.rs`、`crates/xbxengine/core/src/transport/rtc/stream/video_source/source.rs` 现已在真实 `AssembledVideoFrame` 出口写入 `frame_playout_deadline_at_ms`、`frame_recovery_disposition`、`frame_unrecoverable_reason`，不再固定 `Repairing/None`；`crates/xbxengine/core/src/media/video/types.rs`、`crates/xbxengine/core/src/media/video/ingress/scheduler.rs`、`crates/xbxengine/core/src/transport/rtc/pipeline/session_loop.rs` 已引入 `FrameRecoveryDisposition` 与 `DropUnrecoverable`，保证 unrecoverable 帧不会进入 decode，并在参考链损坏时切到 `wait-keyframe`；`crates/xbxengine/core/src/diagnostics/observation_bus.rs`、`crates/xbxengine/core/src/diagnostics/stats.rs`、`crates/xbxengine/protocol/src/runtime.rs`、`src-tauri/src/mods/xbxengine/trace_projection.rs` 已进一步统一 `actualVideoKbps` 口径为“优先 local-feedback TWCC receive bitrate，否则回退 transport inbound video bitrate”，并新增 `frameRecoveryObserved` trace 事件投影真实 `ledgerWrite/ledgerConsume`，同时补上 `observabilitySnapshot.bitrate.actualVideoKbps` 与 `observabilitySnapshot.latest.frameRecovery`，让 runtime trace 可在单条 snapshot 内直接回看统一码率口径和最新帧恢复状态。前端性能面板 [`src/components/stream/StreamPerformancePanel.vue`](/Users/guo.xu/Documents/code/games/xbxrc/src/components/stream/StreamPerformancePanel.vue) 也已同步收口：`VideoDL` 固定显示真实下行流码率 `inboundVideoBitrateKbps`，并将 `VideoActual/VideoActualSrc/VideoActualGuardState` 作为诊断项，累计字节指标改名为 `VideoBytes/AudioBytes`，同步更新中英文文案。RFC：[`docs/rfcs/2026-03-27-cloud-high-rtt-latency-first-recovery.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-27-cloud-high-rtt-latency-first-recovery.md)。验证：`cargo check -p xbxengine`、`cargo check -p xbxrc`。待完成：基于新 Cloud runtime trace 做真实 backlog/latency 回归，并确认 `bweUpdated.actualVideoBitrateKbps` 与 `observabilitySnapshot.bitrate.actualVideoKbps`/`observabilitySnapshot.bwe.actualVideoKbps`、`frameRecoveryObserved(action=ledgerWrite/ledgerConsume)` 在运行态出现。 |
| 2026-03-26-home-session-registration-investigation-todo | 建立 home session 注册异常的持续分析 ToDo，跟踪多次重试与会话卡死问题 | In Progress | 2026-03-26 |  | 已新增跟踪清单：[`docs/todo-home-session-registration-investigation.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/todo-home-session-registration-investigation.md)。当前已沉淀本次日志结论（4 次尝试、前 3 次 `waitingForServerRegistration` 超时、第 4 次成功、旧会话 `4845...` 成功后仍被轮询）并列出下一步分析项（attempt/session 绑定、旧 poll 清理、超时策略与可观测性补齐）。 |
| 2026-03-25-cloud-twcc-local-feedback-control | 显式接管云游戏 TWCC 本地反馈链路，并统一 local/remote TWCC 观测语义 | In Progress | 2026-03-25 |  | 已新增 RFC：[`docs/rfcs/2026-03-25-cloud-twcc-local-feedback-control.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-25-cloud-twcc-local-feedback-control.md)。当前除受控 interceptor registry 与本地 TWCC observer 外，已继续补齐“可证明运行到哪一段”的诊断能力：1) `src-tauri/build.rs`、`src-tauri/src/shell/mod.rs`、`src-tauri/src/mods/xbxengine/runtime_state.rs` 现会把 build fingerprint 写入 `runtimeBuildInfo`、`statsSnapshot` 与 `observabilitySnapshot`，且已拆分默认 `feedback interval` 与当前会话实际生效值，避免 trace 口径歧义；2) `crates/xbxengine/core/src/transport/rtc/connection/builder.rs`、`crates/xbxengine/core/src/transport/rtc/connection/twcc_feedback.rs`、`crates/xbxengine/core/src/transport/rtc/connection/data_channel.rs`、`crates/xbxengine/core/src/runtime_stats_sink.rs`、`crates/xbxengine/core/src/diagnostics/observation_bus.rs`、`src-tauri/src/mods/xbxengine/trace_projection.rs` 现新增/补强结构化断点事件 `rtcBuilderConfigured`、`twccRemoteStreamBound`、`twccInboundExtensionSeen`、`twccInboundExtensionMissing`；其中 `data_channel.rs` 现已在当前实际 `poll_read()` RTP 读循环增加 fallback bind，确保即使 interceptor 没回调 `bind_remote_stream`，trace 也能继续证明断点在“已看到 RTP 但扩展缺失”还是“已看到扩展但本地 feedback 未产出”；3) `crates/xbxengine/core/src/diagnostics/stats.rs`、`crates/xbxengine/protocol/src/runtime.rs`、`src/shared/rpc/xbxengine.ts`、`src/player/domain/media.ts`、`src/streaming/types.ts`、`src/streaming/runtime/xbxengine-runtime.ts`、`src/components/stream/StreamPerformancePanel.vue` 中新增 `actualVideoBitrateSource` 与 `twccObservationState`，让 UI/trace 可直接解释“为什么当前码率/TWCC 为空”；4) 当前 Rust RTC 主链里的 `legacy frame pipeline` 误导性命名已清理为中性主线路径命名，避免误判为仍在绕旧实现；5) 结合 `runtime-trace-1774415000917.jsonl` 继续排查后，已确认最新断点前移到“video 已绑定但未产出 local feedback”，并已把 `twcc_feedback.rs` 的 answer SDP 解析改为按 `m=` section + payload type 精确选取 `extmap / rtcp-fb / rtpmap`，修复 audio/video 同时声明 `transport-cc` 时可能误绑错误 ext id 的问题，同时补上 audio/video ext id 不同的回归测试；6) 已在 `twcc_feedback.rs` 进一步完成有界重构：将 inbound extension 计数改为 per-SSRC，避免跨流污染；将无 `receiver_id` 映射的本地 TWCC feedback 改为有上限的待发送队列并追加显式日志，避免静默丢弃；并新增针对 per-SSRC 计数与“不可路由 feedback 入队”的定点测试；7) 在 `connection/service.rs` 与 `stack/transport_session.rs` 已把 `SetTargetRembKbps` 的真实语义落成 receiver-side `goog-remb`：当已有 video feedback 目标时直接发送 `ReceiverEstimatedMaximumBitrate` RTCP；若 video receiver/SSRC 尚未完成真实绑定，则在 connection 层先排队，待后续 `pump()` 拿到 video 目标后自动补发，避免继续停留在“只写 stats”的假行为；8) 结合后续真实 trace 暴露出的剩余问题，已继续把 connection 层的 video REMB 目标收成稳定状态，不再每次临时从 `remote_twcc_streams` 扫描推导，同时为 `local-feedback` 的字节估算增加更强保护：优先使用 transport bitrate * interval 反推区间字节数，且拒绝回退到 `observedPacketCount == observedByteCount` 的异常口径；9) 进一步顺着代码时序审查后，已补 `register_track_open()` 对既有 TWCC binding 的 receiver backfill：当 `OnTrack` 晚于首个 video RTP 到达时，现会把新得到的 `receiver_id` 回填到已有 video binding，并同步刷新稳定的 `preferred_video_receiver_id/preferred_video_media_ssrc`，避免 `rtcTargetRembQueued` 因时序问题长期无法升为 `rtcTargetRembRequested`；10) 参考旧版 Rust `webrtc` 主线 `spawn_video_track_stats_loop()` 的行为，本轮已在当前 rtc connection 层补回“周期性 REMB 持续刷新”能力：在初次 `requested` 之后，connection 层会基于稳定的 video target 和 `active_target_remb_kbps` 按反馈间隔继续 refresh `ReceiverEstimatedMaximumBitrate`，并在 gap/nack/waitingKeyframe 等压力场景下加快刷新频率，尽量对齐旧主线对远端编码器的持续刺激方式；11) 本轮已继续把标准 SDP 协商字段对齐到 Xbox 云端兼容口径：移除了 `x-xbx-*` 私有 SDP 属性；将 `H264High` 的 negotiation 语义改为对齐 Better xCloud 的 `4d` family 偏好；把自有 H264 family 注册顺序调整为 `4d -> 42e -> 64 -> 420`；并在最终 offer patch 后去重重复的标准 `a=rtcp-fb:*` 行，避免继续输出重复的 `goog-remb/transport-cc` 声明；12) 结合 `runtime-trace-1774512342749.jsonl` 与历史 Rust 主线口径继续比对后，已确认当前 cloud `RuntimeVideoPipelinePlan` 把 `feedback_interval_ms` 编译成 `1000ms` 是与现有 Rust-owned BWE/恢复策略设计不一致的配置偏差，现已在 [`crates/xbox-streaming/src/policy/runtime/compiler.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbox-streaming/src/policy/runtime/compiler.rs) 将 RustOwned + Cloud 的 TWCC feedback interval 调回 `100ms`，同时保留 cloud 的 NACK/jitter 宽容参数不变，避免 cloud 场景被长期误判为 `await-feedback/unstable` 并放大保守 backoff；13) 按“反馈乐观内容对齐”继续修正后，已在 [`crates/xbxengine/core/src/transport/rtc/bwe/policy/twcc_rules.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/bwe/policy/twcc_rules.rs) 增加 cloud 场景的 delayed-feedback 乐观降级分支：当 TWCC feedback interval 明显慢于当前配置节奏时，不再直接走 `severe-backoff`，而是改为 `severe-optimistic-cap`（只做上限收敛、不立刻大幅下砍），并在 [`crates/xbxengine/core/src/transport/rtc/bwe/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/bwe/policy.rs) 增加对应定点单测，避免 cloud 抖动/延迟反馈被误判为持续重度拥塞；14) 针对最新 trace 暴露出的剩余问题继续补齐后，已在 [`crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack/transport_session.rs) 将 `bweUpdated.actualVideoBitrateKbps` 改为优先使用 `latest_video_twcc_observation.receive_bitrate_kbps`（再回退到 transport metrics），修复“`actualVideoBitrateSource=local-twcc` 但数值长期为 0”口径错位；同时在 [`crates/xbxengine/core/src/transport/rtc/bwe/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/bwe/policy.rs) 调整稳定反馈判定容忍度（允许小幅 interval spike），并新增 `direct_feedback_interval_small_spike_does_not_trigger_unstable_hold` 回归测试，减少偶发长间隔尖峰导致的过于频繁 `twcc-gcc-direct-unstable-hold`；15) 针对 cloud 回归中 `twcc-gcc-cloud-unstable-hold` 仍偏高的问题，已在 [`crates/xbxengine/core/src/transport/rtc/session/policy.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/session/policy.rs) 增加 unstable-hold 连续确认门槛（首次命中且目标不变时不立即发命令，需要连续两次再发），并补充 `unstable_hold_requires_consecutive_confirmation_before_emit` 单测，降低单次反馈抖动造成的策略噪声；16) 基于最新 trace [`runtime-trace-1774576415729.jsonl`](/Users/guo.xu/Documents/code/games/xbxrc/runtime-logs/runtime-trace-1774576415729.jsonl) 的回归分析，前一轮止血修复已解决“首个 `feedbackIntervalMs=None` 的 cloud local-feedback 稀疏样本导致 `severe-backoff`”与 `videoTrackState` 持久化缺口；本轮继续做去补丁化收敛：已将 TWCC 样本质量抽象收回到 `XbxEngineVideoTwccObservation.quality` 强类型，由 [`crates/xbxengine/core/src/transport/rtc/connection/transport_metrics.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/connection/transport_metrics.rs) 单点判定 `Stable / Delayed / BootstrapSparse / Unstable / RemoteObserved`，`bwe policy` 只消费 `quality` 而不再重复猜测“首样本是否可信”；同时 `videoTrackState` 已在 [`crates/xbxengine/core/src/transport/rtc/stack/runtime_stats.rs`](/Users/guo.xu/Documents/code/games/xbxrc/crates/xbxengine/core/src/transport/rtc/stack/runtime_stats.rs) 收敛为显式状态机，由共享 runtime stats 持久推进，`runtime_port` / `runtime sync` 只读不再间接维护。验证：`cargo check -p xbxengine`、`cargo test -p xbxengine first_local_feedback_twcc_uses_optimistic_delivery_ratio_when_interval_is_missing -- --nocapture`、`cargo test -p xbxengine cloud_delayed_feedback_uses_optimistic_cap_instead_of_severe_backoff -- --nocapture`、`cargo test -p xbxengine media_snapshot_promotes_track_status_to_remote_track_attached_when_video_exists -- --nocapture`、`cargo test -p xbxengine media_snapshot_track_state_machine_flows_audio_then_video_then_attached -- --nocapture`。下一步只需要用下一份真实 trace 确认 `twccFeedbackSent.quality` 与 `videoTrackState` 的单点语义在运行态与预期一致。 |
| 2026-03-24-streaming-native-video-dual-window-architecture | 规划串流 native video 双窗口架构，收口首帧后透明叠加与保比例黑底展示策略 | In Progress | 2026-03-24 |  | 已新增 RFC，明确 canonical 方案为 `main` + 独立 `native-video` 双窗口；窗口协同能力继续收口在 `src-tauri/src/mods/native_video/*`，不新增 `window_coordinator`；RFC 覆盖双窗口拓扑、首帧透明时序、`contain + black background`、窗口同步、平台差异、失败回滚与验证计划。RFC：[`docs/rfcs/2026-03-24-streaming-native-video-dual-window-architecture.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-24-streaming-native-video-dual-window-architecture.md)。 |
| 2026-03-24-home-local-srflx-gather-scheme-b | 方案 B：在 canonical Rust RTC 里补齐 host/srflx/relay，并把 relay 通道接入 pump | In Progress | 2026-03-24 |  | 已在 `crates/xbxengine/core/src/transport/rtc/connection/builder.rs` 复用 ICE server 列表，`crates/xbxengine/core/src/transport/rtc/connection/io_runtime.rs` 产出 host/srflx/relay 候选并依据 `transport.local_addr` 做 direct/relay 路由，`crates/xbxengine/core/src/transport/rtc/connection/turn_runtime.rs` 已接入 `turn`/`stun` crate 的 `listen + allocate`、relay 收发 worker、drop 清理（stop/rebuild 释放）；同时修复 RFC 7064/7065 形态 URL（`stun:host:port`/`turn:host:port`）解析兼容，并补上两轮收口：其一是结构防腐化，`runtime-host` 通过独立 `runtimeTrace.recordEvent` 写 trace、policy helper 独立化、`connection/helpers.rs` 拆分小模块；其二是策略收口，`crates/xbox-streaming/src/session/signaling/ice.rs` 现在只做稳定排序与轻量清洗，默认不再改写 foundation/priority、也不再无条件补 `end-of-candidates`，Teredo 派生改成显式开关；`crates/xbxengine/core/src/transport/rtc/connection/negotiation.rs` 的 family 过滤也改成“仅 host mismatch 硬跳过，srflx/relay 保留并记录观测”。在最新回归 trace `runtime-trace-1774341849717.jsonl` 中，已确认这轮 P1 收口把 home 直连所需的 Teredo 派生 IPv4 host candidate 关掉了，导致远端候选计数从成功样本的 `host=12 srflx=1` 回落到 `host=8 srflx=1`、ICE 长时间停在 `Connecting`；现已在 `crates/xbox-streaming/src/session/api/signaling.rs` 仅对 home 场景恢复 `with_teredo_ipv4_derivation(true)`，作为最小回补。验证：`cargo fmt --all`、`cargo test -p xbox-streaming session::signaling::ice -- --nocapture`、`cargo check -p xbox-streaming`、`cargo check -p xbxengine`、`cargo check -p xbxrc` 已通过；待下一份真实 trace 继续确认 selected pair / Connected 恢复。RFC：[`docs/rfcs/2026-03-24-home-local-srflx-gather-scheme-b.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-24-home-local-srflx-gather-scheme-b.md)。 |
| 2026-03-24-home-turn-gathering-failure-observability | 补充 home TURN gather 失败观测，并给 StopRuntime 透传 reason | In Progress | 2026-03-24 |  | 已在 `crates/xbxengine/core/src/transport/rtc/connection/lifecycle.rs` 接住 `OnIceCandidateErrorEvent`，把 URL / code / text / address / port 与本地 candidate summary 写入观测；同时在 `src/streaming/runtime/runtime-host.ts`、`src/streaming/runtime/xbxengine-runtime.ts`、`src-tauri/src/mods/xbxengine/rpc.rs`、`src-tauri/src/mods/xbxengine/service.rs`、`src-tauri/src/mods/xbxengine/runtime_state.rs`、`crates/xbxengine/protocol/src/control.rs` 之间透传 `StopRuntime(reason)`，让下一份 trace 能直接看到是谁触发了 stop。RFC：[`docs/rfcs/2026-03-24-home-turn-gathering-failure-observability.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-24-home-turn-gathering-failure-observability.md)。验证：`cargo fmt --all`、`cargo check -p xbxengine`。 |
| 2026-03-28-progressive-keyframe-request-cooldown-rfc | 为 Cloud 高 RTT / 长空窗场景制定渐进式 PLI / keyframe request 冷却策略 RFC | In Progress | 2026-03-28 |  | 当前执行重心已收敛到 [`docs/rfcs/2026-03-29-cloud-supply-pressure-hysteresis.md`](/Users/guo.xu/Documents/code/games/xbxrc/docs/rfcs/2026-03-29-cloud-supply-pressure-hysteresis.md)：先把 `timeline` 的低价值 admission 保持为软处理，再在 owner 层补供给压力 hysteresis，避免 Cloud 高 RTT / 网络波动下短时间重复请求给服务端造成过多压力。 |
