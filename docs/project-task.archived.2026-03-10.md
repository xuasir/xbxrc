# Project Task Tracker

> 本文件已于 2026-03-10 归档。

## Current Task List

| ID | Task | Status | Started On | Completed On | Notes |
| --- | --- | --- | --- | --- | --- |
| P-359 | 实现 `xbox-streaming` 首版纯函数 compiler 流水线 | Done | 2026-03-10 | 2026-03-10 | 在 `crates/xbox-streaming/src/compiler.rs` 中把占位契约补成首版可执行编译逻辑：1) 新增 `CompileError`，覆盖缺失 `gs_token`、region 为空、base URL 非法、runtime 不可用、native MKB 不可用等失败场景；2) 实现 `compile` 入口和 `resolve_session_access`，完成 region 选择优先级（`force_region_ip -> preferred_region_name -> is_default -> first`）与 base URL 归一化；3) 实现 `compile_runtime/compile_session/compile_negotiation/compile_input`，把 runtime mode、TURN 来源、设备画像、`x-ms-device-info` 请求头、codec/profile、bitrate、stereo、console addrs 注入、输入模式与触控/麦克风开关编译成稳定 `Plan`；4) 补齐 6 个单测，锁定 region 选择、runtime 自动选择、H264 profile 映射、native MKB 判定和 xHome fallback TURN + console addrs 场景；5) `Cargo.toml` 为 crate 增加 `thiserror` 依赖。验证通过：`cargo fmt -p xbox-streaming`、`cargo check -p xbox-streaming`、`cargo test -p xbox-streaming`。 |
| P-358 | 将 `xbox-streaming` crate 模型定义收口为更 Rust 的代码风格 | Done | 2026-03-10 | 2026-03-10 | 对刚新增的 `crates/xbox-streaming` 做一轮“去 DDD 化、去冗余前缀”的风格收口：1) 删掉 `domain/application/policy` 多层目录和空骨架，改为顶层 `src/{config,context,plan,compiler}.rs` 直出模块，更符合 Rust crate 常见组织方式；2) 将 `StreamingConfig/StreamingContext/StreamingPlan` 等长前缀类型重命名为 `Config/Context/Plan`、`SessionConfig/NegotiationConfig/InputConfig/RuntimeConfig`、`SessionPlan/NegotiationPlan/InputPlan/RuntimePlan` 等短名，减少 crate 内部重复命名；3) 将 `FeatureSwitch`、`TouchModePreference`、`RuntimeModePreference` 等带 Java/DDD 味的命名收口为 `Switch`、`TouchPreference`、`RuntimePreference`，并把 `ForceOn/ForceOff` 简化为 `On/Off`；4) 保留全部已定义能力字段，但整体 API 面改为更 Rust 化的模块与类型命名。验证通过：`cargo fmt -p xbox-streaming`、`cargo check -p xbox-streaming`。 |
| P-357 | 新增 `xbox-streaming` crate 并固定首版 `StreamingConfig/Context/Plan` 类型定义 | Done | 2026-03-10 | 2026-03-10 | 新增 `crates/xbox-streaming` crate，先只落可编译的领域模型和中文注释，不接现有调用链：1) 在 `src/domain/config.rs` 中定义 `StreamingConfig` 及分组后的 `session/negotiation/input/runtime` 偏好结构，补齐区服、分辨率画像、codec/bitrate、IPv6、TURN、输入模式、runtime mode 等首版可实现字段；2) 在 `src/domain/context.rs` 中定义 `StreamingContext`、`StreamingSessionAccessContext`、`StreamingTitleCapabilities`、`StreamingRuntimeCapabilities`、`StreamingRegionDescriptor`、`ResolvedStreamingSessionAccess` 等编译期上下文与接入结果结构，明确“配置”和“运行事实”分层；3) 在 `src/domain/plan.rs` 中定义 `StreamingPlan` 及 `SessionPlan/NegotiationPlan/InputPlan/RuntimePlan`，并补齐 `StreamingDeviceProfile`、`StreamingMsDeviceInfo`、`StreamingTurnPlan` 等最终决策结构；4) 在 `src/policy/compiler.rs` 中固定 `StreamingPlanCompilerInput/Output` 与 `SessionAccessResolverOutput` 契约；5) 根 `Cargo.toml` 已将 `crates/xbox-streaming` 加入 workspace。当前仅固定模型，不包含真实 compiler/orchestrator 逻辑。验证通过：`cargo fmt -p xbox-streaming`、`cargo check -p xbox-streaming`。 |
| P-356 | 调整 `data` 域服务拆分：删除 `client.rs`，将响应包装下沉到对应子服务 | Done | 2026-03-10 | 2026-03-10 | 基于“`client.rs` 实际承担的是响应包装，不应成为独立抽象层”的判断，对 `src-tauri/src/mods/data` 再做一轮结构修正：1) 删除 `client.rs`；2) 恢复 `services/host_service.rs`、`profile_service.rs`、`streaming_query_service.rs`、`xcloud_service.rs`，让 `service.rs` 回到纯 orchestrator；3) `host_service` 现在直接持有 `SmartglassApi` 调用与主机列表解析逻辑，`profile_service` 直接持有 `ProfileApi` 调用与 profile setting 归一化逻辑，响应包装各自就地负责；4) `session_resolver.rs` 去掉 Web API client 复用层，只保留 session 解析与 `resolve_web_token_claims`；5) `mod.rs` 重新导出 `services` 模块。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n "mods::data::client|client::XboxWebApiClient|pub mod client" src-tauri/src/mods/data src-tauri/src` 已确认 `client` 层清零。 |
| P-355 | 继续收敛 `data` 域文件数：合并辅助实现到核心文件 | Done | 2026-03-10 | 2026-03-10 | 在上一轮完成职责收口后，继续把 `data` 域的文件数压缩到更符合“查询模块”的形态：1) `session_types.rs` 合并进 `types.rs`，`DataSessionContext/DataAuthState` 不再独立一层；2) `web_api_provider.rs` 合并进 `session_resolver.rs`，Web API client 复用逻辑与 session 解析逻辑收口到同一文件；3) 原 `services/host_service.rs`、`profile_service.rs`、`streaming_query_service.rs`、`xcloud_service.rs` 全部并回 `service.rs`，以 `HostQuery/ProfileQuery/StreamingQuery/XcloudQuery` 内聚实现，避免目录上再挂一层薄 service 文件；4) 删除 `services/` 目录及对应导出；5) `mod.rs` 现已收口为 `cache_repository + client + rpc + service + session_resolver + types`。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`find src-tauri/src/mods/data -maxdepth 2 -type f | sort` 现在只剩 7 个文件。 |
| P-354 | 去掉 settings store 旧文件迁移兼容 | Done | 2026-03-10 | 2026-03-10 | 按当前决策进一步收紧 tauri 侧 settings store：1) `src-tauri/src/settings_store.rs` 删除 `LEGACY_SETTINGS_STORES`、旧文件存在性探测和首次迁移逻辑；2) `SettingsStoreResolver` 现在只负责打开 `settings.xbx.json`，不再读取或合并 `settings.json/settings.v2.json`；3) 这样 `clear_data` 等对新 store 的删除语义不再受旧文件残留影响。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n 'LEGACY_SETTINGS_STORES|settings\\.json|settings\\.v2\\.json' src-tauri/src/settings_store.rs src-tauri/src` 已确认兼容常量与迁移逻辑已移除。 |
| P-353 | 统一 tauri 侧 settings store 到 `settings.xbx.json` | Done | 2026-03-10 | 2026-03-10 | 收口 tauri 侧散落的 store 文件名与访问方式：1) 新增 `src-tauri/src/settings_store.rs`，统一只打开 `settings.xbx.json`，不再做运行期 fallback；2) 首次打开新 store 时，会把旧 `settings.json` 与 `settings.v2.json` 的条目一次性迁移到 `settings.xbx.json`，之后读写都只认新文件；3) `src-tauri/src/mods/config/storage_repository.rs`、`src-tauri/src/mods/auth/storage_repository.rs`、`src-tauri/src/mods/data/cache_repository.rs`、`src-tauri/src/mods/app_state/service.rs` 已全部切到共享解析器，不再裸写文件名；4) 顺手修复了 `AppStateService::new` 的 `AppHandle` move 问题。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n 'settings(\\.v2)?\\.json|settings\\.xbx\\.json|\\.store\\(\"settings' src-tauri/src` 显示旧文件名只保留在迁移常量里，新文件名只保留在共享入口。 |
| P-352 | 精简 `config` 域结构：删除无价值 persistence 层 | Done | 2026-03-10 | 2026-03-10 | 基于“`config` 是轻量配置模块，不需要仿照 `auth` 做多层编排”的结论，对 `src-tauri/src/mods/config` 做结构简化：1) 删除 `persistence_service.rs` 与 `ConfigPersistenceService`，因为这一层只做转发和极薄装配，没有独立状态与边界；2) `service.rs` 改为让 `ConfigService` 直接组合 `ConfigStorageRepository`，自行完成 `get_normalized_config/get_by_keys/set_by_patch/get_groups/get_force_region_ip/get_streaming_config`；3) `config_policy.rs` 从 `ConfigPolicy` 空 struct + 静态方法改成普通纯函数，减少无意义类型包装；4) `mod.rs` 已移除 `persistence_service` 导出。当前 `config` 域稳定收口为 `service + storage_repository + config_policy + defaults + grouping + rpc`，更符合其轻量配置模块定位。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n "config::persistence_service|ConfigPersistenceService|persistence_service" src-tauri/src/mods/config src-tauri/src` 已确认 `config` 域无残留。 |
| P-351 | 精简 `data` 域结构：回收到查询模块形态 | Done | 2026-03-10 | 2026-03-10 | 基于“`data` 是读模型/查询模块，不应照搬 `auth` 状态域复杂度”的结论，对 `src-tauri/src/mods/data` 做结构回收：1) `persistence_service.rs` 更名为 `cache_repository.rs`，类型改为 `DataCacheRepository`，明确这里只承接 profile/xcloud cache 的简单存取；2) `session_service.rs` 更名为 `session_resolver.rs`，类型改为 `DataSessionResolver`，并把原 `web_token_policy.rs` 中的 `resolve_web_token_claims` helper 合并进来；3) 删除 `web_token_policy.rs`，避免查询模块再额外维护一个名不副实的 policy 层；4) `service.rs`、`web_api_provider.rs`、`services/streaming_query_service.rs`、`services/xcloud_service.rs` 已同步切到新的 `session_resolver + cache_repository` 命名；5) `mod.rs` 导出更新后，当前 `data` 顶层已经稳定为 `service + session_resolver + cache_repository + services/* + rpc + types`。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`find src-tauri/src/mods/data -maxdepth 2 -type f | sort` 已确认目录收口完成。 |
| P-350 | 抽出 `data` 域持久化层，收口 profile/xcloud 缓存到 `DataPersistenceService` | Done | 2026-03-10 | 2026-03-10 | 继续按新 Rust mod 规范收口 `src-tauri/src/mods/data`：1) 新增 `src-tauri/src/mods/data/persistence_service.rs`，统一承接 `data.profileCache` 与 `data.xcloudTitlesCache` 的 store 读写，以及 xCloud 标题的进程内缓存；2) `services/profile_service.rs` 改为纯远端 profile 查询与响应归一化，不再直接访问 `settings.json`；3) `services/xcloud_service.rs` 改为无状态查询服务，TTL/陈旧缓存回退仍保留，但缓存介质统一切到 `DataPersistenceService`，`DataService` 因此不再需要为 xcloud 额外维护 `Mutex<XcloudService>`；4) `service.rs` 现已组合 `DataPersistenceService + ProfileService + XcloudService`，负责“远端拉取 -> 持久化缓存 -> 回退缓存”的应用编排；5) `mod.rs` 已导出新持久化模块。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n "StoreExt|store\\(|save\\(|PROFILE_CACHE_KEY|XCLOUD_TITLES_CACHE_KEY" src-tauri/src/mods/data` 显示 store 访问已只剩 `persistence_service.rs`。 |
| P-349 | 沉淀 streaming 策略层 crate 化与双 runtime 收敛 RFC | Done | 2026-03-10 | 2026-03-10 | 新增 [`rfc/streaming-strategy-layer-and-runtime-convergence.md`](/Users/guo.xu/Documents/code/games/xbxrc/rfc/streaming-strategy-layer-and-runtime-convergence.md)，压缩当前关于 `streaming` 重构方向的结论：1) 明确后续要把 `src-tauri/src/mods/*` 中与 Xbox 相关的强逻辑继续提取到 crate，而不是继续堆在 tauri mod 内；2) 明确 `rust-owned` 与 `webrtc-direct` 双 runtime 的收敛原则是“共享策略判定，允许 adapter 实现重复”；3) 建议新增 `crates/xbox-streaming`，以 `StreamingPlan` 作为 session / negotiation / input / runtime 的统一策略输出；4) 明确本机参考代码源目录为 `/Users/guo.xu/Documents/code/games/better-xcloud`，作为 better-xcloud 能力参考锚点。此次为文档沉淀，未运行额外测试。 |
| P-348 | 按 Rust mod 规范摊平 `data` 域目录并抽出 session 编排层 | Done | 2026-03-10 | 2026-03-10 | 对 `src-tauri/src/mods/data` 做第二个范本化重构，去掉旧 `application/domain/infrastructure` 目录层级：1) `application/data_service.rs` 提升为顶层 `service.rs`，保留 `DataService` 作为模块主编排器；2) 新增 `session_service.rs`，将认证 session 兜底解析与 `XboxWebApiClient` 复用逻辑从 `DataService` 中抽出；3) `domain/types.rs` / `domain/web_token.rs` 分别提升为 `session_types.rs` / `web_token_policy.rs`，`infrastructure/xbox_webapi_provider.rs` 提升为 `web_api_provider.rs`；4) `application/services/*` 提升为顶层 `services/*`；5) 删除旧 `application/domain/infrastructure` 模块入口和未使用 bridge 残留。当前 `data` 域顶层结构已收口为 `service + session_service + services/* + web_api_provider + web_token_policy + session_types + rpc + client + types`，更符合新规范。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n "mods::data::(application|domain|infrastructure)|data::(application|domain|infrastructure)" src-tauri/src` 已无结果。 |
| P-347 | 按 Rust mod 规范调整 `config` 域结构 | Done | 2026-03-10 | 2026-03-10 | 以新沉淀的 mod 规范为基线，对 `src-tauri/src/mods/config` 做第一轮收口：1) 原 `repository.rs` 改为 `storage_repository.rs`，类型改为 `ConfigStorageRepository`，仅负责 store 读写与 fallback 文件处理；2) 新增 `config_policy.rs`，把配置默认值归一化、patch 过滤、配置分组等纯判定/派生逻辑从 `service.rs` 中抽出；3) 新增 `persistence_service.rs`，统一组合 `ConfigStorageRepository + ConfigPolicy`，对上层暴露 `get_normalized_config/get_by_keys/set_by_patch/get_groups/get_force_region_ip/get_streaming_config`；4) 重写 `service.rs`，让 `ConfigService` 退化为薄编排层；5) `mod.rs`、`shell/mod.rs` 已同步切到新的 `ConfigService::new(AppHandle)` 构造方式。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n "mods::config::repository|ConfigRepository|repository.rs" src-tauri/src/mods/config src-tauri/src/shell` 已无结果。 |
| P-346 | 沉淀 Rust mod 组织规范到 `dev-docs` 并挂到 `AGENTS.md` | Done | 2026-03-10 | 2026-03-10 | 新增 [`dev-docs/rust-mod-organization.md`](/Users/guo.xu/Documents/code/games/xbxrc/dev-docs/rust-mod-organization.md)，以当前 `auth` 模块为范本，明确 `src-tauri/src/mods/*` 的 Rust 风格组织方式：`service + runtime_state + persistence_service + storage_repository + *_policy + rpc/events`，并定义与 `crates/xbox-webapi` / `crates/*-flow` 的边界、命名规则、裁剪规则和硬约束；同时更新 [`AGENTS.md`](/Users/guo.xu/Documents/code/games/xbxrc/AGENTS.md)，将该文档作为后续 mod 开发和重构的执行规范入口。此次为文档更新，未运行额外测试。 |
| P-345 | 理顺 Auth 持久化层命名：`AuthStorageRepository + AuthTokenPolicy` | Done | 2026-03-10 | 2026-03-10 | 解决 auth 持久化层“双 repository”命名混乱：1) 原 `src-tauri/src/mods/auth/repository.rs` 改为 `storage_repository.rs`，类型改为 `AuthStorageRepository`，仅保留原始 token/JWK/web/stream 存取与清理；2) 原 `token_repository.rs` 改为 `token_policy.rs`，类型改为 `AuthTokenPolicy`，仅承接 token validity、session snapshot、app level 推导等策略逻辑；3) `AuthPersistenceService` 已切到组合 `AuthStorageRepository + AuthTokenPolicy`，命名与职责一致；4) `runtime_state.rs`、`mod.rs` 等引用已同步更新，auth 模块内已无 `CoreTokenRepository/AuthTokenRepository` 旧命名残留。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`；`rg -n "CoreTokenRepository|AuthTokenRepository|token_repository|repository.rs" src-tauri/src/mods/auth` 已无结果。 |
| P-344 | 拆分 Auth 应用层：`AuthService + AuthPersistenceService + AuthRuntimeState` | Done | 2026-03-10 | 2026-03-10 | 继续瘦身 tauri 侧 auth 编排层：1) 新增 `src-tauri/src/mods/auth/runtime_state.rs`，将 `AuthState`、`pending_redirect_flow`、callback 处理标记、check cooldown 等内存态与并发控制从 `AuthService` 中抽出；2) 新增 `src-tauri/src/mods/auth/persistence_service.rs`，统一承接 repository 读写、`AuthBundle` 持久化、`AuthFlowSeed/FlowSisuTokenData` 装配、有效 session/web/stream token 读取与判定；3) 重写 `src-tauri/src/mods/auth/service.rs`，让 `AuthService` 退化为应用编排层，只保留 flow 调用、策略判断与窗口协作；4) `src-tauri/src/mods/auth/mod.rs` 注册新模块。这样 token 持久化与应用态 token 策略被明确留在调用侧，没有继续下沉到 flow。验证通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。当前 auth 模块未新增 warning，剩余 warning 仅来自仓库既有 `xbxengine`。 |
| P-343 | 收口 Auth transfer token 到 `xbox-webapi` 与 `xbox-auth-flow` | Done | 2026-03-10 | 2026-03-10 | 将原本仍留在 `src-tauri/src/mods/auth/transfer_token_service.rs` 的 cloud transfer token HTTP 请求正式收口：1) `crates/xbox-webapi/src/auth_api.rs` 新增 `AuthApi::get_cloud_transfer_token`，统一走现有 `HttpTransport`；2) `crates/xbox-auth-flow/src/flow.rs` 新增 `AuthFlow::transfer_token`，让 transfer token 流程与其他 auth flow 保持同一入口；3) `src-tauri/src/mods/auth/service.rs` 的 `get_transfer_token` 改为只读取本地 `refresh_token` 并调用 flow，不再自己持有 HTTP client；4) 删除 `src-tauri/src/mods/auth/transfer_token_service.rs`，并从 `src-tauri/src/mods/auth/mod.rs` 清理模块声明。至此 auth 协议 HTTP 与流程实现已全部移出 tauri，调用侧仅保留 store、窗口、状态和事件。验证通过：`cargo fmt -p xbox-webapi -p xbox-auth-flow -p xbxrc`、`cargo check -p xbox-webapi`、`cargo check -p xbxrc`。 |
| P-342 | 收口 Auth skip-refresh 下游令牌链路到 `xbox-auth-flow` | Done | 2026-03-10 | 2026-03-10 | 在 `crates/xbox-auth-flow` 中新增 `BuildDownstreamTokensInput/Output` 与 `AuthFlow::build_downstream_tokens`，把“复用已有 user/sisu token 继续获取 Web/XSTS/streaming token”从 tauri 抽到 flow，并让 `finalize_login` 与 skip-refresh 复用同一段 `build_auth_bundle` 逻辑。`src-tauri/src/mods/auth/service.rs` 的 `start_silent_flow_impl` 现在在 23 小时窗口命中时也只负责读取本地 `user_token/sisu_token/private_jwk`、调用 flow、持久化 `AuthBundle` 与更新 `AuthState`；旧的 `get_downstream_tokens` 已删除。验证通过：`cargo fmt -p xbox-auth-flow -p xbxrc`、`cargo check -p xbox-auth-flow`、`cargo check -p xbxrc`。 |
| P-341 | 迁移 Auth 静默刷新主链路到 `xbox-auth-flow` | Done | 2026-03-10 | 2026-03-10 | 在 `crates/xbox-auth-flow/src/flow.rs` 中新增 `AuthFlow::refresh_and_finalize`，将 `refresh_token -> user token -> finalize_login` 收口到 flow，复用已抽出的下游 `SISU/XSTS/streaming token` 链路；`src-tauri/src/mods/auth/service.rs` 的 `start_silent_flow_impl` 现在在“需要刷新核心 token”分支里仅保留 skip-window 判定、读取本地 `refresh_token/private_jwk`、调用 flow、持久化 `AuthBundle` 与更新 `AuthState`。同时删除迁移后不再使用的 `refresh_sisu_token` 与旧 token details helper，避免 auth 模块继续保留死代码。当前仍保留一个明确边界：23 小时窗口内的 skip-refresh 路径仍沿用 tauri 现有 `get_downstream_tokens`，留待下一步继续收口。验证通过：`cargo fmt -p xbox-auth-flow -p xbxrc`、`cargo check -p xbox-auth-flow`、`cargo check -p xbxrc`。 |
| P-340 | 迁移 Auth OAuth callback 完成链路到 `xbox-auth-flow` | Done | 2026-03-10 | 2026-03-10 | 在 `crates/xbox-auth-flow` 中新增 `AuthFlow::complete_oauth_login` 与内部 `finalize_login`，把 callback URL 校验、authorization code 交换、SISU authorize、双 XSTS 获取、`xhome/xgpuweb/xgpuwebf2p` streaming token 获取统一收口到 flow；同时为 flow 补齐 callback 校验错误与 `FlowSisuTokenData/FlowTokenDetails`。`src-tauri/src/mods/auth/service.rs` 的 `handle_oauth_callback_impl` 已改为只负责取出 `pending_redirect_flow`、加载本地 `private_jwk` seed、调用 flow、持久化 `AuthBundle` 并更新 `AuthState`，不再自己解析 callback 或串联下游 HTTP。验证通过：`cargo fmt -p xbox-auth-flow -p xbxrc`、`cargo check -p xbox-auth-flow`、`cargo check -p xbxrc`。 |
| P-339 | 新增 `xbox-auth-flow` crate 并迁移 Auth 登录启动链路 | Done | 2026-03-10 | 2026-03-10 | 新增 `crates/xbox-auth-flow`，先落第一批稳定边界：1) `flow.rs` 中实现 `AuthFlow::start_oauth_login`，直接依赖 `xbox-webapi` 完成 ECDSA seed 生成、device token 获取、SISU authenticate 与 pending flow 组装；2) `types.rs` 中建立 `PendingOAuthLogin`、`AuthFlowSeed`、`StartOAuthLoginInput/Output` 以及后续 callback/refresh/transfer 的流程 DTO 占位；3) `src-tauri/src/mods/auth/service.rs` 的 `login_impl` 改为调用 flow，只保留 `is_authenticating` 状态切换、`private_jwk` 持久化、`pending_redirect_flow` 保存与窗口侧 `LoginResponse` 返回；4) `pending_redirect_flow` 在 tauri 中已切到 flow DTO，后续可继续迁移 callback/finalize 链路而不再回退到裸 `xbox_webapi::XalRedirectFlow`。验证通过：`cargo fmt -p xbox-auth-flow -p xbxrc`、`cargo check -p xbox-auth-flow`、`cargo check -p xbxrc`。 |
| P-338 | macOS GameController haptics 最小链路接通：host 改走 Apple provider 并打通基础 pattern 播放 | Done | 2026-03-10 | 2026-03-10 | 在已确定 `GameController + Core Haptics` 路线后，继续把最小运行链路真正接起来：1) `crates/ohmygamepad/backends/macos-gccontroller-haptics/Cargo.toml` 新增 `objc2` / `objc2-foundation` / `objc2-game-controller` / `objc2-core-haptics` 的 macOS 目标依赖；2) `src/lib.rs` 从占位升级为最小可执行 provider，当前实现会在 macOS 上通过 `GCController::current()/controllers()` 选主控制器，取 `haptics`，用 `msg_send!` 调 `createEngineWithLocality:` 建默认 locality engine，再把 `OhMyGamepadRumbleEffectDto` 映射成基础 `CHHapticPattern + PatternPlayer`；3) `crates/ohmygamepad/backends/gilrs/src/service_rumble.rs` 增加 `HapticsProvider -> ServiceRumbleBackend` 适配层，并在 `InputRuntimeError` 中补齐 `HapticsUnavailable/HapticsTransportFailed`；4) `crates/ohmygamepad/backends/gilrs/src/oh_my_gamepad_service.rs` 新增 `spawn_with_haptics_provider`，允许宿主注入平台 haptics provider；5) `crates/ohmygamepad/bridge/host/src/lib.rs` 现已按 `DesktopDriverSelector` 在 macOS 上选择 `MacosGcControllerHapticsProvider`，而不是继续硬编码走 `gilrs` rumble。当前仍有两个明确边界：`stop_rumble` 先退化为 no-op，且设备选择仍偏“当前/首个主控制器”，后续再补 controller registry 与精确 device 映射。验证通过：`cargo fmt -p ohmygamepad-macos-gccontroller-haptics -p ohmygamepad-gilrs -p ohmygamepad-host -p ohmygamepad-core`、`cargo check -p ohmygamepad-macos-gccontroller-haptics -p ohmygamepad-gilrs -p ohmygamepad-host -p ohmygamepad-core`、`cargo test -p ohmygamepad-macos-gccontroller-haptics -p ohmygamepad-core --lib`、`cargo test -p ohmygamepad-gilrs -p ohmygamepad-host`。 |
| P-337 | 重写 Auth 改造 RFC：仅抽取认证协议流程，持久化与窗口控制保留在调用侧 | Done | 2026-03-10 | 2026-03-10 | 重写 `rfc/auth-flow-crate-refactor-plan.md`，明确边界为：`xbox-webapi` 负责 Auth HTTP API，`xbox-auth-flow` 直接依赖 `xbox-webapi` 并只封装 `start_oauth_login / complete_oauth_login / refresh_and_finalize / transfer_token` 四类认证流程，`src-tauri` 保留 `pending_redirect_flow`、token 持久化、OAuth 窗口控制、`AuthState` 与事件发射。RFC 同时补齐 crate 结构、DTO 草案、旧方法到新方法映射、分阶段施工步骤、DoD 与测试计划，能直接作为后续改造执行文档。 |
| P-336 | macOS 蓝牙 Xbox 手柄震动路径收口：确定 `GameController + Core Haptics` 主线并起骨架 | Done | 2026-03-10 | 2026-03-10 | 为避免在 macOS 蓝牙 Xbox 手柄震动上重复造轮子，已明确主实现路径为“`gilrs` 保留输入，新增 `macos-gccontroller-haptics` 承接 Apple `GameController + Core Haptics` 震动输出”，并落下骨架：1) workspace 新增 `crates/ohmygamepad/backends/macos-gccontroller-haptics` crate，固定 `bluetooth_only + prefer_primary_controller` 默认策略；2) 新增 RFC `rfc/macos-gamecontroller-haptics-plan.md`，明确复用边界（Apple framework 主线、SDL 仅作参考、`hidapi`/`360Controller` 不作为当前主线）；3) `crates/ohmygamepad/core/src/runtime/selector.rs` 新增 `DesktopHapticsProviderKind::MacosGcController`，macOS 默认选择该 provider；4) `crates/ohmygamepad/bridge/host/src/lib.rs` 将 runtime bootstrap 收口为单独装配锚点，便于下一阶段把 GCController haptics 真正接进去；5) `crates/README.md` 同步当前 backend 版图。验证通过：`cargo fmt -p ohmygamepad-macos-gccontroller-haptics -p ohmygamepad-core -p ohmygamepad-host`、`cargo test -p ohmygamepad-macos-gccontroller-haptics -p ohmygamepad-core -p ohmygamepad-host -p ohmygamepad-gilrs`、`cargo check -p ohmygamepad-host -p ohmygamepad-macos-gccontroller-haptics`。 |
| P-335 | 修复 macOS 下 Xbox 手柄 rumble 缺失：纠正 native 可用性判断并补浏览器 haptics fallback | Done | 2026-03-10 | 2026-03-10 | 修复两层问题：1) `crates/ohmygamepad/backends/gilrs/src/source.rs` 现在会把 `gilrs` force feedback `NotImplemented` 明确视为“无 rumble backend”，`oh_my_gamepad_service.rs` 不再在 macOS 这类平台把无效 rumble 请求误报为 `accepted`；2) `src/player/app/input/RumbleService.ts` 新增 native 失败/未实现时的浏览器 haptics fallback，仅负责震动输出，不恢复旧浏览器输入链，同时补出更明确的 unavailable 日志。验证通过：`cargo fmt -p ohmygamepad-gilrs`、`cargo test -p ohmygamepad-gilrs`；`pnpm exec tsc --noEmit --pretty false` 仅剩仓库既有 `src/player/app/{media,session,stats}/*` 与 `src/player/protocol/channels/MessageChannel.ts` 的 `TS2206` 错误，未新增本次改动相关 TS 问题。 |
| P-334 | 下线 `StreamingHttpError`，统一 streaming 模块错误模型到 `xbox_webapi::WebApiError` | Done | 2026-03-10 | 2026-03-10 | `src-tauri/src/mods/streaming/{session_api,signaling_api}.rs` 已改为直接返回 `WebApiError`；`src-tauri/src/mods/streaming/service.rs` 改为直接基于 `WebApiError::Http` 处理 `404` 和 keepalive `400` 语义，并统一错误格式化；`StreamingHttpError` 与 `error_map.rs` 已移除，`streaming_query_service.rs` 同步去掉对包装错误类型的依赖。补充测试：`service` keepalive 语义与错误格式化测试保持通过。验证通过：`cargo test -p xbxrc --lib service::tests`、`cargo test -p xbxrc --lib session_api::tests`、`cargo test -p xbxrc --lib signaling_api::tests`、`cargo check -p xbxrc`。 |
| P-333 | 恢复 `getStreamingTitleInputConfig` 旧契约，移除迁移后多余包装层 | Done | 2026-03-10 | 2026-03-10 | `src-tauri/src/mods/streaming/session_api.rs` 的 `input_configs` 已改回直接返回上游原始配置 payload，不再额外包装 `{ titleId, config }`，从而保持 `DataStreamingTitleInputConfig.config` 与迁移前一致；同时补充单测锁定该行为。验证通过：`cargo test -p xbxrc --lib session_api::tests`、`cargo check -p xbxrc`。 |
| P-332 | 接口统一回归收尾：清理 streaming 适配层残留并补单测 | Done | 2026-03-10 | 2026-03-10 | 清理 `src-tauri/src/mods/streaming/{session_api,signaling_api}.rs` 中迁移后残留的未使用导入/字段/本地 helper；新增 `crates/xbox-webapi/src/transport.rs` 的 `RetryMode` 单测、`src-tauri/src/mods/streaming/error_map.rs` 的错误映射单测，以及 `src-tauri/src/mods/streaming/signaling_api.rs` 的 ICE 归一化单测。验证通过：`cargo test -p xbox-webapi transport::tests --lib`、`cargo test -p xbxrc --lib error_map::tests`、`cargo test -p xbxrc --lib signaling_api::tests`、`cargo check -p xbxrc`。 |
| P-331 | 接口统一收口后复审：确认主风险已修复并整理剩余优化项 | Done | 2026-03-10 | 2026-03-10 | 复审当前本地改动：`HttpTransport` 已引入 `RetryMode` 并把默认重试限制为安全请求；streaming adapter 已通过 `error_map` 恢复 `status/body` 透传；ICE 响应已重新走 `StreamingIceNormalizer`。未再发现新的高严重度回归。剩余优化主要是收敛未使用导入/字段、移除 `session_api.rs` 中无效的 `_device_info/create_device_info` 本地残留、以及确认 `getStreamingTitleInputConfig` 返回结构是否需要继续保持旧契约。验证：`cargo check -p xbxrc` 通过，仍有少量 streaming 相关 warning。 |
| P-330 | 将接口统一回归风险沉淀为 RFC，指导后续 streaming/transport 收口改造 | Done | 2026-03-10 | 2026-03-10 | 新增 `rfc/interface-unification-regression-remediation.md`，收敛 3 个核心风险：1) streaming adapter 丢失 `status/body` 错误元数据；2) ICE candidate 归一化被绕过；3) `HttpTransport` 对非幂等 `POST/DELETE` 自动重试。RFC 已给出分阶段改造方案、边界、测试计划与验收标准，供后续施工直接执行。 |
| P-329 | 接口迁移回归审查：聚焦 streaming/error 语义与传输层行为回归 | Done | 2026-03-10 | 2026-03-10 | 评审提交 `33208e9 perf: 接口统一`，发现 3 个需优先处理的问题：1) `src-tauri/src/mods/streaming/session_api.rs` / `signaling_api.rs` 在迁移到 `xbox-webapi` 后把 `WebApiError` 统一降级为仅字符串，导致 `404/400 + body` 分支失效；2) `src-tauri/src/mods/streaming/signaling_api.rs` 的 ICE 返回路径未再执行 `StreamingIceNormalizer`，IPv6 优先与 `a=end-of-candidates` 归一化回归；3) `crates/xbox-webapi/src/transport.rs` 对全部 `POST/DELETE` 自动重试，存在重复建会话/重复信令风险。验证：`cargo check -p xbxrc` 通过，存在若干未使用导入/字段 warning。 |
| P-328 | 修复 streaming session 接口 `builder error`：`createSession/getStreamingTitleInputConfig` 失败 | Done | 2026-03-09 | 2026-03-09 | `src-tauri/src/mods/streaming/api_provider.rs` 改为同时解析 `gsToken + baseUri`，并把 `baseUri` 归一化为绝对 `base_url` 传入下游；`crates/xbox-webapi/src/session_api.rs` 与 `crates/xbox-webapi/src/signaling_api.rs` 全量改为绝对 URL（基于 region `baseUri`）并补齐 `Authorization: Bearer {gsToken}` 与 `Content-Type` 头，避免 `reqwest` 因相对路径报 `builder error`，同时防止后续 SDP/ICE 接口同类失败。回归：`cargo fmt -p xbox-webapi -p xbxrc`、`cargo check -p xbxrc` 通过。 |
| P-327 | 网络与认证稳定性收口：请求超时/重试、静默刷新失败分级、重复 checkAuthentication 去重 | Done | 2026-03-09 | 2026-03-09 | 解决 1-4 点：1) `crates/xbox-webapi/src/transport.rs` 将默认请求超时从 30s 调整为 12s，并新增最多 3 次可重试请求（仅对可重试网络/HTTP 错误，指数退避）；2) `crates/xbox-webapi/Cargo.toml` 增加 `tokio` time 依赖用于退避 sleep；3) `src-tauri/src/mods/auth/service.rs` 的 `check_authentication_impl` 增加并发去重与 15s 冷却窗口，静默刷新失败改为“可重试网络错误保留会话、非可重试错误清空重登”；4) `src-tauri/src/shell/mod.rs` 与 `src/router/index.ts` 减少重复触发 `checkAuthentication`，避免登录后短时间多次拉起 silent flow。回归：`cargo fmt -p xbox-webapi -p xbxrc`、`cargo check -p xbxrc` 通过；`pnpm build` 被仓库既有 `src/player/*` TS2206 错误阻断（非本次改动引入）。 |
| P-326 | 登录后数据可用性修复：`getUserProfile` 空数据与 `getHosts` 高耗时超时降级 | Done | 2026-03-09 | 2026-03-09 | 修复三处关键问题：1) `src-tauri/src/mods/data/client.rs` 的 `get_current_user_profile` 返回原始 profile 响应，不再提前下钻到 `profileUsers[0]`；2) `src-tauri/src/mods/data/application/services/profile_service.rs` 增加双结构兼容解析（`profileUsers[0].settings` 与 `settings` 直出），并在解析结果为空时拒绝覆盖本地缓存，避免把已登录用户资料写空；3) `src-tauri/src/mods/data/application/data_service.rs` 为 profile 刷新增加 6s 超时降级，`src-tauri/src/mods/data/application/services/host_service.rs` 为 hosts 查询增加 8s 超时降级，网络抖动时快速回退缓存/空列表，不再长时间阻塞。回归通过：`cargo fmt -p xbxrc`、`cargo check -p xbxrc`。 |
