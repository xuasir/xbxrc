[package]
name = "ohmygamepad-core"
version = "0.1.0"

edition.workspace = true

rust-version.workspace = true

license.workspace = true

authors.workspace = true

publish = false

[dependencies]
ohmygamepad-protocol = { path = "../protocol" }

[lib]
path = "src/lib.rs"
